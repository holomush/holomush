# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 HoloMUSH Contributors

name: core-aliases
version: 1.0.0
type: lua
load_priority: -1000
lua-plugin:
  entry: main.lua
commands:
  - name: alias
    capabilities:
      - action: write
        resource: alias
    help: "Add a personal alias"
    usage: "alias <name>=<command>"
    helpText: |
      ## Alias

      Create or update a personal command alias.

      ### Usage

      - `alias <name>=<command>` - Create an alias

      ### Examples

      - `alias l=look` - Create shortcut for look
      - `alias n=north` - Create shortcut for north
      - `alias aa=attack all` - Create alias with arguments

      ### Notes

      - Personal aliases take precedence over system aliases
      - Warnings are shown if your alias shadows an existing command or system alias
      - Circular alias chains are automatically rejected

      ### Permissions

      Requires write access to the alias resource.

  - name: unalias
    capabilities:
      - action: write
        resource: alias
    help: "Remove a personal alias"
    usage: "unalias <name>"
    helpText: |
      ## Unalias

      Remove a personal command alias.

      ### Usage

      - `unalias <name>` - Remove the named alias

      ### Examples

      - `unalias l` - Remove the 'l' alias

      ### Permissions

      Requires write access to the alias resource.

  - name: aliases
    capabilities:
      - action: read
        resource: alias
    help: "List your personal aliases"
    usage: "aliases"
    helpText: |
      ## Aliases

      Display all your personal command aliases.

      ### Usage

      - `aliases` - List all your aliases

      ### Permissions

      Requires read access to the alias resource.

  - name: sysalias
    capabilities:
      - action: write
        resource: alias
        scope: global
    help: "Add a system alias"
    usage: "sysalias <name>=<command>"
    helpText: |
      ## Sysalias

      Create or update a system-wide command alias.

      ### Usage

      - `sysalias <name>=<command>` - Create a system alias

      ### Examples

      - `sysalias l=look` - Create system shortcut for look
      - `sysalias n=north` - Create system shortcut for north

      ### Notes

      - System aliases apply to all players
      - Cannot shadow an existing system alias (remove it first with sysunsalias)
      - Warnings are shown if the alias shadows an existing command
      - Circular alias chains are automatically rejected

      ### Permissions

      Requires write access to the alias resource at global scope.

  - name: sysunsalias
    capabilities:
      - action: write
        resource: alias
        scope: global
    help: "Remove a system alias"
    usage: "sysunsalias <name>"
    helpText: |
      ## Sysunsalias

      Remove a system-wide command alias.

      ### Usage

      - `sysunsalias <name>` - Remove the named system alias

      ### Examples

      - `sysunsalias l` - Remove the system 'l' alias

      ### Permissions

      Requires write access to the alias resource at global scope.

  - name: sysaliases
    capabilities:
      - action: read
        resource: alias
        scope: global
    help: "List all system aliases"
    usage: "sysaliases"
    helpText: |-
      ## Sysaliases

      Display all system-wide command aliases.

      ### Usage

      - `sysaliases` - List all system aliases

      ### Permissions

      Requires read access to the alias resource at global scope.

policies:
  - name: execute-aliases-player
    dsl: >-
      permit(principal is character, action in ["execute"], resource is command) when { resource.command.name in ["alias",
      "unalias", "aliases"] };
  - name: execute-aliases-admin
    dsl: >-
      permit(principal is character, action in ["execute"], resource is command) when { "admin" in principal.character.roles
      && resource.command.name in ["sysalias", "sysunsalias", "sysaliases"] };
