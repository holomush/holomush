// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 HoloMUSH Contributors

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: holomush/plugin/host/v1/session.proto

package hostv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/holomush/holomush/pkg/proto/holomush/plugin/host/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SessionServiceName is the fully-qualified name of the SessionService service.
	SessionServiceName = "holomush.plugin.host.v1.SessionService"
	// SessionAdminServiceName is the fully-qualified name of the SessionAdminService service.
	SessionAdminServiceName = "holomush.plugin.host.v1.SessionAdminService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SessionServiceFindByNameProcedure is the fully-qualified name of the SessionService's FindByName
	// RPC.
	SessionServiceFindByNameProcedure = "/holomush.plugin.host.v1.SessionService/FindByName"
	// SessionServiceListActiveProcedure is the fully-qualified name of the SessionService's ListActive
	// RPC.
	SessionServiceListActiveProcedure = "/holomush.plugin.host.v1.SessionService/ListActive"
	// SessionServiceSetLastWhisperedProcedure is the fully-qualified name of the SessionService's
	// SetLastWhispered RPC.
	SessionServiceSetLastWhisperedProcedure = "/holomush.plugin.host.v1.SessionService/SetLastWhispered"
	// SessionAdminServiceBroadcastProcedure is the fully-qualified name of the SessionAdminService's
	// Broadcast RPC.
	SessionAdminServiceBroadcastProcedure = "/holomush.plugin.host.v1.SessionAdminService/Broadcast"
	// SessionAdminServiceDisconnectProcedure is the fully-qualified name of the SessionAdminService's
	// Disconnect RPC.
	SessionAdminServiceDisconnectProcedure = "/holomush.plugin.host.v1.SessionAdminService/Disconnect"
)

// SessionServiceClient is a client for the holomush.plugin.host.v1.SessionService service.
type SessionServiceClient interface {
	// FindByName returns the active session for a character looked up by name
	// (case-insensitive), mirroring the Lua session.find_by_name(name) host
	// function (SessionAccess.FindSessionByName). The response session is absent
	// when no active session matches.
	FindByName(context.Context, *connect.Request[v1.FindByNameRequest]) (*connect.Response[v1.FindByNameResponse], error)
	// ListActive returns every currently active session, mirroring the Lua
	// session.list_active() host function (SessionAccess.ListActiveSessions).
	ListActive(context.Context, *connect.Request[v1.ListActiveRequest]) (*connect.Response[v1.ListActiveResponse], error)
	// SetLastWhispered records the last whisper target on a session, mirroring the
	// Lua session.set_last_whispered(session_id, name) host function
	// (SessionAccess.SetLastWhispered).
	SetLastWhispered(context.Context, *connect.Request[v1.SetLastWhisperedRequest]) (*connect.Response[v1.SetLastWhisperedResponse], error)
}

// NewSessionServiceClient constructs a client for the holomush.plugin.host.v1.SessionService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSessionServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SessionServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	sessionServiceMethods := v1.File_holomush_plugin_host_v1_session_proto.Services().ByName("SessionService").Methods()
	return &sessionServiceClient{
		findByName: connect.NewClient[v1.FindByNameRequest, v1.FindByNameResponse](
			httpClient,
			baseURL+SessionServiceFindByNameProcedure,
			connect.WithSchema(sessionServiceMethods.ByName("FindByName")),
			connect.WithClientOptions(opts...),
		),
		listActive: connect.NewClient[v1.ListActiveRequest, v1.ListActiveResponse](
			httpClient,
			baseURL+SessionServiceListActiveProcedure,
			connect.WithSchema(sessionServiceMethods.ByName("ListActive")),
			connect.WithClientOptions(opts...),
		),
		setLastWhispered: connect.NewClient[v1.SetLastWhisperedRequest, v1.SetLastWhisperedResponse](
			httpClient,
			baseURL+SessionServiceSetLastWhisperedProcedure,
			connect.WithSchema(sessionServiceMethods.ByName("SetLastWhispered")),
			connect.WithClientOptions(opts...),
		),
	}
}

// sessionServiceClient implements SessionServiceClient.
type sessionServiceClient struct {
	findByName       *connect.Client[v1.FindByNameRequest, v1.FindByNameResponse]
	listActive       *connect.Client[v1.ListActiveRequest, v1.ListActiveResponse]
	setLastWhispered *connect.Client[v1.SetLastWhisperedRequest, v1.SetLastWhisperedResponse]
}

// FindByName calls holomush.plugin.host.v1.SessionService.FindByName.
func (c *sessionServiceClient) FindByName(ctx context.Context, req *connect.Request[v1.FindByNameRequest]) (*connect.Response[v1.FindByNameResponse], error) {
	return c.findByName.CallUnary(ctx, req)
}

// ListActive calls holomush.plugin.host.v1.SessionService.ListActive.
func (c *sessionServiceClient) ListActive(ctx context.Context, req *connect.Request[v1.ListActiveRequest]) (*connect.Response[v1.ListActiveResponse], error) {
	return c.listActive.CallUnary(ctx, req)
}

// SetLastWhispered calls holomush.plugin.host.v1.SessionService.SetLastWhispered.
func (c *sessionServiceClient) SetLastWhispered(ctx context.Context, req *connect.Request[v1.SetLastWhisperedRequest]) (*connect.Response[v1.SetLastWhisperedResponse], error) {
	return c.setLastWhispered.CallUnary(ctx, req)
}

// SessionServiceHandler is an implementation of the holomush.plugin.host.v1.SessionService service.
type SessionServiceHandler interface {
	// FindByName returns the active session for a character looked up by name
	// (case-insensitive), mirroring the Lua session.find_by_name(name) host
	// function (SessionAccess.FindSessionByName). The response session is absent
	// when no active session matches.
	FindByName(context.Context, *connect.Request[v1.FindByNameRequest]) (*connect.Response[v1.FindByNameResponse], error)
	// ListActive returns every currently active session, mirroring the Lua
	// session.list_active() host function (SessionAccess.ListActiveSessions).
	ListActive(context.Context, *connect.Request[v1.ListActiveRequest]) (*connect.Response[v1.ListActiveResponse], error)
	// SetLastWhispered records the last whisper target on a session, mirroring the
	// Lua session.set_last_whispered(session_id, name) host function
	// (SessionAccess.SetLastWhispered).
	SetLastWhispered(context.Context, *connect.Request[v1.SetLastWhisperedRequest]) (*connect.Response[v1.SetLastWhisperedResponse], error)
}

// NewSessionServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSessionServiceHandler(svc SessionServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	sessionServiceMethods := v1.File_holomush_plugin_host_v1_session_proto.Services().ByName("SessionService").Methods()
	sessionServiceFindByNameHandler := connect.NewUnaryHandler(
		SessionServiceFindByNameProcedure,
		svc.FindByName,
		connect.WithSchema(sessionServiceMethods.ByName("FindByName")),
		connect.WithHandlerOptions(opts...),
	)
	sessionServiceListActiveHandler := connect.NewUnaryHandler(
		SessionServiceListActiveProcedure,
		svc.ListActive,
		connect.WithSchema(sessionServiceMethods.ByName("ListActive")),
		connect.WithHandlerOptions(opts...),
	)
	sessionServiceSetLastWhisperedHandler := connect.NewUnaryHandler(
		SessionServiceSetLastWhisperedProcedure,
		svc.SetLastWhispered,
		connect.WithSchema(sessionServiceMethods.ByName("SetLastWhispered")),
		connect.WithHandlerOptions(opts...),
	)
	return "/holomush.plugin.host.v1.SessionService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SessionServiceFindByNameProcedure:
			sessionServiceFindByNameHandler.ServeHTTP(w, r)
		case SessionServiceListActiveProcedure:
			sessionServiceListActiveHandler.ServeHTTP(w, r)
		case SessionServiceSetLastWhisperedProcedure:
			sessionServiceSetLastWhisperedHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSessionServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSessionServiceHandler struct{}

func (UnimplementedSessionServiceHandler) FindByName(context.Context, *connect.Request[v1.FindByNameRequest]) (*connect.Response[v1.FindByNameResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.plugin.host.v1.SessionService.FindByName is not implemented"))
}

func (UnimplementedSessionServiceHandler) ListActive(context.Context, *connect.Request[v1.ListActiveRequest]) (*connect.Response[v1.ListActiveResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.plugin.host.v1.SessionService.ListActive is not implemented"))
}

func (UnimplementedSessionServiceHandler) SetLastWhispered(context.Context, *connect.Request[v1.SetLastWhisperedRequest]) (*connect.Response[v1.SetLastWhisperedResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.plugin.host.v1.SessionService.SetLastWhispered is not implemented"))
}

// SessionAdminServiceClient is a client for the holomush.plugin.host.v1.SessionAdminService
// service.
type SessionAdminServiceClient interface {
	// Broadcast sends a system message to all active sessions, mirroring the Lua
	// session.broadcast(message) host function
	// (SessionAccess.BroadcastSystemMessage).
	Broadcast(context.Context, *connect.Request[v1.BroadcastRequest]) (*connect.Response[v1.BroadcastResponse], error)
	// Disconnect forcibly disconnects a session with a reason, mirroring the Lua
	// session.disconnect(session_id, reason) host function
	// (SessionAccess.DisconnectSession).
	Disconnect(context.Context, *connect.Request[v1.DisconnectRequest]) (*connect.Response[v1.DisconnectResponse], error)
}

// NewSessionAdminServiceClient constructs a client for the
// holomush.plugin.host.v1.SessionAdminService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSessionAdminServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SessionAdminServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	sessionAdminServiceMethods := v1.File_holomush_plugin_host_v1_session_proto.Services().ByName("SessionAdminService").Methods()
	return &sessionAdminServiceClient{
		broadcast: connect.NewClient[v1.BroadcastRequest, v1.BroadcastResponse](
			httpClient,
			baseURL+SessionAdminServiceBroadcastProcedure,
			connect.WithSchema(sessionAdminServiceMethods.ByName("Broadcast")),
			connect.WithClientOptions(opts...),
		),
		disconnect: connect.NewClient[v1.DisconnectRequest, v1.DisconnectResponse](
			httpClient,
			baseURL+SessionAdminServiceDisconnectProcedure,
			connect.WithSchema(sessionAdminServiceMethods.ByName("Disconnect")),
			connect.WithClientOptions(opts...),
		),
	}
}

// sessionAdminServiceClient implements SessionAdminServiceClient.
type sessionAdminServiceClient struct {
	broadcast  *connect.Client[v1.BroadcastRequest, v1.BroadcastResponse]
	disconnect *connect.Client[v1.DisconnectRequest, v1.DisconnectResponse]
}

// Broadcast calls holomush.plugin.host.v1.SessionAdminService.Broadcast.
func (c *sessionAdminServiceClient) Broadcast(ctx context.Context, req *connect.Request[v1.BroadcastRequest]) (*connect.Response[v1.BroadcastResponse], error) {
	return c.broadcast.CallUnary(ctx, req)
}

// Disconnect calls holomush.plugin.host.v1.SessionAdminService.Disconnect.
func (c *sessionAdminServiceClient) Disconnect(ctx context.Context, req *connect.Request[v1.DisconnectRequest]) (*connect.Response[v1.DisconnectResponse], error) {
	return c.disconnect.CallUnary(ctx, req)
}

// SessionAdminServiceHandler is an implementation of the
// holomush.plugin.host.v1.SessionAdminService service.
type SessionAdminServiceHandler interface {
	// Broadcast sends a system message to all active sessions, mirroring the Lua
	// session.broadcast(message) host function
	// (SessionAccess.BroadcastSystemMessage).
	Broadcast(context.Context, *connect.Request[v1.BroadcastRequest]) (*connect.Response[v1.BroadcastResponse], error)
	// Disconnect forcibly disconnects a session with a reason, mirroring the Lua
	// session.disconnect(session_id, reason) host function
	// (SessionAccess.DisconnectSession).
	Disconnect(context.Context, *connect.Request[v1.DisconnectRequest]) (*connect.Response[v1.DisconnectResponse], error)
}

// NewSessionAdminServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSessionAdminServiceHandler(svc SessionAdminServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	sessionAdminServiceMethods := v1.File_holomush_plugin_host_v1_session_proto.Services().ByName("SessionAdminService").Methods()
	sessionAdminServiceBroadcastHandler := connect.NewUnaryHandler(
		SessionAdminServiceBroadcastProcedure,
		svc.Broadcast,
		connect.WithSchema(sessionAdminServiceMethods.ByName("Broadcast")),
		connect.WithHandlerOptions(opts...),
	)
	sessionAdminServiceDisconnectHandler := connect.NewUnaryHandler(
		SessionAdminServiceDisconnectProcedure,
		svc.Disconnect,
		connect.WithSchema(sessionAdminServiceMethods.ByName("Disconnect")),
		connect.WithHandlerOptions(opts...),
	)
	return "/holomush.plugin.host.v1.SessionAdminService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SessionAdminServiceBroadcastProcedure:
			sessionAdminServiceBroadcastHandler.ServeHTTP(w, r)
		case SessionAdminServiceDisconnectProcedure:
			sessionAdminServiceDisconnectHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSessionAdminServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSessionAdminServiceHandler struct{}

func (UnimplementedSessionAdminServiceHandler) Broadcast(context.Context, *connect.Request[v1.BroadcastRequest]) (*connect.Response[v1.BroadcastResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.plugin.host.v1.SessionAdminService.Broadcast is not implemented"))
}

func (UnimplementedSessionAdminServiceHandler) Disconnect(context.Context, *connect.Request[v1.DisconnectRequest]) (*connect.Response[v1.DisconnectResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.plugin.host.v1.SessionAdminService.Disconnect is not implemented"))
}
