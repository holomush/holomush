// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 HoloMUSH Contributors

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: holomush/plugin/host/v1/eval.proto

package hostv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/holomush/holomush/pkg/proto/holomush/plugin/host/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// EvalServiceName is the fully-qualified name of the EvalService service.
	EvalServiceName = "holomush.plugin.host.v1.EvalService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// EvalServiceEvaluateProcedure is the fully-qualified name of the EvalService's Evaluate RPC.
	EvalServiceEvaluateProcedure = "/holomush.plugin.host.v1.EvalService/Evaluate"
)

// EvalServiceClient is a client for the holomush.plugin.host.v1.EvalService service.
type EvalServiceClient interface {
	// Evaluate runs the host ABAC engine for one action against one resource
	// instance owned by the calling plugin. The subject is derived host-side from
	// the dispatch token exactly as EmitEvent does (token→actor recovery) — there
	// is no subject field on the wire (spec §2, INV-PLUGIN-22). Fails closed on
	// nil engine, missing/rejected token, empty actor subject, or a resource type
	// the plugin does not own.
	Evaluate(context.Context, *connect.Request[v1.EvaluateRequest]) (*connect.Response[v1.EvaluateResponse], error)
}

// NewEvalServiceClient constructs a client for the holomush.plugin.host.v1.EvalService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEvalServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) EvalServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	evalServiceMethods := v1.File_holomush_plugin_host_v1_eval_proto.Services().ByName("EvalService").Methods()
	return &evalServiceClient{
		evaluate: connect.NewClient[v1.EvaluateRequest, v1.EvaluateResponse](
			httpClient,
			baseURL+EvalServiceEvaluateProcedure,
			connect.WithSchema(evalServiceMethods.ByName("Evaluate")),
			connect.WithClientOptions(opts...),
		),
	}
}

// evalServiceClient implements EvalServiceClient.
type evalServiceClient struct {
	evaluate *connect.Client[v1.EvaluateRequest, v1.EvaluateResponse]
}

// Evaluate calls holomush.plugin.host.v1.EvalService.Evaluate.
func (c *evalServiceClient) Evaluate(ctx context.Context, req *connect.Request[v1.EvaluateRequest]) (*connect.Response[v1.EvaluateResponse], error) {
	return c.evaluate.CallUnary(ctx, req)
}

// EvalServiceHandler is an implementation of the holomush.plugin.host.v1.EvalService service.
type EvalServiceHandler interface {
	// Evaluate runs the host ABAC engine for one action against one resource
	// instance owned by the calling plugin. The subject is derived host-side from
	// the dispatch token exactly as EmitEvent does (token→actor recovery) — there
	// is no subject field on the wire (spec §2, INV-PLUGIN-22). Fails closed on
	// nil engine, missing/rejected token, empty actor subject, or a resource type
	// the plugin does not own.
	Evaluate(context.Context, *connect.Request[v1.EvaluateRequest]) (*connect.Response[v1.EvaluateResponse], error)
}

// NewEvalServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEvalServiceHandler(svc EvalServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	evalServiceMethods := v1.File_holomush_plugin_host_v1_eval_proto.Services().ByName("EvalService").Methods()
	evalServiceEvaluateHandler := connect.NewUnaryHandler(
		EvalServiceEvaluateProcedure,
		svc.Evaluate,
		connect.WithSchema(evalServiceMethods.ByName("Evaluate")),
		connect.WithHandlerOptions(opts...),
	)
	return "/holomush.plugin.host.v1.EvalService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EvalServiceEvaluateProcedure:
			evalServiceEvaluateHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEvalServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedEvalServiceHandler struct{}

func (UnimplementedEvalServiceHandler) Evaluate(context.Context, *connect.Request[v1.EvaluateRequest]) (*connect.Response[v1.EvaluateResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.plugin.host.v1.EvalService.Evaluate is not implemented"))
}
