// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 HoloMUSH Contributors

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: holomush/plugin/host/v1/emit.proto

package hostv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/holomush/holomush/pkg/proto/holomush/plugin/host/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// EmitServiceName is the fully-qualified name of the EmitService service.
	EmitServiceName = "holomush.plugin.host.v1.EmitService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// EmitServiceEmitEventProcedure is the fully-qualified name of the EmitService's EmitEvent RPC.
	EmitServiceEmitEventProcedure = "/holomush.plugin.host.v1.EmitService/EmitEvent"
	// EmitServiceRequestEmitTokenProcedure is the fully-qualified name of the EmitService's
	// RequestEmitToken RPC.
	EmitServiceRequestEmitTokenProcedure = "/holomush.plugin.host.v1.EmitService/RequestEmitToken"
	// EmitServiceRegisterEmitTypeProcedure is the fully-qualified name of the EmitService's
	// RegisterEmitType RPC.
	EmitServiceRegisterEmitTypeProcedure = "/holomush.plugin.host.v1.EmitService/RegisterEmitType"
)

// EmitServiceClient is a client for the holomush.plugin.host.v1.EmitService service.
type EmitServiceClient interface {
	// EmitEvent publishes one plugin-originated event onto the bus through the
	// host emit fence. The caller's identity is NOT trusted from the wire — the
	// plugin presents a host-issued dispatch token in the x-holomush-emit-token
	// metadata header, the host recovers the vouched-for actor from
	// tokenStore.Lookup(pluginName, token), and a missing/foreign token is
	// rejected (EMIT_TOKEN_MISSING / EMIT_TOKEN_REJECTED). The recovered actor
	// then flows through PluginEventEmitter.Emit, which enforces the manifest
	// gates: emits (subject namespace must be declared), actor_kinds_claimable
	// (actor kind must be listed — EMIT_ACTOR_KIND_NOT_CLAIMABLE), and the
	// crypto.emits sensitivity fence. These gates fire identically for Lua and
	// binary plugins (plugin runtime symmetry); the token mechanism is the
	// binary-side forgery fence.
	EmitEvent(context.Context, *connect.Request[v1.EmitEventRequest]) (*connect.Response[v1.EmitEventResponse], error)
	// RequestEmitToken issues a self-token bound to {ActorPlugin, pluginName} so
	// a plugin-served gRPC handler (e.g. SceneService.CreateScene) — which is NOT
	// reached via DeliverEvent/DeliverCommand and so holds no dispatch token —
	// can still call EmitEvent. The plugin's identity is taken from the
	// mTLS-bound server struct (s.pluginName); the request carries no identity
	// fields, so a plugin cannot impersonate another actor or escalate to a
	// character actor through this RPC. The actor_kinds_claimable manifest gate
	// still fires when the issued token is later spent at EmitEvent.
	// (Spec §3.3.5 / §5.4 two-token pattern.)
	RequestEmitToken(context.Context, *connect.Request[v1.RequestEmitTokenRequest]) (*connect.Response[v1.RequestEmitTokenResponse], error)
	// RegisterEmitType records one plugin-owned event type the plugin may later
	// emit, validated by the host against the manifest's crypto.emits per
	// INV-PLUGIN-32. Promotes the Lua holomush.register_emit_type(type) host
	// function (internal/plugin/hostfunc/stdlib_emit_registry.go) to the binary
	// surface so both runtimes declare their emit-type set through one channel.
	RegisterEmitType(context.Context, *connect.Request[v1.RegisterEmitTypeRequest]) (*connect.Response[v1.RegisterEmitTypeResponse], error)
}

// NewEmitServiceClient constructs a client for the holomush.plugin.host.v1.EmitService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEmitServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) EmitServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	emitServiceMethods := v1.File_holomush_plugin_host_v1_emit_proto.Services().ByName("EmitService").Methods()
	return &emitServiceClient{
		emitEvent: connect.NewClient[v1.EmitEventRequest, v1.EmitEventResponse](
			httpClient,
			baseURL+EmitServiceEmitEventProcedure,
			connect.WithSchema(emitServiceMethods.ByName("EmitEvent")),
			connect.WithClientOptions(opts...),
		),
		requestEmitToken: connect.NewClient[v1.RequestEmitTokenRequest, v1.RequestEmitTokenResponse](
			httpClient,
			baseURL+EmitServiceRequestEmitTokenProcedure,
			connect.WithSchema(emitServiceMethods.ByName("RequestEmitToken")),
			connect.WithClientOptions(opts...),
		),
		registerEmitType: connect.NewClient[v1.RegisterEmitTypeRequest, v1.RegisterEmitTypeResponse](
			httpClient,
			baseURL+EmitServiceRegisterEmitTypeProcedure,
			connect.WithSchema(emitServiceMethods.ByName("RegisterEmitType")),
			connect.WithClientOptions(opts...),
		),
	}
}

// emitServiceClient implements EmitServiceClient.
type emitServiceClient struct {
	emitEvent        *connect.Client[v1.EmitEventRequest, v1.EmitEventResponse]
	requestEmitToken *connect.Client[v1.RequestEmitTokenRequest, v1.RequestEmitTokenResponse]
	registerEmitType *connect.Client[v1.RegisterEmitTypeRequest, v1.RegisterEmitTypeResponse]
}

// EmitEvent calls holomush.plugin.host.v1.EmitService.EmitEvent.
func (c *emitServiceClient) EmitEvent(ctx context.Context, req *connect.Request[v1.EmitEventRequest]) (*connect.Response[v1.EmitEventResponse], error) {
	return c.emitEvent.CallUnary(ctx, req)
}

// RequestEmitToken calls holomush.plugin.host.v1.EmitService.RequestEmitToken.
func (c *emitServiceClient) RequestEmitToken(ctx context.Context, req *connect.Request[v1.RequestEmitTokenRequest]) (*connect.Response[v1.RequestEmitTokenResponse], error) {
	return c.requestEmitToken.CallUnary(ctx, req)
}

// RegisterEmitType calls holomush.plugin.host.v1.EmitService.RegisterEmitType.
func (c *emitServiceClient) RegisterEmitType(ctx context.Context, req *connect.Request[v1.RegisterEmitTypeRequest]) (*connect.Response[v1.RegisterEmitTypeResponse], error) {
	return c.registerEmitType.CallUnary(ctx, req)
}

// EmitServiceHandler is an implementation of the holomush.plugin.host.v1.EmitService service.
type EmitServiceHandler interface {
	// EmitEvent publishes one plugin-originated event onto the bus through the
	// host emit fence. The caller's identity is NOT trusted from the wire — the
	// plugin presents a host-issued dispatch token in the x-holomush-emit-token
	// metadata header, the host recovers the vouched-for actor from
	// tokenStore.Lookup(pluginName, token), and a missing/foreign token is
	// rejected (EMIT_TOKEN_MISSING / EMIT_TOKEN_REJECTED). The recovered actor
	// then flows through PluginEventEmitter.Emit, which enforces the manifest
	// gates: emits (subject namespace must be declared), actor_kinds_claimable
	// (actor kind must be listed — EMIT_ACTOR_KIND_NOT_CLAIMABLE), and the
	// crypto.emits sensitivity fence. These gates fire identically for Lua and
	// binary plugins (plugin runtime symmetry); the token mechanism is the
	// binary-side forgery fence.
	EmitEvent(context.Context, *connect.Request[v1.EmitEventRequest]) (*connect.Response[v1.EmitEventResponse], error)
	// RequestEmitToken issues a self-token bound to {ActorPlugin, pluginName} so
	// a plugin-served gRPC handler (e.g. SceneService.CreateScene) — which is NOT
	// reached via DeliverEvent/DeliverCommand and so holds no dispatch token —
	// can still call EmitEvent. The plugin's identity is taken from the
	// mTLS-bound server struct (s.pluginName); the request carries no identity
	// fields, so a plugin cannot impersonate another actor or escalate to a
	// character actor through this RPC. The actor_kinds_claimable manifest gate
	// still fires when the issued token is later spent at EmitEvent.
	// (Spec §3.3.5 / §5.4 two-token pattern.)
	RequestEmitToken(context.Context, *connect.Request[v1.RequestEmitTokenRequest]) (*connect.Response[v1.RequestEmitTokenResponse], error)
	// RegisterEmitType records one plugin-owned event type the plugin may later
	// emit, validated by the host against the manifest's crypto.emits per
	// INV-PLUGIN-32. Promotes the Lua holomush.register_emit_type(type) host
	// function (internal/plugin/hostfunc/stdlib_emit_registry.go) to the binary
	// surface so both runtimes declare their emit-type set through one channel.
	RegisterEmitType(context.Context, *connect.Request[v1.RegisterEmitTypeRequest]) (*connect.Response[v1.RegisterEmitTypeResponse], error)
}

// NewEmitServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEmitServiceHandler(svc EmitServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	emitServiceMethods := v1.File_holomush_plugin_host_v1_emit_proto.Services().ByName("EmitService").Methods()
	emitServiceEmitEventHandler := connect.NewUnaryHandler(
		EmitServiceEmitEventProcedure,
		svc.EmitEvent,
		connect.WithSchema(emitServiceMethods.ByName("EmitEvent")),
		connect.WithHandlerOptions(opts...),
	)
	emitServiceRequestEmitTokenHandler := connect.NewUnaryHandler(
		EmitServiceRequestEmitTokenProcedure,
		svc.RequestEmitToken,
		connect.WithSchema(emitServiceMethods.ByName("RequestEmitToken")),
		connect.WithHandlerOptions(opts...),
	)
	emitServiceRegisterEmitTypeHandler := connect.NewUnaryHandler(
		EmitServiceRegisterEmitTypeProcedure,
		svc.RegisterEmitType,
		connect.WithSchema(emitServiceMethods.ByName("RegisterEmitType")),
		connect.WithHandlerOptions(opts...),
	)
	return "/holomush.plugin.host.v1.EmitService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EmitServiceEmitEventProcedure:
			emitServiceEmitEventHandler.ServeHTTP(w, r)
		case EmitServiceRequestEmitTokenProcedure:
			emitServiceRequestEmitTokenHandler.ServeHTTP(w, r)
		case EmitServiceRegisterEmitTypeProcedure:
			emitServiceRegisterEmitTypeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEmitServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedEmitServiceHandler struct{}

func (UnimplementedEmitServiceHandler) EmitEvent(context.Context, *connect.Request[v1.EmitEventRequest]) (*connect.Response[v1.EmitEventResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.plugin.host.v1.EmitService.EmitEvent is not implemented"))
}

func (UnimplementedEmitServiceHandler) RequestEmitToken(context.Context, *connect.Request[v1.RequestEmitTokenRequest]) (*connect.Response[v1.RequestEmitTokenResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.plugin.host.v1.EmitService.RequestEmitToken is not implemented"))
}

func (UnimplementedEmitServiceHandler) RegisterEmitType(context.Context, *connect.Request[v1.RegisterEmitTypeRequest]) (*connect.Response[v1.RegisterEmitTypeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.plugin.host.v1.EmitService.RegisterEmitType is not implemented"))
}
