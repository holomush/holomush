// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 HoloMUSH Contributors

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: holomush/plugin/host/v1/emit.proto

package hostv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	EmitService_EmitEvent_FullMethodName        = "/holomush.plugin.host.v1.EmitService/EmitEvent"
	EmitService_RequestEmitToken_FullMethodName = "/holomush.plugin.host.v1.EmitService/RequestEmitToken"
	EmitService_RegisterEmitType_FullMethodName = "/holomush.plugin.host.v1.EmitService/RegisterEmitType"
)

// EmitServiceClient is the client API for EmitService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// EmitService is the host-brokered `emit` capability: a plugin publishes
// events through the host emit fence, requests the self-token a plugin-served
// gRPC handler needs to do so, and registers the emit-type set the host
// validates against the manifest. Carved from the former PluginHostService
// (holomush-eykuh.1). Served by emitServer in
// internal/plugin/goplugin/host_capability_servers.go.
type EmitServiceClient interface {
	// EmitEvent publishes one plugin-originated event onto the bus through the
	// host emit fence. The caller's identity is NOT trusted from the wire — the
	// plugin presents a host-issued dispatch token in the x-holomush-emit-token
	// metadata header, the host recovers the vouched-for actor from
	// tokenStore.Lookup(pluginName, token), and a missing/foreign token is
	// rejected (EMIT_TOKEN_MISSING / EMIT_TOKEN_REJECTED). The recovered actor
	// then flows through PluginEventEmitter.Emit, which enforces the manifest
	// gates: emits (subject namespace must be declared), actor_kinds_claimable
	// (actor kind must be listed — EMIT_ACTOR_KIND_NOT_CLAIMABLE), and the
	// crypto.emits sensitivity fence. These gates fire identically for Lua and
	// binary plugins (plugin runtime symmetry); the token mechanism is the
	// binary-side forgery fence.
	EmitEvent(ctx context.Context, in *EmitEventRequest, opts ...grpc.CallOption) (*EmitEventResponse, error)
	// RequestEmitToken issues a self-token bound to {ActorPlugin, pluginName} so
	// a plugin-served gRPC handler (e.g. SceneService.CreateScene) — which is NOT
	// reached via DeliverEvent/DeliverCommand and so holds no dispatch token —
	// can still call EmitEvent. The plugin's identity is taken from the
	// mTLS-bound server struct (s.pluginName); the request carries no identity
	// fields, so a plugin cannot impersonate another actor or escalate to a
	// character actor through this RPC. The actor_kinds_claimable manifest gate
	// still fires when the issued token is later spent at EmitEvent.
	// (Spec §3.3.5 / §5.4 two-token pattern.)
	RequestEmitToken(ctx context.Context, in *RequestEmitTokenRequest, opts ...grpc.CallOption) (*RequestEmitTokenResponse, error)
	// RegisterEmitType records one plugin-owned event type the plugin may later
	// emit, validated by the host against the manifest's crypto.emits per
	// INV-PLUGIN-32. Promotes the Lua holomush.register_emit_type(type) host
	// function (internal/plugin/hostfunc/stdlib_emit_registry.go) to the binary
	// surface so both runtimes declare their emit-type set through one channel.
	RegisterEmitType(ctx context.Context, in *RegisterEmitTypeRequest, opts ...grpc.CallOption) (*RegisterEmitTypeResponse, error)
}

type emitServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewEmitServiceClient(cc grpc.ClientConnInterface) EmitServiceClient {
	return &emitServiceClient{cc}
}

func (c *emitServiceClient) EmitEvent(ctx context.Context, in *EmitEventRequest, opts ...grpc.CallOption) (*EmitEventResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(EmitEventResponse)
	err := c.cc.Invoke(ctx, EmitService_EmitEvent_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *emitServiceClient) RequestEmitToken(ctx context.Context, in *RequestEmitTokenRequest, opts ...grpc.CallOption) (*RequestEmitTokenResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RequestEmitTokenResponse)
	err := c.cc.Invoke(ctx, EmitService_RequestEmitToken_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *emitServiceClient) RegisterEmitType(ctx context.Context, in *RegisterEmitTypeRequest, opts ...grpc.CallOption) (*RegisterEmitTypeResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RegisterEmitTypeResponse)
	err := c.cc.Invoke(ctx, EmitService_RegisterEmitType_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// EmitServiceServer is the server API for EmitService service.
// All implementations must embed UnimplementedEmitServiceServer
// for forward compatibility.
//
// EmitService is the host-brokered `emit` capability: a plugin publishes
// events through the host emit fence, requests the self-token a plugin-served
// gRPC handler needs to do so, and registers the emit-type set the host
// validates against the manifest. Carved from the former PluginHostService
// (holomush-eykuh.1). Served by emitServer in
// internal/plugin/goplugin/host_capability_servers.go.
type EmitServiceServer interface {
	// EmitEvent publishes one plugin-originated event onto the bus through the
	// host emit fence. The caller's identity is NOT trusted from the wire — the
	// plugin presents a host-issued dispatch token in the x-holomush-emit-token
	// metadata header, the host recovers the vouched-for actor from
	// tokenStore.Lookup(pluginName, token), and a missing/foreign token is
	// rejected (EMIT_TOKEN_MISSING / EMIT_TOKEN_REJECTED). The recovered actor
	// then flows through PluginEventEmitter.Emit, which enforces the manifest
	// gates: emits (subject namespace must be declared), actor_kinds_claimable
	// (actor kind must be listed — EMIT_ACTOR_KIND_NOT_CLAIMABLE), and the
	// crypto.emits sensitivity fence. These gates fire identically for Lua and
	// binary plugins (plugin runtime symmetry); the token mechanism is the
	// binary-side forgery fence.
	EmitEvent(context.Context, *EmitEventRequest) (*EmitEventResponse, error)
	// RequestEmitToken issues a self-token bound to {ActorPlugin, pluginName} so
	// a plugin-served gRPC handler (e.g. SceneService.CreateScene) — which is NOT
	// reached via DeliverEvent/DeliverCommand and so holds no dispatch token —
	// can still call EmitEvent. The plugin's identity is taken from the
	// mTLS-bound server struct (s.pluginName); the request carries no identity
	// fields, so a plugin cannot impersonate another actor or escalate to a
	// character actor through this RPC. The actor_kinds_claimable manifest gate
	// still fires when the issued token is later spent at EmitEvent.
	// (Spec §3.3.5 / §5.4 two-token pattern.)
	RequestEmitToken(context.Context, *RequestEmitTokenRequest) (*RequestEmitTokenResponse, error)
	// RegisterEmitType records one plugin-owned event type the plugin may later
	// emit, validated by the host against the manifest's crypto.emits per
	// INV-PLUGIN-32. Promotes the Lua holomush.register_emit_type(type) host
	// function (internal/plugin/hostfunc/stdlib_emit_registry.go) to the binary
	// surface so both runtimes declare their emit-type set through one channel.
	RegisterEmitType(context.Context, *RegisterEmitTypeRequest) (*RegisterEmitTypeResponse, error)
	mustEmbedUnimplementedEmitServiceServer()
}

// UnimplementedEmitServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedEmitServiceServer struct{}

func (UnimplementedEmitServiceServer) EmitEvent(context.Context, *EmitEventRequest) (*EmitEventResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method EmitEvent not implemented")
}
func (UnimplementedEmitServiceServer) RequestEmitToken(context.Context, *RequestEmitTokenRequest) (*RequestEmitTokenResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method RequestEmitToken not implemented")
}
func (UnimplementedEmitServiceServer) RegisterEmitType(context.Context, *RegisterEmitTypeRequest) (*RegisterEmitTypeResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method RegisterEmitType not implemented")
}
func (UnimplementedEmitServiceServer) mustEmbedUnimplementedEmitServiceServer() {}
func (UnimplementedEmitServiceServer) testEmbeddedByValue()                     {}

// UnsafeEmitServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to EmitServiceServer will
// result in compilation errors.
type UnsafeEmitServiceServer interface {
	mustEmbedUnimplementedEmitServiceServer()
}

func RegisterEmitServiceServer(s grpc.ServiceRegistrar, srv EmitServiceServer) {
	// If the following call panics, it indicates UnimplementedEmitServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&EmitService_ServiceDesc, srv)
}

func _EmitService_EmitEvent_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(EmitEventRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EmitServiceServer).EmitEvent(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: EmitService_EmitEvent_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EmitServiceServer).EmitEvent(ctx, req.(*EmitEventRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _EmitService_RequestEmitToken_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RequestEmitTokenRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EmitServiceServer).RequestEmitToken(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: EmitService_RequestEmitToken_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EmitServiceServer).RequestEmitToken(ctx, req.(*RequestEmitTokenRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _EmitService_RegisterEmitType_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RegisterEmitTypeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(EmitServiceServer).RegisterEmitType(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: EmitService_RegisterEmitType_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(EmitServiceServer).RegisterEmitType(ctx, req.(*RegisterEmitTypeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// EmitService_ServiceDesc is the grpc.ServiceDesc for EmitService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var EmitService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "holomush.plugin.host.v1.EmitService",
	HandlerType: (*EmitServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "EmitEvent",
			Handler:    _EmitService_EmitEvent_Handler,
		},
		{
			MethodName: "RequestEmitToken",
			Handler:    _EmitService_RequestEmitToken_Handler,
		},
		{
			MethodName: "RegisterEmitType",
			Handler:    _EmitService_RegisterEmitType_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "holomush/plugin/host/v1/emit.proto",
}
