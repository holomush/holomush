// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 HoloMUSH Contributors

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: holomush/channel/v1/channel.proto

package channelv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/holomush/holomush/pkg/proto/holomush/channel/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ChannelServiceName is the fully-qualified name of the ChannelService service.
	ChannelServiceName = "holomush.channel.v1.ChannelService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ChannelServiceCreateChannelProcedure is the fully-qualified name of the ChannelService's
	// CreateChannel RPC.
	ChannelServiceCreateChannelProcedure = "/holomush.channel.v1.ChannelService/CreateChannel"
	// ChannelServiceJoinChannelProcedure is the fully-qualified name of the ChannelService's
	// JoinChannel RPC.
	ChannelServiceJoinChannelProcedure = "/holomush.channel.v1.ChannelService/JoinChannel"
	// ChannelServiceLeaveChannelProcedure is the fully-qualified name of the ChannelService's
	// LeaveChannel RPC.
	ChannelServiceLeaveChannelProcedure = "/holomush.channel.v1.ChannelService/LeaveChannel"
	// ChannelServiceListChannelsProcedure is the fully-qualified name of the ChannelService's
	// ListChannels RPC.
	ChannelServiceListChannelsProcedure = "/holomush.channel.v1.ChannelService/ListChannels"
	// ChannelServicePostToChannelProcedure is the fully-qualified name of the ChannelService's
	// PostToChannel RPC.
	ChannelServicePostToChannelProcedure = "/holomush.channel.v1.ChannelService/PostToChannel"
	// ChannelServiceWhoInChannelProcedure is the fully-qualified name of the ChannelService's
	// WhoInChannel RPC.
	ChannelServiceWhoInChannelProcedure = "/holomush.channel.v1.ChannelService/WhoInChannel"
	// ChannelServiceQueryChannelHistoryProcedure is the fully-qualified name of the ChannelService's
	// QueryChannelHistory RPC.
	ChannelServiceQueryChannelHistoryProcedure = "/holomush.channel.v1.ChannelService/QueryChannelHistory"
	// ChannelServiceInviteToChannelProcedure is the fully-qualified name of the ChannelService's
	// InviteToChannel RPC.
	ChannelServiceInviteToChannelProcedure = "/holomush.channel.v1.ChannelService/InviteToChannel"
	// ChannelServiceMuteMemberProcedure is the fully-qualified name of the ChannelService's MuteMember
	// RPC.
	ChannelServiceMuteMemberProcedure = "/holomush.channel.v1.ChannelService/MuteMember"
	// ChannelServiceBanMemberProcedure is the fully-qualified name of the ChannelService's BanMember
	// RPC.
	ChannelServiceBanMemberProcedure = "/holomush.channel.v1.ChannelService/BanMember"
	// ChannelServiceKickMemberProcedure is the fully-qualified name of the ChannelService's KickMember
	// RPC.
	ChannelServiceKickMemberProcedure = "/holomush.channel.v1.ChannelService/KickMember"
	// ChannelServiceTransferOwnershipProcedure is the fully-qualified name of the ChannelService's
	// TransferOwnership RPC.
	ChannelServiceTransferOwnershipProcedure = "/holomush.channel.v1.ChannelService/TransferOwnership"
)

// ChannelServiceClient is a client for the holomush.channel.v1.ChannelService service.
type ChannelServiceClient interface {
	// CreateChannel allocates a new channel owned by the calling character,
	// seeded active with the supplied name, visibility type, and retention
	// window. The creating character becomes the `owner` member in the same
	// transaction, a create ops-journal entry is recorded, and a channel-created
	// notice is emitted. Creation authority is admin-gated by the host's ABAC
	// policy at dispatch; the handler enforces case-insensitive name uniqueness
	// and rejects a duplicate with codes.AlreadyExists.
	CreateChannel(context.Context, *connect.Request[v1.CreateChannelRequest]) (*connect.Response[v1.CreateChannelResponse], error)
	// JoinChannel adds the calling character to a channel as a `member`. Public
	// channels accept any join; private and admin channels require a pre-existing
	// invitation. A banned character is refused. Idempotent: a repeat join by an
	// existing member succeeds without re-emitting a join notice. On success the
	// host subscribes the joining session's live stream for the channel subject.
	JoinChannel(context.Context, *connect.Request[v1.JoinChannelRequest]) (*connect.Response[v1.JoinChannelResponse], error)
	// LeaveChannel removes the calling character's membership from a channel and
	// unsubscribes their session's live stream. The channel owner cannot leave
	// (codes.FailedPrecondition) — they must transfer ownership first. Emits a
	// leave notice.
	LeaveChannel(context.Context, *connect.Request[v1.LeaveChannelRequest]) (*connect.Response[v1.LeaveChannelResponse], error)
	// ListChannels returns the channels visible to the calling character:
	// all public channels plus any private or admin channels the character is a
	// member of, paginated. Channels the caller cannot see are omitted rather
	// than surfaced as hidden, so the listing cannot be used to probe for the
	// existence of restricted channels.
	ListChannels(context.Context, *connect.Request[v1.ListChannelsRequest]) (*connect.Response[v1.ListChannelsResponse], error)
	// PostToChannel publishes one line of member-authored content (say, pose, or
	// ooc) to a channel. The handler self-enforces that the caller is a
	// non-muted member before building the CommunicationContent payload and
	// emitting it on the channel subject; a muted member's post is suppressed and
	// a non-member's post is refused. The durable effect is the emitted event,
	// which the host audits to channel_log for history.
	PostToChannel(context.Context, *connect.Request[v1.PostToChannelRequest]) (*connect.Response[v1.PostToChannelResponse], error)
	// WhoInChannel returns the current member roster of a channel to a member.
	// The plugin-code membership gate runs before any roster read, so a
	// non-member of a private or admin channel is denied before the channel's
	// existence is confirmed.
	WhoInChannel(context.Context, *connect.Request[v1.WhoInChannelRequest]) (*connect.Response[v1.WhoInChannelResponse], error)
	// QueryChannelHistory returns recent content for a channel to a member,
	// newest last, capped at the server-side scrollback ceiling. The handler
	// enforces the membership gate as its first step (before any store read) and
	// never returns rows older than the caller's most-recent join, so leaving and
	// rejoining does not expose messages sent while the caller was absent.
	QueryChannelHistory(context.Context, *connect.Request[v1.QueryChannelHistoryRequest]) (*connect.Response[v1.QueryChannelHistoryResponse], error)
	// InviteToChannel records an invitation permitting a target character to join
	// a private or admin channel (owner-or-admin only via ABAC). Rejected with
	// codes.AlreadyExists when the target is already a member.
	InviteToChannel(context.Context, *connect.Request[v1.InviteToChannelRequest]) (*connect.Response[v1.InviteToChannelResponse], error)
	// MuteMember marks a target member as muted, suppressing their subsequent
	// posts to the channel while leaving their membership and read access intact
	// (owner-or-admin only via ABAC). Emits a mute notice.
	MuteMember(context.Context, *connect.Request[v1.MuteMemberRequest]) (*connect.Response[v1.MuteMemberResponse], error)
	// BanMember marks a target member as banned, removing them from the channel
	// and preventing any future rejoin until unbanned (owner-or-admin only via
	// ABAC). The channel owner cannot be banned (codes.FailedPrecondition).
	BanMember(context.Context, *connect.Request[v1.BanMemberRequest]) (*connect.Response[v1.BanMemberResponse], error)
	// KickMember removes a target member from the channel and unsubscribes their
	// live stream, without the permanent block a ban imposes (owner-or-admin only
	// via ABAC). The channel owner cannot be kicked (codes.FailedPrecondition).
	// Emits a leave notice recording the removal.
	KickMember(context.Context, *connect.Request[v1.KickMemberRequest]) (*connect.Response[v1.KickMemberResponse], error)
	// TransferOwnership reassigns channel ownership from the calling owner to a
	// target who MUST already be a member (owner-only via ABAC). The former owner
	// is demoted to member in the same transaction.
	TransferOwnership(context.Context, *connect.Request[v1.TransferOwnershipRequest]) (*connect.Response[v1.TransferOwnershipResponse], error)
}

// NewChannelServiceClient constructs a client for the holomush.channel.v1.ChannelService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewChannelServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ChannelServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	channelServiceMethods := v1.File_holomush_channel_v1_channel_proto.Services().ByName("ChannelService").Methods()
	return &channelServiceClient{
		createChannel: connect.NewClient[v1.CreateChannelRequest, v1.CreateChannelResponse](
			httpClient,
			baseURL+ChannelServiceCreateChannelProcedure,
			connect.WithSchema(channelServiceMethods.ByName("CreateChannel")),
			connect.WithClientOptions(opts...),
		),
		joinChannel: connect.NewClient[v1.JoinChannelRequest, v1.JoinChannelResponse](
			httpClient,
			baseURL+ChannelServiceJoinChannelProcedure,
			connect.WithSchema(channelServiceMethods.ByName("JoinChannel")),
			connect.WithClientOptions(opts...),
		),
		leaveChannel: connect.NewClient[v1.LeaveChannelRequest, v1.LeaveChannelResponse](
			httpClient,
			baseURL+ChannelServiceLeaveChannelProcedure,
			connect.WithSchema(channelServiceMethods.ByName("LeaveChannel")),
			connect.WithClientOptions(opts...),
		),
		listChannels: connect.NewClient[v1.ListChannelsRequest, v1.ListChannelsResponse](
			httpClient,
			baseURL+ChannelServiceListChannelsProcedure,
			connect.WithSchema(channelServiceMethods.ByName("ListChannels")),
			connect.WithClientOptions(opts...),
		),
		postToChannel: connect.NewClient[v1.PostToChannelRequest, v1.PostToChannelResponse](
			httpClient,
			baseURL+ChannelServicePostToChannelProcedure,
			connect.WithSchema(channelServiceMethods.ByName("PostToChannel")),
			connect.WithClientOptions(opts...),
		),
		whoInChannel: connect.NewClient[v1.WhoInChannelRequest, v1.WhoInChannelResponse](
			httpClient,
			baseURL+ChannelServiceWhoInChannelProcedure,
			connect.WithSchema(channelServiceMethods.ByName("WhoInChannel")),
			connect.WithClientOptions(opts...),
		),
		queryChannelHistory: connect.NewClient[v1.QueryChannelHistoryRequest, v1.QueryChannelHistoryResponse](
			httpClient,
			baseURL+ChannelServiceQueryChannelHistoryProcedure,
			connect.WithSchema(channelServiceMethods.ByName("QueryChannelHistory")),
			connect.WithClientOptions(opts...),
		),
		inviteToChannel: connect.NewClient[v1.InviteToChannelRequest, v1.InviteToChannelResponse](
			httpClient,
			baseURL+ChannelServiceInviteToChannelProcedure,
			connect.WithSchema(channelServiceMethods.ByName("InviteToChannel")),
			connect.WithClientOptions(opts...),
		),
		muteMember: connect.NewClient[v1.MuteMemberRequest, v1.MuteMemberResponse](
			httpClient,
			baseURL+ChannelServiceMuteMemberProcedure,
			connect.WithSchema(channelServiceMethods.ByName("MuteMember")),
			connect.WithClientOptions(opts...),
		),
		banMember: connect.NewClient[v1.BanMemberRequest, v1.BanMemberResponse](
			httpClient,
			baseURL+ChannelServiceBanMemberProcedure,
			connect.WithSchema(channelServiceMethods.ByName("BanMember")),
			connect.WithClientOptions(opts...),
		),
		kickMember: connect.NewClient[v1.KickMemberRequest, v1.KickMemberResponse](
			httpClient,
			baseURL+ChannelServiceKickMemberProcedure,
			connect.WithSchema(channelServiceMethods.ByName("KickMember")),
			connect.WithClientOptions(opts...),
		),
		transferOwnership: connect.NewClient[v1.TransferOwnershipRequest, v1.TransferOwnershipResponse](
			httpClient,
			baseURL+ChannelServiceTransferOwnershipProcedure,
			connect.WithSchema(channelServiceMethods.ByName("TransferOwnership")),
			connect.WithClientOptions(opts...),
		),
	}
}

// channelServiceClient implements ChannelServiceClient.
type channelServiceClient struct {
	createChannel       *connect.Client[v1.CreateChannelRequest, v1.CreateChannelResponse]
	joinChannel         *connect.Client[v1.JoinChannelRequest, v1.JoinChannelResponse]
	leaveChannel        *connect.Client[v1.LeaveChannelRequest, v1.LeaveChannelResponse]
	listChannels        *connect.Client[v1.ListChannelsRequest, v1.ListChannelsResponse]
	postToChannel       *connect.Client[v1.PostToChannelRequest, v1.PostToChannelResponse]
	whoInChannel        *connect.Client[v1.WhoInChannelRequest, v1.WhoInChannelResponse]
	queryChannelHistory *connect.Client[v1.QueryChannelHistoryRequest, v1.QueryChannelHistoryResponse]
	inviteToChannel     *connect.Client[v1.InviteToChannelRequest, v1.InviteToChannelResponse]
	muteMember          *connect.Client[v1.MuteMemberRequest, v1.MuteMemberResponse]
	banMember           *connect.Client[v1.BanMemberRequest, v1.BanMemberResponse]
	kickMember          *connect.Client[v1.KickMemberRequest, v1.KickMemberResponse]
	transferOwnership   *connect.Client[v1.TransferOwnershipRequest, v1.TransferOwnershipResponse]
}

// CreateChannel calls holomush.channel.v1.ChannelService.CreateChannel.
func (c *channelServiceClient) CreateChannel(ctx context.Context, req *connect.Request[v1.CreateChannelRequest]) (*connect.Response[v1.CreateChannelResponse], error) {
	return c.createChannel.CallUnary(ctx, req)
}

// JoinChannel calls holomush.channel.v1.ChannelService.JoinChannel.
func (c *channelServiceClient) JoinChannel(ctx context.Context, req *connect.Request[v1.JoinChannelRequest]) (*connect.Response[v1.JoinChannelResponse], error) {
	return c.joinChannel.CallUnary(ctx, req)
}

// LeaveChannel calls holomush.channel.v1.ChannelService.LeaveChannel.
func (c *channelServiceClient) LeaveChannel(ctx context.Context, req *connect.Request[v1.LeaveChannelRequest]) (*connect.Response[v1.LeaveChannelResponse], error) {
	return c.leaveChannel.CallUnary(ctx, req)
}

// ListChannels calls holomush.channel.v1.ChannelService.ListChannels.
func (c *channelServiceClient) ListChannels(ctx context.Context, req *connect.Request[v1.ListChannelsRequest]) (*connect.Response[v1.ListChannelsResponse], error) {
	return c.listChannels.CallUnary(ctx, req)
}

// PostToChannel calls holomush.channel.v1.ChannelService.PostToChannel.
func (c *channelServiceClient) PostToChannel(ctx context.Context, req *connect.Request[v1.PostToChannelRequest]) (*connect.Response[v1.PostToChannelResponse], error) {
	return c.postToChannel.CallUnary(ctx, req)
}

// WhoInChannel calls holomush.channel.v1.ChannelService.WhoInChannel.
func (c *channelServiceClient) WhoInChannel(ctx context.Context, req *connect.Request[v1.WhoInChannelRequest]) (*connect.Response[v1.WhoInChannelResponse], error) {
	return c.whoInChannel.CallUnary(ctx, req)
}

// QueryChannelHistory calls holomush.channel.v1.ChannelService.QueryChannelHistory.
func (c *channelServiceClient) QueryChannelHistory(ctx context.Context, req *connect.Request[v1.QueryChannelHistoryRequest]) (*connect.Response[v1.QueryChannelHistoryResponse], error) {
	return c.queryChannelHistory.CallUnary(ctx, req)
}

// InviteToChannel calls holomush.channel.v1.ChannelService.InviteToChannel.
func (c *channelServiceClient) InviteToChannel(ctx context.Context, req *connect.Request[v1.InviteToChannelRequest]) (*connect.Response[v1.InviteToChannelResponse], error) {
	return c.inviteToChannel.CallUnary(ctx, req)
}

// MuteMember calls holomush.channel.v1.ChannelService.MuteMember.
func (c *channelServiceClient) MuteMember(ctx context.Context, req *connect.Request[v1.MuteMemberRequest]) (*connect.Response[v1.MuteMemberResponse], error) {
	return c.muteMember.CallUnary(ctx, req)
}

// BanMember calls holomush.channel.v1.ChannelService.BanMember.
func (c *channelServiceClient) BanMember(ctx context.Context, req *connect.Request[v1.BanMemberRequest]) (*connect.Response[v1.BanMemberResponse], error) {
	return c.banMember.CallUnary(ctx, req)
}

// KickMember calls holomush.channel.v1.ChannelService.KickMember.
func (c *channelServiceClient) KickMember(ctx context.Context, req *connect.Request[v1.KickMemberRequest]) (*connect.Response[v1.KickMemberResponse], error) {
	return c.kickMember.CallUnary(ctx, req)
}

// TransferOwnership calls holomush.channel.v1.ChannelService.TransferOwnership.
func (c *channelServiceClient) TransferOwnership(ctx context.Context, req *connect.Request[v1.TransferOwnershipRequest]) (*connect.Response[v1.TransferOwnershipResponse], error) {
	return c.transferOwnership.CallUnary(ctx, req)
}

// ChannelServiceHandler is an implementation of the holomush.channel.v1.ChannelService service.
type ChannelServiceHandler interface {
	// CreateChannel allocates a new channel owned by the calling character,
	// seeded active with the supplied name, visibility type, and retention
	// window. The creating character becomes the `owner` member in the same
	// transaction, a create ops-journal entry is recorded, and a channel-created
	// notice is emitted. Creation authority is admin-gated by the host's ABAC
	// policy at dispatch; the handler enforces case-insensitive name uniqueness
	// and rejects a duplicate with codes.AlreadyExists.
	CreateChannel(context.Context, *connect.Request[v1.CreateChannelRequest]) (*connect.Response[v1.CreateChannelResponse], error)
	// JoinChannel adds the calling character to a channel as a `member`. Public
	// channels accept any join; private and admin channels require a pre-existing
	// invitation. A banned character is refused. Idempotent: a repeat join by an
	// existing member succeeds without re-emitting a join notice. On success the
	// host subscribes the joining session's live stream for the channel subject.
	JoinChannel(context.Context, *connect.Request[v1.JoinChannelRequest]) (*connect.Response[v1.JoinChannelResponse], error)
	// LeaveChannel removes the calling character's membership from a channel and
	// unsubscribes their session's live stream. The channel owner cannot leave
	// (codes.FailedPrecondition) — they must transfer ownership first. Emits a
	// leave notice.
	LeaveChannel(context.Context, *connect.Request[v1.LeaveChannelRequest]) (*connect.Response[v1.LeaveChannelResponse], error)
	// ListChannels returns the channels visible to the calling character:
	// all public channels plus any private or admin channels the character is a
	// member of, paginated. Channels the caller cannot see are omitted rather
	// than surfaced as hidden, so the listing cannot be used to probe for the
	// existence of restricted channels.
	ListChannels(context.Context, *connect.Request[v1.ListChannelsRequest]) (*connect.Response[v1.ListChannelsResponse], error)
	// PostToChannel publishes one line of member-authored content (say, pose, or
	// ooc) to a channel. The handler self-enforces that the caller is a
	// non-muted member before building the CommunicationContent payload and
	// emitting it on the channel subject; a muted member's post is suppressed and
	// a non-member's post is refused. The durable effect is the emitted event,
	// which the host audits to channel_log for history.
	PostToChannel(context.Context, *connect.Request[v1.PostToChannelRequest]) (*connect.Response[v1.PostToChannelResponse], error)
	// WhoInChannel returns the current member roster of a channel to a member.
	// The plugin-code membership gate runs before any roster read, so a
	// non-member of a private or admin channel is denied before the channel's
	// existence is confirmed.
	WhoInChannel(context.Context, *connect.Request[v1.WhoInChannelRequest]) (*connect.Response[v1.WhoInChannelResponse], error)
	// QueryChannelHistory returns recent content for a channel to a member,
	// newest last, capped at the server-side scrollback ceiling. The handler
	// enforces the membership gate as its first step (before any store read) and
	// never returns rows older than the caller's most-recent join, so leaving and
	// rejoining does not expose messages sent while the caller was absent.
	QueryChannelHistory(context.Context, *connect.Request[v1.QueryChannelHistoryRequest]) (*connect.Response[v1.QueryChannelHistoryResponse], error)
	// InviteToChannel records an invitation permitting a target character to join
	// a private or admin channel (owner-or-admin only via ABAC). Rejected with
	// codes.AlreadyExists when the target is already a member.
	InviteToChannel(context.Context, *connect.Request[v1.InviteToChannelRequest]) (*connect.Response[v1.InviteToChannelResponse], error)
	// MuteMember marks a target member as muted, suppressing their subsequent
	// posts to the channel while leaving their membership and read access intact
	// (owner-or-admin only via ABAC). Emits a mute notice.
	MuteMember(context.Context, *connect.Request[v1.MuteMemberRequest]) (*connect.Response[v1.MuteMemberResponse], error)
	// BanMember marks a target member as banned, removing them from the channel
	// and preventing any future rejoin until unbanned (owner-or-admin only via
	// ABAC). The channel owner cannot be banned (codes.FailedPrecondition).
	BanMember(context.Context, *connect.Request[v1.BanMemberRequest]) (*connect.Response[v1.BanMemberResponse], error)
	// KickMember removes a target member from the channel and unsubscribes their
	// live stream, without the permanent block a ban imposes (owner-or-admin only
	// via ABAC). The channel owner cannot be kicked (codes.FailedPrecondition).
	// Emits a leave notice recording the removal.
	KickMember(context.Context, *connect.Request[v1.KickMemberRequest]) (*connect.Response[v1.KickMemberResponse], error)
	// TransferOwnership reassigns channel ownership from the calling owner to a
	// target who MUST already be a member (owner-only via ABAC). The former owner
	// is demoted to member in the same transaction.
	TransferOwnership(context.Context, *connect.Request[v1.TransferOwnershipRequest]) (*connect.Response[v1.TransferOwnershipResponse], error)
}

// NewChannelServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewChannelServiceHandler(svc ChannelServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	channelServiceMethods := v1.File_holomush_channel_v1_channel_proto.Services().ByName("ChannelService").Methods()
	channelServiceCreateChannelHandler := connect.NewUnaryHandler(
		ChannelServiceCreateChannelProcedure,
		svc.CreateChannel,
		connect.WithSchema(channelServiceMethods.ByName("CreateChannel")),
		connect.WithHandlerOptions(opts...),
	)
	channelServiceJoinChannelHandler := connect.NewUnaryHandler(
		ChannelServiceJoinChannelProcedure,
		svc.JoinChannel,
		connect.WithSchema(channelServiceMethods.ByName("JoinChannel")),
		connect.WithHandlerOptions(opts...),
	)
	channelServiceLeaveChannelHandler := connect.NewUnaryHandler(
		ChannelServiceLeaveChannelProcedure,
		svc.LeaveChannel,
		connect.WithSchema(channelServiceMethods.ByName("LeaveChannel")),
		connect.WithHandlerOptions(opts...),
	)
	channelServiceListChannelsHandler := connect.NewUnaryHandler(
		ChannelServiceListChannelsProcedure,
		svc.ListChannels,
		connect.WithSchema(channelServiceMethods.ByName("ListChannels")),
		connect.WithHandlerOptions(opts...),
	)
	channelServicePostToChannelHandler := connect.NewUnaryHandler(
		ChannelServicePostToChannelProcedure,
		svc.PostToChannel,
		connect.WithSchema(channelServiceMethods.ByName("PostToChannel")),
		connect.WithHandlerOptions(opts...),
	)
	channelServiceWhoInChannelHandler := connect.NewUnaryHandler(
		ChannelServiceWhoInChannelProcedure,
		svc.WhoInChannel,
		connect.WithSchema(channelServiceMethods.ByName("WhoInChannel")),
		connect.WithHandlerOptions(opts...),
	)
	channelServiceQueryChannelHistoryHandler := connect.NewUnaryHandler(
		ChannelServiceQueryChannelHistoryProcedure,
		svc.QueryChannelHistory,
		connect.WithSchema(channelServiceMethods.ByName("QueryChannelHistory")),
		connect.WithHandlerOptions(opts...),
	)
	channelServiceInviteToChannelHandler := connect.NewUnaryHandler(
		ChannelServiceInviteToChannelProcedure,
		svc.InviteToChannel,
		connect.WithSchema(channelServiceMethods.ByName("InviteToChannel")),
		connect.WithHandlerOptions(opts...),
	)
	channelServiceMuteMemberHandler := connect.NewUnaryHandler(
		ChannelServiceMuteMemberProcedure,
		svc.MuteMember,
		connect.WithSchema(channelServiceMethods.ByName("MuteMember")),
		connect.WithHandlerOptions(opts...),
	)
	channelServiceBanMemberHandler := connect.NewUnaryHandler(
		ChannelServiceBanMemberProcedure,
		svc.BanMember,
		connect.WithSchema(channelServiceMethods.ByName("BanMember")),
		connect.WithHandlerOptions(opts...),
	)
	channelServiceKickMemberHandler := connect.NewUnaryHandler(
		ChannelServiceKickMemberProcedure,
		svc.KickMember,
		connect.WithSchema(channelServiceMethods.ByName("KickMember")),
		connect.WithHandlerOptions(opts...),
	)
	channelServiceTransferOwnershipHandler := connect.NewUnaryHandler(
		ChannelServiceTransferOwnershipProcedure,
		svc.TransferOwnership,
		connect.WithSchema(channelServiceMethods.ByName("TransferOwnership")),
		connect.WithHandlerOptions(opts...),
	)
	return "/holomush.channel.v1.ChannelService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ChannelServiceCreateChannelProcedure:
			channelServiceCreateChannelHandler.ServeHTTP(w, r)
		case ChannelServiceJoinChannelProcedure:
			channelServiceJoinChannelHandler.ServeHTTP(w, r)
		case ChannelServiceLeaveChannelProcedure:
			channelServiceLeaveChannelHandler.ServeHTTP(w, r)
		case ChannelServiceListChannelsProcedure:
			channelServiceListChannelsHandler.ServeHTTP(w, r)
		case ChannelServicePostToChannelProcedure:
			channelServicePostToChannelHandler.ServeHTTP(w, r)
		case ChannelServiceWhoInChannelProcedure:
			channelServiceWhoInChannelHandler.ServeHTTP(w, r)
		case ChannelServiceQueryChannelHistoryProcedure:
			channelServiceQueryChannelHistoryHandler.ServeHTTP(w, r)
		case ChannelServiceInviteToChannelProcedure:
			channelServiceInviteToChannelHandler.ServeHTTP(w, r)
		case ChannelServiceMuteMemberProcedure:
			channelServiceMuteMemberHandler.ServeHTTP(w, r)
		case ChannelServiceBanMemberProcedure:
			channelServiceBanMemberHandler.ServeHTTP(w, r)
		case ChannelServiceKickMemberProcedure:
			channelServiceKickMemberHandler.ServeHTTP(w, r)
		case ChannelServiceTransferOwnershipProcedure:
			channelServiceTransferOwnershipHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedChannelServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedChannelServiceHandler struct{}

func (UnimplementedChannelServiceHandler) CreateChannel(context.Context, *connect.Request[v1.CreateChannelRequest]) (*connect.Response[v1.CreateChannelResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.channel.v1.ChannelService.CreateChannel is not implemented"))
}

func (UnimplementedChannelServiceHandler) JoinChannel(context.Context, *connect.Request[v1.JoinChannelRequest]) (*connect.Response[v1.JoinChannelResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.channel.v1.ChannelService.JoinChannel is not implemented"))
}

func (UnimplementedChannelServiceHandler) LeaveChannel(context.Context, *connect.Request[v1.LeaveChannelRequest]) (*connect.Response[v1.LeaveChannelResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.channel.v1.ChannelService.LeaveChannel is not implemented"))
}

func (UnimplementedChannelServiceHandler) ListChannels(context.Context, *connect.Request[v1.ListChannelsRequest]) (*connect.Response[v1.ListChannelsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.channel.v1.ChannelService.ListChannels is not implemented"))
}

func (UnimplementedChannelServiceHandler) PostToChannel(context.Context, *connect.Request[v1.PostToChannelRequest]) (*connect.Response[v1.PostToChannelResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.channel.v1.ChannelService.PostToChannel is not implemented"))
}

func (UnimplementedChannelServiceHandler) WhoInChannel(context.Context, *connect.Request[v1.WhoInChannelRequest]) (*connect.Response[v1.WhoInChannelResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.channel.v1.ChannelService.WhoInChannel is not implemented"))
}

func (UnimplementedChannelServiceHandler) QueryChannelHistory(context.Context, *connect.Request[v1.QueryChannelHistoryRequest]) (*connect.Response[v1.QueryChannelHistoryResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.channel.v1.ChannelService.QueryChannelHistory is not implemented"))
}

func (UnimplementedChannelServiceHandler) InviteToChannel(context.Context, *connect.Request[v1.InviteToChannelRequest]) (*connect.Response[v1.InviteToChannelResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.channel.v1.ChannelService.InviteToChannel is not implemented"))
}

func (UnimplementedChannelServiceHandler) MuteMember(context.Context, *connect.Request[v1.MuteMemberRequest]) (*connect.Response[v1.MuteMemberResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.channel.v1.ChannelService.MuteMember is not implemented"))
}

func (UnimplementedChannelServiceHandler) BanMember(context.Context, *connect.Request[v1.BanMemberRequest]) (*connect.Response[v1.BanMemberResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.channel.v1.ChannelService.BanMember is not implemented"))
}

func (UnimplementedChannelServiceHandler) KickMember(context.Context, *connect.Request[v1.KickMemberRequest]) (*connect.Response[v1.KickMemberResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.channel.v1.ChannelService.KickMember is not implemented"))
}

func (UnimplementedChannelServiceHandler) TransferOwnership(context.Context, *connect.Request[v1.TransferOwnershipRequest]) (*connect.Response[v1.TransferOwnershipResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("holomush.channel.v1.ChannelService.TransferOwnership is not implemented"))
}
