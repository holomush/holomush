// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 HoloMUSH Contributors

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: holomush/adminportal/v1/adminportal.proto

package adminportalv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	AdminPortalService_AdminListSections_FullMethodName = "/holomush.adminportal.v1.AdminPortalService/AdminListSections"
)

// AdminPortalServiceClient is the client API for AdminPortalService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// AdminPortalService is the PLAYER-SESSION admin portal surface: the RPCs the
// browser's /admin routes reach, gated by `admin_section:` ABAC over the
// network. Its identity is resolved server-side from the request's
// player_session_token, and NewAdminSectionInterceptor
// (internal/grpc/admin_interceptor.go) authorizes every method under this
// package prefix from a fail-closed method-to-section descriptor table before
// any handler runs.
//
// It is deliberately a DIFFERENT package and a DIFFERENT trust boundary from
// holomush.admin.v1.AdminService, the break-glass operator control plane. That
// service is served over a UNIX domain socket by internal/admin/socket/ under
// AssertOperatorAdmin, never over the core gRPC server, and no RPC declared
// here is reachable from it or it from here.
//
// Implemented by AdminPortalServer in internal/grpc/admin_service.go and
// registered on the core gRPC server in cmd/holomush/sub_grpc.go.
type AdminPortalServiceClient interface {
	// AdminListSections enumerates the admin sections the calling player may
	// reach, in the shipped registry's row order.
	//
	// AdminPortalServer.AdminListSections runs DOWNSTREAM of the interceptor's
	// admission check: a caller holding no `admin_section:` access is already
	// refused with PermissionDenied and never reaches the handler, so an empty
	// list means "admitted, but nothing permitted" rather than "not an admin".
	// The per-entry filter is section.AssertSectionAdmission, not
	// AssertSectionAccess, so sections that are registered but not yet
	// implemented are still listed and carry their status as data.
	AdminListSections(ctx context.Context, in *AdminListSectionsRequest, opts ...grpc.CallOption) (*AdminListSectionsResponse, error)
}

type adminPortalServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewAdminPortalServiceClient(cc grpc.ClientConnInterface) AdminPortalServiceClient {
	return &adminPortalServiceClient{cc}
}

func (c *adminPortalServiceClient) AdminListSections(ctx context.Context, in *AdminListSectionsRequest, opts ...grpc.CallOption) (*AdminListSectionsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(AdminListSectionsResponse)
	err := c.cc.Invoke(ctx, AdminPortalService_AdminListSections_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// AdminPortalServiceServer is the server API for AdminPortalService service.
// All implementations must embed UnimplementedAdminPortalServiceServer
// for forward compatibility.
//
// AdminPortalService is the PLAYER-SESSION admin portal surface: the RPCs the
// browser's /admin routes reach, gated by `admin_section:` ABAC over the
// network. Its identity is resolved server-side from the request's
// player_session_token, and NewAdminSectionInterceptor
// (internal/grpc/admin_interceptor.go) authorizes every method under this
// package prefix from a fail-closed method-to-section descriptor table before
// any handler runs.
//
// It is deliberately a DIFFERENT package and a DIFFERENT trust boundary from
// holomush.admin.v1.AdminService, the break-glass operator control plane. That
// service is served over a UNIX domain socket by internal/admin/socket/ under
// AssertOperatorAdmin, never over the core gRPC server, and no RPC declared
// here is reachable from it or it from here.
//
// Implemented by AdminPortalServer in internal/grpc/admin_service.go and
// registered on the core gRPC server in cmd/holomush/sub_grpc.go.
type AdminPortalServiceServer interface {
	// AdminListSections enumerates the admin sections the calling player may
	// reach, in the shipped registry's row order.
	//
	// AdminPortalServer.AdminListSections runs DOWNSTREAM of the interceptor's
	// admission check: a caller holding no `admin_section:` access is already
	// refused with PermissionDenied and never reaches the handler, so an empty
	// list means "admitted, but nothing permitted" rather than "not an admin".
	// The per-entry filter is section.AssertSectionAdmission, not
	// AssertSectionAccess, so sections that are registered but not yet
	// implemented are still listed and carry their status as data.
	AdminListSections(context.Context, *AdminListSectionsRequest) (*AdminListSectionsResponse, error)
	mustEmbedUnimplementedAdminPortalServiceServer()
}

// UnimplementedAdminPortalServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAdminPortalServiceServer struct{}

func (UnimplementedAdminPortalServiceServer) AdminListSections(context.Context, *AdminListSectionsRequest) (*AdminListSectionsResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method AdminListSections not implemented")
}
func (UnimplementedAdminPortalServiceServer) mustEmbedUnimplementedAdminPortalServiceServer() {}
func (UnimplementedAdminPortalServiceServer) testEmbeddedByValue()                            {}

// UnsafeAdminPortalServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AdminPortalServiceServer will
// result in compilation errors.
type UnsafeAdminPortalServiceServer interface {
	mustEmbedUnimplementedAdminPortalServiceServer()
}

func RegisterAdminPortalServiceServer(s grpc.ServiceRegistrar, srv AdminPortalServiceServer) {
	// If the following call panics, it indicates UnimplementedAdminPortalServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&AdminPortalService_ServiceDesc, srv)
}

func _AdminPortalService_AdminListSections_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(AdminListSectionsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AdminPortalServiceServer).AdminListSections(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AdminPortalService_AdminListSections_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AdminPortalServiceServer).AdminListSections(ctx, req.(*AdminListSectionsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// AdminPortalService_ServiceDesc is the grpc.ServiceDesc for AdminPortalService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var AdminPortalService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "holomush.adminportal.v1.AdminPortalService",
	HandlerType: (*AdminPortalServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "AdminListSections",
			Handler:    _AdminPortalService_AdminListSections_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "holomush/adminportal/v1/adminportal.proto",
}
