---
phase: 02-scenes-lineage-completion
plan: 05
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - api/proto/holomush/sceneaccess/v1/sceneaccess.proto
  - api/proto/holomush/web/v1/web.proto
  - pkg/proto/holomush/sceneaccess/v1/sceneaccess.pb.go
  - pkg/proto/holomush/sceneaccess/v1/sceneaccess_grpc.pb.go
  - pkg/proto/holomush/web/v1/web.pb.go
  - pkg/proto/holomush/web/v1/web_grpc.pb.go
  - web/src/lib/connect/holomush/sceneaccess/v1/sceneaccess_pb.ts
  - web/src/lib/connect/holomush/web/v1/web_pb.ts
  - internal/grpc/client.go
  - internal/grpc/sceneaccess_service.go
  - internal/grpc/sceneaccess_service_test.go
  - internal/web/scene_handlers.go
  - web/src/lib/scenes/notifyFlow.ts
  - web/src/lib/scenes/notifyFlow.test.ts
  - web/src/lib/scenes/client.ts
  - web/src/lib/scenes/types.ts
  - web/src/lib/scenes/workspaceStore.svelte.ts
  - web/e2e/scenes.spec.ts
autonomous: true
requirements: [SCENEFWD-02]
must_haves:
  truths:
    - "A web player mutes/unmutes a scene and sets their notify preference through a typed BFF RPC (WebMuteScene / WebSetSceneNotifyPref) — proto → BFF → facade → plugin — never the command path (D-04, gateway-boundary)."
    - "The web write reaches the same plugin SceneService.MuteScene/SetSceneNotifyPref RPCs the telnet command uses, so both surfaces share one enforcement point (ABAC in the plugin)."
    - "The SceneAccessService facade SERVER (SceneAccessServer.MuteScene / SetSceneNotifyPref in sceneaccess_service.go) resolves the server-verified owned character, begins a host-vouched dispatch, and sets CharacterId from that verified character on the forwarded plugin request — so the plugin's MANDATORY character_id↔actor-metadata guard (Plan 03, service.go:696-706) PASSES instead of rejecting the web write fail-closed (PermissionDenied)."
    - "The persisted mute/notify state has a typed READ path to the web workspace, so after reload/reconnect the UI shows which scenes are muted and whether global notifications are off (round-3 Concern 1): the existing WebListMyScenes → facade ListMyScenes → plugin ListCharacterScenes read now carries per-scene `muted` (auto-propagated via the re-exported CharacterSceneInfo — Plan 03 Task 4) and a top-level `global_notify_enabled` (added to sceneaccess.ListMyScenesResponse + web.WebListMyScenesResponse here and forwarded by the facade + BFF handlers). The web workspace snapshot (WorkspaceScene + workspaceStore.refresh) carries + renders both; the mute toggle reflects persisted state, not just local session state."
  artifacts:
    - api/proto/holomush/web/v1/web.proto
    - api/proto/holomush/sceneaccess/v1/sceneaccess.proto
    - internal/grpc/sceneaccess_service.go
    - internal/web/scene_handlers.go
    - web/src/lib/scenes/notifyFlow.ts
  key_links:
    - "web notifyFlow.ts → WebMuteScene BFF RPC → SceneAccessService.MuteScene facade → plugin SceneService.MuteScene"
    - "SceneAccessServer.MuteScene / SetSceneNotifyPref (sceneaccess_service.go) → resolveAndGate → ownedCharacter → beginDispatch(host-vouched actor+ownerPlayerID, :150-160) → s.sceneClient.MuteScene/SetSceneNotifyPref with CharacterId: char.ID.String() set (mirror ResumeScene at :433-436) — WITHOUT this the plugin guard rejects the web write fail-closed (Plan 03)."
    - "scene_handlers.go WebMuteScene mirrors WebCreateScene (token via header, nil-check sceneAccess, WithTimeout, opaque errors)"
    - "read-back (round-3 Concern 1): plugin ListCharacterScenes (Plan 03 Task 4) → facade ListMyScenes forwards global_notify_enabled + re-exported CharacterSceneInfo.muted → BFF WebListMyScenes forwards both → web client listMyScenes surfaces {scenes, globalNotifyEnabled} → workspaceStore.refresh maps csi.muted onto WorkspaceScene.muted + stores globalNotifyEnabled → mute toggle renders persisted state on reload"
  prohibitions:
    - "The web mute write MUST NOT use sendCommand / HandleCommand — structural GUI writes go through the typed BFF RPC (gateway-boundary.md)."
    - "notifyFlow.ts MUST mirror membershipFlow.ts (toggle on an existing resource), NOT createFlow.ts (RESEARCH Pattern 3)."
    - "Every new proto element MUST carry a Go-grounded doc comment (proto-doc-comments.md)."
---

<objective>
Give the web client a mute/notify-prefs control as a 4-layer typed slice
(proto → facade → BFF → client), mirroring the shipped WebCreateScene pattern
exactly. The web write forwards to the SAME plugin SceneService RPCs Plan 03
built, so telnet and web share one ABAC enforcement point. This is D-04's web
surface — a structural GUI write, so it MUST use a typed RPC, never the command
path.

Purpose: web players get parity mute/notify controls (D-04) via the typed-RPC
discipline the codebase mandates for structural writes.
Output: WebMuteScene / WebSetSceneNotifyPref (web.proto) → MuteScene /
SetSceneNotifyPref (sceneaccess facade) → BFF handler → notifyFlow.ts client + e2e.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-scenes-lineage-completion/02-CONTEXT.md
@.planning/phases/02-scenes-lineage-completion/02-RESEARCH.md
@.planning/phases/02-scenes-lineage-completion/02-PATTERNS.md
@.claude/rules/gateway-boundary.md
@.claude/rules/proto-doc-comments.md
@.claude/rules/grpc-errors.md
@docs/superpowers/specs/2026-06-19-web-create-scene-design.md
</context>

## Artifacts this phase produces (Plan 05)

New/changed symbols introduced here — exclude from any drift/source-grounding pass:

- `api/proto/holomush/sceneaccess/v1/sceneaccess.proto` — facade RPCs
  `MuteScene(MuteSceneRequest) returns (MuteSceneResponse)` and
  `SetSceneNotifyPref(SetSceneNotifyPrefRequest) returns (SetSceneNotifyPrefResponse)`.
  ALSO a top-level `global_notify_enabled` bool on the EXISTING `ListMyScenesResponse` (:260-263) — the per-scene `muted` field auto-propagates via the re-exported `holomush.scene.v1.CharacterSceneInfo` (Plan 03 Task 4), so ONLY the top-level global flag is added here (round-3 Concern 1 read-back).
- `api/proto/holomush/web/v1/web.proto` — BFF RPCs `WebMuteScene` and `WebSetSceneNotifyPref` + request/response messages.
  ALSO a top-level `global_notify_enabled` bool on the EXISTING `WebListMyScenesResponse` (:1059-1062), mirroring the sceneaccess addition (round-3 Concern 1 read-back).
- `internal/grpc/sceneaccess_service.go` — facade-SERVER handlers `SceneAccessServer.MuteScene` /
  `SceneAccessServer.SetSceneNotifyPref` mirroring `ResumeScene` (:418-441): resolveAndGate →
  ownedCharacter → beginDispatch → forward to `s.sceneClient.MuteScene`/`SetSceneNotifyPref` with
  `CharacterId: char.ID.String()` set (:433-436). This is the piece that makes the plugin's mandatory
  `character_id`↔actor-metadata guard (Plan 03) pass; omitting it silently breaks the web mute/notify
  surface fail-closed. ALSO the EXISTING `SceneAccessServer.ListMyScenes` (:259-282) gains a one-line
  forward of the new top-level flag: `GlobalNotifyEnabled: resp.GetGlobalNotifyEnabled()` on the returned
  `ListMyScenesResponse` (the per-scene `muted` already rides along inside `resp.GetScenes()` — the
  re-exported CharacterSceneInfo — with no code change); round-3 Concern 1 read-back.
- `internal/grpc/client.go` — BFF client stub forwards `MuteScene`/`SetSceneNotifyPref` to the facade (oops.Code("RPC_FAILED")).
- `internal/web/scene_handlers.go` — BFF handlers `WebMuteScene`/`WebSetSceneNotifyPref` mirroring `WebCreateScene`.
  ALSO the EXISTING `WebListMyScenes` handler (:90-115) gains a one-line forward: `GlobalNotifyEnabled: resp.GetGlobalNotifyEnabled()` on the returned `WebListMyScenesResponse` (alongside the existing `Scenes: resp.GetScenes()` at :112-114); round-3 Concern 1 read-back.
- `web/src/lib/scenes/client.ts` — the EXISTING `listMyScenes` wrapper (:35-38) changes to return `{ scenes: res.scenes, globalNotifyEnabled: res.globalNotifyEnabled }` (today it returns only `res.scenes`) so the workspace store can read the persisted global flag; round-3 Concern 1.
- `web/src/lib/scenes/types.ts` — `WorkspaceScene` (:11-40) gains a `muted: boolean` field (the persisted per-scene mute the UI renders on reload); round-3 Concern 1.
- `web/src/lib/scenes/workspaceStore.svelte.ts` — `refresh()` (:79-123) maps `csi.muted` onto the new `WorkspaceScene.muted` and captures the response's `globalNotifyEnabled` into a runes-state field the mute/prefs UI reads; round-3 Concern 1.
- `web/src/lib/scenes/notifyFlow.ts` (NEW) — client flow mirroring `membershipFlow.ts`.
- `web/src/lib/scenes/notifyFlow.test.ts` (NEW) — a fast Vitest unit (mirrors
  `web/src/lib/connect/errors.test.ts` / the `web/src/lib/stores/*.test.ts` specs)
  that mocks the connect-web transport and asserts the flow builds the correct typed
  `WebMuteScene`/`WebSetSceneNotifyPref` request and round-trips the toggle — a
  sub-second inner-loop gate that runs without a browser, alongside the e2e.
- Generated `pkg/proto/holomush/{web,sceneaccess}/v1/*` and web `*_pb.ts` (via `task proto && task web:generate`).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sceneaccess + web proto RPCs + facade-SERVER handlers + BFF client forward (+ read-back global flag)</name>
  <files>api/proto/holomush/sceneaccess/v1/sceneaccess.proto, api/proto/holomush/web/v1/web.proto, internal/grpc/sceneaccess_service.go, internal/grpc/sceneaccess_service_test.go, internal/grpc/client.go</files>
  <read_first>
    - api/proto/holomush/sceneaccess/v1/sceneaccess.proto:260-263 (ListMyScenesResponse re-exports repeated holomush.scene.v1.CharacterSceneInfo — add a top-level `global_notify_enabled` bool; the per-scene `muted` rides inside CharacterSceneInfo from Plan 03 Task 4 and needs no field here — round-3 Concern 1)
    - api/proto/holomush/web/v1/web.proto:1059-1062 (WebListMyScenesResponse re-exports the same CharacterSceneInfo — add the mirroring top-level `global_notify_enabled` bool — round-3 Concern 1)
    - internal/grpc/sceneaccess_service.go:259-282 (the EXISTING ListMyScenes facade handler — forward `GlobalNotifyEnabled: resp.GetGlobalNotifyEnabled()` on the returned ListMyScenesResponse; `resp.GetScenes()` already carries the per-scene muted — round-3 Concern 1)
    - api/proto/holomush/web/v1/web.proto:283-287 (WebCreateScene RPC + its request message at :1083 — the exact BFF precedent to mirror)
    - api/proto/holomush/sceneaccess/v1/sceneaccess.proto:63-102 (CreateScene/EndScene/InviteToScene facade RPCs — the facade precedent)
    - api/proto/holomush/sceneaccess/v1/sceneaccess.proto:558-567 (ResumeSceneRequest field shape: session_id / player_session_token / character_id / scene_id — the sceneaccess request the new MuteSceneRequest mirrors; SetSceneNotifyPrefRequest mirrors it MINUS scene_id, PLUS enabled)
    - internal/grpc/sceneaccess_service.go:418-441 (ResumeScene — the EXACT facade-SERVER analog the two new handlers mirror: resolveAndGate → ownedCharacter → beginDispatch → s.sceneClient.ResumeScene with CharacterId: char.ID.String() set at :433-436)
    - internal/grpc/sceneaccess_service.go:150-160 (beginDispatch — host-vouched actor+ownerPlayerID via pluginManager.BeginServiceDispatch; identity never request-asserted)
    - internal/grpc/sceneaccess_service.go:106-125 (ownedCharacter — server-side ownership verification, INV-SCENE-63; returns the verified *world.Character whose ID stamps CharacterId)
    - internal/grpc/sceneaccess_service_test.go:210-236 (the existing SA facade-server test that asserts the forwarded plugin request carries CharacterId == the verified char via `req.GetCharacterId() == charB.ID.String()` on a scenemocks.MockSceneServiceClient — the fixture + assertion the new handler test reuses)
    - internal/grpc/client.go:419-454 (CreateScene/ResumeScene BFF client stubs — oops.Code("RPC_FAILED").With("method", …).Wrap; PATTERNS.md Slice B excerpt)
    - .claude/rules/proto-doc-comments.md (Go-grounded doc comments; name-echo gate)
  </read_first>
  <behavior>
    - WebMuteScene(scene_id, muted) forwards through the BFF client stub → facade MuteScene → plugin and returns success on a permitted call.
    - WebSetSceneNotifyPref(enabled) forwards through SetSceneNotifyPref.
    - SceneAccessServer.MuteScene resolves the verified owned character (resolveAndGate guest-gate + ownedCharacter ownership), begins a host-vouched dispatch, and forwards to the plugin SceneService.MuteScene with `CharacterId: char.ID.String()` set — so the plugin's mandatory character_id↔actor-metadata guard (Plan 03) passes; an unset/mismatched CharacterId would be rejected PermissionDenied fail-closed.
    - SceneAccessServer.SetSceneNotifyPref behaves symmetrically (character-self, NO scene_id): resolve → beginDispatch → forward to the plugin SetSceneNotifyPref with CharacterId set.
    - A plugin denial (non-participant) or error passes through as an opaque gRPC status; the BFF client stub wraps it with oops.Code("RPC_FAILED") carrying method metadata.
  </behavior>
  <action>Add MuteScene and SetSceneNotifyPref RPCs (+ request/response messages) to sceneaccess.proto, and WebMuteScene / WebSetSceneNotifyPref (+ messages) to web.proto, each fully documented and grounded in the plugin handler behavior (proto-doc-comments). Shape the sceneaccess request messages after ResumeSceneRequest (:558-567): `MuteSceneRequest` carries session_id + player_session_token + character_id (the acting owned alt) + scene_id + a muted bool; `SetSceneNotifyPrefRequest` carries session_id + player_session_token + character_id + an enabled bool and NO scene_id (character-self/global). The web messages proxy the sceneaccess messages (mirror the "WebCreateSceneRequest proxies to SceneAccessService.CreateScene" doc pattern). Implement the facade-SERVER handlers SceneAccessServer.MuteScene and SceneAccessServer.SetSceneNotifyPref in sceneaccess_service.go by mirroring ResumeScene (:418-441) EXACTLY: call resolveAndGate(ctx, req.GetPlayerSessionToken()), then ownedCharacter(ctx, ps.PlayerID, req.GetCharacterId()), then beginDispatch(ctx, char, ps.PlayerID) with defer release(), then forward to s.sceneClient.MuteScene(dctx, &scenev1.MuteSceneRequest{CharacterId: char.ID.String(), SceneId: req.GetSceneId(), Muted: req.GetMuted()}) (SetSceneNotifyPref forwards &scenev1.SetSceneNotifyPrefRequest{CharacterId: char.ID.String(), Enabled: req.GetEnabled()} — no scene_id), returning the plugin error with the same `//nolint:wrapcheck // gRPC status errors pass through as-is` pass-through. The CharacterId set is load-bearing: without it the plugin's mandatory character_id↔actor-metadata guard (Plan 03, service.go:696-706) rejects the web write fail-closed. Add the BFF client stubs in client.go mirroring ResumeScene: `MuteScene(ctx, *sceneaccessv1.MuteSceneRequest) (*sceneaccessv1.MuteSceneResponse, error)` calling `c.sceneAccessClient.MuteScene`, wrapping errors with oops.Code("RPC_FAILED"). ALSO (round-3 Concern 1 read-back): add a top-level `global_notify_enabled` bool to the EXISTING `ListMyScenesResponse` in sceneaccess.proto (:260-263) and to `WebListMyScenesResponse` in web.proto (:1059-1062), each with a Go-grounded doc comment ("the character's persisted global notify preference, forwarded from the plugin ListCharacterScenes read; default true"). Do NOT add a `muted` field to these responses — the per-scene `muted` is on the re-exported `holomush.scene.v1.CharacterSceneInfo` (Plan 03 Task 4) and propagates automatically when the bindings regenerate. Wire the forward in the EXISTING facade `ListMyScenes` handler (sceneaccess_service.go:259-282): set `GlobalNotifyEnabled: resp.GetGlobalNotifyEnabled()` on the returned `&sceneaccessv1.ListMyScenesResponse{Scenes: resp.GetScenes(), ...}` (one line; the scenes forward already carries the per-scene muted). Run `task proto && task web:generate`; commit the generated `*.pb.go` + `*_pb.ts`. Write the tests FIRST (RED then GREEN): a facade-SERVER test mirroring sceneaccess_service_test.go:210-236 that asserts MuteScene/SetSceneNotifyPref forward to a scenemocks.MockSceneServiceClient with CharacterId == the verified char.ID.String() (and that an unowned/spoofed character_id is rejected before any forward), plus the client-stub forward-success + error-wrap test, PLUS a ListMyScenes test that stubs the plugin ListCharacterScenes to return a response with global_notify_enabled=false and a muted CharacterSceneInfo row and asserts the facade response carries global_notify_enabled=false and the muted row through unchanged (round-3 Concern 1).</action>
  <verify>
    <automated>task lint:proto && task test -- ./internal/grpc/...</automated>
  </verify>
  <acceptance_criteria>
    - `task lint:proto` exits 0.
    - `rg -n "rpc WebMuteScene|rpc WebSetSceneNotifyPref" api/proto/holomush/web/v1/web.proto` and `rg -n "rpc MuteScene|rpc SetSceneNotifyPref" api/proto/holomush/sceneaccess/v1/sceneaccess.proto` both match.
    - `rg -n "func \(s \*SceneAccessServer\) MuteScene|func \(s \*SceneAccessServer\) SetSceneNotifyPref" internal/grpc/sceneaccess_service.go` matches BOTH facade-server handlers.
    - Both facade-server handlers set CharacterId from the verified character on the forwarded plugin request — proven by a facade-server unit test that forwards to a mock SceneServiceClient matching `req.GetCharacterId() == <verifiedChar>.ID.String()` on MuteScene AND SetSceneNotifyPref (mirroring sceneaccess_service_test.go:230), and asserts an unowned/spoofed character_id is rejected before any forward.
    - `rg -n "func \(c \*Client\) MuteScene" internal/grpc/client.go` matches with oops.Code("RPC_FAILED").
    - `rg -n "global_notify_enabled" api/proto/holomush/sceneaccess/v1/sceneaccess.proto api/proto/holomush/web/v1/web.proto` shows the top-level flag on BOTH ListMyScenesResponse and WebListMyScenesResponse (round-3 Concern 1); NO `muted` field added to these responses (it rides on the re-exported CharacterSceneInfo).
    - `rg -n "GlobalNotifyEnabled: resp.GetGlobalNotifyEnabled" internal/grpc/sceneaccess_service.go` shows the facade ListMyScenes forward; a facade test proves global_notify_enabled + a muted CharacterSceneInfo row pass through unchanged.
    - Generated bindings updated and committed (no stale-diff).
  </acceptance_criteria>
  <done>The typed proto surface, the facade-SERVER handlers (setting CharacterId from the verified character so the plugin guard passes), and the BFF client stubs exist and lint clean; bindings regenerated; the CharacterId-set is unit-proven on both forwards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BFF WebMuteScene / WebSetSceneNotifyPref handlers</name>
  <files>internal/web/scene_handlers.go</files>
  <read_first>
    - internal/web/scene_handlers.go:149-178 (WebCreateScene handler — token via req.Header().Get(headerInjectSessionToken), nil-check h.sceneAccess, context.WithTimeout(ctx, rpcTimeout), forward, //nolint:wrapcheck on the gRPC pass-through error; errutil.LogErrorContext; PATTERNS.md Slice B)
    - internal/web/scene_handlers.go:90-115 (the EXISTING WebListMyScenes handler — it returns `WebListMyScenesResponse{Scenes: resp.GetScenes()}` at :112-114; add `GlobalNotifyEnabled: resp.GetGlobalNotifyEnabled()` there — round-3 Concern 1 read-back)
    - internal/grpc/client.go (the facade MuteScene from Task 1 that these handlers call)
    - .claude/rules/grpc-errors.md (never leak inner errors past the boundary; opaque status; line-scoped //nolint:wrapcheck)
    - .claude/rules/gateway-boundary.md (structural writes = typed RPC; the BFF proxies, never computes)
  </read_first>
  <behavior>
    - WebMuteScene resolves the session token from the header, rejects when h.sceneAccess is nil, forwards to the facade under an rpcTimeout context, and returns the facade response.
    - A facade/plugin denial (non-participant) surfaces as an opaque permission error to the web client — inner error not leaked.
    - WebSetSceneNotifyPref behaves symmetrically for the notify preference.
  </behavior>
  <action>Add WebMuteScene and WebSetSceneNotifyPref handlers to scene_handlers.go, copying the WebCreateScene shape: read the injected session token from the header, nil-check h.sceneAccess, wrap the call in context.WithTimeout(ctx, rpcTimeout), forward to the facade (Task 1), log failures with errutil.LogErrorContext, and return the response with `//nolint:wrapcheck // gRPC status errors pass through as-is` on the error return. Register the handlers in the web router alongside the other Web*Scene RPCs. ALSO (round-3 Concern 1 read-back): in the EXISTING WebListMyScenes handler (:90-115) add `GlobalNotifyEnabled: resp.GetGlobalNotifyEnabled()` to the returned `&webv1.WebListMyScenesResponse{Scenes: resp.GetScenes()}` (one line; the per-scene muted already rides inside `resp.GetScenes()`). Write a BFF handler test FIRST (mirror an existing scene_handlers test: nil-sceneAccess rejection, successful forward, denial pass-through), RED then GREEN; add a WebListMyScenes assertion that the top-level global_notify_enabled is forwarded from the facade response.</action>
  <verify>
    <automated>task test -- ./internal/web/...</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "func \(h \*Handler\) WebMuteScene|func \(h \*Handler\) WebSetSceneNotifyPref" internal/web/scene_handlers.go` matches.
    - `rg -n "sendCommand|HandleCommand" internal/web/scene_handlers.go` shows NO use in the new handlers (gateway-boundary).
    - The handlers nil-check h.sceneAccess and use rpcTimeout, mirroring WebCreateScene.
    - `rg -n "GlobalNotifyEnabled: resp.GetGlobalNotifyEnabled" internal/web/scene_handlers.go` shows the WebListMyScenes read-back forward (round-3 Concern 1); a test asserts the top-level flag is forwarded.
    - `task test -- ./internal/web/...` exits 0.
  </acceptance_criteria>
  <done>The BFF exposes WebMuteScene/WebSetSceneNotifyPref proxying to the facade, opaque errors, typed-RPC only, and forwards the read-back global_notify_enabled on WebListMyScenes; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: web notifyFlow.ts client + read-back snapshot wiring + fast Vitest smoke + e2e mute toggle</name>
  <files>web/src/lib/scenes/notifyFlow.ts, web/src/lib/scenes/notifyFlow.test.ts, web/src/lib/scenes/client.ts, web/src/lib/scenes/types.ts, web/src/lib/scenes/workspaceStore.svelte.ts, web/e2e/scenes.spec.ts</files>
  <read_first>
    - web/src/lib/scenes/membershipFlow.ts (invite/kick toggle on an existing resource — the exact structural-write analog to mirror per RESEARCH Pattern 3; NOT createFlow.ts)
    - web/src/lib/scenes/client.ts:35-38 (the EXISTING listMyScenes wrapper returns only `res.scenes` — change it to return `{ scenes: res.scenes, globalNotifyEnabled: res.globalNotifyEnabled }` so the store can read the persisted global flag — round-3 Concern 1; it is also the connect-web transport the notifyFlow dials)
    - web/src/lib/scenes/types.ts:11-40 (WorkspaceScene — add a `muted: boolean` field; seeded by refresh() from CharacterSceneInfo.muted — round-3 Concern 1)
    - web/src/lib/scenes/workspaceStore.svelte.ts:79-123 (refresh() maps each CharacterSceneInfo → WorkspaceScene — add `muted: csi.muted ?? false` to the pushed row, and capture the response's globalNotifyEnabled into a runes-state field the prefs UI reads; the fan-out returns per-alt {scenes, globalNotifyEnabled} now — round-3 Concern 1)
    - web/src/lib/connect/errors.test.ts AND web/src/lib/stores/connectionStore.test.ts (existing Vitest unit patterns — mocked transport, no browser, `vitest run` via `pnpm -C web run test:unit`; web has Vitest + Playwright only, NOT @testing-library/svelte, so keep the smoke at the flow-module level, not a Svelte component render)
    - web/e2e/scenes.spec.ts (existing scene e2e specs — add a mute-toggle path)
    - .claude/rules/gateway-boundary.md (never sendCommand for a structural write)
  </read_first>
  <action>Create web/src/lib/scenes/notifyFlow.ts mirroring membershipFlow.ts: a typed request/response flow calling WebMuteScene (and WebSetSceneNotifyPref) via the connect-web client.ts transport, with the same error-handling shape as invite/kick. Never build a command string. ALSO wire the read-back snapshot (round-3 Concern 1): change `listMyScenes` in client.ts (:35-38) to return `{ scenes: res.scenes, globalNotifyEnabled: res.globalNotifyEnabled }` (today it returns only `res.scenes`); add a `muted: boolean` field to `WorkspaceScene` in types.ts (:11-40); in workspaceStore.svelte.ts refresh() (:79-123) stamp `muted: csi.muted ?? false` on each pushed WorkspaceScene row and capture the (per-alt) `globalNotifyEnabled` into a runes-state field the mute/prefs UI reads — so after reload/reconnect the toggle shows persisted mute state and whether global notifications are off, not just local session state. Write a fast Vitest unit web/src/lib/scenes/notifyFlow.test.ts FIRST (mirror web/src/lib/connect/errors.test.ts): mock the connect-web transport and assert the flow issues a WebMuteScene request with the right scene_id/muted fields, surfaces a denial as the flow's typed error, and round-trips a toggle — no browser, sub-second feedback; ALSO add a store/snapshot assertion (mock listMyScenes to return a muted CharacterSceneInfo row + globalNotifyEnabled=false and assert refresh() seeds WorkspaceScene.muted=true + the global-off state — round-3 Concern 1). Then wire the flow to the mute UI control on the scene workspace (minimal — a mute/unmute toggle that calls the flow and initializes from the persisted WorkspaceScene.muted). Add an e2e path to scenes.spec.ts that mutes a scene from the web UI, RELOADS, and asserts the mute toggle still reflects the muted state (proving the persisted read-back, not just an in-session toggle — round-3 Concern 1). The Vitest smoke is the inner-loop gate; the e2e is the full-stack gate — keep BOTH.</action>
  <verify>
    <automated>pnpm -C web run test:unit notifyFlow && { task test:e2e -- scenes 2>/dev/null || task test:e2e; }</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "WebMuteScene|WebSetSceneNotifyPref" web/src/lib/scenes/notifyFlow.ts` matches; `rg -n "sendCommand" web/src/lib/scenes/notifyFlow.ts` shows NO match.
    - `rg -n "muted" web/src/lib/scenes/types.ts` shows the `muted` field on WorkspaceScene; `rg -n "globalNotifyEnabled" web/src/lib/scenes/client.ts web/src/lib/scenes/workspaceStore.svelte.ts` shows the read-back surfaced and captured (round-3 Concern 1).
    - `pnpm -C web run test:unit notifyFlow` exits 0 in sub-second time (the fast inner-loop gate, no browser); it includes the snapshot assertion that refresh() seeds WorkspaceScene.muted + global-off from the read-back.
    - The e2e spec mutes a scene, RELOADS, and asserts the toggle still reflects the persisted muted state (read-back proof, not in-session only — round-3 Concern 1).
    - `task test:e2e` (scenes) passes, or the mute-toggle path is green in the Playwright run.
  </acceptance_criteria>
  <done>The web client mutes/sets-notify via the typed RPC (never the command path) AND seeds the mute/global-notify UI from the typed read-back so persisted state survives reload (round-3 Concern 1); a fast Vitest smoke gates the flow + snapshot logic and an e2e proves the full-stack toggle round-trips across a reload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web client → BFF → facade → plugin | A GUI structural write crosses from an untrusted browser through the BFF to the plugin, which self-enforces ABAC (Plan 03). The BFF is pure proxy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Tampering | web mute bypassing ABAC via the command path | high | mitigate | Typed WebMuteScene RPC only; gateway-boundary forbids sendCommand for structural writes; enforcement is the SAME plugin SceneService.MuteScene ABAC gate the telnet command hits (Plan 03). |
| T-02-15 | Information disclosure | inner error text leaking to the browser | medium | mitigate | BFF returns opaque status errors (//nolint:wrapcheck pass-through per grpc-errors.md); internal error logged via errutil.LogErrorContext, not surfaced. |
| T-02-16 | Spoofing | forged session / arbitrary character on the mute write | high | mitigate | Session token resolved from the injected header; the facade SERVER (SceneAccessServer.MuteScene/SetSceneNotifyPref) resolves identity server-side — resolveAndGate (guest gate) + ownedCharacter (ownership) — and stamps CharacterId from the VERIFIED character on the forwarded plugin request, so the browser cannot assert an arbitrary character and the plugin's character_id↔actor-metadata guard (Plan 03) is satisfied by a host-vouched id, not a client claim. |
| T-02-SC | Tampering | package installs | low | accept | No package-manager installs (no new npm/Go deps; bindings regenerated per RESEARCH web slice). |
</threat_model>

<verification>
- `task lint:proto` green; `task test -- ./internal/grpc/... ./internal/web/...` green.
- `task proto && task web:generate` leaves no stale generated diff (committed in the same change) — including the new top-level global_notify_enabled on ListMyScenesResponse/WebListMyScenesResponse and the re-exported CharacterSceneInfo.muted (round-3 Concern 1).
- `pnpm -C web run test:unit notifyFlow` green (fast flow-logic smoke + read-back snapshot assertion, no browser — inner-loop gate).
- `task test:e2e` (scenes) green for the mute-toggle path INCLUDING the reload read-back assertion.
- `task lint` / `task fmt` clean.
</verification>

<success_criteria>
A web player mutes/unmutes a scene and sets their notify preference through the typed BFF RPC (never the command path), reaching the same plugin ABAC gate as telnet; the persisted mute/global-notify state round-trips back to the web workspace via the typed ListMyScenes read so the UI reflects it after reload (round-3 Concern 1); an e2e proves the write round-trip AND the read-back across a reload.
</success_criteria>

<output>
Create `.planning/phases/02-scenes-lineage-completion/02-05-SUMMARY.md` when done.
</output>
