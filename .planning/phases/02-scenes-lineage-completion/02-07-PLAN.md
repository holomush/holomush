---
phase: 02-scenes-lineage-completion
plan: 07
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified:
  - plugins/core-scenes/commands.go
  - internal/grpc/server.go
  - test/integration/scenes/reconnect_focus_restoration_test.go
autonomous: true
requirements: [SCENEFWD-03]
must_haves:
  truths:
    - "Joining a scene that auto-focuses SOME connections and skips OTHERS renders an explicit informative line (not the bare 'Joined scene #X') — the mixed focused/skipped branch (D-07, commands.go:890 TODO closed)."
    - "On telnet reconnect, a member whose PresentingFocus was a scene has its per-connection FocusKey restored (live scene content, not a badge downgrade) — RestoreConnectionFocus wired at Subscribe (D-08)."
    - "The reconnect restore does NOT clobber a web tab's per-tab focus (gated on PresentingFocus != nil), and does NOT leak a prior character's scene focus to a different character on the same telnet connection after a character swap (D-09)."
  artifacts:
    - plugins/core-scenes/commands.go
    - internal/grpc/server.go
    - test/integration/scenes/reconnect_focus_restoration_test.go
  key_links:
    - "commands.go:892 auto-focus switch → new both-non-empty case before default"
    - "server.go:~878 (after AddConnection) → RestoreConnectionFocus(ctx, sessionID, connID) gated on PresentingFocus != nil"
    - "RestoreConnectionFocus membership validation (INV-SCENE-18) prevents cross-character focus leak on connection character swap"
  prohibitions:
    - "The reconnect restore MUST be gated on PresentingFocus != nil (Pitfall 5 / Assumption A2) — an unconditional restore clobbers web per-tab focus."
    - "MUST NOT re-implement focus restore — RestoreConnectionFocus is already built + tested; this is the missing call site (D-08 is wiring, not build)."
    - "The mixed-render case MUST keep the failure-first ordering (FailedConnectionIDs > 0 stays the first case) and delete the stale TODO comment at commands.go:890."
---

<objective>
Close the three telnet scene-command edge cases (SCENEFWD-03) that today fail
silently: (1) the mixed focused/skipped auto-focus render branch — the
`commands.go:890` TODO where both outcomes are non-empty falls to the
least-informative default; (2) reconnection — the fully-built, fully-tested
`RestoreConnectionFocus` primitive has zero production callers, so a reconnecting
member loses live scene content and gets badge nudges instead; (3) multi-character
per connection — a telnet connection swapping characters must not inherit the
prior character's scene focus.

Purpose: SCENEFWD-03 — telnet scene commands handle these edge cases without
silent failure.
Output: the mixed-render case, the RestoreConnectionFocus call site (gated), and
the multi-char focus-leak guard + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-scenes-lineage-completion/02-CONTEXT.md
@.planning/phases/02-scenes-lineage-completion/02-RESEARCH.md
@.planning/phases/02-scenes-lineage-completion/02-PATTERNS.md
@docs/superpowers/specs/2026-05-21-scenes-phase-5-focus-model-and-multi-connection-visibility-design.md
@.claude/rules/testing.md
</context>

## Artifacts this phase produces (Plan 07)

New/changed symbols introduced here — exclude from any drift/source-grounding pass:

- `plugins/core-scenes/commands.go` — a new `case len(afResult.FocusedConnectionIDs) > 0 && len(afResult.SkippedConnectionIDs) > 0`
  before the default in the auto-focus render switch (:892), replacing the TODO at :890.
- `internal/grpc/server.go` — a `RestoreConnectionFocus(ctx, req.SessionId, connID)` call after AddConnection (:878), gated on `info.PresentingFocus != nil`.
- `test/integration/scenes/reconnect_focus_restoration_test.go` — new end-to-end Subscribe-wiring assertions + web-tab-safety + character-swap no-leak cases.

<tasks>

<task type="auto">
  <name>Task 1: mixed focused/skipped auto-focus render branch (D-07)</name>
  <files>plugins/core-scenes/commands.go</files>
  <read_first>
    - plugins/core-scenes/commands.go:877-908 (the auto-focus render: the FailedConnectionIDs-first guard at :877, the 5-branch switch at :892-908, the TODO at :890, the bare default "Joined scene #%s.")
    - .planning/phases/02-scenes-lineage-completion/02-PATTERNS.md (Slice D exact case text: both-non-empty → "Joined scene #%s and focused some connection(s); others stay on their current focus (use 'scene focus #%s').")
    - plugins/core-scenes/commands_focus_test.go (the focus render test file to extend — verify it is table/testing.T style before adding a case)
  </read_first>
  <action>Insert a new case in the auto-focus render switch at commands.go:892, BEFORE the default, for `len(afResult.FocusedConnectionIDs) > 0 && len(afResult.SkippedConnectionIDs) > 0` (no failures): set the message to inform the player that some connections were focused and others kept their focus, pointing at `scene focus #<id>`. Keep the FailedConnectionIDs-first case as the first branch (failure-first ordering). Delete the stale `// TODO(Phase 6 §7.4)` comment at :890. Extend commands_focus_test.go with a mixed-outcome case asserting the new message (not the bare default).</action>
  <verify>
    <automated>task test -- ./plugins/core-scenes/... -run Focus</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "FocusedConnectionIDs\) > 0 && len\(afResult.SkippedConnectionIDs\) > 0" plugins/core-scenes/commands.go` matches a case before the default.
    - `rg -n "TODO\(Phase 6" plugins/core-scenes/commands.go` returns no match (comment deleted).
    - The focus test asserts the mixed-outcome message, distinct from the bare "Joined scene #X.".
    - `task test -- ./plugins/core-scenes/...` exits 0.
  </acceptance_criteria>
  <done>A mixed focused/skipped join renders an explicit informative line; the TODO is closed; failure-first ordering preserved.</done>
</task>

<task type="auto">
  <name>Task 2: wire RestoreConnectionFocus at Subscribe (D-08) + web-tab safety</name>
  <files>internal/grpc/server.go, test/integration/scenes/reconnect_focus_restoration_test.go</files>
  <read_first>
    - internal/grpc/server.go:850-899 (the Subscribe AddConnection path: fresh Connection{} with FocusKey unset at :861-867, AddConnection at :870, streamRegistry.RegisterConnection near :895 — the call site gap is right after AddConnection succeeds at :878)
    - internal/grpc/focus/restore_connection_focus.go + internal/grpc/focus/coordinator.go:65-71 (RestoreConnectionFocus — already built + tested; gated on FocusMemberships, INV-SCENE-18 grid-fallback when membership revoked; branch-1 no-op when PresentingFocus is nil)
    - test/integration/scenes/reconnect_focus_restoration_test.go (INV-SCENE-18/25/26 coordinator coverage — extend with the Subscribe-wiring end-to-end assertion)
    - .planning/phases/02-scenes-lineage-completion/02-RESEARCH.md (Pitfall 5 / Assumption A2: gate on PresentingFocus != nil so web per-tab focus is not clobbered; Pitfall 6: run task test:int after Subscribe/focus changes)
    - internal/grpc/session.go:239-243 (PresentingFocus is "primarily telnet single-pane reconnect UX")
  </read_first>
  <action>After AddConnection succeeds at server.go:878, call the focus coordinator's `RestoreConnectionFocus(ctx, req.SessionId, connID)`, gated on `info.PresentingFocus != nil` (restore is a documented no-op / grid-fallback otherwise, keeping web per-tab focus safe). Log a restore failure via context-carrying logging without failing the Subscribe (best-effort restore). Extend reconnect_focus_restoration_test.go with an integration assertion that a telnet reconnect whose PresentingFocus was a scene ends with the connection FocusKey set to that scene (receives live content, not a SCENE_ACTIVITY badge downgrade), AND a web-tab case proving a resubscribe with PresentingFocus nil does NOT overwrite the tab's chosen focus. Run task test:int (Pitfall 6).</action>
  <verify>
    <automated>task test:int -- -run 'ReconnectFocus|Restore' ./test/integration/scenes/ 2>/dev/null || task test:int</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "RestoreConnectionFocus" internal/grpc/server.go` shows the call after AddConnection, gated on `PresentingFocus != nil`.
    - Integration test: telnet reconnect with scene PresentingFocus → FocusKey restored to that scene (live content); web-tab resubscribe with nil PresentingFocus → focus unchanged.
    - `task test:int` exits 0.
  </acceptance_criteria>
  <done>Reconnecting telnet members regain live scene focus; web per-tab focus is not clobbered; restore is best-effort and gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: multi-character-per-connection focus no-leak (D-09)</name>
  <files>internal/grpc/server.go, test/integration/scenes/reconnect_focus_restoration_test.go</files>
  <read_first>
    - internal/telnet/gateway_handler.go:244-334 (character swap = QUIT → return-to-picker → h.charName="" → re-pick; the model is SEQUENTIAL swap per connection, RESEARCH Assumption A3 / Open Q3)
    - internal/grpc/focus/restore_connection_focus.go (INV-SCENE-18 membership validation → grid-fallback when the new character is NOT a member of the prior character's scene — this ALREADY blocks the cross-character leak; confirm and lock with a test)
    - docs/superpowers/specs/2026-05-21-scenes-phase-5-focus-model-and-multi-connection-visibility-design.md (per-connection FocusKey + connection→character binding model)
    - test/integration/scenes/multi_connection_visibility_test.go (sibling multi-connection test for the harness shape)
  </read_first>
  <behavior>
    - A telnet connection focused on scene #7 as character A, after swapping to character B (not a member of #7), does NOT restore/inherit #7's focus — B's connection falls back to grid (INV-SCENE-18 membership validation).
    - If B IS a member of a scene with its own PresentingFocus, B restores B's focus, never A's.
    - No stale FocusKey from the prior character remains on the connection after the swap.
  </behavior>
  <action>Confirm (and, if a gap exists, add a defensive clear of the per-connection FocusKey on connection teardown/character-unbind so no stale scene FocusKey survives a character swap) that RestoreConnectionFocus's INV-SCENE-18 membership validation prevents a swapped-in character B from inheriting character A's scene focus. Pin the model as SEQUENTIAL per-connection character swap (Assumption A3). Write the integration test FIRST proving the no-leak behavior (A focused on #7 → swap to B non-member → B at grid, not #7; and B-member → B's own focus), RED then GREEN. If the existing wiring already passes because membership validation blocks the leak, the test binds the guarantee; if it leaks, add the defensive FocusKey clear at the connection-teardown/character-unbind path.</action>
  <verify>
    <automated>task test:int -- -run 'MultiChar|CharacterSwap|Reconnect' ./test/integration/scenes/ 2>/dev/null || task test:int</automated>
  </verify>
  <acceptance_criteria>
    - Integration test proves: character swap A(#7)→B(non-member) leaves B at grid focus, never #7; B-member restores B's own focus.
    - No stale FocusKey from a prior character persists on the connection after a swap (asserted).
    - `task test:int` exits 0.
  </acceptance_criteria>
  <done>A telnet connection swapping characters never leaks the prior character's scene focus; the guarantee is test-bound.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| telnet connection ↔ character binding | One TCP connection may serve different characters over time (sequential swap); focus state keyed to a connection must never cross the character boundary. |
| reconnect → focus restore | A reconnecting connection reclaims prior focus only if the current character still legitimately holds membership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-21 | Elevation of privilege | reconnect restoring focus to a scene the character was kicked from mid-disconnect | high | mitigate | RestoreConnectionFocus validates FocusMemberships (INV-SCENE-18) → grid-fallback when membership was revoked; already tested, locked by the Subscribe-wiring test. |
| T-02-22 | Information disclosure | character B inheriting character A's scene focus on connection swap (D-09) | high | mitigate | INV-SCENE-18 membership validation blocks the leak (B non-member → grid); a defensive FocusKey clear on character-unbind if any residual is found; integration-tested. |
| T-02-23 | Tampering | web per-tab focus clobbered by an unconditional reconnect restore | medium | mitigate | Restore gated on PresentingFocus != nil (telnet-biased); web-tab safety integration-tested (Pitfall 5 / Assumption A2). |
| T-02-24 | Availability | mixed focused/skipped join failing silently | low | mitigate | The mixed-render branch replaces the silent least-informative default with an explicit line (D-07). |
| T-02-SC | Tampering | package installs | low | accept | No package-manager installs (in-tree Go only). |

</threat_model>

<verification>
- `task test -- ./plugins/core-scenes/...` green (mixed-render).
- `task test:int` green — reconnect restores telnet focus; web-tab focus unaffected; character-swap no-leak; INV-SCENE-18/25/26 tests still green (Pitfall 6).
- `task lint` / `task fmt` clean.
</verification>

<success_criteria>
All three telnet scene-command edge cases (mixed focused/skipped render, reconnect membership+focus restore, multi-character-per-connection) are handled without silent failure and without a cross-character or web-tab focus leak.
</success_criteria>

<output>
Create `.planning/phases/02-scenes-lineage-completion/02-07-SUMMARY.md` when done.
</output>
