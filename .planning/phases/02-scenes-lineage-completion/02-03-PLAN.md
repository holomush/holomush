---
phase: 02-scenes-lineage-completion
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - api/proto/holomush/scene/v1/scene.proto
  - pkg/proto/holomush/scene/v1/scene.pb.go
  - pkg/proto/holomush/scene/v1/scene_grpc.pb.go
  - web/src/lib/connect/holomush/scene/v1/scene_pb.ts
  - plugins/core-scenes/service.go
  - plugins/core-scenes/service_test.go
  - plugins/core-scenes/commands.go
  - plugins/core-scenes/plugin.yaml
autonomous: true
requirements: [SCENEFWD-02]
must_haves:
  truths:
    - "`scene mute #X` and `scene unmute #X` telnet subcommands persist the mute and are authorized by a Layer-2 participant-gated ABAC policy — only a participant of #X may mute it (D-04, threat mitigation)."
    - "The plugin SceneService exposes MuteScene, SetSceneNotifyPref, GetSceneNotifyPref, and ListMutedScenes RPCs. Each request carries a `character_id` field guarded against the host-vouched actor metadata (PermissionDenied on mismatch, mirroring EndScene at service.go:696-704 — review Concern 2). Authorization splits by scope: MuteScene is SCENE-scoped (Layer-2 `Evaluate(\"mute\", \"scene:\"+scene_id)`, participant-gated); SetSceneNotifyPref/GetSceneNotifyPref/ListMutedScenes are CHARACTER-SELF-scoped (a character may only read/write its OWN prefs — enforced by the actor-metadata `character_id` guard, NEVER `scene:<id>` — review Concern 1)."
    - "GetSceneNotifyPref is the READ the core mute-suppression checker (Plan 04) uses to honor the per-character GLOBAL notify preference — without it the persisted global pref has no consumer (review Finding 2)."
    - "The existing self-scoped scene-list READ (SceneServiceImpl.ListCharacterScenes → CharacterSceneInfo) is EXTENDED so the character's OWN persisted mute/notify state round-trips to the web workspace snapshot (round-3 Concern 1): each CharacterSceneInfo carries a per-scene `muted` bool (from store.ListMutedScenes) and the ListCharacterScenesResponse carries a top-level `global_notify_enabled` bool (from store.GetSceneNotifyPref). This is the character's OWN state (req.character_id is already host-trusted/facade-verified — no new ABAC action). The read degrades fail-OPEN for display: a store error defaults `muted=false` / `global_notify_enabled=true` and still returns the scene list (never fails the whole list on a prefs read)."
    - "A non-participant's mute attempt is denied fail-closed (default-deny)."
  artifacts:
    - api/proto/holomush/scene/v1/scene.proto
    - plugins/core-scenes/service.go
    - plugins/core-scenes/commands.go
    - plugins/core-scenes/plugin.yaml
  key_links:
    - "commands.go dispatch switch → gated(\"mute\", …) → p.handleMute → store.SetSceneMute"
    - "SceneService.MuteScene → evaluator.Evaluate(\"mute\", \"scene:\"+req.GetSceneId()) → store (SCENE-scoped, participant-gated — review Concern 1)"
    - "SceneService.SetSceneNotifyPref/GetSceneNotifyPref/ListMutedScenes → actor-metadata guard (req.GetCharacterId() == vouched actor, else PermissionDenied) → CHARACTER-SELF read/write → store (NO scene:<id> — review Concern 1/2)"
    - "SceneService.ListCharacterScenes → store.ListMutedScenes(req.character_id) + store.GetSceneNotifyPref(req.character_id) → stamp CharacterSceneInfo.muted per row + ListCharacterScenesResponse.global_notify_enabled → facade ListMyScenes (Plan 05, re-exported by reference) → web workspace snapshot (round-3 Concern 1 read-back path; fail-OPEN on store error)"
    - "plugin.yaml participant DSL policy authorizes action in [\"mute\",\"unmute\"] (scene-scoped only; the character-self notify prefs need no scene participant policy)"
  prohibitions:
    - "MUST NOT add `mute`/`unmute` to validActions/validResourceTypes in internal/command/types.go — they are engine-Evaluate actions like end/pause/resume, NOT command-capability actions (RESEARCH Reconciliation Landmine)."
    - "MUST NOT authorize SetSceneNotifyPref/GetSceneNotifyPref/ListMutedScenes via `scene:<id>` / req.GetSceneId() — they are GLOBAL/character-self prefs with no scene id; their self-scope is the request character_id ↔ host-vouched actor-metadata guard (review Concern 1)."
    - "MUST NOT re-declare any existing event type or crypto.emits entry (no plugin.yaml emit changes here)."
    - "Every new proto element MUST carry a Go-grounded doc comment (no name-echo) — proto-doc-comments.md."
---

<objective>
Give a scene participant the controls to mute a scene's notifications: telnet
`scene mute #X` / `scene unmute #X` subcommands (new — they do not exist today)
and the plugin SceneService RPCs (MuteScene, SetSceneNotifyPref, GetSceneNotifyPref,
ListMutedScenes) that the web slice (Plan 05) and the core suppression checker
(Plan 04) consume — GetSceneNotifyPref gives Plan 04 a way to READ the persisted
per-character GLOBAL notify preference so it is actually enforced (review Finding 2).
The scene-scoped mute/unmute controls are authorized by a new Layer-2
participant-gated ABAC policy, following the `end`/`pause`/`resume`
engine-Evaluate precedent; the character-self notify-pref reads/writes are
authorized by the request `character_id` ↔ host-vouched actor-metadata guard
(no `scene:<id>` — review Concern 1/2).

Purpose: D-04's notification controls — persisted, participant-gated, on both the
telnet command path (this plan) and the web typed-RPC path (Plan 05, which reuses
these same plugin RPCs via the facade).
Output: plugin scene.proto RPCs + handlers, `scene mute`/`unmute` command cases,
the mute/unmute DSL policy, AND the read-back extension of the existing
self-scoped scene-list (CharacterSceneInfo.muted + ListCharacterScenesResponse.global_notify_enabled)
so persisted mute/notify state survives reload/reconnect (round-3 Concern 1 —
the web workspace snapshot in Plan 05 consumes it via the re-exported facade path).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-scenes-lineage-completion/02-CONTEXT.md
@.planning/phases/02-scenes-lineage-completion/02-RESEARCH.md
@.planning/phases/02-scenes-lineage-completion/02-PATTERNS.md
@.claude/rules/proto-doc-comments.md
@.claude/rules/plugin-manifest.md
@.claude/rules/grpc-errors.md
</context>

## Artifacts this phase produces (Plan 03)

New/changed symbols introduced here — exclude from any drift/source-grounding pass:

- `api/proto/holomush/scene/v1/scene.proto` — new RPCs on `SceneService`:
  `MuteScene(MuteSceneRequest) returns (MuteSceneResponse)`,
  `SetSceneNotifyPref(SetSceneNotifyPrefRequest) returns (SetSceneNotifyPrefResponse)`,
  `GetSceneNotifyPref(GetSceneNotifyPrefRequest) returns (GetSceneNotifyPrefResponse)` (the global-pref READ the Plan 04 checker consumes — review Finding 2),
  `ListMutedScenes(ListMutedScenesRequest) returns (ListMutedScenesResponse)`, and their message types.
  EVERY new request message carries a `character_id` field (populated from the host-vouched actor by the facade/loader) matching the existing plugin RPC convention (EndSceneRequest.character_id at scene.proto:404-410; guarded at service.go:696-704 — review Concern 2). `MuteSceneRequest` ALSO carries `scene_id` (scene-scoped); the three notify-pref requests carry `character_id` ONLY (global/character-self — NO scene_id — review Concern 1).
  ALSO reword the stale `service SceneService` doc comment (scene.proto:22-29) which currently claims "The plugin itself runs NO ABAC engine" — an `evaluator` field already exists (service.go:176-180) and this plan adds mute ABAC (review Finding 5).
  ALSO extend the EXISTING read surface (round-3 Concern 1): add a per-scene `muted` bool field to `CharacterSceneInfo` (scene.proto:899-913) and a top-level `global_notify_enabled` bool to `ListCharacterScenesResponse` (scene.proto:915-920) — both fully doc-commented and Go-grounded. Per-row `muted` propagates to the facade/BFF for free (both re-export `holomush.scene.v1.CharacterSceneInfo` by reference — sceneaccess.proto ListMyScenesResponse:260-263, web.proto WebListMyScenesResponse:1059-1062); the top-level `global_notify_enabled` is forwarded explicitly by the facade/BFF in Plan 05.
- `plugins/core-scenes/service.go` — handlers `MuteScene`/`SetSceneNotifyPref`/`GetSceneNotifyPref`/`ListMutedScenes`, PLUS the read-back extension of the existing `ListCharacterScenes` handler (populate `muted` per row + `global_notify_enabled`, fail-OPEN on store error — round-3 Concern 1, Task 4).
  All four FIRST apply the actor-metadata `character_id` guard (PermissionDenied when the vouched actor is a character whose id != req.GetCharacterId(), mirroring EndScene service.go:696-704 — review Concern 2). Authorization then splits: `MuteScene` self-enforces `evaluator.Evaluate("mute", "scene:"+req.GetSceneId())` (SCENE-scoped, one policy family for mute+unmute); `SetSceneNotifyPref`/`GetSceneNotifyPref`/`ListMutedScenes` are CHARACTER-SELF-scoped reads/writes whose only authorization is the character_id↔actor-metadata guard (NO scene:<id> Evaluate — review Concern 1). All forward to the Plan 02 store.
- Generated bindings under `pkg/proto/holomush/scene/v1/` (via `task proto`) — committed in the same change (review suggestion: generated churn visible in files_modified).
- `plugins/core-scenes/commands.go` — `case "mute"` and `case "unmute"` in the subcommand dispatch, via the existing `gated` helper.
- `plugins/core-scenes/plugin.yaml` — participant-gated DSL policy for `action in ["mute","unmute"]`.

<!-- planner-discipline-allow: NO ABAC engine -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SceneService MuteScene/SetSceneNotifyPref/GetSceneNotifyPref/ListMutedScenes RPCs + handlers</name>
  <files>api/proto/holomush/scene/v1/scene.proto, plugins/core-scenes/service.go</files>
  <read_first>
    - api/proto/holomush/scene/v1/scene.proto (existing SceneService RPCs — EndScene/PauseScene precedent at :59-70; mirror request/response shape and doc-comment density)
    - api/proto/holomush/scene/v1/scene.proto:22-29 (the stale `service SceneService` doc comment claiming "The plugin itself runs NO ABAC engine (SceneServiceImpl holds no policy engine)" — to reword; review Finding 5)
    - plugins/core-scenes/service.go:176-180 (the `evaluator pluginsdk.HostEvaluator` field that DOES exist and gates end/pause/resume + now mute — the ground truth the reworded comment must reflect; review Finding 5)
    - plugins/core-scenes/service.go:712 (the `evaluator.Evaluate("end", "scene:"+req.GetSceneId())` Layer-2 gate — the exact ABAC shape MuteScene mirrors; RESEARCH Code Examples)
    - plugins/core-scenes/service.go:696-704 (the `pluginsdk.ActorMetadataFromIncomingContext(ctx)` guard: reject with PermissionDenied when the vouched actor is a character whose id != req.GetCharacterId() — the character_id↔vouched-actor match ALL four new handlers apply; the ONLY authorization the character-self notify-pref reads/writes need — review Concern 1/2)
    - plugins/core-scenes/store.go (Plan 02 methods SetSceneMute/SetSceneNotifyPref/GetSceneNotifyPref/ListMutedScenes this handler forwards to)
    - .claude/rules/proto-doc-comments.md (every element needs a Go-grounded doc comment; name-echo gate)
    - .claude/rules/grpc-errors.md (opaque status errors past the boundary; //nolint:wrapcheck on pass-through)
  </read_first>
  <behavior>
    - Any of the four RPCs called with a request `character_id` that contradicts the host-vouched actor metadata returns PermissionDenied before any store write (review Concern 2, mirroring EndScene service.go:696-704).
    - MuteScene by a participant (scene-scoped Evaluate("mute", "scene:"+scene_id) allow) persists the mute (store.SetSceneMute true) and returns success.
    - UnMute path (MuteScene with muted=false, or a symmetric handling) clears it (store.SetSceneMute false).
    - MuteScene by a non-participant returns a PermissionDenied (Evaluate deny), no store write.
    - SetSceneNotifyPref persists the per-character global pref (store.SetSceneNotifyPref) — CHARACTER-SELF-scoped: it does NOT call GetSceneId() / Evaluate scene:<id>; the caller may only write its OWN pref (character_id guard — review Concern 1).
    - GetSceneNotifyPref returns the caller's persisted global notify-enabled flag (default enabled=true when no row), character-self-scoped — the READ the Plan 04 suppression checker consumes (review Finding 2).
    - ListMutedScenes returns the caller's muted scene ids (store.ListMutedScenes), character-self-scoped (no scene:<id> Evaluate).
    - Infra failure on Evaluate for the scene-scoped MuteScene write fails closed (deny), consistent with default-deny ABAC.
  </behavior>
  <action>Add the RPCs to SceneService in scene.proto with fully documented, Go-grounded doc comments and request/response messages. EVERY new request message carries a `character_id` field (string), matching the existing plugin RPC convention — EndSceneRequest.character_id at scene.proto:404-410, guarded at service.go:696-704 (review Concern 2): `MuteSceneRequest` (character_id + scene_id + a muted bool), `SetSceneNotifyPrefRequest` (character_id + an enabled bool — NO scene_id), `GetSceneNotifyPrefRequest` (character_id ONLY; GetSceneNotifyPrefResponse carries the enabled bool + mode — the READ the Plan 04 checker consumes, review Finding 2), and `ListMutedScenesRequest` (character_id ONLY). ALSO reword the stale `service SceneService` doc comment at scene.proto:22-29 (review Finding 5): it currently asserts "The plugin itself runs NO ABAC engine (SceneServiceImpl holds no policy engine)", but an `evaluator` field already exists (service.go:176-180) gating end/pause/resume and now mute/unmute — reword it, grounded in the handler, to state the plugin runs a host-injected ABAC evaluator for its lifecycle + mute gates (keep `task lint:proto` green; no name-echo). Run `task proto && task web:generate` and commit the generated bindings. Implement the handlers in service.go: EVERY handler FIRST applies the actor-metadata guard mirroring EndScene (service.go:696-704) — via `pluginsdk.ActorMetadataFromIncomingContext(ctx)`, reject with PermissionDenied when the vouched actor is a character whose id != req.GetCharacterId() (review Concern 2). Then authorization splits by scope: `MuteScene` self-enforces `p.evaluator.Evaluate(ctx, "mute", "scene:"+req.GetSceneId())` (SCENE-scoped, mute action for both mute and unmute; keep one policy family), fails closed on evaluator error, forwards to store.SetSceneMute, opaque status errors per grpc-errors.md. `SetSceneNotifyPref`/`GetSceneNotifyPref`/`ListMutedScenes` are CHARACTER-SELF-scoped: they MUST NOT call req.GetSceneId() and MUST NOT Evaluate a `scene:<id>` resource (a global/character pref has no scene id — review Concern 1); their sole authorization is the character_id↔actor-metadata guard above, then forward to store.SetSceneNotifyPref / store.GetSceneNotifyPref / store.ListMutedScenes for the caller's own character. Write the handler unit test FIRST: for MuteScene use the participant/non-participant ABAC test engines (policytest.GrantEngine / DenyAll); for the notify-pref RPCs assert the character_id↔actor-metadata guard (matching metadata → allowed self-read/write; mismatched metadata → PermissionDenied, no store touch) and GetSceneNotifyPref's default-enabled return, RED then GREEN.</action>
  <verify>
    <automated>task lint:proto && task test -- ./plugins/core-scenes/...</automated>
  </verify>
  <acceptance_criteria>
    - `task lint:proto` exits 0 (proto doc-comment + name-echo gates pass), INCLUDING the reworded SceneService service comment (review Finding 5).
    - `rg -n "rpc (MuteScene|SetSceneNotifyPref|GetSceneNotifyPref|ListMutedScenes)" api/proto/holomush/scene/v1/scene.proto` shows all four.
    - `rg -n "character_id" api/proto/holomush/scene/v1/scene.proto` shows a character_id field on MuteSceneRequest, SetSceneNotifyPrefRequest, GetSceneNotifyPrefRequest, and ListMutedScenesRequest (review Concern 2); the three notify-pref requests carry NO scene_id (review Concern 1).
    - `rg -n "ActorMetadataFromIncomingContext" plugins/core-scenes/service.go` shows the character_id↔actor-metadata guard applied in the new handlers (Concern 2).
    - `rg -n "NO ABAC engine" api/proto/holomush/scene/v1/scene.proto` returns NO match (stale comment reworded — Finding 5).
    - `rg -n "Evaluate\(ctx, \"mute\"" plugins/core-scenes/service.go` shows the SCENE-scoped Layer-2 gate in the MuteScene handler; the SetSceneNotifyPref/GetSceneNotifyPref/ListMutedScenes handlers do NOT Evaluate a `scene:<id>` resource (review Concern 1).
    - Unit test proves: MuteScene participant→persist, non-participant→PermissionDenied (no write); notify-pref RPCs with mismatched actor metadata→PermissionDenied (no store touch); GetSceneNotifyPref returns enabled=true by default.
    - `task test -- ./plugins/core-scenes/...` exits 0.
  </acceptance_criteria>
  <done>The plugin SceneService exposes mute/notify-pref/get-notify-pref/list-muted RPCs, each carrying a character_id guarded against the vouched actor (Concern 2); MuteScene is scene-scoped participant-gated, the notify-pref RPCs are character-self-scoped (no scene:<id> — Concern 1); GetSceneNotifyPref gives the global pref a consumer (Finding 2); the stale "runs NO ABAC engine" service comment is reworded (Finding 5); proto lints clean; handler tests green.</done>
</task>

<task type="auto">
  <name>Task 2: telnet `scene mute`/`unmute` subcommand cases</name>
  <files>plugins/core-scenes/commands.go</files>
  <read_first>
    - plugins/core-scenes/commands.go:463-538 (the `gated` helper at :463-477 — reuse verbatim, fails closed when evaluator nil; the end/pause/resume dispatch cases at :479-538 to mirror; the usage strings + default known-subcommands list to update)
    - plugins/core-scenes/commands.go (normalizeSceneID / resolveSceneRef — reuse for the `#X` argument, ASVS V5 input validation)
    - .planning/phases/02-scenes-lineage-completion/02-PATTERNS.md (Slice B command pattern; Reconciliation Landmine: do NOT touch validActions)
    - internal/command/types.go:108-197 (CONFIRM mute/unmute are NOT needed here — engine-Evaluate actions like end/pause are absent from validActions; this read is to VERIFY the landmine, not to edit)
  </read_first>
  <action>Add `case "mute": return gated("mute", "mute", sceneResourceRef, p.handleMute)` and a symmetric `case "unmute"` to the subcommand dispatch switch in commands.go, mirroring the `end`/`pause` cases. Implement `handleMute`/`handleUnmute` to resolve the `#X` argument via `resolveSceneRef`/`normalizeSceneID`, then call the store (in-process) SetSceneMute with muted true/false. Update the two usage strings and the `default` known-subcommands list to include mute/unmute. Do NOT modify internal/command/types.go validActions/validResourceTypes (Reconciliation Landmine — mute is an engine-Evaluate action, not a command capability). Add a command-handler test asserting a participant mutes and a non-participant is denied (via the gated ABAC path).</action>
  <verify>
    <automated>task test -- ./plugins/core-scenes/...</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "case \"mute\"|case \"unmute\"" plugins/core-scenes/commands.go` matches.
    - `git diff --stat internal/command/types.go` shows NO change (landmine respected).
    - The usage string in commands.go includes `mute` and `unmute`.
    - `task test -- ./plugins/core-scenes/...` exits 0.
  </acceptance_criteria>
  <done>`scene mute #X` / `scene unmute #X` route through the gated ABAC helper to the store; validActions is untouched; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: mute/unmute participant-gated DSL policy</name>
  <files>plugins/core-scenes/plugin.yaml</files>
  <read_first>
    - plugins/core-scenes/plugin.yaml:273-361 (the participant-gated policies for end/resume/read/write/leave/invite/publish — the exact DSL shape `permit(principal is character, action in [...], resource is scene) when { principal.id in resource.scene.participants }`)
    - plugins/core-scenes/plugin.yaml:252 (the `execute-scene-commands` Layer-1 policy already covering `scene` — confirm it needs no change for a new subcommand)
    - .claude/rules/plugin-manifest.md (policies block; no actions: change needed for engine-Evaluate actions)
    - .planning/phases/02-scenes-lineage-completion/02-PATTERNS.md (Slice B ABAC: gate mute on membership, not ownership — mute is a per-participant control)
  </read_first>
  <action>Add a participant-gated permit to the scene plugin's ABAC policies in plugin.yaml: `permit(principal is character, action in ["mute","unmute"], resource is scene) when { principal.id in resource.scene.participants };` — mirroring the resume/leave membership policies (fail-closed default-deny; gate on participation, not ownership). This policy authorizes ONLY the scene-scoped mute/unmute action; the character-self notify-pref RPCs (SetSceneNotifyPref/GetSceneNotifyPref/ListMutedScenes) are NOT scene-scoped and need no participant policy — their authorization is the request character_id ↔ host-vouched actor-metadata guard in the handler (review Concern 1/2), so do NOT add a `scene:<id>` policy for them. Do NOT add mute/unmute to any `actions:` manifest list (engine-Evaluate actions need only a DSL policy). Add a fail-closed policy test if the repo has a policy-evaluation test for scene actions (mirror the existing end/resume policy test).</action>
  <verify>
    <automated>task lint && task test -- ./plugins/core-scenes/...</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "action in \[\"mute\",\"unmute\"\]|action in \[\"mute\"" plugins/core-scenes/plugin.yaml` matches a participant-gated permit.
    - `task lint` exits 0 (plugin.schema.json + manifest gates pass).
    - The policy gates on `resource.scene.participants` (membership), consistent with resume/leave.
  </acceptance_criteria>
  <done>A participant-gated mute/unmute DSL policy authorizes the new command and RPCs, fail-closed for non-participants; lint green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: read-back — CharacterSceneInfo.muted + ListCharacterScenesResponse.global_notify_enabled (round-3 Concern 1)</name>
  <files>api/proto/holomush/scene/v1/scene.proto, plugins/core-scenes/service.go, plugins/core-scenes/service_test.go</files>
  <read_first>
    - api/proto/holomush/scene/v1/scene.proto:899-913 (CharacterSceneInfo — scene/role/last_activity_ms/entry_count only today; add a `muted` bool field with the NEXT free field number, fully doc-commented) and :915-920 (ListCharacterScenesResponse — add a top-level `global_notify_enabled` bool)
    - plugins/core-scenes/service.go:650-673 (the ListCharacterScenes handler this task extends — it maps store CharacterSceneResult rows to CharacterSceneInfo; it trusts req.GetCharacterId() as host-trusted, which is the self-scope — the facade stamps it from the verified owned char, sceneaccess_service.go:275-277)
    - plugins/core-scenes/service.go:118-123 (the service `store` interface already gains GetSceneNotifyPref/ListMutedScenes in Task 1 — reuse the same handle here; no new store method)
    - plugins/core-scenes/store.go (Plan 02 GetSceneNotifyPref / ListMutedScenes — the reads this handler consumes)
    - plugins/core-scenes/service_test.go:2620-2695 (the existing ListCharacterScenes unit tests + fakeStore.ListCharacterScenes at :692-695 — extend the fake with GetSceneNotifyPref/ListMutedScenes stubs and mirror the table)
    - .claude/rules/proto-doc-comments.md (every new proto field needs a Go-grounded doc comment; name-echo gate)
    - .claude/rules/logging.md (context-carrying logging for the fail-open WARN)
  </read_first>
  <behavior>
    - ListCharacterScenes for a character with muted scenes stamps `muted=true` on exactly those CharacterSceneInfo rows whose scene_id is in store.ListMutedScenes(req.character_id); all other rows carry `muted=false`.
    - The response's top-level `global_notify_enabled` reflects store.GetSceneNotifyPref(req.character_id) (default true when the character has no prefs row — the notify-ON default from Plan 02).
    - This is the character's OWN state only: the handler reads mute/notify for req.GetCharacterId() (the facade-verified/host-trusted acting character), never another character's — no new ABAC action, reuses the existing ListMyScenes self-auth (round-3 Concern 1).
    - Fail-OPEN for display: if store.ListMutedScenes OR store.GetSceneNotifyPref returns an error, the handler WARN-logs and defaults (muted=false on every row, global_notify_enabled=true) but STILL returns the full scene list — a prefs-read failure MUST NOT fail the whole list.
  </behavior>
  <action>Addresses round-3 Concern 1 (the web mute/prefs UI had writes but no typed read/snapshot). Add a `muted` bool field to `CharacterSceneInfo` and a top-level `global_notify_enabled` bool to `ListCharacterScenesResponse` in scene.proto, each with a fully Go-grounded doc comment (no name-echo): `muted` documents "true when this character has muted this scene (store scene_notify_prefs row); false otherwise — the persisted per-scene mute the workspace renders on reload"; `global_notify_enabled` documents "the character's persisted global notify preference (default true); false when the character turned scene notifications off — read from scene_notify_prefs". Run `task proto && task web:generate` and commit the regenerated scene bindings (pkg + web scene_pb.ts). Extend the existing `ListCharacterScenes` handler in service.go (service.go:650-673): after the store.ListCharacterScenes call, fetch `mutedIDs, mErr := s.store.ListMutedScenes(ctx, req.GetCharacterId())` and `enabled, _, gErr := s.store.GetSceneNotifyPref(ctx, req.GetCharacterId())`; build a set from mutedIDs and stamp `Muted:` per CharacterSceneInfo row (true when the scene id is in the set); set the response `GlobalNotifyEnabled: enabled`. On a store error from EITHER read, WARN-log with context (errutil/slog *Context) and default fail-OPEN — muted=false on every row, global_notify_enabled=true — but still return the full scenes list (do NOT convert the prefs-read error into a handler error; the list read already succeeded). Do NOT add an actor-metadata guard here — ListCharacterScenes is already a host-trusted self-scoped read (the facade stamps req.character_id from the verified owned character), so the mute/notify piggyback on that same self-scope; adding a new ABAC action would be wrong (round-3 Concern 1). Write the unit test FIRST (extend service_test.go's ListCharacterScenes table + fakeStore with GetSceneNotifyPref/ListMutedScenes stubs): assert muted stamped on exactly the muted rows, global_notify_enabled reflects the pref (and defaults true with no row), and a store-error on the prefs reads yields defaults + a non-error full list (fail-OPEN). RED then GREEN.</action>
  <verify>
    <automated>task lint:proto && task test -- ./plugins/core-scenes/... -run 'ListCharacterScenes|Mute'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "bool muted" api/proto/holomush/scene/v1/scene.proto` shows the `muted` field on CharacterSceneInfo; `rg -n "global_notify_enabled" api/proto/holomush/scene/v1/scene.proto` shows the top-level field on ListCharacterScenesResponse.
    - `task lint:proto` exits 0 (both new fields carry Go-grounded doc comments; name-echo gate passes).
    - `rg -n "ListMutedScenes|GetSceneNotifyPref" plugins/core-scenes/service.go` shows both reads wired into the ListCharacterScenes handler.
    - The ListCharacterScenes handler adds NO ActorMetadataFromIncomingContext / Evaluate call (it stays the existing host-trusted self-scoped read — round-3 Concern 1): `git diff` shows no new ABAC gate in ListCharacterScenes.
    - Unit test proves: muted stamped on exactly the muted rows; global_notify_enabled reflects the pref (default true, no row); a prefs-read store error → defaults (muted=false, enabled=true) + full non-error list (fail-OPEN).
    - Regenerated scene bindings (pkg `*.pb.go` + web `scene_pb.ts`) committed, no stale diff.
    - `task test -- ./plugins/core-scenes/...` exits 0.
  </acceptance_criteria>
  <done>The self-scoped scene list carries the character's own persisted mute (per row) + global notify (top-level) state, fail-OPEN on a prefs-read error, no new ABAC action; the web workspace snapshot (Plan 05) now has a typed source of truth to render mute/notify state on reload (round-3 Concern 1 closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| character (telnet CLI / web) → SceneService | A caller asserts an intent to mute a scene; the plugin must confirm the caller participates before mutating. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Elevation of privilege | mute/unmute on a scene the caller is not in | high | mitigate | Layer-2 `Evaluate("mute", "scene:"+id)` against a new participant-gated DSL policy; fail-closed (default-deny) on evaluator infra error. Behavior-tested: non-participant → PermissionDenied, no store write. |
| T-02-08 | Tampering | telnet command bypassing ABAC | high | mitigate | Command routes through the `gated` helper, which fails closed when the evaluator is nil and always evaluates before the store write. |
| T-02-09 | Spoofing | forged character id on the RPC | medium | mitigate | Each RPC carries a request `character_id` cross-checked against the host-vouched actor metadata — a mismatch returns PermissionDenied before any store touch (mirroring EndScene service.go:696-704; review Concern 2). This guard IS the self-scope for the global/character-self notify-pref RPCs (review Concern 1): a character can only read/write its OWN pref because req.character_id must equal the vouched acting character. |
| T-02-09b | Information disclosure | ListCharacterScenes leaking another character's mute/notify state | medium | mitigate | The read-back (round-3 Concern 1) reports mute/notify for req.GetCharacterId() ONLY — the same facade-verified/host-trusted acting character the existing scene list is scoped to (facade ownedCharacter → stamps character_id, sceneaccess_service.go:265-277). No cross-character read: the handler never queries another character's prefs. The read is preference display only (fail-OPEN, no access-control decision), so a store error degrades to defaults rather than leaking. |
| T-02-SC | Tampering | package installs | low | accept | No package-manager installs (in-tree Go + proto only; bindings regenerated, not added). |
</threat_model>

<verification>
- `task lint:proto` green (doc comments, including the reworded SceneService service comment — Finding 5, and the new CharacterSceneInfo.muted / ListCharacterScenesResponse.global_notify_enabled fields — round-3 Concern 1); `task test -- ./plugins/core-scenes/...` green.
- `task lint` green (manifest schema, DSL policy).
- Generated `pkg/proto/holomush/scene/v1/*.pb.go` + web `scene_pb.ts` regenerated and committed in the same change.
- `git diff internal/command/types.go` empty (Reconciliation Landmine).
</verification>

<success_criteria>
A scene participant can `scene mute #X` / `unmute #X` on telnet and via the plugin RPCs; a non-participant is denied fail-closed; the controls persist through the Plan 02 store; GetSceneNotifyPref exposes the global pref for Plan 04 to enforce (Finding 2); the self-scoped scene list carries the character's own persisted mute (per row) + global notify (top-level) state so the web workspace snapshot can render it on reload, fail-OPEN on a prefs-read error (round-3 Concern 1); the stale "runs NO ABAC engine" proto comment is corrected (Finding 5); no validActions change.
</success_criteria>

<output>
Create `.planning/phases/02-scenes-lineage-completion/02-03-SUMMARY.md` when done.
</output>
