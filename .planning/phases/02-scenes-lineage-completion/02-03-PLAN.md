---
phase: 02-scenes-lineage-completion
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - api/proto/holomush/scene/v1/scene.proto
  - plugins/core-scenes/service.go
  - plugins/core-scenes/commands.go
  - plugins/core-scenes/plugin.yaml
autonomous: true
requirements: [SCENEFWD-02]
must_haves:
  truths:
    - "`scene mute #X` and `scene unmute #X` telnet subcommands persist the mute and are authorized by a Layer-2 participant-gated ABAC policy — only a participant of #X may mute it (D-04, threat mitigation)."
    - "The plugin SceneService exposes MuteScene, SetSceneNotifyPref, and ListMutedScenes RPCs that self-enforce ABAC and forward to the Plan 02 store."
    - "A non-participant's mute attempt is denied fail-closed (default-deny)."
  artifacts:
    - api/proto/holomush/scene/v1/scene.proto
    - plugins/core-scenes/service.go
    - plugins/core-scenes/commands.go
    - plugins/core-scenes/plugin.yaml
  key_links:
    - "commands.go dispatch switch → gated(\"mute\", …) → p.handleMute → store.SetSceneMute"
    - "SceneService.MuteScene/ListMutedScenes → evaluator.Evaluate(\"mute\", \"scene:\"+id) → store"
    - "plugin.yaml participant DSL policy authorizes action in [\"mute\",\"unmute\"]"
  prohibitions:
    - "MUST NOT add `mute`/`unmute` to validActions/validResourceTypes in internal/command/types.go — they are engine-Evaluate actions like end/pause/resume, NOT command-capability actions (RESEARCH Reconciliation Landmine)."
    - "MUST NOT re-declare any existing event type or crypto.emits entry (no plugin.yaml emit changes here)."
    - "Every new proto element MUST carry a Go-grounded doc comment (no name-echo) — proto-doc-comments.md."
---

<objective>
Give a scene participant the controls to mute a scene's notifications: telnet
`scene mute #X` / `scene unmute #X` subcommands (new — they do not exist today)
and the plugin SceneService RPCs (MuteScene, SetSceneNotifyPref, ListMutedScenes)
that the web slice (Plan 05) and the core suppression checker (Plan 04) consume.
All authorized by a new Layer-2 participant-gated ABAC policy, following the
`end`/`pause`/`resume` engine-Evaluate precedent.

Purpose: D-04's notification controls — persisted, participant-gated, on both the
telnet command path (this plan) and the web typed-RPC path (Plan 05, which reuses
these same plugin RPCs via the facade).
Output: plugin scene.proto RPCs + handlers, `scene mute`/`unmute` command cases,
and the mute/unmute DSL policy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-scenes-lineage-completion/02-CONTEXT.md
@.planning/phases/02-scenes-lineage-completion/02-RESEARCH.md
@.planning/phases/02-scenes-lineage-completion/02-PATTERNS.md
@.claude/rules/proto-doc-comments.md
@.claude/rules/plugin-manifest.md
@.claude/rules/grpc-errors.md
</context>

## Artifacts this phase produces (Plan 03)

New/changed symbols introduced here — exclude from any drift/source-grounding pass:

- `api/proto/holomush/scene/v1/scene.proto` — new RPCs on `SceneService`:
  `MuteScene(MuteSceneRequest) returns (MuteSceneResponse)`,
  `SetSceneNotifyPref(SetSceneNotifyPrefRequest) returns (SetSceneNotifyPrefResponse)`,
  `ListMutedScenes(ListMutedScenesRequest) returns (ListMutedScenesResponse)`, and their message types.
- `plugins/core-scenes/service.go` — handlers `MuteScene`/`SetSceneNotifyPref`/`ListMutedScenes`
  self-enforcing `evaluator.Evaluate("mute"|"unmute", "scene:"+id)` and forwarding to the Plan 02 store.
- `plugins/core-scenes/commands.go` — `case "mute"` and `case "unmute"` in the subcommand dispatch, via the existing `gated` helper.
- `plugins/core-scenes/plugin.yaml` — participant-gated DSL policy for `action in ["mute","unmute"]`.
- Generated bindings under `pkg/proto/holomush/scene/v1/` (via `task proto`).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SceneService MuteScene/SetSceneNotifyPref/ListMutedScenes RPCs + handlers</name>
  <files>api/proto/holomush/scene/v1/scene.proto, plugins/core-scenes/service.go</files>
  <read_first>
    - api/proto/holomush/scene/v1/scene.proto (existing SceneService RPCs — EndScene/PauseScene precedent at :59-70; mirror request/response shape and doc-comment density)
    - plugins/core-scenes/service.go:712 (the `evaluator.Evaluate("end", "scene:"+req.GetSceneId())` Layer-2 gate — the exact ABAC shape MuteScene mirrors; RESEARCH Code Examples)
    - plugins/core-scenes/store.go (Plan 02 methods SetSceneMute/SetSceneNotifyPref/ListMutedScenes this handler forwards to)
    - .claude/rules/proto-doc-comments.md (every element needs a Go-grounded doc comment; name-echo gate)
    - .claude/rules/grpc-errors.md (opaque status errors past the boundary; //nolint:wrapcheck on pass-through)
  </read_first>
  <behavior>
    - MuteScene by a participant persists the mute (store.SetSceneMute true) and returns success.
    - UnMute path (MuteScene with muted=false, or a symmetric handling) clears it (store.SetSceneMute false).
    - MuteScene by a non-participant returns a PermissionDenied (Evaluate deny), no store write.
    - SetSceneNotifyPref persists the per-character global pref (store.SetSceneNotifyPref).
    - ListMutedScenes returns the caller's muted scene ids (store.ListMutedScenes).
    - Infra failure on Evaluate fails closed (deny), consistent with default-deny ABAC.
  </behavior>
  <action>Add the three RPCs to SceneService in scene.proto with fully documented, Go-grounded doc comments and request/response messages (MuteSceneRequest carries scene_id + a muted bool; SetSceneNotifyPrefRequest carries an enabled bool; ListMutedScenesRequest is character-scoped via the host-vouched dispatch context). Run `task proto && task web:generate` and commit the generated bindings. Implement the handlers in service.go mirroring the EndScene handler: each self-enforces `p.evaluator.Evaluate(ctx, "mute", "scene:"+req.GetSceneId())` (mute action for both mute and unmute; or `"unmute"` if you split — keep one policy family), fails closed on evaluator error, forwards to the Plan 02 store methods, and returns opaque status errors per grpc-errors.md. Write the handler unit test FIRST using the participant/non-participant ABAC test engines (policytest.GrantEngine / DenyAll), RED then GREEN.</action>
  <verify>
    <automated>task lint:proto && task test -- ./plugins/core-scenes/...</automated>
  </verify>
  <acceptance_criteria>
    - `task lint:proto` exits 0 (proto doc-comment + name-echo gates pass).
    - `rg -n "rpc (MuteScene|SetSceneNotifyPref|ListMutedScenes)" api/proto/holomush/scene/v1/scene.proto` shows all three.
    - `rg -n "Evaluate\(ctx, \"mute\"" plugins/core-scenes/service.go` shows the Layer-2 gate in the handler.
    - Unit test proves participant→persist, non-participant→PermissionDenied (no write).
    - `task test -- ./plugins/core-scenes/...` exits 0.
  </acceptance_criteria>
  <done>The plugin SceneService exposes mute/notify-pref/list-muted RPCs, ABAC-gated to participants, forwarding to the store; proto lints clean; handler tests green.</done>
</task>

<task type="auto">
  <name>Task 2: telnet `scene mute`/`unmute` subcommand cases</name>
  <files>plugins/core-scenes/commands.go</files>
  <read_first>
    - plugins/core-scenes/commands.go:463-538 (the `gated` helper at :463-477 — reuse verbatim, fails closed when evaluator nil; the end/pause/resume dispatch cases at :479-538 to mirror; the usage strings + default known-subcommands list to update)
    - plugins/core-scenes/commands.go (normalizeSceneID / resolveSceneRef — reuse for the `#X` argument, ASVS V5 input validation)
    - .planning/phases/02-scenes-lineage-completion/02-PATTERNS.md (Slice B command pattern; Reconciliation Landmine: do NOT touch validActions)
    - internal/command/types.go:108-197 (CONFIRM mute/unmute are NOT needed here — engine-Evaluate actions like end/pause are absent from validActions; this read is to VERIFY the landmine, not to edit)
  </read_first>
  <action>Add `case "mute": return gated("mute", "mute", sceneResourceRef, p.handleMute)` and a symmetric `case "unmute"` to the subcommand dispatch switch in commands.go, mirroring the `end`/`pause` cases. Implement `handleMute`/`handleUnmute` to resolve the `#X` argument via `resolveSceneRef`/`normalizeSceneID`, then call the store (in-process) SetSceneMute with muted true/false. Update the two usage strings and the `default` known-subcommands list to include mute/unmute. Do NOT modify internal/command/types.go validActions/validResourceTypes (Reconciliation Landmine — mute is an engine-Evaluate action, not a command capability). Add a command-handler test asserting a participant mutes and a non-participant is denied (via the gated ABAC path).</action>
  <verify>
    <automated>task test -- ./plugins/core-scenes/...</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "case \"mute\"|case \"unmute\"" plugins/core-scenes/commands.go` matches.
    - `git diff --stat internal/command/types.go` shows NO change (landmine respected).
    - The usage string in commands.go includes `mute` and `unmute`.
    - `task test -- ./plugins/core-scenes/...` exits 0.
  </acceptance_criteria>
  <done>`scene mute #X` / `scene unmute #X` route through the gated ABAC helper to the store; validActions is untouched; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: mute/unmute participant-gated DSL policy</name>
  <files>plugins/core-scenes/plugin.yaml</files>
  <read_first>
    - plugins/core-scenes/plugin.yaml:273-361 (the participant-gated policies for end/resume/read/write/leave/invite/publish — the exact DSL shape `permit(principal is character, action in [...], resource is scene) when { principal.id in resource.scene.participants }`)
    - plugins/core-scenes/plugin.yaml:252 (the `execute-scene-commands` Layer-1 policy already covering `scene` — confirm it needs no change for a new subcommand)
    - .claude/rules/plugin-manifest.md (policies block; no actions: change needed for engine-Evaluate actions)
    - .planning/phases/02-scenes-lineage-completion/02-PATTERNS.md (Slice B ABAC: gate mute on membership, not ownership — mute is a per-participant control)
  </read_first>
  <action>Add a participant-gated permit to the scene plugin's ABAC policies in plugin.yaml: `permit(principal is character, action in ["mute","unmute"], resource is scene) when { principal.id in resource.scene.participants };` — mirroring the resume/leave membership policies (fail-closed default-deny; gate on participation, not ownership). Do NOT add mute/unmute to any `actions:` manifest list (engine-Evaluate actions need only a DSL policy). Add a fail-closed policy test if the repo has a policy-evaluation test for scene actions (mirror the existing end/resume policy test).</action>
  <verify>
    <automated>task lint && task test -- ./plugins/core-scenes/...</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "action in \[\"mute\",\"unmute\"\]|action in \[\"mute\"" plugins/core-scenes/plugin.yaml` matches a participant-gated permit.
    - `task lint` exits 0 (plugin.schema.json + manifest gates pass).
    - The policy gates on `resource.scene.participants` (membership), consistent with resume/leave.
  </acceptance_criteria>
  <done>A participant-gated mute/unmute DSL policy authorizes the new command and RPCs, fail-closed for non-participants; lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| character (telnet CLI / web) → SceneService | A caller asserts an intent to mute a scene; the plugin must confirm the caller participates before mutating. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Elevation of privilege | mute/unmute on a scene the caller is not in | high | mitigate | Layer-2 `Evaluate("mute", "scene:"+id)` against a new participant-gated DSL policy; fail-closed (default-deny) on evaluator infra error. Behavior-tested: non-participant → PermissionDenied, no store write. |
| T-02-08 | Tampering | telnet command bypassing ABAC | high | mitigate | Command routes through the `gated` helper, which fails closed when the evaluator is nil and always evaluates before the store write. |
| T-02-09 | Spoofing | forged character id on the RPC | medium | mitigate | Identity comes from the host-vouched dispatch context, not a request-carried character name (per the plugin least-privilege trust model). |
| T-02-SC | Tampering | package installs | low | accept | No package-manager installs (in-tree Go + proto only; bindings regenerated, not added). |
</threat_model>

<verification>
- `task lint:proto` green (doc comments); `task test -- ./plugins/core-scenes/...` green.
- `task lint` green (manifest schema, DSL policy).
- Generated `pkg/proto/holomush/scene/v1/*.pb.go` regenerated and committed in the same change.
- `git diff internal/command/types.go` empty (Reconciliation Landmine).
</verification>

<success_criteria>
A scene participant can `scene mute #X` / `unmute #X` on telnet and via the plugin RPCs; a non-participant is denied fail-closed; the controls persist through the Plan 02 store; no validActions change.
</success_criteria>

<output>
Create `.planning/phases/02-scenes-lineage-completion/02-03-SUMMARY.md` when done.
</output>
