---
phase: 02-scenes-lineage-completion
plan: 06
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - plugins/core-scenes/idle_scheduler.go
  - plugins/core-scenes/idle_scheduler_test.go
  - plugins/core-scenes/store.go
  - plugins/core-scenes/main.go
  - plugins/core-scenes/plugin.yaml
  - docs/architecture/invariants.yaml
  - docs/architecture/invariants.md
autonomous: true
requirements: [SCENEFWD-02]
must_haves:
  truths:
    - "A scene idle past its threshold (game-wide default, per-scene override) auto-transitions active → paused via a ticker sweep (D-06, INV-SCENE-71)."
    - "The idle nudge (scene_idle_nudge) is emitted ONLY when its flag is ON; it is OFF by default (D-06, spec §4.4)."
    - "A paused scene does not re-transition; the sweep tolerates per-row failures without aborting the batch."
  artifacts:
    - plugins/core-scenes/idle_scheduler.go
    - plugins/core-scenes/store.go
    - plugins/core-scenes/main.go
    - docs/architecture/invariants.yaml
  key_links:
    - "idleScheduler.Run → sweep → store.ListScenesIdlePastThreshold → IsValidTransition(active,paused) → transition + (flag-gated) emit scene_idle_nudge"
    - "game-wide idle default (plugin.yaml config:) overridden by the per-scene idle_timeout_secs column"
  prohibitions:
    - "MUST NOT re-declare scene_idle_nudge in crypto.emits / phase4EmitTypes / EmitRegistry — it is already declared; add ONLY the emitter call (Pitfall 3 / INV-PLUGIN-32 set-equality)."
    - "MUST NOT rebuild the idle_timeout_secs column or the event type — wire the existing partial infra (D-06)."
    - "The idle nudge MUST default OFF (spec §4.4)."
---

<objective>
Wire the dormant idle-timeout lifecycle. The `idle_timeout_secs` column and the
`scene_idle_nudge` event type already exist but nothing drives them. This plan
adds a ticker sweep (mirroring publish_scheduler.go) that transitions idle scenes
`active → paused` and, behind an OFF-by-default flag, emits `scene_idle_nudge`.
The nudge event flows through the existing delivery path and renders on telnet
via the Plan 01 SCENE_ACTIVITY nudge (non-focused members) — no new render seam.

Purpose: D-06 — idle-timeout defaults + transitions + optional idle nudge, using
the existing partial infra, not a rebuild.
Output: idle_scheduler.go sweep + store query + game-default config + the emitter
call, and INV-SCENE-71 for the idle transition.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-scenes-lineage-completion/02-CONTEXT.md
@.planning/phases/02-scenes-lineage-completion/02-RESEARCH.md
@.planning/phases/02-scenes-lineage-completion/02-PATTERNS.md
@.claude/rules/event-conventions.md
@.claude/rules/invariants.md
@.claude/rules/logging.md
</context>

## Artifacts this phase produces (Plan 06)

New/changed symbols introduced here — exclude from any drift/source-grounding pass:

- `plugins/core-scenes/idle_scheduler.go` (NEW) — `idleScheduler{ store, interval, now func() time.Time }`
  with `Run(ctx)` (ticker) and `sweep(ctx)` (mirrors publish_scheduler.go), transitioning
  idle scenes active→paused and flag-gated emitting scene_idle_nudge.
- `plugins/core-scenes/store.go` — `ListScenesIdlePastThreshold(ctx, nowNs int64) ([]…, error)`
  filtered on `state='active' AND last_activity_ms + COALESCE(idle_timeout_secs, <default>)*1000 ≤ now`.
- `plugins/core-scenes/main.go` — registers `idleScheduler.Run(ctx)` alongside publishScheduler; adds the flag-gated emitter call.
- `plugins/core-scenes/plugin.yaml` — game-wide idle-default + idle-nudge-enabled (OFF) in the `config:` block.
- `INV-SCENE-71` — idle-transition invariant in docs/architecture/invariants.yaml.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: idle store query + idleScheduler sweep (active→paused)</name>
  <files>plugins/core-scenes/idle_scheduler.go, plugins/core-scenes/idle_scheduler_test.go, plugins/core-scenes/store.go</files>
  <read_first>
    - plugins/core-scenes/publish_scheduler.go (the EXACT analog: Run(ctx) ticker + sweep(ctx), injected `now func() time.Time`, per-row WARN-log without aborting the batch, oops.Code wrap on scan errors; PATTERNS.md Slice C copies the whole shape)
    - plugins/core-scenes/store.go:44-68 (idle_timeout_secs → IdleTimeoutSecs *int nullable column) and :1772-1794 (last_activity_ms already computed for the board — reuse both)
    - plugins/core-scenes/lifecycle.go:17-32 (IsValidTransition(active, paused) — already valid; use it)
    - .claude/rules/logging.md (context-carrying logging in the sweep)
  </read_first>
  <behavior>
    - A scene with state=active and last_activity_ms + idle_timeout*1000 ≤ now is returned by ListScenesIdlePastThreshold and transitioned to paused.
    - A scene not yet past threshold is NOT returned/transitioned (boundary at exactly idle_timeout_secs).
    - A per-scene idle_timeout_secs override beats the game-wide default.
    - A paused scene is never returned (no re-transition).
    - A per-row transition failure WARN-logs and the sweep continues the batch (does not abort).
    - The sweep uses the injected `now` for deterministic tests.
  </behavior>
  <action>Add `ListScenesIdlePastThreshold(ctx, nowNs int64)` to store.go filtered on `state='active' AND last_activity_ms + COALESCE(idle_timeout_secs, <game_default_secs>)*1000 ≤ nowNs`, reusing the existing last_activity_ms and IdleTimeoutSecs column. Create idle_scheduler.go copying publish_scheduler.go's Run/sweep structure with an injected `now func() time.Time`: sweep queries expired scenes, applies IsValidTransition(active,paused) + the state transition per row, WARN-logs per-row failures without aborting, wraps scan errors with oops.Code. Carry the `// pgnanos-exempt: scheduler clock …` comment for the nowNs computation, matching publish_scheduler. Write the sweep unit test FIRST with injected now (past-threshold → paused; boundary exact; override beats default; paused not re-swept; per-row-failure tolerance), RED then GREEN.</action>
  <verify>
    <automated>task test -- ./plugins/core-scenes/... -run Idle</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "func .*idleScheduler.* (Run|sweep)|ListScenesIdlePastThreshold" plugins/core-scenes/*.go` matches.
    - The sweep uses an injected `now func() time.Time` and IsValidTransition(active,paused).
    - Unit test proves: past-threshold→paused, boundary exact, per-scene override, paused-not-re-swept, per-row-failure tolerance.
    - `task test -- ./plugins/core-scenes/...` exits 0.
  </acceptance_criteria>
  <done>A deterministic idle sweep transitions active→paused at threshold (default or per-scene override), fault-tolerant per row.</done>
</task>

<task type="auto">
  <name>Task 2: wire scheduler + flag-gated idle-nudge emitter + game default config</name>
  <files>plugins/core-scenes/main.go, plugins/core-scenes/plugin.yaml, plugins/core-scenes/idle_scheduler.go</files>
  <read_first>
    - plugins/core-scenes/main.go:177-204 (emit registries — phase4EmitTypes at :186 ALREADY lists scene_idle_nudge; do NOT re-add) and :259-265 (publishScheduler construction + `go sched.Run(schedCtx)` — register idleScheduler the same way)
    - plugins/core-scenes/plugin.yaml:37 (config: block for game-wide knobs) and :113,:177,:180 (scene_idle_nudge already declared; :180 notes trigger deferred to holomush-fux3 — THIS plan is that trigger)
    - .claude/rules/event-conventions.md (core.NewEvent() for construction; <plugin>:<verb> wire type is already declared; emitter only)
    - .claude/rules/plugin-manifest.md (config: knobs; do NOT touch crypto.emits)
  </read_first>
  <action>Register `idleScheduler.Run(schedCtx)` in main.go alongside the existing publishScheduler startup (mirror the `go sched.Run(...)` pattern, its own interval). Add two game-wide knobs to the plugin.yaml `config:` block: an idle-timeout default (seconds) and an idle-nudge-enabled flag defaulting OFF. In the sweep, AFTER a successful active→paused transition, if the idle-nudge flag is ON, emit scene_idle_nudge for that scene via the plugin's standard emit path (core.NewEvent(), the already-declared bare `scene_idle_nudge` verb — do NOT re-register in phase4EmitTypes/crypto.emits, Pitfall 3). The emit is behind the flag; default OFF means no emit. Add a test asserting: flag OFF → transition but NO emit; flag ON → transition + one emit.</action>
  <verify>
    <automated>task test -- ./plugins/core-scenes/... && task lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "idleScheduler|go .*Run\(schedCtx\)" plugins/core-scenes/main.go` shows the scheduler registration.
    - `git diff plugins/core-scenes/main.go` shows NO change to phase4EmitTypes / EmitRegistry declarations (Pitfall 3 respected); plugin load does not raise EVENT_TYPE_REGISTRY_MISMATCH.
    - The idle-nudge config knob defaults OFF; the test proves flag OFF → no emit, flag ON → emit.
    - `task lint` exits 0 (plugin.yaml schema).
  </acceptance_criteria>
  <done>The idle scheduler runs in production; the idle nudge emits only when its flag is ON (default OFF); no emit-registry re-declaration.</done>
</task>

<task type="auto">
  <name>Task 3: register INV-SCENE-71 (idle transition)</name>
  <files>docs/architecture/invariants.yaml, docs/architecture/invariants.md</files>
  <read_first>
    - docs/architecture/invariants.yaml (INV-SCENE entries; 70 is taken by Plan 01 — allocate 71; copy an existing INV-SCENE entry's shape)
    - .claude/rules/invariants.md (define → bind → regenerate; bound needs a genuine // Verifies: assertion; else pending)
    - cmd/inv-render/main.go (`go run ./cmd/inv-render`)
  </read_first>
  <action>Add INV-SCENE-71 to docs/architecture/invariants.yaml: scope SCENE, origin_spec this phase / scenes master spec §4.4/§11, summary "an active scene idle past its (game-default or per-scene-override) idle_timeout_secs auto-transitions to paused and does not re-transition once paused". Set `binding: bound` with `asserted_by:` the Task 1 sweep test IF it genuinely asserts the transition-at-threshold + no-re-transition; add `// Verifies: INV-SCENE-71` above that test. Otherwise `binding: pending` + a coverage bead. Regenerate docs/architecture/invariants.md via `go run ./cmd/inv-render`.</action>
  <verify>
    <automated>go run ./cmd/inv-render && git diff --exit-code docs/architecture/invariants.md; task test -- -run 'TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted' ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "INV-SCENE-71" docs/architecture/invariants.yaml` matches with scope SCENE.
    - `go run ./cmd/inv-render` leaves invariants.md with no uncommitted diff.
    - Registry meta-tests green; if bound, a genuine `// Verifies: INV-SCENE-71` sits above the sweep transition assertion.
  </acceptance_criteria>
  <done>INV-SCENE-71 registered and invariants.md regenerated in the same change; meta-tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler → scene lifecycle | The sweep mutates scene state (active→paused) autonomously; it must only transition legally-eligible scenes and must not leak state via the optional nudge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-17 | Information disclosure | idle nudge leaking scene content/existence to non-participants | high | mitigate | scene_idle_nudge is sensitivity: never (plaintext by design) and reaches members via the same subscription; a non-focused member renders the content-free `[>GAME: …]` nudge (Plan 01), a non-participant never receives the event (INV-SCENE-62). |
| T-02-18 | Tampering | illegal or repeated state transition | medium | mitigate | IsValidTransition(active,paused) gates each row; paused scenes are excluded from the sweep query (no re-transition) — INV-SCENE-71. |
| T-02-19 | Denial of service | a bad row aborting the whole sweep | low | mitigate | Per-row WARN-log-and-continue (publish_scheduler precedent); one failure never stalls the batch. |
| T-02-20 | Tampering | emit-registry set-equality break at load | high | mitigate | scene_idle_nudge is already declared; this plan adds ONLY the emitter call — no phase4EmitTypes/crypto.emits change, so INV-PLUGIN-32 set-equality holds and the plugin loads. |
| T-02-SC | Tampering | package installs | low | accept | No package-manager installs (in-tree Go + SQL). |
</threat_model>

<verification>
- `task test -- ./plugins/core-scenes/...` green (sweep + emit-flag).
- `task test:int` green if run (idle transition end-to-end).
- Plugin loads without EVENT_TYPE_REGISTRY_MISMATCH (no emit-registry change).
- `go run ./cmd/inv-render` leaves invariants.md clean; registry meta-tests green.
- `task lint` / `task fmt` clean.
</verification>

<success_criteria>
Idle scenes auto-transition active→paused at their threshold (default or per-scene override) with per-row fault tolerance; the idle nudge emits only when enabled (default OFF); INV-SCENE-71 is registered and (if genuinely asserted) bound.
</success_criteria>

<output>
Create `.planning/phases/02-scenes-lineage-completion/02-06-SUMMARY.md` when done.
</output>
