---
status: testing
phase: 01.1-migration-framework-adopt-goose-for-go-migrations
source: [01.1-VERIFICATION.md]
started: 2026-08-03T02:25:30Z
updated: 2026-08-03T02:25:30Z
---

## Current Test

number: 1
name: Accept or re-derive the recorded one-time pg_dump schema-identity proof
expected: |
  A reviewer accepts (or independently re-derives) the empty `pg_dump --schema-only`
  diff recorded in `01.1-01-SUMMARY.md`. D-14 forbids committing the converter and its
  verification harness, so the artifact is self-reported and NOT reproducible from the
  tree. The record includes three falsification attempts, a named limit of the harness
  (it is blind to `StatementBegin`/`StatementEnd` placement), and a correction of the
  plan's own vacuous C1 probe instruction.
awaiting: user response

## Tests

### 1. Accept or re-derive the recorded one-time pg_dump schema-identity proof

expected: A reviewer accepts (or re-derives) the empty pg_dump diff. Criterion 1 explicitly specifies proof by a one-time pg_dump diff rather than by tests passing, and D-14 keeps the harness out of the tree, so no automated check in the repository can re-derive or contradict it.
result: [pending]

### 2. Execute the D-16 pre-deploy rehearsal against a restored sandbox snapshot

expected: Adopt runs against real restored data — 44 rows at `version_id > 0` plus goose's version-0 row, `schema_migrations` renamed to `schema_migrations_pre_goose`, boot completes. Procedure is in `site/src/content/docs/operating/how-to/sandbox/sandbox-restore.md`.
result: [pending]

### 3. Reconcile Broken Windows ledger entries #5 and #6

expected: #5 and #6 marked fixed, or an explicit reason recorded for keeping them open. Both say "red until plan 02"; plan 02 landed and both files now read one direction via `store.MigrationSectionForTest("...sql", "up"/"down")`. `open_count` is currently 9 and `/gsd-ship` blocks while > 0.
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
