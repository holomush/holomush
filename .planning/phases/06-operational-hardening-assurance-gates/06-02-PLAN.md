---
phase: 06-operational-hardening-assurance-gates
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/eventbus/audit/retention_partitions.go
  - internal/eventbus/audit/retention_partitions_test.go
  - internal/eventbus/audit/subsystem.go
  - internal/eventbus/config.go
  - cmd/holomush/core.go
autonomous: true
requirements: [OPS-02]

must_haves:
  truths:
    - "The RetentionWorker runs in production inside SubsystemAuditProjection and prunes events_audit on its cycle"
    - "events_audit partitions fully older than the retention window are DETACHed then DROPped after grace; recent partitions are retained"
    - "The retention window is operator-configurable with a 90-day default"
    - "EnsurePartitions keeps a forward window so live writes always have a covering partition"
  artifacts:
    - internal/eventbus/audit/retention_partitions.go
    - internal/eventbus/audit/retention_partitions_test.go
  key_links:
    - "SubsystemAuditProjection.Start constructs audit.NewRetentionWorker(cfg, mgr).Start(); Subsystem.Stop stops it (no productionSubsystems named-param cascade)"
    - "the PartitionManager DDL targets the events_audit table (not access_audit_log)"
---

<objective>
OPS-02 worker (D-01/D-02): make audit retention actually happen. Add a genuine
`events_audit` PartitionManager implementation, wire the (currently unwired)
`RetentionWorker` into the production lifecycle so it prunes on a cycle, and add
the operator-configurable retention window (default 90d).

Purpose: the migration in 06-01 made `events_audit` partitioned, but nothing
DETACHes/DROPs old partitions and nothing maintains the forward window. The
existing `RetentionWorker` (`internal/audit/retention.go`) has ZERO non-test
callers and the full `PartitionManager` interface has NO production impl (only
`EnsurePartitions` exists on the ABAC `access_audit_log` creator; the rest are
test mocks). This plan writes the real impl and starts the worker.

Output: `retention_partitions.go` (PartitionManager for events_audit), the
worker started inside `SubsystemAuditProjection`, and a `RetainWindow` config
knob threaded from operator config into `audit.Config.Defaults()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-operational-hardening-assurance-gates/06-RESEARCH.md
@.planning/phases/06-operational-hardening-assurance-gates/06-PATTERNS.md
@.claude/rules/logging.md

# Grounding analogs
@internal/audit/retention.go
@internal/audit/partition_creator.go
@internal/eventbus/audit/subsystem.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: events_audit PartitionManager implementation</name>
  <files>internal/eventbus/audit/retention_partitions.go, internal/eventbus/audit/retention_partitions_test.go</files>
  <read_first>
    - internal/audit/retention.go:31-38 (the PartitionManager interface this MUST satisfy: EnsurePartitions, PurgeExpiredAllows, DetachExpiredPartitions, DropDetachedPartitions, HealthCheck) and :63-101 (RunOnce order: Ensure → Purge → Detach → Drop)
    - internal/audit/partition_creator.go:29-62 (EnsurePartitions DDL + `%04d_%02d` naming + int64-ns FROM/TO bounds — copy verbatim, retarget the table)
    - 06-PATTERNS.md "retention_partitions.go (NEW — PartitionManager impl)" (Interface + Adaptation) and 06-RESEARCH.md §OPS-02(a) step 2 + §(c) landmines (DETACH CONCURRENTLY outside a tx)
    - internal/store/migrations/000052_events_audit_partition.up.sql (from 06-01 — the partition naming/shape this manager maintains)
  </read_first>
  <behavior>
    - EnsurePartitions(ctx, n): creates the current + next n-1 monthly `events_audit_YYYY_MM` range partitions if absent; re-running is a no-op (CREATE ... IF NOT EXISTS). Verify a covering partition exists for a "now" timestamp after the call.
    - DetachExpiredPartitions(ctx, olderThan): returns the names of child partitions whose entire range is older than `olderThan` and detaches them; a recent partition is NOT detached; returns the detached names.
    - DropDetachedPartitions(ctx, grace): drops previously-detached child tables past the grace period; a just-detached partition within grace is NOT dropped.
    - PurgeExpiredAllows(ctx, olderThan): no-op returning (0, nil) — events_audit has no allow/deny split.
    - HealthCheck(ctx): returns nil when events_audit is reachable (cheap probe).
  </behavior>
  <action>
    Create `retention_partitions.go` with a struct (e.g. `EventsAuditPartitionManager`)
    holding a `*pgxpool.Pool`, satisfying the `internal/audit.PartitionManager`
    interface for the `events_audit` table:
    - `EnsurePartitions`: copy the DDL/naming from partition_creator.go, retargeting
      `access_audit_log` → `events_audit` and naming `events_audit_%04d_%02d`;
      int64-ns FROM/TO bounds (BIGINT epoch-ns columns).
    - `DetachExpiredPartitions`: query child ranges (pg_inherits / pg_class /
      pg_get_expr of the partition bound, or pg_partitions) and
      `ALTER TABLE events_audit DETACH PARTITION <child> CONCURRENTLY` for children
      whose upper bound is <= olderThan. MUST run outside an explicit transaction
      (pool.Exec autocommits — do NOT wrap in Begin/Commit); one DETACH CONCURRENTLY
      at a time. Never detach the DEFAULT partition (the attached legacy remnant).
    - `DropDetachedPartitions`: `DROP TABLE IF EXISTS` on detached children past grace.
    - `PurgeExpiredAllows`: return (0, nil).
    - `HealthCheck`: `SELECT 1 FROM events_audit LIMIT 0` (or a partition-existence probe).
    Structured `oops` at each DDL boundary; `*Context` slog variants (a ctx is in scope).
    Write the integration test (`//go:build integration`, Postgres) covering the five
    behaviors — seed rows across an OLD month and a RECENT month partition, run the
    manager methods (or RunOnce via a constructed RetentionWorker), assert old
    partition DETACHed+DROPped and recent retained.
  </action>
  <verify>
    <automated>task test:int -- ./internal/eventbus/audit/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `EventsAuditPartitionManager` satisfies `internal/audit.PartitionManager` (compile-time assignment to the interface, e.g. `var _ audit.PartitionManager = ...`).
    - Integration test: seed old+recent partitions → run retention → old DETACHed+DROPped, recent retained (OPS-02 success criterion "rows past window pruned; table bounded"); DEFAULT partition untouched.
    - DETACH runs outside an explicit tx (no "DETACH PARTITION CONCURRENTLY cannot run inside a transaction block" error).
    - New lines clear codecov patch @ 80% (budget error-branch tests).
  </acceptance_criteria>
  <done>A production events_audit PartitionManager exists and prunes old partitions, proven by the seed-old/recent integration test.</done>
</task>

<task type="auto">
  <name>Task 2: retention config knob (RetainWindow, default 90d) threaded into audit.Config</name>
  <files>internal/eventbus/audit/subsystem.go, internal/eventbus/config.go</files>
  <read_first>
    - internal/eventbus/audit/subsystem.go:65-137 (audit `Config` struct + `Config.Defaults()` at :100 — where retention fields land)
    - internal/audit/retention.go:16-29 (RetentionConfig shape: RetainDenials/RetainAllows/PurgeInterval + DefaultRetentionConfig 90d/7d/24h — the mapping target)
    - internal/eventbus/config.go (operator-facing event_bus config — where an operator-adjustable window is surfaced)
    - 06-PATTERNS.md "subsystem.go (MOD — wire the worker) + config.go (retention cfg)" + 06-RESEARCH.md §OPS-02(a) Config knob paragraph (D-02)
  </read_first>
  <action>
    Add an operator-configurable retention window to the audit config. Add a
    `RetainWindow time.Duration` field (default 90 days, per D-02, mirroring the ABAC
    `RetentionConfig.RetainDenials` default) to `audit.Config`, defaulted in
    `Config.Defaults()` (only when zero-valued, matching the existing Defaults()
    pattern), plus a `PurgeInterval` default of 24h. Surface the knob through the
    operator-facing `internal/eventbus/config.go` event_bus config so it is not
    hardcoded (D-02: configurable, 90d is a starting default not a locked value).
    Provide a mapping that produces an `internal/audit.RetentionConfig` from the
    audit config: set `RetainDenials = RetainWindow`, leave `RetainAllows` unused
    (the PurgeExpiredAllows no-op consumes it harmlessly), set `PurgeInterval`.
    Do NOT start the worker here (that is Task 3) — this task only lands the config
    surface + the mapping helper.
  </action>
  <verify>
    <automated>task test -- ./internal/eventbus/audit/... ./internal/eventbus/... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `audit.Config` has a `RetainWindow` field defaulted to `90 * 24 * time.Hour` in `Defaults()`; a caller can override it via the event_bus config (proven by a unit test asserting a non-default value survives Defaults()).
    - A mapping helper returns `internal/audit.RetentionConfig{RetainDenials: RetainWindow, PurgeInterval: ...}` (unit test asserts the mapping).
    - `task test` (unit) green for the audit package.
  </acceptance_criteria>
  <done>The retention window is operator-configurable with a 90d default and maps cleanly onto RetentionConfig.</done>
</task>

<task type="auto">
  <name>Task 3: wire RetentionWorker into SubsystemAuditProjection lifecycle</name>
  <files>internal/eventbus/audit/subsystem.go, cmd/holomush/core.go</files>
  <read_first>
    - internal/eventbus/audit/subsystem.go:202-293 (Subsystem.Start resolves the pool via s.poolProv.Pool() and owns s.cancel/s.worker + rollback discipline; Subsystem.Stop drains) — the exact seam to co-locate the worker
    - internal/audit/retention.go:52-117 (NewRetentionWorker(cfg, manager), Start(ctx) spawns run(), Stop() cancels+waits, RunOnce order)
    - cmd/holomush/core.go (audit subsystem construction — where the retention config value is passed in; note core.go is in the .codecov.yml ignore list, so its lines are coverage-exempt)
    - 06-RESEARCH.md §OPS-02(a) step 3 + assumption A5 (home the worker inside SubsystemAuditProjection to AVOID the productionSubsystems named-param cascade / new SubsystemID iota + stringer regen)
  </read_first>
  <action>
    Inside `Subsystem.Start` (after the pool is resolved and the projection has
    started successfully), construct the events_audit PartitionManager (Task 1) over
    the resolved pool, build `audit.NewRetentionWorker(retentionCfg, mgr)` using the
    mapped RetentionConfig (Task 2), and call `worker.Start(workerCtx)`. Store the
    worker on the Subsystem struct. In `Subsystem.Stop`, call `worker.Stop()` as part
    of the existing drain sequence. Mirror the existing rollback discipline: if the
    worker fails to start, roll back the already-started projection (the Start path is
    all-or-nothing today). Do NOT introduce a new `SubsystemID` / touch
    `productionSubsystems` (that forces a named-param signature cascade + stringer
    regen — A5). Thread the operator retention value from `cmd/holomush/core.go`
    where the audit subsystem is constructed. `*Context` slog variants throughout.
  </action>
  <verify>
    <automated>task test:int -- ./internal/eventbus/audit/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `Subsystem.Start` constructs + starts the RetentionWorker; `Subsystem.Stop` stops it cleanly (no goroutine leak; existing drain test still green).
    - An integration test (or extension of the Task 1 test) drives the subsystem Start→(worker runs RunOnce immediately)→Stop and observes an old partition pruned via the live worker, not a hand-called manager.
    - No new `SubsystemID` added; `cmd/holomush/core.go` compiles with the retention value threaded in; `task test:int` green.
  </acceptance_criteria>
  <done>The RetentionWorker runs in production inside the audit projection subsystem and prunes events_audit on its cycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retention worker → events_audit partitions (DDL) | The worker issues DETACH/DROP; a wrong-window or wrong-table drop destroys audit records |
| operator config → retention window | An operator-supplied window governs how long audit history is retained (compliance-relevant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-2-01 | Denial/Tampering | DetachExpiredPartitions/DropDetachedPartitions | high | mitigate | Detach only children whose UPPER bound <= olderThan; DEFAULT partition never detached; drop only past grace; integration test proves recent partitions survive |
| T-06-2-02 | Tampering | manager targets wrong table | medium | mitigate | DDL literals hardcode `events_audit`; compile-time interface satisfaction; test seeds/asserts against events_audit specifically |
| T-06-2-03 | Denial | worker goroutine leak / start-failure leaves projection half-up | medium | mitigate | Mirror the subsystem's all-or-nothing Start rollback + bounded drain in Stop; drain test remains green |
| T-06-2-04 | Repudiation | over-aggressive window silently discards required audit history | low | accept | Window is operator-configured with a conservative 90d default; documented as adjustable; not auto-shrinking |
</threat_model>

<verification>
- `task test:int` green: PartitionManager prunes old / retains recent; the worker runs via the subsystem lifecycle; Stop is clean.
- `task test` green: config default + mapping unit tests.
</verification>

<success_criteria>
events_audit rows past the retention window are pruned by the wired RetentionWorker; the table no longer grows unbounded (ROADMAP Phase 6 success criterion #1 / OPS-02).
</success_criteria>

## Artifacts this phase produces (this plan)
- `internal/eventbus/audit/retention_partitions.go` (NEW) — `EventsAuditPartitionManager` implementing the full PartitionManager interface for events_audit.
- `internal/eventbus/audit/retention_partitions_test.go` (NEW) — Ensure/Detach/Drop integration test.
- `RetainWindow` / `PurgeInterval` fields on `audit.Config` + Defaults() (90d) + operator plumbing in `internal/eventbus/config.go`.
- RetentionWorker construction + Start/Stop wiring inside `SubsystemAuditProjection` (subsystem.go) + threaded value in `cmd/holomush/core.go`.

<output>
Create `.planning/phases/06-operational-hardening-assurance-gates/06-02-SUMMARY.md` when done.
</output>
