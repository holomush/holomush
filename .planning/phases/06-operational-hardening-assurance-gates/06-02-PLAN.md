---
phase: 06-operational-hardening-assurance-gates
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/eventbus/audit/retention_partitions.go
  - internal/eventbus/audit/retention_partitions_test.go
  - internal/eventbus/audit/subsystem.go
  - internal/eventbus/audit/subsystem_test.go
  - internal/eventbus/config.go
  - internal/audit/retention.go
  - internal/audit/retention_test.go
  - cmd/holomush/core.go
  - cmd/holomush/bootstrap_orphan.go
  - cmd/holomush/bootstrap_orphan_test.go
  - .github/workflows/deploy.yaml
  - site/src/content/docs/operating/how-to/sandbox/sandbox-operations.md
autonomous: false
requirements: [OPS-02]

must_haves:
  truths:
    - "The RetentionWorker runs in production inside SubsystemAuditProjection and prunes events_audit on its cycle"
    - "events_audit partitions fully older than the retention window are DETACHed CONCURRENTLY, renamed to events_audit_<YYYY_MM>_detached_<unix>, then DROPped after grace by name-age; recent partitions are retained"
    - "An interrupted DETACH ... CONCURRENTLY cannot permanently strand a partition: each detach cycle FIRST finalizes any child marked pg_inherits.inhdetachpending (ALTER TABLE events_audit DETACH PARTITION <child> FINALIZE — the pending-detach state PG leaves when the 2-transaction concurrent detach is interrupted), THEN reconciles canonical-named events_audit_YYYY_MM tables that are no longer children of events_audit (renaming them into the _detached_<unix> form)"
    - "Retention config is validated: RetainWindow <= 0 and PurgeInterval <= 0 are REJECTED at config validation (in the tested audit.Config/mapping surface, not only in codecov-ignored core.go) — a negative RetainWindow would make the cutoff future-facing and detach EVERY partition, and a non-positive PurgeInterval would panic time.NewTicker"
    - "EnsurePartitions covers the retention window backward AND a forward window, so an in-window DLQ replay of a historical event lands in a real prunable partition (no unprunable sink)"
    - "A one-time backfill re-homes the legacy events_audit_unpartitioned rows into the partitioned table with no audit-history loss and no duplicate on later replay; event_ms is computed via 06-01's shared eventMsFromULID helper (identical to writeAuditRow) so a legacy row and a later replay of the same event dedup"
    - "Subsystem.Start runs the SYNCHRONOUS boot gate — Backfill + EnsurePartitions ONLY (NOT a full RunOnce; no Detach/Drop on boot) — BEFORE the projection starts accepting traffic, and returns an error if that gate fails; the retention window is operator-configurable (90d default)"
    - "The bootstrap orphan check also scans events_audit_unpartitioned when it is present, so residual legacy rows are not invisible to the pre-Start defense-in-depth gate after 000052's rename"
    - "06-01 and 06-02 co-deploy as a SINGLE PR/ship unit, ENFORCED by a blocking pre-PR checkpoint (Task 6), not only asserted as a truth — this plan's Backfill is the only path that restores cold-history visibility after 000052's rename, so a 06-01-only merge is a cold-history blackout"
    - "The production deploy sequence is RE-CHOREOGRAPHED (round-4 HIGH; operator chose deploy choreography, NOT expand/contract): the CI deploy (.github/workflows/deploy.yaml) and the manual runbook (sandbox-operations.md) STOP the old core AND the player-facing gateway + cloudflared tunnel BEFORE `core migrate` (000052) — stopping gateway + cloudflared severs the entire player-traffic path (cloudflared → gateway → core) so NO player request can reach the new core during the window, regardless of the fact that gRPC begins serving before whole-process readiness and does not DependsOn SubsystemAuditProjection (cmd/holomush/sub_grpc.go:166,771); the old gateway can therefore no longer reach the new core before the backfill/readiness gate completes (round-5 H1) — then start ONLY the NEW core (`up -d --wait --no-deps core`, readiness-gated on 06-02's synchronous Backfill+EnsurePartitions boot gate), then GATE restoring player traffic by starting gateway + cloudflared LAST — replacing the current migrate-while-old-core-still-running → recreate-all order that would make the old binary's INSERT ... ON CONFLICT (id) fail on both NOT NULL and the dropped id-alone unique index during the window (traffic is genuinely stopped, not merely 'restored by final up -d' as the round-4 text imprecisely implied)"
    - "The brief audit-write outage during the stop-old-core → migrate → new-core-ready window is a DELIBERATE, bounded ops risk on the single-node sandbox (no old-core audit writes occur while core is down, so there is no incompatible-write failure); the new-core readiness/backfill gate is the COMPENSATING control that aborts the deploy before traffic is restored if the backfill fails"
    - "The synchronous Backfill runs in Subsystem.Start BEFORE whole-process readiness, so a large events_audit_unpartitioned can push the new core's start past the core healthcheck budget (compose.prod.yaml core: start_period 15s + retries 15 × interval 5s ≈ 90s) → Docker marks it unhealthy → `restart: unless-stopped` thrashes mid-backfill while `up -d --wait` is pending (round-5 M4). MITIGATION: the deploy choreography (Task 5) runs a PRE-MIGRATE row-count probe of events_audit against a documented threshold and, above it, WARNS the operator to run an ahead-of-deploy backfill or temporarily raise the core start_period; the ~90s budget and the escape hatches are documented in deploy.yaml + the runbook. The sandbox is expected to sit well under threshold; the probe makes the risk bounded and visible rather than silent"
    - "The periodic RetentionWorker does NOT run a destructive cycle immediately on Start: RetentionWorker gains an initial-delay mode (skip the immediate RunOnce; the first Detach/Drop cycle only fires after the first ticker tick / whole-process readiness), so a subsystem that fails after the boot gate cannot trigger DETACH/DROP during a red deploy (retention.go:133 currently RunOnces on Start before the ticker)"
    - "Retention partitions carry a DURABLE provenance marker stamped at creation (a `COMMENT ON TABLE <partition> IS 'holomush:events_audit_partition'`), and the STAMP ITSELF is gated (round-5 H2): the marker is applied ONLY after verifying via pg_inherits (parent OID = the schema-qualified `public.events_audit` regclass, child OID = the partition's regclass) that the relation is a GENUINE current child of events_audit — a `CREATE TABLE IF NOT EXISTS ... PARTITION OF` is a no-op on a pre-existing SAME-NAMED non-child (PG's IF NOT EXISTS does not guarantee the existing relation matches the requested partition), so an unconditional COMMENT would grant the trusted marker to a non-partition; EnsurePartitions FAILS CLOSED (no stamp, structured oops surfacing the occupied-name anomaly) when the relation is not a current child. The crash-orphan reconcile-rename AND the detached-table drop REQUIRE that marker before touching any table — so a same-named non-child is never stamped, never reconciled, and never dropped (provenance is provable at STAMP time, not only at drop time — round-4 F9 + round-5 H2)"
  prohibitions:
    - "The manager never DETACHes/DROPs events_audit_unpartitioned or any non-child table"
    - "The synchronous boot gate does NOT run Detach/Drop (a first-cycle boot must not prune on a red deploy) — only Backfill + EnsurePartitions"
    - "The periodic worker does NOT run a destructive RunOnce the instant Start is called (initial-delay mode); the first Detach/Drop waits for the first ticker tick / process readiness"
    - "Crash-orphan reconciliation and detached-table drop NEVER rename/drop a table that lacks the durable `holomush:events_audit_partition` marker (name+shape alone is insufficient provenance — round-4 F9)"
    - "EnsurePartitions NEVER stamps the `holomush:events_audit_partition` marker on a relation that is not a verified current child of events_audit (the stamp is pg_inherits-gated and fails closed on a same-named non-child — round-5 H2); an unconditional COMMENT after `CREATE TABLE IF NOT EXISTS ... PARTITION OF` is forbidden"
    - "The re-choreographed deploy does NOT run `core migrate` while an old core replica is still running, AND does NOT leave the player-facing gateway or cloudflared tunnel running during the migrate/new-core-readiness window (both are stopped before migrate and restored only after the new core is ready — round-5 H1)"
    - "DropDetachedPartitions is not a no-op stub — it discovers detached children by the _detached_<unix> name convention and drops by age"
    - "The new retention config field does not shadow the existing DLQ MaxAge"
  artifacts:
    - internal/eventbus/audit/retention_partitions.go
    - internal/eventbus/audit/retention_partitions_test.go
  key_links:
    - "SubsystemAuditProjection.Start order: construct EventsAuditPartitionManager → Backfill → EnsurePartitions (synchronous boot gate) → THEN start the projection → THEN start audit.NewRetentionWorker (periodic Detach/Drop); Subsystem.Stop stops the worker (no productionSubsystems cascade)"
    - "detach records grace via the child rename events_audit_<YYYY_MM>_detached_<unix>; drop discovers eligibility by parsing that suffix; a reconciliation pass re-stamps crash-orphaned canonical-named detached tables"
    - "the PartitionManager DDL targets the events_audit table (not access_audit_log); Backfill reuses 06-01's eventMsFromULID (same package)"
    - "runBootstrapOrphanCheck (cmd/holomush/bootstrap_orphan.go) scans events_audit AND events_audit_unpartitioned (when present) so the pre-Start gate sees legacy residual rows"
---

<objective>
OPS-02 worker (D-01/D-02): make audit retention actually happen. Add a genuine
`events_audit` PartitionManager implementation, wire the (currently unwired)
`RetentionWorker` into the production lifecycle so it prunes on a cycle, and add
the operator-configurable retention window (default 90d).

Purpose: the migration in 06-01 made `events_audit` partitioned, but nothing
DETACHes/DROPs old partitions and nothing maintains the forward window. The
existing `RetentionWorker` (`internal/audit/retention.go`) has ZERO non-test
callers and the full `PartitionManager` interface has NO production impl (only
`EnsurePartitions` exists on the ABAC `access_audit_log` creator; the rest are
test mocks). This plan writes the real impl and starts the worker.

Output: `retention_partitions.go` (PartitionManager for events_audit), the
worker started inside `SubsystemAuditProjection`, and a `RetainWindow` config
knob threaded from operator config into `audit.Config.Defaults()`.

ROUND-2 REVIEW INCORPORATION (06-REVIEWS.md consensus, verified against live source):
- **Shared ULID helper (finding 1):** Backfill computes event_ms via 06-01's shared
  `eventMsFromULID(id) = ulid.Time(id.Time()).UnixNano()` (same package) — NOT the
  round-1 `ulid.Time(id)` form (a compile error: Time takes uint64 ms).
- **Start ordering / dual-copy window (finding 10):** the synchronous boot gate runs
  BEFORE the projection starts — construct manager → Backfill → EnsurePartitions → then
  newProjection/start → then the periodic worker. Today Start starts the projection first
  (subsystem.go:229-236), which lets a DLQ replay race a not-yet-backfilled legacy row.
- **First-cycle scope (finding 9):** the boot gate is Backfill + EnsurePartitions ONLY —
  NOT a full RunOnce (which also Detach/Drops via retention.go:63-98); pruning is left to
  the periodic worker so a first boot on a red deploy never prunes.
- **Detach→rename crash atomicity (finding 7):** DETACH CONCURRENTLY runs outside a tx and
  the rename is a separate statement; each detach cycle first RECONCILES canonical-named
  events_audit_YYYY_MM tables that are no longer children (crash-orphaned) into the
  _detached_<unix> form so they cannot be permanently stranded.
- **Bootstrap orphan-check gap (finding 5):** runBootstrapOrphanCheck (core.go:296, BEFORE
  audit Start) scanned only events_audit; after 000052's rename, residual rows sit in
  events_audit_unpartitioned. Task 4 extends the check to also scan that table when present.
- **Co-deploy with 06-01 (finding 4):** ships in the same PR (this Backfill restores
  cold-history visibility). **06-PATTERNS.md was refreshed (finding 12).**

ROUND-3 REVIEW INCORPORATION (06-REVIEWS.md consensus, verified against live source):
- **Interrupted concurrent detach → FINALIZE (round-3 HIGH #2):** PG runs DETACH ...
  CONCURRENTLY in two internal transactions; an interruption leaves the child marked
  `pg_inherits.inhdetachpending` (still a child, mid-detach) — a state the round-2
  crash-orphan reconciliation (which only finds tables ABSENT from pg_inherits) does NOT
  cover. Task 1 now finalizes those with `ALTER TABLE events_audit DETACH PARTITION <child>
  FINALIZE` BEFORE the absent-child reconciliation, with an injected-interruption test.
- **Negative retention config (round-3 HIGH #3):** Config.Defaults() only fills ZERO
  values, so a negative survives. A negative RetainWindow makes `now.Add(-RetainDenials)`
  future-facing → DetachExpiredPartitions detaches EVERY partition (retention.go:83); a
  non-positive PurgeInterval panics `time.NewTicker` (retention.go:130). Task 3 rejects
  both at config validation (in the tested audit.Config surface).
- **Catalog reconciliation provenance (round-3 MEDIUM):** Task 1 schema-qualifies all
  catalog discovery and constrains crash-orphan reconciliation to genuine former-children.
- **Co-deploy tooling-enforced (round-3 MEDIUM):** Task 6 is a blocking pre-PR checkpoint
  that 06-01 + 06-02 land in ONE PR (not only a `must_haves` truth).
- **Fail-open `| tail` (round-3 MEDIUM):** every verify command captures the test exit code
  instead of masking it behind `| tail`.

ROUND-4 REVIEW INCORPORATION (06-REVIEWS.md Consensus Summary, verified against live source):
- **Deploy choreography for the 000052 window (round-4 HIGH — operator chose OPTION A, deploy
  choreography, NOT expand/contract):** the production deploy runs `core migrate` BEFORE recreating
  services (deploy.yaml migrate at :107 / `up -d` at :108; manual runbook sandbox-operations.md:238),
  so the OLD core keeps issuing `INSERT ... ON CONFLICT (id)` with no `event_ms` (projection.go:414)
  while 000052 has made `event_ms NOT NULL` and dropped the id-alone unique index → the write fails
  on BOTH counts across the window. NEW Task 5 re-choreographs BOTH the CI workflow and the manual
  runbook to: stop the old core → migrate → start the new core → gate restoring traffic on the new
  core's readiness (which incorporates this plan's synchronous Backfill+EnsurePartitions boot gate).
  The 06-01/06-02 same-PR checkpoint (now Task 6) only proves file co-presence; it does NOT make the
  live deployment atomic — Task 5 does. The brief audit-write outage is a deliberate, bounded
  single-node risk with the readiness/backfill gate as the compensating control.
- **Worker fires a destructive RunOnce on Start (round-4 MEDIUM):** the synchronous boot gate runs
  Backfill+Ensure only, but `RetentionWorker.Start`→`run` calls `RunOnce` immediately before the ticker
  (retention.go:103,133), so a later subsystem failure can still trigger DETACH/DROP on a red deploy —
  the round-2 "first-cycle scope" resolution is not actually achieved for the periodic worker. Task 3
  now adds an initial-delay mode to `RetentionWorker` (skip the immediate RunOnce; first destructive
  cycle only after the first ticker tick / process readiness), with a unit test.
- **Partition provenance is name/shape-based (round-4 MEDIUM, F9 PARTIAL):** reconcile + `_detached_<unix>`
  drop key on schema+name+shape, which cannot prove a table absent from `pg_inherits` was formerly an
  events_audit child (partition_creator.go applies no durable marker). Task 1 now stamps a DURABLE marker
  at partition creation (`COMMENT ON TABLE <partition> IS 'holomush:events_audit_partition'`) and REQUIRES
  that marker before any reconcile-rename or drop; detached-table discovery is schema-qualified.
- **Config.Validate test file named (round-4 LOW):** the `Config.Validate` unit test is named explicitly
  in files_modified (`internal/eventbus/audit/subsystem_test.go`, adjacent to Config.Defaults()).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-operational-hardening-assurance-gates/06-RESEARCH.md
@.planning/phases/06-operational-hardening-assurance-gates/06-PATTERNS.md
@.claude/rules/logging.md

# Grounding analogs
@internal/audit/retention.go
@internal/audit/partition_creator.go
@internal/eventbus/audit/subsystem.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: events_audit PartitionManager implementation</name>
  <files>internal/eventbus/audit/retention_partitions.go, internal/eventbus/audit/retention_partitions_test.go</files>
  <read_first>
    - internal/audit/retention.go:31-38 (the PartitionManager interface this MUST satisfy: EnsurePartitions, PurgeExpiredAllows, DetachExpiredPartitions, DropDetachedPartitions, HealthCheck) and :63-101 (RunOnce order: Ensure → Purge → Detach → Drop)
    - PostgreSQL DETACH PARTITION docs (postgresql.org/docs/current/sql-altertable.html): `DETACH PARTITION ... CONCURRENTLY` runs in TWO internal transactions and takes only SHARE UPDATE EXCLUSIVE; if interrupted, the child is left marked `pg_inherits.inhdetachpending = true` (still listed as a child, mid-detach) and MUST be completed with `ALTER TABLE <parent> DETACH PARTITION <child> FINALIZE` — a state distinct from the round-2 crash-orphan (already ABSENT from pg_inherits). CI/prod run Postgres 18 (ci.yaml postgres:18), so FINALIZE + inhdetachpending are available.
    - internal/audit/partition_creator.go:29-62 (EnsurePartitions DDL + `%04d_%02d` naming + int64-ns FROM/TO bounds — copy verbatim, retarget the table; NOTE it applies NO durable provenance marker (round-4 F9) — this plan ADDS a `COMMENT ON TABLE ... IS 'holomush:events_audit_partition'` stamp the ABAC creator does not have)
    - 06-PATTERNS.md "retention_partitions.go (NEW — PartitionManager impl)" (Interface + Adaptation) and 06-RESEARCH.md §OPS-02(a) step 2 + §(c) landmines (DETACH CONCURRENTLY outside a tx)
    - internal/store/migrations/000052_events_audit_partition.up.sql (from 06-01 — the partition naming/shape this manager maintains)
  </read_first>
  <behavior>
    - EnsurePartitions(ctx, months): creates monthly event_ms `events_audit_YYYY_MM` partitions covering the RETENTION WINDOW backward (the manager is constructed with the operator-configured retention window and derives the backward month-span deterministically from it — NOT from the worker's forward `months` arg) AND `months` forward from now (so an in-window DLQ replay of a historical event has a covering, prunable partition — not an unprunable sink); re-running is a no-op (CREATE ... IF NOT EXISTS). Immediately after each `CREATE TABLE IF NOT EXISTS ... PARTITION OF events_audit`, GATE the durable provenance stamp on genuine child-ness (round-5 H2): verify via pg_inherits that the partition is a CURRENT child of the schema-qualified `public.events_audit` parent (parent OID = events_audit regclass, child OID = the partition's regclass), and ONLY THEN stamp `COMMENT ON TABLE <partition> IS 'holomush:events_audit_partition'`. FAIL CLOSED — do NOT stamp, return a structured oops — when the relation is present but NOT a current child (a pre-existing same-named non-child that `CREATE ... IF NOT EXISTS` silently skipped; PG's IF NOT EXISTS does not attach or reshape it). The marker survives DETACH (which strips the pg_inherits parent link) so it is the only durable proof a later-orphaned table was ours — which is exactly why it must never be granted to a non-partition. Verify a covering partition exists for BOTH a now() and a within-retention past event_ms after the call, AND that each GENUINE created partition carries the marker while a pre-existing same-named non-child is left markerless.
    - DetachExpiredPartitions(ctx, olderThan): run recovery FIRST, in two ordered passes, before any new detach. (0-FINALIZE, round-3 finding 2) find any child of events_audit marked `pg_inherits.inhdetachpending = true` (an interrupted DETACH ... CONCURRENTLY) and run `ALTER TABLE events_audit DETACH PARTITION <child> FINALIZE` to complete it, then rename it to the `_detached_<unix>` form. (0-RECONCILE, round-2 finding 7 + round-4 F9) THEN find any canonical-named `events_audit_YYYY_MM` table that is NOT currently a child of events_audit (absent from pg_inherits), lacks the `_detached_<unix>` suffix, AND carries the durable `holomush:events_audit_partition` provenance marker (a partition DETACHed by a prior cycle that crashed before its rename) — the marker requirement is what proves the orphan was formerly OURS, not a coincidentally-named non-child table; rename it to `events_audit_<YYYY_MM>_detached_<now>` (conservative fresh grace). THEN DETACH (CONCURRENTLY) each child whose entire event_ms range is older than olderThan, and rename it to `events_audit_<YYYY_MM>_detached_<unix>` (stamping the detach epoch in the name — durable metadata that survives the loss of partition-bound catalog info; the provenance COMMENT survives the rename); returns the new names; a recent partition is NOT detached.
    - DropDetachedPartitions(ctx, grace): discovers detached tables (schema-qualified) by the `events_audit_%_detached_%` name pattern AND the durable `holomush:events_audit_partition` provenance marker (round-4 F9 — never drop a same-named table that lacks the marker), parses the trailing unix epoch, and DROPs those where now-unix > grace; a just-detached partition within grace is NOT dropped.
    - PurgeExpiredAllows(ctx, olderThan): no-op returning (0, nil) — events_audit has no allow/deny split.
    - HealthCheck(ctx): returns nil when events_audit is reachable (cheap probe).
  </behavior>
  <action>
    Create `retention_partitions.go` with `EventsAuditPartitionManager` (holds a
    `*pgxpool.Pool`) satisfying `internal/audit.PartitionManager` for the `events_audit`
    table (partitioned on the `event_ms` BIGINT key from 06-01). IMPORT ALIAS (round-5 M5 —
    compile landmine): this file lives in package `audit` (`internal/eventbus/audit`) AND the
    interface/worker live in a DIFFERENT package ALSO named `audit` (`internal/audit`), so
    the import MUST be aliased — `retaudit "github.com/holomush/holomush/internal/audit"` —
    and every reference to the interface/worker/config uses that alias (`retaudit.PartitionManager`,
    `retaudit.NewRetentionWorker`, `retaudit.RetentionConfig`, `retaudit.WithInitialDelay`);
    a bare `audit.PartitionManager` inside this package refers to the local package and does
    NOT compile:
    - `EnsurePartitions`: copy the DDL/naming from partition_creator.go, retargeting
      `access_audit_log` → `events_audit`, naming `events_audit_%04d_%02d`, int64-ns
      event_ms FROM/TO bounds. EXTEND it to also create partitions BACKWARD across the
      retention window (not only current+forward) so an in-window historical DLQ replay
      lands in a real prunable partition — addresses the review blocker "EnsurePartitions
      must cover the retention + DLQ-replay horizon." DURABLE PROVENANCE MARKER, STAMP-TIME
      GATED (round-4 F9 + round-5 H2): after each `CREATE TABLE IF NOT EXISTS ... PARTITION OF
      events_audit`, do NOT stamp unconditionally. First VERIFY genuine child-ness with a
      schema-qualified pg_inherits probe — `SELECT 1 FROM pg_inherits i JOIN pg_class c ON
      c.oid = i.inhrelid JOIN pg_namespace n ON n.oid = c.relnamespace JOIN pg_class p ON
      p.oid = i.inhparent JOIN pg_namespace pn ON pn.oid = p.relnamespace WHERE
      pn.nspname = <target schema> AND p.relname = 'events_audit' AND n.nspname = <target
      schema> AND c.relname = <partition>` — and ONLY when it returns a row run
      `COMMENT ON TABLE <partition> IS 'holomush:events_audit_partition'`. If the probe returns
      NO row (the name is occupied by a pre-existing SAME-NAMED NON-CHILD that
      `CREATE ... IF NOT EXISTS` silently skipped — PG's IF NOT EXISTS does not guarantee the
      existing relation matches the requested partition, per the PostgreSQL CREATE TABLE docs),
      FAIL CLOSED: return a structured `oops` (e.g. `AUDIT_PARTITION_NAME_OCCUPIED`) and do NOT
      stamp. Re-stamping a genuine existing child on a re-run is still idempotent (the probe
      passes, the COMMENT is set-to-the-same-value). This COMMENT survives both DETACH (which
      strips the pg_inherits parent link) and the `_detached_<unix>` rename, so it is the ONLY
      durable proof that a table now absent from pg_inherits was formerly an events_audit child —
      which is precisely why it must never be granted to a non-partition at stamp time.
    - `DetachExpiredPartitions`: run TWO recovery passes FIRST, then the normal detach.
      Pass 0-FINALIZE (round-3 finding 2 — interrupted concurrent detach): query
      `pg_inherits i JOIN pg_class c ON c.oid = i.inhrelid JOIN pg_namespace n ON n.oid =
      c.relnamespace` for children of the events_audit parent (schema-qualified — restrict to
      the target schema) where `i.inhdetachpending = true`, and for each run
      `ALTER TABLE events_audit DETACH PARTITION <child> FINALIZE` (completes the half-done
      concurrent detach PG left mid-way), then `ALTER TABLE <child> RENAME TO
      events_audit_<YYYY_MM>_detached_<now-unix>` (fresh grace). Pass 0-RECONCILE (round-2
      finding 7 — crash between a completed DETACH and its rename): `SELECT relname FROM
      pg_class c JOIN pg_namespace n ON n.oid = c.relnamespace` for tables in the TARGET
      SCHEMA matching `events_audit_[0-9]{4}_[0-9]{2}` that are NOT in `pg_inherits` as a
      child of events_audit AND do NOT already carry `_detached_` AND carry the durable
      provenance marker `obj_description(c.oid, 'pg_class') = 'holomush:events_audit_partition'`
      (round-4 F9 — the marker is what PROVES former ownership; round-3 MEDIUM shape-guard is
      insufficient because schema+name+shape cannot distinguish a coincidentally-named non-child
      table from a genuine crash-orphan), and `ALTER TABLE <orphan> RENAME TO
      events_audit_<YYYY_MM>_detached_<now-unix>`. THEN the normal detach: query current
      child ranges (pg_inherits / pg_class / pg_get_expr of the partition bound) and
      `ALTER TABLE events_audit DETACH PARTITION <child> CONCURRENTLY` for children whose
      UPPER event_ms bound <= olderThan. MUST run outside an explicit transaction (pool.Exec
      autocommits — no Begin/Commit); one DETACH CONCURRENTLY at a time. Immediately after
      each DETACH, `ALTER TABLE <child> RENAME TO events_audit_<YYYY_MM>_detached_<unix>` to
      record the detach epoch in the name (DETACH removes the bound metadata, so the name is
      the durable grace clock). Return the renamed names. Schema-qualify all catalog discovery
      and quote every catalog-derived identifier (pgx/quoting).
    - `DropDetachedPartitions`: `SELECT c.relname FROM pg_class c JOIN pg_namespace n ON
      n.oid = c.relnamespace WHERE n.nspname = <target schema> AND c.relname LIKE
      'events_audit_%_detached_%' AND obj_description(c.oid, 'pg_class') =
      'holomush:events_audit_partition'` (schema-qualified AND marker-gated — round-4 F9;
      never drop a same-named table lacking the durable marker), parse the trailing unix
      epoch from each name, `DROP TABLE IF EXISTS <name>` where now-epoch > grace.
    - `PurgeExpiredAllows`: return (0, nil).
    - `HealthCheck`: `SELECT 1 FROM events_audit LIMIT 0`.
    Never DETACH/DROP `events_audit_unpartitioned` or any table that is not a current or
    detached child of events_audit. Structured `oops` at each DDL boundary; `*Context`
    slog variants (a ctx is in scope).
    Write the integration test (`//go:build integration`, Postgres): seed rows across an
    OLD and a RECENT event_ms partition; run Detach then Drop; assert the old partition is
    DETACHed, RENAMEd to the `_detached_<unix>` form, and DROPped past grace, while a
    recent partition is retained and a just-detached partition WITHIN grace is NOT dropped;
    assert EnsurePartitions produced a covering partition for a within-retention past event_ms.
    ALSO test crash-atomicity (finding 7): simulate a crash-orphaned partition by manually
    DETACHing a canonical-named child WITHOUT renaming it (leaving `events_audit_YYYY_MM` no
    longer a child, no `_detached_` suffix), then run a SECOND DetachExpiredPartitions cycle →
    assert the orphan is reconciled (renamed to the `_detached_<unix>` form) and is eventually
    DROPped by DropDetachedPartitions past grace (not permanently stranded).
    ALSO test the interrupted-concurrent-detach FINALIZE recovery (round-3 finding 2): drive a
    partition into the `inhdetachpending = true` state, then assert the next
    DetachExpiredPartitions cycle FINALIZEs it. To create the state deterministically: from a
    SECOND pool connection open a transaction that reads events_audit (holding a lock that
    blocks the second internal transaction of a concurrent detach), start
    `ALTER TABLE events_audit DETACH PARTITION <child> CONCURRENTLY` under a short
    `statement_timeout` so it errors mid-way, then roll back the blocking txn — the child is now
    marked `inhdetachpending` (assert via a `pg_inherits.inhdetachpending` query). Run
    DetachExpiredPartitions and assert the child is FINALIZEd (no longer inhdetachpending, no
    longer a child) and renamed to the `_detached_<unix>` form, then DROPped past grace — never
    permanently stranded. (If deterministic reproduction proves impractical on the CI Postgres,
    the equivalent is to assert the FINALIZE query path runs and clears a child seeded into the
    pending state; keep the assertion on the observable end state, not the reproduction mechanism.)
    ALSO test the STAMP-TIME provenance gate (round-5 H2 — a full RunOnce non-child test): BEFORE
    running the manager, manually create a standalone table named exactly like a partition the
    manager will EnsurePartitions (e.g. `CREATE TABLE events_audit_<YYYY_MM> (id bytea)` — a
    same-named, non-child, MARKERLESS relation, NOT attached to events_audit). Then run a FULL
    `RunOnce` (Ensure → Purge → Detach → Drop, the real worker lifecycle in which Ensure precedes
    reconcile/drop). Assert the invariant end state: the non-child relation (a) carries NO
    `holomush:events_audit_partition` marker (`obj_description` is NULL — EnsurePartitions failed
    closed rather than stamping it, or surfaced the occupied-name oops), (b) is NEVER renamed to a
    `_detached_<unix>` form by the reconcile pass, and (c) is NEVER dropped by DropDetachedPartitions.
    This proves the "never a same-named non-child" claim is enforced at STAMP time, not merely at
    drop time.
  </action>
  <verify>
    <automated># offload-exempt: acceptance gate — raw exit code + tail output is the verify signal (executor sub-agent, offload-deny exempt)
out=$(task test:int -- ./internal/eventbus/audit/... 2>&1); rc=$?; printf '%s\n' "$out" | tail -30; test "$rc" -eq 0 || { echo "FAIL: audit integration suite red (rc=$rc)"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `EventsAuditPartitionManager` satisfies `internal/audit.PartitionManager` via the aliased import (compile-time `var _ retaudit.PartitionManager = (*EventsAuditPartitionManager)(nil)` with `retaudit "…/internal/audit"` — round-5 M5; a bare `audit.PartitionManager` would resolve to the local package and not compile).
    - Integration test: old partition DETACHed → renamed to `events_audit_<YYYY_MM>_detached_<unix>` → DROPped past grace; recent retained; a just-detached partition within grace is NOT dropped (the durable name-age discovery works).
    - Crash-atomicity test: a canonical-named partition detached-but-not-renamed is reconciled on the next cycle (renamed to `_detached_<unix>`) and eventually dropped — never permanently stranded (finding 7).
    - FINALIZE-recovery test: a child left `pg_inherits.inhdetachpending = true` (interrupted concurrent detach) is FINALIZEd on the next DetachExpiredPartitions cycle (no longer inhdetachpending / no longer a child), renamed to `_detached_<unix>`, and eventually dropped (round-3 finding 2).
    - All catalog discovery (reconcile AND drop, not only current-child queries) is schema-qualified (pg_namespace join); crash-orphan reconciliation and detached-table drop target ONLY tables carrying the durable `holomush:events_audit_partition` provenance marker (round-4 F9), never a same-named non-child table that lacks it — proven by a test that seeds a same-named, same-shape table WITHOUT the marker and asserts neither reconcile nor drop touches it.
    - STAMP-TIME provenance gate (round-5 H2): EnsurePartitions applies the marker ONLY after a schema-qualified pg_inherits child-ness check; a pre-existing SAME-NAMED NON-CHILD is NEVER stamped (EnsurePartitions fails closed with a structured oops rather than `COMMENT`-ing it). Proven by the full-RunOnce non-child test: after a RunOnce over a manually-created markerless same-named non-child, the relation carries NO marker (`obj_description` NULL), is not renamed to `_detached_<unix>`, and is not dropped.
    - EnsurePartitions stamps `COMMENT ON TABLE <partition> IS 'holomush:events_audit_partition'` on every GENUINE created child (assert via `obj_description`); the marker survives DETACH + the `_detached_<unix>` rename (assert on a detached partition before drop).
    - EnsurePartitions creates a covering partition for a within-retention PAST event_ms (not only current+forward).
    - DETACH runs outside an explicit tx (no "cannot run inside a transaction block" error).
    - New lines clear codecov patch @ 80% (budget error-branch tests).
  </acceptance_criteria>
  <done>A production events_audit PartitionManager exists; it prunes old partitions via detach-rename-then-drop-by-name-age and covers the retention+forward horizon, proven by the seed-old/recent integration test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: one-time legacy backfill — re-home events_audit_unpartitioned into the partitioned table</name>
  <files>internal/eventbus/audit/retention_partitions.go, internal/eventbus/audit/retention_partitions_test.go</files>
  <read_first>
    - internal/store/migrations/000052_events_audit_partition.up.sql (from 06-01 — leaves legacy rows in events_audit_unpartitioned, un-attached, with NO event_ms column)
    - internal/eventbus/audit/projection.go — the SHARED `eventMsFromULID(id ulid.ULID) int64 = ulid.Time(id.Time()).UnixNano()` helper (from 06-01, same package `internal/eventbus/audit`); the backfill MUST call this EXACT helper so a legacy row and a later replay of the same event derive an IDENTICAL event_ms and dedup. Do NOT reimplement it, and do NOT write `ulid.Time(id)` (a compile error — Time takes a uint64 ms; the round-1 form was invalid).
    - internal/eventbus/history/cold_postgres.go:140 (cold reads FROM events_audit — legacy rows are invisible to history until re-homed; this is why the backfill exists)
    - .claude/rules/database-migrations.md (backfills run as a separate one-shot job, NOT inside the migration — this is that job)
  </read_first>
  <behavior>
    - Backfill(ctx): if `events_audit_unpartitioned` exists, copy every legacy row into the partitioned `events_audit`, computing `event_ms = eventMsFromULID(parse(id))` in Go (the SAME 06-01 helper writeAuditRow uses), ensuring a covering partition exists for each row's event_ms first; `ON CONFLICT (id, event_ms) DO NOTHING`; process in bounded chunks. Then rename `events_audit_unpartitioned` → `events_audit_legacy_migrated` (or DROP). Re-running is a no-op once the old table is gone (idempotent).
    - Straddle idempotency (the review's required migration-straddle test): a legacy row for event E (in events_audit_unpartitioned) → Backfill → a later DLQ replay of E via writeAuditRow → exactly ONE events_audit row (backfill and writeAuditRow derive event_ms identically from E's ULID via the shared helper).
  </behavior>
  <action>
    Add a one-time `Backfill(ctx)` method (on the manager or a sibling) that re-homes legacy
    rows so no audit history is lost after 06-01's rename. Read legacy rows from
    `events_audit_unpartitioned` in bounded chunks (ORDER BY id, keyset-paginate); for each
    row compute `event_ms := eventMsFromULID(parsedID)` in Go (06-01's shared helper —
    `ulid.Time(parsedID.Time()).UnixNano()`; NOT `ulid.Time(parsedID)`); EnsurePartitions
    over the legacy event_ms range (reuse Task 1's ensure logic — create the historical
    monthlies the rows need); `INSERT INTO events_audit (... , event_ms) SELECT/VALUES ...
    ON CONFLICT (id, event_ms) DO NOTHING`. After all rows are copied, rename the old table
    to `events_audit_legacy_migrated` (retained briefly for ops) or DROP it. Make it
    idempotent: if `events_audit_unpartitioned` is absent, return nil immediately. Structured
    `oops` at each boundary; `*Context` slog variants.
    Write the STRADDLE integration test (`//go:build integration`, Postgres): insert a legacy
    row into events_audit_unpartitioned with an OLD event ULID, run the backfill, then call
    writeAuditRow (the DLQ-replay path) for the SAME event with a DIFFERENT meta.Timestamp →
    assert exactly ONE events_audit row (the review's required migration-straddle idempotency
    test). Also assert legacy rows are queryable via events_audit after backfill.
  </action>
  <verify>
    <automated># offload-exempt: acceptance gate — raw exit code + tail output is the verify signal (executor sub-agent, offload-deny exempt)
out=$(task test:int -- ./internal/eventbus/audit/... 2>&1); rc=$?; printf '%s\n' "$out" | tail -30; test "$rc" -eq 0 || { echo "FAIL: audit integration suite red (rc=$rc)"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - After Backfill, every legacy row is present in the partitioned events_audit (queryable via cold reads) and events_audit_unpartitioned is gone (renamed/dropped).
    - Straddle test: legacy row + post-backfill replay of the same event → exactly ONE row (backfill/writeAuditRow event_ms parity).
    - Backfill is idempotent (second call is a no-op).
    - New lines clear codecov patch @ 80% (budget error-branch tests).
  </acceptance_criteria>
  <done>Legacy events_audit rows are re-homed into the partitioned table with no audit-history loss and no duplicate on later replay.</done>
</task>

<task type="auto">
  <name>Task 3: config knob + RetentionWorker initial-delay mode + wire into SubsystemAuditProjection with a synchronous first-cycle gate</name>
  <files>internal/eventbus/audit/subsystem.go, internal/eventbus/audit/subsystem_test.go, internal/eventbus/config.go, internal/audit/retention.go, internal/audit/retention_test.go, cmd/holomush/core.go</files>
  <read_first>
    - internal/eventbus/audit/subsystem.go:65-137 (audit Config + Config.Defaults() at :100 — where retention fields land), :202-262 (Subsystem.Start: resolves the pool via s.poolProv.Pool() at :213, newProjection + p.start(workerCtx) at :229-236, plugin-mgr rollback drain pattern at :246-260 — TODAY the projection starts FIRST), :270-293 (Subsystem.Stop drains)
    - internal/eventbus/audit/subsystem_test.go (the existing audit-subsystem test file — the named home for the Config.Validate unit test, round-4 LOW: negative RetainWindow/PurgeInterval rejection lives here, adjacent to Config.Defaults())
    - internal/audit/retention.go:16-29 (RetentionConfig / DefaultRetentionConfig 90d/7d/24h — the mapping target), :51-59 (NewRetentionWorker — extend with an initial-delay option), :62-101 (RunOnce: Ensure→Purge→Detach→Drop — note it ALSO Detach/Drops; the boot gate must NOT call RunOnce), :83 (`now.Add(-w.cfg.RetainDenials)` — a NEGATIVE RetainDenials makes this cutoff FUTURE-facing → detaches every partition; validation must reject it), :103-109 (Start ALWAYS returns nil + spawns run() — so a plain Start CANNOT surface a first-cycle DDL failure; the prior "rollback on Start failure" was dead), :127-148 (run() calls RunOnce IMMEDIATELY at :133-136 THEN on a ticker — round-4 MEDIUM: this immediate destructive cycle is exactly what must be gated behind an initial-delay so a red deploy never prunes) — :130 `time.NewTicker(w.cfg.PurgeInterval)` PANICS on a non-positive interval; validation must reject `PurgeInterval <= 0`
    - internal/audit/retention_test.go (the existing RetentionWorker test file — the named home for the initial-delay unit test)
    - internal/eventbus/config.go (operator event_bus config; the existing DLQ MaxAge lives here — do NOT shadow it; name the new field distinctly, e.g. audit.retain_window)
    - cmd/holomush/core.go (audit subsystem construction; core.go is in the .codecov.yml ignore list, coverage-exempt)
    - 06-RESEARCH.md §OPS-02(a) step 3 + assumption A5 (home the worker inside SubsystemAuditProjection to AVOID the productionSubsystems named-param cascade / new SubsystemID iota + stringer regen)
    - IMPORT ALIAS (round-5 M5): `internal/eventbus/audit` (this subsystem's package) and `internal/audit` (RetentionWorker / RetentionConfig / PartitionManager / WithInitialDelay) are BOTH `package audit`. In `subsystem.go` (package audit) AND in `cmd/holomush/core.go` (where the name `audit` already binds to `internal/eventbus/audit`, per core.go:41), `internal/audit` MUST be imported under a distinct alias — `retaudit "github.com/holomush/holomush/internal/audit"` — and referenced as `retaudit.NewRetentionWorker`, `retaudit.RetentionConfig`, `retaudit.WithInitialDelay`. A bare `audit.NewRetentionWorker` does NOT compile at either site.
  </read_first>
  <action>
    Config (D-02): add a `RetainWindow time.Duration` field (default 90 days, mirroring the
    ABAC RetentionConfig.RetainDenials default) + `PurgeInterval` (default 24h) to
    `audit.Config`, defaulted in `Config.Defaults()` ONLY when zero-valued. Surface it via
    `internal/eventbus/config.go` under a DISTINCT name that does NOT shadow the existing DLQ
    `MaxAge` (e.g. `audit.retain_window`). Map it onto
    `retaudit.RetentionConfig{RetainDenials: RetainWindow, PurgeInterval: ...}` (the aliased
    `internal/audit` import — round-5 M5; a bare `audit.RetentionConfig` inside package audit
    resolves to the local package and does not compile)
    (RetainAllows unused — the PurgeExpiredAllows no-op consumes it harmlessly).
    CONFIG VALIDATION (round-3 finding 3 — negative config → detach-all / ticker panic):
    Defaults() only fills ZERO values, so a NEGATIVE survives. Add a validation step (a
    `Config.Validate()` method, or validation inside the retention-config mapping helper) that
    REJECTS `RetainWindow <= 0` and `PurgeInterval <= 0` with a structured `oops` error BEFORE
    the value reaches `NewRetentionWorker`. This lives in the TESTED audit.Config / mapping
    surface (NOT only in codecov-ignored core.go), so a unit test covers it. Rationale: a
    negative RetainWindow makes `now.Add(-RetainDenials)` future-facing → DetachExpiredPartitions
    would detach EVERY partition (retention.go:83); a non-positive PurgeInterval panics
    `time.NewTicker` (retention.go:130). The boot gate (below) surfaces a validation error from
    Start before the projection accepts traffic.
    RETENTION WORKER INITIAL-DELAY (round-4 MEDIUM — "no prune on a red deploy" is not actually
    achieved): today `RetentionWorker.run` calls `RunOnce` IMMEDIATELY (retention.go:133-136) before
    the ticker, so even though the boot gate below is Backfill+Ensure-only, the periodic worker STILL
    fires a destructive Detach/Drop the instant it is started — a later subsystem failure on a red
    deploy can therefore trigger DETACH/DROP. Add an INITIAL-DELAY mode to `RetentionWorker`
    (internal/audit/retention.go): a functional option (e.g. `WithInitialDelay(d time.Duration)` or
    `WithSkipFirstRun()`) that makes `run` SKIP the immediate `RunOnce` and instead wait for the
    first ticker tick (or a configured delay) before the first destructive cycle; the default
    (no option) preserves the existing immediate-run behavior so the ABAC access_audit worker is
    unaffected. Wire the audit RetentionWorker with this option so its first Detach/Drop only fires
    after the first PurgeInterval tick (whole-process readiness is established by then). Unit-test the
    option in internal/audit/retention_test.go: assert that with the initial-delay option, RunOnce is
    NOT invoked before the first tick (e.g. inject a mock PartitionManager and a controllable clock/
    short ticker, assert zero Detach/Drop calls immediately after Start, and at-least-one after a tick).
    Wiring — REORDER Start so the backfill precedes traffic (review findings 9 & 10). Inside
    `Subsystem.Start`, AFTER the pool is resolved but BEFORE `newProjection`/`p.start`:
    (1) construct the `EventsAuditPartitionManager` (Task 1) over the pool;
    (2) SYNCHRONOUS BOOT GATE — run `Backfill` (Task 2) then an initial `EnsurePartitions`
    ONLY. Do NOT call a full `RunOnce` here (it also Detach/Drops via retention.go:63-98; a
    first-cycle boot must never prune, especially on a red deploy — review finding 9). If the
    gate errors, RETURN the error from Start immediately (nothing to roll back — the projection
    has not started yet; this also fixes the dead "RetentionWorker.Start always returns nil"
    rollback, finding 10);
    (3) ONLY after the gate succeeds, `newProjection` + `p.start(workerCtx)` so the projection
    begins accepting traffic AFTER legacy rows are re-homed (no dual-copy race where a DLQ
    replay inserts into the parent before the backfill while the legacy row still sits in
    events_audit_unpartitioned — after backfill, ON CONFLICT collapses to one row);
    (4) THEN `retaudit.NewRetentionWorker(retentionCfg, mgr, retaudit.WithInitialDelay(...))`
    (the aliased `internal/audit` import — round-5 M5; NOT `audit.NewRetentionWorker`) with the
    initial-delay option from above so the FIRST Detach/Drop only fires after the first ticker
    tick, not the instant Start is called — round-4 MEDIUM) `.Start(workerCtx)` for the periodic
    Detach/Drop loop (later RunOnce failures are logged non-fatally — documented async
    degradation). Store the worker on the Subsystem; `Subsystem.Stop` calls `worker.Stop()`
    in the drain sequence. Keep the existing projection-drain rollback for the case where
    p.start or the plugin-mgr fails AFTER the gate. Do NOT add a new `SubsystemID` / touch
    `productionSubsystems` (A5). Thread the operator RetainWindow from `cmd/holomush/core.go`
    (which ALSO needs the `retaudit` alias — its `audit` import is `internal/eventbus/audit`).
    FIRST-PRUNE DELAY DOC (round-5 LOW): document in the subsystem comment AND the SUMMARY that,
    because of the initial-delay option, the FIRST destructive Detach/Drop is deferred by one
    `PurgeInterval` (~24h at the 24h default) after boot — operators must NOT expect an immediate
    detach on deploy; the boot gate only runs the non-destructive Backfill + EnsurePartitions.
    `*Context` slog variants. Document the intermediate dual-existence window in a comment.
  </action>
  <verify>
    <automated># offload-exempt: acceptance gate — raw exit code + tail output is the verify signal (executor sub-agent, offload-deny exempt)
out=$(task test:int -- ./internal/eventbus/audit/... 2>&1); rc=$?; printf '%s\n' "$out" | tail -30; test "$rc" -eq 0 || { echo "FAIL: audit integration suite red (rc=$rc)"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `audit.Config` has `RetainWindow` (default 90d) + `PurgeInterval` defaulted in Defaults(); overridable via event_bus config under a name that does NOT collide with DLQ MaxAge (unit test asserts a non-default survives Defaults()); a mapping helper yields `internal/audit.RetentionConfig{RetainDenials: RetainWindow, PurgeInterval: ...}` (unit test).
    - Config validation REJECTS `RetainWindow <= 0` and `PurgeInterval <= 0` (unit test in the NAMED file `internal/eventbus/audit/subsystem_test.go` asserts both error, in the tested audit.Config/mapping surface — NOT only in codecov-ignored core.go; round-4 LOW: the Config.Validate test file is named explicitly); a rejected config surfaces as a Start error, never a detach-all cutoff or a time.NewTicker panic.
    - RetentionWorker has an initial-delay option (round-4 MEDIUM); the audit worker is wired with it so the FIRST destructive Detach/Drop cycle does NOT run the instant Start is called — a unit test in `internal/audit/retention_test.go` asserts zero Detach/Drop invocations immediately after Start and at-least-one after the first tick (the default no-option behavior — used by the ABAC access_audit worker — still runs immediately).
    - Start order (grep/inspection + test): Backfill + EnsurePartitions run BEFORE `p.start`; the boot gate does NOT call RunOnce/Detach/Drop (finding 9); the periodic worker starts AFTER the projection AND does not prune until its first tick (finding round-4 MEDIUM).
    - `Subsystem.Start` runs the backfill + first EnsurePartitions SYNCHRONOUSLY and returns an error (before starting the projection) if that gate fails — proven by a test injecting a first-cycle DDL/backfill failure and asserting Start errors and the projection never started.
    - After a successful Start, the periodic worker prunes an old partition; `Subsystem.Stop` stops it cleanly (no goroutine leak; existing drain test green).
    - No new `SubsystemID`; `cmd/holomush/core.go` compiles with RetainWindow threaded in; `task test:int` green.
  </acceptance_criteria>
  <done>The RetentionWorker runs in production inside the audit projection subsystem; the synchronous boot gate (Backfill + EnsurePartitions only, before the projection accepts traffic) makes the all-or-nothing rollback real; the window is operator-configurable (90d default).</done>
</task>

<task type="auto">
  <name>Task 4: bootstrap orphan-check also scans events_audit_unpartitioned (defense-in-depth after the 000052 rename)</name>
  <files>cmd/holomush/bootstrap_orphan.go, cmd/holomush/bootstrap_orphan_test.go</files>
  <read_first>
    - cmd/holomush/bootstrap_orphan.go (runBootstrapOrphanCheck — the EXISTS/COUNT probe scanning ONLY events_audit for actor_kind=plugin AND actor_id IS NULL)
    - cmd/holomush/core.go:292-303 (runBootstrapOrphanCheck runs BEFORE the audit subsystem Start — so BEFORE 06-02's Backfill; after 000052 the fresh events_audit is empty and residual orphans sit in events_audit_unpartitioned, invisible to the current check)
    - cmd/holomush/bootstrap_orphan_test.go (the existing test seeding an orphan row into events_audit — extend it to also cover the events_audit_unpartitioned residual case)
    - internal/store/migrations/000017/000038 (the to_regclass existence idiom, for the "only scan events_audit_unpartitioned WHEN it exists" guard)
  </read_first>
  <action>
    Addresses review finding 5 (bootstrap orphan-check gap). Extend `runBootstrapOrphanCheck`
    so the plugin-actor-orphan probe ALSO covers residual legacy rows: when
    `events_audit_unpartitioned` exists (to_regclass guard), the EXISTS probe (and the
    failure-path COUNT) must consider rows in BOTH `events_audit` AND
    `events_audit_unpartitioned` (a UNION ALL of the two EXISTS probes, or an OR over both
    tables guarded by presence — do NOT hard-reference events_audit_unpartitioned when it is
    absent, or a clean install errors). This keeps the pre-Start defense-in-depth gate honest
    for a restore-from-old-backup that lands orphaned plugin rows in the un-partitioned table
    before 06-02's Backfill re-homes them. Structured `oops`; `*Context` slog (a ctx is in scope).
    Extend the test to seed an orphan into events_audit_unpartitioned (with the table present)
    and assert the check fails; and assert a clean install (no events_audit_unpartitioned)
    still passes.
  </action>
  <verify>
    <automated># offload-exempt: acceptance gate — raw exit code + tail output is the verify signal (executor sub-agent, offload-deny exempt)
out=$(task test:int -- ./cmd/holomush/... 2>&1); rc=$?; printf '%s\n' "$out" | tail -20; test "$rc" -eq 0 || { echo "FAIL: cmd/holomush integration suite red (rc=$rc)"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - runBootstrapOrphanCheck detects a plugin-actor orphan seeded into events_audit_unpartitioned (table present) — not only events_audit.
    - When events_audit_unpartitioned is ABSENT (clean install), the check still runs without a "relation does not exist" error and passes when there are no orphans.
    - The existing events_audit orphan test still passes; `task test:int` green.
  </acceptance_criteria>
  <done>The bootstrap orphan gate scans residual events_audit_unpartitioned rows when the table is present, closing the defense-in-depth hole 000052's rename opened.</done>
</task>

<task type="auto">
  <name>Task 5: re-choreograph the deploy sequence — stop gateway + cloudflared + old core → migrate (000052) → start new core (readiness-gated) → restore traffic</name>
  <files>.github/workflows/deploy.yaml, site/src/content/docs/operating/how-to/sandbox/sandbox-operations.md</files>
  <read_first>
    - .github/workflows/deploy.yaml:71-109 (the "Pull + migrate + restart" step — the CURRENT order: `pull` at :96, `build backup` at :97, `up -d --no-recreate postgres` at :98, `run --rm -T core migrate` at :107, then `up -d` at :108 recreates ALL services; the OLD core is still running while migrate runs — the round-4 HIGH window)
    - .github/workflows/deploy.yaml:111-150 (the post-restart "Verify deployed version + health" step that polls gateway readiness — this runs AFTER traffic is restored; the new choreography must gate the new core BEFORE the final `up -d`)
    - compose.prod.yaml:54-90 (the `core` service: `--metrics-addr=0.0.0.0:9100`, healthcheck `wget --spider http://127.0.0.1:9100/healthz/readiness`, `interval: 5s`, `timeout: 5s`, `retries: 15`, `start_period: 15s` — health budget ≈ start_period 15s + retries 15 × interval 5s ≈ 90s; the readiness signal `up -d --wait` blocks on) and :92-126 (the `gateway` service, `depends_on: core: condition: service_healthy` — gateway is player-facing traffic) and :146-170 (`cloudflared`, `profiles: [tunnel]`, `depends_on: gateway` — the public tunnel; the deploy uses `--profile tunnel --profile backups`, so the three services to stop/restore are exactly `gateway`, `cloudflared`, `core`). VERIFY these service names against compose.prod.yaml before writing the stop/start commands.
    - site/src/content/docs/operating/how-to/sandbox/sandbox-operations.md:207-240 (the "Deploy a new version" runbook; the manual command sequence at :235-239 mirrors deploy.yaml and MUST be reordered identically — NOTE its migrate line at :238 lacks the `-T` + `</dev/null` stdin guard the CI has at deploy.yaml:107; the reorder MUST also ADD that guard, round-5 LOW)
    - internal/eventbus/audit/subsystem.go Subsystem.Start (from Task 3 — the synchronous Backfill+EnsurePartitions boot gate; a gate failure returns an error from Start → the core never reaches readiness) and cmd/holomush/core.go:1102-1104 (`registry.WaitReady` — the core process's readiness is what the healthcheck reflects)
    - internal/eventbus/audit/projection.go:414 (the old binary's `INSERT ... ON CONFLICT (id)` with no `event_ms` — the write that fails on BOTH the NOT NULL event_ms and the dropped id-alone unique index once 000052 lands)
  </read_first>
  <action>
    Addresses the round-4 HIGH + round-5 H1/M4 (operator decision: OPTION A — deploy
    CHOREOGRAPHY, NOT expand/contract; do NOT redesign 000052). Re-choreograph BOTH the CI
    workflow AND the manual runbook so `core migrate` never runs against a live old core, the
    ENTIRE player-traffic path is severed during the window, and player traffic is only restored
    after the NEW core is ready.
    In `.github/workflows/deploy.yaml` "Pull + migrate + restart" step, change the order to:
    (1) keep `pull core gateway cloudflared`, `build backup`, `up -d --no-recreate postgres`;
    (2) PRE-MIGRATE BACKFILL-BUDGET PROBE (round-5 M4): before stopping anything, count the
    legacy audit rows the new core's synchronous Backfill will process —
    `ROWS=$(docker compose --profile tunnel --profile backups exec -T postgres psql -U holomush
    -d holomush -tAc "SELECT count(*) FROM events_audit" </dev/null | tr -d '[:space:]')` — and
    echo it alongside the documented ~90s core health budget (start_period 15s + retries 15 ×
    interval 5s). If `ROWS` exceeds a documented threshold (a conservative constant, e.g. a few
    hundred thousand, chosen so the Backfill comfortably fits the ~90s window on the sandbox),
    print a clear `::warning::` that the operator SHOULD run an ahead-of-deploy backfill or
    temporarily raise the core `start_period` in compose before proceeding — so a fat history
    does not silently thrash `restart: unless-stopped` mid-backfill while `up -d --wait` is
    pending. Keep it a WARNING (non-fatal) so a legitimate large migration is not hard-blocked
    with no in-band alternative; the point is to make the risk BOUNDED and VISIBLE, not silent;
    (3) STOP the player-facing traffic path AND the old core BEFORE migrate (round-5 H1): stop
    ALL THREE — `docker compose --profile tunnel --profile backups stop cloudflared gateway core`.
    Stopping `cloudflared` + `gateway` severs the whole cloudflared→gateway→core traffic path, so
    NO player request can reach the new core during the migrate/readiness window — this is the
    belt that makes gating real EVEN THOUGH the new core's gRPC begins serving before whole-process
    readiness and does not DependsOn SubsystemAuditProjection (sub_grpc.go:166,771). Stopping `core`
    ENDS the incompatible-write window (no old-core audit INSERTs occur while it is down; the brief
    audit-write outage is the accepted, bounded risk). VERIFY the three service names against
    compose.prod.yaml;
    (4) `run --rm -T core migrate </dev/null` (unchanged flags — the `</dev/null` + `-T` stdin
    guard MUST be preserved per the existing holomush-aocap comment; migrate now runs with NO old
    core writing);
    (5) START ONLY the new core AND GATE on its readiness before restoring traffic:
    `docker compose --profile tunnel --profile backups up -d --wait --no-deps core` — `--wait`
    blocks until the core healthcheck (`/healthz/readiness`) passes, which only happens after the
    new core's synchronous audit Backfill+EnsurePartitions boot gate (Task 3) succeeds. `--no-deps`
    keeps gateway/cloudflared DOWN at this step (do not let compose pull them up as core's
    dependents). If the backfill/readiness gate fails, `up -d --wait` exits non-zero and
    `set -euo pipefail` ABORTS the deploy BEFORE gateway/cloudflared are started — traffic is never
    restored onto a bad core (the compensating control);
    (6) `docker compose --profile tunnel --profile backups up -d` to RESTORE player traffic
    (starts gateway + cloudflared, which were stopped in step 3) only after core is ready.
    Add a short comment block above the reordered commands explaining the choreography, the
    accepted bounded audit-write window (single-node sandbox), the traffic-path severance, and the
    ~90s backfill health budget + escape hatches, referencing the round-4 decision + round-5 H1/M4.
    Apply the IDENTICAL reorder to the manual runbook at sandbox-operations.md:235-239: insert the
    pre-migrate row-count probe + `stop cloudflared gateway core` before the migrate line; CHANGE
    the runbook migrate line to `docker compose --profile tunnel --profile backups run --rm -T core
    migrate </dev/null` (ADD the `-T` + `</dev/null` stdin guard the runbook currently lacks —
    round-5 LOW); add `up -d --wait --no-deps core` after migrate; then the final `up -d`. Add a
    one-paragraph note in the "Deploy a new version" section explaining WHY (000052 makes the old
    core's audit INSERT incompatible; stop cloudflared+gateway+core before migrate + readiness-gate
    is the safe sequence; the ~90s backfill budget). Do NOT change 000052 itself.
    Keep the existing post-deploy "Verify deployed version + health" step; it remains a
    belt-and-braces check after traffic is restored.
  </action>
  <verify>
    <automated>f=.github/workflows/deploy.yaml; stop_ln=$(rg -n 'docker compose[^\n]*stop[^\n]*cloudflared[^\n]*gateway[^\n]*core' "$f" | head -1 | cut -d: -f1); mig_ln=$(rg -n 'docker compose[^\n]*core migrate' "$f" | head -1 | cut -d: -f1); wait_ln=$(rg -n 'docker compose[^\n]*up -d --wait[^\n]*core' "$f" | head -1 | cut -d: -f1); test -n "$stop_ln" && test -n "$mig_ln" && test -n "$wait_ln" && test "$stop_ln" -lt "$mig_ln" && test "$mig_ln" -lt "$wait_ln" || { echo "FAIL: deploy.yaml choreography not stop(cloudflared,gateway,core) < migrate < readiness-gated start (stop=$stop_ln mig=$mig_ln wait=$wait_ln)"; exit 1; }; rg -q 'docker compose[^\n]*run --rm -T core migrate[^\n]*</dev/null' "$f" || { echo "FAIL: deploy.yaml migrate lost the -T + </dev/null stdin guard"; exit 1; }; r=site/src/content/docs/operating/how-to/sandbox/sandbox-operations.md; rg -q 'docker compose[^\n]*stop[^\n]*cloudflared[^\n]*gateway[^\n]*core' "$r" && rg -q 'docker compose[^\n]*up -d --wait[^\n]*core' "$r" && rg -q 'docker compose[^\n]*run --rm -T core migrate[^\n]*</dev/null' "$r" || { echo "FAIL: manual runbook not re-choreographed (stop-three / wait-core / -T stdin guard)"; exit 1; }; echo "PASS: deploy choreography stop(cloudflared,gateway,core) → migrate(-T </dev/null) → readiness-gated new core in both deploy.yaml and runbook"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yaml` stops ALL THREE of `cloudflared`, `gateway`, and `core` BEFORE `core migrate` (round-5 H1 — the whole player-traffic path is severed, not only `core`), starts ONLY the new core with `up -d --wait --no-deps core` (readiness-gated on the healthcheck that reflects the synchronous backfill gate), and only then runs the final `up -d` to restore gateway + cloudflared traffic — line-order asserted (stop(cloudflared,gateway,core) < migrate < wait-gated-start).
    - The manual runbook (sandbox-operations.md) mirrors the same reorder (stop all three → migrate → wait-gated new core → restore) + an explanatory paragraph on the 000052 old-writer incompatibility, the traffic-path severance, the accepted bounded audit-write window, and the ~90s backfill health budget.
    - The `-T` + `</dev/null` stdin guard is present on BOTH the CI migrate line (preserved) AND the manual runbook migrate line (ADDED — round-5 LOW; the runbook currently lacks it).
    - A PRE-MIGRATE backfill-budget probe (round-5 M4) counts `events_audit` rows against a documented threshold and warns (non-fatal) above it, recommending an ahead-of-deploy backfill or a temporary `start_period` raise; the ~90s health budget + escape hatches are documented in deploy.yaml + the runbook.
    - 000052 itself is UNCHANGED (choreography, not expand/contract — per the operator decision).
    - The post-deploy readiness/version verify step is retained.
  </acceptance_criteria>
  <done>The production deploy (CI + manual runbook) stops the player-facing gateway + cloudflared tunnel AND the old core before applying 000052, starts only the new core, gates restoring player traffic on the new core's readiness (its synchronous audit backfill gate), and surfaces the backfill-vs-health-budget risk with a pre-migrate size probe — closing the round-4/round-5 live-deployment migration + traffic-gating window without redesigning the migration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: co-ship gate — 06-01 (000052 migration) and 06-02 (Backfill + deploy choreography) MUST land in ONE PR</name>
  <files>(no in-repo file — a pre-PR ship gate)</files>
  <action>
    Addresses round-3 finding 4/11 (co-deploy is content-truth but NOT tooling-enforced). HALT
    before PR creation. 000052 (06-01) renames all history off `events_audit`; ONLY this plan's
    Backfill re-homes it and restores cold-history visibility. Shipping 06-01 alone is a
    cold-history blackout in production. A human MUST confirm the branch/PR contains BOTH
    06-01's `000052_events_audit_partition.{up,down}.sql` + projection.go changes AND 06-02's
    `retention_partitions.go` Backfill + subsystem wiring AND the Task 5 deploy-choreography edits
    (`.github/workflows/deploy.yaml` + `sandbox-operations.md`) — the deploy reorder is what makes
    000052 safe against the live old writer, so it is part of the SAME indivisible ship unit — in a
    SINGLE PR, before push/merge. The executor writes no file; it verifies the diff and records the
    confirmation in the SUMMARY. This checkpoint is never auto-approvable.
  </action>
  <what-built>
    06-01 and 06-02 are one logical ship unit. 06-01's migration renames history off
    events_audit; 06-02's Backfill is the only path that restores it. `must_haves` asserted the
    co-deploy but nothing GATED it — a 06-01-only merge would blackout cold history. This
    blocking checkpoint is the tooling enforcement the round-3 review required.
  </what-built>
  <how-to-verify>
    1. `git diff --name-only origin/main...HEAD` (or the PR file list) includes ALL of:
       `internal/store/migrations/000052_events_audit_partition.up.sql` /`.down.sql`,
       `internal/eventbus/audit/retention_partitions.go` (Backfill) + the subsystem.go Start reorder,
       AND the Task 5 deploy-choreography edits `.github/workflows/deploy.yaml` +
       `site/src/content/docs/operating/how-to/sandbox/sandbox-operations.md`.
    2. Confirm they are in ONE PR (not two separate PRs where 06-01 could merge first).
    3. If 06-01 or the deploy-choreography edits are not present in the same PR, STOP — do not merge until co-included.
  </how-to-verify>
  <resume-signal>Type "co-ship confirmed" (with the PR/file-list evidence) or describe the gap.</resume-signal>
  <verify>
    <automated>MISSING — pre-PR ship gate; verified by confirming 06-01's 000052 migration + projection.go and 06-02's Backfill + subsystem wiring are in ONE PR (evidence recorded in SUMMARY)</automated>
  </verify>
  <done>A human has confirmed 06-01 and 06-02 ship in a single PR; the confirmation is recorded in the SUMMARY, so 000052 cannot reach production without the Backfill that restores cold history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retention worker → events_audit partitions (DDL) | The worker issues DETACH/DROP; a wrong-window or wrong-table drop destroys audit records |
| operator config → retention window | An operator-supplied window governs how long audit history is retained (compliance-relevant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-2-01 | Denial/Tampering | DetachExpiredPartitions/DropDetachedPartitions | high | mitigate | Detach only children whose UPPER event_ms bound <= olderThan; never touch events_audit_unpartitioned or non-child tables; drop only detached children whose `_detached_<unix>` name-age exceeds grace AND that carry the durable `holomush:events_audit_partition` provenance marker; integration test proves recent partitions survive and within-grace detached ones are not dropped |
| T-06-2-11 | Tampering (forge provenance at stamp time / drop a non-partition same-named table) | provenance of an absent-from-pg_inherits table | high | mitigate | Round-5 H2: the STAMP itself is gated, not only reconcile/drop. EnsurePartitions applies the durable `COMMENT ON TABLE ... IS 'holomush:events_audit_partition'` marker ONLY after a schema-qualified pg_inherits check (parent OID = `public.events_audit`, child OID = the partition) confirms genuine current child-ness; it FAILS CLOSED (no stamp, structured oops) on a pre-existing same-named NON-child that `CREATE TABLE IF NOT EXISTS ... PARTITION OF` silently skipped (PG's IF NOT EXISTS does not guarantee the existing relation matches the partition). Reconcile AND drop still additionally REQUIRE the marker (schema-qualified). A full-RunOnce test seeds a markerless same-named non-child and asserts NO marker, NO rename, NO drop — so a coincidentally-named non-child can neither be granted the trusted marker at stamp time nor swept later |
| T-06-2-12 | Denial (destructive prune on a red deploy) | RetentionWorker immediate RunOnce on Start | medium | mitigate | RetentionWorker gains an initial-delay mode (round-4 MEDIUM); the audit worker is wired to SKIP the immediate RunOnce so the first Detach/Drop only fires after the first ticker tick / process readiness — a subsystem failing after the boot gate cannot trigger DETACH/DROP during a failed deploy; unit test asserts no Detach/Drop immediately after Start |
| T-06-2-13 | Denial/Tampering (live old-writer breakage + un-gated player traffic across migrate) | 000052 applied while the old core / player traffic path is still live | high | mitigate | Deploy choreography (round-4 HIGH + round-5 H1, Task 5): the CI workflow + manual runbook STOP all three of `cloudflared`, `gateway`, AND `core` before `core migrate`, start ONLY the new core, and gate restoring traffic on `up -d --wait --no-deps core` (the readiness/backfill gate) before starting gateway + cloudflared LAST. Stopping cloudflared+gateway severs the whole player-traffic path so the old (or any) gateway cannot reach the new core before the readiness gate — closing the round-5 gap that gRPC serves before whole-process readiness and does not DependsOn SubsystemAuditProjection (sub_grpc.go:166,771). The old binary's `INSERT ... ON CONFLICT (id)` (projection.go:414) never runs against the post-000052 schema; the brief audit-write outage is an accepted bounded single-node risk with the readiness gate as the compensating control |
| T-06-2-14 | Denial (health-check restart thrash mid-backfill) | synchronous Backfill in Subsystem.Start exceeds the core health budget on a fat history | medium | mitigate | Round-5 M4: the synchronous Backfill runs before whole-process readiness; a large events_audit_unpartitioned can exceed the core health budget (compose.prod.yaml: start_period 15s + retries 15 × interval 5s ≈ 90s) → Docker marks unhealthy → `restart: unless-stopped` thrashes while `up -d --wait` is pending. Task 5 adds a PRE-MIGRATE row-count probe against a documented threshold that warns the operator (ahead-of-deploy backfill or a temporary start_period raise) before proceeding; the budget + escape hatches are documented in deploy.yaml + the runbook. Sandbox is expected well under threshold; the probe makes the risk bounded and visible |
| T-06-2-02 | Tampering | manager targets wrong table | medium | mitigate | DDL literals hardcode `events_audit`; compile-time interface satisfaction; test seeds/asserts against events_audit specifically |
| T-06-2-03 | Denial | first-cycle DDL failure leaves projection half-up | medium | mitigate | The SYNCHRONOUS boot gate (Backfill + EnsurePartitions ONLY — no Detach/Drop, finding 9) runs BEFORE the projection starts; a gate failure returns an error from Start before any projection exists (finding 10), so there is nothing half-up; bounded drain in Stop; drain test green |
| T-06-2-04 | Denial | replayed historical event has no covering partition (unprunable sink) | medium | mitigate | EnsurePartitions covers the retention window backward + forward; there is no DEFAULT sink (06-01), so an out-of-window replay fails loud (message retained) rather than piling into an unprunable partition |
| T-06-2-06 | Denial (permanent strand) | interrupted/crashed DETACH CONCURRENTLY | medium | mitigate | Each detach cycle FIRST finalizes any child marked pg_inherits.inhdetachpending (ALTER TABLE ... DETACH PARTITION ... FINALIZE — round-3 finding 2) THEN reconciles canonical-named events_audit_YYYY_MM tables that are no longer children (crash-orphaned) into the _detached_<unix> form so DropDetachedPartitions can eventually reclaim them; integration tests inject BOTH the pending-finalize state and the post-detach crash and assert recovery+drop |
| T-06-2-09 | Denial (detach-all / process panic) | negative/zero retention config | high | mitigate | Config validation REJECTS RetainWindow <= 0 (would make the cutoff future-facing and detach every partition, retention.go:83) and PurgeInterval <= 0 (would panic time.NewTicker, retention.go:130) in the tested audit.Config surface, surfaced as a Start error before the projection accepts traffic; unit test asserts both rejections (round-3 finding 3) |
| T-06-2-10 | Denial (cold-history blackout) | 000052 (06-01) merged without 06-02's Backfill | high | mitigate | 000052 renames all history off events_audit; only 06-02's Backfill re-homes it. A blocking pre-PR co-ship checkpoint (Task 6) confirms 06-01 + 06-02's Backfill + the deploy-choreography edits land in ONE PR — tooling-enforced, not only a must_haves truth (round-3 finding 4/11) |
| T-06-2-07 | Repudiation (missed orphan) | restore-from-backup orphans invisible to the pre-Start gate | medium | mitigate | runBootstrapOrphanCheck also scans events_audit_unpartitioned when present (finding 5), so residual legacy plugin-actor orphans are caught by the boot gate that runs before the Backfill re-homes them |
| T-06-2-08 | Tampering (cross-table dual row) | DLQ replay races the backfill (no cross-table UNIQUE) | medium | mitigate | Start order backfills BEFORE the projection accepts traffic (finding 10); the transient dual-existence window is documented and ON CONFLICT (id, event_ms) collapses to one row after backfill |
| T-06-2-05 | Repudiation | over-aggressive window silently discards required audit history | low | accept | Window is operator-configured with a conservative 90d default; documented as adjustable; not auto-shrinking |
</threat_model>

<verification>
- `task test:int` green: PartitionManager prunes old / retains recent + reconciles crash-orphans (marker-gated); the boot gate (Backfill + EnsurePartitions) runs before the projection; the worker runs via the subsystem lifecycle; Stop is clean; the bootstrap orphan check catches events_audit_unpartitioned residuals.
- `task test` green: config default + mapping + Config.Validate (negative-config rejection) unit tests; the RetentionWorker initial-delay unit test (no destructive cycle on Start).
- Deploy choreography: `.github/workflows/deploy.yaml` + the manual runbook are re-ordered (pre-migrate budget probe → stop cloudflared+gateway+core → migrate(-T </dev/null) → readiness-gated new-core start → restore gateway+cloudflared), asserted statically by Task 5's ordering check.
</verification>

<success_criteria>
events_audit rows past the retention window are pruned by the wired RetentionWorker; the table no longer grows unbounded (ROADMAP Phase 6 success criterion #1 / OPS-02).
</success_criteria>

## Artifacts this phase produces (this plan)
- `internal/eventbus/audit/retention_partitions.go` (NEW) — `EventsAuditPartitionManager` (full PartitionManager for events_audit, importing `internal/audit` under the alias `retaudit` — round-5 M5, both packages are `package audit`: EnsurePartitions covers retention-window-backward+forward AND stamps the durable `holomush:events_audit_partition` provenance marker on each created partition ONLY after a schema-qualified pg_inherits child-ness check (round-5 H2 — fails closed on a same-named non-child); DetachExpiredPartitions FINALIZEs inhdetachpending children THEN reconciles crash-orphans (marker-gated) THEN detaches CONCURRENTLY + renames to `events_audit_<YYYY_MM>_detached_<unix>`, all catalog discovery schema-qualified; DropDetachedPartitions drops by parsed name-age AND the provenance marker) PLUS the one-time `Backfill(ctx)` that re-homes legacy events_audit_unpartitioned rows via 06-01's shared `eventMsFromULID`.
- `internal/eventbus/audit/retention_partitions_test.go` (NEW) — Ensure/Detach-rename/Drop-by-name-age integration test + crash-orphan reconciliation test + FINALIZE-recovery test (pending-detach child finalized) + STAMP-TIME provenance-gate test (a full RunOnce over a markerless same-named NON-child asserts NO marker / NO rename / NO drop — round-5 H2) + marker-provenance reconcile/drop test (round-4 F9) + the migration-straddle idempotency test (legacy row + replay → 1 row).
- NEW SYMBOL: durable partition provenance marker `COMMENT ON TABLE <partition> IS 'holomush:events_audit_partition'` (round-4 F9) — STAMPED only behind a pg_inherits child-ness gate (round-5 H2); required by reconcile + drop; survives DETACH and the `_detached_<unix>` rename.
- NEW ALIAS convention (round-5 M5): `retaudit "github.com/holomush/holomush/internal/audit"` at every site (`retention_partitions.go`, `subsystem.go`, `cmd/holomush/core.go`) that references `internal/audit`'s `PartitionManager` / `NewRetentionWorker` / `RetentionConfig` / `WithInitialDelay`, because the local package is also `audit` (`internal/eventbus/audit`).
- NEW SYMBOL: `RetentionWorker` initial-delay option (e.g. `audit.WithInitialDelay(...)` / `WithSkipFirstRun()`) in `internal/audit/retention.go` (round-4 MEDIUM) — the audit worker skips the immediate RunOnce so no destructive cycle fires on Start; unit-tested in `internal/audit/retention_test.go`. Default (no option) preserves the existing immediate-run behavior for the ABAC access_audit worker.
- `RetainWindow` / `PurgeInterval` fields on `audit.Config` + Defaults() (90d) + a Config validation step (`Config.Validate`) that REJECTS non-positive RetainWindow/PurgeInterval (round-3 finding 3), unit-tested in the NAMED file `internal/eventbus/audit/subsystem_test.go` (round-4 LOW) + operator plumbing in `internal/eventbus/config.go` (distinct name — does not shadow DLQ MaxAge).
- RetentionWorker wiring inside `SubsystemAuditProjection` (subsystem.go) with a REORDERED Start (construct manager → Backfill → EnsurePartitions boot gate → then start projection → then periodic worker started with the initial-delay option) + threaded value in `cmd/holomush/core.go`; documents that the first destructive prune is deferred ~one PurgeInterval (~24h) after boot (round-5 LOW — ops must not expect immediate detach).
- DEPLOY CHOREOGRAPHY (round-4 HIGH + round-5 H1/M4/LOW, Task 5): `.github/workflows/deploy.yaml` + `site/src/content/docs/operating/how-to/sandbox/sandbox-operations.md` re-choreographed to PRE-MIGRATE row-count budget probe → stop `cloudflared` + `gateway` + old `core` (sever the whole player-traffic path, round-5 H1) → `core migrate` (`-T </dev/null` guard on BOTH the CI and runbook lines — round-5 LOW) → `up -d --wait --no-deps core` (readiness-gated on the backfill boot gate) → `up -d` (restore gateway + cloudflared). NOT expand/contract — 000052 is unchanged. The ~90s core health budget + escape hatches (ahead-of-deploy backfill / raise start_period) are documented inline.
- Extended `runBootstrapOrphanCheck` (cmd/holomush/bootstrap_orphan.go) — also scans events_audit_unpartitioned when present.
- NEW convention: detached-partition durable metadata via the `events_audit_<YYYY_MM>_detached_<unix>` rename (drop-eligibility parsed from the suffix) + the provenance COMMENT marker; FINALIZE + crash-orphan reconciliation passes.
- GATE (pre-PR, not a file): blocking co-ship checkpoint (Task 6) confirming 06-01 + 06-02 Backfill + the deploy-choreography edits land in ONE PR.

<output>
Create `.planning/phases/06-operational-hardening-assurance-gates/06-02-SUMMARY.md` when done.
</output>
