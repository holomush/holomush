---
phase: 06-operational-hardening-assurance-gates
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .codecov.yml
  - codecov.yml
  - .claude/rules/testing.md
  - CLAUDE.md
autonomous: false
requirements: [QUAL-01]
user_setup:
  - service: github-repo-settings
    why: "VERIFIED via gh api: the protect-main ruleset 11923801 currently requires [Build, Lint, Test, CodeRabbit, Integration Test, E2E Test] — NEITHER codecov/patch NOR codecov/project is a required check, and classic branch protection is off (404). So codecov POSTS the statuses but does NOT block. Making the coverage gate real requires adding both codecov checks to the ruleset (operator action, not in-repo YAML)."
    dashboard_config:
      - task: "Add BOTH the `codecov/patch` and `codecov/project` statuses to the protect-main ruleset's required status checks (neither is required today), then verify via `gh api repos/holomush/holomush/rulesets/11923801`"
        location: "GitHub → repo Settings → Rules → protect-main ruleset → Require status checks to pass"

must_haves:
  truths:
    - ".claude/rules/testing.md and CLAUDE.md describe the TRUE enforced reality: required protect-main checks are [Build, Lint, Test, CodeRabbit, Integration Test, E2E Test]; codecov/patch and codecov/project POST statuses but are NOT currently required checks; codecov measures patch + project, never per-package"
    - "Exactly one codecov config file exists at the repo root (.codecov.yml)"
    - ".codecov.yml project status is {target: auto, threshold: 1%} (a 1-percentage-point regression allowance); patch stays {target: 80%, threshold: 5%}"
  prohibitions:
    - "No doc states a 'per-package >80%' coverage MUST (the fictional bar)"
    - "No doc claims codecov/patch is CURRENTLY a hard/required merge gate (it is not — that would be a new false MUST)"
    - "The threshold is NOT described as 'no-drop' (1% permits a 1-point regression)"
    - "The duplicate codecov.yml no longer exists"
  artifacts:
    - .codecov.yml
  key_links:
    - "codecov/patch and codecov/project only BLOCK once added to the protect-main ruleset 11923801 (operator task); the YAML only makes codecov POST the statuses"
---

<objective>
QUAL-01 (D-07/D-08): reconcile the coverage policy so the documented bar and the
enforced bar agree, and add a project-coverage ratchet.

PREMISE CORRECTION — this OVERRIDES CONTEXT D-07 and durable memory 7qhyhb3hsb /
v5k0e4zs3s. D-07 assumed "codecov/patch @ 80% is ALREADY a hard merge gate via the
protect-main ruleset." That is FALSE against live state, VERIFIED via `gh api`: the
protect-main ruleset `11923801` (the only repo ruleset; no org rulesets; classic
branch protection OFF → `branches/main/protection` 404) requires exactly [Build, Lint,
Test, CodeRabbit, Integration Test, E2E Test] — NEITHER `codecov/patch` NOR
`codecov/project` is a required check. `.github/workflows/ci.yaml` marks the coverage
upload nonfatal (`fail_ci_if_error: false`). So codecov/patch POSTS a status but does
NOT currently BLOCK merges.

Therefore this plan does TWO things:
- Corrects the docs to the TRUE enforced reality (codecov/patch posts a status that is
  NOT currently a required check) — NOT the false "patch is a hard merge gate." This
  satisfies success-criterion #4's "the doc MUST is corrected to the enforced reality."
- Provides an operator task to ADD `codecov/patch` and `codecov/project` to the
  protect-main ruleset's required checks (making the gate real), verified via the ruleset
  API. If the operator does that, criterion #4's "the gate blocks merges" half also holds;
  if deferred, criterion #4 is still met via the doc correction, and the SUMMARY records
  the ratchet as "posting, not blocking."

Keeps the `.codecov.yml` project → {target: auto, threshold: 1%} change (a 1-percentage-
point regression allowance, NOT "no-drop") and the dual-file cleanup (D-08).

Output: `.codecov.yml` project ratchet, the duplicate `codecov.yml` deleted, the doc rows
rewritten to enforced reality, and an operator ruleset task (add both codecov checks).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-operational-hardening-assurance-gates/06-RESEARCH.md
@.planning/phases/06-operational-hardening-assurance-gates/06-PATTERNS.md

# Grounding analogs
@.codecov.yml
@.claude/rules/testing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: .codecov.yml project ratchet + delete duplicate codecov.yml</name>
  <files>.codecov.yml, codecov.yml</files>
  <read_first>
    - .codecov.yml:23-32 (the `coverage.status.project.default` = {target: 80%, threshold: 2%} block to change; `patch.default` = {target: 80%, threshold: 5%} to KEEP) and :14-20 (`notify.after_n_builds: 2` — why a pending status is not a failure)
    - codecov.yml (the 375B ignore-only duplicate to DELETE — its ignore list is a strict subset of .codecov.yml's)
    - 06-PATTERNS.md ".codecov.yml (MOD) + codecov.yml (DELETE)" + 06-RESEARCH.md §QUAL-01(a) steps 2 & 3 + landmine (two-upload merge jitter → threshold 1%)
  </read_first>
  <action>
    In `.codecov.yml`, change `coverage.status.project.default` from
    `{target: 80%, threshold: 2%}` to `{target: auto, threshold: 1%}`. `target: auto`
    means "must not fall below the base commit's project coverage" — the rising-floor
    ratchet D-07 wants (baseline ~54.6% is NOT retroactively blocked; only a PR that
    LOWERS project coverage beyond the 1% tolerance fails). Keep the `patch` status
    EXACTLY as-is ({target: 80%, threshold: 5%}) — do not touch it. (NOTE: patch is
    currently POSTED, NOT a required/blocking check — see Task 3; do not describe it as
    an enforced gate.) Label the 1% threshold accurately as a "1-percentage-point
    regression allowance" (NOT "no-drop" — 1% permits repeated 1-point declines); it
    absorbs the two-upload merge jitter (unit + integration + e2e merge after
    `after_n_builds: 2`). A note may record that it can tighten toward `threshold: 0%`
    (a true no-drop ratchet) once coverage stabilizes.
    DELETE the duplicate `codecov.yml` (the dotfile `.codecov.yml` carries everything;
    codecov's precedence between two co-located files is undocumented — deleting the
    duplicate removes the ambiguity entirely, D-08).
  </action>
  <verify>
    <automated>test ! -e codecov.yml && rg -n "target: auto|threshold: 1%|target: 80%" .codecov.yml | head</automated>
  </verify>
  <acceptance_criteria>
    - `.codecov.yml` `coverage.status.project.default` is `{target: auto, threshold: 1%}`; `patch.default` unchanged at `{target: 80%, threshold: 5%}`.
    - The duplicate `codecov.yml` no longer exists (exactly one codecov config at the repo root).
    - (Optional) `.codecov.yml` validated via `https://api.codecov.io/validate`; result noted in SUMMARY.
  </acceptance_criteria>
  <done>A single authoritative .codecov.yml carries a project ratchet {target: auto, threshold: 1%} (a 1-point regression allowance) and the unchanged patch status {target: 80%, threshold: 5%}.</done>
</task>

<task type="auto">
  <name>Task 2: rewrite the fictional "per-package >80%" doc MUST to enforced reality</name>
  <files>.claude/rules/testing.md, CLAUDE.md</files>
  <read_first>
    - .claude/rules/testing.md (the coverage MUST rows: "MUST maintain >80% coverage | Per-package coverage must exceed 80%" and "SHOULD target 90%+ coverage | For core packages" — the rows to rewrite)
    - CLAUDE.md (the Testing section row: "MUST maintain >80% coverage | Per-package | verify with `task test:cover`")
    - 06-PATTERNS.md ".claude/rules/testing.md + CLAUDE.md (doc MOD)" + 06-RESEARCH.md §QUAL-01(a) step 1 (the enforced reality to state; the 404-is-expected note)
  </read_first>
  <action>
    Rewrite the coverage MUST/SHOULD rows in BOTH files (the rows that currently mandate a
    whole-package coverage floor and a 90%+ core-package target) to the TRUE enforced
    reality (VERIFIED via gh api — do NOT write the false "patch is a hard merge gate"):
    - The protect-main ruleset `11923801` currently requires exactly [Build, Lint, Test,
      CodeRabbit, Integration Test, E2E Test]. codecov/patch and codecov/project POST
      statuses on PRs but are NOT (currently) required checks — they do NOT block merges
      today. (Adding them to the ruleset is the operator step in Task 3; write the docs so
      they are TRUE whether or not that step has been done — e.g. "codecov/patch posts an
      80%-on-changed-lines status; it becomes a merge gate only when added to the
      protect-main ruleset.")
    - Classic branch protection is OFF (`branches/main/protection` → 404 is expected).
    - codecov measures PATCH (changed lines) and PROJECT (whole-repo), and does NOT measure
      at package granularity — so the old "per-package >80%" floor was doubly wrong (wrong
      granularity AND not enforced).
    - A project ratchet ({target: auto, threshold: 1%}, a 1-point regression allowance —
      NOT "no-drop") guards whole-repo coverage; legacy code is not retroactively blocked.
    Remove the 90%+ core-package SHOULD line. Keep `task test:cover` as the local
    verification command. Make the edits targeted (do not disturb the eventbustest/natstest
    depguard MUSTs). The MUST/SHOULD ROWS must no longer assert any package-granularity
    coverage requirement, and must not claim codecov/patch is a current hard gate; an
    explanatory sentence noting codecov does not measure per-package is correct.
  </action>
  <verify>
    <automated>rg -n "codecov/patch|codecov/project|protect-main|project ratchet" .claude/rules/testing.md CLAUDE.md | head</automated>
  </verify>
  <acceptance_criteria>
    - Review confirms neither file has a MUST or SHOULD ROW mandating a package-granularity coverage floor (a 90%+ core-package target, or a "coverage must exceed 80% per package" requirement); an explanatory clause stating codecov does not measure per-package is allowed.
    - Neither file claims codecov/patch is CURRENTLY a hard/required merge gate; the docs describe it as a POSTED status that becomes a gate only when added to the protect-main ruleset. Positive grep confirms both files describe codecov patch (changed lines) + project ratchet and that the `branches/main/protection` 404 is expected.
    - `task lint` (docs/markdown lint) green after the edits; AGENTS.md symlink integrity intact (`task lint:docs-symmetry` if present).
  </acceptance_criteria>
  <done>The documented coverage bar matches the enforced bar; the fictional per-package MUST is gone.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: MANUAL — add codecov/patch AND codecov/project to the protect-main ruleset (operator action; optional-but-recommended)</name>
  <files>GitHub repo settings — protect-main ruleset 11923801 (no in-repo file)</files>
  <read_first>
    - 06-RESEARCH.md §QUAL-01(a) step 4 + §(b) (the ruleset action) + 06-VALIDATION.md "Manual-Only Verifications"
    - The VERIFIED live state: `gh api repos/holomush/holomush/rulesets/11923801` currently lists required checks [Build, Lint, Test, CodeRabbit, Integration Test, E2E Test] — NEITHER codecov check is present
  </read_first>
  <action>
    Success-criterion #4 is ALREADY MET by the Task 2 doc correction (docs now describe the
    enforced reality: codecov/patch posts, does not block). This task is the OPTIONAL step
    that makes the coverage gate REAL: a maintainer adds BOTH `codecov/patch` and
    `codecov/project` to the protect-main ruleset's required status checks (repo Settings →
    Rules → protect-main → Require status checks), so patch @80% blocks and the project
    ratchet blocks coverage-lowering PRs. This is a repo-settings/operator action — NOT an
    in-repo YAML edit. Whichever way this resolves, the SUMMARY MUST record the true state:
    if added → docs may be tightened to "required/blocking"; if deferred → the SUMMARY
    records the statuses as "posting, not blocking" (and Task 2's docs already say exactly
    that, so nothing is misreported).
  </action>
  <what-built>
    Task 1 set the `.codecov.yml` project status to the ratchet and Task 2 corrected the docs
    to the TRUE state (codecov/patch + codecov/project POST but are NOT required checks in the
    protect-main ruleset today — verified via gh api). Making either status BLOCK requires
    adding it to the protect-main ruleset's required status checks. This is a repo-settings/
    operator action, NOT an in-repo YAML change.
  </what-built>
  <how-to-verify>
    1. GitHub → repo Settings → Rules → the protect-main ruleset → "Require status checks to
       pass" → add BOTH `codecov/patch` and `codecov/project`.
    2. Confirm via API: `gh api repos/holomush/holomush/rulesets/11923801` now lists both
       codecov checks among required_status_checks.
    3. Optionally open a throwaway PR that lowers project coverage beyond the 1% threshold and
       confirm `codecov/project` reports FAILURE and `mergeStateStatus` shows BLOCKED
       (`gh pr view <n> --json mergeStateStatus,statusCheckRollup`).
    4. If deferred, the SUMMARY records the statuses as "posting, not blocking" (matches the
       Task 2 docs — criterion #4 is still met via the doc correction).
  </how-to-verify>
  <resume-signal>Type "added to ruleset" (with the gh api evidence) or "deferred" (recorded as posting-not-blocking in SUMMARY).</resume-signal>
  <verify>
    <automated>MISSING — repo-settings action; verified via `gh api repos/holomush/holomush/rulesets/11923801` showing codecov/patch + codecov/project among required checks (or the deferral recorded as "posting, not blocking" in SUMMARY)</automated>
  </verify>
  <done>codecov/patch + codecov/project are required checks in the protect-main ruleset (gh api evidence captured), OR the deferral is recorded as "posting, not blocking" in the SUMMARY (criterion #4 already met via the Task 2 doc correction either way).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coverage doc → contributor expectation | A doc claiming a bar that CI does not enforce misleads contributors and reviewers |
| codecov status → merge decision | The ratchet only guards coverage erosion if it is a required ruleset check; a posted-but-not-required status is false assurance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-4-01 | Repudiation (false assurance) | docs claim patch/project block when the ruleset does not require them | high | mitigate | Docs are corrected to the VERIFIED state (patch/project POST, not required — gh api ruleset 11923801); Task 3 (optional) adds both to the ruleset with gh-api evidence; if deferred, SUMMARY records "posting, not blocking" — never claim "codecov will block" when the ruleset is unchanged |
| T-06-4-02 | Tampering (ratchet bypass) | threshold too loose | medium | mitigate | 1% is a 1-point regression allowance (labeled honestly, not "no-drop") that absorbs merge jitter while still blocking larger regressions; documented as tightenable toward threshold: 0% |
| T-06-4-03 | Information (doc drift) | leftover per-package claim or false "hard gate" claim | medium | mitigate | Negative acceptance criteria assert the fictional per-package bar AND any "patch is a current hard gate" claim are absent from both files |
| T-06-4-04 | Denial | deleting the wrong codecov file breaks coverage reporting | low | mitigate | Delete only the 375B ignore-only `codecov.yml`; keep the full `.codecov.yml`; optional api.codecov.io/validate |
</threat_model>

<verification>
- Exactly one codecov config (`.codecov.yml`) with project `{target: auto, threshold: 1%}` and unchanged patch.
- Neither doc states a per-package coverage MUST.
- `task lint` green; manual ruleset step recorded (added or explicitly deferred).
</verification>

<success_criteria>
Coverage policy and CI enforcement agree — the doc MUST is corrected to the enforced reality and a project ratchet guards whole-repo coverage (ROADMAP Phase 6 success criterion #4 / QUAL-01).
</success_criteria>

## Artifacts this phase produces (this plan)
- `.codecov.yml` project status changed to `{target: auto, threshold: 1%}` (a 1-point regression allowance, NOT "no-drop").
- `codecov.yml` DELETED (dual-file conflict resolved).
- Rewritten coverage MUST rows in `.claude/rules/testing.md` and `CLAUDE.md` — TRUE enforced reality (patch/project POST, not required); CORRECTS CONTEXT D-07's false premise.
- MANUAL (out-of-repo, optional-but-recommended): BOTH `codecov/patch` and `codecov/project` added to the protect-main ruleset 11923801 required checks (gh-api-verified), or the deferral recorded as "posting, not blocking".

<output>
Create `.planning/phases/06-operational-hardening-assurance-gates/06-04-SUMMARY.md` when done (record whether the ruleset step was completed or deferred).
</output>
