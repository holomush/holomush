---
phase: 06-operational-hardening-assurance-gates
plan: 05
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - cmd/holomush/cmd_audit.go
  - cmd/holomush/cmd_audit_test.go
  - cmd/holomush/cmd_audit_dlq_replay_integration_test.go
  - internal/eventbus/audit/dlq_replay_integration_test.go
autonomous: true
requirements: [OPS-04]

must_haves:
  truths:
    - "runAuditDLQReplay resolves the effective game_id MIRRORING the server (core.go:300-303): --game-id override first, else the configured ROOT game_id (the field core.go reads as cfg.GameID, NOT event_bus.game_id, and WITHOUT event_bus.Defaults()), else the persisted holomush_system_info game_id"
    - "DLQ replay against an external-NATS deployment whose DLQ subject carries a ULID game prefix recovers messages (Replayed==1, correct events_audit.subject)"
    - "The replacement integration test lives in cmd/holomush and drives the REAL resolveGameID seam end-to-end (not a hand-built config), exercising DIVERGENT server/CLI game_id — BOTH the failure guard (wrong game → Failed>0, 0 rows) and recovery (right game → Replayed==1)"
    - "The replacement test drives a real NATS container via internal/testsupport/natstest, not embedded eventbustest"
  prohibitions:
    - "The resolver does NOT put DB before the configured root game_id (that would diverge from the server for deployments with an explicit root game_id)"
    - "The CLI does NOT apply event_bus.Config.Defaults() to the game_id (it forces 'main' and reintroduces the mismatch)"
    - "The replacement test is NOT a hand-built magic-string DLQConfig that bypasses the resolver"
  artifacts:
    - cmd/holomush/cmd_audit.go
    - cmd/holomush/cmd_audit_dlq_replay_integration_test.go
  key_links:
    - "resolveGameID output feeds dlqConfigForGame so the CLI DLQ subject prefix matches the server's persisted subject (closes the F3 empty-game_id → internal.main.audit.dlq mismatch)"
    - "the cmd/holomush package-main integration test can reach the unexported resolveGameID, so the actual resolver seam is exercised end-to-end"
---

<objective>
OPS-04 (D-05/D-06): fix the audit-DLQ replay CLI for external-NATS deployments
and replace its tautological coverage test with a genuine recovery assertion.

The F3 bug: the CLI reads `event_bus.game_id` from operator config; when that is
empty, `dlqConfigForGame("")` yields an empty-Subject config that `Defaults()`
fills to `internal.main.audit.dlq`. But the server persists its game_id (usually
a ULID) and publishes to `internal.<ULID>.audit.dlq`, so the CLI's prefix never
matches → every dead letter is counted Failed and nothing is recovered.

Fix (REDESIGNED per review): resolve the effective game_id MIRRORING the server's
own order at core.go:300-303 — `--game-id` override → configured ROOT game_id
(the field core.go reads as `cfg.GameID`, NOT `event_bus.game_id`, and WITHOUT
applying `event_bus.Config.Defaults()`, which fills empty GameID with "main" and
would reintroduce the mismatch) → persisted DB value. The prior plan had this
inverted (override → DB → config), which diverges from the server whenever an
explicit root game_id ≠ the DB value. Replace the tautological test (same `"main"`
on both seed and replay, embedded NATS) with a natstest-backed test IN cmd/holomush
that drives the REAL resolveGameID seam end-to-end and asserts both the failure
guard and recovery on a divergent-game path.

Depends on 06-01 so the rewritten integration test runs against the final
composite-PK / deterministic-timestamp writeAuditRow regime (the DLQ replay path
shares writeAuditRow; its dedup now keys on the composite (id, event_ms)).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-operational-hardening-assurance-gates/06-RESEARCH.md
@.planning/phases/06-operational-hardening-assurance-gates/06-PATTERNS.md
@.claude/rules/testing.md

# Grounding analogs
@cmd/holomush/cmd_audit.go
@internal/eventbus/audit/dlq_replay_integration_test.go
@internal/testsupport/natstest/nats.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: --game-id flag + resolveGameID (override → configured root game_id → DB, mirroring the server) wired into runAuditDLQReplay</name>
  <files>cmd/holomush/cmd_audit.go, cmd/holomush/cmd_audit_test.go</files>
  <read_first>
    - cmd/holomush/cmd_audit.go:107-119 (newAuditDLQReplayCmd — where the flag is registered), :143-149 (loadEventBusConfig — loads ONLY the event_bus section; cfg.GameID here is event_bus.game_id, NOT the root game_id), :298-331 (runAuditDLQReplay — openAuditPool at :319, the ReplayDLQ call at :325 passing dlqConfigForGame(cfg.GameID)), :333-343 (dlqConfigForGame — receives the resolved id unchanged)
    - cmd/holomush/core.go:300-303 (the server's order: `gameID := cfg.GameID; if gameID == "" { gameID = dbSub.GameID() }` — configured ROOT game_id FIRST, DB only when empty; MIRROR this) — and how core.go LOADS that root `game_id` config field (the resolver must read the SAME root field, not event_bus.game_id)
    - internal/store/postgres.go:70,97-99 (GetSystemInfo(ctx, "game_id") / InitGameID — the DB read path; ErrSystemInfoNotFound when absent — reuse, do NOT reimplement the key)
    - 06-PATTERNS.md "cmd/holomush/cmd_audit.go (MOD)" and 06-RESEARCH.md §OPS-04(a)
    - .claude/rules/logging.md (*Context slog), .claude/rules/grpc-errors.md (oops at boundaries)
  </read_first>
  <behavior>
    - resolveGameID mirrors the server (core.go:300-303): (1) non-empty `--game-id` override wins; (2) else the configured ROOT game_id — the SAME value core.go resolves as cfg.GameID (the core/root config `game_id`, NOT event_bus.game_id, and WITHOUT applying event_bus.Config.Defaults() which would force "main"); (3) else the persisted holomush_system_info game_id.
    - When override empty AND root config game_id set → returns the root config value (even if it differs from the DB — matching the server, which prefers configured root).
    - When override empty AND root config empty AND the DB has a persisted game_id → returns the DB value (the zero-operator-burden auto-resolve of D-05).
    - When all three empty → returns empty (documented: the caller then defaults to internal.main.audit.dlq, the legacy single-game shape) — the resolver invents nothing.
  </behavior>
  <action>
    Register `cmd.Flags().String("game-id", "", "Override the game_id whose DLQ subject to
    replay (default: the server's configured/persisted game_id)")` in newAuditDLQReplayCmd.
    Extract a resolver that MIRRORS the server order at core.go:300-303:
    `resolveGameID(ctx, sysInfoLookup, override, rootCfgGameID string) (string, error)` —
    take the system-info read as an INJECTED seam (function value or narrow interface) so it
    is unit-testable without a live pool. Precedence: (1) non-empty `override`; (2) else
    non-empty `rootCfgGameID` (the ROOT/core config `game_id` — the SAME field core.go reads
    as cfg.GameID; load it from the ROOT config section the way core.go does, NOT from
    event_bus, and do NOT call event_bus.Config.Defaults(), which fills empty GameID with
    "main" and would reintroduce the mismatch); (3) else the persisted value via
    GetSystemInfo(ctx, "game_id") (ErrSystemInfoNotFound = no DB value).
    In runAuditDLQReplay, after openAuditPool, read the `--game-id` flag and the ROOT config
    game_id, call resolveGameID, and pass the RESOLVED id to dlqConfigForGame (replacing the
    current `dlqConfigForGame(cfg.GameID)`, which used the empty event_bus.game_id).
    `dlqConfigForGame` is unchanged. Structured `oops` at the DB-read boundary; `*Context`
    slog variants.
    Write unit tests for resolveGameID covering: override wins; root-config beats DB;
    DB used when override+root empty; all-empty → empty.
  </action>
  <verify>
    <automated>task test -- ./cmd/holomush/... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `--game-id` flag exists on the DLQ replay command; `runAuditDLQReplay` passes the RESOLVED id (not raw event_bus `cfg.GameID`) to `dlqConfigForGame`.
    - `resolveGameID` mirrors the server order and its unit tests pass all four precedence cases (override wins / root-config beats DB / DB used when override+root empty / all-empty → empty).
    - The resolver reads the ROOT config game_id (not event_bus.game_id) and never applies event_bus.Config.Defaults() (grep confirms no Defaults() call on the game_id path).
    - New lines clear codecov patch @ 80% (budget error-branch tests — the DB-read error branch counts).
    - `task build` green.
  </acceptance_criteria>
  <done>The CLI resolves the effective game_id (override → configured root game_id → persisted DB) mirroring the server, so its DLQ subject prefix matches the server's, closing the F3 mismatch.</done>
</task>

<task type="auto">
  <name>Task 2: divergent-game natstest test IN cmd/holomush — exercise the REAL resolver seam (replace the tautology)</name>
  <files>cmd/holomush/cmd_audit_dlq_replay_integration_test.go, internal/eventbus/audit/dlq_replay_integration_test.go</files>
  <read_first>
    - internal/eventbus/audit/dlq_replay_integration_test.go (the CURRENT tautology to REMOVE: `replayDLQSubject = "internal.main.audit.dlq"` at :27 used as BOTH seed and replay prefix, embedded eventbustest at :102) — a package in internal/eventbus/audit CANNOT reach the unexported cmd/holomush resolver, so a hand-built same-game config only re-proves ReplayDLQ; per D-06 replace, don't augment
    - cmd/holomush/cmd_audit.go resolveGameID / dlqConfigForGame / runAuditDLQReplay (from Task 1 — same package `main`, so an integration test HERE reaches the unexported resolver; that is the point) and the memory note "cmd/holomush/*_test.go is package main"
    - internal/testsupport/natstest/nats.go:38-77 (StartNATS(ctx) (*NATSEnv, error), env.Conn(t) *nats.Conn, env.Terminate(ctx)) and test/integration/resilience/chaos_helpers_test.go (startExternalNATS reference usage)
    - internal/store/postgres.go:70,97-99 (seed holomush_system_info game_id so the resolver's DB leg is real, not a magic string)
    - internal/eventbus/audit/replay.go:75,198-217,219 (ReplayDLQ, originalSubject prefix check that counts Failed, writeAuditRow — now composite-PK after 06-01)
    - 06-PATTERNS.md "dlq_replay_integration_test.go (REWRITE)" (Adaptation D-06) and 06-RESEARCH.md §OPS-04(c)
    - .claude/rules/testing.md (external-NATS tier MUST use natstest; production code MUST NOT import natstest; `//go:build integration`)
  </read_first>
  <action>
    REPLACE the tautological test with a `//go:build integration` test in package `main`
    under cmd/holomush, so it can drive the REAL unexported resolver seam
    (resolveGameID → dlqConfigForGame → audit.ReplayDLQ) END-TO-END — NOT a hand-built
    magic-string DLQConfig (which only re-proves ReplayDLQ, the behavior already known).
    Stand up a REAL single-node NATS container via `natstest.StartNATS(ctx)` + `env.Conn(t)`
    (Terminate on cleanup) and a Postgres pool (the package's existing integration-DB helper)
    with the composite-PK events_audit from 06-01. Seed `holomush_system_info` with a
    SERVER-STYLE ULID game_id and seed the DLQ under `internal.<that-ULID>.audit.dlq.<orig-subject>`.
    Assert BOTH:
    (i) FAILURE GUARD: resolve with a WRONG override (or a root-config game that diverges
        from the seeded DB ULID) → the resolved subject prefix mismatches → `res.Failed > 0`
        and ZERO events_audit rows.
    (ii) RECOVERY: resolve with override empty + root-config empty so resolveGameID reads the
        seeded DB ULID (or pass `--game-id` = the seeded ULID) → `res.Replayed == 1` and the
        events_audit row's `subject` == the original subject.
    Drive the ACTUAL resolveGameID (the seeded system_info feeds its DB leg); do NOT bypass it.
    Be explicit about the subject prefix under test — `ReplayDLQ` applies `cfg.Defaults()`
    internally, so an empty Subject silently becomes `internal.main.audit.dlq`.
    REMOVE the old internal/eventbus/audit/dlq_replay_integration_test.go F3 test (its same-game
    embedded-NATS shape is the tautology D-06 forbids). If a plain same-game happy-path restore
    assertion is still wanted, it MAY remain ONLY if renamed so it does not masquerade as
    F3/external-NATS coverage — but the F3-covering test is the new cmd/holomush one.
  </action>
  <verify>
    <automated>task test:int -- ./cmd/holomush/... 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The new test lives in cmd/holomush (package main, `//go:build integration`) and calls the REAL resolveGameID + dlqConfigForGame + audit.ReplayDLQ — the resolver seam is exercised END-TO-END (this is an ACCEPTANCE criterion, not a preference).
    - It uses `natstest.StartNATS` (real NATS container), NOT embedded eventbustest; no production import of natstest.
    - Seed prefix (ULID game from a seeded holomush_system_info row) and the wrong-override prefix DIVERGE — not `"main"` on both sides.
    - Failure guard: divergent game → `res.Failed > 0`, 0 events_audit rows. Recovery: resolved-from-DB (or overridden) correct game → `res.Replayed == 1`, events_audit.subject == original.
    - The tautological internal/eventbus/audit/dlq_replay_integration_test.go F3 test is removed (or renamed to a non-F3 happy-path test).
    - `task test:int` green on the composite-PK events_audit (recovery yields exactly one row).
  </acceptance_criteria>
  <done>A genuine, non-tautological cmd/holomush natstest test drives the real game_id resolver end-to-end, proving DLQ recovery for an external-NATS deployment and guarding the mismatch path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI (--game-id / config) → DLQ subject prefix | The resolved game_id decides WHICH game's DLQ subject is replayed into events_audit |
| DLQ replay → events_audit (per-game audit records) | Replaying under a wrong game_id could write another game's dead letters into the wrong game's audit trail (cross-tenant contamination) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-5-01 | Tampering/Information (cross-tenant) | resolveGameID / dlqConfigForGame | high | mitigate | Resolution mirrors the server's EXACT order (--game-id → configured ROOT game_id → DB; NOT DB-before-root; no event_bus.Defaults() forcing "main") so CLI and server agree on the subject; the `originalSubject` prefix check counts mismatches as Failed (fail-loud, retains the message) rather than mis-writing; test asserts the failure guard |
| T-06-5-02 | Repudiation (false recovery) | tautological / resolver-bypassing test masking the bug | high | mitigate | Replace with a cmd/holomush test that drives the REAL resolveGameID seam end-to-end (not a hand-built config) on a divergent-game path, asserting BOTH failure-guard and recovery on a real broker |
| T-06-5-03 | Denial | empty resolution silently defaults to internal.main.audit.dlq | medium | mitigate | resolveGameID does not invent a value; the empty→legacy-default path is documented; the --game-id escape hatch lets an operator force the correct subject |
| T-06-5-04 | Tampering (test infidelity) | embedded NATS instead of real broker | medium | mitigate | natstest real container per the testing rule; production code must not import natstest (depguard) |
</threat_model>

<verification>
- `task test` green: resolveGameID precedence unit tests.
- `task test:int` green: divergent-game natstest test (failure guard + recovery) on the composite-PK events_audit.
</verification>

<success_criteria>
The audit-DLQ replay CLI recovers messages for an external-NATS deployment (game_id bridge fixed), proven by a non-tautological test (ROADMAP Phase 6 success criterion #3 / OPS-04).
</success_criteria>

## Artifacts this phase produces (this plan)
- `--game-id` flag + `resolveGameID(...)` in `cmd/holomush/cmd_audit.go` (game_id resolution MIRRORS the server: override → configured ROOT game_id → persisted DB; no event_bus.Defaults()).
- `cmd/holomush/cmd_audit_test.go` (NEW or extended) — resolveGameID precedence unit tests (four cases).
- `cmd/holomush/cmd_audit_dlq_replay_integration_test.go` (NEW, package main, //go:build integration) — divergent-game, natstest-backed, drives the REAL resolver seam end-to-end (failure-guard + recovery).
- REMOVED tautological F3 test in `internal/eventbus/audit/dlq_replay_integration_test.go` (replaced per D-06).

<output>
Create `.planning/phases/06-operational-hardening-assurance-gates/06-05-SUMMARY.md` when done.
</output>
