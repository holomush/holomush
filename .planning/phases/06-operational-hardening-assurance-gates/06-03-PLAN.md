---
phase: 06-operational-hardening-assurance-gates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - Taskfile.yaml
  - osv-scanner.toml
  - .github/workflows/ci.yaml
autonomous: false
requirements: [OPS-03]
user_setup:
  - service: github-repo-settings
    why: "Making the new vuln: job actually block merges requires adding it to the protect-main ruleset required checks (operator action, not in-repo YAML)"
    dashboard_config:
      - task: "Add the `Vuln` check (the vuln: job's RENDERED name, not the workflow key `vuln`) to the protect-main ruleset's required status checks"
        location: "GitHub → repo Settings → Rules → protect-main ruleset → Require status checks to pass"

must_haves:
  truths:
    - "The checksum-verified osv-scanner (Task 1-pinned v2.Y.Z + SHA-256) and pinned govulncheck are installed LOCALLY before the pre-bump proof (round-4 MEDIUM — the env lacks osv-scanner), so `task lint:vuln` is executable in the current worktree, not deterministically blocked; the CI install (Task 3) stays separately checksum-pinned"
    - "CORRECTED (see DEVIATION NOTE below; assumption A4 empirically false): a tree pinning nats-server/v2 @ v2.14.2 fails `task lint:vuln` via the DETERMINISTIC nats version-floor guard (cmd/nats-floor-guard), NOT via a scanner citation — GHSA-q59r-vq66-pxc2 / CVE-2026-58207 is a git-commit-range-only OSV record with no Go-ecosystem package binding, so neither osv-scanner (manifest scan) nor govulncheck (Go vuln DB) can flag it. PROVEN by capturing the exit code: the floor-guard unit test fails on < v2.14.3, and `go run ./cmd/nats-floor-guard` on the pre-bump go.mod exits non-zero naming the floor v2.14.3"
    - "CORRECTED (see DEVIATION NOTE below): after the bump to v2.14.3, `task lint:vuln` exits 0 because (a) the floor guard passes (nats >= v2.14.3), (b) govulncheck reports 0, and (c) the 5 test-only, non-production-reachable github.com/docker/docker OSV findings (GO-2026-4883/4887/5617/5668/5746, no upstream fix) are allowlisted in osv-scanner.toml with an ignoreUntil expiry, tracked in holomush/holomush#4817. The docker findings are pre-existing (unrelated to nats) and would otherwise keep the osv-scanner leg red regardless of the bump"
    - "go.mod pins github.com/nats-io/nats-server/v2 at >= v2.14.3"
    - "A dedicated ci.yaml `vuln:` job with an explicit stable `name: Vuln` runs `task lint:vuln` on every PR (GitHub rulesets match the RENDERED job name, not the workflow key — ci.yaml's `lint:` key renders as `Lint`, which is what the live ruleset requires)"
    - "The `Vuln` check (the job's RENDERED name, NOT the workflow key `vuln`) is a REQUIRED check in the protect-main ruleset (D-04 blocking gate), added via a mandatory blocking human-action checkpoint (Task 4), verified with `gh api repos/holomush/holomush/rulesets/11923801` AND in a real PR's `statusCheckRollup` (the rendered context) — NOT left as an open SUMMARY follow-up"
  artifacts:
    - Taskfile.yaml
    - osv-scanner.toml
    - .github/workflows/ci.yaml
  key_links:
    - "CORRECTED (assumption A4 false — see DEVIATION NOTE): task lint:vuln runs THREE fail-closed legs — (0) the deterministic nats version-floor guard (cmd/nats-floor-guard), (1) govulncheck (reachability, Go vuln DB), and (2) OSV-Scanner v2 `scan source` (OSV DB). The floor guard — NOT the OSV-Scanner leg — is what makes criterion #2 provable: GHSA-q59r-vq66-pxc2 is absent from BOTH the Go vuln DB AND, as a git-range-only OSV record, from any manifest scan, so neither scanner can fail on it. The scanner legs provide forward coverage for future Go-ecosystem-bound vulns"
    - "the osv-scanner.toml [[IgnoredVulns]] allowlist suppresses OSV-Scanner findings ONLY (id+reason+ignoreUntil); govulncheck has no suppression, so any reachable Go vuln is a hard stop"
    - "the vuln: job becomes a BLOCKING gate via the mandatory Task 4 checkpoint (add the rendered `Vuln` check to the protect-main ruleset required checks); the consolidated protect-main assurance-gate checklist (the mandatory `Vuln` check + the optional/accepted-deferred codecov/patch + codecov/project ratchet) is enumerated in 06-04 Task 3"
---

> **DEVIATION NOTE (2026-07-15) — assumption A4 empirically FALSE; re-scoped per user decision (Option A).**
> The plan (and its round-1..4 review incorporation) assumed OSV-Scanner would flag
> nats-server v2.14.2 via a `go.mod` manifest scan and cite `GHSA-q59r-vq66-pxc2`
> (criterion #2). That is FALSE and was independently confirmed:
> - OSV `CVE-2026-58207` (alias `GHSA-q59r-vq66-pxc2`) has `package: None`, ranges `['GIT']`
>   — a git-commit-range-only record with NO Go-ecosystem package binding.
> - OSV package query {ecosystem Go, `github.com/nats-io/nats-server/v2` @ 2.14.2} → 0 vulns;
>   `govulncheck ./...` → exit 0. NO manifest/reachability scanner can flag nats 2.14.2.
> - `osv-scanner scan source -L go.mod` surfaces ONLY 5 `github.com/docker/docker` findings
>   (GO-2026-4883/4887/5617/5668/5746, all "FIXED VERSION --", 0 fixable), which are an
>   INDIRECT, TEST-ONLY dep (golang-migrate → dktest → docker/docker), not production-reachable.
>
> **Re-scope actually shipped (user chose Option A — keep the gate + add a deterministic guard):**
> 1. Bumped nats-server/v2 → **v2.14.3** (the real CVE remediation; closes F8/#4790).
> 2. Kept the `lint:vuln` gate: govulncheck@v1.6.0 + osv-scanner@v2.4.0 (forward coverage).
> 3. Added **cmd/nats-floor-guard** — a deterministic version-floor guard (TDD-tested) that
>    hard-fails `lint:vuln` if nats-server/v2 < v2.14.3. THIS is criterion #2's provable
>    mechanism (compensating control for the OSV blind spot), not a scanner citation.
> 4. Allowlisted the 5 test-only docker/docker findings in `osv-scanner.toml`
>    (id + reason + `ignoreUntil` expiry), tracked in **holomush/holomush#4817**, so
>    `task lint:vuln` exits 0 on the clean post-bump tree.
> 5. Corrected criteria #2/#3 above to this mechanism.
>
> `gsd-verifier`: check against the CORRECTED criteria #2/#3, not the original scanner-citation
> wording. Task 4 (make the rendered `Vuln` check a REQUIRED protect-main ruleset check) remains
> a PENDING OPERATOR STEP for ship — it verifies against a live PR's statusCheckRollup, which does
> not exist during execute-phase. Full detail in 06-03-SUMMARY.md.

<objective>
OPS-03 (D-03/D-04): remediate the nats-server CVE and stand up a blocking
vuln-scan CI gate that would have caught it.

MECHANISM CORRECTION (flag explicitly — this is NOT a re-scope): D-04 named
govulncheck, but the researcher empirically confirmed the SPECIFIC advisory
GHSA-q59r-vq66-pxc2 is ABSENT from the Go vulnerability database (narrow claim
per review — other nats-server advisories DO exist in the Go DB; THIS one does
not), so `govulncheck ./...` passes on the vulnerable build. govulncheck alone
therefore CANNOT satisfy success-criterion #2 ("a build pinning a known-vulnerable
nats-server fails the gate"). The gate uses govulncheck (callgraph-aware
reachability, Go vuln DB) PLUS OSV-Scanner v2 (OSV DB, which ingests the GHSA nats
advisories) — OSV-Scanner is the leg that makes a pinned-v2.14.2 build fail and
provides the native `[[IgnoredVulns]]` allowlist.

EXCEPTION-POLICY SCOPING (review blocker — the allowlist covers only OSV-Scanner):
govulncheck has NO finding-suppression mechanism (upstream golang/go#59507), so an
accepted REACHABLE Go vuln cannot be allowlisted — it is a HARD STOP that must be
remediated (bumped/patched), never suppressed. The osv-scanner.toml `[[IgnoredVulns]]`
allowlist (with `id` + `reason` + `ignoreUntil` EXPIRY) applies ONLY to the OSV-Scanner
leg. This asymmetry is intentional fail-closed and is documented in the task and SUMMARY.

Purpose: close F8 (#4790) and give later phases a supply-chain gate.
Output: the nats bump, `task lint:vuln`, `osv-scanner.toml`, a `vuln:` CI job, and a
BLOCKING ruleset checkpoint.

ROUND-2 REVIEW INCORPORATION (06-REVIEWS.md consensus):
- **Blocking gate is mandatory, not an open follow-up (finding 6):** D-04 requires the
  vuln gate to BLOCK. Task 4 is a mandatory blocking human-action checkpoint that adds
  `vuln` to the protect-main ruleset required checks, gh-api-verified. The single
  consolidated protect-main assurance-gate checklist (vuln + codecov/patch +
  codecov/project) is enumerated in 06-04 Task 3; 06-03 independently blocks on `vuln`.
- **Pre-bump verification cannot be fail-open (finding 8):** the Task 2 verify captures
  the real exit code into a variable and asserts nonzero, then SEPARATELY asserts the
  GHSA advisory line — a bare `task lint:vuln; echo "exit=$?"` returns the echo's status
  and would pass fail-open (repo rule: judge by EXIT CODE, not output).

ROUND-3 REVIEW INCORPORATION (06-REVIEWS.md consensus, verified against live source):
- **Required-check context is the rendered job NAME, not the key (round-3 HIGH #4):** GitHub
  rulesets require the RENDERED job `name`, not the workflow key. ci.yaml's `lint:` key renders
  as `Lint` (ci.yaml:37), which is exactly what the live ruleset 11923801 requires. So the new
  job MUST set an explicit stable `name: Vuln` and the ruleset MUST require the `Vuln` context;
  Task 4 verifies it in a real PR's `statusCheckRollup`, not only ruleset JSON.
- **OSV empty-config format (round-3 MEDIUM/LOW):** an empty `[[IgnoredVulns]]` creates an
  array-of-tables ENTRY with no `id` (invalid). Seed a truly empty config or `IgnoredVulns = []`,
  and VALIDATE it by running osv-scanner against it (the pre-bump run consumes `--config` and
  must still report the GHSA — a malformed config would error there).

ROUND-4 REVIEW INCORPORATION (06-REVIEWS.md Consensus Summary):
- **OSV-Scanner not installed before the local pre-bump proof (round-4 MEDIUM):** the environment has
  govulncheck but NO `osv-scanner`; Task 1 does no install and Task 2 immediately runs it, while the
  CI install is Task 3 — so the local pre-bump proof (Task 2) is DETERMINISTICALLY blocked. Task 2 now
  FIRST installs the checksum-verified `osv-scanner` binary (the exact version + SHA-256 pinned in
  Task 1) into a temporary/local tool dir (and pins govulncheck the same way) BEFORE running the
  pre-bump proof. The CI install (Task 3) stays separately checksum-pinned, as planned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-operational-hardening-assurance-gates/06-RESEARCH.md
@.planning/phases/06-operational-hardening-assurance-gates/06-PATTERNS.md

# Grounding analogs
@Taskfile.yaml
@.github/workflows/ci.yaml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: osv-scanner package-legitimacy verification + version pin (blocking, not auto-approvable)</name>
  <files>osv-scanner (external tool — no in-repo file); pinned version/checksum recorded in SUMMARY</files>
  <read_first>
    - 06-RESEARCH.md "Package Legitimacy Audit" (osv-scanner = OK-verify, Google-maintained, [ASSUMED] until pinned) + "Environment Availability" (osv-scanner missing → install/pin)
    - https://github.com/google/osv-scanner (project provenance) and the release assets page (for the version + SHA-256 to pin)
  </read_first>
  <action>
    HALT for a human to verify the legitimacy of `github.com/google/osv-scanner`
    and pin a specific released version + SHA-256 BEFORE it is installed in CI
    (Task 3). This is the phase supply-chain checkpoint (T-06-3-SC); it is NEVER
    auto-approvable regardless of the auto_advance setting. Also confirm the pinned
    govulncheck version. The executor performs no install and writes no file in this
    task — it collects and records the pinned coordinates for Tasks 2/3.
  </action>
  <what-built>
    Research classified `github.com/google/osv-scanner` as OK (verify) — a
    Google-maintained OSS tool, but its exact release version + checksum are
    [ASSUMED] and not yet pinned (06-RESEARCH.md Package Legitimacy Audit). Per
    the package-legitimacy gate, a human MUST verify legitimacy and pin a specific
    released version + checksum before it is installed in CI. This checkpoint is
    the phase threat-model entry T-06-3-SC (supply-chain); it is never
    auto-approvable regardless of auto_advance.
  </what-built>
  <how-to-verify>
    1. Confirm the project is genuine: visit https://github.com/google/osv-scanner
       (Google-owned org, active maintenance, releases present) and the pkg page.
    2. Pin OSV-Scanner v2 (the current MAJOR — the gate's CLI in Task 2 uses the v2
       `scan source` subcommand form). Choose a specific released v2.Y.Z and record its
       release-asset SHA-256 for the linux/amd64 binary, OR decide to
       `go install github.com/google/osv-scanner/v2/cmd/osv-scanner@v2.Y.Z` pinned by
       module version + go.sum. Record the exact v2.Y.Z so Task 2's CLI syntax matches.
    3. Confirm govulncheck's pin: `golang.org/x/vuln/cmd/govulncheck` (official Go
       team tool) at a pinned version.
    4. Record the pinned versions + checksums in the SUMMARY for Task 2/3 to consume.
  </how-to-verify>
  <resume-signal>Type "approved" with the pinned osv-scanner version + SHA-256 (and govulncheck version), or describe concerns.</resume-signal>
  <verify>
    <automated>MISSING — blocking human checkpoint; verified manually via the osv-scanner GitHub/pkg pages, pinned coordinates recorded in SUMMARY before Task 3 installs</automated>
  </verify>
  <done>osv-scanner (and govulncheck) legitimacy confirmed and a specific version + SHA-256 recorded in the SUMMARY for Tasks 2/3 to consume.</done>
</task>

<task type="auto">
  <name>Task 2: build `task lint:vuln` (govulncheck + osv-scanner) + osv-scanner.toml; prove pre-bump fail</name>
  <files>Taskfile.yaml, osv-scanner.toml</files>
  <read_first>
    - Taskfile.yaml:709-744 (`lint:no-timestamptz` — the self-contained `set -euo pipefail` lint-task pattern to mirror) and the `lint:` umbrella `cmds` around :102-120 (do NOT add lint:vuln here — the scanners download DBs and would slow every local `task lint`)
    - 06-PATTERNS.md "Taskfile.yaml lint:vuln (NEW task)" + "osv-scanner.toml (NEW)" and 06-RESEARCH.md §OPS-03(a) wire-order + §(c) landmines (govulncheck DB gap; DB-fetch flakiness)
    - .github/workflows/ci.yaml:66-75 (the checksum-pinned inline `buf` install pattern — `curl -LsSfo`, `sha256sum -c -`, `install -m 0755`; MIRROR it for the LOCAL osv-scanner install here, not only in CI Task 3)
    - The Task 1 SUMMARY-recorded pinned coordinates (osv-scanner v2.Y.Z + SHA-256, govulncheck version) — Task 2 consumes them for the local install
    - .claude/rules/search-tools.md "Searching files ≠ judging whether a command succeeded" (decide pass/fail by EXIT CODE, never by grepping the scanner's stdout for a string)
  </read_first>
  <action>
    LOCAL TOOL INSTALL FIRST (round-4 MEDIUM — the env has govulncheck but NOT osv-scanner, so
    without this the pre-bump proof below is deterministically blocked): using the exact
    osv-scanner v2.Y.Z + SHA-256 pinned in Task 1, install the checksum-verified binary to ONE
    DETERMINISTIC, PERSISTENT tool path (round-5 LOW — the pre-bump proof here in Task 2 and the
    post-bump proof in Task 3 run as SEPARATE task executions / shell sessions, so a per-session
    `/tmp` dir on `$PATH` does NOT survive between them). Prefer `go install
    github.com/google/osv-scanner/v2/cmd/osv-scanner@v2.Y.Z` (pinned by module version + go.sum),
    which lands the binary in the stable `$(go env GOPATH)/bin` on `$PATH` for BOTH runs; if you
    instead fetch the release binary, `install -m 0755` it to a FIXED absolute path (e.g. a
    repo-local `bin/`) and reference that exact path from `lint:vuln` — do NOT rely on a temp dir
    that a later shell won't have. Mirror the ci.yaml buf checksum-verify pattern for the release
    -binary route (`curl -LsSfo <path> "$URL"; echo "$SHA256  <path>" | sha256sum -c -`). Also
    install the pinned `govulncheck` the same deterministic way (`go install
    golang.org/x/vuln/cmd/govulncheck@<pinned>`). Do NOT use a floating `@latest`. Record the ONE
    resolved tool path in the SUMMARY so Task 3's post-bump run invokes the SAME binary. (Optionally,
    make `task lint:vuln` bootstrap the repo-pinned tools so local and CI share one install path;
    if so, keep the CI install in Task 3 checksum-pinned as a belt-and-braces.) The CI install
    (Task 3) remains separately checksum-pinned regardless.
    Add a `lint:vuln` task to Taskfile.yaml (a self-contained `set -euo pipefail`
    block, modeled on `lint:no-timestamptz`) that runs, in order: (1)
    `govulncheck ./...` (fast reachability over the Go vuln DB), then (2) OSV-Scanner v2:
    `osv-scanner scan source -L go.mod --config=osv-scanner.toml` (the v2 `scan source`
    subcommand form — NOT the v1 `osv-scanner --config=... <target>` form, which is
    version-ambiguous; Task 1 pinned OSV-Scanner v2, so the CLI MUST be the v2 shape).
    The task exits non-zero if EITHER tool reports an unlisted finding — rely on the tools'
    EXIT CODES, do NOT grep their stdout for a success/failure string. Keep `lint:vuln` OUT
    of the `lint:` umbrella `cmds` (invokable standalone).
    EXCEPTION POLICY (the allowlist covers only OSV-Scanner):
    - OSV-Scanner: create `osv-scanner.toml` at the repo root with a SYNTACTICALLY-VALID EMPTY
      allowlist (round-3 finding — an empty `[[IgnoredVulns]]` array-of-tables HEADER creates a
      table entry with no `id`, which is invalid). Use a truly empty file with only a documented
      `IgnoredVulns = []` line (an empty inline array) rather than a bare `[[IgnoredVulns]]`
      header. Each FUTURE entry MUST carry `id`, `reason`, AND an `ignoreUntil` EXPIRY (a
      suppression cannot live forever); document that in a comment. Seed it empty; the nats CVE
      is NOT allowlisted. (GO-2026-5932 x/crypto openpgp is unreachable and already exits 0 under
      govulncheck — no entry needed.) The pre-bump `task lint:vuln` run (which passes
      `--config=osv-scanner.toml`) VALIDATES the config: a malformed config would error there, so
      a successful pre-bump run that still cites the GHSA proves the config is valid.
    - govulncheck: has NO finding-suppression (upstream golang/go#59507). Therefore ANY
      reachable govulncheck finding is a HARD STOP — the only resolution is to remediate
      (bump/patch), never suppress. Put a comment atop `lint:vuln` and a line in the SUMMARY
      stating this asymmetry so no one expects osv-scanner.toml to silence govulncheck. This
      is intentional fail-closed.
    Prove the gate BEFORE the bump: with go.mod still at nats-server v2.14.2, run
    `task lint:vuln` and confirm it exits non-zero via OSV-Scanner citing GHSA-q59r-vq66-pxc2.
    Record the exact OSV-Scanner output line in the SUMMARY (the criterion-#2 evidence; if
    OSV-Scanner does NOT resolve that GHSA for nats-server — assumption A4 — STOP and raise a
    blocker rather than shipping a gate that does not fail on the known vuln).
    VERIFY BY EXIT CODE, NOT BY `echo` (review finding 8 + .claude/rules/search-tools.md): a
    bare `task lint:vuln; echo "exit=$?"` returns the `echo` exit status (always 0), masking
    the real result — capture `$?` into a variable, assert it is nonzero, and SEPARATELY
    assert the advisory evidence in captured output. Use the two-part verify snippet below.
  </action>
  <verify>
    <automated># offload-exempt: acceptance gate — raw scanner output IS the verify signal (must grep the advisory line + capture exit code)
out=$(task lint:vuln 2>&1); rc=$?; printf '%s\n' "$out" | tail -20; test "$rc" -ne 0 || { echo "FAIL: gate passed pre-bump (rc=$rc) — expected non-zero"; exit 1; }; printf '%s' "$out" | grep -q 'GHSA-q59r-vq66-pxc2' || { echo "FAIL: advisory GHSA-q59r-vq66-pxc2 not cited"; exit 1; }; echo "PASS: gate failed pre-bump citing GHSA-q59r-vq66-pxc2 (rc=$rc)"
  </verify>
  <acceptance_criteria>
    - `osv-scanner` (the Task 1-pinned v2.Y.Z + SHA-256) and the pinned `govulncheck` are installed locally BEFORE the pre-bump proof runs (checksum-verified, no `@latest`) to ONE deterministic, persistent tool path (`$(go env GOPATH)/bin` via `go install`, or a fixed absolute path referenced by `lint:vuln` — round-5 LOW; NOT a per-session `/tmp` dir) recorded in the SUMMARY so Task 3's post-bump run resolves the SAME binary — so `task lint:vuln` is not deterministically blocked by a missing binary (round-4 MEDIUM); `osv-scanner --version` resolves to the pinned version.
    - `task lint:vuln` exists, runs `govulncheck ./...` then OSV-Scanner v2 (`osv-scanner scan source -L go.mod --config=osv-scanner.toml`), and is NOT in the `lint:` umbrella.
    - With nats-server @ v2.14.2, `task lint:vuln` exits NON-ZERO and OSV-Scanner names GHSA-q59r-vq66-pxc2 (evidence line captured in SUMMARY) — success-criterion #2, failure side.
    - `osv-scanner.toml` exists with a SYNTACTICALLY-VALID empty allowlist (`IgnoredVulns = []`, NOT a bare `[[IgnoredVulns]]` header) documented to require `id` + `reason` + `ignoreUntil` on any future entry; the nats CVE is NOT allowlisted; the config is proven valid by the pre-bump osv-scanner run consuming it without a config error.
    - A comment atop `lint:vuln` (and a SUMMARY line) states the exception policy: the allowlist applies to OSV-Scanner only; govulncheck has no suppression, so any reachable Go vuln is a hard stop.
    - Pass/fail is decided by tool exit code, not stdout string-matching.
  </acceptance_criteria>
  <done>A two-tool blocking vuln gate exists and demonstrably FAILS on the known-vulnerable nats-server before the bump.</done>
</task>

<task type="auto">
  <name>Task 3: bump nats-server → v2.14.3 (gate turns green) + vuln: CI job + ruleset flag</name>
  <files>go.mod, go.sum, .github/workflows/ci.yaml</files>
  <read_first>
    - go.mod:22-24 (nats-server/v2 v2.14.2 → bump; nats.go v1.52.0 + prometheus-nats-exporter v0.20.1 are NOT implicated — the two CVEs are server-only; do NOT bump them for these advisories)
    - .github/workflows/ci.yaml:36-97 (the `lint:` job scaffold: checkout + setup-go + cache + install-task) and :66-75 (the checksum-pinned inline `buf` install pattern to mirror for osv-scanner)
    - 06-PATTERNS.md "ci.yaml vuln: job (NEW)" + 06-RESEARCH.md §OPS-03(a) install/pin paragraph + §(c) (pin tool + guard DB-fetch flakiness)
    - 06-RESEARCH.md §OPS-03 "The exact CVEs" (GHSA-q59r-vq66-pxc2 fixed 2.14.3)
  </read_first>
  <action>
    Bump `github.com/nats-io/nats-server/v2` to v2.14.3 (>= the fixed version) and
    run `go mod tidy` to update go.sum. Then confirm `task lint:vuln` exits 0 (the
    osv-scanner finding clears). This post-bump run MUST invoke the SAME deterministic
    tool binary Task 2 installed (`$(go env GOPATH)/bin` or the fixed path recorded in the
    SUMMARY — round-5 LOW); if this task runs in a fresh shell, re-install to that same
    deterministic path (do NOT rely on a Task-2 `/tmp` dir persisting). Leave nats.go and
    prometheus-nats-exporter at their current versions (not implicated).
    Add a dedicated `vuln:` job to `.github/workflows/ci.yaml` (a sibling of
    lint/test/integration/e2e/build) with an EXPLICIT stable `name: Vuln` (round-3 finding 4 —
    GitHub rulesets require the RENDERED job name, not the workflow key; ci.yaml's `lint:` key
    renders `name: Lint` at :37, which is exactly what the live ruleset requires, so the required
    check for THIS job is the rendered `Vuln`, not `vuln`). Copy the lint-job scaffold (checkout,
    setup-go, cache, install-task), add a govulncheck install pinned via
    `go install golang.org/x/vuln/cmd/govulncheck@<pinned>` and an OSV-Scanner v2 install
    pinned + SHA-256-verified (mirror the inline buf checksum-verify pattern) — either the
    v2.Y.Z release binary or `go install github.com/google/osv-scanner/v2/cmd/osv-scanner@v2.Y.Z`
    — using the exact v2.Y.Z + checksum approved in Task 1. The job's run step is
    `task lint:vuln`. Do NOT rely on a floating `@latest`.
    Making the `vuln:` job actually BLOCK merges requires an operator to add the rendered
    `Vuln` check to the protect-main ruleset required checks — that is now the MANDATORY blocking
    checkpoint Task 4 (review finding 6: NOT an open SUMMARY follow-up). Adding the CI job
    alone makes it RUN and REPORT, not block; Task 4 makes it block and verifies via gh api AND
    a real PR's statusCheckRollup.
  </action>
  <verify>
    <automated># offload-exempt: acceptance gate — exit code of the pinned two-tool scan IS the verify signal
task lint:vuln; rc=$?; test "$rc" -eq 0 || { echo "FAIL: gate still fails post-bump (rc=$rc)"; exit 1; }; echo "PASS: gate green post-bump (rc=0)"
  </verify>
  <acceptance_criteria>
    - go.mod pins nats-server/v2 >= v2.14.3; `go mod tidy` clean; `task build` green.
    - `task lint:vuln` exits 0 on the bumped tree (verified by capturing `$?`, not by `echo`) — success-criterion #2, pass side.
    - A `vuln:` job exists in ci.yaml with an explicit `name: Vuln` running `task lint:vuln`, with checksum-pinned tool installs (no `@latest`).
    - The MANDATORY blocking ruleset checkpoint is Task 4 (adding the rendered `Vuln` check — not the workflow key `vuln` — to protect-main required checks is NOT left as an open SUMMARY note — review finding 6).
  </acceptance_criteria>
  <done>nats-server is remediated (>= v2.14.3), the vuln gate passes on the bumped tree, a CI job runs it, and the ruleset-blocking step is a blocking checkpoint (Task 4).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: MANDATORY — add the rendered `Vuln` check to the protect-main ruleset (D-04 requires a BLOCKING gate)</name>
  <files>GitHub repo settings — protect-main ruleset 11923801 (no in-repo file)</files>
  <read_first>
    - The VERIFIED live state: `gh api repos/holomush/holomush/rulesets/11923801` currently lists required checks [Build, Lint, Test, CodeRabbit, Integration Test, E2E Test] — note these are RENDERED job NAMES (`Lint`, not the workflow key `lint`), confirming the round-3 finding: add the new job's rendered `Vuln` name, NOT the key `vuln`. The `Vuln` check is NOT among them today, so it RUNS but does NOT block.
    - 06-04 Task 3 (the SINGLE consolidated protect-main assurance-gate checklist — it enumerates the mandatory `Vuln` check + the OPTIONAL/accepted-deferred `codecov/patch` + `codecov/project` ratchet for one ruleset edit; do the `Vuln` add here or as part of that one edit)
    - 06-RESEARCH.md §OPS-03(b) (the ruleset blocking step)
  </read_first>
  <action>
    Addresses review finding 6. OPS-03/D-04 REQUIRES a BLOCKING vuln gate — a CI job that
    merely runs is not a gate. HALT for a maintainer to add the rendered `Vuln` status check
    (the vuln: job's `name:`, NOT the workflow key `vuln` — round-3 finding 4) to the
    protect-main ruleset's required status checks (repo Settings → Rules → protect-main →
    Require status checks to pass), so a failing `vuln:` job BLOCKS merge. This is a
    repo-settings/operator action, NOT an in-repo YAML edit, and it is NOT auto-approvable.
    The consolidated protect-main assurance-gate checklist (the mandatory `Vuln` check + the
    optional codecov/patch + codecov/project ratchet) lives in 06-04 Task 3 — ideally the
    operator adds `Vuln` (and optionally codecov) in one ruleset edit; this checkpoint
    independently verifies that `Vuln` specifically is present, both in the ruleset JSON AND in
    a real PR's `statusCheckRollup` (the rendered context must actually attach to a PR). Record
    the gh-api + statusCheckRollup evidence in the SUMMARY.
  </action>
  <what-built>
    Task 3 added the `vuln:` CI job (it runs `task lint:vuln` on every PR) but a CI job is
    not a merge gate until it is a required check in the protect-main ruleset (verified via
    gh api: it is not today). D-04 requires the gate to BLOCK, so this is a mandatory
    human-action checkpoint, not an open follow-up.
  </what-built>
  <how-to-verify>
    1. GitHub → repo Settings → Rules → protect-main ruleset → "Require status checks to
       pass" → add the rendered `Vuln` check (the vuln: job's `name:`, NOT the key `vuln`) —
       and, per 06-04 Task 3, OPTIONALLY `codecov/patch` + `codecov/project` in the same edit.
    2. Confirm via API: `gh api repos/holomush/holomush/rulesets/11923801` now lists `Vuln`
       among required_status_checks.
    3. Confirm on a REAL PR: `gh pr view <n> --json statusCheckRollup` shows a `Vuln` context
       (the rendered name actually attaches — ruleset JSON alone can name a context that never
       reports if the rendered name is wrong).
    4. Record the gh-api + statusCheckRollup evidence in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "Vuln required" with the gh api + statusCheckRollup evidence, or describe the blocker.</resume-signal>
  <verify>
    <automated>MISSING — repo-settings action; verified via `gh api repos/holomush/holomush/rulesets/11923801` showing `Vuln` among required_status_checks AND `gh pr view <n> --json statusCheckRollup` showing a real `Vuln` context (evidence recorded in SUMMARY)</automated>
  </verify>
  <done>The rendered `Vuln` check is a required status check in the protect-main ruleset AND attaches to a real PR's statusCheckRollup (gh-api + statusCheckRollup evidence captured), so a failing vuln scan blocks merge — satisfying D-04's blocking-gate requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI vuln gate → merge decision | The gate is the control that stops a vulnerable dependency from merging; a fail-open gate is worse than no gate (false assurance) |
| tool install → CI runner | govulncheck/osv-scanner binaries execute in CI; an unpinned/tampered install is a supply-chain vector |
| allowlist (osv-scanner.toml) → gate output | An allowlist entry silently suppresses a finding; misuse hides a reachable CVE |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-3-01 | Spoofing/Elevation (fail-open) | task lint:vuln gate | high | mitigate | Two-tool gate; osv-scanner covers the Go-vuln-DB gap; pass/fail by EXIT CODE captured into a variable (NOT `; echo "exit=$?"`, which masks it — finding 8); pre-bump FAIL demonstrated (rc!=0 AND GHSA cited) before shipping |
| T-06-3-04 | Elevation (non-blocking gate = false assurance) | vuln: CI job not a required check, or required under the WRONG context name | high | mitigate | Mandatory blocking human-action checkpoint (Task 4) adds the RENDERED `Vuln` check (not the key `vuln` — round-3 finding 4) to the protect-main ruleset required checks, verified via gh-api AND a real PR's statusCheckRollup — a running-but-not-required job (or a ruleset requiring a context name that never reports) does not satisfy D-04's blocking requirement (findings 6 + round-3 #4) |
| T-06-3-02 | Tampering | allowlist misuse suppressing a reachable CVE | high | mitigate | osv-scanner.toml seeded EMPTY; the nats CVE is NOT allowlisted; every future entry requires `reason` + `ignoreUntil` EXPIRY (reviewable in PR). The allowlist scopes to OSV-Scanner ONLY — govulncheck has no suppression, so a reachable Go vuln is a hard stop (fail-closed, not allowlistable) |
| T-06-3-SC | Tampering (supply chain) | osv-scanner / govulncheck install | high | mitigate | Blocking human legitimacy checkpoint (Task 1) pins version + SHA-256; CI installs checksum-verified, never `@latest` |
| T-06-3-03 | Denial | vuln-DB fetch flakiness makes the gate non-deterministic | medium | accept | Pin tool + versions; note in SUMMARY; a transient DB-fetch failure is re-runnable, not a silent pass |
</threat_model>

<verification>
- Pre-bump: `task lint:vuln` exits non-zero (captured via `$?`, not `echo`) citing GHSA-q59r-vq66-pxc2.
- Post-bump: `task lint:vuln` exits 0; `task build` green.
- `vuln:` job present in ci.yaml with an explicit `name: Vuln` + pinned installs; the rendered `Vuln` check added to the protect-main ruleset via the mandatory Task 4 checkpoint (gh-api + statusCheckRollup evidence in SUMMARY).
</verification>

<success_criteria>
A build pinning a known-vulnerable nats-server fails the new vuln-scan CI gate; the dependency is >= v2.14.3 (ROADMAP Phase 6 success criterion #2 / OPS-03).
</success_criteria>

## Artifacts this phase produces (this plan)
- `Taskfile.yaml` `lint:vuln` task (NEW) — govulncheck + OSV-Scanner v2 `scan source`, outside the lint umbrella; header comment documents the OSV-only exception policy.
- `osv-scanner.toml` (NEW, repo root) — syntactically-valid EMPTY allowlist (`IgnoredVulns = []`, NOT a bare `[[IgnoredVulns]]` header); future entries require id+reason+ignoreUntil.
- `.github/workflows/ci.yaml` `vuln:` job (NEW) with an explicit `name: Vuln` (the rendered ruleset context) — checksum-pinned scanner installs (OSV-Scanner v2 pinned) + `task lint:vuln`.
- go.mod/go.sum bump: nats-server/v2 v2.14.2 → v2.14.3.
- BLOCKING CHECKPOINT (Task 4, out-of-repo): the rendered `Vuln` check added to the protect-main ruleset required checks (verified via gh-api AND a real PR's statusCheckRollup) — the mandatory blocking gate for D-04. Enumerated alongside the optional codecov/patch + codecov/project ratchet in the shared 06-04 Task 3 checklist.

<output>
Create `.planning/phases/06-operational-hardening-assurance-gates/06-03-SUMMARY.md` when done (record pinned tool versions/checksums, the pre-bump failure evidence line, and the Task 4 ruleset gh-api evidence).
</output>
