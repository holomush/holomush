---
phase: 05-character-identity-ui-public-profiles
plan: 05
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - test/integration/access/character_profile_read_test.go
  - test/integration/access/character_readtime_floor_test.go
  - test/integration/access/media_schema_test.go
  - docs/architecture/invariants.yaml
  - docs/architecture/invariants.md
autonomous: true
requirements: [PROFILE-01, EXT-05]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Mutating the seeded viewer-tier policy corpus and reloading the policy cache changes what the SAME anonymous viewer's GetCharacterProfile returns, with NO write to the character row and NO write to any of its entity_properties rows (ROADMAP criterion 4, D-98 delta 1)."
    - "The floor is therefore evaluated at read time and is never stamped onto a row, so no backfill exists — proven by the mutation being the only thing that changed between the two reads."
    - "One profile.image.primary row plus ten profile.image.gallery.00 through .09 rows insert through the REAL schema and read back through the viewer-filtered read path, with the gallery emitted in slot order (ROADMAP criterion 5, EXT-05, D-98 delta 2)."
    - "An anonymous viewer receives neither the primary image nor the gallery for the same character a guest-rung viewer does — the paired positive control proving the guest rung is doing work."
    - "A profile.image.gallery.10 row that EXISTS is absent from the response, because §8.6's totality rule denies an unenumerated name rather than defaulting it."
    - "An eleventh primary is rejected by UNIQUE(parent_type,parent_id,name) — CITED from TestPropertyRepository_ParentNameUniqueness, never reproved here (rule 7zy1161fh1 duplicate-gate anti-pattern, D-98)."
    - "Clearing a tier floor is set membership and never string ordering — CITED from TestNoTierFloorPolicyUsesAnOrdinalTierComparison and TestASyntheticFourthRungClearsNeitherShippedTierFloor, never reproved."
    - "The policy enumerates exactly eleven media names and not a twelfth — CITED from TestTheElevenMediaNamesAreEnumeratedAndTheTwelfthIsNot, never reproved."
    - "The two deltas ship as TWO tests, not one: a media-schema regression and a read-time-evaluation regression must not become one red test with two possible causes (D-98)."
    - "INV-ACCESS-10 is flipped to bound ONLY if every clause of its summary has a genuine asserting site named in asserted_by; otherwise it stays pending and a coverage issue is filed (.claude/rules/invariants.md)."
    - "Four owed ROADMAP / SPEC / REQUIREMENTS amendments are recorded in a GitHub issue rather than hand-edited into .planning/ROADMAP.md (rule a32nfcekfc)."
  artifacts:
    - "test/integration/access/character_readtime_floor_test.go — the criterion-4 read-time spec"
    - "test/integration/access/media_schema_test.go — the EXT-05 media-schema spec"
    - "test/integration/access/character_profile_read_test.go — newCorpusEngine widened to hand back its cache and corpus"
    - "docs/architecture/invariants.yaml + regenerated docs/architecture/invariants.md — the INV-ACCESS-10 binding decision"
  key_links:
    - "profileCorpusStore.excluded / .appended -> policy.Cache.Reload(ctx) -> the SAME *policy.Engine the server under test already holds: the spec mutates the corpus and reloads, never rebuilds the server, so the only variable between read A and read B is the policy."
    - "The 10-second poller (internal/access/policy/poller.go:95-96, cfg.Interval <= 0 defaults to 10s) is why the spec drives Reload() explicitly rather than sleeping past a tunable interval (D-97, Pitfall 9)."
    - "internal/grpc/characteraccess_projection.go:14,27-38 -> the eleven exact media names -> the rows this spec inserts. These are compared as exact BYTES; there is no normalization anywhere in the read path."
  prohibitions:
    - "MUST NOT reprove the UNIQUE(parent_type,parent_id,name) constraint by inserting a second primary — cite TestPropertyRepository_ParentNameUniqueness (rule 7zy1161fh1)."
    - "MUST NOT reprove set-membership tier clearing, the two-evaluations-per-attribute structure, or the eleven-media-name enumeration — all five already-covered clauses are CITED, not rebuilt (D-98)."
    - "MUST NOT merge the two deltas into one spec."
    - "MUST NOT sleep past the poller interval or assert on a later call instead of driving Reload() explicitly."
    - "MUST NOT weaken newCorpusEngine's two guards — the differs-in-at-least-one-direction refusal and the removed count are what stop a disarmed control."
    - "MUST NOT fabricate a // Verifies: annotation on a test that does not genuinely assert the invariant, and MUST NOT flip a registry entry to bound while any clause of its summary is unasserted."
    - "MUST NOT hand-edit docs/architecture/invariants.md inside its generated regions — regenerate with go run ./cmd/inv-render."
    - "MUST NOT hand-edit .planning/ROADMAP.md, .planning/REQUIREMENTS.md or .planning/STATE.md (rule a32nfcekfc)."
    - "MUST NOT claim anywhere in a test name, comment or acceptance criterion that a configuration change is visible on the next load or immediately (D-97)."
---

<objective>
Close ROADMAP criteria 4 and 5 with exactly two new integration specs, and cite — never reprove
— the five clauses already covered by shipped tests.

Purpose: both criteria are noun-phrased ("a change to the game's viewer-tier configuration
is what a logged-out visitor sees…", "one primary plus ten gallery image property rows insert
through the real schema and read back"), which is the shape rule `7zy1161fh1` exists to catch —
a criterion phrased as a noun names a property to establish, not an artifact to author. The
prior-phase artifacts and the tree were searched first and most of both is already proven. The
genuine deltas are two, and keeping them two matters: a media-schema regression and a
read-time-evaluation regression must not collapse into one red test with two possible causes.

Output: two Ginkgo specs, one widened harness helper, one honest invariant-binding decision,
and one GitHub issue carrying the four owed amendments.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-character-identity-ui-public-profiles/05-CONTEXT.md
@.planning/phases/05-character-identity-ui-public-profiles/05-RESEARCH.md
@.planning/phases/05-character-identity-ui-public-profiles/05-VALIDATION.md
@.planning/phases/01-portal-spec/01-SPEC.md
@.planning/phases/05-character-identity-ui-public-profiles/05-03-SUMMARY.md
</context>

## Existing coverage this plan CITES and must not rebuild

Re-verified this session against the tree. Each row is a clause of criterion 4 or 5 that is
already green; adding a second gate over any of them is the duplicate-gate anti-pattern
`01-SPEC.md` §2.6 and rule `7zy1161fh1` both name.

| Clause | Proven by | Location |
|---|---|---|
| Clearing a floor is set membership, never ordinal | `TestNoTierFloorPolicyUsesAnOrdinalTierComparison` | `internal/access/policy/seed_profile_visibility_test.go:260` |
| A synthetic 4th rung clears neither shipped floor | `TestASyntheticFourthRungClearsNeitherShippedTierFloor` | `internal/access/profilevis/tierfloor_test.go:173` |
| The floor is evaluated twice per attribute, separated by the action token | `TestAttributeVisibleIssuesExactlyTwoEvaluationsSeparatedByTheActionToken` | `internal/access/profilevis/profilevis_test.go:113` |
| `UNIQUE(parent_type,parent_id,name)` rejects a duplicate | `TestPropertyRepository_ParentNameUniqueness` | `internal/world/postgres/property_repo_test.go:430-450` |
| The policy enumerates exactly eleven media names, not twelve | `TestTheElevenMediaNamesAreEnumeratedAndTheTwelfthIsNot` | `internal/access/policy/seed_profile_visibility_test.go:393` |
| Unreachable profile is byte-identical to nonexistent | `INV-PRIVACY-9`, already `binding: bound` | `invariants.yaml:2164-2172` → `character_profile_read_test.go` |
| An infrastructure failure in the evaluation resolves DENY | `TestVisibleAttributesAbortsTheWholeCallWhenAnyEvaluationFails` | `internal/access/profilevis/profilevis_test.go:344` |
| Never stamped onto a row | structural — `entity_properties` carries no tier column and no migration writes one | — |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the corpus harness and prove the floor is read at read time</name>
  <files>test/integration/access/character_profile_read_test.go, test/integration/access/character_readtime_floor_test.go</files>

  <read_first>
    - `test/integration/access/character_profile_read_test.go:55-270` — `profileCorpusStore` (`:69-93`), `newCorpusEngine` (`:112-132`) with its two guards, `newServer` (`:161-185`), `read` (`:187-192`), `insertProperty` (`:197-203`) and the `BeforeEach` fixture with its documented cleanup order
    - `internal/access/policy/cache.go:141` — `Reload`, and what it recompiles
    - `internal/access/policy/poller.go:83-103` — `cfg.Interval <= 0` defaulting to 10 seconds, which is why the spec drives `Reload` rather than sleeping
    - `internal/access/policy/seed_profile_visibility_test.go` — the seeded tier-floor policy NAMES this spec will exclude or shadow, read them verbatim rather than guessing
    - `internal/access/profilevis/profilevis.go` — `Reachable`, `AttributeVisible`, `VisibleAttributes`
    - `internal/grpc/characteraccess_service.go:372-425` — `GetCharacterProfile`, including `resolveViewerIdentity`'s empty-token anonymous path
    - `.claude/rules/testing.md` — Ginkgo/Gomega under `//go:build integration`, ACE naming, `require` for preconditions
    - `01-SPEC.md` §8.2.1, §8.5.1, §8.6, §12.1 (PORTAL-10's six rules)
  </read_first>

  <behavior>
    - Read A: an anonymous viewer (`token = ""`) reads a character carrying a `profile.*` row seeded at the guest rung and receives a field set that does NOT contain that name.
    - The corpus is then mutated (the guest-rung policy for that name excluded, or a policy placing it at the anonymous rung appended) and `cache.Reload(ctx)` is called on the SAME cache the server's engine already holds.
    - Read B: the SAME anonymous viewer, against the SAME server value, receives a field set that DOES contain that name — so B differs from A.
    - Between the two reads `characters.version` for that character is unchanged, read by direct SQL.
    - Between the two reads the full set of that character's `entity_properties` rows — every `(id, name, value, visibility)` tuple — is unchanged, read by direct SQL.
    - The paired positive control (PORTAL-10 rule 2): the same reader is shown the attribute PERMITTED under one corpus and WITHHELD under the other, which the A/B shape carries by construction.
    - A second character that no mutated policy names returns the same field set under both corpora, so the mutation is targeted rather than global.
  </behavior>

  <action>
**Widen the harness first.** `newCorpusEngine` discards both the `*policy.Cache` and the
`*profileCorpusStore`, so no caller can reload a second time. Add a sibling
`newReloadableCorpusEngine(ctx, excluded []string, appended ...*policystore.StoredPolicy)
(*policy.Engine, *policy.Cache, *profileCorpusStore)` and have the existing `newCorpusEngine`
delegate to it and return only the engine, so there is exactly one definition of the two
guards. **Keep both guards**: the differs-in-at-least-one-direction refusal and the `removed`
count are what stop a disarmed control from passing while still LOOKING like a control. Do not
change `newCorpusEngine`'s signature — three shipped call sites depend on it.

**Write the spec** in a new `test/integration/access/character_readtime_floor_test.go`, build
tag `//go:build integration`, Ginkgo/Gomega, reusing the package's existing fixture rather than
building a second one. Build the server ONCE over the reloadable engine, seed one character
with a `profile.*` row at the guest rung and `visibility='public'`, then run the A → mutate →
`Reload` → B sequence above.

**Step 6 is the discriminating step and the whole point of the spec.** Snapshot
`characters.version` and the character's complete `entity_properties` row set by direct SQL
before read A and again after read B, and assert both are byte-identical. Without it the spec
degenerates into "the anonymous read omits field X", a shipped-state assertion that passes
whether or not the floor is read-time — which is exactly the vacuity rule `7zy1161fh1` and
memory `av7r20vcmg` warn about.

Do **not** sleep, and do **not** assert on "the next call": the policy cache is refreshed by a
poller whose interval defaults to ten seconds, so a time-based assertion is either flaky or
slow and its wording would overclaim the latency (D-97). Name the test after the property it
proves — that the floor is evaluated per read and that no row was written — and not after any
claim about when a change becomes visible.

Observe the spec RED against the pre-widening harness (it cannot compile without
`newReloadableCorpusEngine`) and then RED against a hypothetical stamped-floor implementation
by temporarily asserting the two field sets are EQUAL; record both observations in the SUMMARY
(PORTAL-10 rule 4).
  </action>

  <verify>
    <automated>task test:int -- ./test/integration/access/</automated>
  </verify>

  <acceptance_criteria>
    - `task test:int -- ./test/integration/access/` passes, including the three shipped call sites of `newCorpusEngine` which must be unmodified.
    - The spec calls `cache.Reload(ctx)` exactly once between the two reads: `rg -c 'Reload\(' test/integration/access/character_readtime_floor_test.go` returns 1.
    - `rg -n 'time.Sleep|Eventually' test/integration/access/character_readtime_floor_test.go` returns no match.
    - The spec asserts `characters.version` unchanged AND the complete `entity_properties` tuple set unchanged, both by direct `env.pool` SQL — a grep for `SELECT` in the file shows both queries.
    - The two field sets are asserted DIFFERENT, and a second untouched character's field set is asserted IDENTICAL under both corpora.
    - `rg -in 'next load|immediately' test/integration/access/character_readtime_floor_test.go` returns no match.
    - `newCorpusEngine`'s two guards still exist and are reached by the new sibling: `rg -n 'must differ from the seeded one|the control corpus must exclude exactly' test/integration/access/character_profile_read_test.go` returns both.
    - The temporary inverted assertion was observed RED and is recorded in the SUMMARY.
  </acceptance_criteria>

  <done>A corpus change with no write to the character or its rows changes what the same anonymous viewer sees, proving the floor is read at read time and never stamped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The media schema, through the real read path</name>
  <files>test/integration/access/media_schema_test.go</files>

  <read_first>
    - `internal/grpc/characteraccess_projection.go:14, 27-38, 79-106` — `profileImagePrimaryName`, the ten `profileGallerySlotNames` in emission order, and `projectPublic`'s routing of media names out of the profile map into `primary_image` / `gallery`
    - `test/integration/access/character_profile_read_test.go:187-203` — `read` and `insertProperty` (existing call sites pass `"public"` as `visibility`, which is what makes the row-keyed term permit a viewer)
    - `test/integration/access/character_profile_read_test.go:205-270` — the `BeforeEach` fixture, including the guest player and its live session token
    - `internal/access/policy/seed_profile_visibility_test.go:393` — `TestTheElevenMediaNamesAreEnumeratedAndTheTwelfthIsNot`, the enumeration this spec must CITE rather than restate
    - `internal/world/postgres/property_repo_test.go:430-450` — `TestPropertyRepository_ParentNameUniqueness`, the UNIQUE-constraint proof this spec must CITE rather than reprove
    - `api/proto/holomush/characteraccess/v1/characteraccess.proto:69-120` — `ProfileImage` and the `repeated ... max_items = 10` bound on `gallery`
    - `01-SPEC.md` §7.3 (exact-bytes media naming; `.0` and `.00` are two different rows) and §8.6 (an unenumerated name is DENIED, not defaulted)
  </read_first>

  <behavior>
    - Eleven rows insert through the real schema: `profile.image.primary` plus `profile.image.gallery.00` through `profile.image.gallery.09`, each `visibility='public'` and each carrying a distinct sentinel value.
    - A guest-rung viewer's `GetCharacterProfile` returns `PrimaryImage.MediaId` equal to the primary sentinel.
    - The same response's `Gallery` has exactly ten entries, in slot order `00` through `09`, matching the ten sentinels positionally.
    - None of the eleven media names appears as a key in the response's `profile` map — the projection routes them out.
    - An ANONYMOUS viewer receives neither the primary nor any gallery entry for the same character (the paired positive control that proves the guest rung is doing work).
    - A twelfth row named `profile.image.gallery.10` that EXISTS in the table is absent from the guest-rung response, because §8.6's totality rule denies an unenumerated name rather than defaulting it.
    - Assertions are made against the MARSHALED response bytes with a distinctive sentinel, not against a populated Go struct (PORTAL-10 rule 3).
  </behavior>

  <action>
Write `test/integration/access/media_schema_test.go`, build tag `//go:build integration`,
Ginkgo/Gomega, reusing the package's existing fixture and its `insertProperty` helper. Insert
the eleven rows with the names taken **verbatim** from `profileGallerySlotNames` and
`profileImagePrimaryName` — exact bytes, zero-padded two digits — and give each a distinct
sentinel media handle so a positional mix-up in the gallery is visible in the failure message
rather than hidden behind eleven identical strings.

Read as the guest rung using the fixture's existing guest player and live session token, then
read the same character anonymously, and assert the contrast. Marshal the response and assert
the sentinels' presence and absence in the **bytes**, per PORTAL-10 rule 3 — a populated struct
assertion would pass against an implementation that never serialized the field.

Insert one `profile.image.gallery.10` row and assert its sentinel is absent from the guest-rung
response. That is the §8.6 totality rule doing real work and is a genuinely non-obvious
property: the eleventh slot name is not enumerated by any policy, so it is denied rather than
defaulted to the guest rung its ten siblings sit at.

**Do NOT insert a second `profile.image.primary` row.** The UNIQUE constraint is already proven
by `TestPropertyRepository_ParentNameUniqueness`; reproving it here is the duplicate-gate
anti-pattern. Cite that test by name and path in the spec's header comment instead, and do the
same for the eleven-name enumeration test.

Keep this spec SEPARATE from Task 1's. A media-schema regression and a read-time-evaluation
regression must not become one red test with two possible causes (D-98).
  </action>

  <verify>
    <automated>task test:int -- ./test/integration/access/</automated>
  </verify>

  <acceptance_criteria>
    - `task test:int -- ./test/integration/access/` passes.
    - `rg -o 'profile\.image\.gallery\.[0-9]+' test/integration/access/media_schema_test.go | sort -u | wc -l` outputs `11` (the ten enumerated slots plus the deliberately-unenumerated eleventh).
    - The ten enumerated slot names in the spec are set-equal to `rg -o '"profile\.image\.gallery\.[0-9]+"' internal/grpc/characteraccess_projection.go | sort -u`.
    - The spec header names `TestPropertyRepository_ParentNameUniqueness` and `TestTheElevenMediaNamesAreEnumeratedAndTheTwelfthIsNot` as the cited proofs.
    - `rg -c 'profile\.image\.primary' test/integration/access/media_schema_test.go` shows the primary inserted exactly once — no duplicate-insert reprove.
    - The gallery order assertion compares the ten sentinels positionally, so a re-sorted gallery fails.
    - The assertions run against marshaled bytes: `rg -n 'proto.Marshal|MarshalVT|Marshal\(' test/integration/access/media_schema_test.go` matches.
    - The anonymous-viewer control asserts BOTH the primary and the gallery are absent.
  </acceptance_criteria>

  <done>The real media naming scheme survives the real viewer-filtered read path, and the no-migration-later claim is demonstrated rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 3: The INV-ACCESS-10 binding decision and the four owed amendments</name>
  <files>docs/architecture/invariants.yaml, docs/architecture/invariants.md</files>

  <read_first>
    - `docs/architecture/invariants.yaml:2300-2330` — `INV-ACCESS-10`'s three-clause summary and its current `binding: pending`, plus `INV-ACCESS-11`/`-12` for the neighbouring shape
    - `.claude/rules/invariants.md` — the binding ratchet, the "MUST NOT fabricate a binding" rule, and the note that a PARTIAL binding is a false green `TestBoundInvariantsAreGenuinelyAsserted` cannot detect
    - `test/meta/invariant_registry_test.go` — the drift, provenance, binding-presence and genuinely-asserted guards, and the `// Verifies:` parser's requirement of a trailing number
    - `internal/access/profilevis/profilevis_test.go:113` and `:344` — the two shipped tests that carry clauses of INV-ACCESS-10
    - `test/integration/access/character_readtime_floor_test.go` — created in Task 1
    - `.planning/phases/05-character-identity-ui-public-profiles/05-CONTEXT.md` § "Roadmap amendments owed" and `05-RESEARCH.md` § "Roadmap / SPEC Amendments Owed"
  </read_first>

  <action>
**Decide the binding honestly, clause by clause.** `INV-ACCESS-10`'s summary carries three
clauses: (a) the floor is evaluated at READ TIME by the default-deny engine against the
attribute name and the viewer's tier; (b) it is never stamped onto `entity_properties.visibility`
per row; (c) an infrastructure failure in that evaluation resolves DENY, never permit and never
a silently sparse profile.

Write the clause→site table into the plan SUMMARY first, then act on it. Task 1's spec asserts
(a) and (b). `TestAttributeVisibleIssuesExactlyTwoEvaluationsSeparatedByTheActionToken`
(`profilevis_test.go:113`) asserts the attribute-name-and-tier structure inside (a).
`TestVisibleAttributesAbortsTheWholeCallWhenAnyEvaluationFails` (`profilevis_test.go:344`)
asserts (c). If and only if every clause has a genuine asserting site, annotate each of the
three sites with `// Verifies: INV-ACCESS-10`, set `binding: bound` and list all three files in
`asserted_by`. If ANY clause turns out to lack a genuine assertion, leave the entry `pending`,
add **no** annotation anywhere, and file a coverage-gap issue instead — a `// Verifies:` on a
test proving only part of a multi-clause summary is a false green the registry's own
genuinely-asserted guard cannot detect, which is exactly the INV-RB-3 failure mode.

Regenerate with `go run ./cmd/inv-render`; never hand-edit inside the generated regions of
`docs/architecture/invariants.md`.

**File the amendments as ONE GitHub issue**, not as an edit to any `.planning/` file. Rule
`a32nfcekfc` forbids hand-editing `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` and
`.planning/STATE.md`, and no `gsd-tools` verb rewrites an existing phase's success criteria —
the `phase` verbs are `uat-passed`, `next-decimal`, `add`, `add-batch`, `insert`, `remove`,
`complete`, `list-plans` and the `roadmap` verbs are `analyze`, `get-phase`,
`update-plan-progress`, `annotate-dependencies`, `validate`, `upgrade`. Run
`gh issue create -R holomush/holomush` with the four owed amendments, each quoting the current
text and the replacement:

1. **Criterion 4 latency** — "on the next load" becomes "on the next load after the policy cache
   reloads (poller interval, default 10s)". The criterion's substance is intact; only the
   latency claim overclaims (D-97, `internal/access/policy/poller.go:95-96`).
2. **Criterion 4 / PROFILE-12 scope** — strike "both the retirement flow and"; the retirement
   half moves to Phase 6 alongside `AdminRetireCharacter`, because there is no player-facing
   retirement flow in v0.13 (D-91, IDENT-04). `REQUIREMENTS.md`'s PROFILE-12 row needs the same
   note.
3. **`01-SPEC.md` §9.3** — the mutation table has no `SetDefaultCharacter` row. D-89 acknowledges
   it and §9.1 authorises adding the RPC, but the table is the SPEC's own inventory of the
   mutation surface, and plan 05-01 shipped the RPC.
4. **`01-SPEC.md` §9.6** — the eight-code table has no row for `CHARACTER_LIMIT_REACHED` or
   `CHARACTER_NO_STARTING_LOCATION` (both reachable from the create path, both pinned to
   `FailedPrecondition` by plan 05-03), nor for `CHARACTER_NOT_PLAYABLE` (plan 05-01's
   retired-default refusal, pinned to `FailedPrecondition`).

Record the issue number in the SUMMARY. Pass `-R holomush/holomush` — `gh` is being run from a
worktree.
  </action>

  <verify>
    <automated>go run ./cmd/inv-render &amp;&amp; git diff --exit-code docs/architecture/invariants.md &amp;&amp; task test -- -run 'TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted|TestInvariantRegistryRender' ./test/meta/</automated>
  </verify>

  <acceptance_criteria>
    - `go run ./cmd/inv-render` followed by `git diff --exit-code docs/architecture/invariants.md` reports no drift (the generated file matches the YAML).
    - `task test -- ./test/meta/` passes, including the binding-presence, provenance and genuinely-asserted guards.
    - The SUMMARY contains the clause→site table for `INV-ACCESS-10`, enumerating all three clauses of its summary and naming a `path:test` for each, or naming the clause that has none.
    - If `binding: bound` was set, `rg -n '// Verifies: INV-ACCESS-10' --type go` returns exactly the sites listed in `asserted_by`, and none of them is a `Skip`-only placeholder.
    - If `binding: pending` was kept, `asserted_by` is absent from the entry (the meta-test rejects a pending entry that lists one) and a coverage-gap issue number is recorded in the SUMMARY.
    - `git diff --stat .planning/` shows no change to `ROADMAP.md`, `REQUIREMENTS.md` or `STATE.md`.
    - `gh issue view <number> -R holomush/holomush --json title,body` shows all four amendments, each with its current text and its replacement.
  </acceptance_criteria>

  <done>The registry says exactly what the tests prove, and the four owed amendments are tracked where the ROADMAP's owner can act on them rather than edited into a tool-owned file.</done>

  <reversibility rating="costly">An invariant binding is a claim about what the test suite proves; a fabricated one is a false green that survives indefinitely and is not detectable by the registry's own guards.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy corpus → compiled snapshot | The only channel by which a configuration change reaches a read decision |
| anonymous viewer → `GetCharacterProfile` | The unauthenticated read whose field set this plan proves is corpus-derived |
| stored property rows → marshaled response | Where absence must be produced, and where these specs assert it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05-01 | Information disclosure | a stamped (cached-onto-row) floor | **high** | mitigate | Task 1 proves the field set changes with the corpus alone, with the character row and every property row byte-identical across the two reads. A stamped floor would fail that assertion. |
| T-05-05-02 | Information disclosure | an unenumerated media name defaulting to permit | **high** | mitigate | Task 2 inserts a real `profile.image.gallery.10` row and asserts its sentinel is absent from the response — §8.6's totality rule denies rather than defaults. |
| T-05-05-03 | Information disclosure | present-but-empty media field | medium | mitigate | Assertions run against marshaled bytes with distinctive sentinels (PORTAL-10 rule 3), not against a populated Go struct. |
| T-05-05-04 | Tampering | a disarmed control corpus | medium | mitigate | `newReloadableCorpusEngine` inherits both shipped guards; the acceptance criteria grep for their messages so a future edit that drops one is visible. |
| T-05-05-05 | Repudiation | a fabricated invariant binding | **high** | mitigate | The clause→site table is written before the annotation, and a clause without a genuine site leaves the entry `pending` with a filed coverage issue. `TestBoundInvariantsAreGenuinelyAsserted` cannot detect a partial binding, which is why the human-readable table is the control. |
| T-05-05-06 | Tampering | a green census bought by deleting a row | high | mitigate | No census row is touched by this plan; §3.1 rule 3's erosion path is simply not reachable here. |
| T-05-05-07 | Denial of service | integration specs holding a Postgres testcontainer | low | accept | Both specs reuse the package's existing shared fixture rather than starting a container of their own. |
</threat_model>

<verification>
- `task test:int -- ./test/integration/access/` green, with the three shipped `newCorpusEngine` call sites unmodified.
- `task test -- ./test/meta/` green.
- `go run ./cmd/inv-render && git diff --exit-code docs/architecture/invariants.md` clean.
- `git diff --stat .planning/` shows no change to `ROADMAP.md`, `REQUIREMENTS.md` or `STATE.md`.
- Both specs were observed RED before their implementations, recorded in the SUMMARY.

**No denial tests ship in this plan's Task 3** (it is a registry and documentation task), so
PORTAL-10 rule 2's paired-positive-control obligation does not apply there. It DOES apply to
Tasks 1 and 2, and both carry their pairing by construction: Task 1's A/B shape shows the same
reader permitted under one corpus and withheld under the other, and Task 2 pairs the guest-rung
read against the anonymous one. This is stated explicitly rather than omitted, per PORTAL-10
rule 2's own instruction.
</verification>

<success_criteria>
- Criterion 4 is proven by a corpus mutation that writes nothing to the character or its rows.
- Criterion 5 is proven by eleven real rows through the real read path, with the UNIQUE constraint cited rather than reproved.
- The two deltas are two tests.
- `INV-ACCESS-10` says exactly what the suite proves, and the four owed amendments are filed.
</success_criteria>

## Artifacts this phase produces (plan 05-05)

**Go (new file):** `test/integration/access/character_readtime_floor_test.go`,
`test/integration/access/media_schema_test.go`.
**Go (new symbol):** `newReloadableCorpusEngine` in
`test/integration/access/character_profile_read_test.go`.
**Registry:** the `INV-ACCESS-10` entry's `binding` / `asserted_by` fields in
`docs/architecture/invariants.yaml` and the regenerated regions of
`docs/architecture/invariants.md`; up to three `// Verifies: INV-ACCESS-10` annotations.
**External:** one GitHub issue carrying the four owed ROADMAP / SPEC / REQUIREMENTS amendments.

<output>
Create `.planning/phases/05-character-identity-ui-public-profiles/05-05-SUMMARY.md` when done.
</output>
