---
phase: 03-platform-hardening-deployment-scaling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/testsupport/natstest/nats.go
  - internal/testsupport/natstest/nats_test.go
  - .claude/rules/testing.md
  - CLAUDE.md
autonomous: true
requirements: [CLUSTER-03]
must_haves:
  truths:
    - "A //go:build integration test can start a single external NATS JetStream node via natstest and obtain a URL (D-05a, OQ-4)"
    - "natstest hands each caller an independent *nats.Conn to the container (per-replica connections, not a shared conn) — the exact gap CLUSTER-03 closes (RESEARCH Pitfall 2)"
    - "The 'embedded NATS is correct at every test tier' rule is amended: embedded stays correct EXCEPT external-mode-specific behavior, which MUST use a real NATS container (D-06)"
  artifacts:
    - "internal/testsupport/natstest/nats.go — external-NATS testcontainer helper (single node, JetStream)"
    - "internal/testsupport/natstest/nats_test.go — smoke test proving container start + two independent conns"
  key_links:
    - "natstest.URL() -> per-replica nats.Connect(url) consumed by Plans 03/04/05/06 integration tests"
  prohibitions:
    - "MUST NOT add a NATS testcontainer go.mod module — use the generic testcontainers.GenericContainer running nats:2-alpine (RESEARCH OQ-4 / Alternatives)"
    - "MUST NOT spin a 3-node NATS cluster — the invariants bind on HoloMUSH replicas, not NATS replication (OQ-4)"
    - "MUST NOT be importable by production code (test-support package; depguard parity with eventbustest/coretest)"
---

<objective>
Build the external-NATS integration substrate: a testcontainer helper that starts
a single real NATS JetStream node and hands each replica its OWN `*nats.Conn`.
Amend the test-tier rule so external-mode behavior is verified against a real
container rather than the embedded harness.

Purpose: This is the Wave-0 substrate every downstream integration proof
(CLUSTER-01 external connect, CLUSTER-02 scoping self-check, CLUSTER-03 multi-node
invalidation, CLUSTER-04 DLQ) stands on. Today the "multi-member" crypto test
shares ONE in-process conn (`cache_invalidation_test.go:41`) — that is exactly the
gap this harness closes.
Output: `internal/testsupport/natstest/` helper + the D-06 rule amendment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-hardening-deployment-scaling/03-CONTEXT.md
@.planning/phases/03-platform-hardening-deployment-scaling/03-RESEARCH.md
@.planning/phases/03-platform-hardening-deployment-scaling/03-PATTERNS.md
@test/testutil/postgres.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: External-NATS testcontainer helper with per-replica connections</name>
  <files>internal/testsupport/natstest/nats.go, internal/testsupport/natstest/nats_test.go</files>
  <read_first>
    - test/testutil/postgres.go:1-142 (testcontainer helper pattern: imports, env struct, Start with retry, Terminate, wait strategy)
    - internal/cluster/clustertest/harness.go:32-110 (the shared-conn shape the new harness must replace with per-replica conns)
    - test/integration/crypto/cache_invalidation_test.go:37-63 (the `Conn: h.Embedded.Conn` shared-conn gap this substrate closes)
    - 03-PATTERNS.md § "internal/testsupport/natstest/" (analog = postgres.go; nats:2-alpine -js -sd /data + wait.ForListeningPort)
    - 03-RESEARCH.md § OQ-4 (single node, generic container, no NATS module)
  </read_first>
  <action>
    Create `internal/testsupport/natstest/nats.go` (package natstest, SPDX header). Model
    the container lifecycle on `test/testutil/postgres.go`: a `NATSEnv` struct holding the
    `testcontainers.Container` and the resolved client `URL string`; a `StartNATS(ctx)
    (*NATSEnv, error)` that runs a generic `testcontainers.GenericContainer` with image
    `nats:2-alpine`, `Cmd: []string{"-js","-sd","/data"}`, `ExposedPorts: []string{"4222/tcp"}`,
    and `WaitingFor: wait.ForListeningPort("4222/tcp")` (or `wait.ForLog("Server is ready")`);
    resolve the mapped 4222 port into `URL` (`nats://host:port`). Add a `Terminate(ctx)`
    and a `Conn(t testing.TB) *nats.Conn` helper that calls `nats.Connect(env.URL)` and
    registers `t.Cleanup(conn.Close)` — each call yields an INDEPENDENT connection so a test
    can build N replicas each with its own conn. Do NOT add a NATS testcontainer go.mod
    module (use the generic container per OQ-4). Do NOT wire JetStream provisioning here —
    the helper only provides transport; callers provision what they need.
  </action>
  <verify>
    <automated>task test:int -- -run TestNatstest ./internal/testsupport/natstest/</automated>
  </verify>
  <acceptance_criteria>
    - `natstest.StartNATS(ctx)` returns a running container with a dialable `URL`; smoke test connects and JetStream `AccountInfo` succeeds.
    - Two `env.Conn(t)` calls return two distinct `*nats.Conn` values (distinct client IDs), proving independent connections.
    - No new module appears under `require` in go.mod for a NATS testcontainer (generic container only).
    - File is under `internal/testsupport/` and carries `//go:build integration` where needed for the smoke test (the helper itself may be build-tag-free; the smoke test file is integration-tagged).
  </acceptance_criteria>
  <done>A real single-node NATS JetStream container is startable from the integration tier and hands out independent per-replica connections.</done>
</task>

<task type="auto">
  <name>Task 2: Amend the "embedded NATS is correct at every tier" rule (D-06)</name>
  <files>.claude/rules/testing.md, CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (Testing section: the "embedded NATS is correct at every test tier" MUST bullet, and the depguard note on eventbustest/coretest)
    - .claude/rules/testing.md (Test Tiers table + the "Production code MUST NOT import eventbustest" line)
    - 03-CONTEXT.md D-06 (the exact amendment: embedded remains correct EXCEPT external-mode-specific behavior)
  </read_first>
  <action>
    Amend the standing rule in both CLAUDE.md and `.claude/rules/testing.md`: embedded NATS
    (`eventbustest`) stays the correct harness for every non-external-specific test, but
    external-mode-specific behavior (external dial/fail-closed boot, single-principal
    scoping, multi-node per-replica invalidation, DLQ against a real broker) MUST use a real
    NATS container via `internal/testsupport/natstest` (D-06). Add a Test-Tiers row naming the
    external-NATS integration tier. Keep the depguard prohibition intact (production code MUST
    NOT import natstest either — mirror the eventbustest/coretest wording). Run `task fmt`
    afterward (it reflows the CLAUDE.md tables + license headers) and commit its output.
  </action>
  <verify>
    <automated>task lint:docs-symmetry && rg -q "natstest" CLAUDE.md .claude/rules/testing.md</automated>
  </verify>
  <acceptance_criteria>
    - Both CLAUDE.md and `.claude/rules/testing.md` state that external-mode-specific behavior MUST use a real NATS container (`natstest`), while embedded stays correct elsewhere.
    - A Test-Tiers entry references the external-NATS integration tier.
    - `task lint:docs-symmetry` passes (AGENTS.md symlink integrity preserved); `task fmt` output committed.
  </acceptance_criteria>
  <done>The test-tier taxonomy rule is amended in lockstep with introducing the external-NATS harness (D-06).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- New package: `internal/testsupport/natstest` with `NATSEnv`, `StartNATS(ctx)`, `(*NATSEnv).Conn(t)`, `(*NATSEnv).URL`, `(*NATSEnv).Terminate(ctx)`
- Rule amendment text in `CLAUDE.md` + `.claude/rules/testing.md` (external-NATS tier; natstest depguard parity)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → ephemeral NATS container | Test-only; container torn down at process exit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | natstest imported by production code (privilege/coupling) | medium | mitigate | Package placed under `internal/testsupport/`; depguard parity with eventbustest/coretest so production imports fail lint (T2) |
| T-03-04 | Denial of Service | leaked containers exhaust CI Docker resources | low | mitigate | `Terminate` + `t.Cleanup`, mirroring the postgres helper's reclaim-on-failure pattern |
</threat_model>

<verification>
- `task test:int -- -run TestNatstest ./internal/testsupport/natstest/` green (Docker required).
- `task lint` + `task lint:docs-symmetry` clean.
- go.mod gains no NATS testcontainer module.
</verification>

<success_criteria>
The external-NATS integration substrate exists (single JetStream node, per-replica
connections) and the test-tier rule is amended (D-06), unblocking every downstream
external-mode integration proof.
</success_criteria>

<output>
Create `.planning/phases/03-platform-hardening-deployment-scaling/03-02-SUMMARY.md` when done.
</output>
