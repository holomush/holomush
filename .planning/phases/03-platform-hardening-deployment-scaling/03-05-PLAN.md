---
phase: 03-platform-hardening-deployment-scaling
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - test/integration/crypto/cache_invalidation_test.go
  - test/integration/cluster/cluster_test.go
  - docs/architecture/invariants.yaml
  - docs/architecture/invariants.md
autonomous: true
requirements: [CLUSTER-03]
must_haves:
  truths:
    - "Multi-node crypto invalidation is verified with each replica on its OWN *nats.Conn to a single external NATS container (closes the shared-conn gap at cache_invalidation_test.go:41, D-05a)"
    - "A KEK/DEK rotation across N replicas receives N-of-N acks (INV-CLUSTER-1/2) and evicts every replica's DEK/Participants cache (INV-CLUSTER-9); cluster_id-prefix filtering holds (INV-CLUSTER-4)"
    - "A hung/dead replica triggers probe-and-pill; the cluster proceeds with N-1 and rotation completes (D-08)"
    - "INV-CLUSTER-1 flips pending->bound and INV-CLUSTER-2/4/9 gain the multi-node test as an additional asserted_by — bound only where a test GENUINELY asserts (D-07, no fabricated bindings)"
    - "New INV-EVENTBUS-29 (external boot fails closed) and INV-EVENTBUS-30 (DLQ never drops) are registered, bound to the Plan 03/04 integration tests"
    - "invariants.md is regenerated from invariants.yaml (no drift) via cmd/inv-render"
  artifacts:
    - "Rebound test/integration/crypto/cache_invalidation_test.go (per-replica conns + // Verifies annotations)"
    - "Extended test/integration/cluster/cluster_test.go (multi-node cluster_id filtering + hung-replica probe-pill over external conns)"
    - "Updated docs/architecture/invariants.yaml + regenerated invariants.md"
  key_links:
    - "natstest container URL -> per-replica nats.Connect -> invalidation.Coordinator/cluster.Registry members"
    - "// Verifies: INV-CLUSTER-1/2/4/9 + INV-EVENTBUS-29/30 -> invariants.yaml asserted_by/binding"
  prohibitions:
    - "MUST NOT fabricate a binding — annotate/flip to bound ONLY where a test genuinely asserts the invariant (D-07; meta-test TestBoundInvariantsAreGenuinelyAsserted)"
    - "MUST NOT leave a binding: pending entry carrying asserted_by (TestRegistryBindingChecks)"
    - "MUST NOT hand-edit inside the generated regions of invariants.md — regenerate via go run ./cmd/inv-render"
    - "MUST NOT share one NATS conn across replicas (that is the gap being closed, Pitfall 2)"
---

<objective>
Verify the shipped crypto-invalidation + cluster substrate against REAL multi-node
replicas — each with its own `*nats.Conn` to a single external NATS container — and
land the phase's invariant-registry discipline in one consolidated change: bind the
inherently-multi-replica invariants (INV-CLUSTER-1/2/4/9), mint the two new
INV-EVENTBUS guarantees (external boot fail-closed; DLQ never drops), and audit the
remaining CLUSTER entries.

Purpose: CLUSTER-03's heart. The substrate is transport-agnostic core-NATS pub/sub;
today's "multi-member" test shares ONE in-process conn, so it does not actually
prove multi-node behavior. Giving each replica an independent connection is the
verification delta. Consolidating all invariants.yaml edits here avoids concurrent
registry conflicts and gives the phase one reviewable binding change (D-07).
Output: rebound multi-node specs + updated invariant registry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-hardening-deployment-scaling/03-CONTEXT.md
@.planning/phases/03-platform-hardening-deployment-scaling/03-RESEARCH.md
@.planning/phases/03-platform-hardening-deployment-scaling/03-PATTERNS.md
@test/integration/crypto/cache_invalidation_test.go
@.claude/rules/invariants.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebind crypto invalidation to per-replica external connections (D-05a, D-08)</name>
  <files>test/integration/crypto/cache_invalidation_test.go</files>
  <read_first>
    - test/integration/crypto/cache_invalidation_test.go:27-80 (newCoordOnMember + the shared `Conn: h.Embedded.Conn` at :41; the `// Verifies: INV-CLUSTER-2` annotation shape at :66)
    - internal/cluster/clustertest/harness.go:32-110 (Harness/HarnessMember shape; the members share Embedded.Conn today)
    - internal/eventbus/crypto/invalidation/coordinator.go:69-406 (request-reply acks, probe-and-pill retry) — read to confirm what a genuine N-of-N-ack / N-1 assertion looks like
    - internal/testsupport/natstest (Plan 02 harness — per-replica Conn)
    - 03-PATTERNS.md § "cache_invalidation_test.go (multi-node rebind)" + § "Invariant // Verifies: binding"
    - 03-RESEARCH.md § OQ-4, § Pitfall 2, § Pitfall 4 (no fabricated bindings)
  </read_first>
  <action>
    Rework the crypto invalidation integration specs so member `i`'s Coordinator wires to its
    OWN connection from `natstest` (each member does `env.Conn(t)` → distinct `*nats.Conn`),
    replacing the shared `h.Embedded.Conn` at :41 (Pitfall 2). Keep the cluster.Registry
    members pointed at the same container so heartbeats/membership converge across independent
    conns. Add/keep genuine assertions: (a) a rotation where all N live replicas ack (proves
    N-of-N, INV-CLUSTER-1 for KEK rotation, INV-CLUSTER-2 for Rotate/Rekey), asserting every
    replica's DEK cache AND ParticipantsCache is evicted for the context on return
    (INV-CLUSTER-9); (b) a hung/dead-replica case where one member's conn is closed mid-flight
    → probe-and-pill fires → the Coordinator proceeds with N-1 and the rotation completes
    (D-08). Place `// Verifies: INV-CLUSTER-1`, `// Verifies: INV-CLUSTER-2`, `// Verifies:
    INV-CLUSTER-9` immediately above the specs that genuinely assert each (D-07 — do NOT annotate
    a spec that merely touches the code). This file becomes `//go:build integration` under the
    external tier.
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/crypto/</automated>
  </verify>
  <acceptance_criteria>
    - No `h.Embedded.Conn` (or any single shared conn) is passed to more than one member's Coordinator (`rg "Embedded.Conn" test/integration/crypto/cache_invalidation_test.go` returns nothing, or only a non-shared use).
    - The all-ack spec asserts N-of-N acks + eviction of every replica's DEK/Participants cache; the hung-replica spec asserts probe-pill + N-1 completion.
    - `// Verifies: INV-CLUSTER-1`, `-2`, `-9` sit above genuinely-asserting specs.
    - `task test:int -- ./test/integration/crypto/` green.
  </acceptance_criteria>
  <done>Crypto invalidation is proven across per-replica external connections, including the hung-replica probe-pill path (D-08).</done>
</task>

<task type="auto">
  <name>Task 2: Multi-node cluster_id filtering + probe-pill over external conns (INV-CLUSTER-4)</name>
  <files>test/integration/cluster/cluster_test.go</files>
  <read_first>
    - test/integration/cluster/cluster_test.go (existing cluster specs + clustertest.Harness usage)
    - internal/cluster/heartbeat.go + internal/eventbus/crypto/invalidation/coordinator.go (cluster_id-prefix drop logic referenced by INV-CLUSTER-4)
    - internal/testsupport/natstest (per-replica conns)
    - docs/architecture/invariants.yaml:2213-2226 (INV-CLUSTER-4 current binding to single-conn cluster_test.go)
    - 03-RESEARCH.md § D-07 binding plan + § Security Domain (cross-cluster message injection row)
  </read_first>
  <action>
    Extend `test/integration/cluster/cluster_test.go` with a multi-node spec (external conns
    from natstest) proving cluster_id-prefixed coordination subjects: a member configured with
    a DIFFERENT cluster_id drops messages whose payload cluster_id disagrees (INV-CLUSTER-4),
    and — if not already covered against real conns — that the hung-replica probe-pill path
    (INV-CLUSTER-5/-7/-10 mechanics) still holds over independent connections. Annotate the
    genuinely-asserting spec with `// Verifies: INV-CLUSTER-4`. For INV-CLUSTER-3/5/6/7/8/10:
    only add `// Verifies:` where THIS multi-node spec genuinely asserts the invariant; do not
    touch the others (they stay bound to their existing single-conn/unit specs per the registry).
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/cluster/</automated>
  </verify>
  <acceptance_criteria>
    - A multi-node spec asserts cross-cluster_id message rejection over per-replica conns, annotated `// Verifies: INV-CLUSTER-4`.
    - No fabricated annotations added to specs that do not genuinely assert their cited invariant.
    - `task test:int -- ./test/integration/cluster/` green.
  </acceptance_criteria>
  <done>Multi-node cluster_id filtering is proven over real independent connections (INV-CLUSTER-4).</done>
</task>

<task type="auto">
  <name>Task 3: Invariant registry capstone — mint, bind, audit, regenerate</name>
  <files>docs/architecture/invariants.yaml, docs/architecture/invariants.md</files>
  <read_first>
    - docs/architecture/invariants.yaml:2176-2306 (INV-CLUSTER-1..10 current state: 1 & 8 pending, 2/3/4/5/6/7/9/10 bound) and :2607 (last INV-EVENTBUS entry = INV-EVENTBUS-28; next free = 29,30)
    - .claude/rules/invariants.md (binding ratchet: bound needs a genuine // Verifies:; pending MUST NOT carry asserted_by; regenerate with go run ./cmd/inv-render)
    - test/meta/invariant_registry_test.go (TestBoundInvariantsAreGenuinelyAsserted, TestRegistryBindingChecks, TestEveryRegistryInvariantHasBinding, provenance guard)
    - 03-PATTERNS.md § "docs/architecture/invariants.yaml (MODIFY)" (entry shape) + 03-RESEARCH.md § OQ-8
  </read_first>
  <action>
    Update `docs/architecture/invariants.yaml`: (a) flip INV-CLUSTER-1 `binding: pending` →
    `bound` with `asserted_by` including the rebound `test/integration/crypto/cache_invalidation_test.go`
    (only because Task 1 now genuinely asserts N-of-N KEK-rotation acks); (b) add the multi-node
    test file to `asserted_by` for INV-CLUSTER-2/-4/-9 (they stay bound, now with stronger
    provenance); (c) mint `INV-EVENTBUS-29` ("external-mode boot fails closed: mode:external with
    an unreachable NATS server or a provision:false stream-config mismatch refuses to Start")
    under the INV-EVENTBUS scope after :2607, `binding: bound`, `asserted_by:
    ["test/integration/eventbus_external/external_boot_test.go"]`; (d) mint `INV-EVENTBUS-30`
    ("DLQ capture never drops: a message exhausting MaxDeliver is Term'd only after a successful
    DLQ publish; a failed DLQ publish Naks — redelivery continues"), `binding: bound`,
    `asserted_by: ["test/integration/audit/dlq_capture_test.go"]`. Audit INV-CLUSTER-3/5/6/7/8/10:
    where an existing test genuinely asserts, leave as-is; for INV-CLUSTER-8 (analyzer-only, not
    multi-node) leave `binding: pending` and file `gh issue create -R holomush/holomush --label bug
    --title "coverage gap: INV-CLUSTER-8 unbound" ...`. Regenerate with `go run ./cmd/inv-render`
    and DO NOT hand-edit the generated regions of invariants.md.
  </action>
  <verify>
    <automated>go run ./cmd/inv-render && task test -- -run 'TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted|TestRegistryBindingChecks' ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - INV-CLUSTER-1 is `bound` with a genuine `// Verifies:` in cache_invalidation_test.go; INV-CLUSTER-2/4/9 list the multi-node test in asserted_by.
    - INV-EVENTBUS-29 and INV-EVENTBUS-30 exist, `bound`, each with a genuine asserting test file.
    - `go run ./cmd/inv-render` produces no diff after commit (invariants.md regenerated in the same change).
    - Meta-tests green: no pending-with-asserted_by, no Skip-only bindings, no drift.
    - Any left-pending CLUSTER invariant (e.g. INV-CLUSTER-8) has a filed coverage issue recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The phase's invariant discipline is landed in one consolidated, genuinely-asserted registry change with regenerated docs and filed coverage gaps.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- New invariant entries: `INV-EVENTBUS-29` (external boot fail-closed), `INV-EVENTBUS-30` (DLQ never drops)
- Binding changes: `INV-CLUSTER-1` pending→bound; `INV-CLUSTER-2/-4/-9` gain multi-node asserted_by
- Regenerated `docs/architecture/invariants.md`
- Coverage GitHub issue(s) for any left-pending CLUSTER invariant (e.g. INV-CLUSTER-8)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replica ↔ replica (cross-cluster coordination) | Multi-node invalidation subjects; a rogue/misconfigured member could inject stale rotation pings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Tampering | cross-cluster message injection (wrong cluster_id) | high | mitigate | cluster_id-prefixed subjects; members drop payloads whose cluster_id disagrees (INV-CLUSTER-4), now proven over per-replica conns |
| T-03-14 | Repudiation | fabricated invariant binding hides an unverified guarantee | high | mitigate | D-07 no-fabricated-bindings; annotate only genuinely-asserting specs; meta-tests (TestBoundInvariantsAreGenuinelyAsserted) gate CI; left-pending gaps filed as issues |
| T-03-15 | Denial of Service | a hung replica stalls rotation cluster-wide | medium | mitigate | probe-and-pill proceeds with N-1 (D-08); the pilled member self-terminates (exit 125 → supervisor restart) |
</threat_model>

<verification>
- `task test:int -- ./test/integration/crypto/ ./test/integration/cluster/` green (Docker).
- `go run ./cmd/inv-render` clean (no post-commit diff).
- `task test -- ./test/meta/` green (binding/provenance/genuine-assertion meta-tests).
- Crypto-reviewer gate expected pre-push (crypto invalidation surface touched).
</verification>

<success_criteria>
The crypto key-invalidation coordinator is proven to propagate rotation across real
multi-node replicas including a hung-replica N-1 path (CLUSTER-03, D-08), and the
phase's invariant guarantees are genuinely bound in the registry with no fabrication
(D-07).
</success_criteria>

<output>
Create `.planning/phases/03-platform-hardening-deployment-scaling/03-05-SUMMARY.md` when done.
</output>
