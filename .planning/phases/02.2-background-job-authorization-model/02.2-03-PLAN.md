---
phase: 02.2-background-job-authorization-model
plan: 03
type: execute
wave: 3
depends_on:
  - 02.2-01
  - 02.2-02
files_modified:
  - internal/access/policy/attribute/action_schema.go
  - internal/access/policy/attribute/action_schema_test.go
  - internal/access/setup/setup.go
  - internal/access/setup/setup_test.go
  - .planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md
autonomous: true
requirements:
  - AUTHZ-02

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An exhaustive, reproducible audit of every `action.*` reference reachable from the tree exists as a committed artifact, listing for each key: the referencing `path:line`, whether it is in-tree or test-only, and whether it must be declared."
    - "The audit's grep commands are recorded verbatim and re-running them reproduces the same key set — the audit is a gate that can be re-run, not a snapshot."
    - "The `action` namespace is registered exactly once, declaring exactly five keys: `name`, `dispatch_location`, `job.trigger_event_id`, `job.trigger_event_type`, `job.trigger_subject` — all `AttrTypeString`."
    - "The `action` schema keys carry the dot LITERALLY (`\"job.trigger_subject\"` is a single map key), because `IsRegistered(namespace, key)` is an exact map lookup and `compiler.go:161` composes `ref.namespace + \".\" + ref.key` (research assumption A3)."
    - "The test-only keys `action.event_type` and `action.bogus` are deliberately NOT declared, and the audit says why — declaring `event_type` would declare a key nothing produces, and `bogus` exists to prove the hard-error branch."
    - "Registering `action` changes NO observable behavior on this tree — the audit and the registration are a precondition for plan 04, not a deliverable in themselves (research F1)."
    - statement: "`abac-reviewer` returns READY on the new principal type, its provider, its schema, and its seeds (ROADMAP criterion 5)."
      verification: backstop
  artifacts:
    - .planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md
    - internal/access/policy/attribute/action_schema.go
    - internal/access/setup/setup.go
  key_links:
    - "attribute.ActionNamespaceSchema() -> attribute.Register(schemaReg, \"action\", ...) in BuildABACStack -> SchemaRegistry.Schema() -> (plan 04) policy.NewCompiler"
  prohibitions:
    - "MUST NOT verify this plan by asserting that `action` appears in the schema registry. On this tree that assertion is true and means nothing — the production compiler holds a different, orphaned schema object (research F1). A verification of the form \"the namespace is registered\" is the exact warning sign research Pitfall 1 names."
    - "MUST NOT add `dispatch_location` to `reservedActionKeys` in `internal/access/policy/types/types.go`. The host's own scope fence routes that key through the same `IsReservedActionKey` loop at `internal/plugin/pluginauthz/capability.go:50`, so the denylist entry would break `seed:plugin-world-mutation-own-location` (INV-PLUGIN-50/52) — a shipped-behavior regression."
---

<objective>
Produce the exhaustive `action.*` audit D-62 requires, then register the `action` namespace with
every key that audit found.

Purpose: this is the GATE in front of plan 04. D-61 wires the production compiler to the schema
registry, which turns `validateAttributes`'s `action` hard-error branch from dead code into a
boot-time gate for the whole repo. Any `action.*` key that exists and is not declared becomes a boot
failure. The audit is what makes that flip safe, and D-62 makes the audit's exhaustiveness a
maintainer obligation rather than best-effort.

Output: a committed audit artifact, `attribute.ActionNamespaceSchema()` as the single source of
truth for the key set, and the D-60 registration call in `BuildABACStack`.

(The `<assumption_delta_decision>` block for this phase lives in `02.2-01-PLAN.md`.)
</objective>

<flagged_assumptions>
- **A1 (highest-severity assumption in the research, knowingly accepted by D-62):** no production
  deployment currently has an operator-authored DB policy referencing `action.*`. This is
  UNKNOWABLE from the tree. Task 1 MUST ship the operator pre-flight query as part of the audit
  artifact so a deployment can answer it for itself before upgrading. It remains an assumption, not
  a finding.
</flagged_assumptions>

<artifacts_this_phase_produces>
| Kind | Symbol | File |
|---|---|---|
| doc | `02.2-ACTION-AUDIT.md` | `.planning/phases/02.2-background-job-authorization-model/` |
| func | `attribute.ActionNamespaceSchema() *types.NamespaceSchema` | `internal/access/policy/attribute/action_schema.go` |
| func | `attribute.NewActionOnlySchemaRegistry() *attribute.SchemaRegistry` | `internal/access/policy/attribute/action_schema.go` |
| schema ns | `action` -> 5 keys, all `AttrTypeString` | registered in `BuildABACStack` |
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-background-job-authorization-model/02.2-CONTEXT.md
@.planning/phases/02.2-background-job-authorization-model/02.2-RESEARCH.md
@.planning/phases/02.2-background-job-authorization-model/02.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The exhaustive `action.*` audit (the gate in front of D-61)</name>
  <files>.planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md</files>
  <read_first>
.planning/phases/02.2-background-job-authorization-model/02.2-RESEARCH.md §Q2 "The complete `action.*` key audit" (the 7-row table and the non-Go sweep results — this task re-runs and re-verifies it on the CURRENT tree, it does not copy it)
internal/access/policy/compiler.go lines 120-250 (validateAttributes, collectAttrRefs, collectFromCondition, collectFromExpr — what actually counts as an `action.*` reference)
internal/access/policy/seed.go line 332 and its surrounding comment block (the shipped `action.dispatch_location`)
internal/access/policy/attribute/resolver.go lines 180-200 (the resolver-owned `bags.Action["name"]` writer)
internal/access/policy/types/types.go lines 100-175 (reservedActionKeys, IsReservedActionKey, NewAccessRequest — read this to confirm the prohibition against widening the denylist, do NOT change it)
docs/specs/abac/01-core-types.md lines 415-450 (the `action.name` registration requirement)
.claude/rules/grepping.md and the `dev-flow:grepping` skill contract (durable-gate discipline: prove the grep goes RED, count with `-o | wc -l`, never `-c`)
  </read_first>
  <action>
Re-run the audit on the CURRENT tree and commit the result. Do not copy the research table — the
research is dated 2026-08-09 and its own metadata declares itself invalidated by any change to
`internal/access/setup/setup.go`, `compiler.go`, or `types.go`. Plans 01 and 02 changed `setup.go`.

Run at minimum these searches, and record each command VERBATIM in the artifact next to its result
so the audit is reproducible:

- Go and non-Go references: `rg -n --no-heading 'action\.[a-z_]' --glob '!*.md' --glob '!.planning/**' --glob '!docs/**'`
- Policy text in non-Go sources (plugin manifests, SQL migrations, YAML):
  `rg -n 'permit\(|forbid\(' --glob '!*.go'` then inspect every hit for an `action.` root
- The resolver-owned writer: `rg -n 'bags\.Action\[' internal/access/policy/`
- The caller-attribute producers: `rg -n 'NewAccessRequest\(' --glob '*.go' -g '!*_test.go'`
- Migration `dsl_text` inserts: `rg -n 'dsl_text' internal/store/migrations/`

Write `02.2-ACTION-AUDIT.md` with:

1. **The key table.** One row per distinct `action.<key>`: the key, EVERY referencing `path:line`,
   whether it is production or test-only, and the verdict `DECLARE` / `DO NOT DECLARE` with the
   reason. Expected verdicts, to be CONFIRMED not assumed: `name` DECLARE (resolver-owned writer +
   `docs/specs/abac/01-core-types.md` requires it); `dispatch_location` DECLARE (shipped in
   `seed.go:332`, produced by `internal/plugin/hostcap/interceptor.go`); the three
   `job.trigger_*` keys DECLARE (this phase); `event_type` DO NOT DECLARE (test-only, no production
   producer — `internal/eventbus/authguard/guard.go` passes nil attrs); `bogus` DO NOT DECLARE
   (deliberate negative fixture in `compiler_test.go`). If the current tree yields a key the
   research did not list, that is a REAL finding — add it, declare it, and say so loudly in the
   SUMMARY.

2. **The non-Go sweep**, with its commands and its result (expected: zero `action.*` in plugin
   manifests and migrations — re-verify, do not assert).

3. **The audit's honest limit and the operator pre-flight (D-62 obligation).** State plainly that
   operator-authored rows in the `policies` table compile through the same `Compiler` and cannot be
   enumerated from this repo. Ship the pre-flight query verbatim for an operator to run BEFORE
   upgrading: `SELECT id, name, dsl_text FROM policies WHERE dsl_text LIKE '%action.%';` — with the
   instruction that every returned row's `action.*` keys must appear in the declared set or that
   deployment will fail to boot after plan 04 lands.

4. **The upgrade-risk statement**, in the words the phase SUMMARY and release notes will reuse: a
   deployment carrying an operator-authored `action.*` policy that references an undeclared key
   fails to boot on upgrade, and the remedy is an operator DB edit to that policy row. This risk was
   knowingly accepted by D-62 in preference to a typo'd `action.*` reference silently
   default-denying.

This task changes no Go code. It is a first-class deliverable because D-62 makes it one.
  </action>
  <acceptance_criteria>
- `.planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md` exists and
  contains a table with one row per distinct `action.<key>`, each row carrying at least one
  `path:line` citation.
- Every grep command in the artifact is copy-pasteable and, when re-run from the repo root,
  produces the recorded result.
- The artifact contains the literal SQL pre-flight query and the upgrade-risk paragraph.
- `rg -o 'DECLARE' .planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md | wc -l`
  is >= 7 (5 DECLARE verdicts + 2 DO NOT DECLARE verdicts contain the token).
- The artifact records that `reservedActionKeys` was NOT widened, citing
  `internal/plugin/pluginauthz/capability.go:50` as the reason.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md &amp;&amp; rg -q 'dsl_text LIKE' .planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md &amp;&amp; rg -q 'capability.go:50' .planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md</automated>
  </verify>
  <done>A reproducible, committed audit enumerates every `action.*` key reachable from the tree with a declare/don't-declare verdict per key, states the operator-DB limit with a runnable pre-flight query, and states the upgrade risk D-62 accepted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Declare the `action` namespace from a single source of truth (D-60)</name>
  <files>internal/access/policy/attribute/action_schema.go, internal/access/policy/attribute/action_schema_test.go, internal/access/setup/setup.go, internal/access/setup/setup_test.go</files>
  <precondition>Task 1's audit artifact exists and its DECLARE set is final. The key set written here MUST equal the audit's DECLARE set exactly — if they differ, the audit is wrong or this task is, and the discrepancy is resolved before writing code.</precondition>
  <read_first>
.planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md (Task 1's output — the DECLARE set is this task's input)
internal/access/policy/attribute/schema.go lines 1-100 (the package-level `Register` function, its five validation rules, the receiver-method delegate, `IsRegistered`, `HasNamespace`, and `Schema()` returning the LIVE `*types.AttributeSchema` pointer)
internal/access/policy/compiler.go lines 145-172 (validateAttributes — note `IsRegistered(ns, key)` is an exact map lookup, so the dot must be part of the key)
internal/access/setup/setup.go lines 300-340 (the `// 10.` / `// 10b.` provider registrations and the `warnOnMissingSeedCoverage` call — where the D-60 Register lands)
internal/access/policy/attribute/plugin_provider.go lines 54-62 (the `&types.NamespaceSchema{Attributes: map[string]types.AttrType{...}}` literal shape)
  </read_first>
  <behavior>
RED first — `internal/access/policy/attribute/action_schema_test.go`:

- Test 1: `ActionNamespaceSchema().Attributes` has EXACTLY the five keys `name`,
  `dispatch_location`, `job.trigger_event_id`, `job.trigger_event_type`, `job.trigger_subject` —
  assert set equality against a literal expected set, so adding or dropping a key goes red.
- Test 2: every value is `types.AttrTypeString`.
- Test 3 (assumption A3, the dot-literal contract): register the schema on a fresh
  `NewSchemaRegistry()` and assert `IsRegistered("action", "job.trigger_subject")` is TRUE and
  `IsRegistered("action", "job")` is FALSE. This pins that the dot is part of the KEY, which is the
  only spelling `compiler.go:161`'s `ref.namespace + "." + ref.key` composition can match.
- Test 4: `NewActionOnlySchemaRegistry()` returns a registry where `HasNamespace("action")` is TRUE
  and `HasNamespace("character")` is FALSE — the deliberate asymmetry plan 04 relies on.
- Test 5: calling `attribute.Register(reg, "action", ActionNamespaceSchema())` twice returns an
  error on the second call (`Register` rejects an already-registered namespace) — pinning that the
  registration in `BuildABACStack` happens exactly once.
  </behavior>
  <action>
1. `internal/access/policy/attribute/action_schema.go` — NEW file (SPDX Apache-2.0 header).
   `ActionNamespaceSchema() *types.NamespaceSchema` returns the five-key literal, all
   `types.AttrTypeString`, with a per-key comment naming the key's producer:
   `name` -> resolver-owned writer at `resolver.go:185` and reserved supply-side at
   `types.go:108-110`; `dispatch_location` -> shipped seed `seed.go:332`, produced by
   `internal/plugin/hostcap/interceptor.go:294`, and the note that omitting it would break
   INV-PLUGIN-50's scope fence at boot once plan 04 lands; the three `job.trigger_*` keys -> this
   phase's `world.JobCaller`. Add a file-level comment stating that this function is the SINGLE
   source of truth for the `action` key set, that the audit artifact is its provenance, and that
   adding a key here without adding it to the audit is a drift bug.

   `NewActionOnlySchemaRegistry() *SchemaRegistry` allocates a fresh `NewSchemaRegistry()`,
   registers `action` on it, and returns it — panicking on a `Register` error, because the only way
   it can fail is a programming error in this file. Document what it is FOR: policy-compilation
   sites that have no provider set to draw on (`internal/bootstrap/setup/subsystem.go`'s seed
   installer and the `WithRealABAC` test harness — plan 04's sites 2 and 3), so that the `action`
   hard-error branch behaves IDENTICALLY at every compilation site even though provider-namespace
   warnings do not.

2. `internal/access/setup/setup.go` — add the D-60 registration immediately after the `// 10b.` job
   provider registration and BEFORE `warnOnMissingSeedCoverage`, as a numbered `// 10c.` step:
   `attribute.Register(schemaReg, "action", attribute.ActionNamespaceSchema())` wrapped with
   `eb.Wrapf(err, "register action namespace schema")`. Use the PACKAGE-LEVEL spelling D-60 names,
   not the receiver method.

   The comment on this step MUST state the honest truth (research F1, D-59's ⚠ AMENDED banner): on
   the current tree this registration is a NO-OP, because `BuildABACStack`'s compiler is built on a
   separately-allocated `types.AttributeSchema` that is never populated. It becomes load-bearing
   only when plan 04 wires the compiler to `schemaReg`. Do not write a comment claiming it
   currently protects anything.

   Note the ordering fact that makes this safe to place here: `SchemaRegistry.Schema()` returns the
   LIVE `*types.AttributeSchema` pointer (`schema.go:97-99`), so a compiler later constructed on
   `schemaReg.Schema()` sees this registration regardless of relative construction order — what it
   does NOT see is a compile that RUNS before the registration, which is plan 04's `cache.Reload`
   ordering problem, not this task's.

3. `internal/access/setup/setup_test.go` — assert the registration exists on a stack-equivalent
   path (mirroring whatever seam Task 1 of plan 02 used), and assert that `warnOnMissingSeedCoverage`
   produces NO new `action` warning: `seed_coverage.go:21`'s regex
   `\b(principal|resource)\.([a-z_]+)\.[a-z_]+` never matches an `action.` root, so the direct
   `Register` (which does not touch `RegisteredNamespaces()`) cannot cause a coverage WARN. Assert
   that regex's behavior directly rather than trusting the reasoning.
  </action>
  <acceptance_criteria>
- `task test -- -run 'TestActionNamespaceSchema|TestActionOnlySchemaRegistry' ./internal/access/policy/attribute/` passes with the exact-five-key set-equality assertion.
- `rg -o '"job\.trigger_' internal/access/policy/attribute/action_schema.go | wc -l` is 3.
- `rg -n 'attribute.Register\(schemaReg, "action"' internal/access/setup/setup.go` returns exactly
  1 line, and its line number is greater than the `NewJobProvider` line and less than the
  `warnOnMissingSeedCoverage` line.
- `task test -- ./internal/access/policy/ ./internal/access/setup/` passes — the whole shipped seed
  corpus still compiles and no new boot WARN is produced.
- `git diff --name-only` for this task does NOT include `internal/access/policy/types/types.go`
  (the `reservedActionKeys` prohibition).
  </acceptance_criteria>
  <verify>
    <automated>task test -- ./internal/access/policy/attribute/ ./internal/access/setup/ ./internal/access/policy/</automated>
  </verify>
  <done>The `action` namespace is declared once, from a single source of truth whose key set equals the audit's DECLARE set, registered in `BuildABACStack` at D-60's insertion point — with the registration's current no-op status stated honestly at the call site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-authored DB policy -> `policy.Compiler` | Policy text an operator wrote crosses into compilation at `cache.Reload`. The audit is what bounds what can be there. |
| `ActionNamespaceSchema()` -> every future `action.*` reference | The declared set is the allowlist that plan 04 will enforce at boot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-13 | Denial of Service | `ActionNamespaceSchema()` completeness | high | mitigate | A key omitted from the declared set becomes a boot failure once plan 04 wires the compiler — `cache.Reload` fails, `BuildABACStack` fails, boot fails. Task 1's exhaustive audit is the mitigation; Task 2's set-equality test is the tripwire that catches drift. |
| T-02.2-14 | Denial of Service | operator-authored `action.*` DB policy | high | accept | Knowingly accepted by D-62. An operator policy referencing an undeclared key fails boot on upgrade. Compensating control: the pre-flight SQL query shipped in the audit artifact, plus the release-note callout owned by plan 05. |
| T-02.2-15 | Elevation of Privilege | `reservedActionKeys` widening | high | mitigate | Adding `dispatch_location` to the denylist would make `internal/plugin/pluginauthz/capability.go:50` return `ACCESS_REQUEST_RESERVED_ATTRIBUTE` on every scope-eligible plugin capability call, breaking INV-PLUGIN-50/52's scope fence. Prohibited outright; acceptance criterion asserts `types.go` is untouched. |
| T-02.2-16 | Spoofing | caller self-assertion of a host-vouched key | medium | transfer | A future attrs-carrying caller could assert `action.dispatch_location`. Not reachable today (research Q1 enumerates both producers). Structural fix — a host-vouched attribute channel separate from `Attributes` — is DEFERRED; plan 05 files the GitHub issue referencing research §Q1. This phase's own contribution is that `world.JobCaller` cannot carry arbitrary keys (plan 01). |
</threat_model>

<verification>
- `task test -- ./internal/access/...` green (delegate the full `task test` to `local-check`).
- `task test:int -- ./test/integration/access/...` green.
- Re-running every grep command recorded in the audit artifact reproduces the recorded output.
- `task fmt` run and mutations committed.
</verification>

<success_criteria>
- A reproducible, committed `action.*` audit exists with a declare verdict and `path:line` citations
  per key, plus the operator pre-flight query and the upgrade-risk statement.
- The `action` namespace is registered once with exactly the audited DECLARE set, from one source of
  truth, with the dot carried literally in each key.
- Nothing else changed: `reservedActionKeys` untouched, no new boot WARN, whole seed corpus still
  compiles.
</success_criteria>

<output>
Create `.planning/phases/02.2-background-job-authorization-model/02.2-03-SUMMARY.md` when done.

Record: the final DECLARE set; any key the current tree yielded that the research table did not
list; confirmation that `action.event_type` and `action.bogus` were deliberately not declared and
why; and an explicit statement that this plan's registration is a no-op until plan 04 lands, so no
reader mistakes it for criterion 3 being met.
</output>
