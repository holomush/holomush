---
phase: 02.2-background-job-authorization-model
plan: 04
type: execute
wave: 4
depends_on:
  - 02.2-03
files_modified:
  - internal/access/setup/setup.go
  - internal/access/setup/setup_test.go
  - internal/access/policy/compiler.go
  - internal/access/policy/compiler_test.go
  - internal/access/policy/cache_test.go
  - internal/bootstrap/setup/subsystem.go
  - internal/testsupport/integrationtest/real_abac.go
autonomous: false
requirements:
  - AUTHZ-02

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No production policy-compilation path builds its `Compiler` on a freshly-allocated, never-populated `types.AttributeSchema`: all three sites named by research finding F1 compile against a schema that has the `action` namespace registered."
    - "A seed policy referencing an undeclared `action.*` key FAILS boot — it does not warn, and it does not silently default-deny."
    - "An operator-authored DB policy row referencing an undeclared `action.*` key ALSO fails boot: the hard-error branch is fatal for ALL policy sources, not seed-source only (D-62)."
    - "The boot failure names the offending POLICY (name and DB id) and the offending KEY, so an operator can fix it without reading Go source."
    - "In `BuildABACStack` the initial `cache.Reload` runs AFTER all provider registrations and after the `action` registration, so boot-time compilation and every subsequent poller/invalidation reload validate against the SAME populated schema."
    - "Every shipped seed still compiles after the wiring — the whole corpus, not a sample."
    - "The `action` hard-error branch behaves IDENTICALLY at all three compilation sites, because all three carry the `action` namespace; only provider-namespace WARNINGS differ, and that asymmetry is documented at the two sites that have no provider set."
  artifacts:
    - internal/access/setup/setup.go
    - internal/access/policy/compiler.go
    - internal/bootstrap/setup/subsystem.go
    - internal/testsupport/integrationtest/real_abac.go
  key_links:
    - "attribute.SchemaRegistry.Schema() -> policy.NewCompiler -> policy.Cache.Reload -> validateAttributes action branch -> BuildABACStack error -> boot failure"
    - "attribute.NewActionOnlySchemaRegistry() -> policy.NewCompiler -> policy.Bootstrap (seed installer + WithRealABAC harness)"
    - "cache.go:141-158 `%q (id=%s)` policy naming + compiler.go key naming = the operator-actionable message"
  prohibitions:
    - "MUST NOT soften the `action` hard-error branch to a WARN, a log-and-skip, or a source-conditional (seed-only) fatal. D-62 rejected the fatal-for-seed-only alternative explicitly; a typo'd `action.*` reference must never silently default-deny anywhere. If a deployment breaks on upgrade, the remedy is the operator DB edit named in the audit artifact — not a weakened gate."
    - "MUST NOT gate the wiring behind an environment variable, config flag, or build tag as a hedge. A validation gate that can be turned off is a validation gate that will be off in the deployment that needed it."
---

<objective>
Wire the production policy compiler to the schema registry the providers populate (research finding
F1, D-61), in the correct order relative to `cache.Reload`, and make the `action` hard-error branch
fatal for every policy source with a message an operator can act on (D-62).

Purpose: this is the only way ROADMAP success criterion 3 can be honestly met. Until it lands,
`validateAttributes` is dead code, plan 03's `action` registration is a complete no-op, and a
typo'd `action.*` reference silently default-denies forever. It is also the phase's riskiest change:
it converts a dead validation path into a live boot-time gate for the whole repo.

Output: three wired compilation sites, an operator-actionable boot failure, and tests proving BOTH
the seed-source and the DB-row-source branches are fatal.

(The `<assumption_delta_decision>` block for this phase lives in `02.2-01-PLAN.md`.)
</objective>

<flagged_assumptions>
- **A1, restated at the point of no return:** whether any production deployment carries an
  operator-authored `action.*` DB policy is unknowable from this tree. D-62 knowingly accepts that
  such a deployment fails to boot on upgrade. The checkpoint in Task 1 is where a human confirms
  that acceptance with the audit in hand.
- **A4 — RESOLVED, do not re-litigate.** The pattern mapper traced the `cache.Reload` reorder for
  site 1: `ps.SetOnMutate` (`setup.go:380-390`), the `healthTracker` closure (`:360-376`), and the
  poller (`:393-401`) are ALL constructed after the provider block, and nothing between `:155` and
  `:323` reads `cache`. The reorder is safe. A4's "3 one-line wirings" half is WRONG for sites 2
  and 3 — those are handled by Task 3 and are not one-liners.
</flagged_assumptions>

<artifacts_this_phase_produces>
| Kind | Symbol | File |
|---|---|---|
| error code | `POLICY_UNREGISTERED_ACTION_ATTRIBUTE` (oops code on the `action` hard-error branch) | `internal/access/policy/compiler.go` |
| behavior | `BuildABACStack` step reorder: schemaReg/resolver -> providers -> action Register -> compiler -> cache -> Reload | `internal/access/setup/setup.go` |
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-background-job-authorization-model/02.2-CONTEXT.md
@.planning/phases/02.2-background-job-authorization-model/02.2-RESEARCH.md
@.planning/phases/02.2-background-job-authorization-model/02.2-PATTERNS.md
@.planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md
@.planning/phases/02.2-background-job-authorization-model/02.2-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way flip — D-61 wiring and D-62 fatal-for-all-sources</name>
  <decision>Proceed to wire the production compiler to the schema registry (D-61) and make the `action` hard-error branch fatal for ALL policy sources including operator-authored DB rows (D-62).</decision>
  <context>
Both decisions are rated **one-way in practice** by the research and by CONTEXT.md:

- **D-61** — once strict validation is live, any policy authored while it is on may reference keys
  that only validate in that mode. Turning it back off later does not un-author those policies.
- **D-62** — the branch is fatal for in-tree seeds AND operator-authored DB rows. This knowingly
  accepts research assumption **A1** ("no production deployment currently has an operator-authored
  DB policy referencing `action.*`"), which is UNKNOWABLE from the tree and is the
  highest-severity assumption in the research document. If A1 is wrong, that deployment **fails to
  boot on upgrade**, and the remedy is an operator DB edit.

This checkpoint exists because the gate in front of it is now complete: plan 03 shipped the
exhaustive `action.*` audit at
`.planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md`, including the
operator pre-flight query `SELECT id, name, dsl_text FROM policies WHERE dsl_text LIKE '%action.%';`

**Before approving, read the audit artifact and confirm its DECLARE set is complete.** After this
point the phase stops being additive.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed — wire all three sites, fatal for all sources</name>
      <pros>ROADMAP criterion 3 becomes honestly true; a typo'd `action.*` reference can never silently default-deny; the whole repo gains a live boot-time validation gate. Matches D-61 + D-62 exactly.</pros>
      <cons>A deployment carrying an undeclared operator `action.*` policy fails to boot on upgrade. Not reversible in the sense that matters (policies authored under strict mode).</cons>
    </option>
    <option id="pause">
      <name>Pause — the audit revealed a key or a deployment concern not accounted for</name>
      <pros>Avoids shipping a boot-time gate on top of an incomplete allowlist.</pros>
      <cons>Criterion 3 stays unmet and the phase must reword it; plan 03's registration stays a permanent no-op.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, or pause (and state what the audit revealed)</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Site 1 — reorder `BuildABACStack` and wire the compiler to the populated registry</name>
  <reversibility rating="one-way">Once strict `action` validation is live at boot, policies authored while it is on may reference keys that validate only in that mode; unregistering later does not un-author them (D-61). Gated by Task 1's checkpoint.</reversibility>
  <files>internal/access/setup/setup.go, internal/access/policy/compiler.go, internal/access/policy/compiler_test.go, internal/access/setup/setup_test.go</files>
  <precondition>Task 1's checkpoint was approved with `proceed`, and `.planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md` exists with a final DECLARE set.</precondition>
  <read_first>
internal/access/setup/setup.go lines 138-175 (steps 1-7 in their CURRENT order: the orphan `schema := types.NewAttributeSchema()` at :150, `compiler` at :151, `cache` at :154, `cache.Reload` at :155-157, then `schemaReg` at :160 and `resolver` at :161) and lines 300-433 (the provider tail, the `action` Register plan 03 added, the seed-coverage sweep, the healthTracker closure at :360-376, `ps.SetOnMutate` at :380-390, the poller at :393-401, and the returned ABACStack literal)
internal/access/policy/compiler.go lines 120-180 (NewCompiler, Compile, validateAttributes and its `action` hard-error branch — note it is a bare `fmt.Errorf`, inconsistent with the repo's `oops` convention)
internal/access/policy/cache.go lines 125-165 (Reload's `%q (id=%s)` compile-error wrapping — the EXISTING operator-actionable policy-naming half — and its doc comment on all-or-nothing / last-good-snapshot semantics)
internal/access/policy/attribute/schema.go lines 90-100 (`Schema()` returns the LIVE `*types.AttributeSchema` pointer)
internal/access/policy/compiler_test.go lines 160-185 and 360-395 (the existing `action.bogus` and `resource.bogus` negative fixtures — these assert the current error and MUST be updated in the same change)
.planning/phases/02.2-background-job-authorization-model/02.2-PATTERNS.md §"D-61 — the three compiler↔schema wiring sites" (the A4 trace result and the verbatim surrounding code)
.claude/rules/logging.md (context-carrying slog variants are mandatory)
  </read_first>
  <behavior>
RED first:

- Test 1 (`compiler_test.go`): a compiler built on a registry carrying the `action` namespace
  rejects `permit(principal, action, resource) when { action.bogus == "x" };` with an error whose
  oops code is `POLICY_UNREGISTERED_ACTION_ATTRIBUTE` and whose message contains the literal
  `action.bogus`. Assert the TOP-LEVEL code via `oops.AsOops(err).Code()`.
- Test 2 (`compiler_test.go`): the SAME compiler ACCEPTS every key in the declared set — one
  subtest per key, driven from `attribute.ActionNamespaceSchema()`'s key set so a future key
  addition is covered automatically.
- Test 3 (`compiler_test.go`): a compiler built on an EMPTY schema still skips `action` entirely
  (`HasNamespace` false), preserving the pre-existing behavior for any caller that has no registry.
- Test 4 (`setup_test.go`, the criterion-3 proof): a `Cache.Reload` over a store containing one
  policy with an undeclared `action.*` key FAILS, and the returned error text contains BOTH the
  policy name and the offending key — assert both substrings in one assertion so a message that
  loses either goes red.
- Test 5 (`setup_test.go`, the regression proof): the WHOLE shipped seed corpus
  (`policy.SeedPolicies()`) compiles clean against a compiler built on a registry carrying the
  `action` namespace plus every production provider's schema. Iterate the corpus — do not sample.
- Test 6 (ordering, the Pitfall-2 proof): assert structurally that in `BuildABACStack` the
  `cache.Reload` call site appears AFTER the `action` registration call site. A source-order
  assertion is legitimate here because the hazard IS source order and no runtime seam exposes it;
  implement it as a `go/ast` or line-number check in `internal/access/setup/setup_test.go` (or
  `test/meta/`) that reads the file and compares positions, not as a comment.
  </behavior>
  <action>
Two coordinated changes.

1. `internal/access/policy/compiler.go` — upgrade the `action` hard-error branch from
   `fmt.Errorf(...)` to `oops.Code("POLICY_UNREGISTERED_ACTION_ATTRIBUTE").With("attribute", fqn).Errorf(...)`,
   keeping the existing message text (`unregistered action attribute %q: action namespace is
   registered but attribute is not`) so the key is still named in the message as well as in the
   oops context. Do NOT change the branch's CONDITION, do NOT add a policy-source parameter, and do
   NOT add an escape hatch — D-62 makes it fatal for every source, and the source-conditional
   variant was considered and rejected. Add a doc-comment sentence on `validateAttributes` recording
   that the `action` branch is deliberately fatal for all sources and pointing at the audit artifact
   for the declared set.

   The existing `resource.bogus` negative fixture at `compiler_test.go:385` exercises the
   NON-action WARN path and must stay a warning, not an error — confirm it still passes unchanged.

2. `internal/access/setup/setup.go` — perform the three-way reorder inside `BuildABACStack`. The
   target order, replacing the current steps 2-7:

   - allocate `schemaReg := attribute.NewSchemaRegistry()` and `resolver := attribute.NewResolver(schemaReg)`
   - register all providers (the existing `// 8.` … `// 10b.` block, unmoved relative to each other)
   - the `// 10c.` `attribute.Register(schemaReg, "action", attribute.ActionNamespaceSchema())` step
   - `compiler := policy.NewCompiler(schemaReg.Schema())` — DELETE the orphan
     `schema := types.NewAttributeSchema()` line entirely; leaving it as an unused variable or a
     second schema object is the defect this task exists to remove
   - `cache := policy.NewCache(ps, compiler)` and then `cache.Reload(ctx)`

   Everything from `warnOnMissingSeedCoverage` onward stays where it is. Renumber the step comments
   so the numbering still reads in execution order, and add a comment at the `cache.Reload` site
   stating WHY it moved: compiling the boot snapshot before the providers register would validate
   against an empty registry while every subsequent poller/invalidation reload validated against a
   populated one — different validation behavior at boot than at steady state, which is worse than
   the uniform no-op it replaces (research Pitfall 2).

   Verify against the A4 trace before committing: nothing between the old `:155` and `:323` reads
   `cache`; `healthTracker` (`:360-376`), `ps.SetOnMutate` (`:380-390`), and the poller (`:393-401`)
   are all constructed after the whole provider block. If the current file no longer matches that
   trace, STOP and re-trace — do not assume.

   Also update `BuildABACStack`'s doc comment: it currently claims to wire components "in the
   correct dependency order" and names the order; that sentence is now wrong and must describe the
   new order.

Commit this task separately from Task 3. This is D-61's first-class, separately-committed half.
  </action>
  <acceptance_criteria>
- Comment-immune orphan-schema gate:
  `rg -v '^\s*//' internal/access/setup/setup.go | rg -o 'types\.NewAttributeSchema\(\)' | wc -l`
  reports 0.
- `rg -v '^\s*//' internal/access/setup/setup.go | rg -c 'policy.NewCompiler'` is exactly 1, and
  that call's argument is `schemaReg.Schema()`.
- The line number of `cache.Reload(` in `internal/access/setup/setup.go` is GREATER than the line
  number of `attribute.Register(schemaReg, "action"` — asserted by Test 6, not by inspection alone.
- `task test -- -run 'TestValidateAttributes|TestCompile' ./internal/access/policy/` passes,
  including the unchanged `resource.bogus` WARN fixture.
- `task test -- ./internal/access/policy/ ./internal/access/setup/` passes; the whole shipped seed
  corpus compiles clean (Test 5).
- `task test:int -- ./test/integration/access/... ./test/integration/world/...` passes — the real
  stack still boots.
- Comment-immune escape-hatch gate:
  `rg -v '^\s*//' internal/access/policy/compiler.go | rg -o 'PolicySourceSeed' | wc -l` reports 0 —
  no source-conditional escape was introduced in code. (Prose about the rejected alternative in a
  doc comment is expected and is why the grep strips comments.)
  </acceptance_criteria>
  <verify>
    <automated>task test -- ./internal/access/policy/ ./internal/access/setup/ ./internal/access/policy/attribute/</automated>
  </verify>
  <done>`BuildABACStack` compiles every stored policy — seeds and operator-authored DB rows alike — against the schema its providers populate, with the initial reload running after registration, and an undeclared `action.*` key fails boot with a message naming both the policy and the key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sites 2 and 3 — the seed installer and the `WithRealABAC` harness</name>
  <reversibility rating="one-way">Same flip as Task 2, applied to the seed-installation path: a seed referencing an undeclared `action.*` key now fails `policy.Bootstrap` at install time, not just at reload.</reversibility>
  <files>internal/bootstrap/setup/subsystem.go, internal/testsupport/integrationtest/real_abac.go, internal/access/policy/cache_test.go, internal/access/setup/setup_test.go</files>
  <precondition>Task 2 has landed and `attribute.NewActionOnlySchemaRegistry()` exists from plan 03. Verify with `rg -n 'func NewActionOnlySchemaRegistry' internal/access/policy/attribute/action_schema.go`.</precondition>
  <read_first>
internal/bootstrap/setup/subsystem.go lines 120-135 (`DependsOn()` — it names `lifecycle.SubsystemABAC`) and lines 186-200 (`policyBootstrapFn`, the closure with the orphan schema at :193)
internal/access/setup/subsystem.go lines 190-270 (the accessor family, every one panicking before `Prepare()` — this is what tells you whether the ABAC stack exists when `policyBootstrapFn` runs)
internal/testsupport/integrationtest/real_abac.go (full — note the comment at :40-42 explaining that seeding runs BEFORE `abacSub.Prepare/Activate`, because the subsystem's Start reads the store)
internal/access/policy/attribute/action_schema.go (as landed by plan 03 — `NewActionOnlySchemaRegistry` and its stated purpose)
.planning/phases/02.2-background-job-authorization-model/02.2-PATTERNS.md §"No Analog Found" (why these two sites have NO in-tree analog, and why A4's "one-line wiring" is wrong for them)
  </read_first>
  <behavior>
RED first:

- Test 1 (`cache_test.go` or `setup_test.go`): `policy.Bootstrap` given a seed set containing one
  policy with an undeclared `action.*` key, compiled with
  `policy.NewCompiler(attribute.NewActionOnlySchemaRegistry().Schema())`, FAILS with the
  `POLICY_UNREGISTERED_ACTION_ATTRIBUTE` code and names the offending key.
- Test 2 (D-62's DB-row half — the branch Task 2's seed tests do not cover): a policy row that
  ORIGINATED as an operator-authored DB row (not a `SeedPolicy`) and references an undeclared
  `action.*` key ALSO fails, with the same code. Drive it through the same path the production
  reload uses (`Cache.Reload` over a store containing that row), and assert the error names both the
  policy and the key. This is the assertion that proves the branch is fatal for ALL sources, which
  is the whole content of D-62.
- Test 3: an action-only registry ACCEPTS a policy referencing `resource.character.anything` —
  `HasNamespace("character")` is false there, so provider-namespace refs are skipped, not warned and
  not rejected. This pins the documented asymmetry: fatal `action` behavior is uniform across
  sites; WARN behavior is not.
  </behavior>
  <action>
These two sites are NOT one-line wirings — `02.2-PATTERNS.md` establishes there is no in-tree
example of a populated schema registry reaching a `policy.Bootstrap` call, and at both sites the
seed bootstrap runs before (or independently of) any provider registration.

The resolution is deliberately NOT a new accessor threaded through
`s.cfg.ABAC` / `abacSub`. Both sites need only the `action` namespace to make D-62's gate uniform,
and `action` is a caller-supplied bag whose declaration is static — it depends on no repo, no pool,
and no provider. So both sites construct their own action-only registry from plan 03's single source
of truth.

1. `internal/bootstrap/setup/subsystem.go` — inside `policyBootstrapFn`, replace
   `schema := types.NewAttributeSchema()` / `compiler := policy.NewCompiler(schema)` with
   `compiler := policy.NewCompiler(attribute.NewActionOnlySchemaRegistry().Schema())`. Add a comment
   stating: (a) that this closure compiles the SEED corpus, so the `action` gate must be live here
   or a bad seed installs and only fails later at `cache.Reload`; (b) that provider namespaces are
   deliberately absent here, so `resource.<ns>.*` refs are skipped rather than warned — the FATAL
   behavior is uniform across sites, the WARNING behavior is not, and that is intentional;
   (c) why the alternative (an `ABACSubsystem.SchemaRegistry()` accessor) was NOT taken: it would
   couple seed installation to provider-registration ordering across two subsystems for a namespace
   that needs neither. Remove the now-unused `types` import if nothing else in the file uses it.

2. `internal/testsupport/integrationtest/real_abac.go` — replace
   `policy.NewCompiler(policytypes.NewAttributeSchema())` with
   `policy.NewCompiler(attribute.NewActionOnlySchemaRegistry().Schema())`, and extend the existing
   comment at `:40-42` to say that the harness seeds BEFORE `abacSub.Prepare/Activate`, so no
   populated provider registry exists at this point by construction, and the action-only registry is
   what keeps the harness's validation behavior identical to production's for `action`. Drop the
   `policytypes` import if it becomes unused.

3. Confirm — do not assume — that `internal/bootstrap/setup/subsystem.go`'s `DependsOn()` naming
   `lifecycle.SubsystemABAC` means the ABAC stack is already built when `policyBootstrapFn` runs
   (`s.cfg.ABAC.PolicyStore()` panics before `Prepare()`, and this closure already calls it). Record
   the finding in the SUMMARY either way: if the ABAC stack IS built by then, note that a future
   change could switch this site to the real registry; if it is not, note that the action-only
   registry is the only correct option here.
  </action>
  <acceptance_criteria>
- Comment-immune orphan-schema gate at both sites: for each of
  `internal/bootstrap/setup/subsystem.go` and `internal/testsupport/integrationtest/real_abac.go`,
  `rg -v '^\s*//' <file> | rg -o 'NewAttributeSchema\(\)' | wc -l` reports 0.
- `rg -v '^\s*//' <file> | rg -c 'NewActionOnlySchemaRegistry'` is exactly 1 in each of those two
  files.
- `rg -n --no-heading 'policy.NewCompiler\(' --glob '*.go' -g '!*_test.go'` returns ONLY lines whose
  argument is `schemaReg.Schema()` or `NewActionOnlySchemaRegistry().Schema()` — no remaining
  orphaned-schema production site anywhere in the tree.
- `task test -- ./internal/access/policy/ ./internal/bootstrap/...` passes, including Test 2's
  DB-row-source assertion.
- `task test:int` passes in full (this task changes the shared integration harness, and `task test`
  does NOT compile `//go:build integration` files) — delegate to the `local-check` agent.
  </acceptance_criteria>
  <verify>
    <automated>task test -- ./internal/access/policy/ ./internal/access/setup/ ./internal/bootstrap/...</automated>
  </verify>
  <done>All three F1 sites compile against a schema carrying the `action` namespace; a seed AND an operator-authored DB row referencing an undeclared `action.*` key both fail with the same operator-actionable error; the site-to-site asymmetry in WARNING behavior is documented at the two sites that have it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-authored `policies` row -> `Cache.Reload` -> boot | The single highest-consequence path this plan changes: policy text an operator wrote can now stop the server from starting. |
| seed corpus -> `policy.Bootstrap` | In-tree policy text crosses into installation; a bad seed now fails install rather than installing and silently default-denying. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-17 | Denial of Service | `cache.Reload` in `BuildABACStack` | high | mitigate | A policy that fails compilation now fails `BuildABACStack` and therefore fails boot. Mitigations: plan 03's exhaustive audit bounds the in-tree surface; the operator pre-flight SQL query bounds the DB surface; the error names the policy and the key so the remedy is a single DB edit; plan 05 owns the release-note callout. Residual risk knowingly accepted by D-62. |
| T-02.2-18 | Denial of Service | boot/steady-state validation divergence | high | mitigate | Wiring the compiler without moving `cache.Reload` would validate the boot snapshot against an empty registry and every later reload against a populated one — a bad policy would pass boot and kill the first poller reload. Fixed by the three-way reorder; pinned by Task 2 Test 6's source-order assertion. |
| T-02.2-19 | Tampering | the `action` hard-error branch | high | mitigate | Softening the branch under upgrade pressure (WARN for DB rows, an env-var kill switch) would restore silent default-deny for typo'd keys — the exact failure D-62 exists to prevent. Prohibited outright; acceptance criterion greps for a `PolicySourceSeed` conditional. |
| T-02.2-20 | Information Disclosure | the boot failure message | medium | mitigate | The message names a policy NAME, a policy ID, and an attribute KEY — deliberately, per D-62's operator-actionability obligation. It does NOT include the full DSL text or any subject/resource data. Confirm no `%v` of an inner error leaks beyond the compile error itself (`.claude/rules/grpc-errors.md`). |
| T-02.2-21 | Denial of Service | `policy.Bootstrap` at seed install | medium | mitigate | A malformed seed now blocks bootstrap. Acceptable and intended — it is in-tree code caught by `task test` before it can reach a deployment; Task 2 Test 5 compiles the whole corpus on every run. |
</threat_model>

<verification>
- `task test -- ./internal/access/... ./internal/bootstrap/...` green after each task.
- Full `task test` and full `task test:int` green before the plan is considered complete — this plan
  changes the shared integration harness and `task test` does NOT compile
  `//go:build integration` files. Delegate both to the `local-check` agent; do not run them inline.
- Boot the server locally (or run `task test:int -- ./test/integration/access/...`) and confirm it
  starts — the whole point of this plan is that a broken policy now stops boot, so a green unit
  suite is not sufficient evidence that boot works.
- `task fmt` run and mutations committed.
</verification>

<success_criteria>
- No production `policy.NewCompiler` call anywhere in the tree receives a freshly-allocated,
  never-populated schema.
- A seed AND an operator-authored DB row referencing an undeclared `action.*` key both fail boot,
  with an error naming the policy and the key.
- The initial `cache.Reload` runs after registration, so boot and steady-state validate identically.
- The entire shipped seed corpus still compiles.
</success_criteria>

<output>
Create `.planning/phases/02.2-background-job-authorization-model/02.2-04-SUMMARY.md` when done.

Record: the final `BuildABACStack` step order; the exact boot-failure message shape an operator will
see, quoted; the checkpoint decision and who approved it; the DependsOn finding from Task 3 step 3;
and — prominently — the D-62 upgrade risk in the words plan 05 will lift into the release notes.
</output>
