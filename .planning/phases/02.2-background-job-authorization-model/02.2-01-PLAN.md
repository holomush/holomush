---
phase: 02.2-background-job-authorization-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/access/prefix.go
  - internal/access/prefix_test.go
  - internal/jobs/registry.go
  - internal/jobs/registry_test.go
  - internal/access/policy/attribute/job_provider.go
  - internal/access/policy/attribute/job_provider_test.go
  - internal/world/caller.go
  - internal/world/caller_test.go
  - internal/access/policy/seed.go
  - internal/access/policy/seed_smoke_test.go
autonomous: true
requirements:
  - AUTHZ-02

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A background job authorizes a world write under the subject `job:<name>` — never a human subject and never the S1 system bypass; the subject string is produced only by access.JobSubject."
    - "A job that IS in the liveness registry, HAS declared `character` in its capability class, and carries provenance naming aggregate X is PERMITTED to write character X — proven by a PASSING PERMIT, not only a passing deny."
    - "The same job carrying provenance naming aggregate X is DENIED writing character Y (Y != X)."
    - "A job name absent from the liveness registry resolves to NO `job.*` attributes at all (nil, nil — no sentinel value), so every attribute-conditioned permit fails to match and the write is denied."
    - "A job whose declared capability class omits `character` is DENIED writing a character even when its provenance matches the resource."
    - "`principal is job` matches no `system`, `character`, or `plugin` subject, and `principal is system` / `principal is character` / `principal is plugin` match no `job:` subject."
    - "`world.JobCaller` and `world.ScheduledJobCaller` can emit only keys under the `job.` prefix; neither accepts a caller-supplied attribute map, so no caller can assert `action.dispatch_location` or any other host-vouched key through the job door."
    - "`trigger_subject` carries the BARE aggregate ULID (byte-identical to `bags.Resource[\"id\"]`), not a dotted NATS subject — recorded verbatim in the SUMMARY because Phase 3 plan 03-04 copies the spelling byte-for-byte."
  artifacts:
    - internal/access/prefix.go
    - internal/jobs/registry.go
    - internal/access/policy/attribute/job_provider.go
    - internal/world/caller.go
    - internal/access/policy/seed.go
  key_links:
    - "access.JobSubject(name) -> world.JobCaller -> Caller.subject -> checkAccess -> types.NewAccessRequest -> engine bags.Subject[\"id\"]"
    - "jobs.Registry -> attribute.JobRegistry -> JobProvider.ResolveSubject -> mergeAttributes -> bags.Subject[\"job.name\"|\"job.writes\"]"
    - "world.Provenance -> Caller.attrs -> types.NewAccessRequest 4th param -> engine.go:252-265 overlay -> bags.Action[\"job.*\"]"
    - "seed:job-fixture-instance-scoped `when` clause binds action.job.trigger_subject == resource.id"
  prohibitions:
    - "MUST NOT claim — in code comments, docs, the SUMMARY, or the invariant registry — that this model defends against malicious in-process code, or against a job that calls Engine.Evaluate directly with arbitrary arguments. The honest claim is scope drift plus handler bugs (D-53)."
    - "MUST NOT expose any API that lets a caller supply arbitrary `action.*` keys: no `map[string]any` parameter, no variadic attribute option, no exported setter or exported field on world.Caller, and no test-only helper promoted to production."
    - "MUST NOT ship an authorization grant for a real Phase-3 consumer (`job:retirement`, `job:activity-flush`) under cover of the fixture. The fixture job name and seed name must be unmistakably fixture-scoped, and the SUMMARY must state that no real-consumer grant shipped."
---

<objective>
Land the `job:` principal as a production-quality vertical slice, end to end: subject constructor ->
liveness registry -> attribute provider -> `world.JobCaller` -> `checkAccess` -> seed -> a PASSING
PERMIT and a paired DENY through a real policy engine.

Purpose: this is the tracer. Every later plan in the phase expands out from a proven path rather
than stacking unverified layers. It also settles the single most load-bearing open question in the
phase — the `trigger_subject` normalization (research assumption A6) — with a passing permit, which
Phase 3 plan 03-04 copies byte-for-byte.

Output: `access.SubjectJob` / `access.JobSubject`, package `internal/jobs`,
`attribute.JobProvider`, `world.JobCaller` / `world.ScheduledJobCaller` / `world.Provenance`, one
fixture seed, and the paired permit/deny proofs.
</objective>

<assumption_delta_decision>
The deterministic detector fired `kind: pluralization` on this phase. The genuine delta: the
**principal-kind axis** is pluralizing — world writes came from `HumanCaller` / `SystemCaller`, and
this phase adds a third variant with its own attribute namespace.

The promote **already happened** in Phase 02.1: `world.Caller` is the generalized primary and D-57's
typed constructors make invalid combinations unrepresentable. So this phase lands a **variant on an
already-promoted primary**.

- **Primary noun:** `world.Caller` (generalized in 02.1).
- **Decision:** `promote` — already effected upstream; 02.2 adds the `job` variant to it, and does
  NOT add a parallel job-only path alongside.
- **Consequence to hold:** if any part of your design reaches authorization for a job WITHOUT going
  through `world.Caller`, that is the add-alongside anti-pattern re-appearing — flag it rather than
  shipping it.
</assumption_delta_decision>

<flagged_assumptions>
Surfaced, never silently dropped.

- **Edge probe (deterministic) returned exactly 1 item, `unclassified`, status `unresolved`:**
  `{"requirement_id":"AUTHZ-02","category":"unclassified","status":"unresolved","verification":null,"probe":"unclassified — review manually"}`.
  Per the spec-less fallback protocol an `unclassified` row stays `unresolved` and is NOT
  auto-resolved with a backstop. It is carried here as a flagged assumption: AUTHZ-02's edge
  surface was authored goal-backward into `must_haves.truths` above by hand, not derived by the
  probe. A human MUST confirm at `/gsd-verify-work` that no edge of AUTHZ-02 is missing from those
  truths.
- **A6 (load-bearing):** `trigger_subject` normalizes to the bare aggregate ULID. If Task 1's
  PERMIT does not pass, A6 is wrong and the whole `action.job.trigger_subject == resource.id`
  binding is void — halt and escalate rather than switching to a deny-only proof.
- **A5:** a `job:` principal needs no new `lifecycle.SubsystemID`. This plan holds A5 by
  constructing `jobs.Registry` as a dependency-free value injected through config (plan 02), not as
  a lifecycle subsystem. If an executor finds itself adding a `SubsystemID`, STOP — that is a
  measured 15-site cascade across 6 files including the pinned 18-element topological start order
  at `cmd/holomush/core_topo_order_test.go:194-213`.
- **A7:** `TestKnownPrefixes_AllConstantsCovered` requires `SubjectJob` in `knownPrefixes`.
  Confirmed by the pattern mapper (the test's only exclusion is `SubjectSystem`, excluded for
  lacking a `:`); Task 3 pins it.
</flagged_assumptions>

<artifacts_this_phase_produces>
Symbols created by THIS PLAN (source-grounding passes must exclude these from drift verification —
they do not exist on the tree before this plan):

| Kind | Symbol | File |
|---|---|---|
| const | `access.SubjectJob = "job:"` | `internal/access/prefix.go` |
| func | `access.JobSubject(name string) string` | `internal/access/prefix.go` |
| package | `internal/jobs` | `internal/jobs/registry.go` |
| type | `jobs.Registry` | `internal/jobs/registry.go` |
| func | `jobs.NewRegistry() *jobs.Registry` | `internal/jobs/registry.go` |
| method | `(*jobs.Registry).Register(name string, writes []string) error` | `internal/jobs/registry.go` |
| method | `(*jobs.Registry).Unregister(name string)` | `internal/jobs/registry.go` |
| method | `(*jobs.Registry).IsJobRunning(name string) bool` | `internal/jobs/registry.go` |
| method | `(*jobs.Registry).DeclaredWrites(name string) ([]string, bool)` | `internal/jobs/registry.go` |
| iface | `attribute.JobRegistry` | `internal/access/policy/attribute/job_provider.go` |
| type | `attribute.JobProvider` | `internal/access/policy/attribute/job_provider.go` |
| func | `attribute.NewJobProvider(r JobRegistry) *JobProvider` | `internal/access/policy/attribute/job_provider.go` |
| type | `world.Provenance` | `internal/world/caller.go` |
| func | `world.JobCaller(name string, prov Provenance) Caller` | `internal/world/caller.go` |
| func | `world.ScheduledJobCaller(name string) Caller` | `internal/world/caller.go` |
| field | `world.Caller.principalKind` (unexported) | `internal/world/caller.go` |
| schema ns | `job` -> `{name: AttrTypeString, writes: AttrTypeStringList}` | `job_provider.go` `Schema()` |
| seed | `seed:job-fixture-instance-scoped` | `internal/access/policy/seed.go` |
| fixture vocab | job name `fixture`; event type `fixture_triggered` | `seed.go`, tests |
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-background-job-authorization-model/02.2-CONTEXT.md
@.planning/phases/02.2-background-job-authorization-model/02.2-RESEARCH.md
@.planning/phases/02.2-background-job-authorization-model/02.2-PATTERNS.md
@internal/world/caller.go
@internal/access/policy/attribute/plugin_provider.go
</context>

<interface_context>
Signatures the executor MUST match exactly (read the file, do not guess):

- `attribute.AttributeProvider` — 4 methods: `Namespace() string`,
  `ResolveSubject(ctx, entityRef string) (map[string]any, error)`,
  `ResolveResource(ctx, entityRef string) (map[string]any, error)`,
  `Schema() *types.NamespaceSchema`. Template: `plugin_provider.go:36-62`.
- `resolveEntity` (`attribute/resolver.go:350-395`) passes the **FULL ref** (`"character:01ABC"`)
  to **EVERY** registered provider. `mergeAttributes` (`resolver.go:489-504`) writes
  `bag["<namespace>."+key]` only when `IsRegistered(namespace, key)`.
- `bags.Subject["id"]` / `bags.Resource["id"]` are the substring AFTER the first `:`
  (`resolver.go:195-198`, `:206-208`) — stamped provider-independently.
- `types.NewAccessRequest(subject, action, resource, attrs)` rejects the reserved key `"name"`
  (`types/types.go:143-173`).
- `Caller` fields are unexported; `checkAccess` reads `caller.subject`, `caller.attrs`,
  `caller.evalContext(ctx)` (`internal/world/service.go:209-262`).
- DSL list method spelling: `<root>.<path>.containsAll([<string literals>])` — literal needles only
  (`internal/access/policy/dsl/ast.go:167-179`; `seed.go:641-642`; shipped precedent
  `plugins/core-communication/plugin.yaml:234`).
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a live job writes the aggregate its event names" — one path only</name>
  <reversibility rating="costly">The `job:` namespace and the `action.job.*` key spelling appear in seed DSL text, the schema registry, and every future job ref; renaming later is a policy migration, not an edit (D-48). The fixture seed also lands in the shipped corpus.</reversibility>
  <files>internal/access/prefix.go, internal/jobs/registry.go, internal/access/policy/attribute/job_provider.go, internal/world/caller.go, internal/access/policy/seed.go, internal/world/caller_test.go</files>
  <read_first>
internal/world/caller.go (full — the struct doc comment at :35-38 literally reserves this phase's vocabulary)
internal/world/caller_test.go lines 250-330 (the 02.1 `// Verifies: INV-WORLD-8` paired permit/deny against a REAL engine — this is the closest analog and the shape to copy)
internal/world/export_test.go lines 1-60 (SubjectForTest / AttrsForTest / EvalContextForTest / NewCallerWithAttrsForTest already exist; its doc comment about "before Phase 02.2 defines the action vocabulary" goes stale on landing)
internal/access/policy/attribute/plugin_provider.go (full, 63 lines — the provider template)
internal/access/policy/attribute/resolver.go lines 175-215 and 346-400 and 485-510 (bag stamping, full-ref dispatch to every provider, mergeAttributes schema fence)
internal/access/prefix.go lines 1-135 (constants, knownPrefixes, the 4-for-4 panic-on-empty constructor convention)
internal/access/policy/seed.go lines 315-340 (the shipped cross-bag precedent seed:plugin-world-mutation-own-location and its comment density)
internal/world/service.go lines 195-265 (checkAccess: where caller.subject and caller.attrs are consumed)
.claude/rules/abac-providers.md (omit-don't-sentinel; the has_X witness convention)
  </read_first>
  <behavior>
RED first — write this end-to-end test in `internal/world/caller_test.go` (external package
`world_test`, alongside the existing `// Verifies: INV-WORLD-8` pair) and watch it FAIL against the
production `world.Service`, not against a fake:

- Test 1 (THE PERMIT — load-bearing, A6): build a real `policy.Engine` over a resolver carrying the
  real `attribute.NewJobProvider(reg)` where `reg` is a real `jobs.Registry` with
  `Register("fixture", []string{"character"})` already called, plus a character resource provider.
  Compile the fixture seed's DSL text. Invoke a real `world.Service` character-write command with
  `world.JobCaller("fixture", world.Provenance{EventID: <ulid>, EventType: "fixture_triggered",
  Subject: charID.String()})` against resource `character:<charID>`. Assert the command does NOT
  return a permission-denied error — i.e. `checkAccess` PERMITTED. This is the single most
  load-bearing assertion in the phase: it proves `action.job.trigger_subject == resource.id`
  actually MATCHES with the bare-ULID normalization.
- Test 2 (THE PAIRED DENY): identical setup, but the provenance names a DIFFERENT character ULID
  than the resource. Assert the command returns `ErrPermissionDenied`. The mismatch MUST be
  introduced by changing ONLY the provenance value, and the resource ULID MUST be derived from the
  same variable the PERMIT case used — the deny must not be a second hand-written literal pair
  (research Pitfall 9).
- Test 3: `world.JobCaller("fixture", prov)` produces `SubjectForTest() == "job:fixture"` and
  `AttrsForTest()` whose key set is EXACTLY `{"job.trigger_event_id", "job.trigger_event_type",
  "job.trigger_subject"}` — assert set equality, not containment.
- Test 4: `world.ScheduledJobCaller("fixture")` produces `SubjectForTest() == "job:fixture"` and
  `AttrsForTest()` that is nil or empty — and specifically contains NO key whose value is the empty
  string (the fail-open sentinel forbidden by `.claude/rules/abac-providers.md`).
- Test 5: neither job constructor sets `IsSystemForTest()`, and `EvalContextForTest(ctx)` returns a
  context for which `access.IsSystemContext` is false.
  </behavior>
  <action>
Wire ONE entry point through every layer, production quality, no other call sites.

1. `internal/access/prefix.go` — add `SubjectJob = "job:"` to the subject-prefix const block, add
   `JobSubject(name string) string` following the 4-for-4 panic-on-empty convention verbatim (panic
   message `"access.JobSubject: empty name would bypass access control"`), and append `SubjectJob`
   to `knownPrefixes`.

2. `internal/jobs/registry.go` — NEW package `jobs` (SPDX Apache-2.0 header). `Registry` is a
   mutex-guarded `map[string][]string` (job name -> declared write kinds).
   `NewRegistry() *Registry`; `Register(name string, writes []string) error` (rejects an empty name
   and an empty writes list with an `oops.Code("JOB_REGISTRATION_INVALID")` error, defensively
   copies the slice); `Unregister(name string)`; `IsJobRunning(name string) bool`;
   `DeclaredWrites(name string) ([]string, bool)` (returns a defensive copy). The registry has ZERO
   dependencies — that is deliberate and is what lets plan 02 inject it through config instead of
   minting a `lifecycle.SubsystemID` (assumption A5).

3. `internal/access/policy/attribute/job_provider.go` — NEW. Declare the narrow registry interface
   in this file, mirroring `PluginRegistry`: `JobRegistry` with `IsJobRunning(name string) bool` and
   `DeclaredWrites(name string) ([]string, bool)`. `JobProvider` holds a `JobRegistry`;
   `NewJobProvider(r JobRegistry) *JobProvider`. `Namespace()` returns `"job"`.
   `ResolveSubject(ctx, subjectID)` MUST, in this order: return `(nil, nil)` when `subjectID` is
   empty; return `(nil, nil)` unless `strings.HasPrefix(subjectID, access.SubjectJob)` — this
   explicit prefix guard is REQUIRED because `resolveEntity` hands the full ref to every provider
   (research Pitfall 4); strip the prefix to get the bare name; return `(nil, nil)` when the
   registry is nil or `IsJobRunning(bare)` is false. On success return `map[string]any{"name":
   bare}` — the BARE name, NOT the prefixed ref, so `principal.job.name == "fixture"` reads
   naturally and equals `bags.Subject["id"]`. Do NOT copy `PluginProvider`'s prefixed-name stamping.
   `ResolveResource` returns `(nil, nil)` unconditionally. `Schema()` returns
   `&types.NamespaceSchema{Attributes: map[string]types.AttrType{"name": types.AttrTypeString}}` —
   plan task 2 adds `writes`. Import cycle check: `attribute` may import `internal/access` for the
   `SubjectJob` constant only if no cycle results; if a cycle exists, declare an unexported
   `jobSubjectPrefix = "job:"` constant in this file and add a same-package test asserting it equals
   `access.SubjectJob`'s literal value.

4. `internal/world/caller.go` — add an unexported `principalKind` string type with
   `principalKindJob principalKind = "JOB"`, and an unexported `principalKind` field on `Caller`
   (zero value = the existing human/system behavior, unchanged). Add
   `Provenance struct { EventID, EventType, Subject string }` with a doc comment stating that
   `Subject` is the BARE aggregate ULID, byte-comparable to `bags.Resource["id"]`, and NOT a dotted
   NATS subject (A6). Add `JobCaller(name string, prov Provenance) Caller`: it calls
   `access.JobSubject(name)` (inheriting the panic-on-empty — state that deviation from
   `HumanCaller` explicitly in the doc comment, mirroring how `HumanCaller` documents its own
   deviation at `caller.go:58-71`), sets `principalKind: principalKindJob`, and builds `attrs`
   INTERNALLY as exactly the three keys `"job.trigger_event_id"`, `"job.trigger_event_type"`,
   `"job.trigger_subject"`. It MUST NOT accept a `map[string]any` parameter, a variadic option, or
   any other channel for caller-chosen keys (D-56 rationale; research Q1 scope verdict). Omit any
   provenance field that is empty rather than writing an empty string — omit-don't-sentinel
   (`.claude/rules/abac-providers.md`). Add `ScheduledJobCaller(name string) Caller`: same subject
   and `principalKind`, `attrs` left nil, with a doc comment stating plainly that a timer-driven
   job's authority is identity plus declared capability class only and is NOT instance-scoped
   (D-63). Update the `Caller` struct doc comment at `caller.go:35-38` and
   `export_test.go`'s `NewCallerWithAttrsForTest` doc comment, both of which say the vocabulary is
   "Phase 02.2's" / does not exist "before Phase 02.2" — those sentences go stale on this landing.

5. `internal/access/policy/seed.go` — add ONE seed to the returned corpus, placed after the plugin
   host-capability block with a comment block matching the density of its neighbours (state: what
   the seed proves, that `fixture` is a FIXTURE job with no production consumer, that D-52 defers
   real-consumer grants to Phase 3, and that a bare `permit(principal is job, ...)` would be a
   blanket grant to every present and future job because target matching is prefix-only):
   `Name: "seed:job-fixture-instance-scoped"`, `SeedVersion: 1`, and DSLText exactly
   `permit(principal is job, action in ["write"], resource is character) when { principal.job.name == "fixture" && action.job.trigger_event_type == "fixture_triggered" && action.job.trigger_subject == resource.id };`
   Task 2 adds the `principal.job.writes.containsAll(["character"])` conjunct.

Commit this task separately. It is the tracer: the phase's architecture is either proven here or it
is wrong here.
  </action>
  <acceptance_criteria>
- `task test -- -run 'TestJobCaller' ./internal/world/` passes, and the PERMIT subtest is present
  and green (a green run with only deny subtests is a FAILURE of this task).
- `rg -c 'job\.trigger_subject' internal/world/caller.go` >= 1.
- Signature gate (comment-immune — strip comments first):
  `rg -v '^\s*//' internal/world/caller.go | rg -o '^func (JobCaller|ScheduledJobCaller)\([^)]*\)' | rg -c 'map\[string\]any'`
  must report 0 matches — neither job constructor takes an attribute map.
- `rg -v '^\s*//' internal/access/policy/attribute/job_provider.go | rg -c 'HasPrefix'` is >= 1
  (the Pitfall-4 foreign-ref guard, in code rather than in a comment).
- `task test -- ./internal/access/policy/` passes — the whole shipped seed corpus, including the new
  fixture seed, compiles (`createSeedEngine`/`seed_validation_test.go` are the backstop).
- No empty-string sentinel is assigned to a map value in the provider:
  `rg -v '^\s*//' internal/access/policy/attribute/job_provider.go | rg -o ':\s*""' | wc -l`
  reports 0.
- The SUMMARY records the `trigger_subject` normalization VERBATIM as the bare aggregate ULID,
  with the fixture's exact DSL text quoted, because Phase 3 plan 03-04 copies it byte-for-byte.
  </acceptance_criteria>
  <verify>
    <automated>task test -- -run 'TestJobCaller' ./internal/world/ &amp;&amp; task test -- ./internal/access/policy/ ./internal/access/policy/attribute/ ./internal/jobs/</automated>
  </verify>
  <done>A live, registered fixture job is PERMITTED to write the character its provenance names and DENIED writing a different character, proven end-to-end through the production `world.Service` and a real policy engine, and the slice is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Declared capability class — the second gate the seed must actually read</name>
  <files>internal/jobs/registry.go, internal/jobs/registry_test.go, internal/access/policy/attribute/job_provider.go, internal/access/policy/attribute/job_provider_test.go, internal/access/policy/seed.go, internal/access/policy/seed_smoke_test.go</files>
  <read_first>
internal/access/policy/seed_smoke_test.go lines 15-55 (createSeedEngine + the mockAttributeProvider factory shape) and lines 950-1005 (the DENY-only precedent this task must exceed)
internal/access/policy/attribute/job_provider.go (as landed by Task 1)
internal/jobs/registry.go (as landed by Task 1)
internal/access/policy/dsl/ast.go lines 165-182 (ContainsCondition — root + path + `.containsAll(` list `)`; literal needles only)
internal/access/policy/seed.go lines 636-648 (the in-tree note that containsAll/containsAny take literal needles only)
internal/access/policy/attribute/resolver.go lines 485-510 (mergeAttributes drops any key absent from Schema(), with a WARN and a rejectedAttributesCounter increment — no error)
  </read_first>
  <behavior>
RED first — add these to `internal/access/policy/seed_smoke_test.go` and
`internal/access/policy/attribute/job_provider_test.go` and watch them FAIL:

- Test 1 (seed tier, PERMIT control): a `jobProvider(...)` mock returning
  `{"name": "fixture", "writes": []string{"character"}}` plus a character resource provider; a
  request with subject `access.JobSubject("fixture")`, action `"write"`, resource
  `"character:"+charID`, and `Attributes` carrying the three `job.*` keys with
  `job.trigger_subject == charID`. Assert `decision.IsAllowed()` is TRUE.
- Test 2 (AUTHZ-02 (e), the capability-class DENY): identical, except `writes` is
  `[]string{"location"}` — `character` undeclared. Assert `decision.IsAllowed()` is FALSE.
- Test 3 (provider unit): `JobProvider.ResolveSubject` for a registered job returns a map whose key
  set is EXACTLY `{"name", "writes"}`, with `writes` a `[]string`.
- Test 4 (schema-fence unit): `JobProvider.Schema().Attributes["writes"]` is
  `types.AttrTypeStringList` — asserted directly, because declaring it `AttrTypeString` still
  registers successfully and then misbehaves silently under `containsAll` (research Pitfall 5).
- Test 5 (registry unit): `DeclaredWrites` returns `(nil, false)` for an unregistered name;
  returns a copy such that mutating the returned slice does not change the registry's stored value.
  </behavior>
  <action>
Three coordinated changes.

1. `internal/access/policy/attribute/job_provider.go` — on the success path of `ResolveSubject`,
   look up `p.registry.DeclaredWrites(bare)`. When the lookup returns `ok == true` and a non-empty
   slice, add `"writes": <the slice>` to the returned map. When it returns `ok == false` or an
   empty slice, OMIT the `writes` key entirely — do NOT emit `[]string{}` or `nil` as a value
   (`.claude/rules/abac-providers.md`: omit, never sentinel; an empty list would satisfy
   `containsAll` semantics ambiguously). Extend `Schema()` to declare
   `"writes": types.AttrTypeStringList` alongside `"name"`. Per the `has_X` witness convention in
   the same rule, also emit `"has_writes": true` on the path that emits `writes` and
   `"has_writes": false` on the path that omits it, and declare `"has_writes":
   types.AttrTypeBool` — the witness is emitted on EVERY code path, only the value key is omitted.

2. `internal/jobs/registry.go` — no signature change; confirm `DeclaredWrites` returns a defensive
   copy and `(nil, false)` for an unknown name.

3. `internal/access/policy/seed.go` — extend `seed:job-fixture-instance-scoped`'s `when` clause with
   the conjunct `principal.job.writes.containsAll(["character"])`, so the final DSLText is exactly
   `permit(principal is job, action in ["write"], resource is character) when { principal.job.name == "fixture" && principal.job.writes.containsAll(["character"]) && action.job.trigger_event_type == "fixture_triggered" && action.job.trigger_subject == resource.id };`
   Add a sentence to the seed's comment block recording D-51 plainly: the declaration NARROWS and
   the seed GRANTS — both gates must pass, and the declaration alone authorizes and denies nothing
   unless a seed reads it (research Pitfall 6). Bump `SeedVersion` only if the corpus convention
   requires it for an edit within the same unreleased phase; otherwise leave it at 1 and say why in
   the commit message.

Add the `jobProvider(subjectAttrs map[string]any) *mockAttributeProvider` factory to
`seed_smoke_test.go` mirroring `characterProvider`/`locationProvider` at `:35-52`, with
`namespace: "job"` and a schema declaring `name`/`writes`/`has_writes`.

If the compound `when` clause combining `containsAll(...)` with `&&`-chained comparisons does not
parse, that is a REAL finding, not a licence to simplify: report it and stop. Do not drop the
capability-class conjunct to make the seed compile — that would make D-50/D-51 decorative and
AUTHZ-02's second clause unprovable.
  </action>
  <acceptance_criteria>
- `task test -- -run 'TestSeedSmokeJob' ./internal/access/policy/` passes with BOTH a
  `decision.IsAllowed() == true` subtest and a `decision.IsAllowed() == false` subtest present.
- `rg -o 'containsAll' internal/access/policy/seed.go | wc -l` is >= 1.
- `task test -- -run 'TestJobProvider' ./internal/access/policy/attribute/` passes, including the
  assertion that `Schema().Attributes["writes"] == types.AttrTypeStringList`.
- `task test -- -run 'TestSeedSmokePluginWorldMutationOwnLocation' ./internal/access/policy/`
  still passes — the pre-existing plugin scope fence is untouched (Q1 tripwire).
  </acceptance_criteria>
  <verify>
    <automated>task test -- ./internal/access/policy/ ./internal/access/policy/attribute/ ./internal/jobs/</automated>
  </verify>
  <done>A job that did not declare `character` in its capability class is DENIED writing a character, and a job that did declare it is PERMITTED — both proven at the seed-smoke tier against the whole shipped corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fences — liveness gate, foreign-ref isolation, namespace disjointness, prefix coverage</name>
  <files>internal/access/prefix_test.go, internal/access/policy/attribute/job_provider_test.go, internal/access/policy/seed_smoke_test.go</files>
  <read_first>
internal/access/prefix_test.go lines 495-560 (`TestKVResourcePanicsOnEmptyNamespace` panic-test shape; `TestKnownPrefixes_AllConstantsCovered` table and its SubjectSystem exclusion comment)
internal/access/policy/attribute/plugin_provider_test.go (full — the provider test shape)
internal/access/policy/engine.go lines 85-110 (the S1 gate: EXACT string equality on "system") and lines 538-552 (parseEntityType — prefix-only matching, the D-48 rationale)
internal/access/policy/seed.go lines 200-220 (the only two `principal is system` permits)
  </read_first>
  <behavior>
RED first — these assert fences that Task 1 and Task 2 built but nothing yet proves:

- Test 1 (AUTHZ-02 (a), liveness): `JobProvider.ResolveSubject(ctx, "job:fixture")` returns
  `(nil, nil)` when the registry reports the job is not running. Assert BOTH returns are nil — a
  non-nil empty map would still be stamped and is not the same thing.
- Test 2 (liveness, nil registry): a `JobProvider` constructed with a nil registry returns
  `(nil, nil)` for a well-formed `job:` ref.
- Test 3 (Pitfall 4, foreign refs): `ResolveSubject` returns `(nil, nil)` for
  `"character:01ABC"`, `"plugin:builder-bot"`, `"player:01ABC"`, and the bare `"system"` — and the
  fake registry records ZERO `IsJobRunning` calls for those refs (assert the call count, so the
  test proves the PREFIX GUARD rather than the registry's answer).
- Test 4 (AUTHZ-02 (c), disjointness, seed tier): with the job provider registered, a subject
  `access.CharacterSubject(<ulid>)` carrying identical `action.job.*` attributes is DENIED by
  `seed:job-fixture-instance-scoped`; and a subject `access.JobSubject("fixture")` is not granted by
  either shipped `principal is system` permit (`seed.go:206-217`) — assert a DENY for a `job:`
  subject on a `location` write, which those two system permits would otherwise cover.
- Test 5 (A7): `TestKnownPrefixes_AllConstantsCovered` gains a `{name: "subject job prefix",
  constant: access.SubjectJob, desc: "SubjectJob"}` row and still passes.
- Test 6: `assert.PanicsWithValue(t, "access.JobSubject: empty name would bypass access control",
  func() { access.JobSubject("") })`.
  </behavior>
  <action>
Test-only task; it adds no production behavior and MUST NOT change any production file. Its purpose
is that every fail-closed property Tasks 1 and 2 rely on has an assertion that can go red.

Add a fake `JobRegistry` to `job_provider_test.go` that records the names it was asked about, so
Test 3 can assert the prefix guard short-circuits BEFORE the registry lookup. Mirror the fake shape
already used in `plugin_provider_test.go`.

For Test 4, reuse `createSeedEngine` with the `jobProvider(...)` factory added in Task 2 plus a
character provider and a location provider, so the disjointness assertions run against the WHOLE
shipped seed corpus rather than an isolated policy — a corpus-level DENY is the claim D-48 actually
makes.

Name the tests so the `-run` filters in `02.2-VALIDATION.md` are real: `TestJobProvider*` in
`internal/access/policy/attribute/`, `TestSeedSmokeJob*Disjoint*` in `internal/access/policy/`.
  </action>
  <acceptance_criteria>
- `task test -- -run 'TestJobProvider' ./internal/access/policy/attribute/` passes and covers the
  not-running, nil-registry, and foreign-ref cases.
- `task test -- -run 'TestSeedSmokeJob.*Disjoint' ./internal/access/policy/` passes (a non-empty
  test set — an empty `-run` match is a false green; assert the run reports at least 1 test).
- `task test -- -run 'TestKnownPrefixes_AllConstantsCovered' ./internal/access/` passes with the new
  `SubjectJob` row present.
- `git diff --name-only` for this task lists ONLY `_test.go` files.
  </acceptance_criteria>
  <verify>
    <automated>task test -- ./internal/access/ ./internal/access/policy/ ./internal/access/policy/attribute/</automated>
  </verify>
  <done>Every fail-closed property of the `job:` principal — liveness gating, foreign-ref isolation, cross-namespace disjointness, and the prefix-constant coverage contract — has an assertion that goes red when broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job consumer boundary -> handler | Provenance is stamped from the delivered message BEFORE handler logic; the handler independently derives the resource. Neither side can see or alter the other's value (D-55). |
| `world.Caller` -> `types.NewAccessRequest` | The only channel by which a caller value influences `bags.Action`. Unexported field, no exported setter, constructor-built keys only. |
| host process -> policy engine | NOT a trust boundary. Nothing in-process can stop Go from calling `Evaluate` with arbitrary arguments (D-53). This model does not claim otherwise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-01 | Elevation of Privilege | `seed:job-fixture-instance-scoped` target clause | high | mitigate | `parseEntityType` matches the prefix only, so a bare `permit(principal is job, ...)` grants EVERY present and future job. Task 1's seed carries a mandatory `when` clause binding `principal.job.name`; Task 2 adds the capability-class conjunct. `abac-reviewer` gates `seed.go` before push. |
| T-02.2-02 | Elevation of Privilege | `attribute.JobProvider.ResolveSubject` | high | mitigate | Stale/dead job retaining authority. Liveness gate returns `(nil, nil)` for an unregistered job; missing attributes are `false` for every operator (ADR `holomush-iv43`). Proven by Task 3 Test 1/2. |
| T-02.2-03 | Spoofing | `attribute.JobProvider.ResolveSubject` | high | mitigate | `resolveEntity` hands the FULL ref to every provider, so a guard-less job provider could stamp `job.*` keys onto a character subject's bag. Explicit `strings.HasPrefix` guard; Task 3 Test 3 asserts the registry is never consulted for a foreign ref. |
| T-02.2-04 | Spoofing / EoP | `world.JobCaller` signature | high | mitigate | A `map[string]any` parameter would hand every caller the ability to assert `action.dispatch_location` — the host-vouched key behind INV-PLUGIN-50 — through a brand-new door. Typed `Provenance` only; no variadic; no exported setter. Pinned by Task 1 Test 3's exact key-set equality. |
| T-02.2-05 | Elevation of Privilege | provider attribute emission | high | mitigate | An empty-string sentinel for an unresolved optional attribute is fail-OPEN in this DSL (it matches any other unresolved peer). Omit-don't-sentinel per `.claude/rules/abac-providers.md`; `has_writes` witness on every path; acceptance criterion greps for `: ""`. |
| T-02.2-06 | Tampering | handler resource derivation | medium | mitigate | A handler bug deriving the wrong aggregate. Provenance comes from the boundary, the resource from the handler; the `when` clause binds them. Task 1 Test 2's paired deny, with the mismatch introduced by changing ONLY the provenance. |
| T-02.2-07 | Repudiation | `seed:job-fixture-instance-scoped` in the shipped corpus | medium | accept | The fixture seed ships in the production corpus. Accepted because no job named `fixture` is ever registered in production, so the liveness gate returns `(nil, nil)` and the permit cannot match. The SUMMARY must state this and state that NO real-consumer grant shipped. |
| T-02.2-08 | Information Disclosure | in-process job code | low | accept | A malicious in-process job can call `Evaluate` directly with any arguments. Explicitly out of scope (D-53); no control is claimed. |
</threat_model>

<verification>
- `task test -- ./internal/access/... ./internal/world/ ./internal/jobs/` green after every task.
- `task test` (full unit) green before the plan is considered complete — delegate to the
  `local-check` agent per `CLAUDE.md`; do NOT run it inline in the parent.
- `task test:int -- ./test/integration/access/...` green (this plan touches `seed.go`).
- `task fmt` run and its mutations committed (SPDX headers on the two new files; aligned `const`
  blocks in `prefix.go` and `caller.go` are edited, which is the exact shape that passes `task
  build` but fails `task fmt:check` in CI).
</verification>

<success_criteria>
- A live fixture job is PERMITTED to write the character its provenance names (passing permit) and
  DENIED writing a different character (paired deny), end-to-end through `world.Service`.
- A job absent from the liveness registry, and a job whose capability class omits `character`, are
  both DENIED.
- `principal is job` is provably disjoint from `system`, `character`, and `plugin`.
- The `trigger_subject` normalization is settled as the bare aggregate ULID and recorded verbatim
  in the SUMMARY for Phase 3 plan 03-04 to copy.
</success_criteria>

<output>
Create `.planning/phases/02.2-background-job-authorization-model/02.2-01-SUMMARY.md` when done.

The SUMMARY MUST record, verbatim: (a) the fixture seed's full DSL text; (b) the exact
`world.JobCaller` and `world.Provenance` signatures; (c) that `trigger_subject` is the BARE
aggregate ULID; (d) that ONLY the fixture grant shipped — `job:retirement` and `job:activity-flush`
did NOT ship and are Phase 3's to add (this is what resolves `03-04-PLAN.md:141`'s read-only
precondition); (e) the `jobs.Registry` API Phase 3 subsystems will call.
</output>
