---
status: testing
phase: 02.2-background-job-authorization-model
source: [02.2-VERIFICATION.md]
started: 2026-08-09T18:46:23Z
updated: 2026-08-09T18:46:23Z
---

## Current Test

number: 2
name: Confirm research assumption A1 against every deployed HoloMUSH database
expected: |
  The D-67 pre-flight query returns no row referencing an `action.<key>` outside
  the declared set {name, dispatch_location, job.trigger_event_id,
  job.trigger_event_type, job.trigger_subject}.

      SELECT id, name, source, dsl_text
      FROM access_policies
      WHERE enabled = true AND dsl_text LIKE '%action.%';

  Run this against every deployed database BEFORE this change ships. A row
  outside the declared set is a hard boot failure on upgrade, with no runtime
  override — by design (D-67). The remedy is either to edit the policy row so it
  stops referencing the key, or to add the key to
  `attribute.ActionNamespaceSchema()` and rebuild.
awaiting: user response

## Tests

### 1. `abac-reviewer` returns READY on the new principal type, provider, schema, and seeds

expected: READY.
result: pass
source: automated
evidence: |
  ROADMAP criterion 5. The first run returned NOT READY with three blocking
  findings: the D-29 fence admitted the new `resource is character` permit on a
  justification no test enforced (and its comment falsely claimed it did); the
  now-fatal `action` gate was not applied at the plugin-policy install path, so a
  plugin manifest could persist a row that bricked every later cache reload; and
  the compiler aliased a mutable, unsynchronized `types.AttributeSchema`.

  All three were fixed (b05024c3d, 526b035f5, 178d72448; recorded in
  02.2-REVIEW-FIXES.md), plus the non-blocking WR-01 pre-flight query correction
  (35f44c542). The re-review returned **VERDICT: READY** with all three marked
  CLOSED and nothing newly introduced at blocking severity.

  The re-review verified rather than accepted each fix: it censused all four
  store-writing entry points on `PolicyInstaller` and confirmed no fifth door;
  confirmed the validated DSL is byte-identical to what `ReplaceBySource`
  persists (no trust-escalating rewrite between validation and storage); proved
  the rejection tests are not vacuous by checking that the fake store populates
  both asserted fields and that a success-path control exists; verified the
  "no new oops code" reasoning against `oops@v1.22.0/error.go:233-250` rather
  than against the comment claiming it; and confirmed by direct census that
  `AttributeSchema.namespaces` is touched in exactly seven places, six of them
  the lock-taking accessors.

  Four non-blocking Low findings remain and are filed, not fixed: the lock gives
  memory safety but not atomic check-then-act in `SchemaRegistry.Register`;
  `attribute.Resolver`'s own maps remain unsynchronized (pre-existing, not
  introduced by this phase); three action keys produced at
  `internal/eventbus/authguard/guard.go:134` are undeclared; and
  `reservedActionKeys` remains a one-entry denylist rather than an allowlist.

### 2. Confirm research assumption A1 against every deployed HoloMUSH database

expected: |
  No row returned by the D-67 pre-flight query references an `action.*` key
  outside the declared set.
result: [pending]
why_human: |
  Plan 04 Task 1 was a `checkpoint:decision` with `gate: blocking`. It was
  AUTO-RESOLVED to `proceed` under `workflow.auto_advance: true` (recorded
  verbatim in 02.2-04-SUMMARY.md:169-178), which accepted assumption A1 — that
  no operator-authored `action.*` DB policy outside the declared set exists in
  any deployed database.

  Nothing in this repository can see a deployed operator's database, so no test
  and no reviewer can discharge this. Everything that WAS verifiable from the
  tree has been: the audit's DECLARE set was re-swept, `action.dispatch_location`
  is declared, and the whole shipped seed corpus compiles under the live gate
  (`TestEveryShippedSeedCompilesUnderTheLiveActionGate`). Plugin-installed rows
  — a third policy source D-67 never enumerated — are now caught at plugin load
  rather than at the next cache reload.

  This is the one item that requires a human with access to the deployments.

## Summary

total: 2
passed: 1
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
