---
phase: 08-god-object-decomposition
plan: 07
type: execute
wave: 5
depends_on: ["08-05"]
files_modified:
  - internal/grpc/server.go
  - internal/grpc/query_handler.go
  - internal/grpc/query_handler_test.go
  - internal/grpc/query_stream_history.go
  - internal/grpc/list_focus_presence.go
  - internal/grpc/list_session_streams.go
  - internal/grpc/list_available_commands.go
  - internal/grpc/refresh_connection.go
  - internal/grpc/session_identity.go
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "QueryHandler is constructible from package grpc_test with only its own collaborators — no *CoreServer, no harness, no integration build tag. Per D-16: this test asserts the new seam’s structure and unit-testability, not behavior the existing integration and whole-system suites already pin."
    - "The nil semantics of every migrated collaborator survive verbatim: nil accessEngine DENIES public stream reads (fail-closed), nil commandQuerier fails closed with PERMISSION_DENIED, nil historyReader returns INTERNAL, nil identityRegistry falls back to ULID-string form."
    - "focusCoordinator, streamContributor and identityRegistry are passed as the SAME values into QueryHandler that 08-03 passed into SubscribeHandler; neither unit copies, wraps, or re-derives a shared collaborator."
    - "All five migrated RPC methods keep their pre-split exported signatures on *CoreServer and delegate; CoreServer still implements corev1.CoreServiceServer with an unchanged exported method set. Per D-01: this RPC group is split into a collaborator-scoped unit behind a thin facade, continuing the auth_handlers.go precedent."
    - "After this plan CoreServer's struct holds the four sub-unit pointers plus only the fields NewCoreServer needs to build their Deps; every remaining field is justified in the SUMMARY."
    - "task test:int is green and git diff --stat origin/main...HEAD -- test/integration/ shows no assertion edit."
  artifacts:
    - internal/grpc/query_handler.go
    - internal/grpc/query_handler_test.go
  key_links:
    - "CoreServer.{GetCommandHistory,QueryStreamHistory,ListFocusPresence,ListSessionStreams,ListAvailableCommands,RefreshConnection} -> QueryHandler: six facade delegations keeping the proto contract fixed (D-03)."
    - "QueryHandler -> auth.ValidateSessionOwnership: called at list_focus_presence.go:47 and refresh_connection.go:24 today; stays a free-function call on the moved paths."
    - "QueryHandler + SubscribeHandler -> the same focusCoordinator / streamContributor / identityRegistry values: the last of the shared-collaborator resolutions."
  prohibitions:
    - statement: "QueryHandler MUST NOT hold a *CoreServer field, accept one as a constructor parameter, or reach a collaborator through one (D-02)."
    - statement: "A nil-collaborator fail direction MUST NOT be flipped or 'cleaned up' during migration. accessEngine nil DENIES and commandQuerier nil DENIES; turning either into a permissive default would silently open ABAC-gated reads to everyone, and no test that only exercises the wired path would catch it."
    - statement: "Assertions under test/integration/** MUST NOT be edited to make the extraction pass (D-15)."
---

<objective>
Extract the current-state query cluster into a `QueryHandler` and finish the ARCH-01 facade
reduction. This is the last of the four CoreServer clusters.

Methods: `GetCommandHistory`, `QueryStreamHistory`, `ListFocusPresence`, `ListSessionStreams`,
`ListAvailableCommands`, `RefreshConnection`, `buildCharacterIdentity`.

Five of the seven already live in their own one-method files — this plan gives them their own
*dependencies*, which is the part the earlier file-splitting never did. That distinction is the
whole point of D-02.

Purpose: ARCH-01 / success criterion 1, completing the CoreServer decomposition.

Output: `internal/grpc/query_handler.go` + its proof test, and a `CoreServer` reduced to a facade
over four sub-units.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-god-object-decomposition/08-CONTEXT.md
@.planning/phases/08-god-object-decomposition/08-PATTERNS.md
@.planning/phases/08-god-object-decomposition/08-05-SUMMARY.md
</context>

<planner_settled_decisions>
**This plan implements D-01's current-state query group** — the last of the four RPC groups D-01
names, completing the extraction 08-03 and 08-05 carried. Test scope follows D-16: the new tests
assert this seam's structure and unit-testability, not behavior the integration suite already pins.

**This cluster carries the phase's densest set of fail-closed defaults.** `accessEngine` nil denies
(`server.go:191-193`), `commandQuerier` nil denies with `PERMISSION_DENIED`, `historyReader` nil
returns `INTERNAL`. All three are recorded *only* in struct-field doc comments. Migrating those
comments with their fields is not documentation hygiene here — it is the sole written record of
which way each collaborator fails, and the prohibition above exists because a "cleanup" that
inverts one would be invisible to any test that exercises only the wired path.

**`buildCharacterIdentity` moves with the cluster.** It touches `bindings` and `cryptoActive`
(`session_identity.go:31`) and is consumed by the query paths; it is not a separate concern.
</planner_settled_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create QueryHandler and move the seven query methods</name>
  <files>internal/grpc/query_handler.go, internal/grpc/query_handler_test.go, internal/grpc/query_stream_history.go, internal/grpc/list_focus_presence.go, internal/grpc/list_session_streams.go, internal/grpc/list_available_commands.go, internal/grpc/refresh_connection.go, internal/grpc/session_identity.go</files>
  <read_first>
    - internal/grpc/server.go (lines 154-232 — the CoreServer struct; the accessEngine comment at :191-193, identityRegistry at :186-189, commandQuerier and its PERMISSION_DENIED note, historyReader and its INTERNAL-when-nil note, bindings and cryptoActive)
    - internal/grpc/server.go (lines 1811-1891 — GetCommandHistory at :1811)
    - internal/grpc/query_stream_history.go (the whole file — QueryStreamHistory at :49; note eventbusEventToEventFrame and the oops-code opacity conventions)
    - internal/grpc/list_focus_presence.go (the whole file — ListFocusPresence at :28 and its auth.ValidateSessionOwnership call at :47)
    - internal/grpc/list_session_streams.go (the whole file — ListSessionStreams at :30)
    - internal/grpc/list_available_commands.go (the whole file — ListAvailableCommands at :21)
    - internal/grpc/refresh_connection.go (the whole 46-line file — RefreshConnection at :19, the preamble at :24, and the line-scoped nolint at :43)
    - internal/grpc/session_identity.go (the whole file — buildCharacterIdentity at :31)
    - internal/grpc/subscribe_handler.go and internal/grpc/command_handler.go (from 08-03/08-05 — the established Deps-struct + constructor shape; follow it)
    - .claude/rules/grpc-errors.md (the never-leak-inner-errors and translate-at-one-layer rules that the moved bodies already satisfy and must continue to)
  </read_first>
  <behavior>
    - Test 1: `NewQueryHandler` returns a usable handler constructed from `package grpc_test` with mock collaborators only — no `*CoreServer`, no `integrationtest` import, no integration build tag. The SC1 proof for this unit.
    - Test 2: with a **nil** `accessEngine`, a public stream read through `QueryStreamHistory` is DENIED. Assert the deny, not merely "no panic" — this is the fail-closed default the field comment records.
    - Test 3: with a **nil** `commandQuerier`, `ListAvailableCommands` fails closed with the `PERMISSION_DENIED` oops code. Assert the code via the repo's top-level-code idiom, not a substring match on the message.
    - Test 4: with a **nil** `historyReader`, `QueryStreamHistory` returns the `INTERNAL` code rather than panicking or returning an empty success.
    - Test 5: `RefreshConnection` with an empty `session_id` or empty `connection_id` returns `INVALID_ARGUMENT`, and an ownership failure collapses to `SESSION_NOT_FOUND` rather than distinguishing "exists but not yours" (the enumeration-safety property recorded at `refresh_connection.go:17-18`).
    - RED note: all five fail to compile before `QueryHandler` exists. Do not first assert the equivalents against `*CoreServer`.
  </behavior>
  <action>
Create `internal/grpc/query_handler.go` (package `grpc`) declaring `QueryDeps`, `QueryHandler`, and
`NewQueryHandler(deps QueryDeps) *QueryHandler`, following the shape 08-03 and 08-05 established.

`QueryDeps` carries the collaborators the verified matrix attributes to this cluster:
`accessEngine`, `historyReader`, `identityRegistry`, `characterNameResolver`, `commandQuerier`,
`focusCoordinator`, `streamContributor`, `bindings`, `cryptoActive`, plus the session-ownership
preamble pair `playerSessionRepo` and `sessionStore`.

Move these seven methods off `*CoreServer` onto `*QueryHandler`, **bodies byte-identical except for
receiver and field-selector prefix**: `GetCommandHistory` (`server.go:1811`), `QueryStreamHistory`
(`query_stream_history.go:49`), `ListFocusPresence` (`list_focus_presence.go:28`),
`ListSessionStreams` (`list_session_streams.go:30`), `ListAvailableCommands`
(`list_available_commands.go:21`), `RefreshConnection` (`refresh_connection.go:19`),
`buildCharacterIdentity` (`session_identity.go:31`).

Keep each method in its existing one-method file where it already has one — change the receiver and
field-selector prefix in place rather than consolidating six files into one. `query_handler.go`
holds the `QueryDeps` struct, the `QueryHandler` struct, and the constructor. This keeps the diff
reviewable as a receiver change and preserves each file's existing package-level helpers.

Migrate the load-bearing field doc comments onto the `QueryHandler` struct with their fields —
specifically `accessEngine`'s "Nil if ABAC is not configured (public stream reads will be denied)",
`commandQuerier`'s nil-fails-closed-with-PERMISSION_DENIED note, `historyReader`'s
returns-INTERNAL-when-nil note, and `identityRegistry`'s ULID-fallback note.

Preserve exactly: every `oops.Code(...)` string and its `.With(...)` keys; every
`slog.*Context(ctx, ...)` call in `*Context` form; every line-scoped `//nolint:<rule>` with its
comment (including `refresh_connection.go:43`); and the free-function
`auth.ValidateSessionOwnership` calls.

Do not re-translate errors. `.claude/rules/grpc-errors.md` requires status/oops translation at
exactly one layer; the moved bodies already satisfy it, and adding a wrap during relocation would
break the opacity contract these handlers hold.

Write `internal/grpc/query_handler_test.go` in `package grpc_test` implementing the five behaviors,
following `internal/grpc/character_name_resolver_test.go` conventions with ACE names. SPDX header on
both new files.
  </action>
  <verify>
    <automated>task test -- ./internal/grpc/...</automated>
  </verify>
  <acceptance_criteria>
    - `task test -- ./internal/grpc/...` exits 0.
    - `internal/grpc/query_handler_test.go` declares `package grpc_test`; `rg -c 'CoreServer|integrationtest|go:build integration' internal/grpc/query_handler_test.go` returns 0 matches.
    - `rg -n 'CoreServer' internal/grpc/query_handler.go` returns no match (no backpointer).
    - All seven methods are on the new receiver: `rg -c 'func \(h \*QueryHandler\)' internal/grpc/*.go` reports 7.
    - The fail-direction comments survived: `rg -c 'public stream reads will be denied' internal/grpc/query_handler.go` reports at least 1, and the `commandQuerier` and `historyReader` nil notes are present on their fields.
    - Body fidelity: each moved method agrees line-for-line with its pre-move form after normalizing receiver and field-selector prefix; the diff review is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>`QueryHandler` holds the seven-method query cluster with verbatim bodies and preserved fail-closed defaults, provably constructible from an external test package.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce CoreServer to a four-unit facade</name>
  <files>internal/grpc/server.go</files>
  <read_first>
    - internal/grpc/query_handler.go (created in Task 1)
    - internal/grpc/server.go (lines 154-232 — the struct after 08-03 and 08-05; lines 234-350 — the CoreServerOption set; line 352 — NewCoreServer and its sub-unit construction block)
    - internal/grpc/subscribe_handler.go, command_handler.go, lifecycle_handler.go (the three sibling units whose Deps are built alongside this one)
    - cmd/holomush/sub_grpc.go (the production NewCoreServer call site — D-04)
    - internal/testsupport/integrationtest/harness.go (the harness call site — D-04)
    - internal/grpc/mocks/mock_CoreServerOption.go (line 9 — the mockery-generated mock that must stay current)
  </read_first>
  <action>
In `internal/grpc/server.go`:

Add a `queryHandler *QueryHandler` field and construct it in `NewCoreServer` alongside the three
sub-units 08-03 and 08-05 added — after the option loop, for the same reason.

Pass the **same** `focusCoordinator`, `streamContributor`, and `identityRegistry` values into
`QueryDeps` that are passed into `SubscribeDeps`. Do not clone or re-derive them.

Reduce the five exported RPC methods (`GetCommandHistory`, `QueryStreamHistory`,
`ListFocusPresence`, `ListSessionStreams`, `ListAvailableCommands`, `RefreshConnection`) to one-line
delegations on `*CoreServer`, each keeping its exact signature. `CoreServer` must continue to
implement `corev1.CoreServiceServer` with an unchanged exported method set (D-03). No
`api/proto/**` edit, no `task proto`, no `task web:generate`.

Now that all four clusters have moved, prune the `CoreServer` struct: delete every field that is
neither read by `NewCoreServer` when building a Deps struct nor used by a remaining `CoreServer`
method (`currentGameID` at `:373` still uses `gameID`; `newSessionID` and the auth cluster's fields
are still used by `auth_handlers.go`). Let the compiler arbitrate — do not guess. For each field
that REMAINS, state in the SUMMARY which unit's Deps it feeds or which method still uses it. A
field with no justification is a leftover the split missed.

Keep every `CoreServerOption` signature byte-identical (D-04). Options still set fields on
`*CoreServer`; only the routing into sub-unit constructors changes, inside `NewCoreServer`.
`cmd/holomush/sub_grpc.go` and `internal/testsupport/integrationtest/harness.go` must not learn a
new wiring concept.

Verify `internal/grpc/mocks/mock_CoreServerOption.go` is still current. `CoreServerOption` remains
`func(*CoreServer)`, so `mockery` should be a no-op — confirm with a diff, do not assume (research
assumption A3).

Run `task fmt` and commit the output — this is the largest reflow of the aligned `CoreServer` struct
block in the phase.
  </action>
  <verify>
    <automated>task test -- ./internal/grpc/... ./cmd/holomush/... ./internal/testsupport/... ./internal/web/...</automated>
  </verify>
  <acceptance_criteria>
    - `task build` exits 0; `task test -- ./internal/grpc/... ./cmd/holomush/... ./internal/testsupport/... ./internal/web/...` exits 0.
    - `CoreServer`'s exported method set matches the pre-phase list: `rg -o 'func \(s \*CoreServer\) [A-Z]\w*' internal/grpc/*.go | sort` equals the list recorded in 08-03's SUMMARY. Record the comparison.
    - Each of the five delegating RPC methods spans fewer than 10 lines.
    - `git diff --stat origin/main...HEAD -- api/proto/ pkg/proto/ internal/grpc/mocks/` produces no output.
    - `git diff -- cmd/holomush/sub_grpc.go internal/testsupport/integrationtest/harness.go` shows no change, or only a mechanical compile-level adjustment — never a new wiring concept.
    - The SUMMARY lists every remaining `CoreServer` field with its justification (which Deps it feeds or which method uses it).
    - `wc -l internal/grpc/server.go` recorded in the SUMMARY — this is the number Wave C calibrates the ceiling from.
  </acceptance_criteria>
  <done>`CoreServer` is a facade over four constructor-injected units; the proto contract, option set, and both production call sites are unchanged, and every surviving field is accounted for.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-boundary integration gate and ARCH-01 closeout record</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/08-god-object-decomposition/08-VALIDATION.md (§ Existing Coverage Inventory — the current-state query cluster rows)
    - .planning/phases/08-god-object-decomposition/08-03-SUMMARY.md and 08-05-SUMMARY.md (the pre/post method-set lists and LoC figures to carry forward)
    - Taskfile.yaml (lines 160-208 — `test:int` definition)
  </read_first>
  <action>
Run the mandatory wave-boundary gate (D-17).

Run `task test:int` to completion and record the exit code. Judge by exit code only.

The suites directly downstream are `test/integration/stream_history/`,
`test/integration/list_session_streams/`, `test/integration/streams/`, `test/integration/scenes/`,
and `test/integration/access/` (which covers the `accessEngine` deny paths this cluster owns). Name
their results in the SUMMARY.

Then run `task lint`.

Because this plan closes ARCH-01, record the closeout evidence in the SUMMARY:
  - the four extracted unit names and, for each, the path of the `package grpc_test` test that
    constructs it with only its own collaborators — this is the SC1 evidence Wave C's census reads;
  - the pre-phase and post-phase `wc -l internal/grpc/server.go` figures;
  - the pre/post exported `CoreServer` method-set comparison, confirming equality;
  - `git diff --stat origin/main...HEAD -- test/integration/`, confirming no assertion churn;
  - whether `internal/access/` was touched at all (`git diff --stat -- internal/access/`), which
    determines whether `abac-reviewer` fires at PR time (D-19). State which; do not assume.
  </action>
  <verify>
    <automated>task test:int</automated>
  </verify>
  <acceptance_criteria>
    - `task test:int` exits 0.
    - `task lint` exits 0.
    - `git diff --stat origin/main...HEAD -- test/integration/` shows no file with an `assert.*` / `require.*` / `Expect(` hunk; the SUMMARY records the stat verbatim.
    - The SUMMARY names `test/integration/stream_history/`, `test/integration/list_session_streams/`, `test/integration/streams/`, `test/integration/scenes/`, and `test/integration/access/` with results.
    - The SUMMARY contains the ARCH-01 closeout record: four unit names, four proof-test paths, pre/post `server.go` LoC, and the method-set equality confirmation.
    - The SUMMARY states whether `internal/access/` was touched and therefore whether `abac-reviewer` must run.
  </acceptance_criteria>
  <done>ARCH-01 is complete and its evidence recorded in a form Wave C's census and the phase verifier can consume directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → the five current-state RPCs | Untrusted `player_session_token` + `session_id` cross here; `auth.ValidateSessionOwnership` is the ownership gate at `list_focus_presence.go:47` and `refresh_connection.go:24`. |
| query path → ABAC engine | `accessEngine` authorizes stream reads (Layer 2, default-deny) for `QueryStreamHistory` and `ListFocusPresence`. |
| query path → command enumeration | `commandQuerier` is the ABAC-filtered command list for `ListAvailableCommands`; nil fails closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-01 | Elevation of Privilege | `accessEngine` fail-closed default after migration | **high** | mitigate | The nil-denies default is recorded only in a struct-field comment (`server.go:191-193`). Controls: the comment migrates with the field (asserted by grep), Behavior 2 asserts the nil path DENIES rather than merely not panicking, and an explicit prohibition forbids flipping a fail direction during migration. Detector: `test/integration/access/`. |
| T-8-22 | Elevation of Privilege | `commandQuerier` nil fail-closed in `ListAvailableCommands` | **high** | mitigate | Same shape: a nil querier must deny with `PERMISSION_DENIED`. Behavior 3 asserts the oops code via the top-level-code idiom rather than a message substring, so a differently-worded permissive return cannot pass. |
| T-8-02 | Elevation of Privilege | Session-ownership preambles on the migrated RPCs | **high** | mitigate | Verbatim body moves; the preamble is an ordinary statement inside each. Line-for-line diff review required. Detector: `test/integration/auth/` and the enumeration-safety assertion in Behavior 5. |
| T-8-23 | Information Disclosure | Enumeration safety in `RefreshConnection` | medium | mitigate | Ownership failures deliberately collapse to `SESSION_NOT_FOUND` so a caller cannot distinguish "exists but not yours" from "does not exist" (`refresh_connection.go:17-18`). Behavior 5 pins it; a relocation that distinguishes the cases would leak session existence. |
| T-8-24 | Information Disclosure | Error re-translation during relocation | medium | mitigate | `.claude/rules/grpc-errors.md` forbids leaking inner errors past the boundary and requires translation at exactly one layer. A wrap added during the move would double-translate and break the opacity contract. Task 1's action forbids re-translation; verbatim bodies make compliance automatic. |
| T-8-13 | Spoofing | `CoreServer`'s `corev1.CoreServiceServer` method set | low | mitigate | D-03; Task 2 diffs the exported method-name list against 08-03's recorded pre-phase list and asserts an empty proto diff. |
</threat_model>

<verification>
- `task test -- ./internal/grpc/...` green; the SC1 proof test compiles in `package grpc_test` with no `*CoreServer`
- `task test -- ./internal/grpc/... ./cmd/holomush/... ./internal/testsupport/... ./internal/web/...` green
- Exported `CoreServer` method-name list equals the pre-phase list
- `git diff --stat origin/main...HEAD -- api/proto/ pkg/proto/ internal/grpc/mocks/` all empty
- `task test:int` green; `task lint` green
- `git diff --stat origin/main...HEAD -- test/integration/` shows no assertion hunk
- ARCH-01 closeout record present in the SUMMARY
</verification>

<success_criteria>
- `QueryHandler` is constructible from `package grpc_test` with only its own collaborators.
- All seven cluster methods moved with byte-identical bodies and preserved fail-closed defaults.
- `CoreServer` is a facade over four units, with every surviving field justified.
- Proto contract, option set, and both production call sites untouched.
- ARCH-01 closeout evidence recorded for Wave C.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**New files**
- `internal/grpc/query_handler.go`
- `internal/grpc/query_handler_test.go`

**New exported symbols (package `grpc`)**
- `QueryDeps` (struct), `QueryHandler` (struct), `NewQueryHandler(QueryDeps) *QueryHandler`

**New unexported symbols**
- `CoreServer.queryHandler` (field)

**Relocated methods (receiver `*CoreServer` → `*QueryHandler`, bodies verbatim, each staying in its existing file where it has one)**
- `GetCommandHistory` (from `server.go`), `QueryStreamHistory`, `ListFocusPresence`, `ListSessionStreams`, `ListAvailableCommands`, `RefreshConnection`, `buildCharacterIdentity`

**New test symbols**
- ACE-named test functions in `package grpc_test` (exact names chosen at implementation)

**Unchanged by contract**
- `CoreServer`'s exported method set, `NewCoreServer`'s signature, every `CoreServerOption`, `internal/grpc/mocks/**`, all of `api/proto/**` and `pkg/proto/**`
</artifacts_produced>

<output>
Create `.planning/phases/08-god-object-decomposition/08-07-SUMMARY.md` when done.
</output>
