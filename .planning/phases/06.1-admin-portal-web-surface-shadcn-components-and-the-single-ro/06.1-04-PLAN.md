---
phase: 06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro
plan: 04
type: execute
wave: 4
depends_on: [06.1-03]
files_modified:
  - web/src/lib/components/admin/EditCharacterSheet.svelte
  - web/src/lib/components/admin/EditCharacterSheet.svelte.test.ts
  - web/src/lib/components/admin/LifecycleConfirmDialog.svelte
  - web/src/lib/components/admin/LifecycleConfirmDialog.svelte.test.ts
  - web/src/lib/text/byteCount.ts
  - web/src/lib/text/byteCount.test.ts
  - web/src/lib/components/characters/ByteCounter.svelte
  - web/src/lib/admin/client.ts
  - web/src/routes/(authed)/admin/characters/+page.svelte
  - web/src/routes/(authed)/admin/+layout.svelte
  - web/e2e/admin-portal.spec.ts
autonomous: true
requirements: [ADMIN-03, ADMIN-04, ADMIN-05]

estimate:
  tokens: 104000
  raw_tokens: 104000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ── UI consideration: populated — edit Sheet (form) ──
    - "The Sheet has two groups: `Managed elsewhere` FIRST and COLLAPSED, then `Editable here`. version is header metadata — never a row and never editable."
    - "The status control is a transition picker that never sends a status value: selecting Retired routes to the confirm dialog which sends AdminRetireCharacter, and selecting Active routes to the confirm which sends AdminUnretireCharacter. idle appears and is permanently unselectable."
    # ── UI consideration: empty — Sheet with every profile field blank (form) ──
    - "A wholly-blank profile form is a normal state: all 13 maskable paths are optional, `Save changes` is disabled because the mask is empty, and no validation renders before a save."
    # ── UI consideration: partial — some fields set and others blank (form) ──
    - "Blank optional fields render as empty inputs with their counters at `0 of {cap}`; the field mask carries only CHANGED fields, so the footer count is a count of edits and never of filled fields."
    # ── UI consideration: loading — edit Sheet (form) — INVERTED, see R-21 ──
    - "The Sheet header (character name, id, v{n}) renders IMMEDIATELY from the row, and the Editable-here group fetches AdminGetCharacter on open and shows skeleton rows until it resolves. It has an honest loading state because it has a real fetch. The row carries no profile prose — AdminCharacter is the list projection and deliberately has none — so seeding the 13 inputs from row data would render them BLANK and overwrite existing content on the first save."
    - "Save changes stays disabled until the detail fetch resolves, so a save cannot be issued against an unpopulated form."
    - "A failed detail fetch renders `Couldn't load this character. Try again.` inside the Sheet with a retry control, and Save stays disabled — never an empty form that looks editable."
    # ── UI consideration: loading — in-flight mutation (interactive-control) ──
    - "The submit button keeps its label, gains disabled and aria-busy=true; there is no label swap and no spinner."
    # ── UI consideration: error — version conflict (form) ──
    - "On Aborted the Sheet STAYS OPEN, every typed value is preserved, an inline alert names BOTH version numbers and says the changes were not applied, focus moves to the first conflicting field, and NO toast fires because nothing finished."
    # ── UI consideration: error — any other Sheet failure (form) ──
    - "Any other save failure renders `Couldn't save. Try again.`; a lifecycle failure renders `Couldn't change this character's lifecycle. Try again.` No internal code, message or wrapped error text is rendered."
    # ── D-110 mutation loop ──
    - "On success the row enters a pending state, the Sheet CLOSES, the row updates IN PLACE from the response (never a table refetch), and the toast fires naming the RPC."
    - "The retire toast carries an Undo action that sends AdminUnretireCharacter — never a status value — with expected_version set to the NEW version from the retire response, through the same RPC and code path the transition picker uses."
    # ── D-108 confirm copy: four clauses ──
    - "The retire confirm body contains all four required clauses: out of active play; NOT hidden (the public profile stays visible and published history is unchanged); the name stays reserved; and it can be undone."
    - "The confirm is an alert-dialog: it is NOT dismissible by backdrop click and initial focus lands on Cancel, not on the destructive confirm."
    # ── D-109 / geometry / phone band — MECHANISM CORRECTED TWICE, see R-44 and C2-08 ──
    - "The Sheet has NO grab handle; closing is backdrop tap, Escape, the close control and Cancel, and all four work at every band."
    - "`side` is DERIVED IN SVELTE from window.matchMedia('(max-width: 767px)'), not by CSS: a change listener on that MediaQueryList drives `side = matches ? 'bottom' : 'right'`. CSS cannot do this — `side` is a Svelte PROP (sheet-content.svelte:18,25) emitted as data-side={side} (:36), and no CSS rule can mutate an attribute or select the bottom transition classes."
    - "CSS keeps exactly the two responsibilities it can actually discharge: the 84vh height on the bottom variant and the 16px input font, both inside one @media (max-width: 767px) block. The Svelte derivation and the CSS block read the SAME 767px literal through the SAME viewport mechanism, in the same file, so they cannot drift apart or fire at different widths."
    - "The Sheet is a 380px right overlay at every band above 768px, overriding the generated 384px sm:max-w-sm default."
    - "The Sheet is NOT portalled anywhere. It keeps sheet-content.svelte:31's generated document.body target, so its position: fixed content and overlay resolve against the VIEWPORT: the backdrop covers the full viewport INCLUDING the topbar, and 84vh is 84% of the viewport rather than of some ancestor. No portalProps is passed and no portal target element is read from any store."
    - "The height is expressed in vh, not %, so the value the E2E asserts and the value the CSS declares are the same quantity by construction rather than by an argument about which ancestor a percentage resolves against."
    - "The E2E asserts BOTH data-side=bottom AND the computed geometry (height ~84% of viewport, inputs 16px), because the two have two different mechanisms and either can fail alone."
    # ── UI consideration: long-text — the 13 maskable fields (BACKSTOP) ──
    - statement: "The admin Sheet is a SECOND editor for the 13 profile fields Phase 5 authored, so it needs its own held-out proof that its counter agrees with the server at BOTH caps — 99/100/101 against world.MaxNameLength (100) for a short field and 3999/4000/4001 against world.MaxDescriptionLength (4000) for a long one — plus a CJK value crossing each respective cap. Not folded into Phase 5's row: that row tests a different component, and asking a 4000-byte field to reject 101 bytes would assert behaviour the server correctly refuses to exhibit."
      verification: backstop
    # ── One byte-counting function, two display components (R-45; criterion corrected, C2-09) ──
    - "There is exactly ONE PRODUCTION implementation of `new TextEncoder().encode(v).length` under web/src/. ByteCounter.svelte (:34) already had it; this plan EXTRACTS it to web/src/lib/text/byteCount.ts and makes both call sites import it. The two DISPLAY components stay distinct — ByteCounter shows only within 20% of the cap and renders `{bytes} / {maxBytes}`, the admin Sheet's counter is always visible and reads `{n} of {cap}` — but a second copy of the counting expression is the same two-sources-of-truth shape 06.1-03 forbids for charname.Normalize."
    - "Phase 5's three test files keep their OWN independent TextEncoder oracles (ByteCounter.svelte.test.ts:43, ProfileSection.svelte.test.ts:253, CreateCharacterForm.svelte.test.ts:318) and are not modified. A test that recomputes the expected value independently is an ORACLE, not a second implementation — collapsing it onto the function under test would make the test agree with the code by construction. The single-implementation property is therefore asserted over NON-TEST sources only."
    # ── UI consideration: populated,error — the phone band (BACKSTOP) ──
    - statement: "Neither phone-band mechanism can be verified by a passing build, and they are two mechanisms with two failure modes. A held-out Playwright run pinned to 375px asserts: Sheet.Content carries data-side=bottom (the Svelte matchMedia derivation), its inputs compute to 16px and its height is ~84% of the viewport (the @media block), the overlay's bounding box covers the FULL viewport including the topbar band, the rail and admin nav are both zero-width, the mobilebar renders, Created and Ver are absent, and the row's tap target opens the Sheet from a cell that is not the Name cell. A second run at 780px pins the previously untested 768-815px band: rail visible and non-zero, admin nav in its merged rail-width strip, data-side=right."
      verification: backstop
    # ── Chrome-level indistinguishability is OUT OF SCOPE and is recorded, not claimed (C2-39) ──
    - statement: "Indistinguishability is asserted at PAGE level — same heading, same body, same destination list — and NOT at chrome level. SvelteKit renders every layout ABOVE the failed segment, so an /admin/* denial thrown in admin/+layout.ts renders inside (authed)/+layout.svelte's shell and rail, while an unknown /c/[id] (outside (authed), per D-85) renders bare. For one logged-in non-admin, two of the four miss kinds therefore carry shell chrome and two do not. That residual is ACCEPTED and its mechanism is named here rather than papered over: closing it would mean either routing every miss through a chrome-less boundary or rendering shell chrome on a route the viewer has no session for, and neither is in this phase's budget. Proof 4 asserts the page-level property and says so."
      verification: backstop
  artifacts:
    - path: "web/src/lib/components/admin/EditCharacterSheet.svelte"
      provides: "the two-group edit Sheet with the transition picker, the mask footer, and the matchMedia-derived side"
      contains: "matchMedia"
    - path: "web/src/lib/components/admin/LifecycleConfirmDialog.svelte"
      provides: "the alert-dialog confirm carrying D-108's four clauses"
    - path: "web/e2e/admin-portal.spec.ts"
      provides: "the phone-band and mutation-loop end-to-end proofs"
      contains: "375"
  key_links:
    - from: "web/src/lib/components/admin/EditCharacterSheet.svelte"
      to: "web/src/lib/connect/errors.ts"
      via: "isAbortedError classifies the version conflict; classification is reused, not re-spelled"
      pattern: "isAbortedError"
    - from: "web/src/lib/components/admin/LifecycleConfirmDialog.svelte"
      to: "web/src/lib/admin/client.ts"
      via: "both entry points route to one confirm and one RPC"
      pattern: "Admin(Retire|Unretire)Character"

  prohibitions:
    - requirement_id: ADMIN-05
      category: values
      status: resolved
      verification: judgment
      statement: "The admin portal MUST NOT frame retirement as a takedown, a punishment, or a deletion. No copy, label, tooltip, accessible name or confirmation text in this phase may describe retiring a character as hiding them, taking them down, removing them, or as permanent or irreversible — because none of those is true: the public profile stays visible, published history is unchanged, the name stays reserved, and the transition is undoable. Copy that asserts a property of the system is a claim and must be grounded."
    - requirement_id: ADMIN-05
      category: correctness
      status: resolved
      verification: explicit
      statement: "No acceptance criterion, test or E2E assertion in this plan may assert that an admin mutation writes an events_audit row. Issue #4971 is open: the relay publishes through a bare JetStreamPublisher and the audit projector requires an App-Rendering header only RenderingPublisher writes, so no such row is produced today. An audited-write criterion here would fail against correct-for-this-phase behaviour, and its absence must be a stated prohibition rather than a gap a later reader helpfully fills. The accountability this plan delivers is the toast naming the RPC — a UI receipt, not a durable record — and the SUMMARY says so."
    - requirement_id: ADMIN-04
      category: correctness
      status: resolved
      verification: explicit
      statement: "The client MUST NOT refuse to send an over-cap value. The over-cap predicate changes counter styling and nothing else; Save changes is disabled by exactly two conditions — an unresolved or failed detail fetch, and an empty mask — and by no third. The server is the enforcer, and a client that refused would make its own agreement with the server unobservable."
---

<objective>
Ship the edit surface and the mutation loop: a two-group Sheet whose exclusions are declared
before the form, a transition picker that never sends a status value, a confirm that tells the
operator what retiring actually does, and the success/conflict split that decides whether the
Sheet closes.

Purpose: this is where the phase's honesty properties get spent. The Sheet's whole design
problem is that 2 of its 15 rows are deliberately un-editable and must read as intentional. The
status control is exactly the control an implementer would otherwise wire to a `status` field.
And the phone-band treatment cannot be verified by a passing build: a `side` prop that never
flips and an `@media` block that never matches both compile cleanly and leave every desktop test
green. That is why the two mechanisms carry a matched pair of demonstrated REDs and a held-out
Playwright run at 375px.

Output: `EditCharacterSheet.svelte`, `LifecycleConfirmDialog.svelte`, the byte-counter shared
with the server's caps, the D-110 mutation loop, and `web/e2e/admin-portal.spec.ts` carrying
the two held-out proofs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-CONTEXT.md
@.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-UI-SPEC.md
@.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-PATTERNS.md
@web/CLAUDE.md
@.claude/rules/terminology.md
</context>

<phase_split_ground_truth>
**Read `06.1-01-PLAN.md`'s block of the same name first.** The four facts that bind *this* plan:

1. The UI contract is `06.1-UI-SPEC.md` in **this** directory; `06-UI-SPEC.md` does not exist.
   Same for CONTEXT and PATTERNS.
2. **Phase 6 shipped zero web files.** `web/src/routes/(authed)/admin/characters/+page.svelte` is
   created by **06.1-03**, and `web/src/lib/admin/client.ts` by **06.1-02** — not by 06-01.
   `web/src/routes/(authed)/admin/+layout.svelte` is created by 06.1-02; this plan adds **only**
   the single `<Toaster />` mount to it.
3. **The `AdminGetCharacter` detail this plan's Sheet fetches DOES exist**, verified in the
   shipped proto: `AdminCharacterDetail` (`adminportal.proto:411-430`) carries the `AdminCharacter`
   plus `description` (field 2) and `profile map<string, string>` (field 3). `AdminCharacter`
   itself (`:370-378`) carries only `id`, `player_id`, `player_username`, `name`, `status`,
   `last_active_at`, `created_at`, `version` — **no profile prose**, which is precisely why R-21's
   repair (fetch on open rather than seed from the row) is grounded rather than defensive.
4. **#4971 is open: admin mutations never reach `events_audit`.** The relay publishes through a
   bare `JetStreamPublisher` and the projector needs an `App-Rendering` header only
   `RenderingPublisher` writes. **No criterion in this plan asserts that an audit row appears, and
   none may be added.** This matters here more than anywhere else in the phase, because this is
   where the mutations live and where an "audited write" criterion would read as natural. The
   accountability property this plan actually delivers is the **toast naming the RPC** (T-06-58),
   which is a UI receipt, not a durable record. Say so in the SUMMARY, citing #4971, so a verifier
   does not read a green mutation-loop proof as evidence the write was audited.
</phase_split_ground_truth>

<!-- planner-discipline-allow: grabber, grab-handle, drag-handle, refetch, e instanceof Error, TextEncoder, data-side, portalProps, container -->
<!-- Each literal above is negative-grepped by an acceptance criterion AND named in an action
     body. The greps target web source files, not this plan. Note that the retire/un-retire
     forbidden-copy vocabulary (delete, remove, purge, erase, permanent, irreversible) is
     deliberately asserted against RENDERED OUTPUT rather than source text, precisely so a
     source comment explaining the prohibition can neither satisfy nor break the check. -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The edit Sheet — two groups, a transition picker, a visible mask, and a real bottom sheet at 375px</name>
  <files>web/src/lib/text/byteCount.ts, web/src/lib/text/byteCount.test.ts, web/src/lib/components/characters/ByteCounter.svelte, web/src/lib/components/admin/EditCharacterSheet.svelte, web/src/lib/components/admin/EditCharacterSheet.svelte.test.ts</files>
  <read_first>
    - `web/src/lib/components/ui/sheet/sheet-content.svelte:14-38` — **read all of it, and note
      what `side` actually is.** `side` is a Svelte **prop** with default `"right"` (`:18`),
      typed `side?: Side` (`:25`), emitted as `data-side={side}` (`:36`), with positioning driven
      by `data-[side=…]` Tailwind variants. **CSS cannot mutate a prop**, so no container query
      can produce `data-side="bottom"` or select the bottom transition classes. `portalProps`
      exists as a prop (`:20`, `:24`) and is spread into `<SheetPortal>` (`:31`); the class string
      at `:38` carries `data-[side=right]:sm:max-w-sm`, which is **384px** and must be overridden
      to the locked **380px** at the call site (do not edit the generated file). Note that
      `sheet-content.svelte:38` and `sheet-overlay.svelte:15` both carry the Tailwind `fixed`
      class: their geometry resolves against whatever the nearest containing block is, which —
      because this phase introduces no `container-type` and no `transform`/`filter`/`perspective`
      ancestor — is the **viewport**. That is a property this plan depends on and 06.1-02 protects.
    - `web/src/lib/components/characters/ByteCounter.svelte:10-15,34` — the **existing**
      `new TextEncoder().encode(value).length` and its server-agreement rationale, plus the
      doc-comment note explaining that the file words the forbidden spelling rather than writing
      it, because a gate scans this whole file for it. This plan extracts the expression; keep
      that note working.
    - `web/src/lib/components/shell/SectionRail.svelte:114` — `@media (max-width: 767px)`, the
      shipped rail's own collapse rule. **This is the literal and the mechanism this Sheet's
      `side` derivation and its CSS block both agree with.** Plan 06.1-02 builds the admin shell's
      bands from the same viewport `@media` at the same literals and introduces **no**
      `container-type` anywhere; there is no `vpContainer` store and no container element to
      measure or portal into.
    - `internal/world/validation.go:20-21` — `MaxNameLength = 100`, `MaxDescriptionLength = 4000`.
      **Two different caps**, which is why the byte-boundary proof has two triples.
    - `web/src/lib/components/ui/field/` as generated by plan 06.1-01 — `Field.FieldGroup` is the
      grouping primitive carrying the two-group semantics.
    - `web/src/lib/components/characters/ProfileSection.svelte` — the doc-block contract to copy
      in substance (`:5-38`): the section is the unit of failure, it knows nothing about
      `version`, it renders **no server-supplied string**, and classification lives in
      `$lib/connect/errors`. **The state shape is at `:84-96`, not `:40-60`** (cross-AI review
      L-4): `:41-60` is the `Field` type and the props destructure. `:84-96` is what to copy —
      `loaded` and `working` `$state` records seeded through `untrack` (imported at `:6`), `busy`,
      `saved`, `fieldEls`, and the `failure: '' | 'conflict' | 'generic'` union. The rationale
      comment at `:70-83` explains why the `untrack` capture is load-bearing (a save returns the
      whole post-write character, so a section that tracked the prop would reset its own baseline
      and discard a sibling section's draft) — read it, because the admin Sheet has the same
      shape and the same hazard.
    - `web/src/lib/connect/errors.ts` — `isAbortedError`. **Reuse it; do not re-spell it.** Cited
      by symbol rather than line range: cycle-3 review found this plan and 06.1-02 disagreeing on
      whether it spans `:28-36` or `:29-37`, a distinction with no consequence and a citation that
      drifts on every edit above it.
    - `internal/grpc/admin_characters_write.go` as landed by plan 06-05 — the 13 allowlisted
      paths and their exact byte caps, so the client's counter and the server agree.
    - `06.1-UI-SPEC.md` → "The edit Sheet" (the ASCII frame, the header metadata rule, the
      transition-picker contract, the mask-footer rule, "Geometry: one shape, one override",
      D-109's no-handle rule), "Typography" (the mono-confined-to-one-string rule and the
      phone-band 16px input exception), and "Copywriting Contract → Static explanatory copy".
    - `.claude/skills/sketch-findings-holomush/SKILL.md` →
      `references/forms-and-destructive-actions.md` and `references/anti-patterns.md`.
  </read_first>
  <behavior>
    RED first, in `EditCharacterSheet.svelte.test.ts` and `byteCount.test.ts`:
    - `byteCount('a'.repeat(99))` is 99; `byteCount` of a 34-rune CJK string equals its UTF-8
      byte length, not its rune count; the counter's over-cap predicate flips at exactly 101
      bytes for a 100-byte cap and not at 100, and at exactly 4001 for a 4000-byte cap and not
      at 4000. **Both caps**, because the 13 fields do not share one.
    - `web/src/lib/components/characters/ByteCounter.svelte` imports `byteCount` and its shipped
      Phase 5 tests (`ByteCounter.svelte.test.ts`, `ProfileSection.svelte.test.ts`) stay green
      **unmodified** — the extraction is behaviour-preserving.
    - The Sheet's Editable-here group renders skeletons and a `disabled` Save while the detail
      fetch is in flight, populates all 13 inputs from the resolved detail, and — with a mocked
      detail carrying non-empty prose — shows those values rather than blanks. A Sheet seeded
      from row data alone renders 13 empty inputs and fails this.
    - A failed detail fetch renders the retry copy with Save still `disabled` and no typed values
      lost.
    - The Sheet renders `Managed elsewhere` **before** `Editable here`, and the first group is
      collapsed on open.
    - `version` appears in the header metadata line and appears in **no** row and in **no**
      editable control.
    - The lifecycle picker offers `Active` and `Retired` as selectable and renders `idle`
      disabled with its reason text.
    - Selecting a transition does **not** call any RPC — it opens the confirm.
    - `Save changes` is `disabled` on open (empty mask) and on a field edited back to its
      original value (dirty is `loaded` vs `working`, never a flag set by an input handler).
    - The footer renders `update_mask: {n} paths` when dirty and `update_mask: empty — no-op`
      when clean.
    - Every input and select carries a `name` attribute and the submit control carries
      `type="submit"`.
  </behavior>
  <action>
**`web/src/lib/text/byteCount.ts` — EXTRACT the existing implementation; do not write a second
one** (cross-AI review R-45). `ByteCounter.svelte:34` already computes
`new TextEncoder().encode(value).length`, with the same server-agreement rationale documented at
`:10-15` and boundary tests at the cap. A second copy of a security-adjacent counting expression
is exactly the two-sources-of-truth shape 06.1-03 forbids for `charname.Normalize`, and this plan
mandated a search-before-writing step for `lastActive.ts` but not for this.

- Create `web/src/lib/text/byteCount.ts` exporting `byteCount(v: string): number`. The module is
  under `lib/text/`, not `lib/admin/`, because both a player-facing component and an admin one
  import it.
- Move the doc-comment substance from `ByteCounter.svelte:10-15` onto the function: the caps are
  **byte** caps enforced server-side, a ~34-rune CJK value is rejected while looking short, and a
  UTF-16 code-unit count agrees on ASCII and disagrees on everything else. **Keep wording the
  forbidden spelling rather than writing it** — `ByteCounter.svelte` has a gate scanning it for
  that literal and the same discipline applies to the extracted module.
- Change `ByteCounter.svelte:34` to `const bytes = $derived(byteCount(value));` and leave its
  `over` and `shown` rules untouched — those are its display contract and are not shared.
- The admin Sheet imports the same function. **The two display components stay distinct**:
  `ByteCounter` surfaces only within 20% of the cap and renders `{bytes} / {maxBytes}`; the
  admin Sheet's counter is always visible and reads `{n} of {cap}`. Reuse the *function*, not
  the component.
- Re-run Phase 5's `ByteCounter` and `ProfileSection` tests; they must stay green untouched.

`web/src/lib/components/admin/EditCharacterSheet.svelte`.

**Header:** the title `Edit character` at the Heading role, and one Dense muted metadata line
`{characterId} · v{n}` in **monospace** — the character ULID is the single string in this phase
permitted monospace, because it is an opaque identifier an operator may need to copy.

**Group 1, `Managed elsewhere`, FIRST and COLLAPSED.** Collapsed it costs ~30px and shows
`Name {name} · Status {status}` plus a right-aligned `managed by their own operations`.
Expanded it shows `Name` and `Status` as full rows with their reasons:
- Name: `Names go through the normalization pipeline and a uniqueness check, so they are not
  editable from this form.`
- Status: `Use the lifecycle control below — it sends the transition, not a status value.`
Declaring the exclusions **before** the form is the whole point: the operator learns what this
surface does not do before they start typing.

**`version` is header metadata, never a row and never editable.** It is the concurrency guard
carried as `expected_version` (§9.4); a row alongside `Name`/`Status` would imply a door that
does not exist.

**Group 2, `Editable here`** — the 13 maskable profile fields inside `Field.FieldGroup`, each
with a `name` attribute and a `{byteCount(v)} of {cap}` counter.

**Where those 13 current values come from: an `AdminGetCharacter` fetch on open** (cross-AI review
R-21). An earlier revision required the Sheet to open from row data, issue no fetch, and render
all 13 current values — three properties that cannot all hold, because `AdminCharacter` is the
**list** projection and carries no profile prose at all. Seeding from it would render every prose
input blank and **overwrite existing content** on the first save. Plan 06-04 now returns an
`AdminCharacterDetail` from `AdminGetCharacter` carrying `description` plus the twelve
`profile.*` values; this Sheet fetches it.

The loading state is honest, not hidden:

- The **header** (`Edit character`, `{characterId} · v{n}`) and **Group 1** render immediately
  from the row — the row already has name, status and version, so the operator sees the right
  character instantly and the fetch is never a blank modal.
- **Group 2** renders `skeleton` rows at the field count until the detail resolves.
- `Save changes` is `disabled` while loading — a save cannot be issued against an unpopulated
  form, which is the failure mode this whole change exists to prevent.
- On failure: `Couldn't load this character. Try again.` inside the Sheet with a retry control,
  Save still `disabled`. Never an empty form that looks editable.
- Seed `loaded` from the resolved detail and `working` from `loaded` through `untrack`, per
  `ProfileSection.svelte:40-60`; dirty stays `loaded` vs `working`, so nothing is dirty on arrival.

**The lifecycle control**, labelled `Lifecycle`, is a **transition picker that never sends a
status value**. Selecting `Retired` routes to the confirm dialog, which sends
`AdminRetireCharacter`; selecting `Active` routes to the confirm, which sends
`AdminUnretireCharacter`. Selecting a transition does **not** apply it. `idle` appears and is
permanently unselectable (dimmed) with the reason
`idle — system-invoked on inactivity. Not implemented in this release.` The control carries an
always-visible footnote naming the RPC it will send
(`Sends AdminRetireCharacter — never a status value.`, or the un-retire equivalent when the
character is retired) — stated literally because this is exactly the control an implementer
would otherwise wire to a `status` field.

**The mask is visible and an empty mask is inert.** Dirty is derived from `loaded` vs `working`
`$state` records — **never** a flag set by an input handler, which would survive an edit that
put a field back the way it started. That derivation drives both the footer count and the
`disabled` state of `Save changes`.

**Over-cap display never disables Save** (cross-AI review C2-40). The counter's over-cap predicate
flips at exactly 101 bytes for a 100-byte cap; it changes the counter's *styling* and nothing
else. `Save changes` is disabled by exactly two conditions — the detail fetch being unresolved or
failed, and an empty mask — and by no third. **The server is the enforcer**, and Proof 2 depends
on that: it types 101 bytes and expects the server's `InvalidArgument` to surface as
`Couldn't save. Try again.`, which is unreachable if the client refused to send. The natural move
of matching `ByteCounter`'s `over` contract to a `disabled` binding would silently make that proof
untestable, so say the rule out loud rather than leaving it to inference.

**Geometry: two mechanisms, because one of them cannot be CSS.** An earlier revision had a single
`@container vp (max-width: 767px)` block "flip it to `side=bottom`". **That is impossible**
(cross-AI review R-44, orchestrator-verified): `side` is a Svelte **prop** (`sheet-content.svelte:18`,
`:25`) emitted as `data-side={side}` (`:36`); CSS can restyle `[data-side=right]` geometry but can
never mutate that attribute to `bottom` nor select the bottom transition classes. The E2E
assertion `data-side="bottom"` could not have passed under its own specified mechanism.

Split it — and note that the split is now **cheaper** than the previous revision's, because
06.1-02 dropped the container query it was written around (cross-AI review C2-02, C2-08):

1. **`side` is derived in Svelte from `window.matchMedia`.** In `$effect`, create
   `const mql = window.matchMedia('(max-width: 767px)')`, seed `$state` from `mql.matches`,
   subscribe with `mql.addEventListener('change', …)`, and remove the listener on teardown.
   Compute `const side = $derived(isPhone ? 'bottom' : 'right')`, passed to `Sheet.Content`.
   Guard for SSR/test environments where `window.matchMedia` may be absent by defaulting to
   `'right'` — the desktop shape — rather than flickering through `bottom`.
   **Use `matchMedia`, not a `ResizeObserver` on some element.** The previous revision measured
   the `vp` container so the flip would share a threshold with an `@container` block; with that
   block gone, the viewport is the thing both halves are about, and `matchMedia('(max-width: 767px)')`
   is literally the same query the CSS below evaluates. Same mechanism, same literal, same file —
   they cannot fire at different widths, and there is no element whose width has to be argued
   about.
2. **CSS keeps only what CSS can do**: exactly one `@media (max-width: 767px)` block carrying
   `height: 84vh` on the bottom variant and the 16px font on every `input`, `textarea` and
   `select`. Two rules, one block, so a reader cannot honor one and miss the other. **`vh`, not
   `%`** — a percentage height resolves against the containing block and would make the E2E's
   "≈84% of the viewport" a claim about ancestry; `84vh` is 84% of the viewport by definition.
   The 16px floor is a platform constraint, not a style preference: any font `<16px` in a
   **focused** input triggers iOS Safari's zoom-on-focus, which does not unzoom on blur.
3. **380px right overlay above the phone band**, via an explicit `class` on `Sheet.Content`
   overriding the generated `sm:max-w-sm` 384px default. Do not edit the generated file.
4. **Pass NO `portalProps`.** The Sheet keeps its generated `document.body` portal target
   (`sheet-content.svelte:31`). An earlier revision retargeted it into the `vp` container so an
   `@container vp` block could match; with the block now a viewport `@media`, the portal target
   is irrelevant to whether it matches — and retargeting would actively break things. Both
   `Sheet.Content` and `Sheet.Overlay` are `position: fixed`; portalling them inside an element
   with layout containment rebases them to that element, so the overlay would cover only
   shell-minus-topbar (leaving the topbar undimmed and clickable behind an open modal) and any
   percentage height would resolve against the wrong box. Write one sentence in a comment saying
   the Sheet is deliberately not portalled and why, so a future reader does not "improve" it by
   giving it a target.

**No grab handle** (D-109). `bits-ui` has no swipe-dismiss, so a handle would promise a gesture
the component cannot honor. Closing is backdrop tap, `Escape`, the `×`, and `Cancel`; all four
MUST work at every band. No back arrow on the phone sheet — it would make the sheet a route.

`bits-ui` Dialog semantics: focus trapped, focus returns to the invoking row action on close,
`Sheet.Title` and `Sheet.Description` always present.

Copy: `Save changes` (verb+noun, the milestone convention), `Cancel`. Terminology binding:
**character**, **player**; never "room", "user", "avatar", "account".
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run EditCharacterSheet byteCount && pnpm exec svelte-check --threshold error</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'portalProps' web/src/lib/components/admin/EditCharacterSheet.svelte` returns **zero** matches — the Sheet keeps its generated `document.body` portal so its `position: fixed` geometry resolves against the viewport. `rg -n 'container-type|container-name|@container' web/src/lib/components/admin/EditCharacterSheet.svelte` also returns zero.
    - `rg -n "matchMedia\('\(max-width: *767px\)'\)" web/src/lib/components/admin/EditCharacterSheet.svelte` matches, and — **as a falsifiable inverse rather than a judgment-phrased "shows"** (cross-AI review L-4) — `rg -n '<Sheet\.Content[^>]*\bside="' web/src/lib/components/admin/EditCharacterSheet.svelte` returns **zero** matches: a literal on the `side` **prop** is the exact defect, so its absence is the checkable form.
      **The pattern is anchored to the `Sheet.Content` tag deliberately.** The review suggested the plain inverse `rg -n 'side="(right|bottom)"' …` — which would **fail a correct implementation**, because this task's own `@media` block has to scope the `84vh` height "on the bottom variant", and the natural way to write that is an attribute selector like `[data-side="bottom"]`. A bare `side="…"` pattern matches that CSS selector as readily as the prop it is aiming at. Same class of defect as H-3/H-4 and found the same way — by grepping the new criterion back against the plan that carries it. A Vitest case stubs `window.matchMedia` to report `matches: true` and asserts the rendered `[data-slot="sheet-content"]` carries `data-side="bottom"`, then `matches: false` and asserts `data-side="right"`. **CSS cannot produce either result** — `side` is a prop emitted as `data-side` (`sheet-content.svelte:18,25,36`) — so a criterion that only grepped for a media block would have passed while the property was dead.
    - `rg -c '@media \(max-width: *767px\)' web/src/lib/components/admin/EditCharacterSheet.svelte` is exactly 1, that block contains `84vh` and the `16px` font rule — and **not** a `side` flip, which CSS cannot express — and the `767px` literal in it is byte-identical to the one in the `matchMedia` call above. `rg -n 'height: *84%' web/src/lib/components/admin/EditCharacterSheet.svelte` returns zero matches: the height is `vh`, not `%`, so the E2E's "≈84% of the viewport" is a claim about the same quantity the CSS declares rather than about which ancestor a percentage resolved against.
    - `rg -n '380px' web/src/lib/components/admin/EditCharacterSheet.svelte` matches on the `Sheet.Content` class override; `git diff --exit-code web/src/lib/components/ui/sheet/sheet-content.svelte web/src/lib/components/ui/sheet/sheet-overlay.svelte` exits 0 (the generated defaults are not edited).
    - **Exactly one non-test source computes the byte length.** `rg -l 'new TextEncoder\(\)\.encode' web/src --glob '!*.test.ts' --glob '!*.test.svelte.ts'` lists exactly **one** file, `web/src/lib/text/byteCount.ts`. Verified against HEAD: that command currently lists exactly one file too (`ByteCounter.svelte`), so the extraction moves the single implementation rather than adding one, and adding a second copy inside `EditCharacterSheet.svelte` makes it list two — demonstrate and record.
      **The previous spelling, `rg -c 'TextEncoder' web/src/` totals exactly 1, was unsatisfiable under this same task's own constraint** (cross-AI review C2-09): the literal appears in six places at HEAD across five files — `ByteCounter.svelte:10` and `CharacterPortrait.svelte:28` (prose comments), `ByteCounter.svelte:34` (the implementation), and `ByteCounter.svelte.test.ts:43`, `ProfileSection.svelte.test.ts:253`, `CreateCharacterForm.svelte.test.ts:318` (three independent test oracles the same criterion forbids modifying). A correct extraction leaves six. `rg -c` also prints per-file counts and exits 1 on zero matches, so "totals exactly N" is a summing instruction rather than a check.
    - `rg -n 'byteCount' web/src/lib/components/characters/ByteCounter.svelte web/src/lib/components/admin/EditCharacterSheet.svelte` matches in both — one counting function, two display components. `cd web && pnpm exec vitest run ByteCounter ProfileSection CreateCharacterForm` passes with those three files' shipped assertions unmodified; `git diff --exit-code web/src/lib/components/characters/ByteCounter.svelte.test.ts web/src/lib/components/characters/ProfileSection.svelte.test.ts web/src/lib/components/characters/CreateCharacterForm.svelte.test.ts` exits 0.
    - A Vitest case mocks `AdminGetCharacter` with non-empty prose and asserts all 13 inputs hold those values; a case with the fetch pending asserts skeletons and a `disabled` Save; a case with the fetch rejected asserts the retry copy and a `disabled` Save. Seeding from row data alone fails the first.
    - A Vitest case asserts the rendered `Managed elsewhere` group precedes `Editable here` in DOM order and is collapsed on open.
    - A Vitest case asserts no `input`, `select` or `textarea` in the Sheet has a `value` bound to the character's `version`, and that the version appears exactly once, in the header metadata line.
    - A Vitest case selects `Retired` in the lifecycle picker and asserts **zero** RPC calls were made and the confirm dialog opened.
    - A Vitest case edits a field then edits it back and asserts `Save changes` is `disabled` again — a dirty flag set by an input handler would fail this.
    - `rg -n 'grabber|grab-handle|drag-handle' web/src/lib/components/admin/EditCharacterSheet.svelte` returns zero matches. **The bare substring `handle` is deliberately NOT in that alternation** (cross-AI review C2-38): it matches `handleSave`, `handleOpenChange` and every other conventionally named Svelte event handler, so the previous spelling failed a natural correct implementation while proving nothing about D-109. A Vitest case additionally asserts the rendered Sheet contains no element with a `role="separator"` or a drag affordance above the header — the property, rather than a name for it.
    - A Vitest case types a value one byte over a short field's cap and asserts `Save changes` is **enabled**, and that the over-cap state changed only the counter's styling. Binding `disabled` to the over-cap predicate makes it FAIL — demonstrate and record. Without this the server-agreement E2E (Proof 2) is unreachable, because the client would refuse to send the request whose refusal it is supposed to observe.
    - `byteCount.test.ts` covers 99/100/101 against a 100-byte cap **and** 3999/4000/4001 against a 4000-byte cap, plus a CJK string crossing each, and the CJK cases assert the counter reports UTF-8 byte length rather than rune count. A table covering only the 100-byte cap is insufficient: the 13 fields split across `world.MaxNameLength` (100) and `world.MaxDescriptionLength` (4000).
    - Every `input`/`select`/`textarea` in the Sheet has a `name` attribute (assert by Vitest query, not by grep) and the submit control has `type="submit"`.
  </acceptance_criteria>
  <done>The Sheet declares its exclusions first, keeps `version` out of the form, routes both transitions through a confirm without ever sending a status value, disables Save on an empty mask (and on nothing else — never on an over-cap value), derives `side` from `matchMedia` at the same literal its one `@media` block reads, and stays portalled to `document.body` so its overlay covers the whole viewport.</done>
</task>

<task type="auto">
  <name>Task 2: The confirm dialog and the D-110 mutation loop</name>
  <files>web/src/lib/components/admin/LifecycleConfirmDialog.svelte, web/src/lib/components/admin/LifecycleConfirmDialog.svelte.test.ts, web/src/routes/(authed)/admin/characters/+page.svelte, web/src/routes/(authed)/admin/+layout.svelte, web/src/lib/admin/client.ts</files>
  <read_first>
    - `web/src/lib/components/ui/alert-dialog/` and `web/src/lib/components/ui/{alert,sonner}/`
      as generated by plan 06.1-01.
    - `web/src/lib/connect/errors.ts:28-36` — `isAbortedError`, reused not re-spelled.
    - `web/src/routes/(authed)/admin/characters/+page.svelte` as landed by plan 06.1-03 —
      `pendingRowId` is the per-row pending state, and the `untrack` rationale there is exactly
      why the row updates in place rather than refetching.
    - `06.1-UI-SPEC.md` → "The mutation loop (D-110 — binding sequence)" (the five-step table with
      its two columns), "Success states — the toast names the RPC" (the three toast strings, the
      6s duration, the `Undo` contract), "Destructive confirmation (D-108)" (both copy tables and
      the clause map), and "Error states" (the exact failure strings).
    - `.planning/phases/05-character-identity-ui-public-profiles/05-UAT.md` test 3 — D-93's
      conflict behaviour verified live, which D-110 mirrors exactly.
    - `06.1-CONTEXT.md` → D-108's four required clauses and D-110's full sequence.
  </read_first>
  <action>
`web/src/lib/components/admin/LifecycleConfirmDialog.svelte` — an **`alert-dialog`**, because
this confirmation requires a decision and MUST NOT be dismissible by a backdrop click; the
installed `dialog` does not carry that role, and reusing it would ship a confirmation for an
audited mutation on a character the operator does not own that a stray backdrop tap can silently
cancel. Initial focus lands on `Cancel`, never on the destructive confirm.

Retire copy — title `Retire {name}?`, confirm `Retire character` on `--color-destructive`,
cancel `Cancel`, and this body verbatim:

> Retiring takes this character out of active play. It does not hide them — their public profile
> stays visible and everything they have already posed or played in is unchanged. The name stays
> reserved. You can undo this at any time.

The clause map, so a reviewer can check all four landed: *out of active play* → sentence 1;
*not hidden* → sentence 2 (profile + published history); *name stays reserved* → sentence 3;
*undoable* → sentence 4. This is **retire-specific copy and is NOT Phase 5's player-facing
not-retroactive string** — that string addresses a player choosing what to reveal about their own
profile, which an admin is not. The misconception this actor is likeliest to hold is that
retiring is a takedown, and the body corrects exactly that.

Un-retire copy — title `Return {name} to active play?`, body `This character becomes playable
again. Their public profile and history are unchanged.`, confirm `Un-retire character`
(**primary, not destructive**), cancel `Cancel`. Un-retire confirms too, deliberately: both
directions are audited mutations by an operator on a character they do not own, and the
transition picker's contract is that selecting a transition *routes to the confirmation* rather
than applying it.

Both entry points — the table's row action and the Sheet's lifecycle picker — route to **this
one component** and send **the same RPC**. Two entrances, one component, one mutation.

**The mutation loop, D-110's binding sequence.** Wire it in the characters page:

| Step | Success | `Aborted` (version conflict) |
|---|---|---|
| 1 | `Save`/confirm becomes `disabled` + `aria-busy="true"`, **keeping its label** — no swap to "Saving…", no spinner | same |
| 2 | the row enters the pending state | same |
| 3 | **the Sheet closes** | **the Sheet stays open** |
| 4 | the row updates **in place from the response** — never a table refetch — and flashes via the shipped `just-arrived` keyframe | **every typed value is preserved** |
| 5 | the toast fires, **naming the RPC** | the conflict `alert` renders inline with the fresh version; focus moves to the first conflicting field. **No toast — nothing finished.** |

The toast is the receipt for a **finished** action, so a Sheet still open behind it would be
ambiguous, and the Sheet's `version` header metadata would already be stale.

Toast strings, exactly:
- `AdminUpdateCharacter · update_mask: {n} paths · v{old} → v{new}`
- `AdminRetireCharacter · {name} · v{old} → v{new}` plus an `Undo` action
- `AdminUnretireCharacter · {name} · v{old} → v{new}`

Duration **6s** (longer than the 4s default — this is a receipt and the `Undo` needs a usable
window), dismissible, and never the sole carrier of the outcome: the row already updated in
place. One `<Toaster />` mounts once, in the admin layout.

Naming the wire call is **accountability**, not debug output: an admin is acting on a character
they do not own, recorded to the audit log under their player id (§10.7). The rule is
*narration is wrong on read surfaces and right on audited writes*.

**The `Undo` sends `AdminUnretireCharacter`** — never a status value — carrying
`expected_version` = the **new** version from the retire response, through the same RPC and the
same code path the transition picker uses.

**Conflict copy**, inline `alert` with `role="alert"` inside the open Sheet:
`Someone else edited this character. Your copy is version {mine}; the server is now at {theirs}.
Your changes were not applied — reload to see the current values, then re-apply.` plus a
`Reload` control. Both numbers are versions, which the table already shows; this is the one place
a server-supplied **number** reaches the screen, and no server-supplied **string** does.

Other failures: `Couldn't save. Try again.` and
`Couldn't change this character's lifecycle. Try again.` Resolve every failure into the small
authored union (`'' | 'conflict' | 'generic'`); never the `e instanceof Error ? e.message : …`
idiom.

Copy that asserts a property of the system is a claim and must be grounded. The properties that
actually hold here are *reserved*, *reversible* and *not hidden*.
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run LifecycleConfirmDialog && pnpm exec svelte-check --threshold error</automated>
  </verify>
  <acceptance_criteria>
    - A Vitest case renders the retire confirm and asserts the **rendered text** contains all four clause markers: `out of active play`, `does not hide them`, `name stays reserved`, `undo this at any time`.
    - A Vitest case renders both confirms and asserts the rendered text matches **none** of `/\bdelete\b|\bremove\b|\bpurge\b|\berase\b|permanent|irreversible|cannot be undone|take.?down/i`. Assert on rendered output, not on file text, so a source comment cannot satisfy or break it.
    - A Vitest case asserts the confirm dialog does **not** close on a backdrop click and that initial focus is on the `Cancel` control.
    - A Vitest case drives the success path and asserts, in order: the button kept its label and gained `aria-busy`, the Sheet closed, the row's rendered values came from the mocked **response** with **zero** additional list requests, and one toast fired containing the RPC name.
    - A Vitest case drives the `Aborted` path and asserts: the Sheet is still open, every typed field still holds its typed value, the `alert` text contains both version numbers, focus is on the first conflicting field, and **zero** toasts fired.
    - A Vitest case asserts the retire toast's `Undo` triggers `AdminUnretireCharacter` with `expectedVersion` equal to the retire response's **new** version.
    - `rg -n 'refetch|invalidate|listAdminCharacters' web/src/lib/components/admin/EditCharacterSheet.svelte web/src/lib/components/admin/LifecycleConfirmDialog.svelte` returns zero matches — no component refetches the table.
    - `rg -l '<Toaster' web/src | wc -l` prints exactly `1`, and that file is `web/src/routes/(authed)/admin/+layout.svelte`. Verified against HEAD: `<Toaster` appears **zero** times today, so a second mount is the only way this can exceed one — mount it in a second component and the count becomes 2. Counting **files** rather than summing `rg -c` output matters: `rg -c` prints per-file counts and exits 1 on zero matches, so a "totals exactly 1" phrasing is a summing instruction a shell does not perform and is satisfied by nothing at all.
  </acceptance_criteria>
  <done>One confirm component serves both entrances and both directions, its retire body carries all four clauses and asserts nothing untrue, and the success and conflict paths differ exactly where D-110 says they do.</done>
</task>

<task type="auto">
  <name>Task 3: The two held-out proofs — the phone band and the byte-cap agreement</name>
  <precondition>Docker is running and `task test:e2e` can bring up the compose stack with the `playwright` service; the phone-band assertions require a real browser layout pass, which no unit test can substitute for.</precondition>
  <files>web/e2e/admin-portal.spec.ts</files>
  <read_first>
    - `web/e2e/` — the **19** existing `*.spec.ts` files and their fixture/auth setup (counted at
      HEAD; the previous revision said 20 — cross-AI review L-4). **`admin.spec.ts` is telnet
      admin commands** (`test.describe('Admin Commands')` at `:44`), NOT the web portal; do
      **not** extend it. `web/e2e/admin-portal.spec.ts` is a new file, making 20.
    - `web/e2e/scenes.spec.ts` or `character-switcher.spec.ts` — the in-tree pattern for signing
      in and driving an authed route.
    - `06.1-UI-SPEC.md` → the two 🧪 backstop rows in `## UI Considerations` (the 13 maskable
      fields; the phone band) — these two are the reason this task exists.
    - `web/src/lib/components/ui/sheet/sheet-content.svelte:31,38` — the portal and the 384px
      default, so the assertions target the right attributes.
    - `internal/grpc/admin_characters_write.go` as landed by plan 06-05 — the exact byte caps.
  </read_first>
  <action>
`web/e2e/admin-portal.spec.ts` — a **new** file.

**Proof 1 — the phone band, pinned to a 375px viewport.** In one `test.describe` with
`test.use({ viewport: { width: 375, height: 812 } })`, sign in as an admin, open
`/admin/characters` and assert:
- The section rail and the admin nav both compute to `width: 0px`, and the `.mobilebar` renders.
  **Identify the `.mobilebar` element explicitly** — it is the breadcrumb strip owned by
  `web/src/routes/(authed)/admin/+layout.svelte`, and it carries **no hamburger**: the drawer is
  opened by the shipped `TopBar.svelte:60` control (cross-AI review L-1). Assert there is
  **exactly one** element with an accessible name of `Open navigation` on the page; two would mean
  a duplicate control was built.
- **The phone drawer carries both groups and is navigable — the assertion H-2 said was missing.**
  Click the TopBar hamburger, then inside the opened `SheetContent` assert: the group labels
  `Workspace` **and** `Admin` are both present; click an admin section entry and assert the URL
  changed to that section; reopen the drawer and click a **second, different** admin section entry
  and assert the URL changed again. A criterion asserting only "the mobilebar renders and the
  rails are zero-width" **passes while the `Admin` group is entirely absent**, which is how C2-34
  survived a full review cycle. Then navigate to a **non-admin** authed route, open the drawer,
  and assert the `Admin` label is **gone** — that is the `adminNavStore` clear-on-teardown path
  (06.1-02 flagged assumption 4), and it is the half a component test cannot reach.
- The `Created` and `Ver` column headers are **absent**.
- Tapping a row opens the Sheet — **and the tap lands on the `Status` or `Last active` cell, not
  the `Name` cell.** That distinction is the whole point of the assertion (cross-AI review
  C2-10): 06.1-03's row target is a button in the `Name` cell whose `::after` overlay must span the
  row, and a tap on the `Name` cell hits the button directly and passes whether or not the
  overlay spans anything.
- The opened `[data-slot="sheet-content"]` carries `data-side="bottom"`.
- Its `input` elements compute to `font-size: 16px`, and its height is within ±2px of
  `0.84 * window.innerHeight`.
- **The overlay covers the full viewport, topbar included.** Assert
  `overlay.getBoundingClientRect()` has `top === 0` and `height === window.innerHeight` (±1px),
  and that a click at `(x, 10)` — inside the 44px topbar band — dismisses the Sheet rather than
  reaching the topbar. This is the assertion that would have caught the previous revision, in
  which the Sheet was portalled into a containing block whose top edge sat `--topbar-h` below the
  viewport, leaving the topbar undimmed and clickable behind an open modal (C2-08).
- The Sheet element is a direct child of `document.body`, **not** a descendant of `.shell` —
  `document.body.contains(sheet) && !shell.contains(sheet)`. The Sheet is deliberately not
  portalled anywhere, and this pins that.
- All four close paths work: backdrop tap, `Escape`, the close control, and `Cancel`.

A green build proves nothing here, and the two phone properties have **two different
mechanisms**, so demonstrate a RED for each (cross-AI review R-44):

- Temporarily replace the `@media (max-width: 767px)` block's `84vh` with `50vh`: the **height**
  assertion fails while `data-side` **still passes**, because the side flip is a Svelte
  derivation and does not depend on the CSS block. Restore.
- Temporarily replace the derived `side` with the literal `"right"`: the **`data-side`**
  assertion fails while the font-size and height assertions still pass. Restore.

Record both, with their observed failure text. The pair is what proves the two mechanisms are
independent — and it is the demonstration that would have caught the earlier design, in which one
CSS block was supposed to do both and could only ever have done one.

**Proof 1b — the 768–815px band, at 780px.** A second `test.describe` with
`test.use({ viewport: { width: 780, height: 900 } })`: assert the section rail is **visible** and
non-zero-width, that the rail carries an `Admin` entry in its `.is-context` treatment, that the
admin nav is in its merged rail-width strip and still lists the admin section entries (they are
navigable at this band, not hidden), and that the opened Sheet carries `data-side="right"`. This
band was untested at every viewport the plans previously named (1200px and 900px manual, 375px
Playwright), and it is where a mechanism mismatch between the rail's `@media` and the admin
shell's rules would show up first.

**Proof 2 — the byte-cap agreement, at BOTH caps.** In a desktop-viewport describe block
(cross-AI review R-33 — the caps are different and an earlier revision applied the short one to
both):

- **Short-cap field** (`world.MaxNameLength` = **100** bytes): type 99 and save (success); 100
  and save (success); **101** and save (expect the server's `InvalidArgument` surfacing as
  `Couldn't save. Try again.`).
- **Long-cap field** (`world.MaxDescriptionLength` = **4000** bytes): type 3999 and save
  (success); 4000 and save (success); **4001** and save (expect the refusal). **101 bytes must
  be ACCEPTED here** — asserting otherwise would demand behaviour the server correctly refuses
  to exhibit.
- A **separate** CJK string per field, each crossing its own cap by UTF-8 byte length while its
  rune count does not (expect the refusal), asserting the on-screen counter had already reported
  the value as over the cap.

The admin Sheet is a **second** editor for these fields, so this proof is its own — it is not
folded into Phase 5's, which tests a different component.

**Proof 3 — the mutation loop over the real stack.** Drive a successful
`AdminUpdateCharacter` and assert the Sheet closed, the row's rendered values changed **without**
a new list request (assert via `page.route` request interception that no list call fired between
the mutation response and the row update), and one toast appeared containing the RPC name. Then
drive a conflict by mutating the row out of band and assert the Sheet stayed open with its typed
text intact and no toast appeared.

**Proof 4 — indistinguishability at PAGE level, with its negative control and its named limit.**
Sign in as a **non-admin** and navigate to `/admin`, `/admin/characters` and `/admin/moderation`;
assert each renders the ordinary not-found — same heading, same body, same destination list — and
that the rendered text contains no path echo and no permission vocabulary. In a correct
implementation the renderer does not branch on which kind of miss occurred, so the paths are
identical **by construction** and a naive assertion passes trivially. **Demonstrate the RED
first**: temporarily make the admin denial branch render a distinct string, watch this assertion
fail, revert, and record it. An opacity test whose green has never been earned is not evidence.

**Scope this proof to the page, explicitly, and record why** (cross-AI review C2-39). Assert
inside the `+error.svelte` boundary's own subtree and do **not** assert the surrounding chrome,
because the chrome genuinely differs and the difference is structural: SvelteKit renders every
layout **above** the failed segment, so a denial thrown in `admin/+layout.ts` renders inside
`(authed)/+layout.svelte`'s shell and rail (that layout's own load succeeded), while an unknown
`/c/[id]` sits outside `(authed)` per D-85 and renders bare. Two of the four miss kinds therefore
carry shell chrome and two do not, for the same viewer. Add a comment on the describe block
naming that residual and pointing at the `must_haves` backstop entry that accepts it, so the next
reader does not mistake a page-level proof for a chrome-level one — and so nobody "strengthens"
the assertion into one that cannot pass.

Do not add these to `admin.spec.ts`. Do not quarantine any of them — quarantine is for
flakiness with an open issue and no reproducible cause, never for a real failure.
  </action>
  <verify>
    <automated>task test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `ls web/e2e/admin-portal.spec.ts` exists and `git diff --exit-code web/e2e/admin.spec.ts` exits 0 (the telnet spec is untouched).
    - `rg -n 'viewport: *\{ *width: *375' web/e2e/admin-portal.spec.ts` matches, **and** `rg -n 'viewport: *\{ *width: *780' web/e2e/admin-portal.spec.ts` matches — the previously untested 768–815px band is covered.
    - **The 375px block navigates between two admin sections from inside the opened drawer** (cross-AI review H-2). It clicks the TopBar hamburger, asserts both the `Workspace` and `Admin` group labels inside the opened `SheetContent`, clicks one admin section entry and asserts the URL changed, then reopens and clicks a **second, different** entry and asserts the URL changed again. Removing the `adminNavStore` read from `SectionRail.svelte` makes it FAIL — demonstrate and record. Without this the 375px block passes while the promised `Admin` group is entirely absent, which is exactly how C2-34 stayed open through a full review cycle.
    - The 375px block asserts **exactly one** element on the page carries the accessible name `Open navigation` — two would mean a duplicate hamburger was built beside the shipped `TopBar.svelte:60` one (cross-AI review L-1) — and that navigating to a non-admin authed route then reopening the drawer shows **no** `Admin` label, which is `adminNavStore`'s clear-on-teardown path (06.1-02 flagged assumption 4) and the half no component test can reach.
    - **The component tests this plan adds are named `*.svelte.test.ts`** (cross-AI review H-1): `EditCharacterSheet.svelte.test.ts` and `LifecycleConfirmDialog.svelte.test.ts` exist; `EditCharacterSheet.test.ts` and `LifecycleConfirmDialog.test.ts` do **not**. `byteCount.test.ts` correctly stays `.test.ts` — it is a pure function that mounts nothing. `git diff --exit-code web/vite.config.ts` exits 0, pinning the project routing (`:15-33`) these names depend on.
    - The spec asserts the Sheet is a direct child of `document.body` and **not** a descendant of `.shell`, and asserts the overlay's bounding rect has `top === 0` and `height === window.innerHeight` (±1px) plus a dismissing click inside the topbar band. These are the assertions that observe "the modal geometry is viewport-relative"; the previous revision's portal-containment assertion observed the opposite property and would have passed while the topbar sat clickable behind an open modal.
    - The 375px row-tap assertion targets the `Status` or `Last active` cell, **not** the `Name` cell; `rg -n "getByRole\('cell'|Last active|Status" web/e2e/admin-portal.spec.ts` shows the tap locator resolving outside the primary cell. A tap on the `Name` cell hits 06.1-03's button directly and passes whether or not its `::after` overlay spans the row.
    - The SUMMARY records **three** demonstrated REDs with their observed failure text: `84vh` replaced by `50vh` (height fails, `data-side` still passes); the derived `side` replaced by the literal `"right"` (`data-side` fails, height and font-size still pass); and the indistinguishability negative control (a distinct denial string failing the opacity assertion). The first two are a matched pair proving the phone treatment has two independent mechanisms — one CSS block could never have carried both.
    - Proof 4's describe block carries a comment naming the chrome-level residual and citing the `must_haves` backstop that accepts it, and its assertions are scoped inside the `+error.svelte` subtree rather than over the whole page.
    - The byte-cap block covers **99/100/101 for the short-cap field and 3999/4000/4001 for the long-cap field**, with a separate CJK string crossing each respective cap. A block asserting 101 is rejected on the 4000-byte field is wrong and would fail against correct server behaviour.
    - The no-refetch assertion uses request interception and asserts a list-request count of **zero** between the mutation response and the row update — an assertion that the row merely changed would pass under a refetch.
    - `task test:e2e` passes; `rg -n 'quarantine|@quarantine' web/e2e/admin-portal.spec.ts` returns zero matches.
  </acceptance_criteria>
  <done>The phone band, the byte-cap agreement, the no-refetch mutation loop and the per-viewer indistinguishability are each proven by a browser-level assertion, and two of them are demonstrated RED before their green is trusted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input → `AdminUpdateCharacter` | Typed values crossing to an audited write on a character the operator does not own. |
| the confirm dialog → the operator's understanding | The copy is the only thing telling the operator what retiring actually does; a false claim here is a real harm even though nothing technical breaks. |
| the rendered `/admin/*` surface → an unauthorised viewer | Every distinguishable state is a bit about whether the viewer is an admin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-53 | Tampering | the status control wired to a `status` field, putting a lifecycle value on a mutation wire | high | mitigate | The picker routes to a confirm and the confirm sends `AdminRetireCharacter`/`AdminUnretireCharacter`; a Vitest case asserts selecting a transition makes zero RPC calls; the control's own footnote names the RPC. |
| T-06-54 | Tampering | a lost update from a stale `version` swallowed as a generic failure | high | mitigate | `Aborted` is classified by the reused `isAbortedError`; the Sheet stays open, the alert names both versions and says the changes were not applied, and no toast fires. |
| T-06-55 | Information Disclosure | a wrapped server error or internal code rendered in the Sheet | high | mitigate | Failures resolve to the authored union; the `e.message` idiom is asserted absent; the only server-supplied values on screen are two version integers. |
| T-06-56 | Information Disclosure | the phone-band rules silently no-op'ing, hiding a whole band from every test | high | mitigate | The rules are viewport `@media` at the same literal the `matchMedia` derivation reads, so there is no ancestry condition for them to fail; the E2E asserts computed height (`84vh`) and font-size at 375px, demonstrated RED by swapping `84vh` for `50vh`. |
| T-06-56b | Information Disclosure | the phone band never rendering as a bottom sheet at all, because a CSS rule was specified to flip a Svelte **prop** it cannot reach — so `data-side` stays `right` while the build is green and every desktop test passes | high | mitigate | `side` is derived in Svelte from `window.matchMedia('(max-width: 767px)')`; a Vitest case stubs `matches` and asserts `data-side` both ways; the E2E asserts `data-side="bottom"` at 375px and `"right"` at 780px; and a **matched pair** of demonstrated REDs (change `84vh` → only the CSS half fails; hard-code `side="right"` → only the attribute half fails) proves the two mechanisms are independent. |
| T-06-56e | Information Disclosure | the modal overlay covering only part of the viewport, leaving the 44px topbar undimmed and **clickable behind an open modal** — a navigation surface reachable while a confirmation is pending | high | mitigate | The Sheet is not portalled: it keeps its `document.body` target, so `position: fixed` resolves against the viewport, and 06.1-02 introduces no `container-type` anywhere in the authed subtree that would rebase it. The E2E asserts the overlay's rect is `top === 0, height === innerHeight` and that a click inside the topbar band dismisses the Sheet rather than reaching the topbar. |
| T-06-56c | Tampering | the edit Sheet seeded from list rows that carry no profile prose, rendering 13 blank inputs and **overwriting existing player-authored content** on the first save | critical | mitigate | The Sheet fetches `AdminGetCharacter`'s `AdminCharacterDetail` on open (plan 06-04) with skeleton rows and a `disabled` Save until it resolves; a rejected fetch keeps Save disabled behind retry copy rather than presenting an empty editable form. Asserted by three Vitest cases, one of which fails outright for a row-seeded implementation. |
| T-06-56d | Tampering | a second byte-counting implementation drifting from the first, so one editor's counter disagrees with the server the other agrees with | medium | mitigate | `byteCount` is extracted to `web/src/lib/text/byteCount.ts` and imported by both `ByteCounter.svelte` and the admin Sheet; `rg -l 'new TextEncoder\(\)\.encode' web/src --glob '!*.test.ts' --glob '!*.test.svelte.ts'` lists exactly one file; Phase 5's three test files keep their independent oracles and stay green unmodified. |
| T-06-56f | Tampering | the client refusing to send an over-cap value, so the server's own cap enforcement is never exercised and a client/server disagreement ships undetected | medium | mitigate | Over-cap changes counter styling only; `Save changes` is disabled by exactly two conditions (unresolved/failed detail fetch, empty mask) and by no third. A Vitest case asserts Save stays enabled one byte over the cap, demonstrated RED by binding `disabled` to the over-cap predicate; Proof 2 then observes the server's `InvalidArgument`. |
| T-06-57 | Information Disclosure | an indistinguishability test that passes by construction and was never earned | high | mitigate | The negative control: a deliberately-divergent denial render must fail the opacity assertion before its green is trusted. |
| T-06-58 | Repudiation | a mutation whose outcome the operator cannot attribute to themselves | medium | mitigate | The toast names the RPC and the version transition; the row also updates in place, so a missed toast loses nothing. |
| T-06-59 | Elevation of Privilege | an `Undo` path that sends a status value rather than the un-retire RPC | medium | mitigate | `Undo` calls `AdminUnretireCharacter` with the response's new `expected_version`, through the same code path the picker uses; asserted by Vitest. |
| T-06-60 | Tampering | a client byte counter disagreeing with the server's caps, producing a save that looks valid and is refused | medium | mitigate | `byteCount` uses `TextEncoder`; the E2E proves agreement at 99/100/101 bytes and on a CJK value against the real server. |
</threat_model>

<artifacts_this_phase_produces>
- `web/src/lib/text/byteCount.ts` (`byteCount`) and `byteCount.test.ts` — **extracted** from the
  existing expression at `ByteCounter.svelte:34`, which is changed to import it. Not a new
  implementation.
- `web/src/lib/components/admin/EditCharacterSheet.svelte` and `EditCharacterSheet.svelte.test.ts`.
- `web/src/lib/components/admin/LifecycleConfirmDialog.svelte` and
  `LifecycleConfirmDialog.svelte.test.ts`.
- `web/src/lib/admin/client.ts` — `updateAdminCharacter`, `retireAdminCharacter`,
  `unretireAdminCharacter` wrappers.
- `web/src/routes/(authed)/admin/characters/+page.svelte` — the D-110 mutation loop wiring.
- `web/src/routes/(authed)/admin/+layout.svelte` — the single `<Toaster />` mount. **Nothing
  else**: no exported element reference and no container declaration; 06.1-02 owns that file's
  frame and introduces neither (cross-AI review C2-41, which flagged this entry as claiming an
  artifact the revised 06.1-02 does not produce).
- `web/e2e/admin-portal.spec.ts` — the four browser-level proofs. It is the **20th** spec in
  `web/e2e/` (19 exist at HEAD — cross-AI review L-4), and it also carries this phase's only
  proof that the `<768px` drawer's `Admin` group is navigable (cross-AI review H-2) and that
  exactly one `Open navigation` control exists (cross-AI review L-1).
</artifacts_this_phase_produces>

<ui_considerations_dismissed_here>
Two of the UI contract's three dismissals belong to this plan. Recorded, not dropped; neither
becomes a truth:

- **long-text, overflow — toast copy (interactive-control)** → dismissed. Toast strings
  interpolate only an RPC name (an authored constant), a mask count (integer), two version
  integers, and a character name bounded server-side at 32 runes. Nothing can run long or
  overflow.
- **error — the `Undo` action inside the toast (interactive-control)** → dismissed. A failed
  `Undo` is a failed `AdminUnretireCharacter`, already contracted by the
  `Couldn't change this character's lifecycle. Try again.` row. It does not need a second,
  toast-local failure state; the row simply does not change.

The third (the command palette on `/admin/*`) belongs to plan 06.1-01 and is recorded there.
</ui_considerations_dismissed_here>

<verification>
- `cd web && pnpm exec vitest run && pnpm exec svelte-check --threshold error` green.
- `task test:e2e` green (Docker compose stack).
- `task lint` green; `task fmt` run and edits committed.
- `task pr-prep` green before push (run inline in the parent, not delegated, per the repo's
  final-gate rule).
- `/holomush-dev:review-abac` before push — the phase touches `internal/access`.
</verification>

<review_dispositions>
Cross-AI review findings owned by this plan.

| Ref | Finding | Severity | Disposition |
|---|---|---|---|
| R-44 | A container query **cannot** flip the Sheet's `side`: it is a Svelte prop (`sheet-content.svelte:18,25`) emitted as `data-side` (`:36`), so CSS can restyle `[data-side=right]` but can never mutate the attribute or select the bottom transition classes. The E2E asserting `data-side="bottom"` could not pass under its own specified mechanism, and retargeting the portal fixes ancestry only. | HIGH (codex, orchestrator-verified) | **INCORPORATED; mechanism simplified again in cycle 3.** `side` is derived in Svelte — now from `window.matchMedia('(max-width: 767px)')` rather than a `ResizeObserver` on a container element, since 06.1-02 no longer declares one (C2-02) and the viewport is what both halves are actually about. CSS keeps only the `84vh` height and the 16px font, in an `@media` block reading the same literal in the same file. The E2E asserts `data-side` **and** computed geometry, and a **matched pair** of demonstrated REDs proves the mechanisms are independent — changing `84vh` fails only the CSS half, hard-coding `side` fails only the attribute half. Threat `T-06-56b`. |
| R-21 | The edit Sheet has no data source: `AdminCharacter` carries no prose, yet the plan requires opening from row data, issuing no fetch, and rendering all 13 current values. Fields would seed blank and overwrite existing content. | HIGH (codex) | **INCORPORATED — both halves.** 06-04 now returns `AdminCharacterDetail` with `description` plus the twelve `profile.*` values; this Sheet **fetches it on open** with an honest loading state: header and Group 1 immediate from the row, Group 2 skeletons, Save `disabled` until resolved, retry copy on failure with Save still disabled. The must-have "issues no fetch, so it has no loading state" is **inverted** rather than quietly kept. Threat `T-06-56c`. |
| R-33 | The long-cap E2E boundary is wrong: short fields cap at `MaxNameLength` (100), long fields at `MaxDescriptionLength` (4000); asking both to reject 101 bytes cannot pass. | HIGH (codex) | **INCORPORATED.** Two triples — 99/100/101 and 3999/4000/4001 — with a separate CJK string crossing each respective cap, in both the unit tests and the E2E, and the criterion states that 101 must be **accepted** on the long field. Mirrored in 06-05's server-side table. |
| R-45 | 06.1-04 mints a second byte-counting implementation: `ByteCounter.svelte:34` already has `new TextEncoder().encode(value).length` with the same rationale; the plan mandates search-before-writing for `lastActive.ts` but not for `byteCount.ts`. | MEDIUM (opencode) | **INCORPORATED.** The function is **extracted** to `web/src/lib/text/byteCount.ts` (neutral location — a player component and an admin one both import it) and `ByteCounter.svelte` is changed to call it. The two **display** components stay distinct, since their contracts genuinely differ. `rg -c 'TextEncoder' web/src/` totals exactly 1, and Phase 5's shipped tests must stay green unmodified. Threat `T-06-56d`. |
| R-46 | The 768–815px band is untested anywhere: the plans verify 1200px and 900px manually and 375px in Playwright. | LOW (opencode) | **INCORPORATED.** A 780px `test.describe` asserts the container/viewport width equality, a visible non-zero rail, the merged admin nav treatment, and `data-side="right"`. Combined with 06.1-02's container move, this is the band where a nested container would have been 48px out of step with nothing catching it. |
| R-47 | The toast-copy and `Undo`-failure dismissals. | — (plan's own `<ui_considerations_dismissed_here>`) | **UNCHANGED.** Neither reviewer disputed either dismissal, and both rationales still hold. |

**Convergence cycle 2.**

| Ref | Finding | Severity | Disposition |
|---|---|---|---|
| C2-08 | **[REGRESSION-FROM-FIX]** The new containment boundary and the portal target jointly rebase fixed Sheet geometry: 06.1-02 makes `.shell` a containing block, 06.1-04 deliberately portals `Sheet.Content` and its overlay (both `position: fixed`) into it, and `.shell` is `calc(100vh - var(--topbar-h))` tall. Consequences: the overlay covers the shell not the viewport (topbar undimmed and clickable behind a modal); percentage height resolves against viewport-minus-topbar so the E2E's "≈84% of viewport" conflicts with the `84%` rule; and "backdrop tap works at every band" never tests the uncovered topbar area. | HIGH (codex) | **INCORPORATED — the portal retarget is removed along with the container.** 06.1-02 no longer creates a containing block (C2-02), so the only reason `portalProps` existed — making an `@container vp` block match — is gone. The Sheet keeps its generated `document.body` target and its fixed geometry resolves against the viewport. The height is expressed in **`84vh`**, not `%`, so the declared quantity and the asserted quantity are the same by construction. The E2E gains two assertions the previous shape could not have passed: the overlay's rect is `top === 0, height === innerHeight`, and a click inside the 44px topbar band dismisses the Sheet. `portalProps` is now grep-asserted **absent**. Threat `T-06-56e`. |
| C2-09 | **[REGRESSION-FROM-FIX]** The `TextEncoder` count criterion is unsatisfiable under the same task's own constraint: `rg -c 'TextEncoder' web/src/` "totals exactly 1" while the task also requires three shipped Phase 5 test files that contain `TextEncoder` to stay unmodified. The literal appears six times across five files at HEAD; a correct extraction leaves six. | HIGH (opencode B1) / MEDIUM (opencode B2) | **INCORPORATED.** Replaced with `rg -l 'new TextEncoder\(\)\.encode' web/src --glob '!*.test.ts' --glob '!*.test.svelte.ts'` listing exactly one file. Verified at HEAD: that command lists exactly one file today (`ByteCounter.svelte`), so the extraction **moves** the single implementation rather than adding one, and a second copy in the Sheet makes it list two — a demonstrated RED. A new truth states why the three test oracles are not second implementations (a test that recomputes the expected value independently is what makes the test non-vacuous; collapsing it onto the function under test would make it agree by construction). The `rg -c … totals exactly N` shape is retired from this plan: `rg -c` prints per-file counts and exits 1 on zero matches, so "totals" is a summing instruction no shell performs. |
| C2-37 | `rg -c '<Toaster' web/src/` "totals exactly 1" is the same unsatisfiable shape. | LOW (opencode) | **INCORPORATED.** Replaced with `rg -l '<Toaster' web/src \| wc -l` printing `1`, with the file named. Verified at HEAD: `<Toaster` appears zero times, so a second mount is the only way to exceed one. |
| C2-38 | `rg -n 'grabber\|grab-handle\|handle'` zero-match bans the bare substring `handle`, which any conventionally named `handleSave` / `handleOpenChange` trips — failing a natural correct implementation. | LOW (opencode) | **INCORPORATED.** Narrowed to `grabber\|grab-handle\|drag-handle`, with the reason recorded inline so it is not re-widened, plus a Vitest case asserting the **property** (no drag affordance or `role="separator"` above the header) rather than a name for it. |
| C2-39 | Indistinguishability holds at page level but not at chrome level, and Proof 4 asserts only page level: an `/admin/*` denial renders inside `(authed)`'s shell and rail (that layout's load succeeded) while an unknown `/c/[id]` renders bare, so for one viewer two miss kinds carry chrome and two do not. | MEDIUM (opencode) | **INCORPORATED as an explicit, reasoned accept — not silently.** A `verification: backstop` truth names the residual, its mechanism (SvelteKit renders every layout above the failed segment), and why closing it is out of budget (it would mean either a chrome-less boundary for every miss or rendering shell chrome on a route the viewer has no session for). Proof 4 is scoped inside the `+error.svelte` subtree and carries a comment pointing at that entry, so a later reader neither mistakes it for a chrome-level proof nor "strengthens" it into an assertion that cannot pass. |
| C2-40 | Proof 2 silently assumes an over-cap field can still be saved, but the natural implementation — matching `ByteCounter`'s `over` contract to a `disabled` binding — makes the proof unreachable. | LOW (codex) | **INCORPORATED.** The action states the rule directly ("over-cap display never disables Save; the server is the enforcer"), enumerates the exactly-two conditions that do disable Save, and adds a Vitest case demonstrated RED by binding `disabled` to the over-cap predicate. Threat `T-06-56f`. |
| C2-41 | `<artifacts_this_phase_produces>` claims the admin layout carries "the exported `vp` container element reference" when 06.1-02 put the `bind:this` and store write in `(authed)/+layout.svelte`. | LOW (opencode) | **INCORPORATED, and moot after C2-02.** Neither file carries an element reference now; the entry states the admin layout produces the `<Toaster />` mount and **nothing else**, with the negative spelled out. |
| C2-42 | The portal-target assertion and the containment boundary are the same defect seen from two sides. | — | **RESOLVED by C2-08's fix.** Both the retarget and the boundary are gone; the E2E now asserts the Sheet is a direct child of `document.body` and **not** a descendant of `.shell` — the inverse of the previous assertion, because the correct property is the inverse. |

**Convergence cycle 3 (carried into Phase 06.1 as `06.1-REVIEWS.md`).**

opencode-4b2 rated this plan **LOW risk** — *"every load-bearing claim executed true against the
tree; all eight C2 dispositions are closed with criteria that can fail"* — with only the four
citation drifts in L-4. That assessment stands; the changes below are drift repairs, one filename
class, and two obligations this plan inherited from its siblings.

| Ref | Finding | Severity | Disposition |
|---|---|---|---|
| L-4 | Four citation drifts. | LOW (opencode-4b2) | **INCORPORATED, each re-verified at HEAD rather than taken from the review.** (a) `web/e2e/` holds **19** `*.spec.ts`, not 20 — corrected. (b) `ProfileSection.svelte`'s state shape is at **`:84-96`**, not `:40-60` (which is the `Field` type and props destructure); the pointer now also names the `untrack` import at `:6` and the load-bearing rationale at `:70-83`. (c) The `errors.ts` range is now cited **by symbol**: the review said `isAbortedError` spans `:29-37`, this plan said `:28-36`, and at HEAD it is `:28-36` — a disagreement with no consequence and a citation that drifts on every edit above it, so the range is dropped rather than adjudicated. (d) The judgment-phrased `rg -n "side=" … shows side bound to a derived value` is replaced with the review's own suggested falsifiable inverse, `rg -n 'side="(right\|bottom)"' … ` returning zero — a literal `side` is the defect, so its absence is the checkable form. |
| H-1 | Component tests assigned filenames that run in the wrong Vitest project. `[REGRESSION-FROM-FIX]` | HIGH (codex-C) | **INCORPORATED.** `EditCharacterSheet.test.ts` → `.svelte.test.ts` and `LifecycleConfirmDialog.test.ts` → `.svelte.test.ts`, swept through `files_modified`, both task `<files>` lists, the `<behavior>` block and `<artifacts_this_phase_produces>`. `byteCount.test.ts` correctly **stays** `.test.ts` — it is a pure function and belongs in the server project. The criterion pins the names against `git diff --exit-code web/vite.config.ts` rather than against a passing run, because a passing run does not reveal which project ran it. |
| H-2 | The `<768px` drawer grouping has no verifying criterion; the 375px E2E asserts the mobilebar and zero-width rails, not drawer contents. | HIGH (codex-C + opencode-4a) | **INCORPORATED — this plan owns the verification half.** 06.1-02 supplies the mechanism (`adminNavStore`, a module singleton `SectionRail` reads directly, mirroring `mobileNavStore`'s existing crossing of the same boundary). Proof 1 now **opens the hamburger and navigates between two admin sections inside the opened drawer**, with a demonstrated RED (remove the store read → FAIL), plus the `Admin`-label-absent assertion on a non-admin route that covers the store's clear-on-teardown path. It also asserts **exactly one** `Open navigation` control exists, which is L-1's decision made checkable. |
| L-1 | `.mobilebar` ownership is unspecified and collides with the shipped TopBar hamburger. | LOW (opencode-4a) | **INCORPORATED — this plan owns the observable half.** 06.1-02 decides it (reuse `TopBar.svelte:60`; `.mobilebar` is breadcrumb-only, owned by `admin/+layout.svelte`). Proof 1's previous "the mobilebar renders" did not identify the element; it now names the owner and asserts the single-hamburger property, so "which control" is pinned rather than left to whichever one the locator happens to find. |
| — | **#4971 binds this plan hardest** (orchestrator). | — | **INCORPORATED as an explicit prohibition, not just an omission.** Audited-write criteria read as natural in a mutation plan, and none is present today — but "absent" is not the same as "forbidden", and the next reader adding one would be adding a criterion that cannot pass. `<phase_split_ground_truth>` states the rule and the reason (the relay publishes through a bare `JetStreamPublisher`; the projector needs an `App-Rendering` header only `RenderingPublisher` writes), and a `must_haves.prohibitions` entry carries it. The accountability this plan actually delivers is the toast naming the RPC (T-06-58) — a UI receipt, not a durable record — and the SUMMARY must say so. |
| — | **Split-seam repair (orchestrator).** `<context>` pointed at `06-CONTEXT.md` / `06-UI-SPEC.md` / `06-PATTERNS.md`; only the first exists, and this phase's copies are the ones to read. | — | **INCORPORATED.** All three repointed, plus every `read_first` reference. `<output>` moved out of Phase 6's directory. The `AdminCharacterDetail` shape R-21's repair depends on is re-verified in the shipped proto (`adminportal.proto:411-430` — `description` + `profile map<string, string>`) and recorded, so the fetch-on-open design rests on a read fact rather than on 06-04's promise. |
</review_dispositions>

<success_criteria>
- The Sheet never sends a status value and never shows `version` as a row.
- The Sheet renders the character's real current values, fetched on open behind an honest
  loading state, so a save cannot blank a field the operator never touched.
- The phone band's two properties have two mechanisms, each proven by its own demonstrated RED.
- The success and conflict paths differ exactly where D-110 says, and the row never refetches.
- The retire confirm carries all four D-108 clauses and asserts nothing untrue.
- One byte-counting function serves both editors, and both caps are proven against the server.
</success_criteria>

<output>
Create `.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-04-SUMMARY.md` when done
</output>
