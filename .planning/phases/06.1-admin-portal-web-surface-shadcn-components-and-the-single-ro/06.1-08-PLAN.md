---
phase: 06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro
plan: 08
type: execute
wave: 2
depends_on: [06.1-07]
files_modified:
  - test/meta/web_phone_band_breakpoint_census_test.go
  - web/e2e/admin-portal.spec.ts
  - web/src/lib/components/admin/CharacterTable.svelte
  - web/src/lib/components/admin/CharacterTable.svelte.test.ts
autonomous: true
gap_closure: true
gap_ids: [G-06.1-5]
requirements: [ADMIN-07, ADMIN-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The census's anti-vacuity floor is derived from the census itself: the count of BAND-RELEVANT viewport lines must be at least the total number of rule occurrences the checked-in census claims, so a walk that stops reaching half the tree fails instead of coasting on `prefers-reduced-motion` lines."
    - "Every enumerated rule is asserted by OCCURRENCE COUNT, not by substring containment, so deleting one of a file's two identical banded rules fails."
    - "The extension filter is inverted: the walk enumerates what to SKIP and fails loudly on an extension it does not recognise, so a `.tsx`, `.scss` or `.js` file added under web/src cannot slip past unscanned."
    - "A Tailwind arbitrary variant (`min-[768px]:`) and a `window.innerWidth` / `window.outerWidth` comparison are viewport-deciding lines, so a band decided in either shape is subject to the same census as an `@media` rule."
    - "`referenceDirectiveFor` returns the correct depth for a file sitting directly under web/src instead of pointing one directory too high."
    - "The admin band sweep settles by POLLING the measured width rather than by a fixed wall-clock wait, so a 180ms transition under CI load cannot be read mid-animation."
    - "The CharacterTable stylesheet scan locates its block by a stable in-source anchor rather than by a fourth textual copy of the media-condition text bound with `indexOf`, so a benign reformat of the condition does not fail it and a second sub-md block added above does not retarget it."
    - "Every one of these is proven by a mutation that makes the repaired guard RED and that the pre-repair guard passed."
    - "The census still cannot pass vacuously: a moved walk root, an empty rule census, an unrecognised extension, and a missing anchor each fail loudly."
  artifacts:
    - test/meta/web_phone_band_breakpoint_census_test.go
    - web/e2e/admin-portal.spec.ts
    - web/src/lib/components/admin/CharacterTable.svelte.test.ts
  key_links:
    - "bandedViewportRules occurrence counts -> the derived anti-vacuity floor -> the walk's band-relevant tally, so the census's reach is measured against its own claims rather than against a hand-picked number"
    - "the CharacterTable anchor markers -> the stylesheet scan's slice -> the containing-block assertion, replacing the indexOf-bound copy of the rule text"
  prohibitions:
    - "Do not weaken, delete or narrow a census assertion to make it pass; a guard that is wrong is repaired to assert the right property, never relaxed."
    - "Do not create a test file whose name begins with `+` anywhere under web/src/routes/ — SvelteKit reserves the whole prefix and `task web:build` throws while `task web:test` stays green."
    - "Do not decide whether a command succeeded by grepping its stdout; read the exit code."
    - "Do not add a `// Verifies: INV-<SCOPE>-N` annotation to any test in this plan — no registry invariant is genuinely asserted here, and a fabricated binding is a false green."
    - "Do not rewrite, renumber or delete any line of an executed PLAN.md or SUMMARY.md; corrections to executed history are append-only annotations."
    - "Do not introduce a second authored band literal under web/src outside the one allowlisted symbol."
    - "Do not invoke `go test`, `npx playwright`, `pnpm vitest` or `golangci-lint` directly where a `task` wrapper exists."
    - "Do not widen .golangci.yaml to silence a finding; use a line-scoped //nolint with a reason."
---

<objective>
Close G-06.1-5: the breakpoint census is non-vacuous over what it scans, but its
reach and its anti-vacuity floor are far weaker than its own documentation
implies, one of its per-rule assertions is decorative, and two adjacent proofs in
the same family are unstable.

Measured at HEAD: 32 lines under `web/src` match `isViewportDecidingLine`, of
which only 7 carry a band-relevant width — and all 7 are inside the two
allowlisted files. The floor is `> 15`. The census could stop reaching more than
half the tree and still report success.

Purpose: the census is the construction that keeps the band literals equal, and
therefore what ROADMAP criterion 4's "by construction" rests on after plan
06.1-07 lands. A guard whose reach is unmeasured is provenance, not construction.

Output: a self-derived floor, occurrence counting, an inverted extension filter,
two new predicate clauses, a depth fix, a polled settle, and a stable stylesheet
anchor — each with a mutation that makes it red.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-VERIFICATION.md
@.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-07-PLAN.md
@CLAUDE.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give the census a derived floor, occurrence counting, a closed extension set, two more predicate clauses, and a correct reference depth</name>
  <files>test/meta/web_phone_band_breakpoint_census_test.go</files>
  <read_first>
    - test/meta/web_phone_band_breakpoint_census_test.go (the whole file AS REPAIRED BY PLAN 06.1-07 — the exemption struct, the rem entries in `forbiddenViewportWidths` and the symbol-scoped walk branch are already there; do not re-derive them)
    - test/meta/meta_helpers_test.go (`findRepoRoot`, `skipDirs` — the shared helpers this file already uses)
    - web/src/lib/components/TopBar.svelte (the file carrying two identical banded rules in one style block, at :204 and :244)
    - .planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-VERIFICATION.md (gap G-06.1-5 `root_cause` items a-f, `missing`, `must_turn_red_on`)
  </read_first>
  <behavior>
    - Deleting either of TopBar.svelte's two banded rules fails the derivation test. (Today: green — two identical entries and a substring assertion.)
    - Adding a `.tsx` or `.scss` file under web/src carrying a forbidden width fails. (Today: green — the extension is not enumerated and the file is skipped.)
    - Adding `class="min-[768px]:flex"` or `if (window.innerWidth < 768)` under web/src fails. (Today: green — neither shape is a viewport-deciding line.)
    - A walk that reaches only part of the tree fails the derived floor. (Today: `> 15` passes on 32 lines of which 7 are band-relevant.)
    - `referenceDirectiveFor` on a path directly under web/src yields a directive with no `../` segment.
    - All three existing meta-tests still pass against the tree as it stands after plan 06.1-07.
  </behavior>
  <action>
Six changes in one file.

1. OCCURRENCE COUNTING. Give `bandedViewportRule` a third field `count int` meaning
   "how many times this exact rule text appears in this file". Collapse the two
   duplicated `TopBar.svelte` entries into a single entry with `count: 2`, and give
   every other entry `count: 1`. In
   `TestEveryBandedViewportRuleDerivesItsWidthFromTheTailwindToken`, replace
   `require.Contains(t, src, r.rule)` with an equality assertion on
   `strings.Count(src, r.rule)` against `r.count`, whose message names both numbers
   and says that a substring test cannot tell one occurrence from two. Assert
   `r.count` is positive so a zero-count entry cannot be a silent no-op.

2. DERIVED FLOOR. Replace the hand-picked anti-vacuity floor at :239. Count a second tally in
   the walk — call it `bandRelevant` — incremented when a viewport-deciding line
   carries EITHER a forbidden width OR the substring `theme(--breakpoint-`. Assert
   `bandRelevant` is at least the SUM of `count` over `bandedViewportRules`. Keep
   `scanned` as a reported number in the failure message (it is useful context) but
   stop asserting on it. Explain in the comment why the sum and not `len`: the
   census is now occurrence-counted, so its own claim about the corpus is a sum.

3. INVERTED EXTENSION FILTER. Replace the `scannedExtensions` allow-set with a
   `skippedExtensions` set — enumerate at minimum `.json`, `.html`, `.svg`, `.png`,
   `.ico`, `.webp`, `.avif`, `.woff`, `.woff2`, `.map`, `.md`, `.snap` — plus the
   scanned set `.svelte`, `.css`, `.ts`. A file whose extension is in NEITHER set
   makes the test fail with a message naming the path and the extension and asking
   the reader to classify it. Extensionless files are skipped. Say in the comment
   that the old allow-set failed OPEN: a `.tsx` added later was skipped with no
   signal and the loose floor could not notice.

4. PREDICATE REACH. Add two clauses to `isViewportDecidingLine`: a line containing
   `min-[` or `max-[` (Tailwind v4 arbitrary variants such as `min-[768px]:flex`,
   which decide a band and satisfy none of the three existing clauses), and a line
   containing `window.innerWidth` or `window.outerWidth` (a JS width comparison
   deciding the same band without a media query). Note in the comment that neither
   shape exists in the tree today, so these clauses are prospective coverage rather
   than a live escape being closed.

5. REFERENCE DEPTH. In `referenceDirectiveFor`, handle `dir == "."` — a file
   directly under `web/src` — as depth 0, yielding a directive with no `../`
   segment. Today `strings.Count(".", "/") + 1` gives 1 and produces a path one
   level above the tree. No such file exists yet; the fix is why one can be added
   without a puzzling failure.

6. Re-run the whole meta package, not only the two breakpoint tests, so a shared
   helper change is caught.
  </action>
  <verify>
    <automated>task test -- ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - `task test -- ./test/meta/` exits 0 against the unmodified tree.
    - RED proof (b, occurrence counting): delete the second `@media (width >= theme(--breakpoint-md))` block from `web/src/lib/components/TopBar.svelte`, re-run `task test -- -run TestEveryBandedViewportRuleDerivesItsWidthFromTheTailwindToken ./test/meta/` — MUST fail naming TopBar and the counts 1 and 2. Confirm the pre-task assertion passes under the same deletion. Revert.
    - RED proof (c, extension set): create `web/src/lib/components/admin/Probe.tsx` containing `@media (max-width: 767px) { .x { color: red } }`, re-run the census — MUST fail. Confirm the pre-task census exits 0 under the same file. Delete the probe file.
    - RED proof (d, predicate reach): add `class="min-[768px]:flex"` to any element in `web/src/lib/components/admin/AdminNav.svelte`, re-run — MUST fail. Revert. Repeat with `if (window.innerWidth < 768) {` added to any `.ts` under `web/src/lib` — MUST fail. Revert.
    - RED proof (a, derived floor): temporarily add `"components"` to the walk's skip set so the walk misses most of the tree, re-run — MUST fail on the derived floor. Confirm the pre-task `> 15` floor passes under the same change. Revert.
    - `referenceDirectiveFor` on a path directly under `web/src` yields a directive with no `../`: assert it in a small table-driven subtest covering depth 0, 1, 2 and 4, named per the ACE convention in `.claude/rules/testing.md`.
    - `rg -c 'require.Greater\(t, scanned' test/meta/web_phone_band_breakpoint_census_test.go` is 0 — the hand-picked floor is gone, replaced rather than adjusted.
    - `task lint` exits 0.
  </acceptance_criteria>
  <done>The census's reach is measured against its own checked-in claims, counts occurrences rather than containment, fails on an unclassified file extension, sees both non-`@media` band shapes, and computes a correct `@reference` depth at every level of the tree.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the admin band sweep's fixed wall-clock settle with a polled one</name>
  <files>web/e2e/admin-portal.spec.ts</files>
  <precondition>Docker is running and no `holomush-e2e` compose project is already up.</precondition>
  <read_first>
    - web/e2e/admin-portal.spec.ts (the band block at :376-451, its `BAND_SETTLE_MS` constant and the `columnsAt` helper; note the helper imports moved to `./helpers/admin` by plan 06.1-07)
    - web/e2e/admin-band-root-font.spec.ts (the polled width reader plan 06.1-07 established — adopt its shape rather than inventing a second one)
    - .planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-VERIFICATION.md (gap G-06.1-5 item g)
  </read_first>
  <action>
`columnsAt` sets the viewport and then waits a fixed 300 milliseconds against
a `transition: width 180ms ease` on both measured columns. A mid-animation
measurement already invalidated one UAT pass; under CI load it recurs, and the
failure looks like a real divergence.

Delete the fixed-millisecond settle constant declared at the top of that describe
block, and rewrite the two band tests to assert through
`expect.poll`, matching the reader plan 06.1-07 added in
`web/e2e/admin-band-root-font.spec.ts`: after `setViewportSize`, each column's
expected value is asserted by polling the measured width until it holds or the
poll budget expires. Keep the existing one-expect-per-column shape — "one column
collapsed and the other did not" is the exact divergence this block closes, so a
failure must still name which column diverged. Keep the `:not(.is-drawer)`
qualifier on the rail locator and every existing width and Sheet `data-side`
assertion; only the settle mechanism changes.

The relative assertions (`Math.abs(nav - rail) <= 1`, `full.nav > full.rail`)
read two values that must be measured at the SAME settled state — poll a single
evaluate that returns both widths, then assert on the returned pair, rather than
polling each column independently into a possibly-different frame.
  </action>
  <verify>
    <automated>task test:e2e -- --project=chromium admin-portal</automated>
  </verify>
  <acceptance_criteria>
    - `task test:e2e -- --project=chromium admin-portal` exits 0. Budget 10-20 minutes; run in the background and poll.
    - `rg -c 'BAND_SETTLE_MS' web/e2e/admin-portal.spec.ts` is 0 and `rg -c 'waitForTimeout' web/e2e/admin-portal.spec.ts` is 0 within the band describe block.
    - RED mutation (the guard still has teeth): change `--breakpoint-md` to `40rem` in `web/src/app.css`'s theme block, re-run the band block — MUST fail at 767/768. Revert.
    - RED mutation (settle correctness): raise the transition on `.adminnav-col` in `web/src/routes/(authed)/admin/+layout.svelte` from 180ms to 1200ms and re-run — the polled version MUST still pass, where a 300ms fixed wait would have read mid-animation. Revert. Record both outcomes in the summary; if the polled version also fails, report that rather than adjusting the assertion.
    - `task web:build` exits 0.
  </acceptance_criteria>
  <done>The band sweep's measurements are taken at a settled width by construction, and a slower transition no longer turns a correct layout into a failure.</done>
</task>

<task type="auto">
  <name>Task 3: Anchor the CharacterTable stylesheet scan in the source instead of a fourth copy of the rule text</name>
  <files>web/src/lib/components/admin/CharacterTable.svelte, web/src/lib/components/admin/CharacterTable.svelte.test.ts</files>
  <read_first>
    - web/src/lib/components/admin/CharacterTable.svelte.test.ts (the stylesheet-scan test around :225-270 — its `indexOf` bind, its brace-balance walk, and its comment-stripping step)
    - web/src/lib/components/admin/CharacterTable.svelte (the phone-band style block and the containing-block rules inside it)
    - .planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-VERIFICATION.md (gap G-06.1-5 item h and the corresponding `missing` bullet)
  </read_first>
  <action>
The test currently locates its target by `indexOf` on a verbatim copy of the
media-condition text — a fourth textual copy of a form that plan 06.1-05 already
moved once — then walks braces from the first match. Two failure modes: a benign
reformat of the condition fails the test with no indication that only the spelling
moved, and a second sub-md block added ABOVE this one silently retargets the walk.

Bracket the rules under test in `CharacterTable.svelte` with two CSS comment
markers placed INSIDE the phone-band block, immediately around the row/cell
containing-block declarations: an opening marker `phone-band-overlay:start` and a
closing marker `phone-band-overlay:end`, each written as an ordinary CSS comment
and each carrying a one-line note that a test slices between them so they are not
decoration.

Rewrite the test to slice between those two markers instead of brace-walking from
a condition match. Assert BOTH markers are present and that the start index is
less than the end index BEFORE slicing — a missing or reordered marker must fail
loudly rather than yield an empty slice that satisfies every assertion below it.
Strip comments from the slice AFTER taking it, exactly as the current test does
and for the same reason. Keep every existing assertion on the sliced text
unchanged: the containing-block rule is present on the row, absent on any cell,
and the `relativeRules` list is non-empty.

Remove the verbatim media-condition copy from the test file entirely; the test no
longer needs to know how the condition is spelled, which is the point.
  </action>
  <verify>
    <automated>task web:test</automated>
  </verify>
  <acceptance_criteria>
    - `task web:test` exits 0.
    - `rg -c 'breakpoint-md' web/src/lib/components/admin/CharacterTable.svelte.test.ts` is 0 — the fourth textual copy is gone, not merely relocated within the file.
    - RED mutation (real teeth): delete the containing-block declaration from the row rule inside the markers in `CharacterTable.svelte`, re-run `task web:test` — MUST fail. Revert.
    - RED mutation (anchor integrity): delete the closing marker from `CharacterTable.svelte`, re-run — MUST fail with a message naming the missing marker, NOT pass on an empty slice. Revert.
    - Reformat resilience: rewrite the media condition in `CharacterTable.svelte` onto two lines with different internal whitespace (keeping it token-derived), re-run `task web:test` — MUST still pass, where the pre-task `indexOf` bind failed. Then re-run `task test -- -run TestEveryBandedViewportRuleDerivesItsWidthFromTheTailwindToken ./test/meta/`, which SHOULD fail because the census asserts the exact rule text — record that the census, not this test, is the thing that owns the spelling. Revert the reformat.
    - `task web:build` exits 0.
  </acceptance_criteria>
  <done>The stylesheet scan is bound to a stable in-source anchor, carries no copy of the media-condition text, and fails loudly when its anchor is missing rather than silently scanning nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → CI | Every change in this plan is inside a Go meta-test, a Playwright spec, a vitest spec, or two CSS comments. No production code path, request handler, or ABAC decision is touched. |
| CSS comments → shipped stylesheet | The two anchor markers are CSS comments; the build strips them and no selector, declaration or media condition changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-08-01 | Tampering | the census's own assertions | medium | mitigate | The recurring failure mode in this phase family is a guard relaxed until it passes. Every change here is paired with a mutation proving the repaired guard goes RED and the pre-repair guard did not; a criterion that cannot be shown red is reported as such rather than claimed. |
| T-06.1-08-02 | Repudiation | the CharacterTable anchor markers | low | accept | A future edit could move the markers to a block they do not describe. The marker-order and marker-presence preconditions catch removal and reordering; relocation to a different valid block is caught by the row/cell assertions failing. |
| T-06.1-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no package. No dependency manifest is touched, so no legitimacy gate is required. |

ASVS enforcement level 1; block-on severity high. No high or critical threat, so
no blocking checkpoint is inserted.
</threat_model>

<verification>
- `task test -- ./test/meta/` exits 0.
- `task web:test` exits 0.
- `task web:build` exits 0.
- `task test:e2e -- --project=chromium admin-portal` exits 0.
- `task lint` exits 0.
- Each of the seven RED mutations named in the acceptance criteria was run, its outcome recorded, and — where the criterion claims new coverage — the pre-repair guard was shown to pass under the same mutation.
</verification>

<success_criteria>
The census's reach is derived from its own claims rather than asserted by a
hand-picked number, and the two adjacent proofs in its family no longer fail for
reasons unrelated to the property they guard.
</success_criteria>

<artifacts_this_phase_produces>
| Kind | Symbol / path | Note |
|---|---|---|
| new Go struct field | `bandedViewportRule.count` | occurrence count per rule text per file |
| new Go var | `skippedExtensions` | replaces the fail-open `scannedExtensions` allow-set |
| new Go tally | `bandRelevant` | the derived anti-vacuity floor's numerator |
| new Go subtest | a table-driven `referenceDirectiveFor` depth case (ACE-named, depths 0/1/2/4) | inside the existing census test file |
| new CSS anchors | `phone-band-overlay:start`, `phone-band-overlay:end` | comment markers in `CharacterTable.svelte` |
| removed constant | `BAND_SETTLE_MS` | `web/e2e/admin-portal.spec.ts` |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-08-SUMMARY.md` when done
</output>
