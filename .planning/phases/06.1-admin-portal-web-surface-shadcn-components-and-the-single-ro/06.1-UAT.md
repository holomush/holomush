---
status: testing
phase: 06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro
source: [06.1-VERIFICATION.md]
started: 2026-08-15T00:45:00Z
updated: 2026-08-15T00:45:00Z
---

## Current Test

number: 1
name: The rail and the admin nav collapse at the same width (767/768 boundary)
expected: |
  Load /admin/characters as an admin at viewport width 767px, then at 768px.
  At 767px BOTH the SectionRail and .adminnav-col are zero-width.
  At 768px BOTH are non-zero.
  Neither may collapse without the other at any width.
awaiting: user response

## Tests

### 1. The rail and the admin nav collapse at the same width (767/768 boundary)
expected: At 767px both the SectionRail and `.adminnav-col` are zero-width; at 768px both are non-zero. Neither collapses without the other at any width.
result: [pending]

### 2. Decide whether Criterion 4's "by construction" is satisfied by mechanism-parity alone
expected: Either accept the current arrangement via an override, or open follow-up work to hoist the breakpoint to one source of truth (Tailwind v4 `--breakpoint-*` theme var read via `@media (width < theme(...))`, or a PostCSS custom-media) and add a 767/768 boundary assertion.
result: [pending]

### 3. Decide whether Criterion 3's "the Sheet's field set is the server's field-mask allowlist" is satisfied by a hand-maintained TS list
expected: Either accept via an override (the server rejects unlisted paths, so drift fails closed), or open follow-up work for a meta-test pinning `ADMIN_EDITABLE_FIELDS` set-equal to `adminProfileMaskablePaths`.
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
