---
phase: 06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/pnpm-lock.yaml
  - web/src/lib/components/ui/table/**
  - web/src/lib/components/ui/pagination/**
  - web/src/lib/components/ui/empty/**
  - web/src/lib/components/ui/alert/**
  - web/src/lib/components/ui/alert-dialog/**
  - web/src/lib/components/ui/avatar/**
  - web/src/lib/components/ui/breadcrumb/**
  - web/src/lib/components/ui/skeleton/**
  - web/src/lib/components/ui/select/**
  - web/src/lib/components/ui/field/**
  - web/src/lib/components/ui/sonner/**
  - web/src/routes/+error.svelte
  - web/src/routes/+error.svelte.test.ts
  - test/meta/web_error_boundary_census_test.go
  - docs/architecture/invariants.yaml
  - docs/architecture/invariants.md
autonomous: true
requirements: [ADMIN-07]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ── The single not-found boundary (UI-SPEC Layout → the not-found page; #4903) ──
    - "Exactly one +error.svelte exists under web/src/routes/, at the root; a meta-test enumerates the files and asserts the count == 1, and adding a second one turns it RED."
    # ── Viewer resolution for the destination list (cross-AI review C2-07) ──
    - "The boundary distinguishes an UNRESOLVED auth store from a resolved registered player using the SHIPPED `isPlayerAuthenticated` field, which is false in `initial` (authStore.ts:26) and set true only by setPlayerProfile. It does NOT infer resolution from `isGuest`, which is also false in `initial` and therefore indistinguishable from a resolved registered player."
    - "An unresolved store is treated as a GUEST for destination purposes — the smallest visible set, since visibleSections filters out requiresPlayer entries for guests. That is fail-safe in the correct direction: the failure mode being closed is showing the REGISTERED-player destination set before session restoration, which is T-06-17's leak. Showing fewer destinations for one frame is not a leak."
    - "No new state field is added to AuthState by this plan for this purpose. `isPlayerAuthenticated` already IS the resolution bit."
    - "The not-found renderer takes no reason parameter and echoes no path: it renders the same Not found heading, the same body, and the same destination list for a routing miss, an /admin denial, an unknown /c/[id] and any other miss."
    - "The destination list is exactly `Home` plus visibleSections(viewer) as shipped; /admin never appears in it and is never added to web/src/lib/nav/sections.ts, and SectionVisibility is not widened."
    # ── UI consideration: error — not-found page itself (static-content) ──
    - "The not-found page IS the error state — it renders no nested error branch, no reason, no path echo, and no status code."
    # ── UI consideration: loading — not-found destination list (nav, static-content) ──
    - "The not-found page never waits and never spins: it renders from the client-side `authState` store singleton, and if the viewer's section list is unresolved at render it draws the `Home` link alone. NOT from `already-resolved layout data` — that phrasing is what R-15 corrected and it survived into this must-have through two revisions. A ROOT `+error.svelte` renders OUTSIDE `(authed)` entirely (SvelteKit renders only the layouts above the boundary), `web/src/routes/+layout.ts` returns no viewer data at all, and the `(authed)` load returns only `defaultCharacterId` — so there is no layout data carrying a viewer for this component to read, and an executor sent looking for it finds nothing."
    # ── UI consideration: error — not-found destination list unavailable (nav, static-content) ──
    - "If the viewer's section list is unavailable the page degrades to `Home` only, silently — no error state, no retry control, no \"couldn't load your sections\" text."
    # ── UI consideration: overflow / long-text — not-found copy (static-content) ──
    - "The not-found copy interpolates no user-supplied text; headings and destination links wrap rather than truncate."
    # ── Component inventory constraints (UI-SPEC Design System) ──
    - "Eleven shadcn-svelte components are generated in style nova into web/src/lib/components/ui/: table, pagination, empty, alert, alert-dialog, avatar, breadcrumb, skeleton, select, field, sonner."
    - "web/package.json declares exactly one icon dependency; no @tabler/icons or @tabler/icons-svelte entry exists, and no generated component imports from @tabler."
    - "svelte-sonner is the only new npm dependency added by this plan, and its name is whatever the shadcn-svelte CLI actually pulled — confirmed against the lockfile, not assumed."
    # ── Branding / copy invariants ──
    - "No copy authored by this plan contains the literal HoloMUSH, and the not-found destination copy is the word Home."
    - "No amber (#ffb300 / --color-cursor) appears outside the cursor in any file this plan adds."
    # ── Vitest project routing (cross-AI review H-1) ──
    - "Every component-test artifact this plan adds is named `*.svelte.test.ts`. web/vite.config.ts:15-33 routes `src/**/*.svelte.test.ts` to the CLIENT project (resolve.conditions ['browser'], which is what makes `mount` work) and `src/**/*.test.ts` MINUS that glob to the SERVER project. A component test named `*.test.ts` lands in the server project without browser resolution. The shipped precedent is SectionRail.svelte.test.ts and ShellFooter.svelte.test.ts; there is no `*.test.ts` component test in the tree. Pure-function tests (no mount) correctly stay `*.test.ts`."
  artifacts:
    - path: "web/src/routes/+error.svelte"
      provides: "the single root error boundary rendering the ordinary not-found"
      contains: "Not found"
    - path: "web/src/routes/+error.svelte.test.ts"
      provides: "the three viewer-state render cases for the boundary; named .svelte.test.ts so it runs in the CLIENT Vitest project"
      contains: "isPlayerAuthenticated"
    - path: "test/meta/web_error_boundary_census_test.go"
      provides: "exactly-one +error.svelte assertion by file enumeration"
      contains: "+error.svelte"
  key_links:
    - from: "web/src/routes/+error.svelte"
      to: "web/src/lib/nav/sections.ts"
      via: "visibleSections(viewer) supplies the destination list, read-only"
      pattern: "visibleSections"

  prohibitions: []
---

<objective>
Install the eleven UI primitives the admin portal composes from, build the single root error
boundary the whole phase's indistinguishability rests on, and file the twelve owed
SPEC/ROADMAP/REQUIREMENTS amendments as issues.

Purpose: `+error.svelte` does not exist **anywhere** under `web/src/routes/` (#4903). Three
surfaces in this phase — the `/admin` deep link, the `/admin/*` denial, and an unknown route —
must all resolve to the *ordinary* not-found or `/admin`'s per-viewer indistinguishability is
lost. A second boundary destroys that property silently, in a one-file PR that looks harmless,
so the count is pinned by a meta-test that goes RED at two.

Output: eleven generated components with their icon imports normalised to `@lucide/svelte`;
one root `+error.svelte`; a file-enumeration meta-test asserting the count is exactly `1`; and
twelve GitHub issues carrying the amendments this phase's decisions owe upstream.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-CONTEXT.md
@.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-UI-SPEC.md
@.claude/rules/branding.md
@web/CLAUDE.md
</context>

<phase_split_ground_truth>
**Read this before treating any cross-plan reference as reliable.** On 2026-08-13 Phase 6 was
split; these four plans moved here unchanged from `06-03 / 06-06 / 06-07 / 06-08`. On 2026-08-14
Phase 6 **executed and closed** (29 commits, tip `ca0ace19b`). Two consequences the plan bodies
predate:

1. **`06-UI-SPEC.md` does not exist.** The UI contract is `06.1-UI-SPEC.md`, in **this** phase
   directory. Same for CONTEXT and PATTERNS: read the `06.1-` copies. `06-RESEARCH.md`,
   `06-01-SUMMARY.md`, `06-02-SUMMARY.md`, `06-04-SUMMARY.md` and `06-05-SUMMARY.md` genuinely do
   live in `.planning/phases/06-admin-portal-shell-character-administration/`.
2. **Phase 6 shipped ZERO web files.** Verified at HEAD: `web/src/routes/(authed)/admin/`,
   `web/src/lib/admin/` and `web/src/lib/components/admin/` do **not exist**. Any sentence in
   these four plans reading "as stubbed by plan 06-01" or "as landed by plan 06-01" about a
   **web** path is false — this phase creates every one of them. Backend paths cited from 06-01 /
   06-02 / 06-04 / 06-05 (`internal/`, `api/proto/`) **did** ship and are read-only here.

What Phase 6 shipped, verified in-tree: `holomush.adminportal.v1.AdminPortalService`
(`api/proto/holomush/adminportal/v1/adminportal.proto`) with eight RPCs — `AdminListSections`,
`AdminGetSection`, `AdminListCharacters`, `AdminSearchCharacters`, `AdminGetCharacter`,
`AdminUpdateCharacter`, `AdminRetireCharacter`, `AdminUnretireCharacter` — each with a
`WebAdmin*` proxy on `holomush.web.v1.WebService` (`web.proto:404-451`), the interceptor at
`internal/grpc/admin_interceptor.go:30` (`adminPortalServicePrefix = "/holomush.adminportal.v1."`),
and the placement fence `TestEveryAdminPrefixedRPCLivesInAnAdminPackage`
(`test/meta/admin_rpc_placement_test.go:70`). The generated browser client is already in the tree
(`web/src/lib/connect/holomush/adminportal/v1/adminportal_pb.ts`, and `WebAdmin*` in
`web/src/lib/connect/holomush/web/v1/web_pb.ts`).
</phase_split_ground_truth>

<shipped_defects_that_bind_this_phase>
Open issues on what Phase 6 shipped. **No plan in this phase may re-plan the backend fix**; these
exist so no acceptance criterion is written against behaviour that does not hold.

- **#4971 — admin mutations never reach `events_audit`.** The relay publishes through a bare
  `JetStreamPublisher`; the projector needs an `App-Rendering` header only `RenderingPublisher`
  writes. **No criterion in any of these four plans may assert that an `events_audit` row
  appears.** Audit-emission accountability in this phase is the toast naming the RPC, not a row.
- **#4972 — `AdminSearchCharacters` binds a charname-normalized term against the verbatim
  `players.username` column**, so a non-ASCII username silently returns **empty**. ASCII-only
  fixtures pass under the bug. Binding on plan `06.1-03`: the no-results state must not claim
  matches do not exist.
- **#4975** — seven owed ROADMAP/SPEC/REQUIREMENTS text amendments. **Do not hand-edit those
  artifacts** (rule `a32nfcekfc`); reference the issue.
- **#4974** — ADMIN-03 reads `[x]` in REQUIREMENTS.md (Phase 6 delivered its server half) while
  the traceability table routes it to Phase 6.1 as `Pending`. This phase delivers the **web**
  half. The mis-routing is tracked, not repaired here.
- **#4964 is CLOSED and the closure changes H-1's stakes.** Verified at HEAD, not assumed:
  `task web:test` exists (`Taskfile.yaml:710`), runs in both pr-prep lanes (`:1152`, `:1288`),
  **and runs in CI** — `.github/workflows/ci.yaml:353` (`run: task web:test`), inside the **Build**
  job, which is a required protect-main check. So a component test placed in the wrong Vitest
  project fails the **merge gate**, loudly. H-1 is a build breaker, not a silent gap.
</shipped_defects_that_bind_this_phase>

<!-- planner-discipline-allow: HoloMUSH, @tabler, data-table, vaul, oops.AsOops, HasPrefix, page.error, error.message, <slot -->
<!-- Each literal above is negative-grepped by an acceptance criterion AND named in an action or
     read_first body, because the action must tell the executor which brand string, package,
     API or markup shape is forbidden. Every one of those greps targets a web source, manifest
     or Go test file — never this plan.

     The last four were added in the cycle-4 pass, after the checker found a criterion that
     negative-grepped `rg ` while the same task's action instructed the executor to write the
     rationale "never `rg -c`" — and `rg -c` contains `rg `. Auditing the rest of this plan for
     the same shape surfaced `HasPrefix` (Task 2's action mandates base-name equality and names
     the predicate it rejects), `page.error` / `error.message` (the action enumerates what the
     boundary must not render) and `<slot` (read_first names the Svelte 4 shape the generated
     components must not carry).

     The general rule, stated so the next reader does not re-derive it: when a criterion asserts
     the ABSENCE of a name, the action almost always has to SAY that name to be executable, and
     the two are only compatible because the grep is scoped to a target file. Where a component
     or test genuinely needs to explain a prohibition to a future reader, put that explanation in
     the plan's SUMMARY — never inside a file the criterion greps. -->

<tasks>

<task type="auto">
  <name>Task 1: Generate the eleven components and normalise their icon imports</name>
  <files>web/package.json, web/pnpm-lock.yaml, web/src/lib/components/ui/table/**, web/src/lib/components/ui/pagination/**, web/src/lib/components/ui/empty/**, web/src/lib/components/ui/alert/**, web/src/lib/components/ui/alert-dialog/**, web/src/lib/components/ui/avatar/**, web/src/lib/components/ui/breadcrumb/**, web/src/lib/components/ui/skeleton/**, web/src/lib/components/ui/select/**, web/src/lib/components/ui/field/**, web/src/lib/components/ui/sonner/**</files>
  <read_first>
    - `web/components.json` — `style: "nova"`, `baseColor: "slate"`, ui alias `$lib/components/ui`.
    - `web/package.json` — the pinned `@lucide/svelte` version and the absence of any `@tabler`
      entry; `bits-ui` `2.18.1`; `tailwind-variants`.
    - `web/CLAUDE.md:103-160` — the component-addition command, the `cn()` requirement, the
      `var(--color-*)` colour rule, Svelte 5 runes only, `{@render children()}` not `<slot />`,
      `onclick=` not `on:click=`, `name` attributes on every form input for Playwright, and
      `type="submit"` on submit buttons.
    - `web/src/lib/components/ui/sheet/sheet-content.svelte` — an already-generated `nova`
      component, as the shape reference the new output must match.
    - `.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-UI-SPEC.md`
      → "Component inventory" (the binding constraint column) and "Explicitly declined".
    - `.claude/skills/sketch-findings-holomush/SKILL.md` and its
      `references/foundations.md` + `references/anti-patterns.md`.
  </read_first>
  <action>
From `web/`, run the shadcn-svelte CLI once for the whole set. **Pin the generator version** —
`@latest` means the same plan run a month apart emits different component code, and the lockfile
check below covers the *dependency* the CLI pulls, not the CLI itself (cross-AI review R-14).
Resolve it first, then invoke the pinned version:

1. `pnpm dlx shadcn-svelte@latest --version` — record the exact version.
2. `pnpm dlx shadcn-svelte@<that version> add table pagination empty alert alert-dialog avatar breadcrumb skeleton select field sonner`

Record the pinned version in the SUMMARY beside the resolved `sonner` package name, so a later
regeneration can reproduce this output rather than a moving target.

`alert-dialog` is the twelfth name beyond the "locked ten" and is deliberate: the retire /
un-retire confirmation MUST NOT be dismissible by a backdrop click, which is the `alertdialog`
role, and the already-installed `dialog` does not carry it. Reusing `dialog` would ship a
confirmation for an audited mutation on a character the operator does not own that a stray
backdrop tap can silently cancel.

After generation:
- **Rewrite every `@tabler/icons-svelte` import to `@lucide/svelte`** (the `empty` and `field`
  generators emit Tabler). Replace each glyph with its `@lucide/svelte/icons/<name>`
  equivalent. A second icon package MUST NOT enter `web/package.json`.
- Diff each generated file against the shape of an existing `nova` component
  (`ui/sheet/sheet-content.svelte`) and confirm the preset matches — same `cn()` usage, same
  `data-slot` attribute convention, same `$props()` runes shape. Fix any file the CLI emitted
  in a different style.
- Confirm what `sonner` actually pulled by reading `web/package.json` and `pnpm-lock.yaml`.
  The package name `svelte-sonner` is **CLI-determined, not plan-determined**; record the real
  name and version in the SUMMARY. If the CLI pulled a different package, verify it on
  `npmjs.com/package/<name>` (age, weekly downloads, source repo) before committing and record
  the check.
- Do **not** install `data-table`/TanStack Table, `drawer`/`vaul-svelte`, `progress`, `spinner`
  or `toggle-group`. Each is explicitly declined by the UI contract.
- Add the locked width override affordance now so plan 06.1-04 does not have to fight the
  generated class string: `ui/sheet/sheet-content.svelte` keeps its generated
  `data-[side=right]:sm:max-w-sm` (384px) default, and the admin Sheet passes an explicit
  `class` overriding it to the locked **380px**. Do not edit the generated default.

Run `task fmt` afterwards and commit its edits — generated files routinely need it.
  </action>
  <verify>
    <automated>cd web && pnpm exec svelte-check --threshold error && pnpm exec vitest run --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `ls web/src/lib/components/ui/` lists all eleven new directories: `table`, `pagination`, `empty`, `alert`, `alert-dialog`, `avatar`, `breadcrumb`, `skeleton`, `select`, `field`, `sonner`.
    - `rg -n '@tabler' web/src web/package.json` returns zero matches.
    - `rg -n '"@lucide/svelte"|"lucide-svelte"' web/package.json` matches exactly one icon dependency entry.
    - `rg -n 'data-table|vaul|tanstack|@tanstack' web/package.json` returns zero matches.
    - `git diff --stat web/package.json` shows exactly one added runtime dependency beyond the components (the toast runtime); the SUMMARY records its real name and version read from `pnpm-lock.yaml`, **and the pinned `shadcn-svelte` generator version** the components were emitted by.
    - `rg -n '<slot' web/src/lib/components/ui/{table,pagination,empty,alert,alert-dialog,avatar,breadcrumb,skeleton,select,field,sonner}` returns zero matches (Svelte 5 `{@render}` only).
    - `cd web && pnpm exec svelte-check --threshold error` exits 0.
  </acceptance_criteria>
  <done>Eleven `nova`-style components are in the tree, no Tabler import survives, and exactly one new npm runtime dependency was added with its real name recorded.</done>
</task>

<task type="auto">
  <name>Task 2: One root +error.svelte, and a meta-test that goes RED at two</name>
  <files>web/src/routes/+error.svelte, web/src/routes/+error.svelte.test.ts, test/meta/web_error_boundary_census_test.go, docs/architecture/invariants.yaml, docs/architecture/invariants.md</files>
  <read_first>
    - `web/src/routes/` — confirm no `+error.svelte` exists anywhere beneath it today (#4903).
    - `web/src/lib/nav/sections.ts:23,35-47,63-67` — the `as const satisfies` registry, the
      `WorkspaceSection` type name, and `visibleSections()`. **Read-only**: `/admin` MUST NOT
      be added to `SECTIONS` and `SectionVisibility` MUST NOT be widened (#4962).
    - `web/src/routes/+layout.svelte` and `+layout.ts` — what layout data is already resolved at
      the moment an error boundary renders.
    - `06.1-UI-SPEC.md` → "Layout → The not-found page (010/B)" (`:424-464`) for the exact copy,
      the four binding rules (no reason parameter, no path echo, `/admin` never in the list,
      per-viewer not global) and the "ship the negative control" instruction.
    - `06.1-UI-SPEC.md` → Typography, Color and Spacing for the token values the page uses.
    - `web/src/lib/components/shell/SectionRail.svelte.test.ts` and
      `web/src/lib/components/shell/ShellFooter.svelte.test.ts` — the **only two** component-test
      files in the tree, and both carry the `.svelte.test.ts` spelling. `web/vite.config.ts:15-33`
      is why: the client project (`resolve.conditions: ['browser']`, which is what makes `mount`
      resolve) includes **only** `src/**/*.svelte.test.ts`. Copy the spelling, not just the shape.
    - `test/meta/meta_helpers_test.go` and
      `test/meta/characteraccess_routing_census_test.go` (`setSymmetricDifference`) — the
      in-tree meta-test idioms.
    - `test/meta/` directory listing, `scripts/`, `Taskfile.yaml` and `.golangci.yaml` — the
      prior-art search required before authoring a new gate (see the action's first paragraph).
  </read_first>
  <action>
**Search before building (project rule: prefer in-tree > OSS > novel).** Before writing the
meta-test, establish and record in the SUMMARY that no existing check covers it. The search
order is: prior-phase `.planning/phases/*/` artifacts (`*-SUMMARY.md` `key_files`/`provides`),
then `test/meta/`, then `.golangci.yaml`, then `Taskfile.yaml` and `scripts/`. The expected
finding is that no in-tree mechanism enumerates web files at all — the 26 existing
`test/meta/` members are Go-AST, YAML-registry or CI-config censuses, and the nearest
*technique* analog (`setSymmetricDifference`) operates on Go AST, not the filesystem. State
what the new check proves that existing coverage does not: **that the SvelteKit error-boundary
resolution point is unique**, which no Go-AST or lint mechanism can see.

**`web/src/routes/+error.svelte`.** Render, per the UI contract, exactly:
a `@lucide/svelte` glyph; `Not found` at the Heading role (20px/600); `We couldn't find that
page.` at the Body role (14px/400, `--color-muted-foreground`); and a destination row
containing the link `Home` followed by `visibleSections(viewer)` as shipped.

Binding constraints, each of which is a rule the next reader would otherwise break:
- **No reason parameter.** The component takes no prop describing *why* it is rendering. It
  often could know (an RPC denied); if it never has the value, a future change cannot surface it.
- **No path echo.** Echoing `/admin/moderation` confirms the string routed somewhere.
- **No status code, no error message, no `$page.error.message`** rendered anywhere.
- **`/admin` is not in the destination list** and is not added to `SECTIONS`. The rail's `Admin`
  entry is drawn separately, by the rail, from `WebCheckSessionResponse.roles`.
- **It never awaits.** Read the viewer from the client-side `authState` store, exactly as
  `(authed)/+layout.svelte:18` does, and pass it to `visibleSections({ isGuest })` — whose
  signature requires that explicit argument (`sections.ts:63`). **Be precise about where the
  viewer comes from**, because "already-resolved layout data" is not accurate here and would send
  the executor looking for something that does not exist (cross-AI review R-15):
  `web/src/routes/+layout.ts` returns **no** viewer data, the `(authed)` load returns only
  `defaultCharacterId`, and a **root** `+error.svelte` renders *outside* the `(authed)` layout
  entirely — SvelteKit renders only the layouts above the boundary — so `(authed)` layout data is
  unavailable to it for a root routing miss. The store is a module singleton and is readable from
  either position.
- **Derive `isGuest` through the resolution bit, not from `$authState.isGuest` directly**
  (cross-AI review C2-07):

  ```
  const resolved = $derived($authState.isPlayerAuthenticated);
  const isGuest  = $derived(resolved ? $authState.isGuest : true);
  ```

  An earlier revision passed `$authState.isGuest` straight through and required an "unpopulated
  store renders `Home` alone" test. That test could not pass: `initial` is
  `{ isPlayerAuthenticated: false, isGuest: false, … }` (`authStore.ts:25-33`), so an unpopulated
  store is **indistinguishable from a resolved registered player** by `isGuest` alone, and
  `visibleSections({ isGuest: false })` returns **both** workspace entries (`sections.ts:63-67`).
  Worse than failing the test: the initial render would expose the registered-player destination
  set before session restoration — exactly the correlation T-06-17 exists to close.

  **No new state field is needed.** `isPlayerAuthenticated` already is the resolution bit: it is
  `false` in `initial` and set `true` only by `setPlayerProfile`, which every resolution path
  calls — including for a guest, who resolves to `{isPlayerAuthenticated: true, isGuest: true}`.
  Adding a parallel `profileResolved` flag would be a second source of truth for the same fact.
  Do **not** add one, and do not modify `authStore.ts` from this plan.

  The direction of the fallback matters and is deliberate: unresolved is treated as **guest**,
  the *smallest* destination set, because `visibleSections` filters `requiresPlayer` entries out
  for guests. Showing fewer destinations for one frame is not a leak; showing the registered set
  to an unauthenticated viewer is.
- If the store is unresolved (a miss before session restore has run), draw the `Home` link
  **alone**, silently — no spinner, no retry, no "couldn't load your sections" text. A page that
  blocks on a request, or flickers its destinations in, makes render timing a per-viewer signal.
  This graceful degradation is what makes the store's timing immaterial rather than a leak.
- Headings and destination links **wrap**; nothing truncates.
- Copy contains no `HoloMUSH` (INV-6) and the back target is the word `Home`.
- Colour comes from `var(--color-*)`; no hardcoded hex; no amber.

**`web/src/routes/+error.svelte.test.ts` — this plan owns the test file, so name it** (cross-AI
review M-3). The three viewer-state render cases below had no artifact: the frontmatter listed the
component, the Go census and the invariant documents, and nothing else. Create
`web/src/routes/+error.svelte.test.ts` and put all three cases in it.

**The `.svelte.test.ts` spelling is load-bearing, not stylistic** (cross-AI review H-1).
`web/vite.config.ts:15-33` declares two Vitest projects: the **client** project includes only
`src/**/*.svelte.test.ts` and sets `resolve.conditions: ['browser']`; the **server** project
includes `src/**/*.test.ts` and *excludes* `src/**/*.svelte.test.ts`. A component test named
`+error.test.ts` therefore runs in the server project without browser resolution, where `mount`
does not work. Both shipped component tests use the long spelling. And this is a merge-gate
concern, not a local-only one: `task web:test` runs in CI at `.github/workflows/ci.yaml:353`,
inside the required **Build** job (#4964, verified at HEAD).

**`test/meta/web_error_boundary_census_test.go`.**
`TestExactlyOneSvelteKitErrorBoundaryExistsUnderWebRoutes` walks `web/src/routes/` with
`filepath.WalkDir`, collects every path whose base name is `+error.svelte`, and asserts
`require.Len(found, 1)` **and** that the single member's path is exactly
`web/src/routes/+error.svelte`. Enumerate files and assert the length; a line-counting shell-out
would count matching *lines* and would also pass at zero (V11). Guard the walk root's existence
with `require.DirExists` so a moved directory fails loudly instead of yielding an empty slice.

**Keep that rationale in the SUMMARY, not in the test file's doc block** — the same discipline
plan 06.1-02 states for its own greps. Two acceptance criteria below scan this file for the
absence of a shell-out and for the presence of `filepath.WalkDir`; a doc comment naming the tool
the test avoids would collide with the first, which is exactly how the previous spelling of that
criterion became unsatisfiable. Write *what the census proves* in the file (the shape
`admin_rpc_placement_test.go`'s "What this fence does NOT cover" block uses) and *what it was
chosen over* in the SUMMARY.

**Match the base name by EQUALITY, never by prefix.** This task now also creates
`+error.svelte.test.ts` in the same directory, so a `strings.HasPrefix(name, "+error.svelte")`
census would count two and fail against its own correct implementation. Use
`d.Name() == "+error.svelte"`. The RED demonstration below is what proves the predicate is the
equality one: if the count were 2 at green, the "add a second boundary" demonstration would show
3 and the test would already be red before the demonstration started.

Prove the check goes RED both ways before trusting its green: create a second
`+error.svelte` under a temp subdirectory of `web/src/routes/`, run the test, observe the
failure, delete it; then rename the real one, run again, observe the failure, restore. Record
both demonstrations in the SUMMARY. A count assertion that has only ever been seen green is
not evidence.

**Register the invariant as what the test actually proves, not as the behaviour it supports**
(cross-AI review C2-16). The meta-test proves exactly one thing: **the SvelteKit error-boundary
resolution point under `web/src/routes/` is unique.** It does not prove that four kinds of miss
render identically per viewer — that depends on route error mapping, store state and rendered
output, none of which a filesystem census can see. Binding the broader behavioural statement to a
`WalkDir` count would fabricate coverage the test does not have, which is the precise failure
`.claude/rules/invariants.md` calls a false-green.

So register **one** `INV-PRIVACY-<next free>` entry in `docs/architecture/invariants.yaml` whose
summary is the census property alone:

> The SvelteKit error-boundary resolution point under `web/src/routes/` is unique: exactly one
> `+error.svelte` exists, at the root. Uniqueness is a precondition for per-viewer
> indistinguishability of routing misses, not a proof of it.

`binding: bound`, `asserted_by` this test file, with a `// Verifies:` annotation on it. The
second clause is in the summary so a reader knows what the id does and does not cover.

**Do not mint a second id for the rendered-output half.** It is asserted by 06.1-04's Proof 4 (page
level, with its own demonstrated RED) and by the Vitest cases here, and 06.1-04 records the
chrome-level residual it does not cover. An invariant whose truth conditions are still being
negotiated across two plans is not ready for the registry; leaving it unregistered is honest,
whereas registering it against a census is not. Regenerate with `go run ./cmd/inv-render`.
  </action>
  <verify>
    <automated>task test -- ./test/meta/ -run TestExactlyOneSvelteKitErrorBoundary && cd web && pnpm exec vitest run --project client src/routes/+error.svelte.test.ts && pnpm exec svelte-check --threshold error</automated>
  </verify>
  <acceptance_criteria>
    - `find web/src/routes -name '+error.svelte'` returns exactly one path, `web/src/routes/+error.svelte`.
    - **The component test is named `+error.svelte.test.ts`, so it runs in the CLIENT Vitest project.** `ls web/src/routes/+error.svelte.test.ts` exists and `ls web/src/routes/+error.test.ts` does **not**. `git diff --exit-code web/vite.config.ts` exits 0 — the project routing this filename depends on is unmodified, so the criterion pins the name against the config that gives it meaning rather than against a passing run. Renaming the file to `+error.test.ts` moves it into the server project (`vite.config.ts:15-21`), where `mount` has no browser resolution: run it under that name once, observe the failure, restore, and record. (cross-AI review H-1; the gate is real — `task web:test` runs in CI's required Build job, `.github/workflows/ci.yaml:353`.)
    - The meta-test matches the base name by **equality** (`d.Name() == "+error.svelte"`), not by prefix: `rg -n 'HasPrefix|Contains\(.*\+error' test/meta/web_error_boundary_census_test.go` returns zero matches. A prefix predicate would also match this task's own `+error.svelte.test.ts` and count two.
    - The meta-test asserts `require.Len(found, 1)`; `rg -n 'GreaterOrEqual|>= 1|NotEmpty' test/meta/web_error_boundary_census_test.go` returns zero matches on the count assertion.
    - **The test enumerates rather than shelling out, pinned by a collision-proof positive/negative pair.** `rg -n 'filepath\.WalkDir' test/meta/web_error_boundary_census_test.go` **matches**, and `rg -n '"os/exec"' test/meta/web_error_boundary_census_test.go` returns **zero** matches — a shell-out needs that import, and the quoted import form is not something a doc comment writes.
      **The previous spelling, `rg -n 'exec.Command|rg |grep ' …` returning zero, was self-tripping** (cycle-4 checker finding 3). The alternation contains the literal `` rg `` **with a trailing space**, and this task's own action tells the executor to record why the census enumerates — "never `rg -c`, which counts matching *lines*". `rg -c` **contains** `rg `, so the moment the executor writes the rationale the repo's meta-test convention asks for, the criterion fails. Same for `grep ` in any sentence explaining that the test does not grep. This is the recorded "absence-grep versus the doc block that mandates the name" variant, live in this plan for a fourth time — plan 06.1-02 guards against it explicitly in its `planner-discipline-allow` note and plan 06.1-03 caught the identical shape in its no-existence-claim pattern.
    - The SUMMARY records the two demonstrated RED runs (second file added → FAIL; real file renamed → FAIL) with the observed failure text.
    - `rg -n 'page.error|status|\$page\.url|error\.message' web/src/routes/+error.svelte` returns zero matches.
    - `rg -n 'authState' web/src/routes/+error.svelte` matches and `rg -n 'visibleSections' web/src/routes/+error.svelte` matches — the viewer comes from the store and is passed explicitly, not read from `(authed)` layout data the root boundary cannot see.
    - `rg -n 'isPlayerAuthenticated' web/src/routes/+error.svelte` matches — the resolution bit is consulted. `rg -n 'visibleSections\(\{ *isGuest: *\$authState\.isGuest' web/src/routes/+error.svelte` returns **zero** matches: `isGuest` is never passed straight through, because `initial` has `isGuest: false` and an unresolved store would then read as a resolved registered player (cross-AI review C2-07).
    - **Three** Vitest cases, all in `web/src/routes/+error.svelte.test.ts`, pin the three viewer states, and the first is the one that would have failed under the previous design: (a) an **unresolved** store (`{...initial}`, `isPlayerAuthenticated: false`) renders the `Home` link and **exactly zero** section links, with no spinner, no retry control and no failure copy; (b) a resolved **guest** (`isPlayerAuthenticated: true, isGuest: true`) renders `Home` plus the non-`requiresPlayer` sections only; (c) a resolved **registered player** renders `Home` plus all sections. Replacing the derivation with a bare `$authState.isGuest` makes case (a) FAIL by rendering both workspace entries — demonstrate and record. That demonstration is the whole point: it is the render that would have exposed the registered-player destination set before session restoration.
    - `git diff --exit-code web/src/lib/stores/authStore.ts` exits 0 — the resolution bit is the shipped `isPlayerAuthenticated`, and no parallel `profileResolved` field is minted. (Plan 06.1-02 does modify this file, to add `roles`; this plan does not.)
    - `rg -n 'HoloMUSH|Back to|ffb300|#f' web/src/routes/+error.svelte` returns zero matches.
    - `git diff --exit-code web/src/lib/nav/sections.ts` shows the file is untouched.
    - `task test -- -run 'TestEveryRegistryInvariantHasBinding|TestBoundInvariantsAreGenuinelyAsserted' ./test/meta/` passes.
  </acceptance_criteria>
  <done>One error boundary exists, renders identically for every kind of miss with no reason and no path, degrades to `Home` alone when sections are unresolved, and its uniqueness is pinned by a test demonstrated RED in both directions.</done>
</task>

<task type="auto">
  <name>Task 3: File the twelve owed upstream amendments as GitHub issues</name>
  <files>(none — this task creates GitHub issues only and MUST NOT edit .planning/ROADMAP.md, .planning/REQUIREMENTS.md or .planning/STATE.md)</files>
  <read_first>
    - `06.1-CONTEXT.md` → `<amendments_owed>` (`:232-260`) — the seven-row table and the "file as
      issues; do NOT hand-edit ROADMAP.md / REQUIREMENTS.md / STATE.md" instruction with its
      rationale. **Its seven rows are the ones #4975 already tracks** — read #4975 first and fold
      rather than duplicate.
    - `.planning/phases/06-admin-portal-shell-character-administration/06-RESEARCH.md` → Open
      Question 4 — the eighth stale ROADMAP statement. (This file genuinely lives in Phase 6's
      directory; see `<phase_split_ground_truth>`.)
    - `test/meta/admin_rpc_placement_test.go:20-70` — the shipped fence, its **two-package**
      membership rule, and its own "What this fence does NOT cover" doc block. Amendment 11 is a
      transcription of that block; read it before writing the issue body.
    - `internal/grpc/admin_interceptor.go:18-35` — `adminPortalServicePrefix`.
    - `.planning/ROADMAP.md` → the `### Phase 6:` block, specifically the `**Requirements:**`
      line, success criterion 3, and the Sketch-findings paragraph's grab-handle sentence.
    - `.planning/REQUIREMENTS.md:194-195` (ADMIN-02) and `:207-211` (ADMIN-06).
    - Existing open issues #4963, #4903, #4905, #4899 — read each before filing so an
      amendment that belongs on an existing thread becomes a comment rather than a duplicate.
  </read_first>
  <action>
File the amendments with `gh issue create -R holomush/holomush` (or `gh issue comment` where
an existing thread already owns the amendment — #4963 already carries two of them). These are
**tool-owned generated artifacts**: do NOT hand-edit `ROADMAP.md`, `REQUIREMENTS.md` or
`STATE.md` to fix them, and do not invent a heading shape either file's parser does not emit.

The twelve (item 9 is a REPLACEMENT for a withdrawn amendment; 10 and 11 are new from
cross-AI review cycle 1; 12 is new from cycle 2):
1. `REQUIREMENTS.md` ADMIN-02 + `01-SPEC.md` §10.4 — "every admin RPC re-asserts its own gate
   … as its first statement" is superseded by D-99: the gate is asserted **once**, by an
   interceptor, over a fail-closed method→section declaration. §10.4's "the redundancy is the
   point" becomes a structural guarantee. Cite `internal/admin/section/descriptor.go` and
   `internal/grpc/admin_interceptor.go` as shipped by plan 06-01.
2. `REQUIREMENTS.md` ADMIN-06 + §10.7 — before-values are scoped **away from prose**:
   changed-attribute **names** only for the 13 allowlisted paths, real before-`status` for
   lifecycle transitions. State the erasure-safety reason (`events_audit` is retained). (D-103)
3. `01-SPEC.md` §10.7 — strike "record both"; the acting character is not recorded. (D-104)
4. `01-SPEC.md` §9.2 and §11.3 — sketch **A3**: `AdminSearchCharacters` "searches names" →
   "searches character names and player usernames". §11.3's prose-search prohibition is
   unchanged. (D-106)
5. `01-SPEC.md` §9.2/§9.3 + the §3.4 census — add `AdminListSections`, `AdminGetSection` and
   their `Web`-prefixed proxies as census members, and record that they live on the new
   `holomush.adminportal.v1.AdminPortalService` rather than on `CharacterAccessService` (plan
   06-01's settled decision). (D-100)
6. `ROADMAP.md` Phase 6 `Requirements:` line — add **PROFILE-12**; it currently lists only
   ADMIN-01..08 + EXT-01..04, so D-91's retirement half has no home
   (`05-VERIFICATION.md:176`). (D-108)
7. `ROADMAP.md` Phase 6 success criterion 3 — it says an admin mutation "writes an
   `events_audit` row in the same transaction"; §14 row 9 already amends this: the **envelope**
   is transactional, the row is **projected**. Ask that the criterion be read through that
   amendment. (D-105)
8. `ROADMAP.md` Phase 6 Sketch-findings paragraph — the grab-handle sentence ("Its grab handle
   promises drag-to-dismiss: honor it or drop it") is stale; **D-109 drops it**. This is the
   eighth statement and is not in CONTEXT.md's seven-row table.
9. **`.claude/rules/grpc-errors.md` is drifted, and the ROADMAP is already right.** An earlier
   revision of this plan filed a ninth amendment asking ROADMAP Phase 6 criterion 1 to replace
   `errutil.AssertErrorCode` with `oops.AsOops(err).Code()`. **That amendment is WITHDRAWN — it
   rested on a false premise and would have written the error into a tool-owned artifact.** The
   facts, each verified against the tree: `pkg/errutil/testing.go:15-20` implements
   `AssertErrorCode` as *literally* `oops.AsOops(err)` plus `assert.Equal(code, oopsErr.Code())`,
   so the two spellings are the **same call**, not a weak one and a strong one; under the pinned
   `samber/oops v1.22.0`, `Code()` recurses to the **deepest** code, so **both** pass under a
   double wrap and neither expresses "top-level"; and `oops.AsOops` returns **two** values, so
   the single-value spelling does not compile. `01-SPEC.md` §14 row 8 **already amended**
   PORTAL-10 rule 5 to exactly this position, and ROADMAP criterion 1 already names
   `errutil.AssertErrorCode` — the plans were arguing against a settled amendment.

   **What to file instead:** a `bug` on `.claude/rules/grpc-errors.md`, whose *"Wire opacity
   needs TOP-LEVEL code assertions"* section still asserts the false mechanism and is cited as
   authority by six v0.13 plans. Ask that it be corrected to the property that is actually
   wanted and actually assertable: **at the wire**, `status.Code(err)` plus a generic
   `status.Convert(err).Message()` with no internal code string in it, plus a differential
   two-viewer assertion; **in-process**, `errutil.AssertErrorCode` for which internal code a
   handler produced; and, where single-wrap-ness matters, an explicit unwrap-chain-depth
   assertion, because no exported oops API can read the outermost code (`OopsError.code` is
   unexported). Cross-link the existing **#4949** and **#4902**, which already own this drift,
   and comment there rather than opening a duplicate if either is the better home. Cite
   `01-SPEC.md` §14 row 8 as the settled statement the rule file must stop contradicting.

10. `01-SPEC.md` §9.2 and the §3.4 census — the three admin **character** RPCs. §9.2's census
    rows name `CharacterAccessService.AdminListCharacters / AdminSearchCharacters /
    AdminGetCharacter` (`01-SPEC.md:1980-1982`) with their `Web`-prefixed proxies as census pairs
    at `:1987-1988`. Plan 06-01's settled decision places all three on the new
    `holomush.adminportal.v1.AdminPortalService` instead, and `06-CONTEXT.md`'s
    Claude's-Discretion grant covered only the two **section** RPCs — so amendment 5 above moves
    the section RPCs but leaves the character RPCs' service unamended. Ask that §9.2 and §3.4
    move the three character rows (and their proxies) to
    `holomush.adminportal.v1.AdminPortalService`, with the same reasoning amendment 5 carries:
    the interceptor gates a service-package prefix, and widening
    `characterFacadeReceiverTypes()` would convert criterion 1's character-audience proof into a
    snapshot of two facades. Filed so the divergence is declared rather than discovered when the
    census goes RED. (06-01 Task 1, settled by the maintainer.)

11. `01-SPEC.md` §10.4 — the **limit** of the admin-placement fence, recorded so it is not
    mistaken for coverage. **Every identifier in this amendment was wrong in the previous
    revision; all four below are verified against what Phase 6 actually shipped** (cross-AI review
    M-2, corroborated independently by codex-C and opencode-4a):

    - The test is `TestEveryAdminPrefixedRPCLivesInAnAdminPackage`
      (`test/meta/admin_rpc_placement_test.go:70`) — note the `An`. The previous text said
      `...InAdminPackage`.
    - It is a **two-package** fence, not a single-package one. It walks every
      `api/proto/holomush/*/v*/*.proto` and requires each `^Admin`-prefixed rpc to live in
      **either** `holomush.adminportal.v1` **or** `holomush.admin.v1`. The second is permitted
      because `holomush.admin.v1` legitimately owns `AdminReadStream`
      (`api/proto/holomush/admin/v1/admin.proto:105`) on the **operator control plane** — served
      over a UNIX domain socket under `AssertOperatorAdmin`, never over the core gRPC server. The
      test's own header says a single-package fence would be RED at HEAD on that one rpc, and a
      guard red before the phase adds anything proves nothing.
    - The interceptor prefix is `adminPortalServicePrefix = "/holomush.adminportal.v1."`
      (`internal/grpc/admin_interceptor.go:30`). The previous text said `/holomush.admin.v1.`,
      which is the operator plane the interceptor does **not** gate.
    - The portal package is `holomush.adminportal.v1.AdminPortalService`, not `holomush.admin.v1`.

    **The amendment to file, rewritten around those facts.** The fence is a guard over a **naming
    convention**, not over **privileged semantics**. A future privileged RPC named without the
    `Admin` prefix — `PurgeCharacter` on `CharacterAccessService` — escapes both the fence and the
    `adminPortalServicePrefix` interceptor check, and nothing in this milestone catches it. Ask
    that §10.4 record that boundary, and the shape that would close it: a **declared
    authorization domain** (a proto option, or an explicit privileged-RPC registry read by both
    the interceptor and the fence) rather than a naming convention. Cite the test's own
    "What this fence does NOT cover" doc block, which already states this in-tree, so the
    amendment is transcribing a recorded limitation rather than discovering one.

    **Do NOT propose a repo-wide "every `^Admin` RPC belongs to the portal package" rule.** That
    is the single-package fence the shipped test explicitly refuses, it would be RED at HEAD on
    `AdminReadStream`, and it reopens the rename collision convergence cycle 3 settled. The
    two-package fence with reasoned membership is the settled shape.

    Label `enhancement` + `priority::medium`; it is a scoped follow-on, not a defect in what
    ships. (Cross-AI review R-06 and M-2; threat `T-06-02d`, disposition `accept`.)

12. **`01-SPEC.md` §9.2/§9.3 and the §3.4 census — the admin portal's proto PACKAGE.** New in
    convergence cycle 2. Amendments 5 and 10 both move RPCs onto a service the SPEC does not name;
    this one records **which package** and why, once, so the other two can cite it instead of each
    re-arguing it. The portal surface is
    `holomush.adminportal.v1.AdminPortalService`, in
    `api/proto/holomush/adminportal/v1/adminportal.proto`. The obvious name
    `holomush.admin.v1.AdminService` was **already occupied** at the time this phase was planned
    (`api/proto/holomush/admin/v1/admin.proto:6,19`) by the break-glass **operator** control
    plane — `Status`, `Authenticate`, `Approve`, `ResetTOTP`, `Rekey*`, `AdminReadStream` — served
    over a UNIX domain socket by `internal/admin/socket/` under `AssertOperatorAdmin`, never over
    the core gRPC server. Two services of that name in one package is a hard `buf build` failure,
    and fusing the two surfaces under one gated prefix would put a player-session ABAC boundary
    and an operator break-glass boundary behind one descriptor table. Ask that §9.2/§9.3 and the
    §3.4 census name the portal package explicitly and record the two-surface distinction, so a
    future reader does not re-derive the collision. Label `bug` + `priority::high` — the SPEC
    currently implies a placement that cannot build. (Cross-AI review C2-01, maintainer-settled;
    see 06-01's `<wire_contract_is_settled>`.)

**Title convention is binding, because the verify command depends on it.** Every issue filed by
this task is titled `Phase 6 amendment: <what it amends>` — for example
`Phase 6 amendment: ADMIN-02 / §10.4 gate is structural, not per-handler (D-99)`. Without it the
`gh issue list --search "Phase 6 amendment in:title"` verify matches nothing and passes vacuously
(cross-AI review R-12). Comments folded onto #4963 carry the same phrase in their first line.

Label each `enhancement` + `priority::medium` unless it is a correctness statement that could
mislead an implementer, in which case use `bug` + `priority::high` (items 1, 2, 7, 9, 10 and 12
qualify — each currently describes or prescribes behaviour that does not ship, or that
contradicts what ships). Cross-link the phase and the deciding `D-NN` (item 9 cites `01-SPEC.md`
§14 row 8, #4949 and #4902; items 10 and 11 cite plan 06-01, having no `D-NN`). Record every
issue number in the SUMMARY so `/gsd-verify-work` can check them.

**Also record in the SUMMARY, without filing:** `06.1-UI-SPEC.md:274-275`'s responsive table says
the character table shows "full table (5 columns)" at ≥768px while its own table section — and
plan 06.1-03 — ship **six** (`Name`, `Player`, `Status`, `Last active`, `Created`, `Ver`). The
detailed section is authoritative and 06.1-03 follows it; the stale parenthetical is a trap for the
next reader. It is a phase-local design artifact that will be archived with this phase, so it
gets a SUMMARY note rather than an issue (cross-AI review R-13).
  </action>
  <verify>
    <automated>gh issue list -R holomush/holomush --limit 30 --search "Phase 6 amendment in:title" --json number,title</automated>
  </verify>
  <acceptance_criteria>
    - `gh issue list -R holomush/holomush --limit 30 --search "Phase 6 amendment in:title" --json number,title` returns a **non-empty** list, and the SUMMARY lists all twelve amendment numbers (issue numbers for new filings, comment URLs for amendments folded into #4963 / #4949 / #4902). The title convention is what makes this command meaningful; without it the search matches nothing and the verify passes vacuously.
    - **The withdrawn amendment is not filed.** `gh issue list -R holomush/holomush --search "oops.AsOops in:title,body" --json number,title` returns no issue authored by this task asking any artifact to mandate `oops.AsOops(err).Code()` over `errutil.AssertErrorCode`, and no plan in this phase mandates that spelling. Pinned mechanically rather than by reading intent: `rg -l 'oops\.AsOops' .planning/phases/06.1-*/06.1-0*-PLAN.md` lists **exactly one** file, `06.1-01-PLAN.md` (this one, where the withdrawal is recorded), and `rg -n 'oops\.AsOops' .planning/phases/06.1-*/06.1-0[2-4]-PLAN.md` returns **zero** matches. Verified at the time of writing: 6 matches in `06.1-01-PLAN.md`, 0 across the other three. **The path was `.../06-admin-portal-shell-character-administration/06-0*-PLAN.md` in the previous revision, which after the 2026-08-13 split names Phase 6's own executed plans rather than this phase's** — an evaluable command pointed at the wrong four files. Note the sibling trap the corrected spelling avoids: `rg -l` on a zero-match set exits 1, so "lists exactly one" must be read as the file list, not as an exit status.
    - `git diff --exit-code .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md` exits 0 — none of the three tool-owned artifacts was hand-edited by this task.
    - Each filed issue body cites its deciding source and the exact artifact + section it amends: a `D-NN` where one decides it, or — for items **10, 11 and 12**, which have no `D-NN` — the named plan finding that decides it (06-01 Task 1 and its `<wire_contract_is_settled>` for 10 and 12; cross-AI review R-06 / threat `T-06-02d` for 11). **This wording replaces "Each filed issue body names the deciding `D-NN`"**, which contradicted items 10 and 11's own text saying they have no `D-NN` and should cite plan 06-01 — the executor could not satisfy both honestly (cross-AI review C2-15).
    - #4904 is NOT reopened (it is correctly closed — the D-06 ordering settles the denial-code oracle).
  </acceptance_criteria>
  <done>All twelve amendments are tracked upstream with their deciding source cited — a `D-NN` where one exists, the deciding plan finding where none does — and no tool-owned planning artifact was hand-edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SvelteKit error boundary → browser | The rendered not-found page is the only thing a denied caller sees. Any variation between kinds of miss is an oracle. |
| npm registry → the repo | Generated component code and a new runtime dependency enter the build from a third party. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Information Disclosure | a second `+error.svelte` under `(authed)` or `/admin` rendering a different page for an admin miss than an anonymous one | high | mitigate | File-enumeration meta-test asserting the count is exactly 1, demonstrated RED at two and at zero; registered as an `INV-PRIVACY` invariant with a real binding. |
| T-06-16 | Information Disclosure | the not-found page echoing the requested path or a denial reason | high | mitigate | No reason parameter on the component; grep-asserted absence of `$page.error`, `status` and `url` reads. |
| T-06-17 | Information Disclosure | render timing of the destination list correlating with permission | medium | mitigate | The page never awaits; it reads the client-side `authState` store singleton and degrades to `Home` alone, silently, when that store is unresolved. (**Not** "already-resolved layout data" — R-15's corrected phrasing; a root `+error.svelte` renders outside `(authed)` and no layout above it returns a viewer.) The resolution bit is `isPlayerAuthenticated`, and unresolved falls back to **guest**, the smallest destination set, so the closed failure mode is exposing the registered-player set before session restoration. |
| T-06-18 | Elevation of Privilege | `/admin` added to `SECTIONS` with a client-side visibility gate, widening `SectionVisibility` | high | mitigate | `web/src/lib/nav/sections.ts` is read-only in this plan and `git diff --exit-code` on it is an acceptance criterion (#4962, §10.1 Pitfall 7). |
| T-06-19 | Tampering | a typosquatted or slopsquatted toast package entering the lockfile under a CLI-determined name | high | mitigate | The package name is treated as CLI-determined, read back from `pnpm-lock.yaml`, and — if it differs from the researched `svelte-sonner` — verified on `npmjs.com/package/<name>` (age, weekly downloads, source repo) before commit. `06-RESEARCH.md`'s audit records `svelte-sonner` as created 2023-07-02 with ~487k weekly downloads and a real source repo. |
| T-06-20 | Tampering | a second icon package entering `web/package.json` via generated `@tabler` imports | low | mitigate | Imports rewritten to `@lucide/svelte`; `rg -n '@tabler' web/` asserted empty. |
</threat_model>

<artifacts_this_phase_produces>
- `web/src/lib/components/ui/{table,pagination,empty,alert,alert-dialog,avatar,breadcrumb,skeleton,select,field,sonner}/**` — CLI-generated `nova`-style components.
- `web/package.json` / `web/pnpm-lock.yaml` — one new runtime dependency (the toast runtime;
  researched name `svelte-sonner`, CLI-determined).
- `web/src/routes/+error.svelte` — the single root error boundary.
- `web/src/routes/+error.svelte.test.ts` — the three viewer-state render cases (unresolved /
  resolved-guest / resolved-registered). Named `.svelte.test.ts` so it runs in the client Vitest
  project (`web/vite.config.ts:24-32`).
- `test/meta/web_error_boundary_census_test.go` —
  `TestExactlyOneSvelteKitErrorBoundaryExistsUnderWebRoutes`.
- `docs/architecture/invariants.yaml` — one new `INV-PRIVACY-<n>` entry for error-boundary
  uniqueness.
- Twelve GitHub issues (or comments on #4963/#4949/#4902) carrying the owed amendments.
</artifacts_this_phase_produces>

<ui_considerations_dismissed_here>
The UI contract dismissed three considerations. They are recorded, not silently dropped, and
none becomes a truth:

- **nav — command palette (⌘K) on `/admin/*`** → dismissed to
  [#4962](https://github.com/holomush/holomush/issues/4962). The palette flows through
  `visibleSections()`, which this phase does not modify and to which `/admin` is never added,
  so the palette renders identically on `/admin/*` as anywhere else and discloses nothing.
  (Enforced here by the `git diff --exit-code web/src/lib/nav/sections.ts` criterion.)

The other two dismissals (toast copy overflow; the `Undo` action's failure state) belong to
plan 06.1-04 and are recorded there.
</ui_considerations_dismissed_here>

<verification>
- `cd web && pnpm exec svelte-check --threshold error` exits 0.
- `cd web && pnpm exec vitest run` green, **and `web/src/routes/+error.svelte.test.ts` ran in the
  `client` project** — confirm from vitest's own per-project output, not from an overall pass.
  A wrong-named file still "passes" the suite by running in the server project, where its `mount`
  calls fail for a reason that reads as a component bug.
- `task test -- ./test/meta/` green; the error-boundary test demonstrated RED in both directions.
- `task lint` green; `task fmt` run and its edits committed (generated components routinely need it).
- `go run ./cmd/inv-render && git diff --exit-code docs/architecture/invariants.md` exits 0.
</verification>

<review_dispositions>
Cross-AI review findings owned by this plan.

| Ref | Finding | Severity | Disposition |
|---|---|---|---|
| R-03 | The ninth owed amendment rests on a false premise: `errutil.AssertErrorCode` **is** `oops.AsOops` + `.Code()`; `Code()` returns the deepest code; the mandated spelling does not compile; and `01-SPEC.md` §14 row 8 already amended PORTAL-10 rule 5 to the opposite position (#4902), which ROADMAP criterion 1 already reflects. | MEDIUM (opencode), corroborated by codex from the wire side and orchestrator-verified | **INCORPORATED — amendment WITHDRAWN and REPLACED.** Item 9 now files a `bug` against the drifted `.claude/rules/grpc-errors.md` section, cross-linking #4949 / #4902 and citing §14 row 8, and states the property actually wanted (wire code + generic message + differential; `errutil.AssertErrorCode` in-process; unwrap-chain depth where single-wrap matters). A criterion asserts the withdrawn spelling appears nowhere in the phase. |
| R-06 | The `^Admin` placement fence is naming-based. | MEDIUM (codex) | **INCORPORATED as amendment 11** — the limitation is filed as a scoped follow-on rather than left implicit. See 06-01 R-06 for the in-tree half. |
| R-12 | `gh issue list --search "Phase 6 amendment in:title"` passes vacuously — the action mandates labels and body contents but no title convention. | LOW (opencode) | **INCORPORATED.** The title convention `Phase 6 amendment: <what it amends>` is now binding, stated as the reason the verify is meaningful, and the acceptance criterion requires the search to return a non-empty list. |
| R-13 | `06.1-UI-SPEC.md:274-275` says "full table (5 columns)" while its own table section and 06.1-03 ship six. | LOW (opencode) | **INCORPORATED as a SUMMARY note, deliberately not an issue.** The detailed section is authoritative and 06.1-03 follows it; `06.1-UI-SPEC.md` is a phase-local artifact archived with this phase, so a GitHub issue would outlive the thing it describes. The note is what stops the next reader trusting the stale parenthetical. **The file name in this row and in Task 3's action both read `06-UI-SPEC.md` until the cycle-4 pass** — a dangling path this plan's own `<phase_split_ground_truth>` declares nonexistent, executable only by the accident that the stale parenthetical sits at the same `:274-275` in this phase's copy. |
| R-14 | `pnpm dlx shadcn-svelte@latest` is unpinned. | LOW (opencode) | **INCORPORATED.** Resolve the version, then invoke the pinned version; record it in the SUMMARY beside the resolved `sonner` package. |
| R-15 | "`+error.svelte` reads already-resolved layout data" is imprecise: `isGuest` lives in the client-side `authState` store, the root layout returns no viewer data, and a root `+error.svelte` renders outside `(authed)` entirely. | LOW (opencode) | **INCORPORATED.** The action now names `authState` and the explicit `visibleSections({ isGuest })` argument, states why `(authed)` layout data is unreachable from a root boundary, and adds a Vitest case for the unpopulated-store degradation branch. |
| R-16 | 06.1-01's own must-have says the destination list is "`Home` plus `visibleSections(viewer)` as shipped". | — | **UNCHANGED and still correct.** R-15 corrects only *where the viewer comes from*, not what the list is. |

**Convergence cycle 2.**

| Ref | Finding | Severity | Disposition |
|---|---|---|---|
| C2-07 | **[REGRESSION-FROM-FIX]** The R-15 repair names `authState` but the store has no resolution bit: `initial` is `{isPlayerAuthenticated: false, isGuest: false, …}` (`authStore.ts:25-33`), so an unpopulated store is indistinguishable from a resolved registered player by `isGuest`, and `visibleSections({isGuest: false})` returns both workspace entries (`sections.ts:63-67`). The proposed "unpopulated store renders `Home` alone" Vitest case cannot pass, and worse, the initial render exposes the registered-player destination set before session restoration — contradicting T-06-17. | HIGH (codex), verified at `authStore.ts:25-33` | **INCORPORATED — using the SHIPPED resolution bit, not a new one.** The boundary derives `resolved = $authState.isPlayerAuthenticated` and `isGuest = resolved ? $authState.isGuest : true`. `isPlayerAuthenticated` already *is* the resolution bit: `false` in `initial`, set `true` only by `setPlayerProfile`, which every resolution path calls — including for a guest, who resolves to `{true, isGuest: true}`. **No `profileResolved` field is minted** (the reviewer's suggestion), because a parallel flag would be a second source of truth for the same fact; `git diff --exit-code web/src/lib/stores/authStore.ts` is an acceptance criterion for this plan. The fallback direction is deliberate and stated: unresolved → **guest**, the *smallest* set, so the one closed failure mode is the leak T-06-17 names. Three Vitest cases pin unresolved / resolved-guest / resolved-registered, with a demonstrated RED — replacing the derivation with a bare `$authState.isGuest` makes the unresolved case render both workspace entries. |
| C2-15 | The amendment acceptance criterion contradicts items 10 and 11: it requires "Each filed issue body names the deciding `D-NN`" while those items explicitly state they have no `D-NN` and should cite plan 06-01. The executor cannot satisfy both honestly. | MEDIUM (codex) | **INCORPORATED.** The criterion is rewritten to "cites its deciding source: a `D-NN` where one decides it, or — for items 10, 11 and 12 — the named plan finding that decides it", naming which finding decides each. Item 12 is new this cycle and has the same shape, so the fix covers it by construction rather than needing a third patch. |
| C2-16 | The error-boundary invariant overclaims what its binding proves: the meta-test proves exactly one `+error.svelte` exists at the root; it does not prove "four kinds of miss render identically per viewer", which depends on route error mapping, store state and rendered output. Registering the behavioural statement as `binding: bound` to a filesystem census fabricates coverage. | MEDIUM (codex) | **INCORPORATED.** The registered summary is narrowed to the census property alone — *"the SvelteKit error-boundary resolution point is unique … a precondition for per-viewer indistinguishability, not a proof of it"* — with the second clause in the summary so the id's limits travel with it. The rendered-output half is **deliberately not registered**: it is asserted by 06.1-04's Proof 4 (page level, with its own RED) and by this plan's Vitest cases, and 06.1-04 records the chrome-level residual it does not cover (C2-39). An invariant whose truth conditions are still being negotiated across two plans is not ready for the registry, and leaving it unregistered is honest where binding it to a census would be a false-green (`.claude/rules/invariants.md`). |
| C2-01 | The `holomush.admin.v1.AdminService` collision (owned by 06-01). | HIGH | **INCORPORATED here as amendment 12** — a new owed upstream amendment recording the portal's proto package, its rationale, and the two-surface distinction, so amendments 5 and 10 can cite it instead of each re-arguing it. Amendments 5 and 10 are updated to name `holomush.adminportal.v1.AdminPortalService`. The amendment count moves eleven → **twelve** and is swept through the objective, both task headings, the label rule, the acceptance criterion, `<artifacts_this_phase_produces>` and `<success_criteria>` — the count has landed partially before, so every site was checked. |

**Convergence cycle 3 (carried into Phase 06.1 as `06.1-REVIEWS.md`).**

| Ref | Finding | Severity | Disposition |
|---|---|---|---|
| H-1 | Five Svelte component tests are assigned filenames that run in the wrong Vitest project. `[REGRESSION-FROM-FIX]` | HIGH (codex-C) | **INCORPORATED, and the stakes are higher than the finding stated.** This plan's share is the `+error.svelte` render cases, which had no test file at all (M-3). The file is `web/src/routes/+error.svelte.test.ts`. The routing rule is verified at `web/vite.config.ts:15-33` and the shipped precedent is the tree's only two component tests, both `.svelte.test.ts`. **Correction to the review's framing:** the orchestrator brief said web tests are "ungated in CI" because `rg 'vitest\|svelte-check' .github/workflows/` is empty. That grep is empty but the conclusion is wrong — CI runs `task web:test` at `.github/workflows/ci.yaml:353`, inside the required **Build** job (a deliberate placement, per the comment there closing #4964). So a wrong-project component test breaks the merge gate. The criterion still pins the filename against `git diff --exit-code web/vite.config.ts` rather than against a passing run, because a passing run does not tell you *which project* ran it. |
| M-2 | Amendment 11 retains the old proto package and a now-invalid placement premise. `[REGRESSION-FROM-FIX]` | MEDIUM (codex-C + opencode-4a) | **INCORPORATED — amendment 11 rewritten around four verified identifiers.** `TestEveryAdminPrefixedRPCLivesInAnAdminPackage` (`test/meta/admin_rpc_placement_test.go:70`, note the `An`); a **two-package** fence permitting `holomush.adminportal.v1` **or** `holomush.admin.v1` (the latter legitimately owns `AdminReadStream`, `admin.proto:105`); `adminPortalServicePrefix = "/holomush.adminportal.v1."` (`admin_interceptor.go:30`); and the portal package `holomush.adminportal.v1.AdminPortalService`. The amendment now transcribes the fence's own in-tree "What this fence does NOT cover" block. The repo-wide `^Admin`-belongs-to-the-portal rule is **explicitly forbidden** in the action body — it is the single-package fence the shipped test refuses and would be RED at HEAD. |
| M-3 | This plan requires component tests without owning or naming a test file. | MEDIUM (codex-C) | **INCORPORATED.** `web/src/routes/+error.svelte.test.ts` is added to `files_modified`, Task 2's `<files>`, the action, the `<verify>` command, two acceptance criteria, `must_haves.artifacts` and `<artifacts_this_phase_produces>` — the six places the previous omission would have to be repaired in. |
| — | **Split-seam repair (orchestrator).** Every `06-UI-SPEC.md` reference in this plan was dangling: that file does not exist, the UI contract is `06.1-UI-SPEC.md` in this directory. `<output>` also wrote the SUMMARY into Phase 6's directory. | — | **INCORPORATED.** `<context>`, both `read_first` blocks and `<output>` are repointed. A new `<phase_split_ground_truth>` block states which cross-phase references are real (`06-RESEARCH.md`, the `06-0N-SUMMARY.md` set, all `internal/`/`api/proto/` paths) and which are not (every **web** path attributed to 06-01 — Phase 6 shipped zero web files, verified at HEAD). |
| — | **`+error.svelte.test.ts` is a self-inflicted census hazard** (found by grepping this plan against its own new criterion, per the REFLEX rule). | — | **CLOSED IN THE SAME EDIT.** Adding the test file next to `+error.svelte` means a `strings.HasPrefix(name, "+error.svelte")` census counts **two** and fails its own correct implementation — the exact "the repair is the defect surface" shape this phase has hit three times. The action now mandates base-name **equality** and a criterion greps `HasPrefix` to zero. |
</review_dispositions>

<success_criteria>
- Exactly one `+error.svelte` exists, and the count is mechanically pinned by an equality-matched
  file census that this task's own sibling test file does not trip.
- The boundary's three viewer-state cases live in `web/src/routes/+error.svelte.test.ts`, which
  runs in the **client** Vitest project — a merge-gated fact, since `task web:test` runs in CI's
  required Build job.
- Eleven components are installed with one icon dependency and one new runtime dependency, from a pinned generator.
- Twelve amendments are filed upstream with no hand-edit to a tool-owned artifact, each titled `Phase 6 amendment: …`, each citing a `D-NN` or a named deciding plan finding, and the withdrawn ninth is provably not among them.
</success_criteria>

<output>
Create `.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-01-SUMMARY.md` when done
</output>
