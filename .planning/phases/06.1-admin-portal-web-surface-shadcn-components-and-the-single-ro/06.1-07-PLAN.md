---
phase: 06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - test/meta/web_phone_band_breakpoint_census_test.go
  - web/src/lib/hooks/mediaQuery.svelte.ts
  - web/src/lib/hooks/mediaQuery.svelte.test.ts
  - web/e2e/helpers/admin.ts
  - web/e2e/admin-portal.spec.ts
  - web/e2e/admin-band-root-font.spec.ts
  - web/playwright.config.ts
  - web/src/lib/components/admin/EditCharacterSheet.svelte
  - web/src/lib/components/scenes/ScenesShell.svelte
  - .planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-05-SUMMARY.md
autonomous: true
gap_closure: true
gap_ids: [G-06.1-2]
requirements: [ADMIN-03, ADMIN-07]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The JS half and the CSS half of the phone band are expressed in ONE unit. `matchMedia(DESKTOP_MEDIA_QUERY).matches` equals `matchMedia('(width >= ' + <the --breakpoint-md value read off :root> + ')').matches` at every probed width, and equals the negation of `(width < <that same value>)`."
    - "That equality is asserted in a real browser at a root font size of 20px — the single axis along which the two halves can diverge, and the axis every proof this phase produced so far was blind to."
    - "The 20px root font size is itself asserted in-page before any complement assertion runs, so a launch flag that silently stops applying fails the proof instead of quietly returning it to a 16px blind spot."
    - "Inside the former divergence band (a viewport width at which the CSS token says phone), the admin shell columns and the edit Sheet's `data-side` agree: both are the phone shape, never a phone shell wearing a desktop side sheet."
    - "Correcting DESKTOP_MEDIA_QUERY to a rem value leaves the breakpoint census GREEN. The guard no longer rejects its own repair."
    - "A newly hand-written `@media (min-width: 48rem)` anywhere under web/src fails the census — the new VISIBLE spelling of these breakpoints is forbidden exactly as the px spelling is."
    - "The census's exemption is keyed to the SYMBOL `DESKTOP_MEDIA_QUERY`, not to the whole file, so a second hand-written media-query constant added beside it is an offender rather than an inherited exemption."
    - "At exactly the token width the two forms are exact complements: `(width >= theme(--breakpoint-md))` matches and `(width < theme(--breakpoint-md))` does not, and DESKTOP_MEDIA_QUERY matches — including at a fractional width, where the retired one-below-the-boundary px pair matched neither."
    - "No source or planning text still claims the px query is 'the exact complement ... at EVERY width'. Where the claim lives in executed history it is annotated, not rewritten."
    - "The census cannot pass vacuously: a walk that reaches nothing, an allowlist entry naming a file that no longer declares the exempted symbol, and a rule text that stopped appearing each fail loudly rather than yielding an empty offender list."
    - statement: "The phone band across the admin table, Sheet and drawer is verified in a browser rather than by a passing build, because jsdom applies no media query and computes no layout."
      verification: backstop
  artifacts:
    - web/e2e/admin-band-root-font.spec.ts
    - web/e2e/helpers/admin.ts
    - test/meta/web_phone_band_breakpoint_census_test.go
    - web/src/lib/hooks/mediaQuery.svelte.ts
  key_links:
    - "Tailwind's --breakpoint-md, emitted into the built stylesheet's :root,:host block -> read at runtime by the browser proof -> compared against DESKTOP_MEDIA_QUERY, which every JS consumer (EditCharacterSheet, ScenesShell) reaches through isDesktop()"
    - "DESKTOP_MEDIA_QUERY (the symbol) -> the census allowlist's symbol key -> the census staleness assertion, which now moves with the constant instead of with a px digit"
    - "the chromium-large-font Playwright project's --blink-settings launch arg -> the in-page root-font-size assertion -> every complement assertion downstream of it"
  prohibitions:
    - "Do not weaken, delete or narrow a census assertion to make it pass; a guard that is wrong is repaired to assert the right property, never relaxed."
    - "Do not create a test file whose name begins with `+` anywhere under web/src/routes/ — SvelteKit reserves the whole prefix and `task web:build` throws while `task web:test` stays green."
    - "Do not decide whether a command succeeded by grepping its stdout; read the exit code."
    - "Do not add a `// Verifies: INV-<SCOPE>-N` annotation to any test in this plan — no registry invariant is genuinely asserted here, and a fabricated binding is a false green."
    - "Do not rewrite, renumber or delete any line of an executed PLAN.md or SUMMARY.md; corrections to executed history are append-only annotations."
    - "Do not introduce a second authored band literal under web/src outside the one allowlisted symbol."
    - "Do not invoke `go test`, `npx playwright`, `pnpm vitest` or `golangci-lint` directly where a `task` wrapper exists."
    - "Do not widen .golangci.yaml to silence a finding; use a line-scoped //nolint with a reason."
---

<objective>
Close G-06.1-2. The phase converted all sixteen authored `@media` rules to
`theme(--breakpoint-md)` / `theme(--breakpoint-lg)`, which Tailwind v4 compiles
to `48rem` / `64rem`. The JS half stayed a px literal. `rem` in a media query
resolves against the browser's *initial* font size, so the two halves are
complements only at exactly 16px — and every proof this phase produced ran at
exactly 16px.

Purpose: ROADMAP criterion 4 asks that the rail's collapse and the admin shell's
collapse fire at the same moment **by construction**. One unit for both halves is
what makes that true; a proof that varies the root font size is what makes it
checkable. Two census defects have to be repaired first or the repair itself
turns the guard red.

Output: a symbol-keyed census that forbids the rem spelling and accepts the fix;
`DESKTOP_MEDIA_QUERY` in the same unit as the CSS; a Playwright project that runs
at a 20px root font size and asserts the two halves agree; and the false
"exact complement at EVERY width" claims corrected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-VERIFICATION.md
@CLAUDE.md
@.claude/rules/testing.md
@.claude/rules/search-tools.md
</context>

<tracer_note>
Task 2 is this plan's tracer: one path wired end to end — Tailwind's emitted
token, through the shared hook, through both JS consumers, to a real Chromium at
a non-default root font size. It is deliberately wider than the five-file
guideline because splitting it produces a commit in which the browser proof is
committed RED. The census repair (Task 1) is separated ahead of it precisely
because the census must be correct *before* the constant changes, and that split
leaves both commits green.
</tracer_note>

<ui_considerations_scope>
`06.1-UI-SPEC.md` `## UI Considerations` carries 32 rows. Twenty-nine were lifted
by the executed plans 06.1-01…04 and are unchanged by this repair round; they are
not re-lifted here, and that scoping is recorded rather than silent. The one row
in this plan's scope — `populated, error | phone band (<768px) across table,
Sheet and drawer | backstop` — is lifted into `must_haves.truths` above as a
structured backstop marker. The `long-text | the 13 maskable profile fields`
backstop row is in plan 06.1-09's scope and is lifted there.
</ui_considerations_scope>

<flagged_assumptions>
**Edge probe item 5 of 5 — ADMIN-07 / `unclassified` — remains UNRESOLVED.**
The probe returned `unclassified — review manually` for ADMIN-07 and none of the
four open gaps touches the permission-filtered nav's *contract* (ROADMAP
criterion 1 is MET and was regression-checked at `f48a34787`). This round changes
only the width at which the nav COLUMN collapses, never which links are drawn.
Assumption: ADMIN-07 needs no new edge coverage in this round. If that is wrong,
the missing coverage is on `AdminNav.svelte:29-44` and `admin/+layout.ts:38-53`,
not on anything this plan edits. Not silently dropped; carried as an assumption.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair the census so it forbids the rem spelling, keys its exemption to the symbol, and stops rejecting its own repair</name>
  <files>test/meta/web_phone_band_breakpoint_census_test.go, web/src/lib/hooks/mediaQuery.svelte.test.ts</files>
  <read_first>
    - test/meta/web_phone_band_breakpoint_census_test.go (the whole file — the allowlist at :81-100, the predicate at :110-118, the walk at :197-235, the staleness assertion at :253-260)
    - web/src/lib/hooks/mediaQuery.svelte.ts (the constant this exemption exists for)
    - web/src/lib/hooks/mediaQuery.svelte.test.ts (which lines are fixtures and which is the pin)
    - .planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-VERIFICATION.md (gap G-06.1-2 `root_cause` sections 2 and 3, and `must_turn_red_on`)
  </read_first>
  <behavior>
    - A newly hand-written `@media (min-width: 48rem)` in a non-allowlisted file under web/src is an offender. (Today: green — the census carries px decimals only.)
    - A second media-query constant added to `mediaQuery.svelte.ts` carrying a hand-written width is an offender. (Today: green — the exemption is keyed by whole file.)
    - Renaming or deleting `DESKTOP_MEDIA_QUERY` makes the exemption stale and fails the census with a message naming the symbol.
    - Rewriting `DESKTOP_MEDIA_QUERY` in rem leaves the census GREEN. (Today: red — `require.Positive(t, allowlistHits[path])` only counts lines carrying 767/768/1023/1024.)
    - Both existing meta-tests still pass unchanged against the rest of the tree.
  </behavior>
  <action>
Repair `test/meta/web_phone_band_breakpoint_census_test.go` in five places, then
adjust the hook's test fixtures so the symbol-scoped exemption is exact.

1. `forbiddenViewportWidths` (:79) gains the two rem spellings the tokens now
   compile to: `"48rem"` and `"64rem"`, alongside the four existing px decimals.
   Extend the doc comment above it to say why: the rem form is now the VISIBLE
   form of these breakpoints and therefore the most likely future hand-written
   duplication, and a census that forbade only the retired spelling would license
   the live one.

2. Replace the `map[string]string` shape of `breakpointLiteralAllowlist` with a
   map from repo-relative path to a new unexported struct type
   `breakpointExemption` carrying two fields: `symbol string` and `reason string`.
   Both existing entries take `symbol: "DESKTOP_MEDIA_QUERY"`.

3. In the walk body (currently :227-230), an offending line inside an allowlisted
   file is exempt ONLY when `strings.Contains(line, ex.symbol)` holds. A line in
   an allowlisted file that carries a forbidden width but NOT the symbol becomes
   an ordinary offender. Increment `allowlistHits[relSlash]` only on the exempted
   branch, so a hit now means "a line carrying the exempted symbol AND a forbidden
   width".

4. Replace the staleness assertion at :257. Keep `require.FileExists`. Then assert
   the allowlisted file's content contains `ex.symbol` (message: the exemption
   names a symbol the file no longer declares, so it is dead and must be removed),
   and keep a positive-hit assertion whose MEANING is now symbol-scoped: zero hits
   means the symbol no longer declares a width literal at all, at which point the
   exemption is genuinely stale. Say that in the failure message, and say
   explicitly that a rem literal on that line satisfies this assertion exactly as a
   px literal did — this is the sentence that stops a future reader re-deriving
   defect 3.

5. Rewrite the `mediaQuery.svelte.ts` exemption's `reason`. DELETE its second and
   third clauses — the assertion about Tailwind compiling the token away at build
   time, and the assertion that the JS side has no way to read the token at
   runtime (census test :92-94). Both are false: the built stylesheet carries
   `--breakpoint-md:48rem` and `--breakpoint-lg:64rem`
   inside its `:root,:host` block and
   `getComputedStyle(document.documentElement).getPropertyValue('--breakpoint-md')`
   returns a value at runtime. State the real reason the constant is authored:
   SSR and this jsdom have no stylesheet to read, so the non-browser path needs a
   literal; the literal is pinned to the built token by the browser proof in
   `web/e2e/admin-band-root-font.spec.ts`, and pinned to ONE place by this census.

6. In `web/src/lib/hooks/mediaQuery.svelte.test.ts`, the five `mediaQuery(...)`
   call sites that pass a band width as an ARBITRARY test INPUT (the initial-state,
   reactive-update, teardown, absent-matchMedia and explicit-fallback cases) take
   `'(min-width: 500px)'` instead. They are inputs to the hook, not declarations of
   any boundary a surface renders at, and moving them off the band leaves the two
   lines carrying `DESKTOP_MEDIA_QUERY` as the only exempted lines in the tree.
   Leave the `expect(DESKTOP_MEDIA_QUERY).toBe(...)` pin alone — Task 2 changes it
   with the constant.

Do not touch `web/src/lib/hooks/mediaQuery.svelte.ts` in this task. The tree must
be green at this commit with the constant still in px.
  </action>
  <verify>
    <automated>task test -- -run 'TestNoAuthoredViewportRuleCarriesAHandWrittenBreakpointLiteral|TestEveryBandedViewportRuleDerivesItsWidthFromTheTailwindToken' ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - `task test -- -run 'TestNoAuthoredViewportRuleCarriesAHandWrittenBreakpointLiteral|TestEveryBandedViewportRuleDerivesItsWidthFromTheTailwindToken' ./test/meta/` exits 0 with the hook constant still in px. RED mutation: none needed — this is the "the repair must not need the fix to be green" precondition.
    - RED proof A (new coverage): append a line reading `@media (min-width: 48rem) { .x { color: red } }` to `web/src/lib/components/shell/SectionRail.svelte`, re-run the census — it MUST fail and name `SectionRail.svelte` in the offender list. Confirm the SAME mutation passes on the pre-task census (`git stash` the meta-test change, re-run, observe exit 0). Revert the mutation.
    - RED proof B (symbol scope): add a second exported constant to `web/src/lib/hooks/mediaQuery.svelte.ts` declared as `'(min-width: 1024px)'`, re-run the census — it MUST fail with that file:line in the offender list rather than exempting it. Revert.
    - RED proof C (staleness): rename `DESKTOP_MEDIA_QUERY` to `DESKTOP_MQ` in the hook only, re-run — the census MUST fail with a message naming `DESKTOP_MEDIA_QUERY` and the allowlist entry. Revert.
    - GREEN-under-repair proof: temporarily rewrite the hook constant to `'(min-width: 48rem)'` and its pin in the test file to match, re-run the census — it MUST exit 0. Confirm the pre-task census FAILS under that same edit with "its exemption is stale and must be removed". Revert (Task 2 lands it for real).
    - `rg -c '767|768|1023|1024' web/src/lib/hooks/mediaQuery.svelte.test.ts` counts exactly 1 (the `DESKTOP_MEDIA_QUERY` pin line); the five arbitrary-input fixtures no longer carry a band width.
    - `rg -n 'does not emit it to :root' test/meta/web_phone_band_breakpoint_census_test.go` returns no match, and `rg -n 'provably cannot read' test/meta/web_phone_band_breakpoint_census_test.go` returns no match — the false rationale is gone, not merely qualified. RED mutation: restoring either clause makes the corresponding grep match again.
    - `task web:test` exits 0 (the fixture-width change compiles and the hook suite still passes).
  </acceptance_criteria>
  <done>The census forbids `48rem`/`64rem`, exempts only lines carrying `DESKTOP_MEDIA_QUERY`, fails loudly when that symbol disappears, states a true reason for the exemption, and is green both before and after the constant is rewritten in rem.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: One unit for both halves, proved end to end in a real browser at a 20px root font size</name>
  <files>web/src/lib/hooks/mediaQuery.svelte.ts, web/src/lib/hooks/mediaQuery.svelte.test.ts, web/e2e/helpers/admin.ts, web/e2e/admin-portal.spec.ts, web/playwright.config.ts, web/e2e/admin-band-root-font.spec.ts</files>
  <precondition>Docker is running and no `holomush-e2e` compose project is already up (`task test:e2e` refuses when one is, and its refusal is an exit-1 with an explicit message).</precondition>
  <reversibility rating="reversible">Expressing DESKTOP_MEDIA_QUERY as a rem literal rather than deriving it at runtime from the `--breakpoint-md` custom property is a one-constant change in one file; the runtime-derived form remains available later and the browser proof written here would pin either shape.</reversibility>
  <read_first>
    - web/src/lib/hooks/mediaQuery.svelte.ts (the constant, the hook, the fallback parameter)
    - web/src/lib/hooks/mediaQuery.svelte.test.ts (the pin assertion at the end of the suite)
    - web/e2e/admin-portal.spec.ts (`signInAsAdmin`, `gotoAdminCharacters`, `rowFor`, `sheet`, and the existing band block at :376-451 whose `BAND_SETTLE_MS` shape this task must NOT copy)
    - web/playwright.config.ts (the single `chromium` project, the config-level `grepInvert` that carries quarantine filtering)
    - compose.e2e.yaml (the playwright service mounts `./web` at `/work`, so `web/src` is readable from a spec via a relative URL)
    - web/src/lib/components/admin/EditCharacterSheet.svelte (`side` derives from `isDesktop(true)`)
    - .planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-VERIFICATION.md (gap G-06.1-2 `missing` and `must_turn_red_on`)
  </read_first>
  <behavior>
    - At a 20px root font size, `matchMedia(DESKTOP_MEDIA_QUERY).matches` equals `matchMedia('(width >= ' + token + ')').matches` at every probed width, where `token` is read from `:root` at runtime.
    - The same equality holds against the negated form `(width < token)`.
    - The in-page root font size is 20px — asserted before any complement assertion, so a launch flag that stops applying fails rather than silently restoring the 16px blind spot.
    - Inside the band where the token says phone, the admin rail width is 0, the `.adminnav-col` width is 0, and the Sheet's `data-side` is `bottom`.
    - Above that band all three flip together: rail > 0, nav > 0, `data-side` is `right`.
    - The pre-existing 16px boundary sweep in `admin-portal.spec.ts` still passes unchanged.
  </behavior>
  <action>
Write the proof FIRST, watch it fail against the px constant, then change the unit.

1. HARNESS. Move four symbols out of `web/e2e/admin-portal.spec.ts` into a new
   `web/e2e/helpers/admin.ts`, verbatim, exported: `signInAsAdmin`,
   `gotoAdminCharacters`, `rowFor`, and the `sheet` locator arrow. Import them back
   into `admin-portal.spec.ts` from `./helpers/admin` and delete the local
   definitions. Everything else in that spec stays local. `helpers/admin.ts` imports
   `test`, `expect`, `db`, `registerPlayer`, `createCharacter` from `./fixtures`
   exactly as the spec does today, and carries the repo's SPDX header.

2. PROJECT. In `web/playwright.config.ts` add a second project named
   `chromium-large-font` with `browserName: 'chromium'` and
   `launchOptions: { args: ['--blink-settings=defaultFontSize=20'] }`, scoped by
   `testMatch: /admin-band-root-font\.spec\.ts/`. Give the existing `chromium`
   project the matching `testIgnore: /admin-band-root-font\.spec\.ts/` so each spec
   runs in exactly one project. Set NO project-level `grep`/`grepInvert` on either
   — the config-level `grepInvert` is what carries quarantine filtering and a
   project-level one would shadow it.

3. THE PROOF. Create `web/e2e/admin-band-root-font.spec.ts` with the SPDX header and
   a header comment stating what it buys that nothing else does: every other proof
   in this tree runs at a 16px root font, where rem and px are numerically
   identical, so none of them can observe the one axis along which the two halves
   diverge.

   It reads `DESKTOP_MEDIA_QUERY` out of the hook's SOURCE TEXT rather than
   importing the module (the module is a `.svelte.ts` carrying runes and does not
   import under plain Node): resolve
   `new URL('../src/lib/hooks/mediaQuery.svelte.ts', import.meta.url)`, read it, and
   match `export const DESKTOP_MEDIA_QUERY = '<value>';`. Assert the match
   succeeded and the captured value is non-empty BEFORE using it — an unparsed
   constant would make every assertion below compare a query against itself.

   Test one, `the JS query and the CSS token are exact complements at a 20px root
   font`: `page.goto('/')`, then in one `page.evaluate` read
   `parseFloat(getComputedStyle(document.documentElement).fontSize)` and
   `getComputedStyle(document.documentElement).getPropertyValue('--breakpoint-md').trim()`.
   Assert the font size is 20 and the token is a non-empty string ending in `rem`.
   Then for each width in 640, 767, 768, 900, 959, 960, 961 and 1024:
   `setViewportSize`, and in-page assert
   `matchMedia(<the parsed constant>).matches === matchMedia('(width >= ' + token + ')').matches`
   and `=== !matchMedia('(width < ' + token + ')').matches`. Report the failing
   width in the assertion message so a failure names the band rather than the count.

   Test two, `the shell columns and the Sheet agree inside the band the token
   decides`: `signInAsAdmin(page, 'rfs')` from `./helpers/admin`, open the Sheet by
   clicking `button.rowbtn` inside `rowFor(page, name)` and waiting for
   `sheet(page)` to be visible. At width 900 — which is inside the band at a 20px
   root font and OUTSIDE it at 16px, which is exactly why this project exists —
   assert the rail width polls to 0, the `.adminnav-col` width polls to 0, and the
   Sheet carries `data-side="bottom"`. Then at width 1000 assert rail > 0, nav > 0,
   `data-side="right"`.

   Settle by POLLING, not by a wall-clock wait: wrap each measurement in
   `expect.poll(...)` so the 180ms `transition: width` is absorbed by the assertion
   retrying rather than by a fixed timeout. Export the poll-based width reader from
   this spec's local helpers in a shape plan 06.1-08 can adopt for
   `admin-portal.spec.ts`, and say so in a comment. Use
   `[data-testid="rail"]:not(.is-drawer)` for the rail — the drawer carries the same
   test id and is full-width when open.

   RUN IT NOW, before step 4, and record the failure text: at width 900 with the
   constant still in px the Sheet reads `data-side="right"` while both columns are
   zero. That is the RED gate.

4. THE UNIT. Change `web/src/lib/hooks/mediaQuery.svelte.ts` line 5 to
   `export const DESKTOP_MEDIA_QUERY = '(min-width: 48rem)';`. Rewrite its doc
   comment to state the unit and why: `48rem` is the value Tailwind's
   `--breakpoint-md` compiles to, so this query and every `theme(--breakpoint-md)`
   rule resolve against the same reference — the browser's initial font size — and
   move together when a reader changes their default font size. Name
   `web/e2e/admin-band-root-font.spec.ts` as what enforces it. Update the pin in
   `web/src/lib/hooks/mediaQuery.svelte.test.ts` to the new value.

5. Re-run the proof: GREEN. Re-run the census: still GREEN. Re-run the pre-existing
   16px band block in `admin-portal.spec.ts`: unchanged.

`ScenesShell.svelte` needs no edit — it already reaches the band through
`isDesktop()`, so this change fixes its px/rem split transitively. Do not add a
second query for it.
  </action>
  <verify>
    <automated>task test:e2e -- --project=chromium-large-font admin-band-root-font</automated>
  </verify>
  <acceptance_criteria>
    - `task test:e2e -- --project=chromium-large-font admin-band-root-font` exits 0. Budget 10-20 minutes; run it in the background and poll rather than blocking a foreground call.
    - RED gate, recorded in the summary: the same command run at step 3 — before the constant changes — exits non-zero, and the failure is the Sheet reading `data-side="right"` at width 900 while both columns measure 0.
    - RED mutation (regression teeth): after step 4, revert the constant to `'(min-width: 768px)'`, re-run — MUST fail. Revert the revert.
    - RED mutation (harness integrity): remove `--blink-settings=defaultFontSize=20` from the project's `launchOptions.args`, re-run — MUST fail on the in-page root-font-size assertion, NOT pass silently at 16px. Restore.
    - `task test:e2e -- --project=chromium admin-portal` exits 0 — the pre-existing 16px boundary sweep survives the helper extraction.
    - `task web:build` exits 0 (not only `task web:test`) — the new spec is under `web/e2e/`, but the helper move and the config edit must not break the SvelteKit build.
    - `task web:test` exits 0.
    - `task test -- -run 'TestNoAuthoredViewportRuleCarriesAHandWrittenBreakpointLiteral' ./test/meta/` exits 0 with the rem constant in place — the criterion the pre-task census failed.
    - `rg -n "min-width: 768px" web/src` returns no match.
    - `rg -c 'expect.poll' web/e2e/admin-band-root-font.spec.ts` is at least 1 — the new proof settles by polling.
    - `rg -c 'waitForTimeout' web/e2e/admin-band-root-font.spec.ts` is 0 — the new proof does not import the fixed-wall-clock settle that plan 06.1-08 is removing.
  </acceptance_criteria>
  <done>Both halves of the phone band are expressed in rem, and a committed Playwright project running at a 20px root font size proves — in a real Chromium, against the token read off `:root` — that they agree at every probed width, and that the admin shell and the edit Sheet take the same branch inside the band that used to split them.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the false "exact complement at EVERY width" claims, in source and in executed history</name>
  <files>web/src/lib/components/admin/EditCharacterSheet.svelte, web/src/lib/components/scenes/ScenesShell.svelte, .planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-05-SUMMARY.md</files>
  <read_first>
    - web/src/lib/components/admin/EditCharacterSheet.svelte (the `side` derivation comment around :186-200)
    - web/src/lib/components/scenes/ScenesShell.svelte (the `isDesktop()` call site around :78 and the `{#if desktop.current}` branch around :459)
    - .planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-05-SUMMARY.md (the `key-decisions` entry at :45)
    - .planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-VERIFICATION.md (gap G-06.1-2 `missing`, final bullet)
  </read_first>
  <action>
Three corrections. Two are ordinary source edits; the third is append-only.

1. `EditCharacterSheet.svelte`: the paragraph above the `side` derivation
   (the sentence spanning roughly :194-197) claims the shared hook's query
   complements this file's viewport rule universally, at any width whatsoever.
   Rewrite that paragraph to state what is now true and WHY it is
   true: both halves are expressed in rem against the same reference (the browser's
   initial font size), which is what makes them complements at every width AND at
   every root font size; the retired px form was a complement only at 16px. Name
   `web/e2e/admin-band-root-font.spec.ts` as the proof, so a future reader meets the
   enforcement rather than the claim. Keep the existing, still-correct sentences
   about `side` being a prop that CSS cannot mutate and about the `true` fallback.

2. `ScenesShell.svelte`: this file needed no code change — `isDesktop()` carried the
   fix transitively. Add one short comment at the `isDesktop()` call site recording
   that its `md:` Tailwind utilities and this hook now resolve against the same
   reference, and that the pairing was a live px/rem split until plan 06.1-07. Do
   not add a second media query here.

3. `06.1-05-SUMMARY.md` is executed history and MUST NOT be rewritten: no existing
   line is edited, reordered or deleted. Append a new final section headed
   `## Erratum (2026-08-15, closed by plan 06.1-07)` that quotes the `key-decisions`
   claim at :45 verbatim, states that it is false at any root font size other than
   16px, gives the one-line reason (the CSS half compiles to rem and the JS half was
   px), and points at `06.1-VERIFICATION.md` gap `G-06.1-2` and this plan. The
   original sentence stays exactly where it is; the correction sits beside it.
  </action>
  <verify>
    <automated>task test -- -run 'TestNoAuthoredViewportRuleCarriesAHandWrittenBreakpointLiteral' ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "at EVERY width" web/src` returns no match. RED mutation: restore the original sentence in `EditCharacterSheet.svelte` and this grep matches again.
    - `rg -n "exact complement" .planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-05-SUMMARY.md` still matches at line 45 — the original claim is preserved, not scrubbed — AND `rg -n "^## Erratum" <same file>` matches, so the correction is present alongside it.
    - `git diff --stat .planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-05-SUMMARY.md` shows insertions only, zero deletions. RED mutation: editing line 45 in place produces a deletion and fails this check.
    - `task test -- -run 'TestNoAuthoredViewportRuleCarriesAHandWrittenBreakpointLiteral' ./test/meta/` exits 0 — the new comment prose names no media condition verbatim, so it is not a viewport-deciding line.
    - `task web:build` and `task web:test` both exit 0.
  </acceptance_criteria>
  <done>No source comment claims the px query was a universal complement; the false claim in executed history carries an adjacent, append-only correction with its original text intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → gateway | Unchanged by this plan. No request shape, credential, header or RPC is touched. |
| build config → shipped stylesheet | A media-query unit change alters which CSS rules apply at which viewport width; it grants no capability and reveals no data. |
| test harness → CI | A new Playwright project runs a browser with a non-default font-size flag inside the existing e2e container. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-07-01 | Information Disclosure | responsive collapse of the admin nav column | low | accept | Which nav links exist is decided server-side by `AdminListSections` (ROADMAP criterion 1, MET). A width change alters only whether the column is drawn, never its contents; nothing becomes visible to a viewer who could not already request it. |
| T-06.1-07-02 | Tampering | `web/playwright.config.ts` launch arguments | low | mitigate | The added arg is a Chromium rendering preference confined to the e2e container. The in-page root-font-size assertion (Task 2) fails closed if the flag stops applying, so the harness cannot silently degrade into a weaker proof. |
| T-06.1-07-03 | Denial of Service | a phone layout served in a band where it was not intended | low | accept | Pre-existing and reduced by this plan: today a 20px-default reader gets a phone shell with a desktop side sheet. After the fix both halves agree. No availability impact on the server. |
| T-06.1-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no package. `web/package.json` dependencies are unchanged; the Playwright image is the existing pinned digest in `compose.e2e.yaml`. No legitimacy gate is required. |

ASVS enforcement level 1; block-on severity high. No high or critical threat is
introduced, so no blocking checkpoint is inserted.
</threat_model>

<verification>
- `task test -- -run 'TestNoAuthoredViewportRuleCarriesAHandWrittenBreakpointLiteral|TestEveryBandedViewportRuleDerivesItsWidthFromTheTailwindToken' ./test/meta/` exits 0.
- `task web:test` exits 0.
- `task web:build` exits 0.
- `task test:e2e -- --project=chromium-large-font admin-band-root-font` exits 0.
- `task test:e2e -- --project=chromium admin-portal` exits 0.
- `rg -n "min-width: 768px" web/src` returns no match.
- Every RED proof in the tasks above was demonstrated and its failure text recorded in the SUMMARY. A criterion that never went red is reported as such rather than claimed.
</verification>

<success_criteria>
ROADMAP criterion 4 is achievable: the rail's collapse and the admin shell's
collapse fire at the same moment by construction, in ONE unit, and the claim is
checked at a root font size where a unit mismatch would show.
</success_criteria>

<artifacts_this_phase_produces>
| Kind | Symbol / path | Note |
|---|---|---|
| new file | `web/e2e/admin-band-root-font.spec.ts` | the non-16px browser proof |
| new file | `web/e2e/helpers/admin.ts` | `signInAsAdmin`, `gotoAdminCharacters`, `rowFor`, `sheet` moved out of `admin-portal.spec.ts` |
| new Playwright project | `chromium-large-font` | `--blink-settings=defaultFontSize=20`, scoped by `testMatch` |
| new Go type | `breakpointExemption` (`symbol`, `reason`) | replaces the `map[string]string` allowlist value |
| changed constant | `DESKTOP_MEDIA_QUERY` = `'(min-width: 48rem)'` | `web/src/lib/hooks/mediaQuery.svelte.ts` |
| extended Go var | `forbiddenViewportWidths` gains `"48rem"`, `"64rem"` | `test/meta/web_phone_band_breakpoint_census_test.go` |
| appended section | `## Erratum (2026-08-15, closed by plan 06.1-07)` | `06.1-05-SUMMARY.md`, insert-only |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro/06.1-07-SUMMARY.md` when done
</output>
