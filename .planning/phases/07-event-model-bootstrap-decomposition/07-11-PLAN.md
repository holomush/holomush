---
phase: 07-event-model-bootstrap-decomposition
plan: 11
type: tdd
wave: 10
depends_on: ["07-10"]
files_modified:
  - internal/lifecycle/subsystem.go
  - internal/lifecycle/orchestrator.go
  - internal/lifecycle/orchestrator_test.go
  - internal/store/subsystem.go
  - internal/tls/subsystem.go
  - internal/eventbus/subsystem.go
  - internal/access/setup/subsystem.go
  - internal/auth/setup/subsystem.go
  - internal/world/setup/subsystem.go
  - internal/world/setup/relay_subsystem.go
  - internal/session/setup/subsystem.go
  - internal/bootstrap/setup/subsystem.go
  - internal/plugin/setup/subsystem.go
  - internal/eventbus/audit/subsystem.go
  - internal/eventbus/audit/chain/verifier_subsystem.go
  - internal/eventbus/crypto/dek/sweep.go
  - internal/cluster/registry.go
  - internal/admin/socket/subsystem.go
  - internal/admin/policy/subsystem.go
  - cmd/holomush/sub_grpc.go
  - cmd/holomush/core_subsystems_test.go
  - internal/eventbus/crypto/invalidation/coordinator_error_test.go
autonomous: true
requirements: [ARCH-03]

must_haves:
  truths:
    - "No subsystem can begin serving before every subsystem has acquired its resources — the interface cannot express it"
    - "An Activate failure after N subsystems prepared tears down everything prepared, in reverse order, within the deadline"
    - "Boot with a KEK configured still succeeds and the topological order is unchanged"
  artifacts:
    - internal/lifecycle/subsystem.go
    - internal/lifecycle/orchestrator.go
  key_links:
    - "StartAll runs Prepare over the whole topo order, then Activate over the whole topo order — two sweeps, one barrier"
    - "Stop is the single teardown for both prepared-only and activated subsystems"
---

<objective>
**D-12 Wave B.** Split `lifecycle.Subsystem.Start` into `Prepare`/`Activate` so
the **structure enforces** acquire-before-serve rather than per-edge discipline
(D-11), and settle the two questions D-13 assigns to the planner.

Purpose (user, explicit): *"let the code/structure enforce the requirement"* — a
`DependsOn` edge can be forgotten by the next person adding a subsystem; an
interface that cannot express "serve before acquire" cannot be. The motivating
evidence is verified: `grpcSubsystem.DependsOn()` (`cmd/holomush/sub_grpc.go:170-178`)
excluded `AuditProjection`, so gRPC could serve before the audit projection was
up. 07-10 added that specific edge; this plan makes the whole **class** of bug
unrepresentable.

Wave B lands on top of Wave A and must be independently reviewable, so a boot
regression bisects to the right wave (D-12).
</objective>

<settlements>
### D-13.1 — two-phase rollback semantics. SETTLED. **There is no in-tree analog; this is genuine design work.**

`Orchestrator` has exactly one phase today; `StopAll` walks only `startOrder`
(`orchestrator.go:81-96`). "Activate fails after N prepared" has no precedent
anywhere in the repo. The design:

**1. `StartAll` = two full sweeps, not per-subsystem Prepare-then-Activate.**
- Sweep 1: `Prepare` every subsystem in `topoSort` order.
- Sweep 2: `Activate` every subsystem in `topoSort` order.

The **global barrier between the sweeps is the whole point**. Per-subsystem
Prepare-then-Activate is just `Start` renamed and buys nothing. With two sweeps,
"no subsystem serves until every subsystem has acquired" is guaranteed by the
orchestrator's structure and does not depend on any `DependsOn` edge being
remembered — which is exactly D-11's stated rationale. (The specific
audit-projection ordering is fixed separately by 07-10's edge; D-11 and D-14 are
complementary, not redundant.)

**2. Three methods, not four: `Prepare` / `Activate` / `Stop`.**
Do **not** invent `Unprepare` and `Deactivate`. `Stop` becomes the single
teardown for both states, contracted to be safe on a subsystem that was prepared
but never activated. `Stop`'s existing "MUST be idempotent and MUST NOT block
indefinitely" contract (`subsystem.go:55-57`) already covers this; extend the doc
to name the prepared-only case explicitly.

**3. Rollback:**
- `Prepare` fails at index i → `Stop` the i already-prepared subsystems in
  reverse order. Nothing activated, so no separate deactivate path.
- `Activate` fails at index j → `Stop` **everything prepared** in reverse order.
  The prepared set is a superset of the activated set, so one teardown walk
  covers both. This is why three methods suffice.
- The orchestrator tracks `preparedOrder` and `activatedOrder`. `StopAll` walks
  `preparedOrder` in reverse (it subsumes `activatedOrder`) and honors the
  deadline 07-10 gave it.
- First-failure abort is preserved (`orchestrator.go:58-65`): a non-nil error
  from either phase is fatal — the server does not start.

### D-13.2 — the `Start`-MUST-be-idempotent contract. SETTLED: **keep it, per-subsystem, with the correct reason.**

CONTEXT.md says the contract *"exists to support the pre-start hack"* and may be
vestigial once eager starts die. **RESEARCH found no second reason. PATTERNS
found one, and it is real.** Verified live at
`internal/access/setup/subsystem.go:69-78`:

> `// Start is idempotent: if the subsystem is already started, it returns nil`
> `// immediately. This allows the ABAC subsystem to be pre-started in core`
> `// boot when admin handler construction needs Resolver() before the`
> `// orchestrator drives StartAll. Mirrors store.DatabaseSubsystem.Start.`
> ```
> if s.stack != nil {
>     return nil // already started — guard against double-start (would launch a duplicate poller goroutine)
> }
> ```

The parenthetical — *"would launch a duplicate poller goroutine"* — is a
**second, independent reason** that survives D-09. The guard is **not purely** a
pre-start artifact.

**Therefore: keep the MUST, and fix the reason.** The interface contract becomes:
`Prepare` and `Activate` MUST each be idempotent, because a subsystem may guard a
non-idempotent side effect (launching a goroutine, binding a listener) and the
orchestrator does not promise to call each phase exactly once under rollback. The
**false** half of the rationale — "so the subsystem can be pre-started in core
boot before the orchestrator drives StartAll" — dies with 07-09's eager starts
and MUST be deleted wherever it appears. This is decided **per-subsystem**, not
by blanket retirement: audit each guard, keep the ones protecting a real side
effect, and delete only the pre-start justification text.

### Blast radius: **16 production + 3 test implementations** (17 prod with TLS)

CONTEXT.md D-11 says "~18 subsystems". The exact figure, verified via
`rg -n 'DependsOn\(\) \[\]lifecycle\.SubsystemID'`:

| # | Impl | Site |
|---|---|---|
| 1 | `store.DatabaseSubsystem` | `internal/store/subsystem.go:43` |
| 2 | `eventbus.Subsystem` | `internal/eventbus/subsystem.go:77` |
| 3 | `setup.ABACSubsystem` | `internal/access/setup/subsystem.go:65` |
| 4 | `setup.AuthSubsystem` | `internal/auth/setup/subsystem.go:59` |
| 5 | `setup.WorldSubsystem` | `internal/world/setup/subsystem.go:58` |
| 6 | `setup.OutboxRelaySubsystem` | `internal/world/setup/relay_subsystem.go:74` |
| 7 | `setup.SessionSubsystem` | `internal/session/setup/subsystem.go:47` |
| 8 | `setup.BootstrapSubsystem` | `internal/bootstrap/setup/subsystem.go:103` |
| 9 | `setup.PluginSubsystem` | `internal/plugin/setup/subsystem.go:154` |
| 10 | `audit.Subsystem` | `internal/eventbus/audit/subsystem.go:255` |
| 11 | `chain.VerifierSubsystem` | `internal/eventbus/audit/chain/verifier_subsystem.go:67` |
| 12 | `dek.CheckpointSweepSubsystem` | `internal/eventbus/crypto/dek/sweep.go:76` |
| 13 | `cluster.registry` | `internal/cluster/registry.go:174` (unexported) |
| 14 | `socket.AdminSocketSubsystem` | `internal/admin/socket/subsystem.go:64` |
| 15 | `policy.CryptoPolicySubsystem` | `internal/admin/policy/subsystem.go:40` |
| 16 | `grpcSubsystem` | `cmd/holomush/sub_grpc.go:170` (`package main`) |
| + | `tls.TLSSubsystem` | `internal/tls/subsystem.go` (new in 07-09) |
| T1 | `stubSubsystem` | `cmd/holomush/core_subsystems_test.go:22` |
| T2 | `stubSubsystem` | `internal/lifecycle/orchestrator_test.go:29` |
| T3 | `stubRegistry` | `internal/eventbus/crypto/invalidation/coordinator_error_test.go:35` |
</settlements>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-event-model-bootstrap-decomposition/07-CONTEXT.md
@.planning/phases/07-event-model-bootstrap-decomposition/07-PATTERNS.md
@.claude/rules/logging.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Split the Subsystem interface and give the Orchestrator two sweeps</name>
  <read_first>
    - internal/lifecycle/subsystem.go (read in full — the interface at :42-58 and the "Start … MUST be idempotent" contract at :51)
    - internal/lifecycle/orchestrator.go (read in full — StartAll's loop + rollback at :44-78, StopAll at :81-96 as 07-10 left it, topoSort at :98-149)
    - internal/lifecycle/orchestrator_test.go (the existing stub returning s.deps, and the rollback tests)
    - internal/access/setup/subsystem.go:69-78 (the idempotency guard and its two-part rationale — one half true, one half dying)
    - .planning/phases/07-event-model-bootstrap-decomposition/07-PATTERNS.md § 5 (the full impl census) and § "No Analog Found" (the confirmation that Prepare/Activate rollback has no precedent)
  </read_first>
  <files>internal/lifecycle/subsystem.go, internal/lifecycle/orchestrator.go, internal/lifecycle/orchestrator_test.go</files>
  <behavior>
    - Given subsystems A→B→C, `StartAll` calls Prepare(A), Prepare(B), Prepare(C), then Activate(A), Activate(B), Activate(C) — recorded call order proves NO Activate precedes ANY Prepare.
    - When Prepare(B) fails, Activate is never called on anything, and Stop is called on A only (the already-prepared), and StartAll returns a `SUBSYSTEM_START_FAILED`-class error naming B.
    - When Activate(B) fails, Stop is called on C, B, A — everything prepared, in reverse order — and StartAll returns an error naming B.
    - When Activate(C) fails, Stop is called on C, B, A.
    - Rollback honors the ctx deadline (07-10's behavior is preserved through the new paths).
    - Stop on a prepared-but-never-activated subsystem is called and must not error.
    - `topoSort` order is unchanged from today for the same graph.
  </behavior>
  <action>
**RED first:** write the recorded-call-order tests above in
`internal/lifecycle/orchestrator_test.go` against the new interface. They will
not compile until the interface exists, which is the RED. Record it in the
SUMMARY.

`internal/lifecycle/subsystem.go` — replace `Start(ctx) error` with:

- `Prepare(ctx context.Context) error` — acquire resources and wire. MUST NOT
  begin serving, accepting connections, publishing, or launching work loops.
  MUST be idempotent.
- `Activate(ctx context.Context) error` — begin serving. MUST assume every other
  subsystem has already prepared. MUST be idempotent.
- `Stop(ctx context.Context) error` — unchanged signature. Extend the doc: Stop
  MUST be safe to call on a subsystem that was prepared but never activated;
  MUST be idempotent; MUST NOT block indefinitely.

The `Prepare`/`Activate` doc comments carry the **corrected** idempotency
rationale (see `<settlements>` D-13.2): each MUST be idempotent because a
subsystem may guard a non-idempotent side effect — launching a goroutine (the
ABAC poller, `internal/access/setup/subsystem.go:77`) or binding a listener —
and the orchestrator does not promise exactly-once under rollback. **Do NOT**
carry the "so it can be pre-started in core boot before the orchestrator drives
StartAll" rationale — 07-09 deleted every pre-start.

`internal/lifecycle/orchestrator.go` — rewrite `StartAll`:
- `order, err := o.topoSort()` — unchanged.
- Reset `o.preparedOrder` and `o.activatedOrder` (replacing `startOrder`).
- Sweep 1: for each id in `order`, `Prepare`; on error, `o.StopAll(ctx)` and
  return the wrapped `SUBSYSTEM_START_FAILED` error (keep the existing
  `oops.Code("SUBSYSTEM_START_FAILED").With("subsystem", …).Wrapf(...)` shape at
  `:61-64` so existing assertions keep passing); else append to `preparedOrder`.
- Sweep 2: for each id in `order`, `Activate`; on error, `o.StopAll(ctx)` and
  return the same error shape; else append to `activatedOrder`.
- Keep the per-subsystem `slog.InfoContext` + duration logging (`:55-74`),
  labelling the phase.
- `StopAll(ctx)` walks **`preparedOrder`** in reverse (it is a superset of
  `activatedOrder`, so one walk covers both — this is why 3 methods suffice) and
  keeps 07-10's deadline handling and its skipped-subsystem error log verbatim.
- `startOrder` is gone; grep for any other reader.

Use `slog.*Context` variants throughout (`.claude/rules/logging.md`).
  </action>
  <acceptance_criteria>
    - RED recorded in the SUMMARY before the fix
    - `rg -c 'Start\(ctx context.Context\) error' internal/lifecycle/subsystem.go` returns 0
    - `rg -c 'Prepare\(ctx context.Context\) error|Activate\(ctx context.Context\) error' internal/lifecycle/subsystem.go` returns 2
    - `rg -c 'Unprepare|Deactivate' internal/lifecycle/` returns 0 (three methods, not four)
    - `rg -c 'pre-started' internal/lifecycle/subsystem.go` returns 0 (the false rationale is not carried)
    - `rg -c 'preparedOrder' internal/lifecycle/orchestrator.go` returns at least 2 and `rg -c 'startOrder' internal/lifecycle/` returns 0
    - `rg -c 'SUBSYSTEM_START_FAILED' internal/lifecycle/orchestrator.go` returns at least 1 (error code preserved)
    - A test proves no Activate precedes any Prepare via recorded call order: `task test -- -run 'StartAll' ./internal/lifecycle/` exits 0
    - Tests cover Prepare-fails and Activate-fails rollback separately and pass
    - `task test -- ./internal/lifecycle/` exits 0
  </acceptance_criteria>
  <verify>
    <automated>task test -- ./internal/lifecycle/</automated>
  </verify>
  <done>The interface cannot express "serve before acquire"; `StartAll` runs two sweeps with a global barrier; both rollback paths tear down everything prepared, in reverse, within the deadline; three methods, not four.</done>
</task>

<task type="execute">
  <name>Task 2: Migrate all 17 production implementations</name>
  <read_first>
    - internal/world/setup/subsystem.go:53-70 (THE cleanest split candidate — its Start already reads `pool := s.cfg.DB.Pool()` / `engine := s.cfg.ABAC.Engine()` (ACQUIRE) then wires; the seam is already visible)
    - internal/access/setup/subsystem.go:69-115 (the idempotency guard whose rationale is half-true; and the ReadinessRegistry registration at :112)
    - internal/eventbus/subsystem.go:77-120 (Start: brings NATS up, obtains JetStream, declares the EVENTS stream)
    - cmd/holomush/sub_grpc.go:165-300 (grpcSubsystem — the one that literally begins serving; its Start is the clearest Prepare/Activate split in the tree)
    - internal/plugin/setup/subsystem.go:154-260,485-510 (PluginSubsystem + ConfigureSystemBroadcaster's late-binding requirement)
    - .planning/phases/07-event-model-bootstrap-decomposition/07-PATTERNS.md § 5 (the full 16+3 census with exact sites)
  </read_first>
  <files>internal/store/subsystem.go, internal/tls/subsystem.go, internal/eventbus/subsystem.go, internal/access/setup/subsystem.go, internal/auth/setup/subsystem.go, internal/world/setup/subsystem.go, internal/world/setup/relay_subsystem.go, internal/session/setup/subsystem.go, internal/bootstrap/setup/subsystem.go, internal/plugin/setup/subsystem.go, internal/eventbus/audit/subsystem.go, internal/eventbus/audit/chain/verifier_subsystem.go, internal/eventbus/crypto/dek/sweep.go, internal/cluster/registry.go, internal/admin/socket/subsystem.go, internal/admin/policy/subsystem.go, cmd/holomush/sub_grpc.go</files>
  <action>
Split every production `Start` into `Prepare` + `Activate` using the census in
`<settlements>` (17 sites; every path is listed there with a line number — verify
each before editing, line numbers drift).

**The dividing line:** `Prepare` acquires and wires. `Activate` begins serving,
accepting, publishing, or launching a work loop. If a subsystem does no serving,
its `Activate` is a documented no-op returning nil — that is correct and common,
not a smell.

Worked examples to follow:
- `WorldSubsystem.Start` (`internal/world/setup/subsystem.go:64-70`) already
  shows the seam: `pool := s.cfg.DB.Pool()` and `engine := s.cfg.ABAC.Engine()`
  are ACQUIRE (→ `Prepare`), and the `world.NewService(...)` wiring is also
  Prepare. It serves nothing → `Activate` is a no-op.
- `grpcSubsystem` (`cmd/holomush/sub_grpc.go`) is the opposite: building the
  server, registering services and wiring the emitter are `Prepare`; **binding
  the listener and serving** is `Activate`. This subsystem is the entire reason
  D-11 exists — get its split exactly right.
- `eventbus.Subsystem.Start` brings NATS up, obtains JetStream and declares the
  EVENTS stream — all acquisition (`Prepare`). Publishing happens through
  consumers, not here.
- `access/setup.ABACSubsystem` launches a **poller goroutine** — that is a work
  loop → `Activate`. Building the stack and registering health is `Prepare`.
- `dek.CheckpointSweepSubsystem` runs a sweep loop → `Activate`.
- `chain.VerifierSubsystem` walks the chains at boot and refuses on a break
  (INV-CRYPTO-102) — that is a **gate**, not serving. Put the walk in `Prepare`
  so it fails before anything activates. This is the whole payoff: with the
  global barrier, the chain is proven intact before **any** subsystem serves,
  independent of any edge.

**Per-subsystem idempotency audit (D-13.2).** For each existing guard (e.g.
`internal/access/setup/subsystem.go:70-77`'s `if s.stack != nil { return nil }`):
- **Keep** the guard if it protects a real non-idempotent side effect. Preserve
  the parenthetical that names it — *"would launch a duplicate poller
  goroutine"* is the only surviving justification and MUST NOT be lost.
- **Delete** the pre-start justification text wherever it appears — e.g.
  *"This allows the ABAC subsystem to be pre-started in core boot when admin
  handler construction needs Resolver() before the orchestrator drives
  StartAll. Mirrors store.DatabaseSubsystem.Start."* — 07-09 deleted every
  pre-start; that sentence now describes a path that does not exist.
- Record the per-subsystem verdict (keep/delete guard, and why) in the SUMMARY.
  A blanket retirement is explicitly rejected.

Carry `// codecov:ignore — tested by integration and E2E tests` onto the new
`Prepare`/`Activate` bodies wherever it is on the `Start` it replaces
(`world/setup/subsystem.go:65`, `access/setup/subsystem.go:74`).

Re-examine `PluginSubsystem.ConfigureSystemBroadcaster`'s late-binding
requirement (`internal/plugin/setup/subsystem.go:490-492`: *"It MUST be called
from the gRPC subsystem's Start once the event appender exists: the appender is
built after the plugin subsystem starts, so a construction-time option cannot
reach it."*). Under the two-sweep barrier this hook may become a plain
`DependsOn` edge with the wiring in `Prepare`. **Decide explicitly and record the
decision in the SUMMARY** — do not inherit the hook by accident. If it stays,
update its doc to name `Prepare`, not `Start`.

⚠️ `task fmt` mutates aligned blocks across many of these files. Run it and
commit.
  </action>
  <acceptance_criteria>
    - `rg -c 'func \(s? ?\*?\w+\) Start\(ctx context.Context\) error' internal/ cmd/holomush/` returns 0 for lifecycle subsystems — verify by reading the matches; no `lifecycle.Subsystem` implementation retains a `Start`
    - `rg -l 'func .*\) Prepare\(ctx context.Context\) error' internal/ cmd/holomush/ | wc -l` returns 17
    - `rg -l 'func .*\) Activate\(ctx context.Context\) error' internal/ cmd/holomush/ | wc -l` returns 17
    - `rg -c 'pre-started in core boot|Mirrors store.DatabaseSubsystem.Start' internal/` returns 0 (the false rationale is gone)
    - `rg -c 'would launch a duplicate poller goroutine' internal/access/setup/subsystem.go` returns 1 (the true rationale survives)
    - `rg -c 'Serve\(|Listen\(' cmd/holomush/sub_grpc.go` — the serving call is inside `Activate`, verified by reading
    - `task build` exits 0
    - `task test` exits 0
    - `task test:int` exits 0
    - The KEK-wired full boot passes: `task test -- -run 'TestAdminAuthenticate|AdminRekey' ./cmd/holomush/` exits 0
    - The topo-order pin from 07-10 still passes: `task test -- -run 'TopologicalStartOrder' ./cmd/holomush/` exits 0
    - `git status --porcelain` is clean after `task fmt`
    - A per-subsystem idempotency verdict table is in the SUMMARY (17 rows)
  </acceptance_criteria>
  <verify>
    <automated>task build && task test && task test:int</automated>
  </verify>
  <done>All 17 production subsystems implement Prepare/Activate; acquisition and serving are separated; the chain verifier gates in Prepare; every idempotency guard has a per-subsystem verdict and only true rationales survive; the KEK-wired boot works.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 3: Migrate the test implementations and prove acquire-before-serve end to end</name>
  <read_first>
    - cmd/holomush/core_subsystems_test.go:16-52 (stubSubsystem — the set/count stub)
    - internal/lifecycle/orchestrator_test.go:20-45 (the dep-carrying stub)
    - internal/eventbus/crypto/invalidation/coordinator_error_test.go:30-45 (stubRegistry)
    - cmd/holomush/core_topo_order_test.go (07-10's pin — must still pass)
  </read_first>
  <files>cmd/holomush/core_subsystems_test.go, internal/lifecycle/orchestrator_test.go, internal/eventbus/crypto/invalidation/coordinator_error_test.go</files>
  <behavior>
    - The three test implementations satisfy the new three-method interface.
    - A regression test over the **production** subsystem set proves the acquire-before-serve property structurally: recording every phase call across the real set, no `Activate` is observed before the last `Prepare`.
  </behavior>
  <action>
Migrate the three test implementations named in `<settlements>`:
- `cmd/holomush/core_subsystems_test.go:22` — `stubSubsystem`. Keep its
  `DependsOn()` returning `nil` (07-10 relies on that for the set/count tests and
  explicitly does not reuse it for the pin). Replace `Start` with no-op
  `Prepare`/`Activate`.
- `internal/lifecycle/orchestrator_test.go:29` — `stubSubsystem` (returns
  `s.deps`). Task 1 already reworked this one for its phase-order tests; verify
  it is consistent.
- `internal/eventbus/crypto/invalidation/coordinator_error_test.go:35` —
  `stubRegistry`.

Add the **property test** to `cmd/holomush/core_subsystems_test.go`: build the
production set (07-09's `productionSubsystemSet`) with recording stubs whose
`DependsOn()` values come from the **real** subsystem types (the same sourcing
07-10's pin uses — do not hand-copy dep lists), register them, run `StartAll`,
and assert over the recorded call log that the **index of the first `Activate` is
greater than the index of the last `Prepare`**. That single assertion is D-11's
whole guarantee, stated as an executable property over the real graph rather than
per-subsystem inspection.

Its doc comment MUST state why it exists: `grpcSubsystem.DependsOn()` excluded
`AuditProjection`, so gRPC could serve before the audit projection was up. 07-10
fixed that specific edge; this property makes the class unrepresentable — a
future subsystem that forgets an edge still cannot serve before everything has
acquired.

ACE name, e.g. `TestStartAllActivatesNothingUntilEverySubsystemHasPrepared`.

**RED proof:** temporarily change `StartAll` to per-subsystem
Prepare-then-Activate and confirm this property fails. Revert. Record in the
SUMMARY — that is what distinguishes the two-sweep design from a rename.
  </action>
  <acceptance_criteria>
    - `task test -- ./cmd/holomush/ ./internal/lifecycle/ ./internal/eventbus/crypto/invalidation/` exits 0
    - `rg -c 'TestStartAllActivatesNothingUntilEverySubsystemHasPrepared' cmd/holomush/core_subsystems_test.go` returns 1
    - `rg -c 'func (s stubSubsystem) Start' cmd/holomush/core_subsystems_test.go` returns 0
    - RED demonstrated and recorded in the SUMMARY: per-subsystem Prepare-then-Activate fails the property
    - `task test` exits 0
    - `task test:int` exits 0
    - `task pr-prep` exits 0 (fast lane; the phase gate before push)
  </acceptance_criteria>
  <verify>
    <automated>task test && task test:int</automated>
  </verify>
  <done>Every implementation satisfies the three-method interface, and acquire-before-serve is an executable property asserted over the real production graph.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
| Artifact | Kind |
|---|---|
| `lifecycle.Subsystem.Prepare` | new interface method (replaces `Start`) |
| `lifecycle.Subsystem.Activate` | new interface method (replaces `Start`) |
| `Orchestrator.preparedOrder`, `Orchestrator.activatedOrder` | new fields (replace `startOrder`) |
| `Prepare` / `Activate` methods on 17 production subsystems | new methods |
| `TestStartAllActivatesNothingUntilEverySubsystemHasPrepared` | new test |
| Phase-order + two rollback-path tests in `internal/lifecycle/orchestrator_test.go` | new tests |

Deleted: `lifecycle.Subsystem.Start` and its 16 production + 3 test
implementations; `Orchestrator.startOrder`.
No `Unprepare` / `Deactivate` — three methods by design.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subsystem acquisition → subsystem serving | The barrier this plan makes structural |
| boot gate (audit chain) → any serving | INV-CRYPTO-102 must hold before anything serves |
| failed boot → partial state | A half-started server is a fail-open posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-56 | Repudiation | A subsystem serving/persisting before a dependency it never declared is up — MEDIUM-11's stated latent risk | high | mitigate | The two-sweep global barrier: no `Activate` runs until every `Prepare` has. Asserted as an executable property over the **real** production graph (Task 3), not per-subsystem inspection. Structural, so a forgotten `DependsOn` edge cannot reintroduce it. |
| T-07-57 | Tampering | Chain verifier's INV-CRYPTO-102 gate running after something already serves | high | mitigate | The chain walk goes in `Prepare` (Task 2), so it refuses before **any** subsystem activates — independent of 07-10's edge. Defense in depth for the same property. |
| T-07-58 | Denial of Service | Leaked resources after an Activate failure at index j | high | mitigate | `StopAll` walks `preparedOrder` in reverse — a superset of `activatedOrder` — so a mid-Activate failure still tears down every acquired resource. Both rollback paths have dedicated tests (Task 1). |
| T-07-59 | Denial of Service | A duplicate poller goroutine from a non-idempotent phase under rollback | medium | mitigate | D-13.2 settled: the idempotency MUST is **kept** with its true rationale (`internal/access/setup/subsystem.go:77`). Task 2 requires a per-subsystem verdict and asserts the true rationale text survives while the false pre-start text is deleted. |
| T-07-60 | Denial of Service | Rollback itself hanging | high | mitigate | `StopAll` retains 07-10's deadline handling through the new paths; asserted by Task 1's ctx-deadline behavior. |
| T-07-61 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — zero external packages installed (RESEARCH.md § Package Legitimacy Audit). |
</threat_model>

<verification>
- `task build` exits 0
- `task test` exits 0
- `task test:int` exits 0
- `task lint` exits 0
- `task pr-prep` exits 0 (fast lane; run inline before push per CLAUDE.md)
- The KEK-wired full boot passes (`task test -- -run 'TestAdminAuthenticate|AdminRekey' ./cmd/holomush/`)
- 07-10's topo-order pin still passes
- Wave B is independently reviewable: it applies cleanly on top of Wave A and a boot regression bisects to one wave (D-12)
</verification>

<success_criteria>
`lifecycle.Subsystem` cannot express "serve before acquire". `StartAll` prepares
everything, then activates everything. Both rollback paths tear down every
prepared subsystem in reverse within the deadline. Every idempotency guard has a
deliberate, per-subsystem verdict grounded in a real side effect. The process
boots with a KEK configured, in the same topological order, with unchanged
startup/shutdown behavior.
</success_criteria>

<output>
Create `.planning/phases/07-event-model-bootstrap-decomposition/07-11-SUMMARY.md` when done.
</output>
</content>
