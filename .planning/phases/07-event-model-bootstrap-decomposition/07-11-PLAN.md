---
phase: 07-event-model-bootstrap-decomposition
plan: 11
type: tdd
wave: 9
depends_on: ["07-10"]
files_modified:
  - internal/lifecycle/subsystem.go
  - internal/lifecycle/orchestrator.go
  - internal/lifecycle/orchestrator_test.go
  - internal/store/subsystem.go
  - internal/tls/subsystem.go
  - internal/eventbus/subsystem.go
  - internal/access/setup/subsystem.go
  - internal/auth/setup/subsystem.go
  - internal/world/setup/subsystem.go
  - internal/world/setup/relay_subsystem.go
  - internal/session/setup/subsystem.go
  - internal/bootstrap/setup/subsystem.go
  - internal/plugin/setup/subsystem.go
  - internal/eventbus/audit/subsystem.go
  - internal/eventbus/audit/chain/verifier_subsystem.go
  - internal/eventbus/crypto/dek/sweep.go
  - internal/cluster/registry.go
  - internal/cluster/heartbeat.go
  - internal/cluster/clustertest/harness.go
  - internal/cluster/clustertest/external.go
  - internal/admin/socket/subsystem.go
  - internal/admin/policy/subsystem.go
  - cmd/holomush/sub_grpc.go
  - cmd/holomush/core_subsystems_test.go
  - internal/eventbus/crypto/invalidation/coordinator_error_test.go
  # ── Added in revision 8 (cross-AI review round 5) ──────────────────────────────
  - cmd/holomush/sub_grpc_test.go              # row-16 focused test: neither reaper runs after Prepare alone
  - internal/world/setup/relay_subsystem_test.go  # D-13.2 repeated-Activate test (relay guard)
  # ── Added in revision 12 (cross-AI review round 9, BLOCKER) ────────────────────
  - internal/plugin/setup/subsystem_test.go    # row-9 fault-injection test: Prepare failure after goplugin.NewHost leaves no token-store sweeper goroutine
  # ── Revision 14 (cross-AI review round 11, BLOCKER 2 + MEDIUM 3) ──────────────
  # Tasks 1–3 are ONE atomic execution unit squashed into ONE commit that builds
  # (the 07-01/07-05 rev-13 pattern). Rev 13's Task-2 boundary demanded
  # `task build && task test && task test:int` + a commit — unsatisfiable on
  # correct code: two Start-only stubs survive until Task 3 and BOTH are compiled
  # by `task test` (cmd/holomush/core_subsystems_test.go:23 stubSubsystem,
  # //go:build !integration; internal/eventbus/crypto/invalidation/
  # coordinator_error_test.go:36 stubRegistry, NO build tag, typed against the
  # cluster.Registry interface Task 2 itself migrates). Whole-repo gates + the
  # single commit now land at Task 3's boundary; each task keeps scoped
  # inner-loop checks. Task 2's <files> also now names the test files its own
  # criteria create/edit (MEDIUM 3). No frontmatter file-set change.
  # ── Revision 15 (cross-AI review round 12, LOW fold-in) ────────────────────────
  # Task 2 also migrates the preserved accessor guards' diagnostics: doc comments
  # and panic strings saying "called before Start()" (e.g. internal/store/
  # subsystem.go:89-95, internal/plugin/setup/subsystem.go:474-477, plus the new
  # tls guard 07-09 Task 1 mints) reference a method this plan deletes — they
  # move to "called before Prepare()", RECEIVER-classified like the existing
  # Start-survivor sweep (non-subsystem types keep their strings). No file-set
  # change: every carrying file is already in Task 2's <files>.
  # ── Added in revision 5 (cross-AI review round 3, HIGH) ────────────────────────
  # "The files_modified manifests are incomplete enough that mandated test gates
  # cannot pass." Rev 4 listed the 17 IMPLEMENTATIONS but almost none of the
  # CALLERS. Removing Subsystem.Start breaks every direct caller — these are
  # COMPILE FAILURES, not cleanup. The review named 4; a live census found ~20.
  # ── Census method corrected in revision 7 (cross-AI round 4, BLOCKER) ─────────
  # Rev 6's regex — rg -n '\.Start\((ctx|context\.|startCtx|_)' — whitelisted
  # ARGUMENT shapes and therefore missed `s.Start(t.Context())` callers
  # (internal/eventbus/audit/subsystem_test.go:130,:145), and its classification
  # pass dropped the admin/policy pair. Census command (re-run it — Step 0 of
  # Task 2 — and classify by RECEIVER TYPE, never by argument shape):
  #   rg -n '\.Start\(' --type go -g '!*.pb.go' | rg -v 'orch\.StartAll'
  # A lifecycle.Subsystem receiver => the file MUST be listed here. Exempt
  # classes are enumerated in Task 2 Step 0.
  # NOTE: integration-tagged files (most of these) are invisible to `task build`
  # AND to `task test` — `task test:int` is the only gate that compiles them.
  # This is exactly the landmine 07-CONTEXT.md § Landmines names.
  #
  # -- production / test-support (compiled by `task build` or `task test`) --
  - internal/testsupport/integrationtest/real_abac.go          # :60  abacSub.Start
  - internal/testsupport/integrationtest/plugins.go            # :342 ps.Start (PluginSubsystem)
  - internal/testsupport/integrationtest/harness.go            # :435 sub.Start (external NATS eventbus)
  - internal/eventbus/eventbustest/embedded.go                 # eventbus.Subsystem.Start
  # -- unit tests --
  - internal/admin/socket/subsystem_test.go                    # :81,:102,:106
  - internal/auth/setup/subsystem_internal_test.go
  - internal/eventbus/subsystem_test.go                        # :100,:111,:125,:143,:180
  - internal/eventbus/subsystem_exporter_internal_test.go      # :43
  - internal/eventbus/audit/projection_test.go
  - internal/eventbus/audit/chain/verifier_subsystem_test.go
  - internal/plugin/subscriber_test.go
  # -- added in revision 7: the three callers the rev-6 census missed (round 4) --
  - internal/eventbus/audit/subsystem_test.go                  # :130,:145 — s.Start(t.Context()), the form the old regex missed
  - internal/admin/policy/subsystem_test.go                    # :82 — s.Start(context.Background())
  # -- integration-tagged (ONLY `task test:int` compiles these) --
  - internal/admin/policy/subsystem_integration_test.go        # :41,:72,:93 — added in revision 7 (round 4)
  - internal/admin/socket/subsystem_integration_test.go
  - internal/eventbus/audit/subsystem_boot_gate_integration_test.go   # :58
  - internal/eventbus/audit/dlq_capture_integration_test.go
  - internal/eventbus/crypto/dek/sweep_integration_test.go     # :147,:170
  - plugins/core-scenes/publish_snapshot_integration_test.go   # :231
  - test/integration/admin_policy_chain_test.go                # :95,:123
  - test/integration/crypto/e2e_test.go                        # :111
  - test/integration/crypto/emit_test.go                       # :93
  - test/integration/crypto/metadata_only_test.go              # :96
  - test/integration/crypto/plugin_decrypt_test.go             # :200
  - test/integration/crypto/readback_test.go                   # :136,:546
  - test/integration/eventbus_e2e/audit_drift_detector_test.go # :46
  - test/integration/eventbus_e2e/audit_only_channel_test.go   # :44
  - test/integration/eventbus_e2e/js_storage_corruption_test.go # :47
  - test/integration/eventbus_e2e/plugin_audit_isolation_test.go # :90
  - test/integration/eventbus_e2e/plugin_audit_round_trip_test.go # :125
  - test/integration/eventbus_e2e/plugin_crash_resilience_test.go # :88
  - test/integration/eventbus_e2e/rendering_completeness_test.go # :39
  - test/integration/eventbus_e2e/soak_test.go                 # :63
  - test/integration/eventbus_external/external_boot_test.go   # :112,:131,:186
  - test/integration/privacy/scene_history_readback_test.go    # :300
autonomous: true
requirements: [ARCH-03]

must_haves:
  truths:
    - "No subsystem accepts externally-reachable DOMAIN traffic, and no host-owned domain work loop runs, before every subsystem has acquired its resources — the interface cannot express it (see D-13.0 for why the barrier is scoped to external DOMAIN surfaces + host-owned domain loops: process-internal substrate like the DontListen embedded NATS server is acquisition, audit's Prepare acquires against it, and plugin-side Init behavior is outside orchestrator enforcement — round 9's one-adjective scope fix). Carve-out, recorded not hidden (D-13.0 exception 1): OBSERVABILITY surfaces may bind in Prepare — the NATS Prometheus monitor binds HTTPPort: s.cfg.MonitorPort (internal/eventbus/subsystem.go:156) during Prepare, the same class as the observability server already started before StartAll today (cmd/holomush/core.go:351). Falsifiable as stated: the ONLY binds permitted in any Prepare body are observability ones; a DOMAIN surface (gRPC listener, admin.sock) binding in Prepare violates this truth."
    - "The only two Activate bodies binding an externally-reachable surface are grpcSubsystem (listener) and socket.AdminSocketSubsystem (admin.sock — via the existing atomic Server.Start, which also acquires admin.lock; that acquisition moves to Activate deliberately, see D-13.3 row 14)"
    - "Every direct caller of a subsystem's former Start calls BOTH Prepare and Activate — ~27 files (rev 7: +3 the rev-6 argument-shape regex missed), ~21 of them integration-tagged and therefore invisible to task build AND task test; task test:int is the only gate that proves it"
    - "audit.Prepare succeeds against a merely-prepared eventbus — no AUDIT_DEP_NOT_STARTED"
    - "audit's Prepare and Activate each guard on PHASE-OWNED state — Prepare on the new preparedProjection field it sets, Activate on the worker field p.start assigns — because the old worker-keyed guard cannot guard Prepare after the split (worker is assigned only after p.start, which moves to Activate); Stop handles and clears both prepared-only and activated states (cross-AI round 7, BLOCKER). Round 8: the prepared aggregate INCLUDES the lateInit-written state (s.cfg.Owners, s.pluginMgr — written at audit/subsystem.go:304-312 BEFORE the fallible projection construction): a failed Prepare restores it, prepared-only Stop clears the IN-MEMORY prepared aggregate, and a retry cannot observe stale owners/consumer wiring. Round 9 wording fix: the durable JetStream consumer newProjection creates at Prepare (projection.go:109-117, CreateOrUpdateConsumer with Durable) is intentionally RETAINED server-side on a prepared-only Stop — drain is a no-op before Consume exists (projection.go:467-470, p.cc == nil), so there is nothing to 'release'; CreateOrUpdateConsumer re-attaches idempotently on retry. Retained-not-deleted, stated explicitly in Stop's doc"
    - "The gRPC session reaper and guest reaper — domain work loops launched today before the listener bind (cmd/holomush/sub_grpc.go:758, :769) — are constructed in Prepare and their Run goroutines launch only in Activate; a focused test proves neither runs after Prepare alone (cross-AI round 5, BLOCKER)"
    - "A PluginSubsystem Prepare failure between goplugin.NewHost (internal/plugin/setup/subsystem.go:322 — the token-store sweeper goroutine launches at construction, goplugin/host.go:345-359) and the s.manager assignment (:384) leaves NO token-store sweeper goroutine running and NO live Lua host: cleanupOnError closes binaryHost and luaHost on the pre-manager error paths (BINARY_HOST_MW_FAILED :333, ALIAS_POOL_FAILED :341, manager-construction :381), proven by a goleak fault-injection test (cross-AI round 9, BLOCKER — Stop no-ops while s.manager == nil (:451-453), so the plan's own partially-prepared rollback contract was empty for this subsystem without in-body cleanup)"
    - "Disabled-mode admin socket boots under two sweeps: Prepare keeps the SocketPath == \"\" early return (server never constructed), and Activate returns nil when s.server == nil instead of nil-panicking in srv.Start() — the degraded no-XDG-runtime-dir environment disabled mode exists for (cross-AI round 5, BLOCKER)"
    - "cluster ACQUISITION — ConnProvider/ClusterIDProvider resolution and their fail-closed checks, which 07-09 moved into the Start body — happens in Prepare, before the barrier; the locked subscribe/publish/goroutine critical section stays whole in Activate (row 13, round 6 BLOCKER: a no-op cluster Prepare composed with 07-09 to put fallible acquisition AFTER the barrier)"
    - "Every non-idempotent Prepare/Activate side effect has a plan-settled guard verdict (D-13.2's 17-row table) — none delegated to the executor"
    - "An Activate failure after N subsystems prepared tears down everything prepared, in reverse order, within the deadline"
    - "Boot with a KEK configured still succeeds and the topological order is unchanged"
  artifacts:
    - internal/lifecycle/subsystem.go
    - internal/lifecycle/orchestrator.go
  key_links:
    - "StartAll runs Prepare over the whole topo order, then Activate over the whole topo order — two sweeps, one barrier"
    - "Stop is the single teardown for both prepared-only and activated subsystems"
---

<objective>
**D-12 Wave B.** Split `lifecycle.Subsystem.Start` into `Prepare`/`Activate` so
the **structure enforces** acquire-before-serve rather than per-edge discipline
(D-11), and settle the two questions D-13 assigns to the planner.

Purpose (user, explicit): *"let the code/structure enforce the requirement"* — a
`DependsOn` edge can be forgotten by the next person adding a subsystem; an
interface that cannot express "serve before acquire" cannot be. The motivating
evidence is verified: `grpcSubsystem.DependsOn()` (`cmd/holomush/sub_grpc.go:170-178`)
excluded `AuditProjection`, so gRPC could serve before the audit projection was
up. 07-10 added that specific edge; this plan makes the whole **class** of bug
unrepresentable.

Wave B lands on top of Wave A and must be independently reviewable, so a boot
regression bisects to the right wave (D-12).
</objective>

<settlements>
### D-13.0 — what "acquire" and "serve" MEAN. SETTLED. **This supersedes the earlier universal-barrier framing, which was INCOHERENT (cross-AI review, HIGH; verified).**

An earlier revision asserted a universal barrier — *"nothing serves until
everything is acquired"* — and then required `eventbus` (`go s.server.Start()`)
and `audit` (projection + consumers + retention worker) to move to `Activate`.
**That pair cannot both hold, and the plan could not have booted.** Verified live
in this worktree:

- `go s.server.Start()` (`internal/eventbus/subsystem.go:164`) lives **inside**
  `connectEmbedded()`, which immediately calls `ReadyForConnections()` (`:166`),
  `nats.Connect(nats.InProcessServer(s.server))` (`:177-185`) and
  `jetstream.New(conn)` (`:193`). `connect()` is called from `Start` at `:91`.
  **Server-start and client-connect are one atomic call.** You cannot move
  `go s.server.Start()` into `Activate` while `s.connect()` stays in `Prepare` —
  there is no seam between them.
- `audit.Subsystem.Start` (`internal/eventbus/audit/subsystem.go:273-279`) reads
  `s.jsProv.JS()` and **fails closed** with `AUDIT_DEP_NOT_STARTED` when it is
  nil; `projection.go:108` makes a live `js.CreateOrUpdateConsumer` **network
  call**. Audit's acquisition therefore REQUIRES a running bus.

So embedded NATS *running* is the substrate audit must *acquire against*. Under a
literal universal barrier, audit's acquisition would require something the
barrier forbids until acquisition completes. The barrier as stated was circular.

**The error was the definition of "serving", not the design.** Fixed by naming it
precisely. The decisive fact is one line: `internal/eventbus/subsystem.go:153`
sets **`DontListen: true`** on the embedded server's options. The embedded NATS
server **binds no socket and accepts no external connection** — it is reachable
only in-process via `nats.InProcessServer(s.server)` (`:179`). Calling it
"serving" was simply wrong: it is process-internal substrate, exactly like a
`pgxpool` handle.

**The definitions, settled — use these verbatim in the interface doc comments:**

| Phase | Contract |
|---|---|
| **`Prepare`** | Acquire and wire everything the subsystem needs, **including bringing up process-internal substrate that other subsystems must acquire against** (the embedded NATS server, the DB pool, JetStream streams/durable consumers, plugin processes). MUST NOT expose any **domain** surface reachable from outside the process, and MUST NOT start a work loop that carries **domain traffic**. **Observability surfaces are excepted** (exception 1 below): the NATS Prometheus monitor binds an operator-facing HTTP port here, which is why this clause says "domain" and not "any". MUST be idempotent. |
| **`Activate`** | Begin serving: bind/accept **externally reachable** connections, and start work loops that **carry domain traffic** (deliver events, dispatch commands, sweep, publish). MUST assume every subsystem has already prepared. MUST be idempotent. |
| **`Stop`** | Single teardown for prepared-only, partially-prepared, and activated subsystems. |

**The guarantee this actually buys — state it exactly, do not overclaim:**

> **No subsystem accepts externally-reachable DOMAIN traffic, and no host-owned
> domain work loop runs, until every subsystem has finished acquiring.**

⚠️ **"Domain" is load-bearing** — exception 1 below permits the NATS Prometheus
monitor to bind an operator-facing HTTP port in `Prepare`. An unqualified
"externally-reachable traffic" would make this guarantee contradict that
exception, i.e. the plan would forbid on one line what it permits 25 lines later.
Observability is not domain traffic.

⚠️ **"Host-owned" is load-bearing too (cross-AI round 9, rev 12 — one adjective,
not a restructure):** plugin Prepare launches plugin subprocesses whose own Init
behavior the orchestrator cannot enforce — row 9 decides that launch is
acquisition (host-controlled child over host-brokered mTLS, no
externally-reachable surface), but what a plugin does inside its Init is outside
the barrier's reach. The structural guarantee applies to host-owned `Activate`
bodies; do not state it as covering plugin-side loops.

That is strictly weaker than "nothing serves until everything is acquired" and
strictly stronger than today. Crucially, **it is exactly the guarantee D-11's
motivating bug needs**: `grpcSubsystem.DependsOn()` omitted `AuditProjection`, so
gRPC could accept external RPCs before the audit projection was up. Under this
barrier gRPC's listener cannot bind until audit has prepared — **regardless of any
`DependsOn` edge**. The class is still unrepresentable.

**Why ordering inside a sweep still saves audit:** Sweep 1 walks `topoSort` order,
so `Prepare(eventbus)` runs before `Prepare(audit)` via audit's existing
`DependsOn(Database, EventBus, Plugins)` (`audit/subsystem.go:255-261`). Intra-sweep
`DependsOn` ordering is unchanged and is what acquisition depends on; the barrier
governs only the **sweep boundary**. Both mechanisms are live and complementary.

**Two deliberate exceptions, recorded rather than hidden:**
1. **The NATS Prometheus monitor / exporter** (`subsystem.go:109-124`;
   `HTTPPort: s.cfg.MonitorPort` at `:156`) DOES bind an operator-facing HTTP
   port during `Prepare`. It is an **observability** surface, not a domain one,
   and it is the same class as the observability server that already starts
   before `StartAll` today (`core.go:351`). Keep it in `Prepare` with a comment
   naming this exception; do not contort the split around it.
2. **External-mode eventbus** (`ModeExternal`, `connectExternal`) dials a remote
   cluster in `Prepare`. Dialling outbound is acquisition, not serving. No change.

### D-13.1 — two-phase rollback semantics. SETTLED. **There is no in-tree analog; this is genuine design work.**

`Orchestrator` has exactly one phase today; `StopAll` walks only `startOrder`
(`orchestrator.go:81-96`). "Activate fails after N prepared" has no precedent
anywhere in the repo. The design:

**1. `StartAll` = two full sweeps, not per-subsystem Prepare-then-Activate.**
- Sweep 1: `Prepare` every subsystem in `topoSort` order.
- Sweep 2: `Activate` every subsystem in `topoSort` order.

The **global barrier between the sweeps is the whole point**. Per-subsystem
Prepare-then-Activate is just `Start` renamed and buys nothing. With two sweeps,
D-13.0's guarantee — *no subsystem accepts externally-reachable DOMAIN traffic
and no host-owned domain work loop runs until every subsystem has acquired* — is
enforced by the orchestrator's structure and does not depend on any `DependsOn`
edge being remembered, which is exactly D-11's stated rationale. (The specific
audit-projection ordering is fixed separately by 07-10's edge; D-11 and D-14 are
complementary, not redundant.)

⚠️ **`DependsOn` ordering is NOT retired — it is what makes acquisition work.**
Sweep 1 walks `topoSort` order, so a subsystem whose Prepare acquires against
another's prepared substrate (audit → eventbus's JetStream; world → the DB pool)
still gets the ordering it needs *within* the sweep. The barrier governs the
**sweep boundary**, not intra-sweep order. Read D-13.0 before touching the split.

**2. Three methods, not four: `Prepare` / `Activate` / `Stop`.**
Do **not** invent `Unprepare` and `Deactivate`. `Stop` becomes the single
teardown for both states, contracted to be safe on a subsystem that was prepared
but never activated. `Stop`'s existing "MUST be idempotent and MUST NOT block
indefinitely" contract (`subsystem.go:55-57`) already covers this; extend the doc
to name the prepared-only case explicitly.

**3. Rollback:**
- `Prepare` fails at index i → `Stop` the i already-prepared subsystems in
  reverse order. Nothing activated, so no separate deactivate path.
- `Activate` fails at index j → `Stop` **everything prepared** in reverse order.
  The prepared set is a superset of the activated set, so one teardown walk
  covers both. This is why three methods suffice.
- The orchestrator tracks `preparedOrder` and `activatedOrder`. `StopAll` walks
  `preparedOrder` in reverse (it subsumes `activatedOrder`) and honors the
  deadline 07-10 gave it.
- First-failure abort is preserved (`orchestrator.go:58-65`): a non-nil error
  from either phase is fatal — the server does not start.

### D-13.2 — the `Start`-MUST-be-idempotent contract. SETTLED: **keep it, per-subsystem, with the correct reason.**

CONTEXT.md says the contract *"exists to support the pre-start hack"* and may be
vestigial once eager starts die. **RESEARCH found no second reason. PATTERNS
found one, and it is real.** Verified live at
`internal/access/setup/subsystem.go:69-78`:

> `// Start is idempotent: if the subsystem is already started, it returns nil`
> `// immediately. This allows the ABAC subsystem to be pre-started in core`
> `// boot when admin handler construction needs Resolver() before the`
> `// orchestrator drives StartAll. Mirrors store.DatabaseSubsystem.Start.`
> ```
> if s.stack != nil {
>     return nil // already started — guard against double-start (would launch a duplicate poller goroutine)
> }
> ```

The parenthetical — *"would launch a duplicate poller goroutine"* — is a
**second, independent reason** that survives D-09. The guard is **not purely** a
pre-start artifact.

**`cluster.registry` is a second confirmed instance, found in the corrected
census.** `Start` guards at `internal/cluster/heartbeat.go:26`:

> `if r.subAlive != nil { return nil // already started }`

Its side effects are unambiguously non-idempotent: four NATS subscriptions
(`:30-64`), a heartbeat publish (`:72`), and **two goroutines** —
`runHeartbeatTicker` (`:82`) and `runEvictionSweeper` (`:87`), both fenced by
`r.wg`. A second `Start` without the guard would double-subscribe and leak two
goroutines that `Stop` (`:110`) could never fence, because `Stop` nils the
channel/ticker fields it uses to signal them. **Verdict: KEEP**, and the true
reason is a real side effect, not a pre-start artifact. Its guard also carries
**no** pre-start rationale text today, so there is nothing to delete there —
record the verdict, change nothing but the method name it lives under.

Note the guard's shape: it keys on `r.subAlive != nil`, which after the split is
an **Activate**-owned field. Whichever phase the subscriptions land in owns this
guard; do not leave it stranded on a `Prepare` that no longer sets `subAlive`.

**Therefore: keep the MUST, and fix the reason.** The interface contract becomes:
`Prepare` and `Activate` MUST each be idempotent, because a subsystem may guard a
non-idempotent side effect (launching a goroutine, binding a listener) and the
orchestrator does not promise to call each phase exactly once under rollback. The
**false** half of the rationale — "so the subsystem can be pre-started in core
boot before the orchestrator drives StartAll" — dies with 07-09's eager starts
and MUST be deleted wherever it appears.

#### The 17-row idempotency disposition — SETTLED HERE, NOT IN THE SUMMARY (cross-AI round 5, WARNING — the zero-executor-decides standard applies to guards too)

Rev 7 stated the per-interface contract and delegated the 17 verdicts to the
executor. Retracted: the verdicts are below. Guard pattern where one is ADDED:
**a phase-owned state field the phase itself sets** (`s.done != nil`,
`s.errCh != nil`, a bool) checked at the top of that phase — never a shared
"started" flag spanning both phases. "KEEP" rows retain the existing guard
(pre-start rationale text deleted per the rule above). Existing guard sites
verified live: store `subsystem.go:52`, eventbus `subsystem.go:88`, auth
`setup/subsystem.go:78`, ABAC `setup/subsystem.go:77`, audit
`audit/subsystem.go:271`, cluster `heartbeat.go:27`, grpc `sub_grpc.go:230`.

| # | Subsystem | Non-idempotent side effect (owning phase) | Guard verdict | Repeated-call test |
|---|---|---|---|---|
| 1 | store.Database | pool + event store + `InitGameID` (Prepare) | KEEP existing (`s.eventStore != nil`, `:52`) on Prepare; **delete** the `:47-48` "started early (before the orchestrator)" rationale text. Activate no-op — nothing to guard | no (existing behavior) |
| 2 | eventbus | embedded server + conn + JS + exporter (Prepare) | KEEP existing (`:88`) on Prepare. Activate no-op | no |
| 3 | ABAC | stack build (Prepare); **poller goroutine** (Activate) | KEEP existing (`s.stack != nil`, `:77`) on Prepare (delete pre-start text, keep the "duplicate poller" parenthetical — reworded to name Activate); ADD Activate guard on the poller's own state (phase-owned) | no (guard verified by reading) |
| 4 | auth | construction only (Prepare) | KEEP existing (`:78`) on Prepare (delete pre-start text). Activate no-op | no |
| 5 | world | construction only (Prepare) | none needed — reassignment is benign; document | no |
| 6 | outbox relay | waker + relay wiring (Prepare); **ctx+goroutine per call** (`relay_subsystem.go:99-105`, Activate) | ADD Prepare guard (`s.relay != nil`); ADD Activate guard (`s.done != nil`) | **YES** (review-named): second `Activate` returns nil, no second goroutine — `internal/world/setup/relay_subsystem_test.go` |
| 7 | session | store construction (Prepare) | none needed — reassignment benign; document. Activate is a **documented no-op** (see the row-7 settlement) | no |
| 8 | bootstrap | one-shot seeding + orphan check (Prepare) | none — seeding already runs on every boot by design; re-run is harmless | no |
| 9 | plugin | full stack build + **binary subprocess launch** (Prepare) | ADD Prepare guard keyed on the same field its accessor panic guards read (`internal/plugin/setup/subsystem.go:502-507`). Activate no-op. ⚠️ **Round 9, BLOCKER — the partially-prepared contract does NOT hold for this subsystem today; fix it while migrating (the prepared-aggregate treatment row 10 received in rounds 7-8, which this row never got):** `Stop` no-ops while `s.manager == nil` (`subsystem.go:451-453`); `cleanupOnError` (`:237-250`) closes aliasPool/schemaProvisioner/worldConn but never `binaryHost` or `luaHost`; and `goplugin.NewHost` launches the token-store sweeper goroutine at construction (`internal/plugin/goplugin/host.go:345-359`). Three verified error paths between `binaryHost := goplugin.NewHost(...)` (`:322`) and `s.manager = mgr` (`:384`) — `BINARY_HOST_MW_FAILED` (`:333`), `ALIAS_POOL_FAILED` (`:341`), manager-construction failure (`:381`) — leak the sweeper goroutine and the live Lua host, and the rollback `Stop` this plan promises then releases nothing. **Settled fix:** extend `cleanupOnError` to close both hosts — `binaryHost.Close` cancels + closes the token store (`goplugin/host.go:1579-1580`), `luaHost.Close` (`lua/host.go:794`) — nil-guarded, with `binaryHost` declared before the closure so it observes the later assignment; nil `s.luaHost` after closing, so `Stop`'s `s.manager == nil` early return becomes a TRUE no-op (everything already released in-body) rather than a leak. Task 2 § "Plugin prepared-state cleanup" carries the full shape + the fault-injection test. | **YES** (round 9): a goleak fault-injection test proves a Prepare failure after `goplugin.NewHost` leaves no token-store sweeper goroutine running — `internal/plugin/setup/subsystem_test.go`; see Task 2 |
| 10 | audit | partition boot gate + durable-consumer construction (Prepare — Backfill/EnsurePartitions are re-run-safe, but the guard still matters: unguarded re-Prepare re-runs lateInit and constructs a second projection/durable consumer); **projection workers + plugin consumers + retention worker** (Activate) | ⚠️ **Round 7, BLOCKER — the earlier "KEEP existing (`:271`) on Prepare" verdict was WRONG and is RETRACTED**: the only live guard is `if s.worker != nil` (`:270`), and `worker` is assigned only AFTER `p.start(workerCtx)` (`:322`) — which this row moves to Activate. Keeping that guard "on Prepare" leaves Prepare permanently unguarded, carries no prepared state into Activate, and leaves Stop unable to distinguish prepared-only from never-prepared. **Explicit phase state instead:** ADD fields `preparedProjection *projection` and `partitionManager *EventsAuditPartitionManager`, both set by Prepare — Prepare guards on `s.preparedProjection != nil`; the existing `worker`/`cancel` pair (`subsystem.go:211-212`) becomes Activate-owned — Activate guards on `s.worker != nil` and FAILS CLOSED (typed error) when `s.preparedProjection == nil` (Prepare never ran); Stop (`:368`, today keyed only on `worker`) handles and CLEARS both states — the activated path unchanged, plus a prepared-only path that releases/nils `preparedProjection`/`partitionManager` without error. ⚠️ **Round 8 — the prepared aggregate is BIGGER than those two fields:** `lateInit` writes `s.cfg.Owners` and `s.pluginMgr` (`subsystem.go:304-312`) BEFORE the fallible `newProjection`, so (a) Prepare's error paths AFTER `lateInit` restore both to their pre-lateInit values (capture them before calling `lateInit` — the current `if owners != nil` conditional means a retry whose provider returns nil would otherwise silently KEEP stale ownership), (b) the prepared-only Stop path also stops/clears `s.pluginMgr` and restores `s.cfg.Owners`, and (c) **(reworded round 9, rev 12 — the round-8 "releases … durable-consumer state with bounded-drain discipline" phrasing was misleading)** prepared-only Stop clears the IN-MEMORY prepared state (nils `preparedProjection`/`partitionManager`); the durable JetStream consumer `newProjection` created (`projection.go:109-117`, `CreateOrUpdateConsumer` with `Durable`) is intentionally **RETAINED** server-side — `drain` is a no-op before Consume exists (`projection.go:467-470`, `p.cc == nil`), so there is nothing to "release", and a retained durable is not a leak: `CreateOrUpdateConsumer` is idempotent, so a retried Prepare re-attaches to the same durable rather than duplicating. State retained-not-deleted explicitly in Stop's doc comment | **YES** (rounds 7-8): second Prepare returns nil with no second projection/durable-consumer construction; Stop after Prepare-only succeeds and clears the FULL in-memory prepared aggregate (incl. `pluginMgr`/`Owners`; the durable consumer is retained by design — round 9); and a Prepare-fail/retry test — fail after `lateInit`, retry with a `lateInit` returning different (or nil) owners/pcm — asserts the retry observes NO stale value from the failed attempt |
| 11 | chain verifier | chain walk is a read-only re-runnable gate (Prepare); `NewVerifier` reassignment benign | none needed; document. Activate no-op | no |
| 12 | checkpoint sweep | provider resolution (Prepare — memoized upstream, benign); **sweepOnce + tick loop per call** (`sweep.go:86-94`, Activate) | ADD Activate guard (`s.done != nil`) | **YES** (review-named): `internal/eventbus/crypto/dek/sweep_integration_test.go` |
| 13 | cluster | provider resolution + fail-closed validation (Prepare — round 6, BLOCKER: acquisition, not a no-op); subscriptions + publish + 2 goroutines (Activate) | KEEP existing (`subAlive`, `heartbeat.go:27`) on **Activate** — it travels with the subscriptions (already settled above). **Prepare-guard verdict (round 6):** none needed — re-resolving `ConnProvider`/`ClusterIDProvider` into the registry's fields is benign reassignment (provider reads are stable) and re-running the nil-Conn / empty-ClusterID fail-closed checks yields the same verdict; document this on `Prepare` | no (settled; existing coverage) |
| 14 | admin socket | `NewServer` reassignment (Prepare — benign, unstarted); **flock + bind + serve per call** (`srv.Start()`, Activate) | ADD Activate guard: `s.server == nil → return nil` (disabled mode, the row-14 blocker) AND `s.errCh != nil → return nil` (already activated) | **YES** (review-named): repeated Activate returns nil without rebinding; plus the migrated disabled-mode two-phase test — `internal/admin/socket/subsystem_test.go` |
| 15 | crypto policy | **snapshot publish PER POLICY NAME** (`policy/subsystem.go:47-56` loops `cfg.PolicyNames` with short-circuit-on-error, Activate) | ADD Activate guard tracking completion **per policy name** (phase-owned `emitted map[string]bool`) — ⚠️ round 6, WARNING: a single bool is UNSOUND for multi-policy configs. Set after the loop, a retry re-emits the successful prefix (duplicate audit events); set before the loop, it suppresses policies that never emitted. Mark each name on ITS successful emit; retry skips marked names and resumes the failed suffix. (Alternative considered and rejected: validate-and-document a single-policy invariant — `PolicyNames` is a config-driven `[]string` and nothing enforces len==1.) | **YES** (review-named): second Activate publishes nothing NEW; after a simulated mid-loop failure, retry emits ONLY the not-yet-emitted names — `internal/admin/policy/subsystem_test.go` |
| 16 | grpc | server build + service registration (Prepare — double `RegisterService` panics); **reapers + coordinator + bind + serve** (Activate) | KEEP existing (`:230`) as the Prepare guard (re-keyed on `s.grpcServer != nil` if needed); ADD Activate guard (`s.listener != nil`) | no (guard verified by reading; the row-16 focused test covers phase placement) |
| 17 | TLS | cert generation is condition-checked by construction (Prepare) | none — `EnsureCerts` is idempotent by its own regeneration conditions; document. Activate no-op | no |

This is decided **per-subsystem**, not by blanket retirement. The SUMMARY's
17-row table reproduces these verdicts filled in with what was actually done;
a disagreement between a verdict and live code is **reported, not improvised**.

### Blast radius: **16 production + 3 test implementations** (17 prod with TLS)

CONTEXT.md D-11 says "~18 subsystems". The census below lists **both** the
`DependsOn` site and the **`Start` site**, located independently.

> ⚠️ **Census method — do not shortcut this.** An earlier draft built this table
> from `rg -n 'DependsOn\(\) \[\]lifecycle\.SubsystemID'` alone and silently
> mislocated `cluster`'s `Start`. Finding `DependsOn` does **not** find `Start`:
> 15 of 16 production impls co-locate them, and `internal/cluster` does not.
> The `Start` column below was rebuilt by locating `Start` **directly**. Two
> further traps this exposed, both invisible to a `DependsOn` census:
> - **`Start(_ context.Context)`** — the two test stubs (T1, T3) name the
>   parameter `_`, so any regex hard-coding `ctx` misses them. Every regex in
>   this plan matches `(ctx|_)`.
> - **Interfaces re-declare `Start`.** `cluster.Registry` (`registry.go:27`) is
>   an interface whose impl is a subsystem — splitting `Start` means editing the
>   **interface** and its callers, not just the method.

| # | Impl | `DependsOn` site | **`Start` site** |
|---|---|---|---|
| 1 | `store.DatabaseSubsystem` | `internal/store/subsystem.go:43` | `:50` |
| 2 | `eventbus.Subsystem` | `internal/eventbus/subsystem.go:77` | `:86` |
| 3 | `setup.ABACSubsystem` | `internal/access/setup/subsystem.go:65` | `:75` |
| 4 | `setup.AuthSubsystem` | `internal/auth/setup/subsystem.go:59` | `:69` |
| 5 | `setup.WorldSubsystem` | `internal/world/setup/subsystem.go:58` | `:64` |
| 6 | `setup.OutboxRelaySubsystem` | `internal/world/setup/relay_subsystem.go:74` | `:80` |
| 7 | `setup.SessionSubsystem` | `internal/session/setup/subsystem.go:47` | `:53` |
| 8 | `setup.BootstrapSubsystem` | `internal/bootstrap/setup/subsystem.go:103` | `:123` |
| 9 | `setup.PluginSubsystem` | `internal/plugin/setup/subsystem.go:154` | `:165` |
| 10 | `audit.Subsystem` | `internal/eventbus/audit/subsystem.go:255` | `:267` |
| 11 | `chain.VerifierSubsystem` | `internal/eventbus/audit/chain/verifier_subsystem.go:67` | `:77` |
| 12 | `dek.CheckpointSweepSubsystem` | `internal/eventbus/crypto/dek/sweep.go:76` | `:86` |
| **13** | **`cluster.registry`** (unexported) | `internal/cluster/registry.go:174` | **`internal/cluster/heartbeat.go:22`** ⚠️ **different file** |
| 14 | `socket.AdminSocketSubsystem` | `internal/admin/socket/subsystem.go:64` | `:73` |
| 15 | `policy.CryptoPolicySubsystem` | `internal/admin/policy/subsystem.go:40` | `:47` |
| 16 | `grpcSubsystem` (`package main`) | `cmd/holomush/sub_grpc.go:170` | `:228` |
| + | `tls.TLSSubsystem` | `internal/tls/subsystem.go` (new in 07-09) | same file |
| T1 | `stubSubsystem` | `cmd/holomush/core_subsystems_test.go:22` | `:23` (`Start(_ context.Context)`) |
| T2 | `stubSubsystem` | `internal/lifecycle/orchestrator_test.go:29` | `:30` |
| T3 | `stubRegistry` | `internal/eventbus/crypto/invalidation/coordinator_error_test.go:35` | `:36` (`Start(_ context.Context)`) |

### `internal/cluster` — the one split subsystem, and its extra blast radius

| Symbol | Site |
|---|---|
| `type Registry interface` — **declares `Start` at `:27`, `Stop` at `:28`** | `internal/cluster/registry.go:23-52` |
| `type registry struct` | `internal/cluster/registry.go:113` |
| `DependsOn` | `internal/cluster/registry.go:174` |
| **`func (r *registry) Start(ctx) error`** | **`internal/cluster/heartbeat.go:22`** |
| **`func (r *registry) Stop(ctx) error`** | **`internal/cluster/heartbeat.go:110`** |
| `reg.Start(startCtx)` caller | `internal/cluster/clustertest/harness.go:125` |
| `reg.Start(startCtx)` caller | `internal/cluster/clustertest/external.go:87` |

Because `Registry` is an **interface**, the split touches the interface
declaration, the impl in `heartbeat.go`, and both `clustertest` callers.

### D-13.3 — the Prepare/Activate disposition of all 17. SETTLED HERE, NOT BY THE EXECUTOR.

An earlier revision left plugin placement to the executor (*"decide and record
whether launching plugin processes is acquisition or serving; it is the least
obvious of the five"*) and asserted the wrong answer for `eventbus`. **Both are
retracted.** Every subsystem's disposition is decided below, applying D-13.0's
definitions. The executor **applies** this table; it does not author it. Where a
row says "no-op", that is a decided answer, not an unexamined default.

| # | Subsystem | `Prepare` | `Activate` |
|---|---|---|---|
| 1 | `store.DatabaseSubsystem` | **entire `Start` body** — pool + event store + `InitGameID`. A `pgxpool` is a handle; nothing external reaches it. | **no-op** — serves no external surface. |
| 2 | `eventbus.Subsystem` | **entire `Start` body**, `go s.server.Start()` **included** (`subsystem.go:86-126`): the `Config.GameIDProvider` resolution 07-09 (round 7, BLOCKER 1) put at the TOP of the Start body (acquisition — it reads the DB-backed provider; the `EventBus → Database` edge 07-09 added orders the Prepare sweep for it), then connect (embedded or external), obtain JetStream, `EnsureStream`, exporter. **Grounding: `DontListen: true` (`:153`) — the embedded server binds no socket**; it is reachable only via `nats.InProcessServer`. Server-start and client-connect are one atomic call (`:91` → `:164-193`) with no seam. ⚠️ **Do NOT move `go s.server.Start()` to `Activate`** — the earlier instruction to do so made `audit.Prepare` unsatisfiable (`AUDIT_DEP_NOT_STARTED`). | **no-op** — publishing happens through consumers, not here. Document the reasoning inline so nobody "corrects" it later. |
| 3 | `setup.ABACSubsystem` | build the stack, register health (`subsystem.go:112`) — **including the crypto-operator validation 07-09 item 6 moved into this body** (`validateCryptoOperators` against `s.cfg.DB.Pool()`, lax-and-warn; it is stack-building input, so it travels with the stack into `Prepare`). | **the poller goroutine** (`:77`) — a domain work loop. |
| 4 | `setup.AuthSubsystem` | entire body — construction only. | **no-op**. |
| 5 | `setup.WorldSubsystem` | entire body — `pool := s.cfg.DB.Pool()`, `engine := s.cfg.ABAC.Engine()`, `world.NewService(...)` (`subsystem.go:64-70`). The cleanest split in the tree: it is all acquire+wire. | **no-op** — serves nothing. |
| 6 | `setup.OutboxRelaySubsystem` | lease/conn acquisition + wiring. | **the relay loop** — it drains the outbox and publishes: domain traffic. |
| 7 | `setup.SessionSubsystem` | store construction + wiring — the **entire** current `Start` body (`internal/session/setup/subsystem.go:53-57`: one `store.NewPostgresSessionStore(s.cfg.DB.Pool())` assignment + a log line). | **documented no-op** — SETTLED (cross-AI round 5, LOW; the earlier "if its Start launches one" conditional is retracted). Verified live: this `Start` launches **no** reaper, ticker, or goroutine. The session reaper is a `grpcSubsystem`-owned loop (row 16, `sub_grpc.go:758`) — do not relocate it here. The no-op's doc comment names this so nobody "fixes" it. |
| 8 | `setup.BootstrapSubsystem` | **entire body** — policy/setting/admin seeding is one-shot DB work against a prepared pool, not a loop and not external. | **no-op**. |
| 9 | `setup.PluginSubsystem` | **plugin loading, including binary-plugin subprocess launch.** ⚠️ **This is the row the earlier revision deferred; it is DECIDED: acquisition.** Two grounds: (a) a plugin subprocess is a host-controlled child reachable only over the host's own mTLS gRPC — no externally-reachable surface, so it is not "serving" under D-13.0; (b) `audit.Subsystem.DependsOn()` includes `SubsystemPlugins` (`audit/subsystem.go:255-261`) because audit's own acquisition reads plugin manifests to build plugin audit consumers — so loading MUST complete inside the Prepare sweep or audit's Prepare breaks. Also Prepare: `resolvePluginsDir`, the capability registry, the hostfunc bridge, the command registry (`:165-200`). **In practice: the ENTIRE current `Start` body.** ⚠️ **Round 9 (BLOCKER):** the pre-manager error paths in this body leak the binary host's token-store sweeper goroutine and the live Lua host — D-13.2 row 9 carries the settled cleanup contract (extend `cleanupOnError` to close both hosts) and Task 2 § "Plugin prepared-state cleanup" the shape + fault-injection test. | **no-op** — a decided answer, not an unexamined default. ⚠️ **Rev 4 required "event-delivery subscription and command dispatch to plugins" here. No such loop exists** — see the note below. |
| 10 | `audit.Subsystem` | the durable-consumer creation and the **synchronous boot gate its own doc already names** — *"construct the events_audit PartitionManager, re-home the legacy history (Backfill), and ensure partition coverage — **ALL BEFORE the projection starts accepting traffic**"* (`subsystem.go:283-289`). That sentence **is** the Prepare/Activate seam, already written in the code; follow it literally. Also `js := s.jsProv.JS()` / `pool := s.poolProv.Pool()` + the `AUDIT_DEP_NOT_STARTED` fail-closed checks (`:273-279`), `cfg.Validate()`, and the `lateInit` owner-map/plugin-consumer binding. ⚠️ **Store the outputs on phase-owned fields (round 7, BLOCKER — the D-13.2 row-10 verdict):** `s.partitionManager` (today a LOCAL at `:293` that the retention-worker construction at `:352` consumes) and `s.preparedProjection` (the `newProjection` result, `:314`) — without them, nothing carries the prepared projection and partition manager into Activate. ⚠️ Round 8: the lateInit-written `s.cfg.Owners`/`s.pluginMgr` are PART of the prepared aggregate — capture their pre-lateInit values so Prepare's post-lateInit error paths and the prepared-only Stop can restore/clear them (D-13.2 row 10 carries the full contract, including — round 9 — the durables-retained-not-deleted disposition for the prepared-only Stop path). | **`p := s.preparedProjection` → `p.start(workerCtx)`** (`:313-320` today), the plugin consumers' dispatch loops (`s.pluginMgr.Start(workerCtx)` with its existing all-or-nothing rollback), and the retention worker (constructed from `s.partitionManager`) — all domain work loops; on success set `s.worker = p` / `s.cancel = cancel` (the Activate-owned running state). ⚠️ Preserve the deliberate `context.WithCancel(context.Background())` parent; the worker must outlive the boot ctx. Do NOT rebind it to `Activate`'s ctx. |
| 11 | `chain.VerifierSubsystem` | **the chain walk** (INV-CRYPTO-102). It is a **boot gate**, not serving. In `Prepare` it refuses before **anything** activates — independent of any edge. This is the barrier's clearest payoff. | **no-op**. |
| 12 | `dek.CheckpointSweepSubsystem` | repo/emitter wiring. | **the sweep loop**. |
| 13 | `cluster.registry` | **resolve and validate BOTH providers** — `ConnProvider.Conn()` (nil ⇒ fail closed, the nil-Conn check 07-09 relocated into the Start body) and `ClusterIDProvider` (resolved-empty ⇒ `CLUSTER_CONFIG_MISSING_CLUSTER_ID`, fail closed) — storing stable values on the registry. ⚠️ **Round 6, BLOCKER — this row previously said "no-op", which composed with 07-09 to put fallible acquisition in the ACTIVATE sweep, after the barrier, violating D-13.0's own Prepare contract ("acquire and wire everything the subsystem needs"). Retracted.** Provider resolution is acquisition; it belongs in Prepare. | **the entire subscribe/publish/goroutine critical section, WHOLE, under `r.mu`** (`heartbeat.go`, today's `:22-97` minus the resolution now in Prepare): four subscriptions (`:30-64`), the first heartbeat publish (`:72`), and both goroutines (`:82`, `:87`). The `subAlive` guard (`:26`) travels with them. **The no-split settlement governs the CRITICAL SECTION, not resolution** — resolution runs in Prepare before any goroutine exists, so it cannot deadlock against them. |
| 14 | `socket.AdminSocketSubsystem` | **`NewServer(Config{…})` only** — construct the `Server` and the empty-`SocketPath` disabled-mode early return (`subsystem.go:74-77`, WARN kept). **NOT the lock.** ⚠️ See the note below: rev 4 said "acquire `admin.lock`" here; that seam **does not exist**. | **guard first: `if s.server == nil { return nil }`** (cross-AI round 5, BLOCKER — in disabled mode Prepare early-returns before `NewServer`, and the two-sweep orchestrator STILL calls Activate; without this guard `srv.Start()` nil-panics the boot in exactly the degraded no-XDG environment disabled mode exists for). Then **the whole of `srv.Start()`** — lock acquisition, stale-socket removal, `admin.sock` bind, and serve, which `Server.Start` performs as **one atomic call** (`server.go:58-113`). This is the **break-glass** surface — a UDS accepting connections is externally reachable, and it accepting before dependencies have prepared is precisely what D-11 exists to prevent. The barrier's promise still holds exactly: the bind is in `Activate`. |
| 15 | `policy.CryptoPolicySubsystem` | wiring. | **the policy-snapshot emit** — it publishes: domain traffic. |
| 16 | `grpcSubsystem` (`package main`) | build the server, register services, wire the emitter, construct `invalidation.Coordinator` (07-09 § Task 2 item 4), **and construct BOTH reapers plus their shared ctx/cancel state** (cross-AI round 5, BLOCKER — rev 7 assigned the reapers to neither sweep): `session.NewReaper(…)` and `auth.NewGuestReaper(…)` with `reaperCtx`/`reaperCancel` stored on the subsystem. Construction only — no `go …Run(…)` here. | **launch both reaper `Run` goroutines** (`go s.sessionReaper.Run(reaperCtx)`, `go s.guestReaper.Run(reaperCtx)` — domain work loops: lease sweeps call `engine.HandleDisconnect`, guest reaping deletes characters + emits tombstones; today they launch at `sub_grpc.go:758`/`:769` BEFORE the listener bind), then `coordinator.Start()`, then **bind the listener + serve**. `Stop` cancels/drains all owned activity (the existing `reaperCancel` path — unchanged). **D-11's whole reason — get this split exactly right.** ⚠️ A Coordinator construction/start failure must still WARN-and-continue: `Activate` MUST NOT propagate it, or `StartAll` turns a degraded boot into a failed one (07-09 § Task 2 item 4). |
| 17 | `tls.TLSSubsystem` (new in 07-09) | **cert generation** — filesystem work against a prepared gameID; nothing external. | **no-op** (07-09 already documents `Stop` as a no-op for the same reason). |

#### ⚠️ Row 9 — rev 4 assigned PluginSubsystem an Activate loop THAT DOES NOT EXIST. Settled: documented no-op. (cross-AI review round 3, HIGH; verified live)

Rev 4's row required *"event-delivery subscription and command dispatch to
plugins"* to start in `Activate`. **`PluginSubsystem` runs no such loop.**
Verified live:

- `internal/plugin/setup/subsystem.go:163-165` — *"`Start` builds the full plugin
  stack and command registry"*. Its body resolves the plugins dir, builds the
  capability registry, the hostfunc bridge, the Lua host, loads plugins, and
  registers commands — **then returns**. It launches **no** goroutine, opens
  **no** subscription, and starts **no** ticker. (Confirmed by inspection of the
  whole body: no `go `, no `Subscribe`, no `for {}`.)
- Runtime delivery is a **synchronous method invoked by callers** —
  `Manager.DeliverEvent` (`internal/plugin/manager.go:458`) is called *by* the
  event subscriber and the command dispatcher. Those live elsewhere; the plugin
  subsystem owns no loop of its own.

**SETTLED: `Prepare` = the entire current `Start` body. `Activate` = a documented
no-op returning nil.** Its doc comment MUST say *why* — "plugin delivery is a
synchronous manager method invoked by the event subscriber and the command
dispatcher (`manager.go:458`); this subsystem owns no work loop" — so the next
reader does not "fix" the no-op by inventing one.

⚠️ **Do NOT relocate a delivery loop here to satisfy the row.** Moving the
subscriber or dispatcher into `PluginSubsystem` is architecture work well outside
this plan's scope (and touches MEDIUM-4's plugin⇄eventbus⇄grpc coupling, which
07-CONTEXT.md § Out of scope assigns to **Phase 8** and D-01 forbids deepening).
The row is now descriptive of the code that exists. **If you find a real
production-owned delivery loop this note missed, stop and report** — do not
improvise a placement.

#### ⚠️ Row 14 — rev 4 required a seam `internal/admin/socket` DOES NOT HAVE. Settled: option (a). (cross-AI review round 3, HIGH; verified live)

Rev 4's row read: *"`Prepare`: create the server, **acquire `admin.lock`**"* /
*"`Activate`: bind `admin.sock` + begin serving"*. **There is no API that does
that.** Verified live at `internal/admin/socket/server.go:58-113`:

> `// Start acquires admin.lock, removes stale admin.sock, binds the UDS, and`
> `// begins serving. Returns a channel that carries at most one server error.`
> `func (s *Server) Start() (<-chan error, error)`

`Start()` performs **all four steps atomically** — `acquireLock` (`:63`),
stale-socket `os.Remove` (`:69-76`), `net.Listen("unix", …)` (`:78`), `os.Chmod`
(`:87`), `go srv.Serve(ln)` (`:103`). There is **no separate lock-acquisition
entry point**, and `AdminSocketSubsystem.Start` (`subsystem.go:73`) delegates to
it wholesale. Acquiring the lock in `Prepare` and then calling the existing
`Start()` in `Activate` would **re-acquire the same flock**.

**SETTLED — option (a): `Prepare` constructs, `Activate` calls the existing atomic
`Start()`.** Concretely:
- `Prepare`: the `SocketPath == ""` disabled-mode early return
  (`subsystem.go:74-77`, keeping its WARN) and `srv := NewServer(Config{…})`
  (`:79-88`), stored on the subsystem. `NewServer`'s own doc already promises
  *"No live resources are allocated until Start"* (`server.go:53`), so this is a
  truthful `Prepare`.
- `Activate`: **FIRST the disabled-mode guard — `if s.server == nil { return nil }`**
  (cross-AI round 5, BLOCKER). Under two sweeps, Prepare's early return does NOT
  skip Activate: the orchestrator calls Activate on every subsystem, and without
  the guard `srv.Start()` nil-panics the boot in exactly the degraded environment
  (no XDG runtime dir) disabled mode exists to survive. The WARN was already
  logged in Prepare; Activate returns nil silently. THEN
  `errCh, err := srv.Start()` plus the existing post-start error-monitor wiring,
  unchanged.
- **Migrate the disabled-mode test to drive BOTH phases** (cross-AI round 5):
  `TestAdminSocketSubsystemStartIsNoopWhenSocketPathEmpty`
  (`internal/admin/socket/subsystem_test.go:110-121`) currently exercises only
  the single-phase behavior. Rewritten (ACE-renamed for the new interface): call
  `Prepare` then `Activate` on an empty-`SocketPath` subsystem, assert both
  return nil and `sub.server` stays nil — the assertion that fails with a
  nil-pointer panic if the guard is dropped.

**⚠️ State the consequence plainly — `admin.lock` acquisition MOVES TO `Activate`.**
That is a real, accepted difference from rev 4's intent, and it is the *reason*
option (b) was rejected rather than an oversight:
- **What is lost:** a second holder of `admin.lock` (a stale process, a
  double-launch) is now detected in the Activate sweep rather than the Prepare
  sweep — i.e. after every subsystem has acquired, instead of before. The boot
  still **fails closed** (`Server.Start` returns the error → `Activate` returns it
  → `StartAll` aborts → `StopAll` rolls back everything prepared); it just fails
  slightly later and does more work first.
- **What is NOT lost:** the barrier's actual guarantee. `admin.sock` still binds
  in `Activate`, which is the only thing D-13.0's promise and the row-14 sanity
  check assert. **No external domain surface moves into `Prepare`.**
- **Option (b) — genuinely designing `Server.Prepare`/`Server.Activate` — is
  REJECTED.** It buys only earlier lock detection, and costs: splitting an
  atomic acquire/bind sequence whose error paths carefully unwind each other
  (`server.go:72`, `:80`, `:88-89` each close the lock file on failure), a new
  half-open `Server` state between the two calls, plus `server.go` and all of
  `internal/admin/socket`'s tests in this plan's blast radius. Not worth it for a
  few milliseconds of earlier failure on a break-glass path. **Recorded as
  considered, not overlooked.**
- Consequently `internal/admin/socket/server.go` is **not** in `files_modified` —
  deliberately. Its tests are (they call `Subsystem.Start`).

Also note (cross-AI review, LOW): `AdminSocketSubsystem.DependsOn()` returns
`nil` today (`internal/admin/socket/subsystem.go:64-66`). Under this plan the
handlers reach it via 07-09's `cryptoWiring` provider, so THE RULE applies and it
declares the wiring's full dep set — **plus `SubsystemCryptoChainVerifier`**
(07-09 item 8, the T-07-51 re-scope: the chain walk precedes the break-glass
bind; 07-10 Task 3 pins verifier-before-admin). Do **not** let a nil-deps test
stub be mistaken for production truth, and do not drop the verifier edge while
splitting — the two-sweep barrier subsumes it only while Wave B is actually
deployed, and the topo pin goes red if it vanishes.

#### ⚠️ Row 16 — rev 7 assigned the two gRPC reapers to NEITHER sweep. Settled: construct in Prepare, launch in Activate. (cross-AI round 5, BLOCKER; verified live)

Current `grpcSubsystem.Start` launches two **domain work loops before the
listener bind**: the session reaper (`go s.sessionReaper.Run(reaperCtx)`,
`cmd/holomush/sub_grpc.go:758` — lease sweeps invoke `engine.HandleDisconnect`
and release guests) and the guest reaper (`go s.guestReaper.Run(reaperCtx)`,
`:769` — reaping deletes characters through `CharacterWriter.Delete` and emits
`character_deleted` tombstones). Rev 7's row 16 assigned Prepare =
build/register/wire/construct-Coordinator and Activate = bind+serve+coordinator —
the reapers appeared in neither column, and under this plan's own
stop-and-report rule an executor would have to halt there (or worse, leave the
loops in Prepare, violating D-13.0). Settled:

- **`Prepare`** constructs both reapers and their shared `reaperCtx`/
  `reaperCancel` state, stored on the subsystem. No goroutine launches.
- **`Activate`** launches both `Run` goroutines FIRST (so lease/guest hygiene is
  live before external traffic lands), then `coordinator.Start()`, then the
  listener bind + `Serve` — per the amended row 16.
- **`Stop`** cancels/drains all owned activity via the existing `reaperCancel`
  path — unchanged; its rollback-state column now also covers "reapers
  constructed but never launched" (cancel a never-used ctx is safe).
- **The guest reaper needs a timing seam FIRST (cross-AI round 7, MEDIUM — the
  test below is not implementable without it):** `grpcSubsystemConfig` exposes
  only the session `ReaperInterval` (`sub_grpc.go:81`); the guest reaper
  hardcodes `Interval: 1 * time.Minute` in its construction
  (`sub_grpc.go:766`), so "shortened interval" cannot reach it as the config
  stands. ADD `GuestReaperInterval time.Duration` to `grpcSubsystemConfig`
  (next to `ReaperInterval`), defaulting to `1 * time.Minute` when zero —
  production wiring passes nothing and observable behavior is unchanged — and
  wire it into the `auth.GuestReaperConfig{Interval: …}` construction. The
  `IdleTTL: 10 * time.Minute` stays hardcoded: the test observes list/sweep
  CALLS, not reap outcomes, so only the tick interval needs a seam.
- **Focused test — neither reaper runs after `Prepare` alone** (add to
  `cmd/holomush/sub_grpc_test.go`, following that file's existing grpcSubsystem
  construction patterns; integration-tag it only if unit fakes cannot satisfy
  `Prepare`): inject a recording session store + guest lister with BOTH
  intervals shortened (`ReaperInterval` AND the new `GuestReaperInterval`),
  call `Prepare` only, wait ≥ 3× the longer shortened interval, assert **zero**
  sweep/list calls recorded; then `Activate` and assert (Eventually) calls
  arrive from BOTH reapers. The dropped-reaper failure mode is silent —
  sessions never expire, guests never reap — and only this test makes it loud.

**Sanity check the table against the barrier's promise:** the only rows with an
externally-reachable `Activate` are **14** (`admin.sock`) and **16** (gRPC
listener); the rest activate only domain work loops. That is the guarantee in
D-13.0, stated per-row. If a proposed split would put an external **domain** bind
in `Prepare`, the split is wrong, not the table.

⚠️ **"Domain" is load-bearing in that sentence — do not generalize it to "any
external bind".** Row 2's `Prepare` legitimately binds an **observability** port:
the NATS Prometheus monitor passes `HTTPPort: s.cfg.MonitorPort`
(`internal/eventbus/subsystem.go:156`; `MonitorPort` declared at
`internal/eventbus/config.go:52`) and `DontListen: true` (`:153`) suppresses only
the **client** listener — the monitoring HTTP socket still binds during `Prepare`.
That is D-13.0's **exception 1**, deliberate and recorded. A reader who applies
this sanity check as an absolute would flag row 2 as a violation of the phase's
own guarantee and "fix" a design that is correct. The barrier constrains
**domain** surfaces; observability is the same class as the observability server
that already starts before `StartAll` today (`cmd/holomush/core.go:351`).

### ⚠️ The CALLERS are the real blast radius — ~27 files beyond the 17 impls. (cross-AI review round 3, HIGH; live census; heading count reconciled to the rev-7 corrected total in round 8)

Rev 4's `files_modified` listed the **implementations** and almost none of the
**callers**. Removing `Subsystem.Start` breaks every direct caller at compile
time. The review named 4; rev 5's census found ~24 files; **round 4 found that
census still short by 3** (the argument-shape regex — see Step 0) — the corrected
total is **~27 files**, now enumerated in `files_modified`:
`internal/admin/policy/subsystem_test.go`,
`internal/admin/policy/subsystem_integration_test.go`, and
`internal/eventbus/audit/subsystem_test.go` joined in rev 7.

**Why this was missed, and why it is dangerous:** the overwhelming majority are
`//go:build integration` files. `task build` does **not** compile `_test.go`, and
`task test` does **not** compile integration-tagged files — so ~21 of these
breakages are invisible to both. **`task test:int` is the only gate that sees
them.** This is precisely 07-CONTEXT.md § Landmines' warning: *"`task test` does
NOT compile `//go:build integration` files. This phase is a cross-package type
refactor — the exact shape that breaks integration silently."*

**The dominant caller shape** is a hand-wired subsystem in an integration test:
`hostSub.Start(ctx)` on `audit.Subsystem` appears in ~12 files under
`test/integration/`. `internal/testsupport/integrationtest/` hand-wires rather
than using the orchestrator (07-10 § Task 2 verified this), so these callers drive
the subsystem lifecycle directly and each one must become a `Prepare` **then**
`Activate` pair.

**The migration rule for every caller — do not shortcut it:**

> A `sub.Start(ctx)` caller becomes `sub.Prepare(ctx)` **followed by**
> `sub.Activate(ctx)`, preserving the existing error handling on **both** calls
> (`require.NoError` / `Expect(...).To(Succeed())` / `if err != nil`).

⚠️ **Do NOT collapse a caller to `Prepare` alone.** That is the silent-failure
mode of this task: the file compiles, the test runs, and the subsystem's work
loops **never start** — so an audit projection test would assert against a
projection that is not consuming. A green suite proving nothing. Where a test
deliberately wants acquisition only (e.g. asserting a fail-closed dep check like
`AUDIT_DEP_NOT_STARTED`), calling only `Prepare` **is** correct — but say so in a
comment at that site, so the omission reads as deliberate rather than dropped.

⚠️ **Do NOT add a `Start` compatibility shim** to keep callers compiling. It would
defeat the entire point of the interface change (D-11: *"an interface that cannot
express 'serve before acquire' cannot be"*) by leaving a method that does both.
The compile errors **are** the census; work through them.

### Non-subsystem `Start` methods that MUST keep `Start`

These have **no `DependsOn`** and are not `lifecycle.Subsystem` — do not migrate
them, and do not write a repo-wide "zero `Start` methods" gate that would demand
it (verified live):

| Type | Site |
|---|---|
| `audit.RetentionWorker` | `internal/audit/retention.go:127` |
| `audit.PluginConsumerManager` | `internal/eventbus/audit/plugin_consumer.go:235` |
| `invalidation.coordinator` (+ its interface decl at `coordinator.go:22`) | `internal/eventbus/crypto/invalidation/coordinator.go:69` |
| `socket.Server` (round 8 — no-ctx, returns `(<-chan error, error)`; the atomic lock/bind/serve op row 14 mandates stays UNMODIFIED, invoked from `AdminSocketSubsystem.Activate`) | `internal/admin/socket/server.go:60` |
| `world.OrphanDetector` (round 8 — non-subsystem periodic cleanup helper; `Start(ctx)` returns nothing and has no `DependsOn`) | `internal/world/property_lifecycle.go:108` |

⚠️ **Round 8 (verified blocker):** the last two rows are legitimate survivors
the "no `Start` compatibility shim" sweep's previous three-item exemption did
not name — that criterion was IMPOSSIBLE on correct code (`Server.Start` is
required by this plan's own row 14; `OrphanDetector.Start` is not a subsystem).
The sweep criterion now classifies survivors by RECEIVER, exactly like Step 0's
caller census. Re-run the sweep after migration: any FURTHER survivor is
classified — a `lifecycle.Subsystem` receiver is a failed migration; a
non-subsystem receiver is recorded in the SUMMARY census as a legitimate
survivor, never deleted to satisfy a count.
</settlements>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-event-model-bootstrap-decomposition/07-CONTEXT.md
@.planning/phases/07-event-model-bootstrap-decomposition/07-PATTERNS.md
@.claude/rules/logging.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Split the Subsystem interface and give the Orchestrator two sweeps</name>
  <read_first>
    - internal/lifecycle/subsystem.go (read in full — the interface at :42-58 and the "Start … MUST be idempotent" contract at :51)
    - internal/lifecycle/orchestrator.go (read in full — StartAll's loop + rollback at :44-78, StopAll at :81-96 as 07-10 left it, topoSort at :98-149)
    - internal/lifecycle/orchestrator_test.go (the existing stub returning s.deps, and the rollback tests)
    - internal/access/setup/subsystem.go:69-78 (the idempotency guard and its two-part rationale — one half true, one half dying)
    - .planning/phases/07-event-model-bootstrap-decomposition/07-PATTERNS.md § 5 (the full impl census) and § "No Analog Found" (the confirmation that Prepare/Activate rollback has no precedent)
  </read_first>
  <files>internal/lifecycle/subsystem.go, internal/lifecycle/orchestrator.go, internal/lifecycle/orchestrator_test.go</files>
  <behavior>
    - Given subsystems A→B→C, `StartAll` calls Prepare(A), Prepare(B), Prepare(C), then Activate(A), Activate(B), Activate(C) — recorded call order proves NO Activate precedes ANY Prepare.
    - When Prepare(B) fails, Activate is never called on anything; Stop is called on **B and A** — the failing subsystem is stopped too, because a failed Prepare may have partially acquired resources — in reverse order (B then A); StartAll returns a `SUBSYSTEM_START_FAILED`-class error naming B.
    - When Activate(B) fails, Stop is called on C, B, A — everything prepared, in reverse order — and StartAll returns an error naming B.
    - When Activate(C) fails, Stop is called on C, B, A.
    - **When the startup ctx is ALREADY CANCELLED and Prepare(B) fails because of it, rollback still calls Stop on B and A** — cleanup does not inherit the cancellation that caused the failure.
    - Rollback honors a bounded deadline (07-10's behavior is preserved, but on a fresh context — see the action).
    - Stop on a prepared-but-never-activated subsystem is called and must not error.
    - `topoSort` order is unchanged from today for the same graph.
  </behavior>
  <action>
**RED first:** write the recorded-call-order tests above in
`internal/lifecycle/orchestrator_test.go` against the new interface. They will
not compile until the interface exists, which is the RED. Record it in the
SUMMARY.

`internal/lifecycle/subsystem.go` — replace `Start(ctx) error` with the three
methods, whose doc comments carry **D-13.0's definitions verbatim** (read that
settlement first — the wording is load-bearing and an imprecise "MUST NOT begin
serving" is what made the earlier revision incoherent):

- `Prepare(ctx context.Context) error` — acquire resources and wire, **including
  bringing up process-internal substrate other subsystems must acquire against**
  (the embedded NATS server, the DB pool, JetStream streams/durable consumers,
  plugin processes). MUST NOT expose a **domain** surface reachable from
  **outside the process**, and MUST NOT start a work loop carrying **domain
  traffic**. MUST be idempotent.
  The doc MUST name the reason the boundary is drawn at "externally reachable
  **domain** surface" rather than "anything running":
  `internal/eventbus/subsystem.go:153` sets `DontListen: true`, and `audit`'s
  acquisition requires that server live (`audit/subsystem.go:273-279`) — a
  barrier that forbade it would be circular.
  ⚠️ **Write "domain surface", not "any surface"** (D-13.0 exception 1). An
  unqualified "MUST NOT expose a surface reachable from outside the process" is
  **false as shipped**: `eventbus.Prepare` binds the NATS Prometheus monitor's
  operator-facing HTTP port (`HTTPPort: s.cfg.MonitorPort`, `subsystem.go:156`;
  `DontListen: true` suppresses only the **client** listener). The doc MUST name
  that observability carve-out, or the very first subsystem in the census
  violates its own interface contract.
- `Activate(ctx context.Context) error` — begin serving: bind/accept
  externally-reachable connections, start domain work loops. MUST assume every
  other subsystem has already prepared. MUST be idempotent.
- `Stop(ctx context.Context) error` — unchanged signature. Extend the doc: Stop
  MUST be safe to call on a subsystem that was prepared but never activated;
  MUST be idempotent; MUST NOT block indefinitely.

The `Prepare`/`Activate` doc comments carry the **corrected** idempotency
rationale (see `<settlements>` D-13.2): each MUST be idempotent because a
subsystem may guard a non-idempotent side effect — launching a goroutine (the
ABAC poller, `internal/access/setup/subsystem.go:77`) or binding a listener —
and the orchestrator does not promise exactly-once under rollback. **Do NOT**
carry the "so it can be pre-started in core boot before the orchestrator drives
StartAll" rationale — 07-09 deleted every pre-start.

`internal/lifecycle/orchestrator.go` — rewrite `StartAll`:
- `order, err := o.topoSort()` — unchanged.
- Reset `o.preparedOrder` and `o.activatedOrder` (replacing `startOrder`).
- Sweep 1: for each id in `order`, `Prepare`; on error, roll back (below) and
  return the wrapped `SUBSYSTEM_START_FAILED` error (keep the existing
  `oops.Code("SUBSYSTEM_START_FAILED").With("subsystem", …).Wrapf(...)` shape at
  `:61-64` so existing assertions keep passing).
- Sweep 2: for each id in `order`, `Activate`; on error, roll back and return the
  same error shape; else append to `activatedOrder`.

### ⚠️ Rollback is UNSAFE as originally drafted — two independent bugs (cross-AI review, HIGH)

Both verified live against `internal/lifecycle/orchestrator.go:44-78`. Fix both;
each has its own test in `<behavior>`.

**Bug 1 — the failing subsystem is never cleaned up.** Today `StartAll` appends
to `startOrder` **only after** `sub.Start(ctx)` succeeds (`:56-67`), and rollback
walks only `startOrder`. So the subsystem that **just failed** is never stopped —
even though a failed `Prepare` is precisely the case most likely to have
**partially acquired** resources (half its connections dialed, a listener bound,
a file handle open) before erroring. The naive Prepare loop inherits this exactly.
→ **Append the id to `preparedOrder` BEFORE calling `Prepare`, not after** (or
otherwise ensure the failing target is included in the rollback walk). This makes
`Stop` responsible for tolerating a subsystem that never fully prepared — which is
already the contract this task writes into `Stop`'s doc: *"MUST be safe to call on
a subsystem that was prepared but never activated; MUST be idempotent."* Extend
that sentence to cover **partially prepared** explicitly. `StopAll` walking
`preparedOrder` in reverse then cleans the failing target first, which is correct
ordering.

**Bug 2 — rollback must not inherit the cancelled startup context.** ⚠️ Rev 7
note: **07-10 Task 1 already fixed this for the single-phase `StartAll`**
(cross-AI round 4 moved the fix forward so 07-10 shipping alone does not amplify
abandon-on-cancel — rollback there now builds
`stopCtx, cancel := context.WithTimeout(context.Background(), 5*time.Second)`
and calls `o.StopAll(stopCtx)`). **This task's job is to PRESERVE that through
the two-sweep rewrite**: BOTH new rollback paths (Prepare-fails-at-i,
Activate-fails-at-j) roll back on a fresh, bounded,
`context.Background()`-parented context — never the startup ctx, and never a
ctx derived from it (derivation re-inherits the cancellation). The failure mode
it prevents: the startup ctx is what *caused* the failure (SIGINT during boot,
a boot deadline elapsing), it is already cancelled, and the deadline-aware
`StopAll` would see `ctx.Done()` closed and abandon every Stop — leaking
exactly the resources rollback exists to release. 07-10 made `StopAll`
correct-vs-hang AND rollback correct-vs-cancel; do not regress either while
rewriting `StartAll`. Reuse 07-10's 5s bound rather than inventing a new
constant.
⚠️ `StopAll`'s **normal** (non-rollback) callers keep passing their own ctx —
the fresh-context rule is for the **rollback paths inside `StartAll`** only.
The already-cancelled-ctx behavior test in `<behavior>` re-pins the property
against the new two-sweep paths.
- Keep the per-subsystem `slog.InfoContext` + duration logging (`:55-74`),
  labelling the phase.
- `StopAll(ctx)` walks **`preparedOrder`** in reverse (it is a superset of
  `activatedOrder`, so one walk covers both — this is why 3 methods suffice) and
  keeps 07-10's deadline handling and its skipped-subsystem error log verbatim.
- `startOrder` is gone; grep for any other reader.

Use `slog.*Context` variants throughout (`.claude/rules/logging.md`).

⚠️ **Commit discipline (cross-AI round 11, rev 14 — BLOCKER): Tasks 1–3 are ONE
atomic execution unit producing ONE commit at Task 3's boundary** (the settled
07-01/07-05 squash pattern). Mid-unit — after this task, before Tasks 2–3 land —
the repo does NOT compile as a whole: all 17 production impls still implement
`Start` until Task 2, and the two Start-only test stubs
(`cmd/holomush/core_subsystems_test.go:23`,
`internal/eventbus/crypto/invalidation/coordinator_error_test.go:36`) survive
until Task 3. That is expected and deliberate. Work the three tasks as separate
work units for context control, but do NOT commit at this task's boundary —
squash all three tasks' edits into ONE commit after Task 3's whole-repo gates
are green ("Every commit pushed builds"; never push a commit whose `task build`
fails). This task's own gate is therefore scoped to `./internal/lifecycle/`,
which compiles standalone (its own tests migrate here).
  </action>
  <acceptance_criteria>
    - RED recorded in the SUMMARY before the fix
    - `rg -c 'Start\(ctx context.Context\) error' internal/lifecycle/subsystem.go` returns 0
    - `rg -c 'Prepare\(ctx context.Context\) error|Activate\(ctx context.Context\) error' internal/lifecycle/subsystem.go` returns 2
    - `rg -c 'Unprepare|Deactivate' internal/lifecycle/` returns 0 (three methods, not four)
    - `rg -c 'pre-started' internal/lifecycle/subsystem.go` returns 0 (the false rationale is not carried)
    - `rg -c 'preparedOrder' internal/lifecycle/orchestrator.go` returns at least 2 and `rg -c 'startOrder' internal/lifecycle/` returns 0
    - `rg -c 'SUBSYSTEM_START_FAILED' internal/lifecycle/orchestrator.go` returns at least 1 (error code preserved)
    - A test proves no Activate precedes any Prepare via recorded call order: `task test -- -run 'StartAll' ./internal/lifecycle/` exits 0
    - Tests cover Prepare-fails and Activate-fails rollback separately and pass
    - **Rollback Bug 1 pinned:** a test asserts that when `Prepare(B)` fails, `Stop` is called on **B itself** (not only on A) — the failing subsystem may hold partially-acquired resources
    - **Rollback Bug 2 pinned:** a test asserts that with an **already-cancelled** startup ctx, a Prepare failure still results in `Stop` being called on every prepared subsystem — rollback must not inherit the cancellation that caused the failure
    - `rg -c 'StopAll\(ctx\)' internal/lifecycle/orchestrator.go` returns 0 inside `StartAll`'s rollback path — rollback uses a fresh bounded context (`context.Background()`-derived), verified by reading
    - `Stop`'s doc contract in `internal/lifecycle/subsystem.go` explicitly covers the **partially prepared** case, not just "prepared but never activated"
    - `task test -- ./internal/lifecycle/` exits 0
  </acceptance_criteria>
  <verify>
    <automated>task test -- ./internal/lifecycle/</automated>
  </verify>
  <done>The interface cannot express "serve before acquire"; `StartAll` runs two sweeps with a global barrier; both rollback paths tear down everything prepared, in reverse, within the deadline; three methods, not four. (No commit yet — Tasks 1–3 squash into one green commit at Task 3's boundary; round 11, rev 14.)</done>
</task>

<task type="execute">
  <name>Task 2: Migrate all 17 production implementations</name>
  <read_first>
    - internal/world/setup/subsystem.go:53-70 (THE cleanest split candidate — its Start already reads `pool := s.cfg.DB.Pool()` / `engine := s.cfg.ABAC.Engine()` (ACQUIRE) then wires; the seam is already visible)
    - internal/access/setup/subsystem.go:69-115 (the idempotency guard whose rationale is half-true; and the ReadinessRegistry registration at :112)
    - internal/eventbus/subsystem.go:77-120 (Start: brings NATS up, obtains JetStream, declares the EVENTS stream)
    - cmd/holomush/sub_grpc.go:165-300 (grpcSubsystem — the one that literally begins serving; its Start is the clearest Prepare/Activate split in the tree)
    - internal/plugin/setup/subsystem.go:154-260,485-510 (PluginSubsystem + ConfigureSystemBroadcaster's late-binding requirement)
    - internal/cluster/registry.go:23-52 (the `Registry` **interface** — it declares `Start` at `:27` and `Stop` at `:28`; the split MUST edit the interface, not only the impl) and :174 (DependsOn — the only lifecycle method actually in this file)
    - internal/cluster/heartbeat.go:15-172 (**the real `Start` (`:22`) and `Stop` (`:110`) — NOT in registry.go.** Read the lock discipline at `:19-21` and `:104-109` before splitting: `Start` holds `r.mu` for its entire body; the first publish (`:72`) calls `publishHeartbeatLocked` precisely to avoid a RLock/write-lock self-deadlock; `Stop` is a deliberate 3-phase locked/unlocked/unlocked dance because `wg.Wait()` under `r.mu` would deadlock against the goroutines. A Prepare/Activate split MUST preserve all of this — splitting the body across two methods must not split a critical section.)
    - internal/cluster/clustertest/harness.go:110-130 and internal/cluster/clustertest/external.go:72-95 (the two `reg.Start(startCtx)` callers — they break at compile time)
    - .planning/phases/07-event-model-bootstrap-decomposition/07-PATTERNS.md § 5 (the census — cross-check it against this plan's `<settlements>` census, which supersedes it for `cluster`'s `Start` location)
  </read_first>
  <files>internal/store/subsystem.go, internal/tls/subsystem.go, internal/eventbus/subsystem.go, internal/access/setup/subsystem.go, internal/auth/setup/subsystem.go, internal/world/setup/subsystem.go, internal/world/setup/relay_subsystem.go, internal/session/setup/subsystem.go, internal/bootstrap/setup/subsystem.go, internal/plugin/setup/subsystem.go, internal/eventbus/audit/subsystem.go, internal/eventbus/audit/chain/verifier_subsystem.go, internal/eventbus/crypto/dek/sweep.go, internal/cluster/registry.go, internal/cluster/heartbeat.go, internal/cluster/clustertest/harness.go, internal/cluster/clustertest/external.go, internal/admin/socket/subsystem.go, internal/admin/policy/subsystem.go, cmd/holomush/sub_grpc.go, cmd/holomush/sub_grpc_test.go, internal/plugin/setup/subsystem_test.go, internal/admin/socket/subsystem_test.go, internal/admin/policy/subsystem_test.go, internal/eventbus/audit/subsystem_test.go, internal/world/setup/relay_subsystem_test.go, internal/eventbus/crypto/dek/sweep_integration_test.go</files>
  <action>
**⚠️ Unit discipline (cross-AI round 11, rev 14 — BLOCKER): this task sits
mid-unit; its whole-repo `task test`/`task test:int` gates are RELOCATED to
Task 3, the unit end.** Rev 13 demanded `task build && task test && task test:int`
plus a commit at this task's boundary — unsatisfiable on correct code, because
two Start-only stubs survive until Task 3 and BOTH are compiled by `task test`:
`cmd/holomush/core_subsystems_test.go:23`'s `stubSubsystem`
(`//go:build !integration` — included in `task test`; passed where
`lifecycle.Subsystem` is required by `productionSubsystems(...)`) and
`internal/eventbus/crypto/invalidation/coordinator_error_test.go:36`'s
`stubRegistry` (NO build tag — compiled by `task test` AND `task test:int`; used
as a `cluster.Registry`, the very interface this task migrates). That demand
also contradicted this plan's own atomicity note ("the tree does not compile
until all 17 impls and every caller land together"). This task's inner-loop
gates are `task build` (satisfiable here: `_test.go` is invisible to it, and
every non-test caller — including `clustertest`, `eventbustest/embedded.go`,
and the untagged `integrationtest` support files — migrates in this task) plus
the package-scoped unit tests in the criteria. The whole-repo `task test`/
`task test:int`, the KEK boot gate, the topo pin, the post-commit `task fmt`
check, and the unit's single squashed commit all land at Task 3's boundary.
**Do NOT commit at this task's boundary.**

**Task-level manifest (cross-AI round 11, rev 14 — MEDIUM): this task's
`<files>` now also names the test files its own criteria create or edit** (the
rev-13 manifest listed only production files plus the two `clustertest`
callers): `cmd/holomush/sub_grpc_test.go` (row-16 reaper focused test),
`internal/plugin/setup/subsystem_test.go` (row-9 fault-injection RED test),
`internal/admin/socket/subsystem_test.go` (row-14 two-phase disabled-mode +
repeated-Activate tests), `internal/admin/policy/subsystem_test.go` (row-15
per-policy-name retry tests), `internal/eventbus/audit/subsystem_test.go`
(row-10 repeated-Prepare / Stop-after-Prepare-only / Prepare-fail-retry tests),
`internal/world/setup/relay_subsystem_test.go` (row-6 repeated-Activate test),
and `internal/eventbus/crypto/dek/sweep_integration_test.go` (row-12 repeated
sweep test). Step 0's caller census additionally edits the ~27 caller files
enumerated in the plan frontmatter's `files_modified` — that plan-level list
remains the authoritative caller manifest for this task's migration sweep.

Split every production `Start` into `Prepare` + `Activate` using the census in
`<settlements>`, which lists the `DependsOn` site and the **`Start` site**
separately for all 17. **Edit where `Start` is, not where `DependsOn` is** — for
`cluster` those are different files. Verify each line number before editing;
they drift.

**Accessor diagnostics migrate with the method they reference (cross-AI round
12, rev 15 — LOW fold-in):** the preserved accessor panic guards and their doc
comments say "called before `Start()`" — e.g. `Pool()`/`GameID()` at
`internal/store/subsystem.go:89-95`, `Manager()`/`CommandRegistry()` at
`internal/plugin/setup/subsystem.go:474-477,500-514`, and the
`TLSConfig()` guard 07-09 Task 1 mints in `internal/tls/subsystem.go`. After
this task `Start` no longer exists on any subsystem, so migrate every
subsystem-receiver accessor doc comment and panic string to "called before
`Prepare()`" across the 17 production subsystem files (accessors resolve in
`Prepare`, so that is the true contract). The eventbus oops message
"EnsureStream called before Start" (`internal/eventbus/subsystem.go:255`) is
the same class — subsystem receiver, no parens, so a parens-keyed grep misses
it: migrate it too. Any exact-string test asserting an old message migrates
with it. Scope by RECEIVER, exactly like this task's Start-survivor sweep:
non-subsystem types — e.g. `world.OrphanDetector`'s "called before Start or
StartupCheck" (`internal/world/property_lifecycle.go:63`) and
`audit.PluginConsumerManager`'s "MUST be called before Start"
(`internal/eventbus/audit/plugin_consumer.go:141`) — legitimately keep `Start`
and keep their strings; do NOT sweep them.

### ⚠️ STEP 0 (MANDATORY, FIRST) — re-census the CALLERS, not just the impls

Rev 4 listed the 17 implementations and almost none of their **callers**, so the
mandated gates could not have passed (cross-AI review round 3, HIGH). **Rev 6's
census regex was then itself the defect** (cross-AI round 4, BLOCKER): it
whitelisted ARGUMENT shapes — `\.Start\((ctx|context\.|startCtx|_)` — and missed
`s.Start(t.Context())` callers, and its classification dropped two more files.
Argument-shape whitelists ALWAYS under-match; **classify by RECEIVER TYPE,
never by argument shape.** Read `<settlements>` § "The CALLERS are the real
blast radius" before editing, then re-run the corrected census (line numbers
drift):

```
rg -n '\.Start\(' --type go -g '!*.pb.go' | rg -v 'orch\.StartAll'
```

This is deliberately BROAD (hundreds of hits) — the census is the sweep PLUS the
classification, and a narrower regex is how three callers went missing for two
revisions. (A `go/packages`-based census that resolves each receiver's type to
`lifecycle.Subsystem` is an acceptable substitute if you prefer mechanical
classification; record whichever method you used in the SUMMARY.)

Classify every hit by RECEIVER:
- **receiver is a value whose type is (or implements) `lifecycle.Subsystem`** →
  it MUST be migrated to `Prepare` + `Activate` (the rule in `<settlements>`),
  and it MUST already appear in `files_modified`. **A caller the manifest does
  not cover is a planning defect — stop and report it** rather than silently
  widening the change. The rev-7 manifest additions found this way:
  `internal/admin/policy/subsystem_test.go:82`,
  `internal/admin/policy/subsystem_integration_test.go:41,:72,:93`,
  `internal/eventbus/audit/subsystem_test.go:130,:145`.
- **receiver is one of the five exempt non-subsystems** (`audit.RetentionWorker`,
  `audit.PluginConsumerManager`, `invalidation.coordinator`, `socket.Server`,
  `world.OrphanDetector` — see `<settlements>`; the last two joined the named
  list in round 8) → leave it alone. Note
  `internal/testsupport/integrationtest/crypto.go:318`'s `pcm.Start(ctx)` is a
  `PluginConsumerManager` — **exempt, do not migrate**, and deliberately not in
  `files_modified`.
- **known false-positive classes** (verified live; skip, but LOOK at each hit
  before skipping): `tracer.Start(` / `Tracer("…").Start(` (OTel spans —
  telnet, grpc, command, telemetry); **package-qualified functions**, not
  methods (`integrationtest.Start(t, …)` — the harness FUNCTION whose callers
  are unaffected because its signature does not change; `telemetry.Start`);
  `socket.Server.Start()` (no-ctx, returns a channel — NOT the lifecycle
  interface; row 14 keeps it atomic; includes `holomushtest/server.go:454`);
  the control server's two-arg `Start(addr, tls)`; `subscriber.Start(ctx,
  events)` (two-arg, `plugins.Subscriber`); plugin observability `Start`s.
- ⚠️ A count-based completion gate (`rg -c '\.Start\(' == 0`) is IMPOSSIBLE by
  design — the exempt receivers legitimately keep `Start`. Completion is proven
  by the compile gates + `task test:int`, never by grep emptiness.

Record the census in the SUMMARY (hits found / migrated / exempt / unaccounted),
naming the method used (broad-rg + receiver classification, or `go/packages`).

⚠️ **`task build` and `task test` are BOTH blind to ~21 of these callers** —
integration-tagged files are compiled by neither. `task test:int` is the only gate
that proves the caller migration is complete. Do not report this task done on a
green `task build`.

**The dividing line:** `Prepare` acquires and wires. `Activate` begins serving,
accepting, publishing, or launching a work loop. If a subsystem does no serving,
its `Activate` is a documented no-op returning nil — that is correct and common,
not a smell.

Worked examples to follow:
- `WorldSubsystem.Start` (`internal/world/setup/subsystem.go:64-70`) already
  shows the seam: `pool := s.cfg.DB.Pool()` and `engine := s.cfg.ABAC.Engine()`
  are ACQUIRE (→ `Prepare`), and the `world.NewService(...)` wiring is also
  Prepare. It serves nothing → `Activate` is a no-op.
- `grpcSubsystem` (`cmd/holomush/sub_grpc.go`) is the opposite: building the
  server, registering services, wiring the emitter, and **constructing the two
  reapers + their ctx/cancel state** are `Prepare`; **launching both reaper
  `Run` goroutines, `coordinator.Start()`, then binding the listener and
  serving** is `Activate` (per the amended row 16 and its round-5 subsection —
  the reapers are domain loops launched today at `:758`/`:769` and belong to
  neither "leave in Prepare" nor "drop"). This subsystem is the entire reason
  D-11 exists — get its split exactly right.
- `eventbus.Subsystem.Start` brings NATS up, obtains JetStream and declares the
  EVENTS stream — **all acquisition (`Prepare`), including `go s.server.Start()`**.
  The embedded server sets `DontListen: true` (`subsystem.go:153`) so it binds no
  socket and is reachable only in-process via `nats.InProcessServer(s.server)`
  (`:179`); and `audit.Prepare` fails closed without the live JetStream this
  produces (`audit/subsystem.go:273-279`). Publishing happens through consumers,
  not here. `Activate` is a no-op. (D-13.3 row 2 — this row is the one an earlier
  revision got backwards; see the retraction in this task's classification note.)
- `access/setup.ABACSubsystem` launches a **poller goroutine** — that is a work
  loop → `Activate`. Building the stack and registering health is `Prepare`.
- `dek.CheckpointSweepSubsystem` runs a sweep loop → `Activate`.
- `chain.VerifierSubsystem` walks the chains at boot and refuses on a break
  (INV-CRYPTO-102) — that is a **gate**, not serving. Put the walk in `Prepare`
  so it fails before anything activates. This is the whole payoff: with the
  global barrier, the chain is proven intact before **any** subsystem serves,
  independent of any edge.
- **`cluster.registry` — the one subsystem needing care, and the one whose
  `Start` is in another file** (`internal/cluster/heartbeat.go:22`). Three
  distinct edits, all required:
  1. **The interface** `cluster.Registry` (`registry.go:23-52`) declares `Start`
     at `:27`. Replace it with `Prepare`/`Activate` there too, or the impl stops
     satisfying its own interface.
  2. **The impl** in `heartbeat.go`. The seam — **amended in round 6 (BLOCKER;
     the earlier "no-op `Prepare`" instruction is retracted):** by the time this
     plan runs, 07-09 has moved `ConnProvider`/`ClusterIDProvider` resolution
     and their fail-closed checks (nil-Conn; resolved-empty
     `CLUSTER_CONFIG_MISSING_CLUSTER_ID`) into the Start body — that resolution
     is fallible ACQUISITION, and declaring `Prepare` a no-op would push it into
     the Activate sweep, AFTER the global barrier, while gRPC/admin.sock may
     already be serving: exactly the D-13.0 violation the barrier exists to
     prevent. So: **`Prepare` resolves and validates both providers**, storing
     stable values on the registry (no goroutines exist yet, so no lock hazard —
     take `r.mu` briefly for the field writes or rely on the orchestrator's
     single-threaded sweeps; document whichever). The four
     `deps.Conn.Subscribe` calls (`:30-64`), the first heartbeat publish
     (`:72`), and the two goroutine launches (`:82`, `:87`) are all **serving**
     → `Activate`.
     ⚠️ **Do not split the critical section.** `Start` holds `r.mu` end to end
     (`:23-24`) and `:72`'s `publishHeartbeatLocked` exists to avoid a
     RLock/write-lock self-deadlock (`:66-71`, `:232-236`). The
     subscribe/publish/goroutine block stays WHOLE, in one locked method
     (`Activate`) — the no-split settlement governs that critical section, not
     provider resolution. If any subscription/publish/goroutine moves into
     `Prepare`, the lock is taken and released twice and the rollback path at
     `:75` (`unsubAllLocked`) no longer covers the publish failure.
     The `if r.subAlive != nil { return nil }` guard (`:26`) travels with the
     subscriptions — i.e. onto `Activate` (see `<settlements>` D-13.2; verdict
     **KEEP**, real side effect, no pre-start text to delete; Prepare's benign
     re-resolution needs no guard — documented in row 13).
  3. **Both `clustertest` callers** — `harness.go:125` and `external.go:87` call
     `reg.Start(startCtx)`. Each becomes `Prepare` then `Activate` (keep the
     existing 5s timeout ctx and the `t.Fatalf` shape) — `Prepare` is now the
     provider-resolution phase, so dropping it would skip the acquisition and
     fail-closed checks entirely; the harness is what proves the production
     two-phase contract in cluster tests.

**Per-subsystem idempotency: APPLY the settled 17-row disposition table
(`<settlements>` § D-13.2) — rev 7 delegated these verdicts to the executor;
retracted (cross-AI round 5, WARNING).** Per that table:
- **KEEP** rows retain the existing guard on the phase named. Preserve the
  parenthetical naming the real side effect — *"would launch a duplicate poller
  goroutine"* is the only surviving justification and MUST NOT be lost (reword
  it to name `Activate`, where the poller now launches).
- **ADD** rows get a phase-owned state-field guard exactly as the table
  specifies (relay/sweep `s.done != nil`; socket `s.server == nil` +
  `s.errCh != nil`; policy a **per-policy-name completion map** —
  `emitted map[string]bool`, round 6: a single bool re-emits the successful
  prefix on retry or suppresses never-emitted policies; plugin the
  accessor-guard field; audit/ABAC/grpc the Activate-owned state).
- The four review-named rows (relay, sweep, policy, admin socket) each get a
  **repeated-call test** — second `Activate` returns nil with no second
  goroutine / bind / publish; the policy test additionally covers the mid-loop
  failure → retry-emits-only-the-remainder case (row 15, round 6).
- **Delete** the pre-start justification text wherever it appears — e.g.
  *"This allows the ABAC subsystem to be pre-started in core boot when admin
  handler construction needs Resolver() before the orchestrator drives
  StartAll. Mirrors store.DatabaseSubsystem.Start."* — 07-09 deleted every
  pre-start; that sentence now describes a path that does not exist.
- Reproduce the verdicts in the SUMMARY filled in with what was done. A verdict
  the live code contradicts is **reported, not improvised**.

**Partial-Prepare teardown — verified per row, not per new test (codex round 6,
MEDIUM — settled scope; round 9 carved out ONE exception):** Task 1's extended
`Stop` contract covers the partially-prepared case at the orchestrator level;
per-subsystem, the verification is the SUMMARY's rollback-state column. For the
subsystems that allocate multiple resources before their guard field is set —
plugin (`internal/plugin/setup/subsystem.go:225-363`) and audit (consumers
created before workers) — verify WHILE MIGRATING that every pre-guard
allocation is either locally cleaned on its error path or released by `Stop`
called on the partially-prepared value, and record that verification in each
row's rollback-state column. ⚠️ **Round 9 found exactly the "pre-guard
allocation that neither path releases" this paragraph anticipated — in plugin**
(the token-store sweeper + Lua host on the pre-manager error paths; the earlier
"already carries local cleanup on most error paths" parenthetical overstated
`cleanupOnError`, which never closed either host). That finding is **settled
here**, not reported mid-execution: D-13.2 row 9 + the "Plugin prepared-state
cleanup" subsection below carry the fix and its fault-injection test. The
general stance is unchanged for the other 15 subsystems: no 17-row fault
matrix — the two rows with multi-resource pre-guard allocation (plugin, round
9; audit, rounds 7-8 via row 10's three tests) now each have dedicated tests,
and the rollback-state column + local cleanup cover the rest. Any FURTHER
pre-guard allocation that neither path releases is still a finding — report it,
do not paper over it.

### Plugin prepared-state cleanup — make the partially-prepared contract real for PluginSubsystem (cross-AI round 9, BLOCKER; verified live)

The plan's own contract — Stop is the *"single teardown for prepared-only,
partially-prepared, and activated"* subsystems (D-13.0), and rollback stops the
failing subsystem *"because a failed Prepare may have partially acquired
resources"* (Task 1, Bug 1) — does NOT hold for `PluginSubsystem` as the code
stands, and assigning the current `Start` body to `Prepare` inherits the hole:

- `Stop` returns immediately while `s.manager == nil`
  (`internal/plugin/setup/subsystem.go:451-453`), so for any failure before the
  `s.manager = mgr` assignment (`:384`) the rollback `Stop` releases nothing —
  everything depends on the in-body `cleanupOnError`.
- `cleanupOnError` (`:237-250`) closes aliasPool/schemaProvisioner/worldConn but
  **never `binaryHost` or `luaHost`**.
- `goplugin.NewHost` launches the token-store sweeper goroutine at construction
  (`go h.tokenStore.Run(h.tokenStoreCtx)`, `internal/plugin/goplugin/host.go:359`;
  ctx built at `:345`). Three error paths between `binaryHost := goplugin.NewHost(...)`
  (`:322`) and `s.manager = mgr` (`:384`) — `BINARY_HOST_MW_FAILED` (`:333`),
  `ALIAS_POOL_FAILED` (`:341`), manager-construction failure (`:381`) — leak
  that goroutine and the live Lua host (`s.luaHost` assigned at `:222`).

**The settled fix — one shape, do not improvise an alternative:**

1. Declare `binaryHost` (a `*goplugin.Host` var) BEFORE `cleanupOnError`'s
   definition so the closure observes the assignment made later at `:322` (Go
   closures capture variables, not values).
2. Extend `cleanupOnError` with nil-guarded closes for both hosts:
   `binaryHost.Close(...)` — its `Close` cancels the sweeper ctx and closes the
   token store (`goplugin/host.go:1579-1580`) — and `s.luaHost.Close(...)`
   (`lua/host.go:794`), then `s.luaHost = nil`. Give `s.worldConn` the same
   nil-guard for uniformity (today's unguarded `s.worldConn.Close()` happens to
   be safe at every current call site; the extension should not rely on that).
   Update the closure's ordering doc comment to name the hosts. Every existing
   `cleanupOnError()` call site gains the host cleanup for free — the nil
   guards make it safe at sites where a host is not yet constructed.
3. With that in place, `Stop`'s `s.manager == nil` early return becomes a TRUE
   no-op for the pre-manager state (everything already released in-body) —
   document that on `Stop` so the early return reads as deliberate, not as the
   gap it was.

**Fault-injection test (RED first — write it against today's leak, observe it
fail, then apply step 1-2, observe green; record both in the SUMMARY):** add to
`internal/plugin/setup/subsystem_test.go` (`package setup_test`, unit tier — no
build tag, no Docker). Construct a `PluginSubsystem` whose config passes every
step up to manager construction and fails there deterministically:
`DataDir: t.TempDir()` (`resolvePluginsDir` joins `"plugins"`, no existence
check — `subsystem.go:558-561`), `DatabaseConnStr: ""` (skips BOTH the schema
provisioner at `:274-281` and the alias pool at `:338` — no database anywhere),
fake providers per the file's existing `fakeEngineProvider` idiom (ABAC; a
World provider returning a nil `*world.Service` — `newWorldInProcessConn`
registers it on an in-memory bufconn without invoking it, `world_conn.go:19-27`,
and `InProcessConn.Close` stops the serve goroutine via `server.Stop()`; a
Sessions provider returning nil), and **`VerbRegistry: nil`** —
`plugins.NewManager` then fails closed with `ErrMissingVerbRegistry`
(`internal/plugin/manager.go:272-273`) on the `:381` path, deterministically
AFTER `goplugin.NewHost`. Assertions: `require.Error` +
`errors.Is(err, plugins.ErrMissingVerbRegistry)` (the intended injection point,
not an earlier failure); `defer goleak.VerifyNone(t, goleak.IgnoreCurrent())`
proves no goroutine created during the failed Prepare survives it — the repo's
established idiom (`internal/plugin/goplugin/emit_token_store_test.go:193-195`;
NOT `t.Parallel` — goleak observes ALL live goroutines, see
`goplugin/host_test.go:1804-1809`); and a follow-up `Stop(ctx)` returns nil
(the `TestPluginSubsystemStopBeforeStartIsNoop` precedent at
`subsystem_test.go:71`). ACE name, e.g.
`TestPluginSubsystemPrepareFailureAfterHostConstructionLeavesNoSweeperGoroutine`.
(Until Task 2 renames the method the test drives `Prepare` — it is written
against the post-split interface in the same task.)

Carry `// codecov:ignore — tested by integration and E2E tests` onto the new
`Prepare`/`Activate` bodies wherever it is on the `Start` it replaces
(`world/setup/subsystem.go:65`, `access/setup/subsystem.go:74`).

### `ConfigureSystemBroadcaster`'s late-binding — SETTLED HERE. Do not defer it. (cross-AI review round 3, MEDIUM; D-13 requires planner settlement)

Rev 4 said *"Decide explicitly and record the decision in the SUMMARY"* — residual
"implementer decides" on real host lifecycle order, which D-13 assigns to the
**planner**. Settled below; the executor applies it.

**The shape (verified live):**
- `PluginSubsystem.ConfigureSystemBroadcaster` (`internal/plugin/setup/subsystem.go:493`)
  is called from exactly **one** production site: `cmd/holomush/sub_grpc.go:292`,
  inside grpcSub's `Start`.
- Its doc (`:482-492`) states the late-binding reason: *"the appender is built
  after the plugin subsystem starts, so a construction-time option cannot reach
  it"*. It no-ops when `s.luaHost == nil` or the arg is nil (`:494-496`) —
  fail-closed.
- **07-06 already changes its signature** to
  `ConfigureSystemBroadcaster(pub eventbus.Publisher, gameID func() string)`
  (07-06 § Task, `files_modified` includes `internal/plugin/setup/subsystem.go`).
  This plan changes **placement only** — do not touch the signature.

**SETTLED: keep the hook; call it from grpcSub's `Prepare`; make its ordering a
declared edge.**

1. **Keep the hook — do NOT convert it to a construction-time option.** The
   rationale survives the split intact: `s.luaHost` is built in
   `PluginSubsystem.Prepare` and the publisher is resolved from the bus, but
   `grpcSubsystem` is **constructed in `runCore` before anything starts**, so a
   construction-time option still cannot reach either. The hook is not a wart the
   barrier removes; it is load-bearing.
2. **It moves to grpcSub's `Prepare`** (it is wiring, not serving — D-13.0), i.e.
   it stays exactly where it is inside the body that becomes `Prepare`. Update its
   doc comment: *"MUST be called from the gRPC subsystem's **Prepare**"*, not
   `Start`. ⚠️ `rg -c 'gRPC subsystem.s Start' internal/plugin/setup/subsystem.go`
   must return 0 afterwards.
3. **Add `lifecycle.SubsystemPlugins` to `grpcSubsystem.DependsOn()`** — the real
   fix, and a genuinely new edge. On `main` today grpcSub's deps are
   `[Bootstrap, Sessions, Auth, EventBus]` (`sub_grpc.go:170-177`); by the time
   this plan runs, 07-09/07-10 have already added `TLS`, `Cluster`,
   `CryptoChainVerifier` (item 8, T-07-51) and `AuditProjection` — read the live
   list, extend it, drop nothing. The
   required plugin ordering is only **transitive and undeclared**: its doc even
   admits *"Bootstrap transitively depends on ABAC, World, Plugins, Database"*
   (`:167`). Relying on a transitive path through Bootstrap for a hook that
   dereferences `s.luaHost` is precisely the MEDIUM-11 failure class this phase
   exists to end — an ordering asserted in prose, not by the graph. Declare it.
   **No cycle:** `PluginSubsystem.DependsOn()` is `[Database, ABAC, World, Auth]`
   (`internal/plugin/setup/subsystem.go:154-161`) — it does not reach gRPC.
   ⚠️ 07-10 Task 4's acyclicity test covers this addition; if it goes red, stop
   and report.
4. **Do NOT delete the nil-luaHost / nil-arg no-op guard** (`:494-496`). It stays
   fail-closed: with the barrier plus the new edge the nil case should be
   unreachable in production, and `internal/plugin/setup/system_broadcaster_test.go:43`
   (`TestConfigureSystemBroadcasterNoopWhenLuaHostUnset`) pins it. Defense in
   depth, not dead code.

### ⚠️ "Mechanical" was WRONG — apply the settled 17-row table, do not improvise

This task previously called 14 of the 17 *"mostly rename `Start`→`Prepare`, add a
no-op `Activate`"*. That is false for the ones where a wrong split is dangerous.
**The disposition of all 17 is now settled in `<settlements>` § D-13.3 — read it
and apply it row by row.** Do not re-derive placements; do not treat a no-op
`Activate` as a default (it is a *decided answer* on 8 rows and a *bug* on the
other 9).

**Two placements in that table reverse this task's earlier instructions. They are
retracted; the table wins:**
- **`eventbus`** — the earlier text said `go s.server.Start()` → `Activate`.
  **Wrong**, and unbootable: it lives inside `connect()` with no seam before
  `nats.Connect`, and `audit.Prepare` hard-requires the live JetStream it
  produces (`AUDIT_DEP_NOT_STARTED`). `DontListen: true` (`subsystem.go:153`)
  proves it binds no socket. **Entire body → `Prepare`.**
- **`plugin/setup`** — the earlier text deferred it to the executor. **Decided:**
  loading + subprocess launch → `Prepare`; event delivery/dispatch → `Activate`.
  See D-13.3 row 9 for both grounds.

**Reproduce the D-13.3 table in the SUMMARY, filled in with what you actually
did**, one row per production subsystem, each with:

| Column | Content |
|---|---|
| Subsystem | the type |
| `Start` site | `file:line` (**verified by reading — they drift**; for `cluster` this is `heartbeat.go:22`, NOT `registry.go`) |
| Prepare actions | the exact acquire/wire steps moved there |
| Activate actions | the exact serve/accept/publish/goroutine-launch steps, **or an explicit "none — does not serve" with the reason** |
| Rollback state | what `Stop` must release if `Prepare` succeeded but `Activate` never ran (feeds Task 1's partially-prepared contract) |
| Idempotency verdict | keep/delete the guard + why (D-13.2) |
| Test | the focused test proving the split for THIS subsystem |

If applying a row proves it wrong against live code, **stop and report** — do not
silently re-place it. The table is the plan's contract, and a disagreement between
it and the code is a planning defect worth surfacing, not an executor judgement
call.

**Recording-stub tests in Task 1 prove the orchestrator's two sweeps; they prove
NOTHING about whether real serving work landed in `Activate`.** That is only
provable per-subsystem. **Four rows get a named focused test** — the two with an
externally-reachable `Activate` plus the two whose placement was previously wrong:
`grpcSubsystem` (listener bind/serve → Activate, plus the row-16 reaper test:
neither reaper runs after Prepare alone), `admin/socket` (`admin.sock`
bind+serve → Activate, plus the two-phase disabled-mode test), `eventbus` (whole
body → **Prepare**, JetStream live at the end of Prepare), and `audit` (boot
gate → Prepare; `p.start` → Activate).
`cluster` keeps the row-13 treatment specified above (round 6: provider
resolution in `Prepare`; the locked subscribe/publish/goroutine critical
section whole in `Activate`).

**`invalidation.Coordinator` — ownership is settled in 07-09 § Task 2 item 4; do
NOT re-litigate it here.** Cross-AI review flagged that 07-09 and this plan
appeared to contradict each other (09: "provider + `DependsOn`"; 11: "preserves it
as a non-subsystem `Start` surface"). Reconciled, with 07-09 as the single source:
- The Coordinator **stays a non-subsystem and keeps its own `Start`/`Stop`**
  (`coordinator.go:21-25` — it has no `ID()`/`DependsOn()`), which is why it stays
  on the "legitimately keeps `Start`" exemption list below. **This plan's earlier
  statement is the correct half.**
- **`grpcSubsystem` owns its lifecycle** (07-09 moved construction + `c.Start()`
  there and its stop into `grpcSubsystem.Stop`, deleting the ad-hoc `defer` at
  `core.go:982-989`). So in this plan it is simply **part of grpcSub's row**:
  construction → grpcSub's `Prepare`; `c.Start()` → grpcSub's `Activate`;
  `c.Stop()` → grpcSub's `Stop`.
- ⚠️ Its **degraded mode is load-bearing**: a Coordinator construction/start
  failure WARNs and the boot **continues** (`core.go:976-980`). grpcSub's
  `Activate` MUST NOT propagate that error — `StartAll` would abort the boot and
  roll back, turning a degraded boot into a failed one. Note this in grpcSub's
  rollback-state column.
- The count stays **17**; the Coordinator is not an 18th subsystem.

**Commit only when the tree compiles (cross-AI review, MEDIUM; the commit is
RELOCATED to the unit end — cross-AI round 11, rev 14, BLOCKER).** The previous
instruction deliberately created a **known-broken Batch 1 commit** and justified it
with bisectability — but *a bisect that lands on a commit that cannot build is
guaranteed red*, which is the opposite of bisectable. It also breaks `task lint`
for every concurrent agent in that window. **Keep the batches as local working
units for context control, but do NOT commit at this task's boundary** — round
11 verified a commit here cannot pass `task test` (the two Task-3 stubs; see
the unit-discipline note at the top of this action). The ONE squashed commit
spanning Tasks 1–3 lands at Task 3's boundary, as
`refactor(07-11): split Start into Prepare/Activate across all 17 subsystems`.
Do not push a commit whose `task build` fails. Run `task build` at THIS boundary
regardless — it is this task's completeness gate for the production migration
(see the first acceptance criterion).

⚠️ `task fmt` mutates aligned blocks across many of these files. Run it; its
mutations ride the unit's single commit at Task 3's boundary (round 11, rev 14).
  </action>
  <acceptance_criteria>
    - **The migration is complete iff it compiles.** Every one of the 17 production subsystems carries a compile-time assertion `var _ lifecycle.Subsystem = (*T)(nil)`, so a type that still has `Start` (or is missing `Prepare`/`Activate`) is a **build failure**, not a grep miss. 10 already exist — `store/subsystem_test.go:16`, `access/setup/subsystem_test.go:16`, `auth/setup/subsystem_test.go:16`, `world/setup/subsystem_test.go:16`, `session/setup/subsystem_test.go:16`, `bootstrap/setup/subsystem_test.go:16`, `chain/verifier_subsystem.go:97`, `dek/sweep.go:185`, `admin/policy/subsystem.go:61`, `cmd/holomush/sub_grpc_test.go:47`. **Add the 7 missing** (verified absent): `eventbus.Subsystem`, `audit.Subsystem`, `plugin/setup.PluginSubsystem`, `world/setup.OutboxRelaySubsystem`, `socket.AdminSocketSubsystem`, `tls.TLSSubsystem`, and `cluster.registry` (unexported → the assertion must live in `package cluster`: `var _ lifecycle.Subsystem = (*registry)(nil)`).
    - `rg -n 'var _ lifecycle\.Subsystem = ' internal/ cmd/holomush/ | wc -l` returns 17 — counting **assertions** (one line per match), not files: one per production subsystem; the three migrated test stubs deliberately carry none. (`rg -c … | wc -l` counted files and breaks the moment two assertions share a file.)
    - **The `Prepare`/`Activate` implementation count is scoped to the EXPLICIT production file list** — ⚠️ rev 6 swept all of `internal/ cmd/holomush/` and demanded exactly 17, which is **guaranteed red on correct code** (cross-AI round 4, MEDIUM): Task 1 migrates the `orchestrator_test.go` stub (≥18) and Task 3 adds two more test impls (≥20). The 17 production impl files (the census's `Start` column; note `cluster` = `heartbeat.go`, NOT `registry.go`):
      `internal/store/subsystem.go internal/tls/subsystem.go internal/eventbus/subsystem.go internal/access/setup/subsystem.go internal/auth/setup/subsystem.go internal/world/setup/subsystem.go internal/world/setup/relay_subsystem.go internal/session/setup/subsystem.go internal/bootstrap/setup/subsystem.go internal/plugin/setup/subsystem.go internal/eventbus/audit/subsystem.go internal/eventbus/audit/chain/verifier_subsystem.go internal/eventbus/crypto/dek/sweep.go internal/cluster/heartbeat.go internal/admin/socket/subsystem.go internal/admin/policy/subsystem.go cmd/holomush/sub_grpc.go`
    - `rg -l 'func .*\) Prepare\((ctx|_) context\.Context\) error' <the 17 files above> | wc -l` returns 17 — note `(ctx|_)`: a no-op `Prepare`/`Activate` idiomatically names the param `_`, so a regex hard-coding `ctx` under-counts (this is exactly how the T1/T3 stubs evade a naive census)
    - `rg -l 'func .*\) Activate\((ctx|_) context\.Context\) error' <the 17 files above> | wc -l` returns 17
    - `rg -c 'Start\(' internal/lifecycle/subsystem.go` returns 0 — the interface no longer declares `Start`. **Do NOT** assert a repo-wide "zero `Start` methods" gate: `audit.RetentionWorker` (`internal/audit/retention.go:127`), `audit.PluginConsumerManager` (`internal/eventbus/audit/plugin_consumer.go:235`) and `invalidation.coordinator` (`internal/eventbus/crypto/invalidation/coordinator.go:69`) are **not** subsystems and legitimately keep `Start` — as do `socket.Server` (`internal/admin/socket/server.go:60`) and `world.OrphanDetector` (`internal/world/property_lifecycle.go:108`), round 8 (see `<settlements>`).
    - `rg -c 'Start\(ctx context\.Context\) error' internal/cluster/registry.go` returns 0 (the `Registry` **interface** at `:27` was migrated too, not just the impl)
    - `rg -c '\.Start\(' internal/cluster/clustertest/` returns 0 (both harness callers migrated)
    - **Row 13 (round 6, BLOCKER) — cluster ACQUISITION is in `Prepare`, not `Activate`:** reading `internal/cluster/heartbeat.go` shows `Prepare` resolving+validating `ConnProvider`/`ClusterIDProvider` — `rg -c 'CLUSTER_CONFIG_MISSING_CLUSTER_ID' internal/cluster/heartbeat.go` returns at least 1 and the match is lexically inside `Prepare` (verified by reading) — while `Activate` holds the whole `r.mu` critical section (four subscriptions + first publish + both goroutines) with **zero** provider resolution inside it. A no-op cluster `Prepare` would put fallible acquisition after the barrier — the exact cross-plan composition round 6 blocked.
    - **Step 0's caller census is in the SUMMARY** — hits found / migrated / exempt / unaccounted. Any caller not covered by `files_modified` was **reported, not silently migrated**.
    - **Every migrated caller calls BOTH phases.** Sweep BROADLY and classify by RECEIVER, exactly like Step 0's census: run `rg -n '\.Prepare\(' --type go -g '!*.pb.go'` and `rg -n '\.Activate\(' --type go -g '!*.pb.go'`, keep only hits whose receiver is (or implements) `lifecycle.Subsystem` (record the classification with the census), and the two classified file sets MUST be equal — except at sites deliberately preparing only (e.g. a fail-closed dep-check test), each of which carries an inline comment saying so. ⚠️ **Do NOT re-narrow these sweeps to argument shapes (round 7, MEDIUM — an earlier version of this criterion whitelisted `(ctx|context\.|startCtx)`, the exact shape-class this plan's own Step 0 retracts):** an argument-shape whitelist misses `s.Prepare(t.Context())` precisely as the old census regex missed `s.Start(t.Context())` (live at `internal/eventbus/audit/subsystem_test.go:130`), reporting success while omitting correctly-migrated files from the comparison. ⚠️ A caller collapsed to `Prepare` alone compiles and runs green while the subsystem's work loops never start — the silent-failure mode of this task.
    - **No `Start` compatibility shim exists on any subsystem** (round 8 — the previous three-item exemption made this criterion IMPOSSIBLE on correct code: `Server.Start` and `OrphanDetector.Start` legitimately survive): run `rg -n 'func .*\) Start\(' internal/store/ internal/eventbus/ internal/access/ internal/auth/ internal/world/ internal/session/ internal/bootstrap/ internal/plugin/setup/ internal/cluster/ internal/admin/ internal/tls/ cmd/holomush/sub_grpc.go` and RECEIVER-classify every match, exactly like Step 0's census. The expected in-scope survivors are exactly the non-subsystem rows from `<settlements>` § "Non-subsystem `Start` methods": `PluginConsumerManager.Start` (`internal/eventbus/audit/plugin_consumer.go:235`), `invalidation.coordinator.Start` (`internal/eventbus/crypto/invalidation/coordinator.go:69`), `socket.Server.Start` (`internal/admin/socket/server.go:60` — mandated unmodified by this plan's own row 14 and invoked from `AdminSocketSubsystem.Activate`), and `world.OrphanDetector.Start` (`internal/world/property_lifecycle.go:108`). (`audit.RetentionWorker.Start` at `internal/audit/retention.go:127` is exempt too but outside this sweep's path set.) Any OTHER surviving match: a `lifecycle.Subsystem` receiver is a FAILED migration — fix it; a non-subsystem receiver is a legitimate survivor — record it in the SUMMARY census, never delete it to satisfy a count. A shim that does both phases would defeat D-11 entirely.
    - **Accessor diagnostics name a method that exists (cross-AI round 12, rev 15 — LOW fold-in):** run `rg -n 'called before Start' internal/ cmd/holomush/` and RECEIVER-classify every match, exactly like the Start-survivor sweep above. Matches whose receiver is one of the 17 production subsystems (the explicit file list above, plus `internal/tls/subsystem.go`) MUST all be migrated — doc comments and panic strings read "called before `Prepare()`", and the eventbus `EnsureStream` oops message (`internal/eventbus/subsystem.go:255`) is migrated with them; any exact-string test asserting an old message was updated in the same sweep. Matches on non-subsystem receivers (`world.OrphanDetector` at `internal/world/property_lifecycle.go:63`, `audit.PluginConsumerManager` at `internal/eventbus/audit/plugin_consumer.go:141`, and any other legitimate `Start` survivor from the exemption list) keep their strings and are recorded in the SUMMARY census — do NOT mandate a blind repo-wide zero.
    - **`task test:int` is the completeness gate for the caller migration, not `task build`.** ~21 of the ~27 caller files are `//go:build integration` (counts reconciled in round 8; Step 0's recorded census is the authoritative number): `task build` does not compile `_test.go` and `task test` does not compile integration-tagged files, so **both are blind to them**. This criterion is the one that actually proves the migration landed (07-CONTEXT.md § Landmines). (Round 11, rev 14 — this gate EXECUTES at Task 3's boundary, the unit end: mid-unit, `task test:int` cannot compile the untagged `coordinator_error_test.go` stub Task 3 owns. The caller migration still happens HERE; only the proving run moves.)
    - `rg -c 'pre-started in core boot|Mirrors store.DatabaseSubsystem.Start' internal/` returns 0 (the false rationale is gone)
    - `rg -c 'would launch a duplicate poller goroutine' internal/access/setup/subsystem.go` returns 1 (the true rationale survives)
    - **A 17-row classification table is in the SUMMARY**, matching `<settlements>` § D-13.3 row for row, with every column filled (Start site, Prepare actions, Activate actions, rollback state, idempotency verdict, test). "Mechanical" is not an acceptable Activate-column value. Any deviation from D-13.3 is reported, not silently applied.
    - The gRPC listener bind/serve is inside `Activate`, not `Prepare` — verified by reading `cmd/holomush/sub_grpc.go`
    - **Row 16 — both reapers construct in `Prepare` and launch in `Activate` (round 5, BLOCKER):** `rg -c 'go s\.(sessionReaper|guestReaper)\.Run' cmd/holomush/sub_grpc.go` returns 2 and both matches are lexically inside `Activate` (verified by reading); `Prepare` contains the `session.NewReaper`/`auth.NewGuestReaper` constructions and the `reaperCtx`/`reaperCancel` state, with **zero** `go ` launches; a focused test in `cmd/holomush/sub_grpc_test.go` proves neither reaper runs after `Prepare` alone (recording store/lister + BOTH intervals shortened via `ReaperInterval` and the new `GuestReaperInterval`: zero calls after Prepare + 3× the longer interval; calls from BOTH reapers observed after Activate). (Round 11, rev 14 — the test is WRITTEN in this task; it EXECUTES at the unit end: any `task test -- ./cmd/holomush/` run compiles `core_subsystems_test.go`, whose Start-only `stubSubsystem` migrates only in Task 3. Task 3's first criterion is where this test's green is proven.)
    - **Row 16 — the guest-reaper timing seam exists (round 7, MEDIUM):** `rg -c 'GuestReaperInterval' cmd/holomush/sub_grpc.go` returns at least 2 (the `grpcSubsystemConfig` field + the `auth.GuestReaperConfig` wiring), and the field defaults to `1 * time.Minute` when zero — production wiring passes nothing, preserving today's hardcoded value (`sub_grpc.go:766`); without the seam the shortened-interval test above is non-deterministic for the guest reaper
    - **`admin.sock` bind+serve is inside `Activate`** — specifically, `Prepare` calls only `NewServer(Config{…})` (plus the empty-`SocketPath` disabled-mode early return) and `Activate` calls the existing atomic `srv.Start()`. Verified by reading, with a focused test. ⚠️ **`admin.lock` acquisition is in `Activate`, NOT `Prepare`** — rev 4 required the reverse, but `Server.Start()` (`internal/admin/socket/server.go:58-113`) acquires the lock, removes the stale socket, binds the UDS and serves as **one atomic call** with no separate lock API; splitting it was rejected (see `<settlements>` row 14). `internal/admin/socket/server.go` is deliberately **unmodified**: `git diff --exit-code internal/admin/socket/server.go` exits 0.
    - **Row 14 — the disabled-mode guard exists (round 5, BLOCKER):** `Activate` opens with `if s.server == nil { return nil }` (verified by reading), and the migrated two-phase disabled-mode test passes — `task test -- -run 'SocketPathEmpty|AdminSocketSubsystem' ./internal/admin/socket/` exits 0 with the test driving `Prepare` THEN `Activate` on an empty-`SocketPath` subsystem and asserting both return nil with `sub.server` still nil
    - **`go s.server.Start()` (`internal/eventbus/subsystem.go:164`) is inside `Prepare`, NOT `Activate`** — it has no seam before `nats.Connect` (`:91` → `:164-193`) and `DontListen: true` (`:153`) means it binds no socket. A focused test asserts JetStream is live at the end of `eventbus.Prepare`. ⚠️ An earlier revision demanded the opposite; that instruction is retracted and would have made the next criterion unsatisfiable.
    - **`audit.Prepare` succeeds against a merely-prepared eventbus** — a focused test prepares `eventbus` then `audit` and asserts no `AUDIT_DEP_NOT_STARTED`. This is the criterion that proves D-13.0's phase semantics are coherent; it fails if `go s.server.Start()` was moved to `Activate`.
    - The audit **boot gate** (PartitionManager + Backfill + EnsurePartitions, `internal/eventbus/audit/subsystem.go:283-289`) is inside `Prepare` and `p.start(workerCtx)` (`:313-320`) is inside `Activate` — the seam the code's own comment already names (*"ALL BEFORE the projection starts accepting traffic"*)
    - **Row 10 — audit's phase state is explicit (round 7, BLOCKER):** `rg -c 'preparedProjection' internal/eventbus/audit/subsystem.go` returns at least 3 (field decl + Prepare guard/assign + Activate consume) and `rg -c 'partitionManager' internal/eventbus/audit/subsystem.go` returns at least 2 (field decl + retention-worker construction in Activate) — verified by reading: Prepare guards on `s.preparedProjection != nil` (the old `s.worker != nil` guard CANNOT guard Prepare — `worker` is assigned only after `p.start`, which now lives in Activate), Activate guards on `s.worker != nil` and fails closed with a typed error when `s.preparedProjection == nil`, and Stop handles + clears BOTH states (prepared-only and activated). **Round 8 — the prepared aggregate includes the lateInit-written state:** Prepare's error paths after `lateInit` (`subsystem.go:304-312` — it writes `s.cfg.Owners`/`s.pluginMgr` BEFORE the fallible `newProjection`) restore both to their captured pre-lateInit values, and the prepared-only Stop path stops/clears `s.pluginMgr`, restores `s.cfg.Owners`, and clears the prepared projection's IN-MEMORY state (nils `preparedProjection`/`partitionManager`) — verified by reading. **Round 9 (rev 12) — durables are RETAINED, not "released":** the durable JetStream consumer created at Prepare (`projection.go:109-117`) is intentionally retained server-side on a prepared-only Stop — `drain` is a no-op before Consume exists (`projection.go:467-470`, `p.cc == nil`), so the round-8 "bounded-drain release" wording implied an impossible pre-Consume drain; `CreateOrUpdateConsumer` is idempotent, so a retried Prepare re-attaches rather than duplicating, and Stop's doc comment states retained-not-deleted explicitly — verified by reading. A repeated-Prepare test asserts the second Prepare returns nil without constructing a second projection/durable consumer; a Stop-after-Prepare-only test passes without error and leaves no lateInit-written state behind (it does NOT assert consumer deletion — retention is the decided disposition); and a Prepare-fail/retry test (fail after `lateInit`, then retry with a `lateInit` returning different or nil owners/pcm) asserts the retry observes NO stale ownership or consumer wiring from the failed attempt (round 8)
    - **`plugin/setup`'s entire current `Start` body is inside `Prepare`, and its `Activate` is a documented no-op** returning nil, per D-13.3 row 9. ⚠️ Rev 4 required an "event-delivery subscription and command dispatch" loop in `Activate`; **no such loop exists** — `Start` (`internal/plugin/setup/subsystem.go:163-165`) builds the stack and returns, launching no goroutine/subscription/ticker, and delivery is a synchronous manager method invoked by callers (`internal/plugin/manager.go:458`). Verified by reading the new `Activate` body: it contains no goroutine launch, no subscription, and no loop — it returns nil. Its doc comment names the reason for the no-op (delivery is a synchronous manager method invoked by callers). **No delivery loop was invented or relocated** — that is Phase 8 / MEDIUM-4 territory (07-CONTEXT.md § Out of scope). `audit.Prepare`'s `DependsOn(SubsystemPlugins)` reliance on loaded manifests is the forcing ground for loading being in `Prepare`.
    - **Row 9 — plugin prepared-state cleanup exists and is proven (round 9, rev 12, BLOCKER):** reading `internal/plugin/setup/subsystem.go` shows `cleanupOnError` closing BOTH `binaryHost` (nil-guarded; its `Close` releases the token-store sweeper, `goplugin/host.go:1579-1580`) and `s.luaHost` (nil-guarded, then nil'd), with `binaryHost` declared before the closure so the `:322` assignment is visible to it; the three pre-manager error paths (`BINARY_HOST_MW_FAILED`, `ALIAS_POOL_FAILED`, manager-construction) all route through the extended `cleanupOnError`; `Stop`'s `s.manager == nil` early return carries a doc sentence naming the in-body cleanup that makes it a true no-op; and the fault-injection test (`internal/plugin/setup/subsystem_test.go`, `VerbRegistry: nil` → `ErrMissingVerbRegistry` at the `:381` path, `goleak.VerifyNone` + Stop-returns-nil assertions, per Task 2 § "Plugin prepared-state cleanup") was observed RED against the pre-fix leak and passes after — `task test -- ./internal/plugin/setup/` exits 0, RED recorded in the SUMMARY
    - **`ConfigureSystemBroadcaster` is settled, not deferred** (`<settlements>` § its heading): the hook is kept and called from grpcSub's **`Prepare`**; `rg -c 'gRPC subsystem.s Start' internal/plugin/setup/subsystem.go` returns 0 (its doc names `Prepare`); `rg -c 'lifecycle.SubsystemPlugins' cmd/holomush/sub_grpc.go` returns at least 1 (the transitive-through-Bootstrap ordering is now a declared edge); and `TestConfigureSystemBroadcasterNoopWhenLuaHostUnset` still passes (the fail-closed guard survives). 07-10 Task 4's acyclicity test still passes with the new edge.
    - **The barrier's promise is asserted, not overclaimed:** the only two `Activate` bodies that bind an externally-reachable surface are `grpcSubsystem` (listener) and `socket.AdminSocketSubsystem` (`admin.sock`) — verified by reading all 17 `Activate` bodies and recorded in the SUMMARY table
    - `rg -c 'context.WithCancel\(context.Background\(\)\)' internal/eventbus/audit/subsystem.go` returns at least 1 — the audit worker's deliberate `context.Background()` parent survives the split (it must outlive the boot ctx; do not rebind it to Activate's ctx)
    - **Every commit pushed builds.** The Tasks-1–3 unit produces ONE pushed commit — at Task 3's boundary — and `task build` exits 0 at it; no deliberately-red intermediate (the previous "Batch 1 is expected not to compile" instruction is retracted; a bisect landing there would be guaranteed red). (Round 11, rev 14 — the commit itself moved to the unit end; rev 13's demand for a commit at THIS boundary was unsatisfiable: `task test` compiles the two Start-only stubs Task 3 owns.)
    - `task build` exits 0 at THIS boundary (the load-bearing inner-loop gate for the production migration — `_test.go` is invisible to `task build`, so the two Task-3 stubs cannot block it; see the first criterion)
    - Package-scoped unit gates pass at THIS boundary: `task test -- ./internal/lifecycle/ ./internal/eventbus/ ./internal/eventbus/audit/ ./internal/plugin/setup/ ./internal/admin/policy/ ./internal/admin/socket/ ./internal/world/setup/` exits 0 — every test file these packages compile at the unit tier migrates in this task (or Task 1); none of them compiles the two Task-3 stub files
    - **Whole-repo `task test` and `task test:int`, the KEK boot gate, and the topo pin are UNIT-END gates — relocated verbatim to Task 3's criteria (cross-AI round 11, rev 14, BLOCKER):** at this boundary `task test` cannot compile `cmd/holomush/core_subsystems_test.go` (`stubSubsystem`, `//go:build !integration`, Start-only until Task 3) or `internal/eventbus/crypto/invalidation/coordinator_error_test.go` (`stubRegistry`, NO build tag — compiled by `task test` AND `task test:int`, and typed against the `cluster.Registry` interface this task migrates), so demanding them here fails on correct code — the exact contradiction of this plan's own atomicity note. The relocated gates are preserved, not weakened: Task 3 carries `task build`, `task test`, `task test:int`, the KEK-wired boot, the topo pin, and the post-commit `task fmt` clean-tree check.
    - **D-13.2's settled dispositions are applied, not re-derived (round 5):** the four ADD-guard rows carry their guards (`rg -c 'if s.done != nil' internal/world/setup/relay_subsystem.go internal/eventbus/crypto/dek/sweep.go` shows 1 per file, or the equivalent phase-owned field verified by reading; the policy **per-policy-name completion map** — `rg -c 'emitted map\[string\]bool' internal/admin/policy/subsystem.go` returns 1, or an equivalent per-name structure verified by reading (round 6: a single bool is unsound for multi-policy configs) — and the socket `s.errCh != nil` guard verified by reading), and the four repeated-call tests exist and pass — `task test -- -run 'Idempotent|Repeated|Twice' ./internal/admin/policy/ ./internal/admin/socket/` plus the relay/sweep tests under `task test`/`task test:int`. Second `Activate` = nil return, no second goroutine/bind/publish; the policy test also proves a mid-loop failure retries ONLY the not-yet-emitted names (no duplicate audit events, no suppressed policies). (Round 11, rev 14 — the policy/socket scoped run and the relay unit test are satisfiable at THIS boundary; the sweep test is integration-tagged, so its proving run is the unit-end `task test:int` gate in Task 3.)
    - The SUMMARY's per-subsystem idempotency table (17 rows) matches `<settlements>` D-13.2's verdicts, including the `cluster.registry` row (`heartbeat.go:26`, verdict KEEP — four subscriptions + two goroutines); any live-code disagreement was reported, not improvised
    - `SessionSubsystem.Activate` is a documented no-op naming row 7's reason (the session reaper is grpcSubsystem-owned; its own Start only constructs the store) — verified by reading
  </acceptance_criteria>
  <verify>
    <automated>task build && task test -- ./internal/lifecycle/ ./internal/eventbus/ ./internal/eventbus/audit/ ./internal/plugin/setup/ ./internal/admin/policy/ ./internal/admin/socket/ ./internal/world/setup/</automated>
  </verify>
  <done>All 17 production subsystems implement Prepare/Activate; acquisition and serving are separated; the chain verifier gates in Prepare; every idempotency guard has a per-subsystem verdict and only true rationales survive; `task build` and the package-scoped unit gates are green at this boundary. (No commit yet — the whole-repo gates, the KEK-wired boot proof, and the unit's single squashed commit land at Task 3's boundary; round 11, rev 14.)</done>
</task>

<task type="execute" tdd="true">
  <name>Task 3: Migrate the test implementations and prove acquire-before-serve end to end</name>
  <read_first>
    - cmd/holomush/core_subsystems_test.go:16-52 (stubSubsystem — the set/count stub)
    - internal/lifecycle/orchestrator_test.go:20-45 (the dep-carrying stub)
    - internal/eventbus/crypto/invalidation/coordinator_error_test.go:30-45 (stubRegistry)
    - cmd/holomush/core_topo_order_test.go (07-10's pin — must still pass)
  </read_first>
  <files>cmd/holomush/core_subsystems_test.go, internal/lifecycle/orchestrator_test.go, internal/eventbus/crypto/invalidation/coordinator_error_test.go</files>
  <behavior>
    - The three test implementations satisfy the new three-method interface.
    - A regression test over the **production** subsystem set proves the acquire-before-serve property structurally: recording every phase call across the real set, no `Activate` is observed before the last `Prepare`.
  </behavior>
  <action>
Migrate the three test implementations named in `<settlements>`:
- `cmd/holomush/core_subsystems_test.go:22` — `stubSubsystem`. Keep its
  `DependsOn()` returning `nil` (07-10 relies on that for the set/count tests and
  explicitly does not reuse it for the pin). Replace `Start` with no-op
  `Prepare`/`Activate`.
- `internal/lifecycle/orchestrator_test.go:29` — `stubSubsystem` (returns
  `s.deps`). Task 1 already reworked this one for its phase-order tests; verify
  it is consistent.
- `internal/eventbus/crypto/invalidation/coordinator_error_test.go:35` —
  `stubRegistry`.

Add the **property test** to `cmd/holomush/core_subsystems_test.go`: build the
production set (07-09's `productionSubsystemSet`) with recording stubs whose
`DependsOn()` values come from the **real** subsystem types (the same sourcing
07-10's pin uses — do not hand-copy dep lists), register them, run `StartAll`,
and assert over the recorded call log that the **index of the first `Activate` is
greater than the index of the last `Prepare`**. That single assertion is D-11's
whole guarantee, stated as an executable property over the real graph rather than
per-subsystem inspection.

Its doc comment MUST state why it exists: `grpcSubsystem.DependsOn()` excluded
`AuditProjection`, so gRPC could serve before the audit projection was up. 07-10
fixed that specific edge; this property makes the class unrepresentable — a
future subsystem that forgets an edge still cannot serve before everything has
acquired.

ACE name, e.g. `TestStartAllActivatesNothingUntilEverySubsystemHasPrepared`.

**RED proof:** temporarily change `StartAll` to per-subsystem
Prepare-then-Activate and confirm this property fails. Revert. Record in the
SUMMARY — that is what distinguishes the two-sweep design from a rename.

**Unit boundary (cross-AI round 11, rev 14 — BLOCKER): this task closes the
Tasks-1–3 execution unit.** Migrating the two Start-only stubs above is what
makes the whole-repo gates satisfiable at all: `stubSubsystem`
(`core_subsystems_test.go:23`, `//go:build !integration` — compiled by
`task test`) and `stubRegistry` (`coordinator_error_test.go:36`, NO build tag —
compiled by `task test` AND `task test:int`, and typed against the
`cluster.Registry` interface Task 2 migrated) both fail compilation from Task
1's interface change until this task lands. After the whole-repo gates below
are green, produce **ONE commit** spanning all three tasks' edits, as
`refactor(07-11): split Start into Prepare/Activate across all 17 subsystems`.
Run `task fmt` first and include its mutations in the commit. The mid-unit
states (interface split with unmigrated impls; impls migrated with unmigrated
stubs) must never be committed: "Every commit pushed builds".
  </action>
  <acceptance_criteria>
    - `task test -- ./cmd/holomush/ ./internal/lifecycle/ ./internal/eventbus/crypto/invalidation/` exits 0 (round 11, rev 14 — this run also executes the row-16 reaper focused test and every other `cmd/holomush` unit test Task 2 wrote: they become compilable only now, once `stubSubsystem` migrates)
    - `rg -c 'TestStartAllActivatesNothingUntilEverySubsystemHasPrepared' cmd/holomush/core_subsystems_test.go` returns 1
    - `rg -c 'func \(s stubSubsystem\) Start' cmd/holomush/core_subsystems_test.go` returns 0 (receiver parens escaped — unescaped they are a capture group, returning 0 even when `Start` IS present; since extra methods do not break the `var _ lifecycle.Subsystem` compile-time assertions, this grep is the only guard that Start was removed)
    - RED demonstrated and recorded in the SUMMARY: per-subsystem Prepare-then-Activate fails the property
    - `task build` exits 0 (unit-end whole-repo build — relocated with the gates below; round 11, rev 14)
    - `task test` exits 0
    - `task test:int` exits 0 (round 11, rev 14 — the unit-end run of the caller-migration completeness gate: ~21 of the ~27 caller files Task 2 migrated are integration-tagged and only this gate compiles them; it also executes the row-12 sweep integration test)
    - **The KEK-wired full boot passes (relocated from Task 2's boundary — round 11, rev 14, BLOCKER):** `task test:int -- -run 'TestAdminAuthenticate|AdminRekey' ./cmd/holomush/` exits 0. ⚠️ **`task test:int`, not `task test`** (cross-AI review; verified live): those E2Es are `//go:build integration` (`admin_authenticate_e2e_test.go:4`) and `task test` (`Taskfile.yaml:85-89`) has no integration tag — it would match zero tests and exit 0 without compiling them. This is the single most important gate in Wave B; a false green here means an unbootable server ships. (Phrasing note, round 11, rev 14 — `admin_rekey_e2e_test.go` declares NO top-level `Test*` func; its rekey `Describe`s run under the suite's single entrypoint `TestAdminAuthenticateE2E` (`admin_authenticate_e2e_suite_test.go:32`), so the `|AdminRekey` half of the pattern matches nothing at the `go test` level and does not scope the run — coverage is real because that one entrypoint runs the whole admin Ginkgo suite, rekey specs included. Keep the command as-is.)
    - The topo-order pin from 07-10 still passes: `task test -- -run 'TopologicalStartOrder' ./cmd/holomush/` exits 0 (relocated from Task 2's boundary — round 11, rev 14: `./cmd/holomush/`'s test binary compiles only once this task migrates `stubSubsystem`)
    - After the unit's single squashed commit, `git status --porcelain` is clean after `task fmt` (round 10, rev 13 — post-commit check: a literal pre-commit run fails on the unit's own uncommitted edits; do not stall on it before committing. Relocated to the unit end in round 11, rev 14: the commit now closes Tasks 1–3.)
    - `task pr-prep` exits 0 (fast lane; the phase gate before push)
  </acceptance_criteria>
  <verify>
    <automated>task build && task test && task test:int</automated>
  </verify>
  <done>Every implementation satisfies the three-method interface, and acquire-before-serve is an executable property asserted over the real production graph. The unit's single squashed commit closes Tasks 1–3 with every whole-repo gate green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
| Artifact | Kind |
|---|---|
| `lifecycle.Subsystem.Prepare` | new interface method (replaces `Start`) |
| `lifecycle.Subsystem.Activate` | new interface method (replaces `Start`) |
| `Orchestrator.preparedOrder`, `Orchestrator.activatedOrder` | new fields (replace `startOrder`) |
| `Prepare` / `Activate` methods on 17 production subsystems | new methods |
| `cluster.Registry.Prepare` / `.Activate` | changed **interface** (`registry.go:27`) |
| 7 new `var _ lifecycle.Subsystem = (*T)(nil)` assertions | new compile-time gates |
| `TestStartAllActivatesNothingUntilEverySubsystemHasPrepared` | new test |
| Phase-order + two rollback-path tests in `internal/lifecycle/orchestrator_test.go` | new tests |
| `grpcSubsystem` reaper split (construct in `Prepare`, `Run` goroutines in `Activate`) + focused neither-runs-after-Prepare test | changed placement + new test (round 5, row 16) |
| `AdminSocketSubsystem.Activate` `s.server == nil` disabled-mode guard + migrated two-phase disabled-mode test | new guard + rewritten test (round 5, row 14) |
| D-13.2 ADD-verdict guards (relay/sweep `s.done != nil`, policy per-policy-name `emitted map[string]bool` — round 6, socket `s.errCh != nil`, plugin/audit/ABAC/grpc phase-owned state) + 4 repeated-call tests | new guards + new tests (rounds 5-6) |
| `cluster.registry.Prepare` = provider resolution + fail-closed validation (round 6 — NOT a no-op); `Activate` = the whole locked subscribe/publish/goroutine critical section | changed placement (row 13, round 6 BLOCKER) |
| `audit.Subsystem.preparedProjection *projection` + `audit.Subsystem.partitionManager *EventsAuditPartitionManager` | new phase-owned fields (row 10, round 7 BLOCKER — Prepare sets + guards on them; Activate consumes them and owns `worker`/`cancel`; Stop clears both states) + repeated-Prepare, Stop-after-Prepare-only, and Prepare-fail/retry (stale-lateInit) tests. Round 8: the prepared aggregate also covers the lateInit-written `s.cfg.Owners`/`s.pluginMgr` (restored on Prepare failure, cleared on prepared-only Stop). Round 9: prepared-only Stop clears in-memory state only — the durable JetStream consumer is intentionally retained (drain no-ops pre-Consume, `projection.go:467-470`; idempotent re-attach on retry), stated in Stop's doc |
| `PluginSubsystem` prepared-state cleanup: `cleanupOnError` extended to close `binaryHost` (token-store sweeper) + `luaHost` on pre-manager error paths, `Stop` doc'd for the pre-manager no-op | changed cleanup contract (row 9, round 9 BLOCKER) + `TestPluginSubsystemPrepareFailureAfterHostConstructionLeavesNoSweeperGoroutine` fault-injection test (`internal/plugin/setup/subsystem_test.go`, goleak) |
| `grpcSubsystemConfig.GuestReaperInterval time.Duration` | new config field (row 16, round 7 MEDIUM — defaults to `1 * time.Minute` when zero, replacing the hardcoded guest-reaper interval at `sub_grpc.go:766` so the neither-runs-after-Prepare test is deterministic) |

Deleted: `lifecycle.Subsystem.Start` and its 16 production + 3 test
implementations; `cluster.Registry.Start`; `Orchestrator.startOrder`.
Untouched: `Start` on `audit.RetentionWorker`, `audit.PluginConsumerManager`,
`invalidation.coordinator` — and, per round 8, on `socket.Server`
(`server.go:60`, row 14's atomic op) and `world.OrphanDetector`
(`property_lifecycle.go:108`) — none are subsystems.
No `Unprepare` / `Deactivate` — three methods by design.

**Scope, accepted deliberately (W-4; recount in revision 8).** This plan's
`files_modified` is **~54** (rev 8: +2 — the row-16 reaper test's home and the
relay repeated-call test) — far above the 10-file advisory, and up from rev 4's
25 because the cross-AI reviews found the manifest omitted **caller** files
(compile failures, not cleanup — see `<settlements>` § "The CALLERS are the real
blast radius"): round 3 added ~24, round 4 added 3 more that the rev-6
argument-shape census regex missed.

Accepted rather than split further, for the same reason as before and more so:
a single interface change forces the whole set to be **atomic** — the tree does
not compile until all 17 impls **and** every caller land together. Splitting the
*plan* would produce a wave whose intermediate state cannot build, which
`<action>` explicitly forbids ("Every commit pushed builds"). Round 11 (rev 14)
made this operational rather than aspirational: Tasks 1–3 are ONE atomic
execution unit with ONE commit at Task 3's boundary — rev 13's Task-2-boundary
commit + whole-repo `task test`/`task test:int` gates contradicted this very
paragraph, because the two Start-only test stubs Task 3 owns
(`core_subsystems_test.go:23`, `coordinator_error_test.go:36`) are compiled by
`task test`.

The added files are overwhelmingly **mechanical and identical in shape**:
`sub.Start(ctx)` → `sub.Prepare(ctx)` + `sub.Activate(ctx)`, preserving the
existing error handling. The executor's judgement is needed on the 17 impls (the
settled D-13.3 table) and on the handful of prepare-only sites; the rest is
compile-error-driven. Step 0's census bounds the working set up front rather than
discovering it mid-task, which is the actual mitigation.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subsystem acquisition → subsystem serving | The barrier this plan makes structural |
| boot gate (audit chain) → any serving | INV-CRYPTO-102 must hold before anything serves |
| failed boot → partial state | A half-started server is a fail-open posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-56 | Repudiation | A subsystem accepting external traffic or running a domain loop before a dependency it never declared is up — MEDIUM-11's stated latent risk | high | mitigate | The two-sweep global barrier: no `Activate` runs until every `Prepare` has. Asserted as an executable property over the **real** production graph (Task 3), not per-subsystem inspection. Structural, so a forgotten `DependsOn` edge cannot reintroduce it. Scope per D-13.0: the guarantee covers **externally-reachable surfaces and host-owned domain work loops** (round 9: plugin-side Init behavior is outside orchestrator enforcement — the barrier governs host-owned `Activate` bodies) — process-internal substrate (the `DontListen` embedded NATS server, the DB pool) is acquisition and legitimately comes up in `Prepare`, because peer subsystems acquire against it. Overclaiming a universal "nothing runs" barrier is what made the earlier revision unbootable. |
| T-07-62 | Elevation of Privilege | A future subsystem binding an externally-reachable **domain** surface in `Prepare`, silently escaping the barrier | high | mitigate | The barrier constrains `Activate`, so work smuggled into `Prepare` bypasses it — the one hole in this design. Mitigated by (a) the `Prepare` doc contract naming "MUST NOT expose a **domain** surface reachable from outside the process" (the "domain" qualifier is required: `eventbus.Prepare` legitimately binds the Prometheus monitor port per D-13.0 exception 1, so an absolute phrasing would be false as shipped and would train reviewers to ignore the clause), (b) Task 2's criterion requiring all 17 `Activate` bodies to be read and the external-surface set recorded as exactly {grpc listener, `admin.sock`}, and (c) the SUMMARY's 17-row table making each placement reviewable. Accepted residual: this is a review-enforced boundary, not a compile-enforced one — the interface can express it, unlike the class D-11 eliminates. The observability carve-out widens this residual by exactly one **audited** case; it is recorded in D-13.0, not implicit. |
| T-07-57 | Tampering | Chain verifier's INV-CRYPTO-102 gate running after something already serves | high | mitigate | The chain walk goes in `Prepare` (Task 2), so it refuses before **any** subsystem activates — independent of 07-10's edge. Defense in depth for the same property. |
| T-07-58 | Denial of Service | Leaked resources after an Activate failure at index j | high | mitigate | `StopAll` walks `preparedOrder` in reverse — a superset of `activatedOrder` — so a mid-Activate failure still tears down every acquired resource. Both rollback paths have dedicated tests (Task 1). |
| T-07-59 | Denial of Service | A duplicate poller goroutine from a non-idempotent phase under rollback | medium | mitigate | D-13.2 settled: the idempotency MUST is **kept** with its true rationale (`internal/access/setup/subsystem.go:77`). Task 2 requires a per-subsystem verdict and asserts the true rationale text survives while the false pre-start text is deleted. |
| T-07-60 | Denial of Service | Rollback itself hanging | high | mitigate | `StopAll` retains 07-10's deadline handling through the new paths; asserted by Task 1's ctx-deadline behavior. |
| T-07-67 | Denial of Service | **Disabled-mode admin socket nil-panics the boot** — Prepare's `SocketPath == ""` early return leaves `s.server == nil`, the two-sweep orchestrator still calls Activate, and an unguarded `srv.Start()` panics the whole server in exactly the degraded no-XDG-runtime-dir environment disabled mode exists for. An availability defect on a break-glass path (cross-AI round 5, BLOCKER) | high | mitigate | Row 14's settled guard: `Activate` returns nil when `s.server == nil` (WARN already logged in Prepare). Pinned by the migrated two-phase disabled-mode test (`internal/admin/socket/subsystem_test.go`), which nil-panics if the guard is dropped. |
| T-07-68 | Denial of Service | **The gRPC session/guest reapers never launch** because rev 7's row 16 assigned them to neither sweep — a silently green outcome where leases never sweep (stale sessions persist past TTL) and expired guests never reap (characters accumulate without tombstones) | high | mitigate | Row 16's settled disposition (construct in Prepare, launch in Activate, Stop cancels/drains) plus the focused neither-reaper-runs-after-Prepare test in `cmd/holomush/sub_grpc_test.go`. The caller-collapse criterion ("every migrated caller calls BOTH phases") covers the same silent-failure class at the caller side. |
| T-07-69 | Denial of Service | **Plugin partial-Prepare leak** — a `PluginSubsystem.Prepare` failure between `goplugin.NewHost` (`setup/subsystem.go:322`) and the manager assignment (`:384`) leaves the token-store sweeper goroutine running and the Lua host live, while `Stop` no-ops on `s.manager == nil` (`:451-453`) — so the rollback the orchestrator promises releases nothing for this subsystem (cross-AI round 9, BLOCKER) | high | mitigate | D-13.2 row 9's settled cleanup: `cleanupOnError` extended to close `binaryHost` (releases the sweeper via `goplugin/host.go:1579-1580`) and `luaHost` on every pre-manager error path, making `Stop`'s early return a true no-op. Pinned by the goleak fault-injection test in `internal/plugin/setup/subsystem_test.go` (Task 2 § "Plugin prepared-state cleanup"), observed RED against the pre-fix leak. |
| T-07-61 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — zero external packages installed (RESEARCH.md § Package Legitimacy Audit). |
</threat_model>

<verification>
- `task build` exits 0
- `task test` exits 0
- `task test:int` exits 0
- `task lint` exits 0
- `task pr-prep` exits 0 (fast lane; run inline before push per CLAUDE.md)
- The KEK-wired full boot passes (`task test:int -- -run 'TestAdminAuthenticate|AdminRekey' ./cmd/holomush/` — **`test:int`, not `test`**: integration-tagged files, else a vacuous zero-match pass)
- 07-10's topo-order pin still passes
- Wave B is independently reviewable: it applies cleanly on top of Wave A and a boot regression bisects to one wave (D-12)
</verification>

<success_criteria>
`lifecycle.Subsystem` cannot express "serve before acquire". `StartAll` prepares
everything, then activates everything, so **no subsystem accepts
externally-reachable DOMAIN traffic and no host-owned domain work loop runs
until every subsystem has acquired** (D-13.0 — the barrier is scoped to external
**domain** surfaces and **host-owned** domain loops, because process-internal
substrate like the `DontListen` embedded NATS server is what peer subsystems
acquire *against*, because plugin-side Init behavior is outside orchestrator
enforcement (round 9), and because observability endpoints are excepted: the
NATS Prometheus monitor binds `HTTPPort: s.cfg.MonitorPort`
(`internal/eventbus/subsystem.go:156`) in `Prepare`, exception 1). All 17
subsystems are placed
per the settled D-13.3 table, with `eventbus`'s whole body in `Prepare`,
`plugin` loading in `Prepare`, the two gRPC reapers constructed in `Prepare` and
launched in `Activate` (row 16, round 5), the disabled-mode admin socket
surviving both sweeps via the `s.server == nil` Activate guard (row 14, round
5), cluster provider resolution in `Prepare` with the locked
subscribe/publish/goroutine critical section whole in `Activate` (row 13, round
6), and crypto-policy emission guarded per policy name (row 15, round 6) — all
decided here, none left to the executor. `audit.Prepare` succeeds
against a merely-prepared bus. Both rollback paths tear down every prepared
subsystem in reverse within the deadline. Every idempotency guard has a
deliberate, plan-settled per-subsystem verdict grounded in a real side effect
(D-13.2's 17-row table). The process boots with a KEK configured, in the same
topological order, with unchanged player/API-observable startup/shutdown
behavior.
</success_criteria>

<output>
Create `.planning/phases/07-event-model-bootstrap-decomposition/07-11-SUMMARY.md` when done.
</output>
