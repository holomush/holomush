---
phase: 07-event-model-bootstrap-decomposition
plan: 09
type: execute
wave: 7
depends_on: ["07-08"]
files_modified:
  - internal/tls/subsystem.go
  - internal/tls/subsystem_test.go
  - internal/cluster/registry.go
  - internal/cluster/heartbeat.go
  - internal/cluster/registry_internal_test.go
  - internal/cluster/clustertest/harness.go
  - internal/cluster/clustertest/external.go
  - cmd/holomush/core.go
  - cmd/holomush/core_test.go
  - cmd/holomush/core_subsystems_test.go
  - cmd/holomush/sub_grpc.go
  - cmd/holomush/sub_grpc_test.go
  - cmd/holomush/admin_authenticate_e2e_test.go
  # Added in revision 3 (cross-AI review, HIGH — the manifest omitted likely owners):
  # eventbus absorbs VerifyAccountScoping (design table row :475); Bootstrap absorbs the
  # orphan boot gate; access/setup + admin/socket take cryptoWiring providers;
  # deps.go still references the deleted ensureTLSCerts (deps.go:51,68).
  - internal/eventbus/subsystem.go
  - internal/eventbus/subsystem_test.go
  - internal/bootstrap/setup/subsystem.go
  - internal/bootstrap/setup/subsystem_test.go
  # Added in revision 4 (plan-checker W-1 — the orphan gate's DEFINITION and TESTS were
  # never declared; revision 3's note cited core.go:296, which is only the CALL SITE).
  # `runBootstrapOrphanCheck` is DEFINED at cmd/holomush/bootstrap_orphan.go:34 (package
  # main) with 4 integration test call sites in cmd/holomush/bootstrap_orphan_test.go.
  # Both files MOVE; a duplicate definition is forbidden (Task 2 item 5).
  - cmd/holomush/bootstrap_orphan.go
  - cmd/holomush/bootstrap_orphan_test.go
  - internal/bootstrap/setup/orphan_check_internal_test.go
  - internal/access/setup/subsystem.go
  - internal/admin/socket/subsystem.go
  - internal/admin/policy/subsystem.go
  - internal/eventbus/audit/chain/verifier_subsystem.go
  - internal/eventbus/crypto/dek/sweep.go
  - cmd/holomush/deps.go
  - cmd/holomush/deps_test.go
autonomous: true
requirements: [ARCH-03]

must_haves:
  truths:
    - "Zero subsystem Start calls exist outside orch.StartAll"
    - "Zero live-value accessor reads (dbSub.Pool/GameID, authSub.Hasher/AuthService, abacSub.Resolver, eventBusSub.Conn/Publisher) execute on runCore's straight-line path — every one resolves inside a subsystem Start or inside the memoized cryptoWiring builder a subsystem Start invokes"
    - "Every cryptoWiring consumer declares DependsOn superset of {Database, Auth, ABAC, EventBus} — the first consumer to resolve the provider is the one that builds it"
    - "Boot succeeds with a KEK configured — the production-deploy shape that panics today"
    - "TLS certificate generation runs as a registered subsystem that declares its gameID dependency on Database, exposes TLSConfig() behind a panic guard, and preserves the deps.TLSCertEnsurer test seam"
    - "The bootstrap orphan boot gate still REFUSES the boot — its DEFINITION (cmd/holomush/bootstrap_orphan.go:34) and its 4 tests MOVED behind the Bootstrap->Database edge. Not dropped, and not duplicated: zero references to runBootstrapOrphanCheck remain anywhere under cmd/holomush/"
    - "A mis-ordered subsystem registration cannot compile"
  prohibitions:
    - statement: "D-10: no countdown latch or bespoke multi-phase ordering gate is introduced — topoSort + DependsOn (internal/lifecycle/orchestrator.go:44-149) remains the SOLE ordering authority. A latch would be a second ordering authority competing with topoSort: the MEDIUM-11 failure mode. If a readiness gate is ever wanted, reuse the existing lifecycle.ReadinessRegistry (internal/lifecycle/registry.go — AllReady/WaitReady/HealthReporter, already used at core.go step 9); do not invent one."
      status: resolved
      verification: "No new latch/countdown/barrier construct is added to internal/lifecycle/ or cmd/holomush/; lifecycle.ReadinessRegistry remains the only readiness surface and topoSort the only ordering authority. Covered by the topological start-order pin test (07-10) and threat row T-07-48."
  artifacts:
    - internal/tls/subsystem.go
  key_links:
    - "cluster subsystem takes a ConnProvider and resolves Conn() at Start, with DependsOn(SubsystemEventBus)"
    - "admin-handler wiring takes providers and resolves Hasher()/Resolver() at Start, with DependsOn(SubsystemAuth, SubsystemABAC)"
    - "SubsystemTLS (lifecycle/subsystem.go:18, iota=1) is finally registered — no const-block edit, no task generate"
---

<objective>
**D-12 Wave A.** Make constructors take handles/providers rather than live
values, so all five eager `Start` calls die and `StartAll` regains sole ownership
of boot ordering (D-09). Build the phantom TLS subsystem (D-14), and replace
`productionSubsystems`' positional parameter list (D-14 / LOW-8).

Purpose: **this has already caused a production incident.** `cmd/holomush/core.go:790-796`
records that Phase 5 sub-epic D's E2E found the production admin-handler wiring
*"panics on every boot when KEK is available"* — and the remedy was to add
*another* pre-start. Each pre-start moves start-ordering authority out of the
orchestrator; MEDIUM-11 is the downstream symptom.

Wave A must be **provable alone**: boot works, no pre-starts, and it remains a
complete win if Wave B (07-11) proves hairy (D-12).

Output: zero eager starts, a real TLS subsystem, and non-positional registration.
</objective>

<settlements>
### The five eager starts (verified live in this worktree)

| Site | Subsystem | Root cause |
|---|---|---|
| `cmd/holomush/core.go:287` | `dbSub.Start(ctx)` | `:282` — "The DB subsystem must start before TLS cert generation because the gameID (from `InitGameID`) is embedded in the CA certificate on first boot." |
| `cmd/holomush/core.go:462` | `eventBusSub.Start(ctx)` | `:457` — "`cluster.NewSubsystem` requires a non-nil `*nats.Conn` **at construction time**, and `eventBusSub.Conn()` returns nil prior to Start." |
| `cmd/holomush/core.go:797` | `abacSub.Start(ctx)` | `:786` — admin-handler wiring calls `abacSub.Resolver()` "without hitting the 'called before Start()' panic guards." |
| `cmd/holomush/core.go:800` | `authSub.Start(ctx)` | same — `authSub.Hasher()`. |
| `cmd/holomush/core.go:977` | crypto | same class. |

Every root cause is **a constructor or accessor demanding a live resource**, and
every fix is expressible as an existing `DependsOn` edge (D-09). The
`eventBusOwnedByOrchestrator` flag (`core.go:494-500`, flipped at `:1104`)
disappears with them.

### D-10 — no latch. `topoSort` is the only ordering authority.

`topoSort` + `DependsOn` (`internal/lifecycle/orchestrator.go:44-149`) **already
is** the ordered boot; the gap is purely constructor-side. A countdown latch
would be a **second ordering authority competing with topoSort** — precisely the
MEDIUM-11 failure mode (comments + slice order asserting an order the graph did
not enforce; the graph silently wins). If a readiness gate is ever wanted, reuse
`lifecycle.ReadinessRegistry` (`internal/lifecycle/registry.go` —
`AllReady`/`WaitReady`/`HealthReporter`, already used at core.go step 9). Do not
invent one.

### D-14 — `productionSubsystems` takes **16** positional params, not 15

CONTEXT.md D-14 (LOW-8) says 15. Verified live at `cmd/holomush/core.go:1462-1472`:
`dbSub, abacSub, authSub, worldSub, sessionSub, pluginSub, bootstrapSub,
cryptoChainVerifierSub, eventBusSub, clusterSub, auditSub, cryptoPolicySub,
grpcSub, adminSub, rekeyCheckpointSweepSub, outboxRelaySub` — **16**. With TLS →
**17**. The count cascade is concrete: `core_subsystems_test.go:26-52`'s
`allStubs() [16]stubSubsystem` and every call site that spreads `s[0]…s[15]`
positionally (e.g. `:57-61`).

### The phantom `SubsystemTLS` needs no const-block edit

`SubsystemTLS` is declared at `internal/lifecycle/subsystem.go:18` at **iota=1**
and is already stringer-generated. Registering it therefore requires **no new
constant and no `task generate`** — CONTEXT.md § Specific Ideas' "new SubsystemID
constants go at the END of the const block, then `task generate`" does **not**
apply here. `internal/tls` is already a true leaf (verified `go list -deps`).

### The pre-orchestrator live-value design. SETTLED HERE — the planner's job, not the executor's.

**Cross-AI review, HIGH:** an earlier revision located ~20 pre-orchestrator
accessor calls and then told the executor to *"classify them and record the
decisions in the SUMMARY"* — i.e. it named the root causes and deferred the
design. **Retracted.** These are load-bearing: `Pool()`/`EventStore()`/`GameID()`
all **panic** before DB start (`internal/store/subsystem.go:89-111`), so deleting
`dbSub.Start(ctx)` (`core.go:287`) makes **the very next line** (`:296`) panic.
One missed disposition is a boot panic — the exact failure this phase exists to
prevent. The design is below; the executor **applies** it.

#### The shape of the problem (verified live)

`runCore` is a ~900-line straight-line wiring routine that must construct all 17
subsystems **before** `orch.StartAll`. `store.DatabaseSubsystem.Start`
(`subsystem.go:50-78`) is what **creates** the pool (`s.pool = es.Pool()` at `:65`)
and resolves the gameID (`InitGameID`, `:67`) — so there is no live pool to hand
out before it runs. That is the whole mechanism: **wiring happens before starting,
and some wiring needs a started resource.** Hence the pre-starts.

D-09 is locked (*"constructors take handles/providers, not resolved live values;
resolution happens at `Start` time"*), so the only compliant shape is: **the reads
move to where the graph guarantees the resource is up.**

#### The settled design: hoist the wiring into a memoized `cryptoWiring`, resolved at Start

`core.go:705-1060` — the crypto/admin wiring block — is **17 of the ~20 reads**
in one contiguous region. It is not moved line-by-line. It is **hoisted whole**
into a memoized builder in `package main`:

```
type cryptoWiring struct { /* the block's outputs */ }
// resolveCryptoWiring returns a func() (*cryptoWiring, error) memoized with
// sync.Once, closing over dbSub/authSub/abacSub/eventBusSub and cfg.
```

- **The block's body moves verbatim.** The `dbSub.Pool()` / `authSub.Hasher()` /
  `abacSub.Resolver()` / `eventBusSub.Conn()` calls **inside** it do not change at
  all — they simply now execute after those subsystems have started. This is why
  the diff is a hoist, not a rewrite.
- **Consumers take a provider, not a value.** Each subsystem config that today
  holds a wiring output takes `func() (*cryptoWiring, error)` (or a narrow
  accessor off it) and calls it **in its own `Start`**.
- **Fatal stays fatal.** `buildRekeyWiring`'s error (`core.go:936`),
  `BOOT_KEK_REQUIRED` (`:816`) and
  `CRYPTO_DEK_MANAGER_CACHE_ACCESSOR_NOT_SATISFIED` (`:961`) surface from the
  memoized builder into the first consumer's `Start`, which returns them →
  `StartAll` aborts the boot. Identical outcome to today's `return err` from
  `runCore`, just relocated.
- **WARN-and-continue stays WARN-and-continue.** Every degraded path inside the
  block (`:831`, `:851`, `:872`, `:881`, `:939`, `:975`, `:978`, `:1036`) is
  **inside the hoisted body** and is untouched.
- **This is not a second ordering authority (D-10).** The builder does no
  ordering — it is lazy memoization. Ordering remains `topoSort` + `DependsOn`,
  exactly as D-10 requires. It is not a latch, a countdown, or a barrier.

#### ⚠️ THE RULE that makes this safe — state it in code, pin it with a test

> **Every subsystem whose config holds a `cryptoWiring` provider MUST declare the
> wiring's full dependency set — `DependsOn(SubsystemDatabase, SubsystemAuth,
> SubsystemABAC, SubsystemEventBus)` — because the FIRST consumer to resolve the
> provider is the one that builds it.**

A consumer missing an edge could start before `Auth` and trigger the build →
`authSub.Hasher()` panics. This is the one sharp edge of the design and it MUST
be blunted mechanically: add a test asserting each wiring consumer's `DependsOn`
set is a **superset** of the wiring's dep set. Under Wave B (07-11) the resolution
point becomes `Prepare`, which the global barrier makes safer still.

#### The design table — one row per live accessor site

Re-run the census before editing (line numbers drift):
```
rg -n 'dbSub\.(Pool|GameID|EventStore)\(\)|eventBusSub\.(Conn|Publisher|JetStream)\(\)|authSub\.(Hasher|AuthService)\(\)|abacSub\.(Resolver|Engine)\(\)' cmd/holomush/core.go
```

| Site | Read | Consumer | Disposition | Owner after | Edge required |
|---|---|---|---|---|---|
| `:296` (call site; **definition at `cmd/holomush/bootstrap_orphan.go:34`**) | `dbSub.Pool()` | `runBootstrapOrphanCheck` — **boot gate**, queries `events_audit` | **(a) the FUNCTION MOVES into a subsystem `Start`** — see Task 2 item 5 for the file-level move | `setup.BootstrapSubsystem` (`internal/bootstrap/setup/subsystem.go`) — it already owns boot-time seeding against a started pool and already has the DB edge. ⚠️ **It already HAS a `PoolProvider`** (`subsystem.go:34-35`) and its `Start` already opens with `pool := s.cfg.DB.Pool()` (`:124`) — **add no new provider**; run the check first against that same `pool`. ⚠️ It MUST stay a **refusal** gate (return the error) and MUST still run before anything serves — `grpcSubsystem.DependsOn()` already includes `Bootstrap`, which preserves that under Wave A; under Wave B it lands in `Prepare`, same class as `chain.VerifierSubsystem`'s INV-CRYPTO-102 walk. Do **not** drop it or demote it to post-serving. | `Bootstrap → Database` (exists) |
| `:302` | `dbSub.GameID()` | `gameID` local, feeds TLS + ~12 downstream consumers | **(b) provider** | `tls.TLSSubsystem` resolves it from its `GameIDProvider` **inside its own `Start`** (Task 1). ⚠️ **This is the entire stated reason the DB pre-start exists** (`core.go:281-283`). Other consumers of `gameID` take `func() string` closing over `dbSub.GameID()` (with the `cfg.GameID` override preserved: `cfg.GameID` wins when non-empty, `:300-303`). | `TLS → Database` (Task 1) |
| `:312`, `:690` | `tlsConfig` (from `deps.TLSCertEnsurer(certsDir, gameID)`) | `grpcSubsystemConfig.TLSConfig` | **(b) provider** | `TLSSubsystem.TLSConfig()` accessor with the standard panic-before-`Start` guard (**Task 1 MUST specify it — see below**); `grpcSubsystem` takes the provider and resolves at its `Start`. Without this the pre-start is renamed, not removed. | `gRPC → TLS` (new) |
| `:475` | `eventBusSub.Conn()` | `eventbus.VerifyAccountScoping` — an `internal/eventbus` function | **(a) moves into a subsystem `Start`** | `eventbus.Subsystem.Start` itself — it is that package's own boot check against its own connection. Move it in; no provider needed. | none (self) |
| `:547` | `eventBusSub.Conn()` | `cluster.Deps.Conn` at construction | **(b) provider** | `cluster.ConnProvider` resolved in `registry.Start` — **which lives at `internal/cluster/heartbeat.go:22`, NOT `registry.go`** (see below). | `Cluster → EventBus` (exists, `registry.go:174`) |
| `:735` | `eventBusSub.Publisher()` | `auditPublisher` = `eventbus.NewRenderingPublisher(...)` | **inside the hoist** | `cryptoWiring` — it is in the `:705-1060` block; the read moves with the body. | (wiring rule) |
| `:967` | `eventBusSub.Conn()` | `invalidation.Deps.Conn` | **inside the hoist** | `cryptoWiring`, consumed by `grpcSubsystem` (item 4 below settles Coordinator ownership). | (wiring rule) |
| `:385` | `dbSub.Pool()` | `validateCryptoOperators` | **inside the hoist** | `cryptoWiring`. ⚠️ It sits **above** the block at `:705`; hoist it **into** the builder rather than leaving it stranded pre-orchestrator. | (wiring rule) |
| `:713`, `:743`, `:763`, `:814`, `:822`, `:858`, `:875`, `:918`, `:1015` | `dbSub.Pool()` ×9 | `chain.NewPostgresRepo`, `policy.EmitDeps.Pool`, `dek.NewCheckpointRepo`, `provisionBootKEKProvider`, `totp.NewRepository`, `store.NewPostgresRoleStore`, `approval.NewPostgresRepo`, `rekeyWiringDeps.Pool`, `readStreamWiringDeps.Pool` | **inside the hoist** | `cryptoWiring` — all nine are in the `:705-1060` block. **No repo signature changes**: each still takes a live `*pgxpool.Pool`, obtained after DB start. | (wiring rule) |
| `:828`, `:866` | `authSub.Hasher()`, `authSub.AuthService()` | `totp.NewService`, `adminauth.NewInGameCredentialsProvider` | **inside the hoist** | `cryptoWiring`. These two reads are why `authSub.Start` is pre-called at `:800`. | (wiring rule) |
| `:868`, `:877`, `:878`, `:925`, `:1019` | `abacSub.Resolver()` ×5 | InGame provider, ApproveHandler, ResetTOTPHandler, `rekeyWiringDeps`, `readStreamWiringDeps` | **inside the hoist** | `cryptoWiring`. These are why `abacSub.Start` is pre-called at `:797`. | (wiring rule) |

**Wiring consumers — each takes a `cryptoWiring` provider and therefore each MUST
carry the full dep set** (the rule above): `policy.CryptoPolicySubsystem`
(`EmitDeps`), `dek.CheckpointSweepSubsystem` (`Repo`/`AuditEmitter`),
`chain.VerifierSubsystem` (`Repo` + `Handlers` — note `chainHandlers`
(`core.go:1046-1052`) depends on `readStreamW.Handler`, which depends on
`rekeyW.Manager`, which needs the pool: its `Handlers` field therefore becomes a
`HandlersProvider func() []chain.Handler` resolved at `Start`, not a slice),
`socket.AdminSocketSubsystem` (the three admin handlers + readstream), and
`grpcSubsystem` (`RekeyManager`, `CoordHolder`).

**No 18th subsystem.** The count stays **17** (Task 3, and 07-11's 17-row census).
`cryptoWiring` is a `package main` helper, not a `lifecycle.Subsystem` — it has no
`ID()`/`DependsOn()` and is never registered.

**Disposition (c) — "genuinely must run pre-orchestrator" — is NOT available for a
subsystem live-value read.** D-09 forbids it. If a row seems to need (c), it is a
planning defect: **stop and report** rather than reintroducing a pre-start.

### ⚠️ `internal/cluster` splits its lifecycle across TWO files

`cluster`'s `Start`/`Stop` do **not** live in `registry.go`. Verified live:

| Symbol | Site |
|---|---|
| `type Registry interface` (declares `Start` at `:27`, `Stop` at `:28`) | `internal/cluster/registry.go:23-52` |
| `NewSubsystem` (the nil-`Conn` check at `:84-87`, `isNilConn` at `:287`) | `internal/cluster/registry.go:78` |
| `type registry struct` | `internal/cluster/registry.go:113` |
| `DependsOn` | `internal/cluster/registry.go:174` |
| **`func (r *registry) Start(ctx) error`** | **`internal/cluster/heartbeat.go:22`** |
| **`func (r *registry) Stop(ctx) error`** | **`internal/cluster/heartbeat.go:110`** |

`cluster` is the **only** subsystem in the tree whose `Start` is not co-located
with its `DependsOn` (re-audited by locating `Start` directly rather than
assuming co-location — the other 15 production impls and all 3 test stubs
co-locate). "Move the nil check to `Start`" therefore edits **`heartbeat.go`**,
not `registry.go`.

**`Registry` is an interface, so the blast radius includes its callers.** Both
harness constructors pass a live `Conn` in the `Deps` literal and must move to
the provider shape:
- `internal/cluster/clustertest/harness.go:115-119` — `cluster.NewSubsystem(cfg, cluster.Deps{Conn: emb.Conn, …})`
- `internal/cluster/clustertest/external.go:77-81` — `cluster.NewSubsystem(cfg, cluster.Deps{Conn: conn, …})`
</settlements>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-event-model-bootstrap-decomposition/07-CONTEXT.md
@.planning/phases/07-event-model-bootstrap-decomposition/07-PATTERNS.md
@.claude/rules/logging.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Build the TLS subsystem</name>
  <read_first>
    - cmd/holomush/core.go:256-360 (the DB pre-start at :287 and its gameID/TLS rationale at :282; ensureTLSCerts' call site)
    - cmd/holomush/core.go:1320-1340 (ensureTLSCerts — the inline plain function being wrapped)
    - internal/world/setup/subsystem.go:19-70 (THE analog: PoolProvider decl, config struct, "does not allocate or start any runtime resources" ctor, ID, DependsOn, Start-that-then-resolves)
    - internal/lifecycle/subsystem.go:16-40 (the SubsystemID const block — confirm SubsystemTLS at iota=1)
    - internal/tls/ (the existing leaf the subsystem wraps)
    - .planning/phases/07-event-model-bootstrap-decomposition/07-PATTERNS.md § 7 "TLS subsystem"
  </read_first>
  <files>internal/tls/subsystem.go, internal/tls/subsystem_test.go</files>
  <action>
Create a `TLSSubsystem` in `internal/tls/subsystem.go` wrapping today's inline
`ensureTLSCerts` (`cmd/holomush/core.go:1325`), following `WorldSubsystem`
(`internal/world/setup/subsystem.go:53-70`) exactly:

- A `TLSSubsystemConfig` struct of **provider interfaces + plain config**, not
  live values. It needs the gameID, which comes from the DB (`InitGameID`), so
  declare a consumer-defined provider interface here — e.g.
  `GameIDProvider interface { GameID() string }` — with the repo's doc
  convention: *"Implemented by the database subsystem without requiring a direct
  import"* (`internal/world/setup/subsystem.go:21-31`). Read the actual
  `InitGameID` call at `core.go:256-300` to get the real accessor name and
  return type; do not invent them.
- `func NewTLSSubsystem(cfg TLSSubsystemConfig) *TLSSubsystem { return &TLSSubsystem{cfg: cfg} }`
  — and its doc MUST be able to say truthfully *"It does not allocate or start
  any runtime resources; call Start to …"*. That sentence is the D-09 acceptance
  criterion in prose (`internal/access/setup/subsystem.go:52`,
  `internal/world/setup/subsystem.go:53-54`).
- `func (s *TLSSubsystem) ID() lifecycle.SubsystemID { return lifecycle.SubsystemTLS }`
  — the constant already exists at `subsystem.go:18` (iota=1). **Do not edit the
  const block; do not run `task generate`.**
- `func (s *TLSSubsystem) DependsOn() []lifecycle.SubsystemID { return []lifecycle.SubsystemID{lifecycle.SubsystemDatabase} }`
  — the gameID edge, which is exactly the reason `core.go:282` gives for the DB
  pre-start.
- `Start(ctx)` resolves the gameID from the provider and runs today's
  `ensureTLSCerts` body **verbatim** — same paths, same permissions, same
  regeneration conditions. Carry the
  `// codecov:ignore — tested by integration and E2E tests` comment used on
  subsystem `Start` bodies (`world/setup/subsystem.go:65`).
- **`func (s *TLSSubsystem) TLSConfig() *tls.Config` — REQUIRED, with the standard
  panic-before-`Start` guard** (`panic("tls: TLSConfig() called before Start()")`,
  the idiom at `internal/store/subsystem.go:89-95` and
  `internal/plugin/setup/subsystem.go:502-507`). ⚠️ **Do not omit this.** An
  earlier revision specified the accessor only in Task 2, which retroactively
  required a symbol Task 1 never defined (cross-AI review, HIGH). It is
  load-bearing: `tlsConfig` (`core.go:312`) is consumed as a **live value** by
  `grpcSubsystemConfig.TLSConfig` (`core.go:690`), so without this accessor
  `runCore` must still compute the cert config eagerly and the DB pre-start is
  **renamed, not removed** — Task 2's whole premise fails. `Start` stores the
  `*tls.Config` that `ensureTLSCerts`' body returns; `TLSConfig()` hands it out.
  Read `core.go:1375-1379`'s `tlscerts.LoadServerTLS(certsDir, "core")` for the
  real return type rather than assuming it.
- `Stop(ctx)` is a no-op returning nil (cert generation owns no runtime
  resource), documented as such.
- Use `slog.*Context` variants (`.claude/rules/logging.md`; `sloglint context: scope`).

Move `ensureTLSCerts`' body out of `cmd/holomush/core.go` into the subsystem;
do not leave a duplicate behind.

### ⚠️ `deps.TLSCertEnsurer` is a LIVE test seam — it breaks at compile time (cross-AI review, HIGH)

Verified live: `cmd/holomush/deps.go:53` declares
`TLSCertEnsurer func(certsDir, gameID string) (*cryptotls.Config, error)` and
`:70-71` defaults it — `d.TLSCertEnsurer = ensureTLSCerts`. **Deleting
`ensureTLSCerts` from `core.go` without this file breaks `task build`**, and
`cmd/holomush/deps_test.go:972` asserts the field is non-nil. An earlier revision
listed neither file in `files_modified`.

**Do NOT delete the seam** — it is how tests substitute cert generation. Settled:
- Export the moved body from the new package as
  `tls.EnsureCerts(certsDir, gameID string) (*cryptotls.Config, error)` — the
  **same signature** `deps.go:53` already declares, so the `Deps` field type does
  not change.
- `deps.go:71` becomes `d.TLSCertEnsurer = tls.EnsureCerts`. That is the whole
  edit to `deps.go`; `deps_test.go:972` then passes unchanged.
- `TLSSubsystemConfig` takes the **ensurer as a field**
  (`CertEnsurer func(certsDir, gameID string) (*cryptotls.Config, error)`), which
  `runCore` populates from `deps.TLSCertEnsurer`. `Start` calls it with the
  resolved gameID. This preserves the seam end-to-end: a test overriding
  `deps.TLSCertEnsurer` still overrides what the subsystem runs.
- ⚠️ `internal/tls` already exists as a leaf and the package name `tls` collides
  with stdlib `crypto/tls` — `deps.go` already aliases it `cryptotls` (`:53`).
  Follow that existing alias convention; do not rename the package.

`internal/tls/subsystem_test.go`: assert `ID()` is `SubsystemTLS`, `DependsOn()`
is `[SubsystemDatabase]`, and that `NewTLSSubsystem` touches no filesystem
(construct it and assert no cert file appears — the "no live resources" contract).
  </action>
  <acceptance_criteria>
    - `rg -n 'return lifecycle.SubsystemTLS' internal/tls/subsystem.go` prints one line
    - `rg -c 'lifecycle.SubsystemDatabase' internal/tls/subsystem.go` returns 1
    - `git diff --exit-code internal/lifecycle/subsystem.go internal/lifecycle/subsystemid_string.go` exits 0 (no const-block edit, no stringer regen)
    - `rg -c 'does not allocate or start any runtime resources' internal/tls/subsystem.go` returns 1
    - `rg -c 'func ensureTLSCerts' cmd/holomush/core.go` returns 0 (no duplicate body left behind)
    - **`rg -n 'func (s \*TLSSubsystem) TLSConfig\(\)' internal/tls/subsystem.go` prints one line, and `rg -c 'called before Start\(\)' internal/tls/subsystem.go` returns 1** — the accessor Task 2 depends on, with its panic guard. Without it, `grpcSubsystemConfig.TLSConfig` (`core.go:690`) still needs an eagerly-computed value and the DB pre-start survives under a new name.
    - A test asserts `TLSConfig()` panics before `Start` and returns the config after — the provider contract
    - **`rg -n 'd.TLSCertEnsurer = ' cmd/holomush/deps.go` prints one line assigning `tls.EnsureCerts`** and `rg -c 'ensureTLSCerts' cmd/holomush/` returns 0 — the test seam survives the move (`deps.go:71` assigns the deleted symbol today; `task build` is the gate)
    - `task test -- -run 'TLSCertEnsurer|Deps' ./cmd/holomush/` exits 0 (`deps_test.go:972`'s non-nil assertion still passes)
    - `task test -- ./internal/tls/` exits 0
    - `task build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>task test -- ./internal/tls/ && task build</automated>
  </verify>
  <done>TLS is a registered subsystem declaring its gameID dependency as a real edge; its constructor allocates nothing; the phantom ID is finally used with no stringer churn.</done>
</task>

<task type="execute">
  <name>Task 2: Constructors take handles — kill all five eager starts</name>
  <read_first>
    - cmd/holomush/core.go:256-300 (DB pre-start), :450-500 (eventbus pre-start + the eventBusOwnedByOrchestrator flag), :780-805 (abac/auth pre-starts + the boot-panic incident note at :790-796), :960-985 (crypto pre-start), :1080-1110 (orch registration + StartAll + the flag flip at :1104)
    - internal/cluster/registry.go:23-52 (the `Registry` **interface** — it declares Start at :27; changing the ctor's shape does not touch it, but read it so you know it exists), :62-111 (Deps.Conn's `natsconn.Conn` type at :63 and the nil-Conn check at :84-87 inside NewSubsystem), :278-295 (isNilConn at :287)
    - internal/cluster/heartbeat.go:15-97 (**where `Start` actually lives — `:22`, NOT registry.go**. The nil check moves HERE. Note the lock discipline documented at :19-21: Start takes `r.mu` for its whole body and the first heartbeat publish at :72 runs under the write lock deliberately — `publishHeartbeatLocked` exists so it does not self-deadlock against a RLock. Resolving `Conn()` from the provider MUST happen where it cannot reintroduce that deadlock.)
    - internal/cluster/clustertest/harness.go:110-130 and internal/cluster/clustertest/external.go:72-95 (the two `NewSubsystem` callers that pass a live `Conn` in the Deps literal — they break at compile time when the ctor takes a provider)
    - cmd/holomush/bootstrap_orphan.go (read in full — ~76 lines; `runBootstrapOrphanCheck` is DEFINED at `:34`, NOT in core.go. `core.go:296` is only the call site. This whole file moves — item 5)
    - cmd/holomush/bootstrap_orphan_test.go (read in full — `//go:build integration` at `:1`, `package main` at `:6`; 4 tests calling `runBootstrapOrphanCheck` at `:43`, `:69`, `:98`, `:121`, plus the local `applyAllMigrations` helper at `:25`. These move WITH the definition — item 5)
    - internal/bootstrap/setup/subsystem.go:33-35 (`PoolProvider` — it ALREADY EXISTS; do not add one), :100-115 (`ID`/`DependsOn` — the `Bootstrap → Database` edge already exists), :123-130 (`Start`, whose first line is already `pool := s.cfg.DB.Pool()` — the orphan check goes first, against that same pool), :28 (it already imports `internal/store`, so `store.NewMigrator` is reachable from a test in this package with no cycle)
    - internal/cluster/registry_internal_test.go:1-6 (the in-repo `package cluster` internal-test-package precedent — the shape item 5's moved tests use, since `runBootstrapOrphanCheck` stays unexported and the existing `subsystem_test.go` is `package setup_test`)
    - internal/store/testhelpers_test.go:31-72 (`freshMigratedPool`/`runMigrations` — the in-repo precedent for composing a postgres testcontainer + `store.NewMigrator` inline. It is `package store_test`, so it is NOT importable: item 5's moved tests compose their own, following this shape)
    - internal/world/setup/relay_subsystem.go:30-80 (EventBusProvider — the NEAREST existing instance of exactly the ConnProvider shape needed here)
    - internal/access/setup/subsystem.go:27-78 (ABACSubsystem: config-of-providers ctor, "No live resources are allocated", Resolver()'s panic guard)
    - internal/auth/setup/subsystem.go:22-60 (AuthSubsystem + Hasher())
    - internal/plugin/setup/subsystem.go:500-510 (the accessor panic-guard idiom to PRESERVE)
    - .planning/phases/07-event-model-bootstrap-decomposition/07-PATTERNS.md § 6 "Handles, not live values"
  </read_first>
  <files>internal/cluster/registry.go, internal/cluster/heartbeat.go, internal/cluster/registry_internal_test.go, internal/cluster/clustertest/harness.go, internal/cluster/clustertest/external.go, cmd/holomush/core.go, cmd/holomush/core_test.go, cmd/holomush/sub_grpc.go, cmd/holomush/bootstrap_orphan.go, cmd/holomush/bootstrap_orphan_test.go, internal/bootstrap/setup/subsystem.go, internal/bootstrap/setup/orphan_check_internal_test.go, internal/eventbus/subsystem.go</files>
  <action>
**No new pattern is needed — the correct shape is already the majority shape.**
`WorldSubsystem`/`ABACSubsystem` take a config struct of provider interfaces and
resolve at `Start`. The five eager starts are deviations *from the repo's own
existing pattern*, not a gap in it.

**Preserve the accessor panic guards.** `Resolver()`, `Hasher()`,
`CommandRegistry()` etc. keep panicking when called before `Start`
(`internal/plugin/setup/subsystem.go:502-507` is the idiom) — the guards **are**
the provider contract. What dies is the **caller-side pre-`Start()`** that exists
to dodge them. A panic guard is the tell for this bug class: it means the type
has two lifecycle states its constructor cannot express, so callers sequence by
hand — the orchestrator's job.

### ⚠️ STEP 0 (MANDATORY, DO THIS FIRST) — verify the settled design table against live code

**The design is settled in `<settlements>` § "The pre-orchestrator live-value
design" — read it before editing anything.** It carries a row per accessor site
with its disposition, its owner after the change, and the required edge. An
earlier revision produced only the census and asked the executor to classify the
rows afterward in the SUMMARY; that deferral is **retracted** (cross-AI review,
HIGH). You are applying a design, not authoring one.

**Step 0 is a verification pass, not a classification pass.** Re-run the census
(line numbers drift):

```
rg -n 'dbSub\.(Pool|GameID|EventStore)\(\)|eventBusSub\.(Conn|Publisher|JetStream)\(\)|authSub\.(Hasher|AuthService)\(\)|abacSub\.(Resolver|Engine)\(\)' cmd/holomush/core.go
```

Then, for each row returned:
- Confirm it appears in the design table. **A site the table does not cover is a
  planning defect — stop and report it**; do not improvise a disposition, and do
  not reintroduce a pre-start to paper over it (disposition (c) is unavailable
  for a subsystem live-value read; D-09 forbids it).
- Confirm the table's line reference still names the same construct.

Record the verification (rows matched / rows drifted / rows unaccounted) in the
SUMMARY. The 17-row-block reads inside `core.go:705-1060` do **not** need
individual treatment — they move with the hoisted body (see the design's
`cryptoWiring` section); verify only that the block's boundaries are still
`:705`-ish to `:1060`-ish and that nothing outside them reads a pre-started
accessor.

### ⚠️ Task 1 owes `TLSSubsystem` a `TLSConfig()` accessor — cross-check before starting

`tlsConfig` (`core.go:312`, from `deps.TLSCertEnsurer(certsDir, gameID)`) is
consumed as a **live value** by the gRPC subsystem: `core.go:690` passes
`TLSConfig: tlsConfig` into `grpcSubsystemConfig`. If TLS becomes a subsystem
(Task 1) but `runCore` still computes `tlsConfig` eagerly to hand to gRPC, the
pre-start is only renamed, not removed. **Task 1 MUST give `TLSSubsystem` a
`TLSConfig()` accessor with the standard panic-before-`Start` guard** (it now
does — see Task 1's action), and gRPC MUST take a provider and resolve it at its
own `Start`, declaring `DependsOn(lifecycle.SubsystemTLS)`. Cross-check Task 1's
shape against this before starting Task 2. (An earlier revision required the
accessor here while Task 1 never specified it — retracted; the requirement now
lives in Task 1.)

Fix each root cause:

1. **`cluster.NewSubsystem` requiring a live `*nats.Conn` at construction**
   (`core.go:543`; the pre-start it forces is `core.go:462`). Give it a
   `ConnProvider interface { Conn() natsconn.Conn }` in its `Deps` — read
   `internal/cluster/registry.go:63`'s existing `Deps.Conn` field for the exact
   type (it is the `natsconn.Conn` interface introduced by holomush-ojw1.3.23,
   not `*nats.Conn`) — and resolve it inside `Start`. Declare
   `DependsOn(lifecycle.SubsystemEventBus)` (`registry.go:174` — already returns
   exactly this; verify rather than re-add).
   `internal/world/setup/relay_subsystem.go:34`'s `EventBusProvider` is the
   nearest existing instance of exactly this shape.

   ⚠️ **The nil check and `Start` are in different files.** Keep `isNilConn`
   (`registry.go:287`) where it is, and move the nil-`Conn` check **out of**
   `NewSubsystem` (`registry.go:84-87`) **into `Start` — which lives at
   `internal/cluster/heartbeat.go:22`, not `registry.go`.** `cluster` is the one
   subsystem in the tree that splits `DependsOn` (registry.go) from `Start`
   (heartbeat.go); see `<settlements>`.

   ⚠️ **Respect `Start`'s lock discipline** (`heartbeat.go:19-21`): the body runs
   under `r.mu` end to end, and the first heartbeat publish (`:72`) calls
   `publishHeartbeatLocked` specifically to avoid a RLock/write-lock
   self-deadlock. Resolve `Conn()` from the provider **before** taking the lock
   (or at the top of the locked section, doing no re-entrant registry calls) —
   do not call a provider method that could re-enter the registry while `r.mu`
   is held.

   ⚠️ **Fix the two harness callers** — they construct with a live `Conn` and
   break at compile time: `internal/cluster/clustertest/harness.go:115-119`
   (`Conn: emb.Conn`) and `internal/cluster/clustertest/external.go:77-81`
   (`Conn: conn`). Give each a tiny provider wrapper rather than reintroducing an
   eager resolve.

   Delete the `eventBusSub.Start(ctx)` pre-start (`core.go:462`) **and** the
   `eventBusOwnedByOrchestrator` flag (`core.go:494-500`, flipped at `:1104`) —
   the flag exists only to track who owns the bus's lifecycle, a question that
   stops existing once the orchestrator always owns it.

2. **The DB pre-start** (`core.go:287`) exists solely so TLS cert generation can
   read the gameID (`:282`). Task 1's `DependsOn(SubsystemDatabase)` edge now
   expresses that. Delete `dbSub.Start(ctx)`.

3. **The abac/auth pre-starts** (`core.go:797`, `:800`) exist so admin-handler
   wiring can call `authSub.Hasher()` / `abacSub.Resolver()` without tripping the
   panic guards (`:785-796`). Make the admin-handler wiring take **providers**
   and resolve at its own `Start`, declaring
   `DependsOn(lifecycle.SubsystemAuth, lifecycle.SubsystemABAC)`. Delete both
   pre-starts.
   ⚠️ Keep the incident record: `core.go:790-796` documents that this wiring
   *"panics on every boot when KEK is available"*. Rewrite the comment to
   describe the **edge** that now prevents it — do not delete the history.

4. **The crypto pre-start** (`core.go:977`) — `invalidation.Coordinator`.
   **SETTLED HERE; THIS PLAN IS THE SINGLE SOURCE. 07-11 defers to it.**

   Cross-AI review found this "fifth eager start" has **no lifecycle owner**, and
   that 07-09 and 07-11 appeared to disagree. Verified live, the shape is:
   `invalidation.Coordinator` (`internal/eventbus/crypto/invalidation/coordinator.go:21-25`)
   is an **interface with `Start`/`Stop`/`RequestInvalidation`** — it has **no
   `ID()` and no `DependsOn()`, so it is NOT a `lifecycle.Subsystem`.**
   `core.go:969-990` constructs it, calls `c.Start(ctx)` eagerly, stores it in
   `coordHolderPtr.coord`, and stops it with an **ad-hoc `defer`** whose own
   comment says *"Coordinator is not orchestrator-owned; stop it on normal exit."*

   **SETTLED: the Coordinator stays a NON-subsystem and keeps its own
   `Start`/`Stop` methods; `grpcSubsystem` takes ownership of its lifecycle.**
   Verified live, grpcSub is already its de-facto owner and already holds both of
   its hard dependencies:
   - `grpcSubsystemConfig.RekeyManager` (`sub_grpc.go:99`) is the `dek.Manager`
     whose caches the Coordinator MUST bind to (`core.go:957-968` — the
     `dek.CacheAccessor` identity; Phase 3c grounding Decision 5, without which
     peers serve stale OLD DEK after a cross-replica Rekey: a forward-secrecy
     regression).
   - `grpcSubsystemConfig.CoordHolder` (`core.go:928`) is already the late-binding
     holder the Coordinator is published into.
   - grpcSub already declares `DependsOn(lifecycle.SubsystemEventBus)`
     (`sub_grpc.go:170-177`), covering `eventBusSub.Conn()`.

   Concretely:
   - **Add `lifecycle.SubsystemCluster` to grpcSub's `DependsOn`** — the
     Coordinator needs `Registry: clusterSub` (`core.go:971`) and that edge does
     not exist today. This is a real new edge, not a formality.
   - Move the construction + `c.Start(ctx)` into grpcSub (Wave A: its `Start`;
     Wave B: construction in `Prepare`, `c.Start()` in `Activate` — 07-11 applies
     the split, it does not re-litigate ownership).
   - **Move the stop into `grpcSubsystem.Stop`**, deleting the ad-hoc `defer` at
     `core.go:982-989`. This is the actual win: the Coordinator gains
     orchestrator-ordered shutdown and 07-10's deadline handling instead of a
     `defer` that only fires on the normal-exit path.
   - ⚠️ **Preserve the degraded mode EXACTLY.** Today a construction failure
     (`:976-978`) and a start failure (`:977-980`) both **WARN and continue** —
     the server boots with cluster fan-out unavailable and Rekey surfaces
     `INVALIDATION_NO_LIVE_MEMBERS`. `StartAll` aborts the boot on any error, so
     these paths MUST stay non-fatal: WARN and return nil. Do NOT let this
     ride-along turn a degraded boot into a failed boot.
   - ⚠️ **Keep the `dek.CacheAccessor` fail-closed check** (`core.go:959-965`,
     `CRYPTO_DEK_MANAGER_CACHE_ACCESSOR_NOT_SATISFIED`) fatal — it is deliberately
     fail-closed and is NOT part of the degraded path.
   - The subsystem count is **unchanged at 17** — the Coordinator does not become
     an 18th subsystem. Task 3's count and 07-11's 17-row census both hold.

5. **The bootstrap orphan boot gate — a FILE MOVE across a package boundary, not
   a call-site edit. SETTLED HERE; the executor applies it.**

   Deleting the DB pre-start (item 2) makes `core.go:296` — the **very next
   line** — panic, because `dbSub.Pool()` panics before DB start
   (`internal/store/subsystem.go:89-111`). Design table row `:296` moves the gate
   into `setup.BootstrapSubsystem.Start`.

   ⚠️ **`:296` is the CALL SITE, not the symbol.** Verified live:
   `runBootstrapOrphanCheck` is **DEFINED at `cmd/holomush/bootstrap_orphan.go:34`**
   (`package main`), with **4 call sites in `cmd/holomush/bootstrap_orphan_test.go`**
   (`:43`, `:69`, `:98`, `:121`). An earlier revision named neither file, and its
   frontmatter note cited `core.go:296` as though that were the symbol's home
   (plan-checker W-1). `package main` cannot reference a symbol that now lives in
   `internal/bootstrap/setup`, **so the tests move with the definition** — this is
   settled below, not an executor judgement call.

   ⚠️ **A DUPLICATE IS FORBIDDEN — this is the failure mode that matters.** Both
   greps in the acceptance criteria go green if you *define a second copy* in
   `internal/bootstrap/setup/subsystem.go` and leave `bootstrap_orphan.go` in
   place: `core.go` is clean, the new file has the symbol, and you have shipped
   dead duplicated boot-gate logic in `package main` plus 4 tests still exercising
   the dead copy. The `rg -c 'runBootstrapOrphanCheck' cmd/holomush/` **= 0**
   criterion exists specifically to fail that outcome. **Move; do not copy.**

   Concretely:
   - **`cmd/holomush/bootstrap_orphan.go` is DELETED outright.** It contains
     exactly one func (verified) — there is no residue to keep. Its body + doc
     comment move **verbatim** into `internal/bootstrap/setup/subsystem.go`,
     keeping the name `runBootstrapOrphanCheck` (unexported — only the subsystem
     calls it; do not widen the package API). Its `internal/eventbus` import
     (`bootstrap_orphan.go:13`, for `eventbus.ActorKindPlugin`) travels with it.
   - **Call it first in `BootstrapSubsystem.Start`**, against the pool the method
     already resolves: `Start` (`subsystem.go:123`) opens with
     `pool := s.cfg.DB.Pool()` (`:124`). ⚠️ **Add no `PoolProvider`** — the
     subsystem already has one (`subsystem.go:34-35`), and the
     `Bootstrap → Database` edge already exists (`:104-106`). Return the error
     unchanged: it stays a **refusal**.
   - **`cmd/holomush/bootstrap_orphan_test.go` is DELETED and reborn as
     `internal/bootstrap/setup/orphan_check_internal_test.go`**, carrying all 4
     tests verbatim (`TestBootstrapPassesWithCleanEventsAudit`,
     `TestBootstrapFailsWithSyntheticOrphan`,
     `TestBootstrapFailsWithOrphanInUnpartitioned`,
     `TestBootstrapPassesWhenUnpartitionedAbsent`) plus its local
     `applyAllMigrations` helper (`:25`, used only by these 4 — verified, so
     nothing else breaks).
     - **`//go:build integration`** (`:1` today) — **keep the tag**. These are
       testcontainer tests; they do not run under `task test`.
     - **`package setup`**, NOT `package setup_test`. `runBootstrapOrphanCheck`
       stays unexported, and the existing `internal/bootstrap/setup/subsystem_test.go`
       is `package setup_test` — an external test package cannot reach the symbol.
       Go permits both in one directory. Precedent: `internal/cluster/registry_internal_test.go:4`
       (`package cluster`), hence the `_internal_test.go` filename.
     - ⚠️ **The container helper does not travel.** The tests call
       `startPostgresContainer(t)`, which lives in
       `cmd/holomush/automigrate_integration_test.go` (`package main`) and is
       therefore unreachable from `internal/`. Compose a postgres testcontainer +
       `store.NewMigrator` inline in the new file, following
       `internal/store/testhelpers_test.go:31-72` (`freshMigratedPool` /
       `runMigrations` — `package store_test`, so it is a **shape to copy, not a
       helper to import**). `internal/bootstrap/setup/subsystem.go:28` already
       imports `internal/store`, so `store.NewMigrator` is reachable with no cycle.
     - ⚠️ **Do NOT delete `startPostgresContainer`.** Two other users remain in
       `cmd/holomush` (`automigrate_integration_test.go` defines it;
       `cmd_audit_dlq_replay_integration_test.go` uses it). Removing
       `bootstrap_orphan_test.go` does not orphan it.
   - `task build` does not catch a stranded test file (`_test.go` is not built by
     `build`), and `task test` does not compile integration-tagged files. **The
     gate that proves the tests followed the definition is
     `task test:int -- ./internal/bootstrap/setup/`.**

Then delete every remaining `*.Start(ctx)` in `cmd/holomush/core.go` outside
`orch.StartAll` (`:1100`-ish). The acceptance criterion below is mechanical.

⚠️ **`task fmt` mutates aligned blocks** and `core.go` is heavily edited here —
run it and commit the mutation.
⚠️ Use `slog.*Context` variants; bootstrap code is the classic bare-`slog`
offender and `sloglint context: scope` is enforced. `core.go:1453`'s bare
`slog.Warn` in a no-ctx helper is legitimate and may stay.
  </action>
  <acceptance_criteria>
    - `rg -c '\.Start\(ctx\)|\.Start\(context\.' cmd/holomush/core.go` returns 0 except the single `orch.StartAll(ctx)` call — verify by reading the matches; zero subsystem `Start` calls remain
    - `rg -c 'eventBusOwnedByOrchestrator' --type go` returns 0 matches repo-wide
    - `rg -c 'called before Start\(\)' cmd/holomush/core.go` returns 0 (no caller-side pre-start rationale remains)
    - `rg -c 'called before Start\(\)' internal/plugin/setup/subsystem.go internal/access/setup/subsystem.go` returns at least 1 (the accessor panic guards are PRESERVED)
    - `rg -n 'ConnProvider' internal/cluster/registry.go` prints at least one line
    - `rg -c 'lifecycle.SubsystemEventBus' internal/cluster/registry.go` returns 1 (the cluster→eventbus edge is real)
    - The nil-`Conn` check left `NewSubsystem` and landed in `Start`: `rg -c 'isNilConn' internal/cluster/registry.go` returns 1 (the helper stays) **and** `rg -c 'isNilConn' internal/cluster/heartbeat.go` returns 1 (the call moved to where `Start` actually is)
    - `task build` exits 0 — this is the real gate on the two harness callers (`clustertest/harness.go:115`, `clustertest/external.go:77`); a missed `Deps{Conn: …}` literal is a compile error, not a grep miss
    - `task test -- ./internal/cluster/ ./internal/cluster/clustertest/ ./cmd/holomush/` exits 0
    - `task test:int` exits 0
    - **The KEK-wired boot regression passes:** `task test:int -- -run 'TestAdminAuthenticate|AdminRekey' ./cmd/holomush/` exits 0. `cmd/holomush/admin_authenticate_e2e_test.go:215-426` and `admin_rekey_e2e_test.go` boot the full server through `runCoreWithDeps` **with a KEK wired** (`:265` — "the same KEK fingerprint runCoreWithDeps will load at boot"). This is the only place the boot panic reproduces: `cryptoActiveFor(cfg) = cfg.RekeyManager != nil` (`sub_grpc.go:184`), so without a KEK the regression is untested.
      ⚠️ **`task test:int`, NOT `task test` — this criterion was previously a FALSE GREEN** (cross-AI review; verified live). `admin_authenticate_e2e_test.go:4` carries `//go:build integration`, and `task test` (`Taskfile.yaml:85-89`) runs `gotestsum -- -race -count=1 ./...` with **no integration tag** — so `task test -- -run 'TestAdminAuthenticate…'` never compiles the file, matches zero tests, and **exits 0 while proving nothing**. A `-run` filter that matches nothing is indistinguishable from a pass. The same correction applies to the `<verification>` block below and to 07-11's identical criterion.
    - **Step 0's verification pass is in the SUMMARY** — rows matched / rows drifted / rows unaccounted against `<settlements>`'s design table. Any unaccounted site was **reported, not improvised**; no disposition was authored during execution.
    - **The `cryptoWiring` hoist is real, not a rename:** `rg -n 'sync.Once' cmd/holomush/core.go` prints at least one line, and `rg -c 'dbSub\.Pool\(\)|authSub\.Hasher\(\)|authSub\.AuthService\(\)|abacSub\.Resolver\(\)|eventBusSub\.Conn\(\)|eventBusSub\.Publisher\(\)' cmd/holomush/core.go` returns **only** matches that are lexically **inside** the memoized builder or inside a subsystem's `Start` — verified by reading. Zero such reads execute on `runCore`'s straight-line path.
    - **THE RULE is pinned by a test:** every subsystem whose config holds a `cryptoWiring` provider declares `DependsOn` ⊇ `{SubsystemDatabase, SubsystemAuth, SubsystemABAC, SubsystemEventBus}`. A test asserts the superset relation for each wiring consumer (`policy.CryptoPolicySubsystem`, `dek.CheckpointSweepSubsystem`, `chain.VerifierSubsystem`, `socket.AdminSocketSubsystem`, `grpcSubsystem`). This is the design's one sharp edge — the first consumer to resolve the provider builds it, so a missing edge is a boot panic.
    - `rg -c 'HandlersProvider|func\(\) \[\]chain.Handler' internal/eventbus/audit/chain/verifier_subsystem.go` returns at least 1 — `chainHandlers` (`core.go:1046-1052`) depends on `readStreamW.Handler` → `rekeyW.Manager` → the pool, so `Handlers` cannot be a resolved slice at construction
    - The three fatal paths still abort the boot from inside a subsystem `Start`: `rg -c 'BOOT_KEK_REQUIRED|CRYPTO_DEK_MANAGER_CACHE_ACCESSOR_NOT_SATISFIED' cmd/holomush/` returns at least 2 (relocated into the builder, not deleted)
    - `rg -c 'runBootstrapOrphanCheck' internal/bootstrap/setup/subsystem.go` returns at least 1 and `rg -c 'runBootstrapOrphanCheck' cmd/holomush/core.go` returns 0 — the boot gate moved behind the `Bootstrap → Database` edge and still **refuses** (returns the error), it was not dropped or demoted to post-serving
    - **`rg -c 'runBootstrapOrphanCheck' cmd/holomush/` returns 0 across the WHOLE directory** (not just `core.go`), and `ls cmd/holomush/bootstrap_orphan.go cmd/holomush/bootstrap_orphan_test.go` exits non-zero — both files are **gone**. ⚠️ This criterion is the one that forces a **move** instead of a **duplicate**: the definition lives at `cmd/holomush/bootstrap_orphan.go:34` with 4 test call sites, so defining a second copy under `internal/bootstrap/setup/` while leaving those in place satisfies the two criteria above while shipping dead duplicated boot-gate logic in `package main`.
    - The 4 moved tests live in `internal/bootstrap/setup/orphan_check_internal_test.go` with `//go:build integration` and `package setup`, and **`task test:int -- ./internal/bootstrap/setup/` exits 0** — `task build` cannot catch a stranded test file and `task test` does not compile integration-tagged files, so this is the only gate proving the tests followed the definition across the package boundary
    - `rg -c 'startPostgresContainer' cmd/holomush/` returns at least 2 — the shared container helper was NOT collaterally deleted (`automigrate_integration_test.go` defines it; `cmd_audit_dlq_replay_integration_test.go` uses it)
    - `rg -c 'VerifyAccountScoping' internal/eventbus/subsystem.go` returns 1 and `rg -c 'VerifyAccountScoping' cmd/holomush/core.go` returns 0 (design table row `:475` — the check moved into the package that owns the connection)
    - `rg -c 'lifecycle.SubsystemCluster' cmd/holomush/sub_grpc.go` returns at least 1 (the new grpcSub→cluster edge the Coordinator needs)
    - `rg -c 'Coordinator is not orchestrator-owned' cmd/holomush/core.go` returns 0 (the ad-hoc stop `defer` is gone; grpcSub.Stop owns it)
    - `rg -c 'invalidation.New\(' cmd/holomush/core.go` returns 0 (construction moved into grpcSub)
    - A test asserts the Coordinator's **degraded mode survives**: a construction/start failure WARNs and the boot still succeeds (it MUST NOT become fatal under `StartAll`)
    - `git status --porcelain` is clean after `task fmt`
  </acceptance_criteria>
  <verify>
    <automated>task build && task test && task test:int</automated>
  </verify>
  <done>All five eager starts are gone, the ownership flag is gone, every fix is an existing `DependsOn` edge, the accessor panic guards survive, and the full server boots with a KEK configured.</done>
</task>

<task type="execute">
  <name>Task 3: LOW-8 — replace productionSubsystems' 16 positional params</name>
  <read_first>
    - cmd/holomush/core.go:1459-1492 (productionSubsystems — the 16 same-typed positional params and the returned slice with its ordering comments)
    - cmd/holomush/core_subsystems_test.go (read in full — allStubs() [16]stubSubsystem at :26-52 and every positional spread; the count cascade lives here)
    - cmd/holomush/admin_authenticate_e2e_test.go:210-220 (the stale "full 14-subsystem server" comment)
    - internal/lifecycle/orchestrator.go:32-39 (Register — panics on duplicate)
  </read_first>
  <files>cmd/holomush/core.go, cmd/holomush/core_subsystems_test.go, cmd/holomush/admin_authenticate_e2e_test.go</files>
  <action>
`productionSubsystems` (`core.go:1462-1472`) takes **16** same-typed positional
`lifecycle.Subsystem` params; a mis-order compiles silently, defused today only
because `topoSort` re-derives the real order. It has already grown 12→16, and
Task 1 makes it 17.

Replace the positional list with a **named struct** — e.g.
`productionSubsystems(s productionSubsystemSet) []lifecycle.Subsystem` where
`productionSubsystemSet` has one named `lifecycle.Subsystem` field per subsystem.
This makes a mis-order impossible to express rather than merely harmless, and it
is the shape that scales as the set keeps growing. (D-14 offers "per-subsystem
`Register` calls **or** a named struct"; the struct is chosen because
`productionSubsystems` is called by tests that assert on the returned set
without an orchestrator, and per-subsystem `Register` would push that assertion
surface into `orch`.)

Add TLS to the set with the other subsystems.

**The count cascade is concrete and hard-coded — fix every site:**
- `core_subsystems_test.go:26-52` — `allStubs() [16]stubSubsystem` → `[17]`, and
  its index comments ("Index 14 (SubsystemRekeyCheckpointSweep) …", "Index 15
  (SubsystemOutboxRelay) …") gain a TLS row.
- Every call site that spreads `s[0]…s[15]` positionally (e.g. `:57-61`) becomes
  a struct literal.
- The seven tests in `core_subsystems_test.go` that assert on the set/count —
  including `TestProductionSubsystemsIncludesCluster` (a count assertion),
  `TestProductionSubsystemsIncludesAdminSocket`, and
  `TestSubsystemAdminSocketConstantExists` — must be updated for 17 and gain a
  `TestProductionSubsystemsIncludesTLS`.
- `cmd/holomush/admin_authenticate_e2e_test.go:215` says "boots the full
  **14-subsystem** server" — already stale at 16; make it accurate.

⚠️ `stubSubsystem` in `core_subsystems_test.go:22` returns `nil` from
`DependsOn()`. That is fine for a set/count assertion but **cannot** be reused
for 07-10's topo-order pin test — do not "helpfully" repurpose it here.
`internal/lifecycle/orchestrator_test.go:29`'s stub (which returns `s.deps`) is
the right analog for that; 07-10 owns it.

⚠️ `task fmt` mutates aligned struct blocks — the new
`productionSubsystemSet` is one. Run it and commit.
  </action>
  <acceptance_criteria>
    - `rg -c 'lifecycle.Subsystem,$' cmd/holomush/core.go` returns 0 (no positional param list remains)
    - `rg -n 'func productionSubsystems' cmd/holomush/core.go` shows a single non-variadic struct parameter
    - `rg -c '\[17\]stubSubsystem' cmd/holomush/core_subsystems_test.go` returns 1
    - `rg -c '14-subsystem' cmd/holomush/` returns 0 matches
    - `rg -c 'TestProductionSubsystemsIncludesTLS' cmd/holomush/core_subsystems_test.go` returns 1
    - `task test -- ./cmd/holomush/` exits 0 (all seven set/count tests updated and green)
    - `task build` exits 0
    - `git status --porcelain` is clean after `task fmt`
  </acceptance_criteria>
  <verify>
    <automated>task build && task test -- ./cmd/holomush/ && task lint</automated>
  </verify>
  <done>Subsystem registration is by name, not position; the set is 17 including TLS; every count assertion and the stale 14-subsystem comment are corrected.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
| Artifact | Kind |
|---|---|
| `internal/tls/subsystem.go`, `internal/tls/subsystem_test.go` | new files |
| `tls.TLSSubsystem`, `tls.TLSSubsystemConfig`, `tls.NewTLSSubsystem` | new types/func |
| `tls.TLSSubsystem.TLSConfig()` | new accessor (panic-before-`Start` guard) — required by `grpcSubsystem`'s provider |
| `tls.EnsureCerts` | new exported func (relocated from `cmd/holomush`'s `ensureTLSCerts`; same signature, so `Deps.TLSCertEnsurer`'s field type is unchanged) |
| `tls.GameIDProvider` | new consumer-defined interface |
| `cluster.ConnProvider` (name per the executor's reading of `Deps.Conn`) | new consumer-defined interface |
| `cryptoWiring` + its memoized `sync.Once` builder | new struct/func in `package main` — the hoisted `core.go:705-1060` block; **NOT a `lifecycle.Subsystem`** (no `ID()`/`DependsOn()`, never registered — the count stays 17) |
| `productionSubsystemSet` | new struct in `package main` |
| `TestProductionSubsystemsIncludesTLS` | new test |
| A test asserting every `cryptoWiring` consumer's `DependsOn` ⊇ the wiring's dep set | new test (THE RULE's mechanical guard) |

| `internal/bootstrap/setup/orphan_check_internal_test.go` | new file (`//go:build integration`, `package setup`) — the 4 orphan-gate tests relocated verbatim from `cmd/holomush/bootstrap_orphan_test.go`, with their `applyAllMigrations` helper and an inline postgres testcontainer (`startPostgresContainer` is `package main` and does not travel) |

Deleted: the five eager `Start` calls, `eventBusOwnedByOrchestrator`,
`ensureTLSCerts` in `cmd/holomush/core.go` (its body survives as
`tls.EnsureCerts`; `deps.go:71` repoints to it — the test seam is preserved),
`productionSubsystems`' 16 positional parameters, and **both
`cmd/holomush/bootstrap_orphan.go` and `cmd/holomush/bootstrap_orphan_test.go`**
(whole-file **moves**, not copies: `runBootstrapOrphanCheck`'s body relocates into
`internal/bootstrap/setup/subsystem.go` and its 4 tests into
`orphan_check_internal_test.go` — a duplicate left behind in `package main` is
the explicitly-forbidden outcome, Task 2 item 5).
Not deleted: `startPostgresContainer` (`cmd/holomush/automigrate_integration_test.go`)
— two other users remain.
No `SubsystemID` const-block edit; no `task generate`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process boot → serving | Fail-closed posture: a failed subsystem MUST abort the boot, not degrade it |
| TLS CA generation → gameID | The gameID is embedded in the CA certificate on first boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-44 | Denial of Service | Boot panic → crash-loop under a production KEK | high | mitigate | The documented incident (`core.go:790-796`). Task 2's criteria require the KEK-wired full-boot E2Es (`admin_authenticate_e2e_test.go`, `admin_rekey_e2e_test.go`) to pass — the only shape where `cryptoActiveFor` is true and the panic reproduces. |
| T-07-45 | Tampering | TLS CA regenerated with the wrong/absent gameID | high | mitigate | The gameID edge becomes a real `DependsOn(SubsystemDatabase)` rather than a hand-sequenced pre-start; `ensureTLSCerts`' body moves verbatim (same paths, permissions, regeneration conditions). |
| T-07-46 | Elevation of Privilege | A subsystem serving with an unresolved ABAC resolver or password hasher | high | mitigate | The accessor panic guards are explicitly PRESERVED (asserted by a grep criterion) so an unresolved provider fails loudly at Start rather than fails open. The full acquire-before-serve guarantee lands in 07-11 (D-11). |
| T-07-47 | Tampering | Silent mis-ordering via 16 same-typed positional args | medium | mitigate | Task 3's named struct makes a mis-order inexpressible. `topoSort` already re-derived the real order, so the risk was latent, not live — hence `medium`, not `high`. |
| T-07-48 | Spoofing | A second ordering authority (latch) diverging from the graph | medium | mitigate | D-10 — no latch. `topoSort` stays sole authority; `ReadinessRegistry` is reused if a gate is ever wanted. |
| T-07-49 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — zero external packages installed (RESEARCH.md § Package Legitimacy Audit). |
</threat_model>

<verification>
- `task build` exits 0
- `task test` exits 0
- `task test:int` exits 0
- `task lint` exits 0
- `rg -c 'eventBusOwnedByOrchestrator' --type go` returns 0
- The KEK-wired full-boot E2Es pass (`task test:int -- -run 'TestAdminAuthenticate|AdminRekey' ./cmd/holomush/` — **`test:int`, not `test`**: these files are `//go:build integration` and `task test` would skip-and-pass them)
- `git diff --exit-code internal/lifecycle/subsystemid_string.go` exits 0
</verification>

<success_criteria>
Wave A is provable alone: the process boots with a KEK configured, zero
subsystem `Start` calls exist outside `orch.StartAll`, `StartAll` owns ordering,
TLS is a real subsystem on a real edge, and registration is by name. This
remains a complete win even if Wave B is deferred (D-12).
</success_criteria>

<output>
Create `.planning/phases/07-event-model-bootstrap-decomposition/07-09-SUMMARY.md` when done.
</output>
