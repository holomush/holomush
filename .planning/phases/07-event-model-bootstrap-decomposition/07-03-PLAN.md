---
phase: 07-event-model-bootstrap-decomposition
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - internal/ulidgen/ulidgen.go
  - internal/ulidgen/ulidgen_test.go
  - internal/cmdparse/cmdparse.go
  - internal/cmdparse/cmdparse_test.go
  - internal/sessionlease/sessionlease.go
  - internal/sessionlease/sessionlease_test.go
  - internal/core/ulid.go
  - internal/core/ulid_test.go
  - internal/core/command.go
  - internal/core/command_test.go
  - internal/session/reaper.go
  - internal/telnet/gateway_handler.go
  - internal/telnet/limits.go
  - internal/web/handler.go
  - CLAUDE.md
autonomous: true
requirements: [ARCH-05]

must_haves:
  truths:
    - "internal/telnet and internal/web import no internal package that is not a dependency-free leaf"
    - "This plan is where the gateway's internal/core and internal/session imports actually die — go list -deps ./internal/telnet and ./internal/web contain neither after Task 3 (07-02 deliberately does NOT assert this; it moves vocabulary only)"
    - "The lease refresh interval is still exactly 15s and still has exactly one definition"
    - "ULID monotonicity is unchanged — one shared entropy source, one clamp"
    - "internal/naming stays in internal/telnet (confirmed leaf, D-16)"
  artifacts:
    - internal/ulidgen/ulidgen.go
    - internal/cmdparse/cmdparse.go
    - internal/sessionlease/sessionlease.go
  key_links:
    - "core.NewULID forwards to ulidgen.New — one entropy source, zero call-site churn (D-03's rejection of moving NewULID out of core is honored)"
    - "session.DefaultLeaseRefreshInterval forwards to sessionlease.DefaultRefreshInterval — one definition"
    - "internal/telnet/gateway_handler.go:406 calls cmdparse.ParseCommand"
---

<objective>
Close D-16's three remaining gateway leaks by moving each symbol the gateway
legitimately needs into a dependency-free leaf: the ULID generator, the command
grammar, and the session-lease refresh interval.

Purpose: after this plan, `internal/web` and `internal/telnet` import **only**
leaves, so 07-04 can add `internal/core`, `internal/session`, and `internal/grpc`
to `forbidden` wholesale with no escape hatch (D-15).

Output: `internal/ulidgen`, `internal/cmdparse`, `internal/sessionlease`.
</objective>

<drift_correction>
**Do NOT restate D-16's stated rationale for `internal/session`.** D-16 says
*"`internal/session` **is** the DB-reaching session store — the dangerous import
the tripwire would miss."* This is **false**, verified live:
`go list -deps ./internal/session` returns `internal/session` and nothing else —
it is **already a dependency-free leaf**. Its DB-reaching code lives in
`internal/session/setup`, not its root. `internal/core` is likewise already a
leaf.

The **decision** stands and is defensible — D-15 forbids them wholesale to kill
the per-symbol-allow-list escape hatch, which is how `cmd/holomush`'s
`coreOnlyFiles` grew to ~30 judgement calls nobody re-checks. The rationale is
**drift-prevention**, not "reaches the DB". Only `internal/grpc` (41-package
closure reaching world/access/command/store — handled in 07-01) matched the
stated rationale.

Write the drift-prevention rationale in code comments and the SUMMARY; do not
repeat the DB claim.

**D-16's `naming.Theme` question is answered: `internal/naming` IS a true leaf.**
Verified: `go list -deps ./internal/naming` returns only itself. Per D-15's
leaf-only principle `naming.Theme` (`internal/telnet/guest_auth.go:21,28`) MAY
stay. **This plan makes no change to `internal/naming` or `guest_auth.go`.**
</drift_correction>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-event-model-bootstrap-decomposition/07-CONTEXT.md
@.planning/phases/07-event-model-bootstrap-decomposition/07-PATTERNS.md
@.claude/rules/gateway-boundary.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: internal/ulidgen — move the generator, keep core.NewULID as the forwarder</name>
  <read_first>
    - internal/core/ulid.go (read in full — every symbol and the load-bearing entropy/clamp doc comments)
    - internal/core/ulid_test.go (the holomush-nri6e monotonicity tests move with the generator)
    - internal/telnet/gateway_handler.go:665-675 (connectionID generation)
    - internal/web/handler.go:240-250 (connID generation)
  </read_first>
  <files>internal/ulidgen/ulidgen.go, internal/ulidgen/ulidgen_test.go, internal/core/ulid.go, internal/core/ulid_test.go, internal/telnet/gateway_handler.go, internal/web/handler.go, CLAUDE.md</files>
  <action>
Create package `ulidgen` at `internal/ulidgen/ulidgen.go` holding the generator
machinery moved **verbatim** from `internal/core/ulid.go`:

- The coupled `entropy` / `entropyLock` / `lastMs` var block **and its
  four-line "one coupled unit" doc comment verbatim** — that comment is the
  contract for the clamp's monotonicity guarantee.
- `func New() ulid.ULID` — the body of today's `core.NewULID` unchanged.
- `func generateULID(r io.Reader, last *uint64, ms uint64) ulid.ULID` — moved
  verbatim **including its entire holomush-nri6e rationale comment**. That
  comment explains why the clamp exists and why we do not spin-wait; losing it
  loses the fix's only explanation.
- `func Parse(s string) (ulid.ULID, error)` — the body of today's
  `core.ParseULID` unchanged.

`New`'s doc comment: carry the "Use this for: event IDs, session IDs, and any
identifier whose lexicographic order MUST match arrival order" sentence. Two
sentences in the current doc are **stale and MUST NOT be carried**:
- *"The PostgresEventStore relies on this property — Replay uses `WHERE id > afterID ORDER BY id` …"* — `PostgresEventStore` was deleted by the F1–F7 JetStream cutover; it does not exist.
- *"core.Event{} struct literals must use core.NewEvent() …"* — 07-07 deletes `core.NewEvent`.
Replace both with the live rationale: monotonic-within-millisecond ordering, and
`Nats-Msg-Id` dedup identity per `.claude/rules/event-conventions.md`.

Move `internal/core/ulid_test.go` to `internal/ulidgen/ulidgen_test.go`
(`package core` → `package ulidgen`; the `generateULID` tests are in-package and
must stay in-package).

**The moved test's rationale comment is stale too — fix it in the move (cross-AI
round 5, LOW).** `TestNewULIDRemainsStrictlyMonotonicUnderRapidSuccessiveCalls`'s
comment (`internal/core/ulid_test.go:36-44`) justifies monotonicity by naming two
downstream invariants: (1) `PostgresEventStore.Replay` ordering by event ULID —
contradicting the settled JetStream-sequence ordering model
(`.claude/rules/event-conventions.md` § "Event identity vs ordering"; the store
was deleted by the F1–F7 cutover), and (2) `PostgresSessionStore.UpdateCursors` —
verify with `rg -ln 'PostgresSessionStore'` / `rg -n 'UpdateCursors' internal/store/`;
if retired, it goes too. Replace the stale rationale with the same live rationale
this task writes into `New`'s doc (monotonic-within-millisecond ordering +
`Nats-Msg-Id` dedup identity). **Keep the strict-monotonicity assertion body —
including the deliberate string-comparison note — byte-identical**; only the
"why" prose changes.

Rewrite `internal/core/ulid.go` to hold **only two documented forwarders**:
`func NewULID() ulid.ULID { return ulidgen.New() }` and
`func ParseULID(s string) (ulid.ULID, error) { return ulidgen.Parse(s) }`.
Doc each: the generator's single home is `internal/ulidgen`; `core.NewULID` is
retained as the canonical core-side spelling (CLAUDE.md § ULID Generation) so
the ~68 existing call sites and the `internal/eventbus` → `internal/core` import
edge are unchanged. **This is deliberate:** CONTEXT.md D-03 rejects inverting
that dep, so the forwarder — not a repo-wide rename — is the shape that honors
it. There is exactly one entropy source; the forwarder adds no second generator.

Repoint the two gateway call sites to `ulidgen.New()`:
`internal/telnet/gateway_handler.go:670` (`h.connectionID = ulidgen.New().String()`)
and `internal/web/handler.go:245` (`connID := ulidgen.New()`).

Amend `CLAUDE.md` § "ULID Generation": add one row/note stating the generator
lives in `internal/ulidgen`; `core.NewULID()` remains the spelling for core-side
event/session IDs; gateway-side code (`internal/web`, `internal/telnet`) calls
`ulidgen.New()` directly because INV-EVENTBUS-1 forbids importing
`internal/core`. Do **not** touch the `core.NewEvent()` sentences in that
section — 07-07 owns those.
  </action>
  <acceptance_criteria>
    - `go list -deps ./internal/ulidgen | rg -c 'holomush/'` returns 1 (itself only)
    - `rg -c 'ulid.Monotonic' internal/core/ulid.go` returns 0 and `rg -c 'ulid.Monotonic' internal/ulidgen/ulidgen.go` returns 1 (exactly one entropy source)
    - `rg -c 'PostgresEventStore' internal/ulidgen/ulidgen.go` returns 0 (stale doc not carried)
    - `rg -c 'PostgresEventStore' internal/ulidgen/ulidgen_test.go` returns 0 (the moved test's stale replay rationale is corrected, not carried — cross-AI round 5), while `rg -c 'func TestNewULIDRemainsStrictlyMonotonicUnderRapidSuccessiveCalls' internal/ulidgen/ulidgen_test.go` returns 1 (the assertion itself survives)
    - `rg -n 'func NewULID\(\) ulid.ULID' internal/core/ulid.go` prints one line (forwarder retained)
    - `rg -c 'ulidgen.New\(\)' internal/telnet/gateway_handler.go internal/web/handler.go` returns 2 matches total
    - `task test -- ./internal/ulidgen/ ./internal/core/` exits 0 (the nri6e monotonicity tests pass in their new home)
  </acceptance_criteria>
  <verify>
    <automated>task test -- ./internal/ulidgen/ ./internal/core/ ./internal/telnet/ ./internal/web/</automated>
  </verify>
  <done>`internal/ulidgen` owns the single entropy source; `core.NewULID` forwards; both gateways generate IDs from a leaf; the holomush-nri6e monotonicity tests pass in the new package.</done>
</task>

<task type="execute">
  <name>Task 2: internal/cmdparse — move the command grammar</name>
  <read_first>
    - internal/core/command.go (read in full — 21 lines, `ParseCommand` only)
    - internal/core/command_test.go
    - internal/telnet/gateway_handler.go:400-412 (the only external consumer)
  </read_first>
  <files>internal/cmdparse/cmdparse.go, internal/cmdparse/cmdparse_test.go, internal/core/command.go, internal/core/command_test.go, internal/telnet/gateway_handler.go</files>
  <action>
Create package `cmdparse` at `internal/cmdparse/cmdparse.go` holding
`func ParseCommand(input string) (cmd, arg string)` moved **verbatim** from
`internal/core/command.go:9-21`. The grammar MUST be byte-identical: trim,
empty→`("","")`, `strings.SplitN(input, " ", 2)`, `strings.ToLower` on the verb,
trim on the argument. Its only import is `strings`.

Move `internal/core/command_test.go` to `internal/cmdparse/cmdparse_test.go`
(adjust package + qualifier). Add a case, if not already present, pinning that
the verb is lowercased and the argument's inner spacing is preserved (e.g.
`"SAY  hello  world"` → `("say", "hello  world")`) — the grammar is V5 input
validation for the telnet surface and a subtle change here changes what players
can type.

Delete `internal/core/command.go` and `internal/core/command_test.go`. There is
no forwarder: `ParseCommand` has exactly one external consumer
(`internal/telnet/gateway_handler.go:406`, verified live via
`rg -l 'ParseCommand'` → `internal/telnet/gateway_handler.go`,
`internal/core/command.go`, `internal/core/command_test.go`), so a forwarder
would be pure drift.

Repoint `internal/telnet/gateway_handler.go:406` to
`cmd, arg := cmdparse.ParseCommand(line)`.
  </action>
  <acceptance_criteria>
    - `go list -deps ./internal/cmdparse | rg -c 'holomush/'` returns 1 (itself only)
    - `rg -c 'core\.ParseCommand' --type go` returns 0 matches repo-wide
    - `test -f internal/core/command.go` exits non-zero (deleted)
    - `rg -n 'cmdparse.ParseCommand' internal/telnet/gateway_handler.go` prints one line
    - `task test -- ./internal/cmdparse/ ./internal/telnet/` exits 0
  </acceptance_criteria>
  <verify>
    <automated>task test -- ./internal/cmdparse/ ./internal/telnet/</automated>
  </verify>
  <done>The command grammar lives in one leaf, is byte-identical, and telnet parses through it.</done>
</task>

<task type="execute">
  <name>Task 3: internal/sessionlease — move the lease refresh interval</name>
  <read_first>
    - internal/session/reaper.go:10-30 (the constant and its I-LIVE-4 doc comment)
    - internal/telnet/limits.go (the LeaseRefreshInterval default)
    - internal/web/handler.go:455-470 (the heartbeat interval default)
  </read_first>
  <files>internal/sessionlease/sessionlease.go, internal/sessionlease/sessionlease_test.go, internal/session/reaper.go, internal/telnet/limits.go, internal/web/handler.go</files>
  <action>
Create package `sessionlease` at `internal/sessionlease/sessionlease.go` holding
`const DefaultRefreshInterval = 15 * time.Second`, with the **entire existing
doc comment moved verbatim** from `internal/session/reaper.go` (the "source of
truth" / "floor the session reaper's LeaseTTL and BootGrace must clear" /
I-LIVE-4 / "parseSessionConfig rejects lease/grace below 2× this value"
paragraph). That comment is the constant's contract; losing it loses why 15s is
a floor and not a preference. Its only import is `time`.

⚠️ **The value MUST NOT change.** It is a live session-lease timing constant
(ASVS V3 Session Management). `parseSessionConfig` in `cmd/holomush` rejects
lease/grace below 2× it, so any drift silently re-gates config validation.

In `internal/session/reaper.go`, replace the const declaration with a forwarder:
`const DefaultLeaseRefreshInterval = sessionlease.DefaultRefreshInterval`, with
a one-line doc pointing at the leaf as the source of truth and noting the
gateway reads the leaf directly because INV-EVENTBUS-1 forbids
`internal/session`. Keeping the `session.DefaultLeaseRefreshInterval` spelling
avoids churning `internal/session`'s own callers and `cmd/holomush`'s
`parseSessionConfig`.

Repoint the two gateway sites to the leaf:
- `internal/telnet/limits.go:47` → `LeaseRefreshInterval: sessionlease.DefaultRefreshInterval`,
  and update the neighbouring comment at `:37` which names
  `session.DefaultLeaseRefreshInterval` — it must name the leaf.
- `internal/web/handler.go:462` → `hbInterval = sessionlease.DefaultRefreshInterval`.

Add `internal/sessionlease/sessionlease_test.go` asserting
`DefaultRefreshInterval == 15 * time.Second` — a value pin, since the whole
point of the move is that three packages now agree on one number.
  </action>
  <acceptance_criteria>
    - `go list -deps ./internal/sessionlease | rg -c 'holomush/'` returns 1 (itself only)
    - `rg -c '15 \* time.Second' internal/sessionlease/sessionlease.go` returns 1 and `rg -c 'DefaultLeaseRefreshInterval = 15 \* time.Second' internal/session/reaper.go` returns 0 (exactly one definition)
    - `rg -c '"github.com/holomush/holomush/internal/session"' internal/telnet/ internal/web/` returns 0 matches
    - `go list -deps ./internal/telnet | rg -c 'holomush/internal/session$'` returns 0
    - `go list -deps ./internal/web | rg -c 'holomush/internal/session$'` returns 0
    - `task test -- ./internal/sessionlease/ ./internal/session/ ./internal/telnet/ ./internal/web/ ./cmd/holomush/` exits 0
    - `task test:int` exits 0

    **⚠️ End-of-plan gate — this plan owns the full gateway ban (moved here from 07-02).**
    Task 3 is the last task in the phase's ARCH-05 leaf work, so the whole-gateway
    assertions land here. They were previously (and wrongly) demanded by 07-02, which
    cannot satisfy them: `core.ParseCommand` and `core.NewULID` do not leave
    `internal/core` until **this plan's Tasks 1-2**. Run these only after Tasks 1, 2 and 3
    are all applied:
    - `rg -c '"github.com/holomush/holomush/internal/core"' internal/telnet/ internal/web/` returns 0 matches
    - `go list -deps ./internal/telnet | rg -c 'holomush/internal/core$'` returns 0
    - `go list -deps ./internal/web | rg -c 'holomush/internal/core$'` returns 0
    - `go list -deps ./internal/telnet | rg -c 'holomush/internal/(core|session|grpc|world|access|command|store|plugin|eventbus|auth)$'` returns 0
    - `go list -deps ./internal/web | rg -c 'holomush/internal/(core|session|grpc|world|access|command|store|plugin|eventbus|auth)$'` returns 0
    - If any of the five is non-zero, name the surviving symbol in the SUMMARY and fix it here — 07-04's `forbidden` list and closure gate go red otherwise, and 07-04 has no code to change.
  </acceptance_criteria>
  <verify>
    <automated>task test -- ./internal/sessionlease/ ./internal/session/ ./internal/telnet/ ./internal/web/ ./cmd/holomush/ && task lint</automated>
  </verify>
  <done>The lease interval has one definition at 15s; neither gateway imports `internal/session` **or `internal/core`**; both gateway closures are leaf-only; `parseSessionConfig`'s 2× validation still passes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
| Artifact | Kind |
|---|---|
| `internal/ulidgen/` + `ulidgen.go` + `ulidgen_test.go` | new package/files |
| `ulidgen.New`, `ulidgen.Parse`, `ulidgen.generateULID` | funcs (relocated from `core.NewULID` / `core.ParseULID` / `core.generateULID`) |
| `internal/cmdparse/` + `cmdparse.go` + `cmdparse_test.go` | new package/files |
| `cmdparse.ParseCommand` | func (relocated from `core.ParseCommand`) |
| `internal/sessionlease/` + `sessionlease.go` + `sessionlease_test.go` | new package/files |
| `sessionlease.DefaultRefreshInterval` | const (relocated from `session.DefaultLeaseRefreshInterval`) |

Retained as documented forwarders (NOT duplicates): `core.NewULID`,
`core.ParseULID`, `session.DefaultLeaseRefreshInterval`.
Deleted: `internal/core/command.go`, `internal/core/command_test.go`,
`core.ParseCommand`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player terminal → telnet command parser | Untrusted input crosses here; `ParseCommand` is the grammar |
| gateway → session lease | The refresh interval gates whether a live session is reaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-10 | Tampering | `ParseCommand` grammar (ASVS V5 Input Validation) | high | mitigate | Moved verbatim; a changed split/trim/lowercase rule changes what a player can invoke and could smuggle a verb past dispatch authorization. Task 2 adds a lowercase + inner-spacing pin test and the existing `command_test.go` cases move with it. |
| T-07-11 | Denial of Service | `DefaultRefreshInterval` value drift (ASVS V3) | high | mitigate | The value is a floor: `parseSessionConfig` rejects lease/grace below 2× it, and a LeaseTTL at or below it lets a healthy session lapse between refreshes (I-LIVE-4). Task 3 pins `== 15 * time.Second` and asserts exactly one definition exists. |
| T-07-12 | Spoofing | ULID monotonicity / uniqueness | high | mitigate | `New` keeps `crypto/rand` (never `math/rand`, per CLAUDE.md) and the single shared `ulid.Monotonic` entropy + clamp. Task 1 asserts exactly one `ulid.Monotonic` site repo-wide and moves the holomush-nri6e monotonicity tests intact. A second entropy source would allow lex-inverted IDs within a millisecond, breaking `Nats-Msg-Id` dedup identity. |
| T-07-13 | Elevation of Privilege | Gateway retaining a non-leaf import | medium | mitigate | Tasks 1-3 each assert the gateway's `go list -deps` no longer contains the source package; 07-04 makes it permanent. |
| T-07-14 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — zero external packages installed (RESEARCH.md § Package Legitimacy Audit). |
</threat_model>

<verification>
- `task build` exits 0
- `task test` exits 0
- `task test:int` exits 0 (`parseSessionConfig` and the reaper are exercised there)
- `task lint` exits 0
- For each of `./internal/ulidgen`, `./internal/cmdparse`, `./internal/sessionlease`: `go list -deps <pkg> | rg -c 'holomush/'` returns 1
- `go list -deps ./internal/telnet | rg -c 'holomush/internal/(core|session|grpc|world|access|command|store|plugin|eventbus)$'` returns 0
- `go list -deps ./internal/web | rg -c 'holomush/internal/(core|session|grpc|world|access|command|store|plugin|eventbus)$'` returns 0
</verification>

<success_criteria>
Every symbol the gateway needs lives in a dependency-free leaf; `internal/web`
and `internal/telnet` import no `internal/core`, `internal/session`, or
`internal/grpc`, **and neither package's transitive closure contains any of
them**; `internal/naming` is confirmed a leaf and `naming.Theme` stays; the
lease interval, the command grammar, and ULID monotonicity are all unchanged.

**This plan is the ARCH-05 leaf-work convergence point.** 07-01 removed
`internal/grpc`; this plan removes `internal/core` and `internal/session`. 07-04
adds no code — it only makes the resulting state permanent (forbidden list +
closure gate + INV-EVENTBUS-1 binding). So if any of Task 3's end-of-plan gate
assertions is non-zero when this plan closes, **07-04 cannot go green**, and the
fix belongs here, not there.
</success_criteria>

<output>
Create `.planning/phases/07-event-model-bootstrap-decomposition/07-03-SUMMARY.md` when done.
</output>
