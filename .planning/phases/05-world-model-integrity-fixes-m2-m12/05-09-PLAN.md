---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 09
type: execute
wave: 8
depends_on: [05-08]
files_modified:
  - internal/world/outbox/taxonomy.go
  - internal/world/outbox/taxonomy_test.go
  - test/meta/world_sql_fence_test.go
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "A versioned taxonomy schema registry declares the ~15-20 world-change envelope kinds with per-type payload schemas and an App-Schema-Version (the ARCH-04 / Phase-7 input)."
    - "A real AST/token meta-test scans production Go source for world-table mutation SQL (INSERT/UPDATE/DELETE against locations/exits/characters/objects/entity_properties/scene_participants/outbox/world_feed_counter) and FAILS if any appears outside internal/world/postgres — NOT a depguard rule (depguard matches imported packages, not SQL literals; Codex finding 6). entity_properties is IN the set (round-2: property_repo.go:60 writes it; omitting it weakens INV-WORLD-4 and lets raw property writes escape the boundary)."
  artifacts:
    - path: internal/world/outbox/taxonomy.go
      provides: "versioned envelope-kind registry (~15-20 kinds, per-type payload schema, App-Schema-Version)"
      contains: "SchemaVersion|App-Schema-Version|schema_version"
    - path: test/meta/world_sql_fence_test.go
      provides: "AST/token meta-test scanning production Go for world-table mutation SQL outside internal/world/postgres"
      contains: "world"
  key_links:
    - from: test/meta/world_sql_fence_test.go
      to: internal/world/postgres
      via: "the meta-test walks production .go files and allowlists only internal/world/postgres for world-table write SQL"
      pattern: "internal/world/postgres"
  prohibitions:
    - "Raw world-table mutation SQL (writes to locations/exits/characters/objects/entity_properties/scene_participants/outbox/world_feed_counter) MUST NOT appear outside internal/world/postgres (the writer boundary)."
    - "The fence MUST NOT be a depguard rule — depguard matches imported packages, not SQL literals, so it cannot enforce this (Codex finding 6)."
---

<objective>
Build the versioned taxonomy schema registry (the declared-envelope-kind contract the rollout fills, and the designated ARCH-04 input) and the raw-world-SQL fence as a real AST/token meta-test. This is interface-first for the rollout: 05-10/05-11 wire each command to a declared kind; the census meta-test that asserts full coverage lands in 05-11 (after rollout) so no intermediate commit ships a failing census.

**Slice-3 ordering deviation (Codex finding 11 — made explicit).** The one-pager's normative sequence (`model-01-consensus-onepager.md:41`) is taxonomy → census → invariants BEFORE the mechanical rollout. These plans deliberately deviate to **data-first, enforcement-last**: the taxonomy + SQL fence land here (05-09, both pass immediately), the rollout lands in 05-10/05-11, and the coverage-asserting census + invariant binding land in 05-11/05-12 — AFTER coverage is complete. Rationale: a coverage census introduced before the rollout would leave `task test`/`task lint` red at every intermediate rollout commit (breaking `git bisect` and any concurrent lint run). This is a considered deviation from a locked sequencing detail, not a scope change — every enforcement artifact still lands in Phase 5; only its ordering relative to the rollout differs. Acknowledged here and in 05-10/05-12.

Purpose: The one-pager's static-enforcement layer is a versioned taxonomy + a census + a writer-boundary fence. The taxonomy defines the vocabulary; the fence forbids world-table writes leaking outside the repo boundary. depguard CANNOT implement this fence — it matches imported packages, not SQL literals (Codex finding 6) — so the fence is an AST/token meta-test that scans production Go for world-table mutation SQL.
Output: taxonomy registry, the AST/token raw-world-SQL fence meta-test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/event-conventions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Versioned taxonomy schema registry</name>
  <files>internal/world/outbox/taxonomy.go, internal/world/outbox/taxonomy_test.go</files>
  <read_first>
    - internal/world/wmodel/envelope.go (05-05 — the envelope's `kind` + `schema_version` fields the registry populates; the cycle-neutral leaf package)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md (World Write Command Census table — the ~15 commands → kinds; examine-fate Open Question 1; CreateCharacter Open Question 3)
    - docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md (Schema governance — versioned CI-validated registry, per-type payload schemas, App-Schema-Version, ARCH-04 input)
  </read_first>
  <behavior>
    - RED: looking up a declared envelope kind returns its per-type payload schema + schema_version; an undeclared kind is rejected (not silently accepted).
    - Every declared kind carries an App-Schema-Version.
    - The registry enumerates the world-change kinds (create/update/delete/move per aggregate + scene-participant add/remove + character genesis) — state-change kinds only; examine (read) is intentionally excluded.
  </behavior>
  <action>
    Define a versioned taxonomy registry in `internal/world/outbox/taxonomy.go`: a table mapping each world-change envelope `kind` to its per-type payload schema and a `schema_version`/`App-Schema-Version`. Declare the kinds for the state-change commands (location/exit/object/character create-update-delete-move as applicable, scene-participant add/remove, character genesis) — this is the ARCH-04/Phase-7 input, so keep the schemas self-describing and versioned. Resolve the open questions as concrete registry decisions: (1) examine commands are READS, not state changes — they get NO envelope kind (dropped from the world-change feed, per Open Question 1); (2) `CreateCharacter` (a world-state write in the gRPC registration flow, `internal/grpc/auth_handlers.go`) gets a character-genesis kind (Open Question 3). Provide a lookup that rejects an undeclared kind. RED test first: declared kind → schema + version; undeclared → rejected; every kind has an App-Schema-Version.
  </action>
  <verify>
    <automated>task test -- ./internal/world/outbox/</automated>
  </verify>
  <acceptance_criteria>
    - The registry declares the world-change kinds with per-type payload schemas + App-Schema-Version.
    - Examine kinds are absent (reads, not state changes).
    - A character-genesis kind exists for CreateCharacter.
    - Lookup rejects an undeclared kind; `task test -- ./internal/world/outbox/` passes.
  </acceptance_criteria>
  <done>The versioned envelope taxonomy exists as the rollout contract + ARCH-04 input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raw-world-SQL fence as an AST/token meta-test</name>
  <files>test/meta/world_sql_fence_test.go</files>
  <read_first>
    - test/meta/depguard_config_test.go (lines ~18-57 — a meta-test file-walk idiom to model the structure on; NOTE this one asserts config, but the world-SQL fence must inspect SOURCE, not config)
    - internal/world/postgres/location_repo.go, scene_repo.go (the write SQL that legitimately lives inside the allowlisted package — INSERT/UPDATE/DELETE against the world tables)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (Codex finding 6 — depguard matches imported packages, NOT SQL literals; the fence needs a real AST/token analyzer)
  </read_first>
  <behavior>
    - RED: given a synthetic fixture Go file OUTSIDE internal/world/postgres containing a string literal `INSERT INTO locations ...` (or UPDATE/DELETE against a world table), the meta-test FAILS; the same SQL inside internal/world/postgres passes (allowlisted).
    - The fence recognizes all world-owned tables: locations, exits, characters, objects, entity_properties, scene_participants, outbox, world_feed_counter.
    - Reads (SELECT) anywhere are allowed; only mutation SQL (INSERT/UPDATE/DELETE) is fenced.
  </behavior>
  <action>
    Add `test/meta/world_sql_fence_test.go` — a real fence, NOT a depguard rule (depguard matches imported packages, not SQL literals; Codex finding 6). Walk the production Go source tree (parse with `go/ast`/`go/parser`, or a token scan of string literals if simpler and robust), find string literals containing world-table mutation SQL — `INSERT INTO`, `UPDATE`, or `DELETE FROM` targeting any of `locations`, `exits`, `characters`, `objects`, `entity_properties`, `scene_participants`, `outbox`, `world_feed_counter` — and FAIL if any is found in a file OUTSIDE the allowlisted `internal/world/postgres` directory (skip `_test.go` and generated files). `entity_properties` is IN the set (round-2: property_repo.go:60/136 mutate it; without it a raw property write outside the repo package would not be caught). This is the writer-boundary enforcement (INV-WORLD-4 / WRITER-BOUNDARY, bound in 05-12). Include a positive in-fixture assertion (SQL inside the allowlist passes) and a negative fixture (SQL outside fails) so the fence itself is proven, not just asserted. Confirm the fence is green against the current tree (writes live only in `internal/world/postgres` after 05-05/05-14; if it flags an existing site that IS a real boundary violation to fix, not to exempt). Do NOT introduce the coverage-asserting census meta-test here — it lands in 05-11 after the rollout completes coverage. Keep any literal world-table/SQL tokens the test negative-checks out of comment bodies (comment-text discipline).
  </action>
  <verify>
    <automated>task test -- ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - `test/meta/world_sql_fence_test.go` scans production Go source (AST/token) and fails on world-table mutation SQL outside `internal/world/postgres`; a negative fixture proves it fails, a positive fixture proves the allowlist passes.
    - The fence covers all eight world-owned tables incl. entity_properties + scene_participants + outbox + world_feed_counter.
    - No depguard SQL rule is added to `.golangci.yaml`.
    - `task test -- ./test/meta/` passes against the current tree.
  </acceptance_criteria>
  <done>The writer-boundary is enforced by a real source-scanning meta-test, not an unenforceable depguard rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go code → world tables | A world-table write outside the repo boundary escapes the envelope/version guard entirely. |
| lint config → CI | A silently-dropped fence rule re-opens the boundary without a test failing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-27 | Tampering | raw world-SQL outside the repo | high | mitigate | The AST/token meta-test scans production Go for world-table mutation SQL and fails on any outside `internal/world/postgres`; a negative fixture proves the fence actually fires (depguard could not — it matches imported packages, not SQL literals; Codex finding 6). |
| T-05-28 | Tampering | taxonomy drift | medium | mitigate | The registry is versioned (App-Schema-Version) and CI-validated; the census (05-11) asserts every command maps to a declared kind — an unregistered kind is rejected at lookup. |
</threat_model>

<verification>
- `task test -- ./internal/world/outbox/ ./test/meta/` passes (run via `local-check`).
- The world-SQL fence meta-test fails on a negative fixture (world-table mutation SQL outside `internal/world/postgres`) and passes on the current tree.
- `grep -c 'world' test/meta/world_sql_fence_test.go` confirms the fence references the world tables (filter comments: `grep -v '^//' | grep -c`).
</verification>

<success_criteria>
- The versioned taxonomy registry exists (ARCH-04 input) declaring the world-change kinds.
- The raw-world-SQL fence is a real source-scanning meta-test (not depguard) and is enforced.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-09-SUMMARY.md` when done.
</output>
