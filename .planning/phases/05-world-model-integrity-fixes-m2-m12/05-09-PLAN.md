---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 09
type: execute
wave: 8
depends_on: [05-08]
files_modified:
  - internal/world/outbox/taxonomy.go
  - internal/world/outbox/taxonomy_test.go
  - internal/world/service.go
  - internal/world/service_test.go
  - internal/world/mutator.go
  - internal/world/postgres/character_repo.go
  - internal/world/postgres/character_repo_test.go
  - internal/store/character_settings_repo.go
  - internal/settings/character_store.go
  - internal/settings/character_store_integration_test.go
  - cmd/holomush/sub_grpc.go
  - test/meta/world_sql_fence_test.go
autonomous: true
requirements: [MODEL-03, MODEL-04]
must_haves:
  truths:
    - "A versioned taxonomy schema registry declares the world-change envelope kinds with per-type payload schemas and an App-Schema-Version (the ARCH-04 / Phase-7 input), INCLUDING a character_preferences_update kind for the folded-in character-settings write (round-4 C5 / D-05)."
    - "A real AST/token meta-test scans production Go source for world-table mutation SQL (INSERT/UPDATE/DELETE against the CORE/WORLD surface: locations/exits/characters/objects/entity_properties/outbox/world_feed_counter) and FAILS if any appears outside internal/world/postgres — NOT a depguard rule (depguard matches imported packages, not SQL literals; Codex finding 6). The fence is SCHEMA-SCOPED to the core/world surface per D-05 (round-4 C5): scene_participants is EXCLUDED and the plugins/ tree is EXCLUDED from the scan — there are two distinct scene_participants tables (plugin_core_scenes.scene_participants is plugin-owned and correctly written by the plugin OUTSIDE the world fence; public.scene_participants is the vestigial world-layer table with no live prod writer outside internal/world), so Codex's round-4 'plugin escapes the fence' finding is a schema-blind false positive — REJECTED here with issue #4815 tracking verify-or-remove of the vestigial world scene_repo + future plugin-table-outbox work. entity_properties stays IN the set (property_repo.go:60 writes it, in-boundary)."
    - "The genuine world-fence escape — internal/store/character_settings_repo.go's raw envelope-less UPDATE characters SET preferences (character_settings_repo.go:80, on r.pool) — is FOLDED into the guarded/versioned/envelope path (round-4 C5 / D-05): the preferences write moves into internal/world/postgres (fence-compliant), becomes version-guarded (MODEL-03), and emits exactly one character_preferences_update envelope in the same tx (INV-WORLD-4 true for the characters table); the settings subsystem calls the world boundary instead of writing public.characters directly. This fold-in lands BEFORE the SQL fence's greenness assertion so the re-scoped fence is HONESTLY green against the tree."
  artifacts:
    - path: internal/world/outbox/taxonomy.go
      provides: "versioned envelope-kind registry (per-type payload schema, App-Schema-Version) incl. a character_preferences_update kind (round-4 C5)"
      contains: "SchemaVersion|App-Schema-Version|schema_version"
    - path: internal/world/postgres/character_repo.go
      provides: "version-guarded UpdatePreferences writer (round-4 C5 / D-05 — the character_settings write folded into the world boundary; UPDATE characters SET preferences with a version CAS, returns a wmodel.MutationDelta) — replaces internal/store/character_settings_repo.go's raw r.pool write"
      contains: "preferences|Preferences"
    - path: test/meta/world_sql_fence_test.go
      provides: "AST/token meta-test scanning production Go for CORE/WORLD-table mutation SQL outside internal/world/postgres (scene_participants + plugins/ EXCLUDED per D-05, round-4 C5)"
      contains: "world"
  key_links:
    - from: test/meta/world_sql_fence_test.go
      to: internal/world/postgres
      via: "the meta-test walks production .go files (excluding the plugins/ tree) and allowlists only internal/world/postgres for core/world-table write SQL"
      pattern: "internal/world/postgres"
    - from: internal/settings/character_store.go
      to: internal/world/service.go
      via: "the settings preferences write routes through the world boundary (world.Service.UpdateCharacterPreferences → mutate()) instead of a raw UPDATE characters on its own pool (round-4 C5 fold-in)"
      pattern: "preferences|Preferences|UpdateCharacterPreferences"
  prohibitions:
    - "Raw CORE/WORLD-table mutation SQL (writes to locations/exits/characters/objects/entity_properties/outbox/world_feed_counter) MUST NOT appear outside internal/world/postgres (the writer boundary)."
    - "The fence MUST NOT be a depguard rule — depguard matches imported packages, not SQL literals, so it cannot enforce this (Codex finding 6)."
    - "The fence MUST NOT scan the plugins/ tree and MUST NOT include scene_participants in the fenced-table set (round-4 C5 / D-05) — the core-scenes plugin's plugin_core_scenes.scene_participants writes are correctly outside the world fence; flagging them is a schema-blind false positive. Do NOT plan any plugins/core-scenes change; the vestigial world scene_repo / public.scene_participants question is deferred to issue #4815."
    - "internal/store/character_settings_repo.go MUST NOT retain a raw UPDATE characters write (round-4 C5) — the preferences write is folded into the guarded/versioned/envelope path in internal/world/postgres before the fence asserts greenness."
---

<objective>
Build the versioned taxonomy schema registry (the declared-envelope-kind contract the rollout fills, and the designated ARCH-04 input) and the raw-world-SQL fence as a real AST/token meta-test. This is interface-first for the rollout: 05-10/05-11 wire each command to a declared kind; the census meta-test that asserts full coverage lands in 05-11 (after rollout) so no intermediate commit ships a failing census.

**Round-4 C5 scope decision (governed by CONTEXT.md D-05).** Round-4 Codex flagged the SQL fence as "cannot pass" because two production writers escape it. D-05 splits the finding:
- **REJECTED (documented) — the core-scenes plugin's scene_participants writes.** There are TWO distinct `scene_participants` tables: `plugin_core_scenes.scene_participants` (the plugin's OWN schema — the core-scenes writes at `plugins/core-scenes/store.go:205/781/1013/1246` correctly target THIS, OUTSIDE the world fence) and `public.scene_participants` (world-layer, VESTIGIAL — no live production writer outside `internal/world`). Codex's finding is schema-blind. This plan therefore SCHEMA-SCOPES the fence to the core/world surface and EXCLUDES both `scene_participants` and the `plugins/` tree; no `plugins/core-scenes` change is planned. Issue **#4815** tracks verify-or-remove of the vestigial world `scene_repo` / `public.scene_participants` and any future plugin-table-outbox work — explicitly NOT Phase 5.
- **INCORPORATED — the character_settings escape.** `internal/store/character_settings_repo.go:80` does a genuine unversioned, envelope-less `UPDATE characters SET preferences` on its own pool — a real world-fence escape. Task 2 FOLDS it into the guarded/versioned/envelope path (move the SQL into `internal/world/postgres`, version-guard it for MODEL-03, emit one `character_preferences_update` envelope for INV-WORLD-4, rewire the settings subsystem to call the world boundary) — landing BEFORE Task 3's fence so the re-scoped fence is HONESTLY green (05-09's greenness assertion was false until this re-scope + fold-in land).

<!-- planner-discipline-allow: UPDATE characters -->
(The literal `UPDATE characters` appears in Task 2's action and is negative-checked by an acceptance criterion against `internal/store/character_settings_repo.go`; the executor MUST keep it out of that file's comment bodies — see the comment-text-discipline note in Task 2's action.)

**Slice-3 ordering deviation (Codex finding 11 — made explicit).** The one-pager's normative sequence (`model-01-consensus-onepager.md:41`) is taxonomy → census → invariants BEFORE the mechanical rollout. These plans deliberately deviate to **data-first, enforcement-last**: the taxonomy + SQL fence land here (05-09, both pass immediately), the rollout lands in 05-10/05-11, and the coverage-asserting census + invariant binding land in 05-11/05-12 — AFTER coverage is complete. Rationale: a coverage census introduced before the rollout would leave `task test`/`task lint` red at every intermediate rollout commit (breaking `git bisect` and any concurrent lint run). This is a considered deviation from a locked sequencing detail, not a scope change — every enforcement artifact still lands in Phase 5; only its ordering relative to the rollout differs. Acknowledged here and in 05-10/05-12.

Purpose: The one-pager's static-enforcement layer is a versioned taxonomy + a census + a writer-boundary fence. The taxonomy defines the vocabulary; the fence forbids core/world-table writes leaking outside the repo boundary. depguard CANNOT implement this fence — it matches imported packages, not SQL literals (Codex finding 6) — so the fence is an AST/token meta-test that scans production Go (excluding the plugins/ tree) for core/world-table mutation SQL. The one genuine escape (character_settings' raw `UPDATE characters`) is folded into the boundary first so the fence is honestly green.
Output: taxonomy registry (incl. the character_preferences_update kind), the folded-in guarded/versioned/envelope character-preferences write path, the AST/token raw-world-SQL fence meta-test (schema-scoped to core/world; scene_participants + plugins/ excluded per D-05).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/event-conventions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Versioned taxonomy schema registry</name>
  <files>internal/world/outbox/taxonomy.go, internal/world/outbox/taxonomy_test.go</files>
  <read_first>
    - internal/world/wmodel/envelope.go (05-05 — the envelope's `kind` + `schema_version` fields the registry populates; the cycle-neutral leaf package)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md (World Write Command Census table — the ~15 commands → kinds; examine-fate Open Question 1; CreateCharacter Open Question 3)
    - docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md (Schema governance — versioned CI-validated registry, per-type payload schemas, App-Schema-Version, ARCH-04 input)
  </read_first>
  <behavior>
    - RED: looking up a declared envelope kind returns its per-type payload schema + schema_version; an undeclared kind is rejected (not silently accepted).
    - Every declared kind carries an App-Schema-Version.
    - The registry enumerates the world-change kinds (create/update/delete/move per aggregate + character genesis + character delete/tombstone + character_preferences_update for the folded-in settings write) — state-change kinds only; examine (read) is intentionally excluded. NO scene-participant kinds are declared (round-5 D-07: the vestigial world scene-participant WRITE surface is REMOVED in 05-14, so there is no scene-participant command to declare a kind for — this resolves the D-01↔D-05 contradiction by removal).
  </behavior>
  <action>
    Define a versioned taxonomy registry in `internal/world/outbox/taxonomy.go`: a table mapping each world-change envelope `kind` to its per-type payload schema and a `schema_version`/`App-Schema-Version`. Declare the kinds for the state-change commands (location/exit/object/character create-update-delete-move as applicable, character genesis, a character delete/tombstone kind — REUSED by both world.Service.DeleteCharacter (05-11) and the guest CharacterReapingService (05-16, D-06), AND a `character_preferences_update` kind for the folded-in character-settings write — round-4 C5 / D-05, Task 2). Do NOT declare any scene-participant kind (round-5 D-07: the vestigial `world.Service.AddSceneParticipant`/`RemoveSceneParticipant` write surface is removed in 05-14, so no scene-participant command exists to map — this resolves the D-01↔D-05 scene-participant contradiction by removal). This is the ARCH-04/Phase-7 input, so keep the schemas self-describing and versioned. Resolve the open questions as concrete registry decisions: (1) examine commands are READS, not state changes — they get NO envelope kind (dropped from the world-change feed, per Open Question 1); (2) character creation gets a character-genesis kind (Open Question 3) — its emitting site is the atomic character-genesis service (05-15), which covers ALL THREE production creation paths (registered gRPC, guest, bootstrap-admin — round-3 blocker #5), not just the gRPC handler. Provide a lookup that rejects an undeclared kind. RED test first: declared kind → schema + version; undeclared → rejected; every kind has an App-Schema-Version.
  </action>
  <verify>
    <automated>task test -- ./internal/world/outbox/</automated>
  </verify>
  <acceptance_criteria>
    - The registry declares the world-change kinds with per-type payload schemas + App-Schema-Version.
    - Examine kinds are absent (reads, not state changes).
    - A character-genesis kind exists for CreateCharacter.
    - Lookup rejects an undeclared kind; `task test -- ./internal/world/outbox/` passes.
  </acceptance_criteria>
  <done>The versioned envelope taxonomy exists as the rollout contract + ARCH-04 input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fold character_settings_repo.go's UPDATE characters into the guarded/versioned/envelope world path (round-4 C5 / D-05)</name>
  <files>internal/world/postgres/character_repo.go, internal/world/postgres/character_repo_test.go, internal/world/service.go, internal/world/service_test.go, internal/world/mutator.go, internal/store/character_settings_repo.go, internal/settings/character_store.go, internal/settings/character_store_integration_test.go, cmd/holomush/sub_grpc.go</files>
  <read_first>
    - internal/store/character_settings_repo.go (SetPreferences :72-92 does `UPDATE characters SET preferences` on r.pool — the envelope-less, unversioned world-fence escape; GetPreferences :40-66 is a read and MAY stay a direct read)
    - internal/settings/character_store.go (:34-56 — the settings.CharacterRepository interface + NewRepoCharacterSettingsStore; the seam a world-boundary-backed impl swaps into)
    - internal/world/postgres/character_repo.go (05-03/05-14 — the version-guarded CAS + wmodel.MutationDelta shape to mirror for the new UpdatePreferences writer)
    - internal/world/service.go + internal/world/mutator.go (05-06/05-10 — the mutate() seam + the write-command descriptor set the preferences command registers into; the 05-11 census reads this set)
    - internal/world/outbox/taxonomy.go (Task 1 — the character_preferences_update kind)
    - cmd/holomush/sub_grpc.go (:524 store.NewCharacterSettingsRepository(pool) — the composition site rewired to the world-boundary-backed settings store)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-CONTEXT.md (D-05 — character_settings folds into the guarded/versioned path; scene_participants is OUT)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (round-4 C5 — the character_settings escape is the genuine finding to incorporate)
  </read_first>
  <behavior>
    - RED: a character-preferences update reads the character version, does a version-guarded `UPDATE characters SET preferences=$1, version=version+1 WHERE id=$2 AND version=$3` inside internal/world/postgres, and returns a wmodel.MutationDelta (before/after version) — a concurrent conflicting write surfaces WORLD_CONCURRENT_EDIT (MODEL-03), never a silent last-write-wins.
    - RED: the preferences write emits exactly ONE character_preferences_update envelope in the SAME transaction as the UPDATE (INV-WORLD-4 true for characters); an outer rollback removes both.
    - RED: the settings subsystem's SetPreferences routes through the world boundary — internal/store no longer issues a raw UPDATE characters.
  </behavior>
  <action>
    Move the character-preferences WRITE out of internal/store and into the world boundary (round-4 C5 / D-05 — the read/GetPreferences may remain a direct read; only the mutation is fenced). Add a version-guarded `UpdatePreferences(ctx, characterID, prefs, expectedVersion) (*wmodel.MutationDelta, error)` writer to `internal/world/postgres/character_repo.go` (mirror the 05-03 CAS: read version → `UPDATE characters SET preferences=$1, version=version+1 WHERE id=$2 AND version=$3` → classify a zero-row result via the locked follow-up read → return the delta with before/after version). Expose it as a `world.Service.UpdateCharacterPreferences` command routed through `mutate()` with the `character_preferences_update` taxonomy kind (Task 1), registered as a write-command descriptor in `internal/world/mutator.go` (so the 05-11 census bijection includes it and the go/ast Service-mutating-method cross-check sees it). Because the settings write is an RMW with no caller-supplied version, `UpdateCharacterPreferences` reads the current character version internally and CASes; a concurrent edit surfaces the typed `WORLD_CONCURRENT_EDIT` (MODEL-03; D-02 — no auto-retry, the settings caller surfaces the typed error). Rewire the settings subsystem: `internal/store/character_settings_repo.go`'s `SetPreferences` no longer issues a raw `UPDATE characters` — the `settings.CharacterRepository` write is satisfied by an adapter backed by the world boundary (an impl calling `world.Service.UpdateCharacterPreferences`); construct it at `cmd/holomush/sub_grpc.go:524` in place of `store.NewCharacterSettingsRepository(pool)`'s write path. RED tests first (version-guarded write + one envelope + conflict surfacing; settings SetPreferences routes through the world boundary; no raw UPDATE characters remains in internal/store). Keep any literal `UPDATE characters` token the SQL fence (Task 3) negative-checks out of comment bodies (comment-text discipline).
  </action>
  <verify>
    <automated>task test -- ./internal/world/ ./internal/settings/ ./internal/store/ && task test:int -- -run 'Preference|CharacterPreferences' ./internal/world/postgres/ ./internal/settings/</automated>
  </verify>
  <acceptance_criteria>
    - The character-preferences write lives in internal/world/postgres, is version-guarded (MODEL-03 — a concurrent conflict surfaces WORLD_CONCURRENT_EDIT), and emits exactly one character_preferences_update envelope in the same tx (INV-WORLD-4 for characters).
    - `grep -n 'UPDATE characters' internal/store/character_settings_repo.go` returns nothing (the raw world-table write is gone); SetPreferences routes through world.Service.UpdateCharacterPreferences.
    - A world-boundary-backed settings.CharacterRepository write is constructed at cmd/holomush/sub_grpc.go; `task test -- ./internal/world/ ./internal/settings/ ./internal/store/` passes.
  </acceptance_criteria>
  <done>The character_settings UPDATE characters escape is folded into the guarded/versioned/envelope world path; MODEL-03 + INV-WORLD-4 hold for character-preference updates; the SQL fence (Task 3) can now assert HONEST greenness on the characters table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Raw-world-SQL fence as an AST/token meta-test (schema-scoped to core/world; scene_participants + plugins/ EXCLUDED per D-05)</name>
  <files>test/meta/world_sql_fence_test.go</files>
  <read_first>
    - test/meta/depguard_config_test.go (lines ~18-57 — a meta-test file-walk idiom to model the structure on; NOTE this one asserts config, but the world-SQL fence must inspect SOURCE, not config)
    - internal/world/postgres/location_repo.go, character_repo.go (the write SQL that legitimately lives inside the allowlisted package — INSERT/UPDATE/DELETE against the core/world tables, incl. the Task-2 UpdatePreferences write)
    - plugins/core-scenes/store.go (:205/781/1013/1246 — the plugin's OWN plugin_core_scenes.scene_participants writes that MUST NOT be flagged; the fence excludes the plugins/ tree — round-4 C5 / D-05)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-CONTEXT.md (D-05 — the fence is scoped to the four core tables + characters; scene_participants + plugins/ are OUT; #4815 tracks the vestigial world scene_repo)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (Codex finding 6 — depguard matches imported packages, NOT SQL literals; round-4 C5 — the scene_participants finding is a schema-blind false positive, the character_settings escape is the real one folded in Task 2)
  </read_first>
  <behavior>
    - RED: given a synthetic fixture Go file OUTSIDE internal/world/postgres containing a string literal `INSERT INTO locations ...` (or UPDATE/DELETE against a core/world table), the meta-test FAILS; the same SQL inside internal/world/postgres passes (allowlisted).
    - The fence recognizes the CORE/WORLD tables ONLY: locations, exits, characters, objects, entity_properties, outbox, world_feed_counter. scene_participants is EXCLUDED and the plugins/ tree is NOT scanned (round-4 C5 / D-05).
    - Reads (SELECT) anywhere are allowed; only mutation SQL (INSERT/UPDATE/DELETE) is fenced.
    - A fixture proves the fence does NOT flag a `plugins/core-scenes` scene_participants write (schema-blind false positive rejected per D-05 / #4815).
  </behavior>
  <action>
    Add `test/meta/world_sql_fence_test.go` — a real fence, NOT a depguard rule (depguard matches imported packages, not SQL literals; Codex finding 6). Walk the production Go source tree (parse with `go/ast`/`go/parser`, or a token scan of string literals if simpler and robust), EXCLUDING the `plugins/` tree (round-4 C5 / D-05 — plugin-owned schemas are outside the world fence), find string literals containing core/world-table mutation SQL — `INSERT INTO`, `UPDATE`, or `DELETE FROM` targeting any of `locations`, `exits`, `characters`, `objects`, `entity_properties`, `outbox`, `world_feed_counter` — and FAIL if any is found in a file OUTSIDE the allowlisted `internal/world/postgres` directory (skip `_test.go` and generated files). **`scene_participants` is NOT in the fenced set and the `plugins/` tree is NOT scanned (round-4 C5 / D-05):** there are two distinct scene_participants tables — `plugin_core_scenes.scene_participants` (plugin-owned; the core-scenes writes at `plugins/core-scenes/store.go` correctly live outside the world fence) and vestigial `public.scene_participants` (no live prod writer outside `internal/world`); Codex's "plugin escapes the fence" finding is a schema-blind false positive, REJECTED here, with issue **#4815** tracking verify-or-remove of the vestigial world `scene_repo` + future plugin-table-outbox work (NOT Phase 5). `entity_properties` STAYS IN the set (round-2: property_repo.go:60/136 mutate it in-boundary; without it a raw property write outside the repo package would not be caught). This is the writer-boundary enforcement (INV-WORLD-4 / WRITER-BOUNDARY, bound in 05-12). Include a positive in-fixture assertion (core/world SQL inside the allowlist passes), a negative fixture (core/world SQL outside fails), AND a fixture proving a `plugins/`-tree scene_participants write is NOT flagged (D-05 rejection). The fence is HONESTLY green against the current tree ONLY because Task 2 folded the character_settings `UPDATE characters` escape into `internal/world/postgres` first (round-4 C5 — the greenness assertion was false against an unscoped fence + un-folded character_settings; both the re-scope and the fold-in must land). Do NOT introduce the coverage-asserting census meta-test here — it lands in 05-11 after the rollout completes coverage. Keep any literal world-table/SQL tokens the test negative-checks out of comment bodies (comment-text discipline).
  </action>
  <verify>
    <automated>task test -- ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - `test/meta/world_sql_fence_test.go` scans production Go source (AST/token, EXCLUDING the plugins/ tree) and fails on core/world-table mutation SQL outside `internal/world/postgres`; a negative fixture proves it fails, a positive fixture proves the allowlist passes, and a plugins/-tree scene_participants fixture proves it is NOT flagged (D-05 rejection).
    - The fence covers the CORE/WORLD tables (locations, exits, characters, objects, entity_properties, outbox, world_feed_counter); scene_participants is EXCLUDED and the plugins/ tree is not scanned (round-4 C5 / D-05, #4815).
    - No depguard SQL rule is added to `.golangci.yaml`.
    - `task test -- ./test/meta/` passes against the current tree — HONESTLY green because Task 2 folded the character_settings escape into the boundary first (round-4 C5).
  </acceptance_criteria>
  <done>The writer-boundary is enforced by a real source-scanning meta-test, not an unenforceable depguard rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go code → world tables | A world-table write outside the repo boundary escapes the envelope/version guard entirely. |
| lint config → CI | A silently-dropped fence rule re-opens the boundary without a test failing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-27 | Tampering | raw world-SQL outside the repo | high | mitigate | The AST/token meta-test scans production Go (excluding plugins/) for core/world-table mutation SQL and fails on any outside `internal/world/postgres`; a negative fixture proves the fence actually fires (depguard could not — it matches imported packages, not SQL literals; Codex finding 6). The character_settings escape is folded into the boundary (Task 2) so greenness is honest (round-4 C5). |
| T-05-28 | Tampering | taxonomy drift | medium | mitigate | The registry is versioned (App-Schema-Version) and CI-validated; the census (05-11) asserts every command maps to a declared kind — an unregistered kind is rejected at lookup. |
| T-05-52 | Repudiation | fence asserts false greenness / over-broad scope | high | mitigate | Round-4 C5 / D-05: the fence is schema-scoped to core/world (scene_participants + plugins/ excluded — the plugin's plugin_core_scenes.scene_participants writes are correctly out of scope; a fixture proves they are not flagged), and the genuine character_settings escape is folded in (Task 2) BEFORE the greenness assertion, so the meta-test is HONESTLY green rather than asserting a falsehood. #4815 tracks the deferred vestigial-world-scene_repo question. |
</threat_model>

<verification>
- `task test -- ./internal/world/ ./internal/world/outbox/ ./internal/settings/ ./internal/store/ ./test/meta/` passes (run via `local-check`).
- The world-SQL fence meta-test fails on a negative fixture (core/world-table mutation SQL outside `internal/world/postgres`), does NOT flag a plugins/-tree scene_participants write (D-05 rejection fixture), and passes on the current tree — honestly green because Task 2 folded the character_settings escape into the boundary.
- `grep -n 'UPDATE characters' internal/store/character_settings_repo.go` returns nothing (round-4 C5 fold-in); `grep -c 'world' test/meta/world_sql_fence_test.go` confirms the fence references the core/world tables (filter comments: `grep -v '^//' | grep -c`).
</verification>

<success_criteria>
- The versioned taxonomy registry exists (ARCH-04 input) declaring the world-change kinds incl. character_preferences_update.
- The character_settings `UPDATE characters` escape is folded into the guarded/versioned/envelope world path (MODEL-03 + INV-WORLD-4 hold for character-preference updates; round-4 C5 / D-05).
- The raw-world-SQL fence is a real source-scanning meta-test (not depguard), schema-scoped to core/world (scene_participants + plugins/ excluded per D-05, #4815), enforced, and honestly green.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-09-SUMMARY.md` when done.
</output>
