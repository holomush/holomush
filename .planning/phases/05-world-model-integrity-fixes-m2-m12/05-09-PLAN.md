---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 09
type: execute
wave: 8
depends_on: [05-08]
files_modified:
  - internal/world/outbox/taxonomy.go
  - internal/world/outbox/taxonomy_test.go
  - .golangci.yaml
  - test/meta/world_sql_fence_test.go
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "A versioned taxonomy schema registry declares the ~15-20 world-change envelope kinds with per-type payload schemas and an App-Schema-Version (the ARCH-04 / Phase-7 input)."
    - "A depguard/lint fence forbids raw world-table SQL outside internal/world/postgres, and a meta-test asserts the fence config cannot silently drift."
  artifacts:
    - path: internal/world/outbox/taxonomy.go
      provides: "versioned envelope-kind registry (~15-20 kinds, per-type payload schema, App-Schema-Version)"
      contains: "SchemaVersion|App-Schema-Version|schema_version"
    - path: test/meta/world_sql_fence_test.go
      provides: "meta-test asserting the raw-world-SQL depguard fence is present in .golangci.yaml"
      contains: "world"
  key_links:
    - from: .golangci.yaml
      to: test/meta/world_sql_fence_test.go
      via: "the meta-test reads .golangci.yaml and requires the world-SQL fence deny rule present"
      pattern: "require.Contains"
  prohibitions:
    - "Raw world-table SQL (writes to locations/exits/characters/objects/outbox/world_feed_counter) MUST NOT appear outside internal/world/postgres (the writer boundary)."
---

<objective>
Build the versioned taxonomy schema registry (the declared-envelope-kind contract the rollout fills, and the designated ARCH-04 input) and the raw-world-SQL lint fence with its drift-guard meta-test. This is interface-first for the rollout: 05-10/05-11 wire each command to a declared kind; the census meta-test that asserts full coverage lands in 05-11 (after rollout) so no intermediate commit ships a failing census.

Purpose: The one-pager's static-enforcement layer is a versioned taxonomy + a census + a lint fence. The taxonomy defines the vocabulary; the fence forbids world-table writes leaking outside the repo boundary. Introducing the registry + fence first (both pass immediately) lets the rollout declare kinds against a fixed schema, and defers the coverage-asserting census to the plan that completes coverage (data-first, enforcement-last — avoids a broken `task lint` between rollout commits).
Output: taxonomy registry, .golangci.yaml world-SQL fence, fence drift-guard meta-test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/event-conventions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Versioned taxonomy schema registry</name>
  <files>internal/world/outbox/taxonomy.go, internal/world/outbox/taxonomy_test.go</files>
  <read_first>
    - internal/world/outbox/envelope.go (05-05 — the envelope's `kind` + `schema_version` fields the registry populates)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md (World Write Command Census table — the ~15 commands → kinds; examine-fate Open Question 1; CreateCharacter Open Question 3)
    - docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md (Schema governance — versioned CI-validated registry, per-type payload schemas, App-Schema-Version, ARCH-04 input)
  </read_first>
  <behavior>
    - RED: looking up a declared envelope kind returns its per-type payload schema + schema_version; an undeclared kind is rejected (not silently accepted).
    - Every declared kind carries an App-Schema-Version.
    - The registry enumerates the world-change kinds (create/update/delete/move per aggregate + scene-participant add/remove + character genesis) — state-change kinds only; examine (read) is intentionally excluded.
  </behavior>
  <action>
    Define a versioned taxonomy registry in `internal/world/outbox/taxonomy.go`: a table mapping each world-change envelope `kind` to its per-type payload schema and a `schema_version`/`App-Schema-Version`. Declare the kinds for the state-change commands (location/exit/object/character create-update-delete-move as applicable, scene-participant add/remove, character genesis) — this is the ARCH-04/Phase-7 input, so keep the schemas self-describing and versioned. Resolve the open questions as concrete registry decisions: (1) examine commands are READS, not state changes — they get NO envelope kind (dropped from the world-change feed, per Open Question 1); (2) `CreateCharacter` (a world-state write in the gRPC registration flow, `internal/grpc/auth_handlers.go`) gets a character-genesis kind (Open Question 3). Provide a lookup that rejects an undeclared kind. RED test first: declared kind → schema + version; undeclared → rejected; every kind has an App-Schema-Version.
  </action>
  <verify>
    <automated>task test -- ./internal/world/outbox/</automated>
  </verify>
  <acceptance_criteria>
    - The registry declares the world-change kinds with per-type payload schemas + App-Schema-Version.
    - Examine kinds are absent (reads, not state changes).
    - A character-genesis kind exists for CreateCharacter.
    - Lookup rejects an undeclared kind; `task test -- ./internal/world/outbox/` passes.
  </acceptance_criteria>
  <done>The versioned envelope taxonomy exists as the rollout contract + ARCH-04 input.</done>
</task>

<task type="auto">
  <name>Task 2: Raw-world-SQL lint fence + drift-guard meta-test</name>
  <files>.golangci.yaml, test/meta/world_sql_fence_test.go</files>
  <read_first>
    - .golangci.yaml (lines ~136-154 — the depguard `deny` block shape to extend)
    - test/meta/depguard_config_test.go (lines ~18-57 — the config-assertion meta-test idiom: read .golangci.yaml, require.Contains each deny rule)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md ("Lint fence" section — same model as holomush-1eps2)
  </read_first>
  <action>
    Add a depguard/lint fence to `.golangci.yaml` that forbids raw world-table SQL (direct writes to locations/exits/characters/objects/outbox/world_feed_counter) outside `internal/world/postgres` — the writer-boundary enforcement (INV-WORLD-WRITER-BOUNDARY, bound in 05-12). Model it on the existing depguard `deny` block; the world tables are owned by the postgres repo package, so any Go file outside `internal/world/postgres` that constructs world-table write SQL is denied. Add `test/meta/world_sql_fence_test.go` mirroring `depguard_config_test.go`: read `.golangci.yaml` and `require.Contains` the world-SQL fence rule so the config claim cannot silently drift. Confirm `task lint` is green with the fence in place (the fence should already hold — writes live only in `internal/world/postgres`; if `task lint` flags an existing site, that IS a real boundary violation to fix, not to exempt). Do NOT introduce the coverage-asserting census meta-test here — it lands in 05-11 after the rollout completes coverage.
  </action>
  <verify>
    <automated>task test -- ./test/meta/ && task lint</automated>
  </verify>
  <acceptance_criteria>
    - `.golangci.yaml` contains a fence forbidding raw world-table SQL outside `internal/world/postgres`.
    - `test/meta/world_sql_fence_test.go` asserts the fence rule is present (fails if the config drifts).
    - `task lint` is green (no existing world-table write leaks outside the repo boundary).
  </acceptance_criteria>
  <done>The writer-boundary lint fence is enforced and drift-guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go code → world tables | A world-table write outside the repo boundary escapes the envelope/version guard entirely. |
| lint config → CI | A silently-dropped fence rule re-opens the boundary without a test failing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-27 | Tampering | raw world-SQL outside the repo | high | mitigate | The depguard fence denies world-table write SQL outside `internal/world/postgres`; the meta-test guards the fence config against silent drift (the exact holomush-1eps2 failure mode). |
| T-05-28 | Tampering | taxonomy drift | medium | mitigate | The registry is versioned (App-Schema-Version) and CI-validated; the census (05-11) asserts every command maps to a declared kind — an unregistered kind is rejected at lookup. |
</threat_model>

<verification>
- `task test -- ./internal/world/outbox/ ./test/meta/` passes (run via `local-check`).
- `task lint` is green with the new fence.
- `grep -c 'world' test/meta/world_sql_fence_test.go` confirms the fence assertion references the world-SQL rule (filter comments: `grep -v '^//' | grep -c`).
</verification>

<success_criteria>
- The versioned taxonomy registry exists (ARCH-04 input) declaring the world-change kinds.
- The raw-world-SQL fence is enforced and drift-guarded.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-09-SUMMARY.md` when done.
</output>
