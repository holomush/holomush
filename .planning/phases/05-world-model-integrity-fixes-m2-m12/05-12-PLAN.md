---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 12
type: execute
wave: 11
depends_on: [05-11, 05-15]
files_modified:
  - docs/architecture/invariants.yaml
  - docs/architecture/invariants.md
  - internal/world/outbox/delta_parity_test.go
  - internal/world/outbox/writer_boundary_test.go
  - internal/world/outbox/relay_test.go
  - internal/world/postgres/outbox_store_test.go
  - test/meta/world_sql_fence_test.go
  - test/meta/world_import_graph_test.go
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "A new WORLD scope exists in the invariant registry with four entries using CANONICAL NUMERIC ids the parser recognizes: INV-WORLD-1 (ATOMIC-FEED), INV-WORLD-2 (DELTA-PARITY), INV-WORLD-3 (FEED-ORDER), INV-WORLD-4 (WRITER-BOUNDARY). The ADR/one-pager symbolic names are carried as the summary + legacy provenance, NOT as parser ids (Codex finding 3: invariant_registry_test.go:163 only matches INV-<SCOPE>-<number>; symbolic names would never bind)."
    - "All four INV-WORLD-N are binding: bound (not pending) — each has a genuinely-asserting test carrying a // Verifies: INV-WORLD-N annotation (numeric), and the registry meta-tests pass."
    - "INV-WORLD-1 (ATOMIC-FEED) binds to the ALWAYS-RUN STATE+ENVELOPE atomicity test in internal/world/postgres/outbox_store_test.go (normal task test:int Integration Test lane; round-2 MEDIUM: not the quarantine-gated resilience suite, resilience_suite_test.go:46) — and that test MUST be the round-3-upgraded one that mutates a REAL WORLD ROW and writes its envelope in one tx (rollback → neither survives; commit → both survive; forced outbox failure after the state write → state rolls back). Binding to an envelope-only rollback test would prove only that the outbox insert honors the ambient tx, not state+envelope atomicity (round-3 MEDIUM). The resilience m2_dualwrite spec remains supplemental evidence, not the binding."
    - "INV-WORLD-3 (FEED-ORDER) binds to the relay order + halt-on-poison + SAME-POSITION-SKIP-MARKER test — the binding test asserts that after an operator skip the wire has no missing feed_position (a marker is published at the skipped position), so the round-2 skip redesign is what the invariant actually proves."
    - "invariants.md is regenerated from the yaml (no stale-diff)."
  artifacts:
    - path: docs/architecture/invariants.yaml
      provides: "the WORLD scope + 4 bound INV-WORLD-N entries (numeric ids; symbolic names in summary/legacy)"
      contains: "INV-WORLD-1"
    - path: internal/world/outbox/delta_parity_test.go
      provides: "genuinely-asserting test that the manifest matches the committed delta (before/after versions)"
      contains: "INV-WORLD-2"
  key_links:
    - from: docs/architecture/invariants.yaml
      to: internal/world/outbox/delta_parity_test.go
      via: "asserted_by lists the // Verifies: INV-WORLD-2 test"
      pattern: "asserted_by|Verifies"
  prohibitions:
    - "No INV-WORLD-N may be left binding: pending at phase end (ROADMAP success criterion 4)."
    - "The invariant ids MUST be the numeric INV-WORLD-1..4 form the parser recognizes — do NOT use the symbolic INV-WORLD-ATOMIC-FEED form as a registry id or // Verifies: annotation, and do NOT modify the registry parser to accept symbolic ids (Codex finding 3)."
    - "No // Verifies: annotation may bind a Skip-only or presence-only test — delta-parity (INV-WORLD-2) MUST prove the manifest matches the delta, not merely that a manifest is present."
---

<objective>
Register the WORLD scope and mint + BIND the four invariants the ADR named, using the CANONICAL NUMERIC ids the registry parser recognizes — `INV-WORLD-1` (ATOMIC-FEED), `INV-WORLD-2` (DELTA-PARITY), `INV-WORLD-3` (FEED-ORDER), `INV-WORLD-4` (WRITER-BOUNDARY) — with the ADR's symbolic names preserved as `summary`/`legacy` provenance. Each is bound by a genuinely-asserting `// Verifies: INV-WORLD-N` test. This satisfies ROADMAP success criterion 4 (the invariants are bound, not pending).

**Why numeric, not the symbolic names (Codex finding 3):** the registry's `// Verifies:` parser is `regexp.MustCompile(`//\s*Verifies:\s*(INV-[A-Z]+-\d+)`)` (`test/meta/invariant_registry_test.go:163`) — it requires a trailing NUMBER. `// Verifies: INV-WORLD-ATOMIC-FEED` would NEVER match, so the binding would silently fail and success criterion 4 would be unmet. We adopt numeric ids (the `.claude/rules/invariants.md` canonical `INV-<SCOPE>-N` form) and carry the ADR names as summary text + a `legacy:` alias for provenance. We do NOT modify the shared parser/tooling (a repo-wide change with broad blast radius). Record the id↔name mapping in the summary: INV-WORLD-1=ATOMIC-FEED, INV-WORLD-2=DELTA-PARITY, INV-WORLD-3=FEED-ORDER, INV-WORLD-4=WRITER-BOUNDARY.

Purpose: The drift that caused the ADR is "a stated principle silently unrealized." The registry ratchet makes the four guarantees non-recurrable — each is a durable property a future change could break and a test pins forever. `WORLD` is a brand-new scope (not among the migrated boundaries), so it must be registered by hand (the orphan-check scan does not cover `.planning/` specs).
Output: 4 bound INV-WORLD-N entries (numeric ids, symbolic names as summary/legacy), their binding tests, regenerated invariants.md, green registry meta-tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/adr/holomush-i4784-world-state-model-decision.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/invariants.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the WORLD scope + 4 invariants; write the delta-parity + writer-boundary binding tests</name>
  <files>docs/architecture/invariants.yaml, internal/world/outbox/delta_parity_test.go, internal/world/outbox/writer_boundary_test.go</files>
  <read_first>
    - docs/architecture/invariants.yaml (the scopes: block ~line 11 onward — an existing scope entry shape: name/description/boundary/status/origin_specs/owned_paths; and a per-invariant `- id: INV-<SCOPE>-N` entry shape ~line 774)
    - .claude/rules/invariants.md (the register/bind ratchet; MUST NOT fabricate a binding; MUST NOT bind a Skip/placeholder; pending MUST NOT carry asserted_by; delta-parity presence-insufficient warning)
    - internal/world/wmodel/envelope.go, internal/world/wmodel/mutation_delta.go (the manifest + before/after versions the delta-parity test asserts EQUAL the returned delta)
    - internal/world/mutator.go (the write executor + reader-view fence the writer-boundary test proves; the enforceable boundary = reader-view compile fence + the AST SQL fence)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md (Invariant Registration Mechanics — the four invariants' summaries + binding requirements; orphan-check caveat)
  </read_first>
  <action>
    Add a new `WORLD` scope to `docs/architecture/invariants.yaml` (mirror an existing scope's field set: name `INV-WORLD`, a boundary description, `status`, `origin_specs` pointing to the ADR `docs/adr/holomush-i4784-world-state-model-decision.md` + the consensus one-pager, `owned_paths` covering `internal/world/**`, `internal/world/outbox/**`, `internal/world/postgres/**`). Add four `- id:` entries with CANONICAL NUMERIC ids and the symbolic name in the summary + a `legacy:` alias (Codex finding 3): `INV-WORLD-1` — "ATOMIC-FEED: state change + its one envelope commit atomically in the same tx" (legacy: INV-WORLD-ATOMIC-FEED); `INV-WORLD-2` — "DELTA-PARITY: the affected-aggregates manifest provably matches the committed delta — before/after versions equal the row's actual version transition; presence is insufficient" (legacy: INV-WORLD-DELTA-PARITY); `INV-WORLD-3` — "FEED-ORDER: relay publishes strictly in feed_position order, gap-free, poison halts" (legacy: INV-WORLD-FEED-ORDER); `INV-WORLD-4` — "WRITER-BOUNDARY: world-table mutation SQL is confined to internal/world/postgres (AST fence, incl. entity_properties) AND world.Service exposes no directly-callable writer repo (reader-view compile fence) AND only composition/test packages may import internal/world/postgres (the 05-07 import-graph allowlist) — the write executor is the sole in-world writer-repo owner; the ONE sanctioned out-of-world writer is the atomic character-genesis application service (05-15), through which registered, guest, AND bootstrap-admin character creation all emit their genesis envelope atomically (round-3 blocker #5: guest/bootstrap paths previously bypassed the fence); admin repairs AND migrations/backfills either emit normal mutations or advance the feed epoch" (legacy: INV-WORLD-WRITER-BOUNDARY). This is the round-3-reconciled ENFORCEABLE boundary (Go cannot make a concrete repo method uncallable from a package holding a pool, so the AST SQL fence + the import allowlist are the mechanical backstops; the reader-view fence closes the in-service path; the 05-15 interface Create removal closes the character-creation path). Write two dedicated genuinely-asserting binding tests: `delta_parity_test.go` — commit a mutation (incl. a cascade/bidirectional case), then assert the emitted envelope's manifest before/after versions + affected IDs EQUAL the MutationDelta the repo returned / the rows' actual version transition (not merely that a manifest exists); `writer_boundary_test.go` — assert `world.Service` exposes no directly-callable write repo (its fields are reader views; the write is unreachable except through the envelope seam) — the SQL-side of WRITER-BOUNDARY is proven by the AST fence meta-test `test/meta/world_sql_fence_test.go` (05-09) and the import-side by the composition-allowlist assertion in `test/meta/world_import_graph_test.go` (05-07 Task 4); INV-WORLD-4 lists all of these in `asserted_by`. Do NOT set `asserted_by`/`binding: bound` yet if you register-then-bind in two steps — but this plan does both, so proceed to Task 2 immediately.
  </action>
  <verify>
    <automated>task test -- ./internal/world/outbox/</automated>
  </verify>
  <acceptance_criteria>
    - `invariants.yaml` has a `WORLD` scope + four entries with NUMERIC ids `INV-WORLD-1..4`, each summary naming the symbolic meaning + a `legacy:` alias; ADR/one-pager origin.
    - `delta_parity_test.go` asserts manifest before/after versions + affected IDs EQUAL the returned MutationDelta / row transition (not presence).
    - `writer_boundary_test.go` asserts `world.Service` exposes no directly-callable write repo (write unreachable without the envelope seam); the SQL fence side is covered by `test/meta/world_sql_fence_test.go`.
    - `task test -- ./internal/world/outbox/` passes.
  </acceptance_criteria>
  <done>The WORLD scope + 4 numeric-id invariants are registered; delta-parity + writer-boundary have genuine binding tests.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate all binding tests with numeric ids, flip to bound, regenerate + confirm</name>
  <files>docs/architecture/invariants.yaml, docs/architecture/invariants.md, internal/world/outbox/delta_parity_test.go, internal/world/outbox/writer_boundary_test.go, internal/world/outbox/relay_test.go, test/meta/world_sql_fence_test.go, test/meta/world_import_graph_test.go, test/integration/resilience/m2_dualwrite_test.go</files>
  <read_first>
    - .claude/rules/invariants.md (the binding step: annotate `// Verifies: INV-WORLD-N`, set binding: bound + asserted_by, regenerate `go run ./cmd/inv-render`, confirm the three registry meta-tests)
    - test/meta/invariant_registry_test.go (line ~163 — the `// Verifies:` regex requires INV-<SCOPE>-<number>; the ids MUST be numeric)
    - internal/world/postgres/outbox_store_test.go (05-05 — the ALWAYS-RUN round-3-upgraded state+envelope atomicity test that proves INV-WORLD-1 / ATOMIC-FEED in the normal task test:int lane: real world row + envelope, rollback/commit/forced-outbox-failure)
    - internal/world/outbox/relay_test.go (05-07 — the order + halt-on-poison + same-position-skip-marker test that proves INV-WORLD-3 / FEED-ORDER, including the post-skip no-wire-gap assertion)
    - test/meta/world_sql_fence_test.go (05-09 — the AST SQL fence proving the SQL side of INV-WORLD-4 / WRITER-BOUNDARY)
    - test/integration/resilience/resilience_suite_test.go (:46 — the quarantine gate; the reason INV-WORLD-1 must NOT bind to a spec under this gate — round-2 MEDIUM)
  </read_first>
  <action>
    Add a `// Verifies: INV-WORLD-N` annotation (NUMERIC id — the parser at `invariant_registry_test.go:163` requires it; Codex finding 3) immediately above the genuinely-asserting test/assertion block for each: `INV-WORLD-1` (ATOMIC-FEED) → the ALWAYS-RUN state+envelope atomicity test in `internal/world/postgres/outbox_store_test.go` (normal task test:int lane — round-2 MEDIUM: NOT the quarantine-gated m2_dualwrite spec, which stays supplemental) — verify before annotating that the test genuinely mutates a REAL world row alongside the envelope and covers rollback-both / commit-both / forced-outbox-failure-rolls-back-state (round-3 MEDIUM: an envelope-only rollback test would be a partial binding — the INV-PRIVACY-6 false-green class); `INV-WORLD-3` (FEED-ORDER) → the relay order + halt-on-poison + same-position-skip-marker test (the assertion that after a skip the wire has no missing feed_position — round-2 skip redesign); `INV-WORLD-2` (DELTA-PARITY) → `delta_parity_test.go`; `INV-WORLD-4` (WRITER-BOUNDARY) → `writer_boundary_test.go` (the compile-time reader-view unreachability) AND `test/meta/world_sql_fence_test.go` (the AST SQL fence, now covering entity_properties) AND `test/meta/world_import_graph_test.go` (the composition allowlist constraining concrete-writer construction — round-3). In `invariants.yaml`, flip each of the four entries to `binding: bound` and add `asserted_by:` listing the exact verifying test file(s) (INV-WORLD-1 → `internal/world/postgres/outbox_store_test.go`; INV-WORLD-4 lists writer_boundary_test.go, test/meta/world_sql_fence_test.go, and test/meta/world_import_graph_test.go). Regenerate the human-readable view with `go run ./cmd/inv-render` (never hand-edit the generated regions of `invariants.md`). Confirm the registry meta-tests pass: `TestEveryRegistryInvariantHasBinding`, `TestProvenanceGuard`, `TestBoundInvariantsAreGenuinelyAsserted`. Because `WORLD` is a new scope not covered by the `docs/superpowers/specs/` orphan scan, this hand-registration is correct (orphan-check caveat).
  </action>
  <verify>
    <automated>task test -- -run 'TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted' ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - Each of INV-WORLD-1..4 is `binding: bound` with `asserted_by:` listing a real verifying test; INV-WORLD-1 → the ALWAYS-RUN internal/world/postgres/outbox_store_test.go STATE+ENVELOPE atomicity test (real world row + envelope, all three cases — NOT a quarantine-gated spec, NOT an envelope-only rollback test); INV-WORLD-4 lists writer_boundary_test.go, test/meta/world_sql_fence_test.go, AND test/meta/world_import_graph_test.go (the composition allowlist).
    - Each verifying test carries a NUMERIC `// Verifies: INV-WORLD-N` annotation the parser recognizes and genuinely asserts the invariant (no Skip-only / presence-only binding); INV-WORLD-3's test asserts no post-skip wire gap.
    - `invariants.md` is regenerated (no stale diff under `inv-render -check`).
    - `TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted` pass — proving the numeric ids actually bind (a symbolic id would have silently failed here; finding 3).
  </acceptance_criteria>
  <done>All four INV-WORLD-* are bound to genuinely-asserting tests (INV-WORLD-1 to an always-run atomicity test), and the registry is regenerated + green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invariant claim → test | A bound invariant with a fake/weak test is a false-green (the INV-RB-3 bug class). |
| yaml → generated md | A stale invariants.md misrepresents the guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-35 | Repudiation | fabricated binding | high | mitigate | `TestBoundInvariantsAreGenuinelyAsserted` rejects Skip-only bindings; delta-parity asserts manifest EQUALS the delta (presence-insufficient), avoiding the false-green the invariant rule warns about. |
| T-05-36 | Tampering | stale invariants.md | low | mitigate | `inv-render` regenerates the md and CI runs generate-and-diff — a stale md fails. |
</threat_model>

<verification>
- `task test -- -run 'TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted' ./test/meta/` passes (run via `local-check`).
- `grep -c 'INV-WORLD' docs/architecture/invariants.yaml` returns at least 4.
- `go run ./cmd/inv-render -check` (or `task invariants:render` then `git diff --exit-code docs/architecture/invariants.md`) shows no stale diff.
</verification>

<success_criteria>
- The WORLD scope + four INV-WORLD-* invariants are registered AND bound (not pending).
- Each binding is a genuine assertion; the registry meta-tests are green.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-12-SUMMARY.md` when done.
</output>
