---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 12
type: execute
wave: 11
depends_on: [05-11]
files_modified:
  - docs/architecture/invariants.yaml
  - docs/architecture/invariants.md
  - internal/world/outbox/delta_parity_test.go
  - internal/world/outbox/writer_boundary_test.go
  - internal/world/outbox/relay_test.go
  - test/integration/resilience/m2_dualwrite_test.go
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "A new WORLD scope exists in the invariant registry with four entries: INV-WORLD-ATOMIC-FEED, INV-WORLD-DELTA-PARITY, INV-WORLD-FEED-ORDER, INV-WORLD-WRITER-BOUNDARY."
    - "All four INV-WORLD-* are binding: bound (not pending) — each has a genuinely-asserting test carrying a // Verifies: annotation, and the registry meta-tests pass."
    - "invariants.md is regenerated from the yaml (no stale-diff)."
  artifacts:
    - path: docs/architecture/invariants.yaml
      provides: "the WORLD scope + 4 bound INV-WORLD-* entries"
      contains: "INV-WORLD"
    - path: internal/world/outbox/delta_parity_test.go
      provides: "genuinely-asserting test that the manifest matches the committed delta (before/after versions)"
      contains: "INV-WORLD-DELTA-PARITY"
  key_links:
    - from: docs/architecture/invariants.yaml
      to: internal/world/outbox/delta_parity_test.go
      via: "asserted_by lists the // Verifies: INV-WORLD-DELTA-PARITY test"
      pattern: "asserted_by|Verifies"
  prohibitions:
    - "No INV-WORLD-* may be left binding: pending at phase end (ROADMAP success criterion 4)."
    - "No // Verifies: annotation may bind a Skip-only or presence-only test — delta-parity MUST prove the manifest matches the delta, not merely that a manifest is present."
---

<objective>
Register the WORLD scope and mint + BIND the four invariants named by the ADR: `INV-WORLD-ATOMIC-FEED`, `INV-WORLD-DELTA-PARITY`, `INV-WORLD-FEED-ORDER`, `INV-WORLD-WRITER-BOUNDARY`. Each is bound by a genuinely-asserting `// Verifies:` test (some already written in slices 2-3, annotated here; delta-parity + writer-boundary get dedicated binding tests). This satisfies ROADMAP success criterion 4 (the invariants are bound, not pending).

Purpose: The drift that caused the ADR is "a stated principle silently unrealized." The registry ratchet makes the four guarantees non-recurrable — each is a durable property a future change could break and a test pins forever. `WORLD` is a brand-new scope (not among the migrated boundaries), so it must be registered by hand (the orphan-check scan does not cover `.planning/` specs).
Output: 4 bound INV-WORLD-* entries, their binding tests, regenerated invariants.md, green registry meta-tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/adr/holomush-i4784-world-state-model-decision.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/invariants.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the WORLD scope + 4 invariants; write the delta-parity + writer-boundary binding tests</name>
  <files>docs/architecture/invariants.yaml, internal/world/outbox/delta_parity_test.go, internal/world/outbox/writer_boundary_test.go</files>
  <read_first>
    - docs/architecture/invariants.yaml (the scopes: block ~line 11 onward — an existing scope entry shape: name/description/boundary/status/origin_specs/owned_paths; and a per-invariant `- id: INV-<SCOPE>-N` entry shape ~line 774)
    - .claude/rules/invariants.md (the register/bind ratchet; MUST NOT fabricate a binding; MUST NOT bind a Skip/placeholder; pending MUST NOT carry asserted_by; delta-parity presence-insufficient warning)
    - internal/world/outbox/envelope.go, writer.go (the manifest before/after versions the delta-parity test asserts)
    - internal/world/mutator.go (the write-boundary the writer-boundary test proves unreachable via raw SQL)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md (Invariant Registration Mechanics — the four invariants' summaries + binding requirements; orphan-check caveat)
  </read_first>
  <action>
    Add a new `WORLD` scope to `docs/architecture/invariants.yaml` (mirror an existing scope's field set: name `INV-WORLD`, a boundary description, `status`, `origin_specs` pointing to the ADR `docs/adr/holomush-i4784-world-state-model-decision.md` + the consensus one-pager, `owned_paths` covering `internal/world/**`, `internal/world/outbox/**`, `internal/world/postgres/**`). Add four `- id:` entries with summaries: INV-WORLD-ATOMIC-FEED (state change + its one envelope commit atomically in the same tx), INV-WORLD-DELTA-PARITY (the affected-aggregates manifest provably matches the committed delta — before/after versions equal the row's actual version transition; presence is insufficient), INV-WORLD-FEED-ORDER (relay publishes strictly in feed_position order, gap-free, poison halts), INV-WORLD-WRITER-BOUNDARY (no world-table writes outside the mutation wrapper; admin repairs AND migrations/backfills either emit normal mutations or advance the feed epoch). Write two dedicated genuinely-asserting binding tests: `delta_parity_test.go` — commit a mutation, then assert the emitted envelope's manifest before/after versions EQUAL the row's actual version transition (not merely that a manifest exists); `writer_boundary_test.go` — assert the lint fence denies raw world-table SQL outside the repo AND that a world write is unreachable except through the envelope seam. Do NOT set `asserted_by`/`binding: bound` yet if you register-then-bind in two steps — but this plan does both, so proceed to Task 2 immediately.
  </action>
  <verify>
    <automated>task test -- ./internal/world/outbox/</automated>
  </verify>
  <acceptance_criteria>
    - `invariants.yaml` has a `WORLD` scope + four `INV-WORLD-*` entries with real summaries and ADR/one-pager origin.
    - `delta_parity_test.go` asserts manifest before/after versions EQUAL the row's version transition (not presence).
    - `writer_boundary_test.go` asserts raw world-SQL is denied outside the repo and the write is unreachable without the envelope seam.
    - `task test -- ./internal/world/outbox/` passes.
  </acceptance_criteria>
  <done>The WORLD scope + 4 invariants are registered; delta-parity + writer-boundary have genuine binding tests.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate all four binding tests, flip to bound, regenerate + confirm</name>
  <files>docs/architecture/invariants.yaml, docs/architecture/invariants.md, internal/world/outbox/delta_parity_test.go, internal/world/outbox/writer_boundary_test.go, internal/world/outbox/relay_test.go, test/integration/resilience/m2_dualwrite_test.go</files>
  <read_first>
    - .claude/rules/invariants.md (the binding step: annotate `// Verifies: INV-WORLD-<NAME>`, set binding: bound + asserted_by, regenerate `go run ./cmd/inv-render`, confirm the three registry meta-tests)
    - internal/world/outbox/relay_test.go (05-07 — the order + halt-on-poison test that proves INV-WORLD-FEED-ORDER)
    - test/integration/resilience/m2_dualwrite_test.go (05-06/05-08 — the atomicity test that proves INV-WORLD-ATOMIC-FEED)
  </read_first>
  <action>
    Add a `// Verifies: INV-WORLD-<NAME>` annotation immediately above the genuinely-asserting test/assertion block for each of the four: ATOMIC-FEED → the m2_dualwrite atomicity spec (state+envelope commit-or-rollback together); FEED-ORDER → the relay order + halt-on-poison test; DELTA-PARITY → `delta_parity_test.go`; WRITER-BOUNDARY → `writer_boundary_test.go`. In `invariants.yaml`, flip each of the four entries to `binding: bound` and add `asserted_by:` listing the exact verifying test file(s). Regenerate the human-readable view with `go run ./cmd/inv-render` (never hand-edit the generated regions of `invariants.md`). Confirm the registry meta-tests pass: `TestEveryRegistryInvariantHasBinding`, `TestProvenanceGuard`, `TestBoundInvariantsAreGenuinelyAsserted`. Because `WORLD` is a new scope not covered by the `docs/superpowers/specs/` orphan scan, this hand-registration is correct (orphan-check caveat).
  </action>
  <verify>
    <automated>task test -- -run 'TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted' ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four INV-WORLD-* is `binding: bound` with `asserted_by:` listing a real verifying test.
    - Each verifying test carries a `// Verifies: INV-WORLD-<NAME>` annotation and genuinely asserts the invariant (no Skip-only / presence-only binding).
    - `invariants.md` is regenerated (no stale diff under `inv-render -check`).
    - `TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted` pass.
  </acceptance_criteria>
  <done>All four INV-WORLD-* are bound, genuinely asserted, and the registry is regenerated + green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invariant claim → test | A bound invariant with a fake/weak test is a false-green (the INV-RB-3 bug class). |
| yaml → generated md | A stale invariants.md misrepresents the guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-35 | Repudiation | fabricated binding | high | mitigate | `TestBoundInvariantsAreGenuinelyAsserted` rejects Skip-only bindings; delta-parity asserts manifest EQUALS the delta (presence-insufficient), avoiding the false-green the invariant rule warns about. |
| T-05-36 | Tampering | stale invariants.md | low | mitigate | `inv-render` regenerates the md and CI runs generate-and-diff — a stale md fails. |
</threat_model>

<verification>
- `task test -- -run 'TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted' ./test/meta/` passes (run via `local-check`).
- `grep -c 'INV-WORLD' docs/architecture/invariants.yaml` returns at least 4.
- `go run ./cmd/inv-render -check` (or `task invariants:render` then `git diff --exit-code docs/architecture/invariants.md`) shows no stale diff.
</verification>

<success_criteria>
- The WORLD scope + four INV-WORLD-* invariants are registered AND bound (not pending).
- Each binding is a genuine assertion; the registry meta-tests are green.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-12-SUMMARY.md` when done.
</output>
