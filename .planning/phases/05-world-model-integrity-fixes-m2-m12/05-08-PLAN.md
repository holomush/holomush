---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 08
type: execute
wave: 7
depends_on: [05-07]
files_modified:
  - test/integration/resilience/m2_dualwrite_test.go
  - test/integration/resilience/outbox_faultinjection_test.go
  - test/integration/resilience/chaos_helpers_test.go
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "A NATS blip after commit cannot lose the notification: the relay redelivers the envelope after the broker recovers (M2 closed end-to-end)."
    - "Fault injection covers relay crash around PubAck (redeliver, consumer dedups on Nats-Msg-Id), dual relay (only one lease publishes), duplicate delivery (consumer idempotent), and broker downtime (relay backs off, resumes in order)."
    - "Per-aggregate concurrent-writer races surface the conflict across all four aggregates under two replicas / one broker / one shared DB."
  artifacts:
    - path: test/integration/resilience/outbox_faultinjection_test.go
      provides: "relay-crash-around-PubAck / dual-relay / duplicate-delivery / broker-downtime fault-injection specs"
      contains: "PubAck|dedup|lease"
  key_links:
    - from: test/integration/resilience/outbox_faultinjection_test.go
      to: internal/world/outbox/relay.go
      via: "exercises the relay under fault injection through the two-replica harness"
      pattern: "relay|outbox"
---

<objective>
Extend the permanent D-05 two-replica resilience suite with the outbox fault-injection matrix and per-aggregate race coverage, and complete the M2 end-to-end assertion (relay redelivers after a broker blip). This is the empirical enforcement layer the one-pager names: the resilience suite is the standing regression gate.

Purpose: 05-06 proved outbox atomicity at the DB layer; this plan proves the feed survives real broker chaos end-to-end — relay crash around PubAck, dual relay, duplicate delivery, broker downtime — and that the guard holds per-aggregate under two replicas. Without this, M2 is only half-closed (atomic write, but publish durability unproven).
Output: fault-injection specs + per-aggregate race + M2 redelivery assertion, all under the quarantine-gated resilience lane.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/testing.md
@.claude/rules/references/testing-detail.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Outbox fault-injection matrix (relay crash / dual relay / duplicate delivery / broker downtime)</name>
  <files>test/integration/resilience/outbox_faultinjection_test.go, test/integration/resilience/chaos_helpers_test.go</files>
  <read_first>
    - test/integration/resilience/chaos_helpers_test.go (startReplica ~172, pauseBroker/unpauseBroker ~235/250 — the fault-injection seams; the outbox-writer construction from 05-06/05-07)
    - internal/world/outbox/relay.go (05-07 — the relay under test: lease, PubAck marking, halt posture)
    - internal/testsupport/integrationtest/options.go (WithExternalNATS ~34, WithSharedDatabase ~52), plugins.go (WithInTreePlugins ~163)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md (Fault-injection needed; Codecov gotcha — resilience suites read as uncovered, hence the 05-07 non-quarantined seam test)
  </read_first>
  <action>
    Add `outbox_faultinjection_test.go` (Ginkgo, `//go:build integration`, under the existing D-05 `HOLOMUSH_RUN_QUARANTINED=1` gate) with four fault-injection specs: (1) relay crash around PubAck — kill the relay after publish but before it marks the row published; on restart it redelivers, and the reference consumer dedups on Nats-Msg-Id (no double effect). (2) dual relay — two relay instances contend for the lease; assert exactly one publishes (no duplicate positions on the wire). (3) duplicate delivery — deliver the same envelope twice; the consumer applies it once. (4) broker downtime — pause the broker mid-feed, assert the relay backs off and, on unpause, resumes publishing from the first unpublished position strictly in order. Reuse the existing harness seams (startReplica, pauseBroker/unpauseBroker); extend chaos_helpers only with what these need. Keep the quarantine gate and Ginkgo structure.
  </action>
  <verify>
    <automated>HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience ./test/integration/resilience/</automated>
  </verify>
  <acceptance_criteria>
    - Relay-crash-around-PubAck spec: envelope redelivered, consumer dedups, no double effect.
    - Dual-relay spec: exactly one lease publishes; no duplicate wire positions.
    - Duplicate-delivery spec: consumer applies once.
    - Broker-downtime spec: relay backs off and resumes in order after unpause.
    - The full resilience suite passes under `HOLOMUSH_RUN_QUARANTINED=1`.
  </acceptance_criteria>
  <done>The outbox fault-injection matrix is a standing regression gate.</done>
</task>

<task type="auto">
  <name>Task 2: M2 end-to-end redelivery + per-aggregate race coverage</name>
  <files>test/integration/resilience/m2_dualwrite_test.go</files>
  <read_first>
    - test/integration/resilience/m2_dualwrite_test.go (the 05-06 rewrite — currently asserts DB-layer atomicity; extend with relay redelivery)
    - internal/world/outbox/relay.go, consumer.go (05-07)
    - docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md (§2 M2 — a NATS blip after commit cannot lose the notification)
  </read_first>
  <action>
    Extend `m2_dualwrite_test.go` with the end-to-end assertion the 05-06 rewrite deferred: a MoveCharacter under a broker frozen mid-move commits state + outbox row, and after the broker recovers the relay publishes the move envelope — the notification is NOT lost (M2 closed end-to-end). Add per-aggregate concurrent-writer race coverage: for each of location, exit, character, object, two replicas over one broker + one shared DB racing a write surface the conflict (one commits, the other gets WORLD_CONCURRENT_EDIT). Update the M2-VERDICT log lines to state the notification survives the blip via relay redelivery.
  </action>
  <verify>
    <automated>HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience ./test/integration/resilience/</automated>
  </verify>
  <acceptance_criteria>
    - M2 spec asserts the move envelope is published after broker recovery (notification not lost).
    - Per-aggregate race specs cover all four aggregates and assert the conflict surfaces.
    - The resilience suite passes under `HOLOMUSH_RUN_QUARANTINED=1`.
  </acceptance_criteria>
  <done>M2 is closed end-to-end and the guard is proven per-aggregate under chaos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker blip → notification | The M2 failure mode: a post-commit publish lost on a broker flap. |
| relay crash → at-least-once | A crash around PubAck can double-deliver; the consumer must dedup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-24 | Denial of Service | broker downtime | high | mitigate | Broker-downtime spec proves the relay backs off and resumes in order — the notification is redelivered, not lost (M2 closed end-to-end). |
| T-05-25 | Tampering | dual relay | high | mitigate | Dual-relay spec asserts single-lease publish — no duplicate wire positions even with two relay instances. |
| T-05-26 | Spoofing | duplicate delivery | medium | mitigate | Duplicate-delivery + relay-crash specs prove the consumer dedups on Nats-Msg-Id (event ULID); effects apply once. |
</threat_model>

<verification>
- `HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience ./test/integration/resilience/` passes (run via `local-check`; note the ~30m budget — dispatch with a generous timeout).
- With the env var unset the suite skips (0 specs, exit 0) — it stays off the blocking PR lane.
</verification>

<success_criteria>
- The outbox fault-injection matrix + M2 end-to-end redelivery + per-aggregate races pass under the D-05 gate.
- M2 (MODEL-04) is closed end-to-end: a NATS blip after commit cannot lose the notification.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-08-SUMMARY.md` when done.
</output>
