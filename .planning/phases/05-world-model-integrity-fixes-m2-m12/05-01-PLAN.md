---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/store/migrations/000049_world_version_guard.up.sql
  - internal/store/migrations/000049_world_version_guard.down.sql
  - internal/world/location.go
  - internal/world/exit.go
  - internal/world/character.go
  - internal/world/object.go
  - internal/world/errors.go
  - internal/world/errors_test.go
autonomous: true
requirements: [MODEL-03]
must_haves:
  truths:
    - "The four world tables (locations, exits, characters, objects) carry a version column defaulting to 1 (per D-03 / MODEL-03)."
    - "The four Go world structs carry a Version field that transports the read version into a guarded write."
    - "A typed WORLD_CONCURRENT_EDIT error code exists as the phase's single conflict signal (D-02)."
  artifacts:
    - path: internal/store/migrations/000049_world_version_guard.up.sql
      provides: "version INTEGER NOT NULL DEFAULT 1 on locations/exits/characters/objects"
      contains: "version"
    - path: internal/store/migrations/000049_world_version_guard.down.sql
      provides: "reverses the version-column add"
    - path: internal/world/errors.go
      provides: "WORLD_CONCURRENT_EDIT typed error + sentinel"
      contains: "WORLD_CONCURRENT_EDIT"
  key_links:
    - from: internal/world/location.go
      to: internal/store/migrations/000049_world_version_guard.up.sql
      via: "Version struct field carries the row's version column value"
      pattern: "Version"
---

<objective>
Lay the version-guard foundation (MODEL-03, slice 1): the schema migration that adds the optimistic-concurrency `version` column to all four world tables, the `Version` field on the four Go structs, and the typed `WORLD_CONCURRENT_EDIT` error code that is the phase's single conflict signal. This is interface-first: every downstream slice-1 plan (repo CAS, RMW threading, M12 flip) builds against the column, the struct field, and the error code defined here.

Purpose: A version-predicated CAS write is impossible without a version column + a struct field to carry the read version + a distinct conflict code to surface. Those three contracts must exist before any repo can be rewritten.
Output: migration 000049 (paired up/down), `Version int` on Location/Exit/Character/Object, `WORLD_CONCURRENT_EDIT` error.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-CONTEXT.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/adr/holomush-i4784-world-state-model-decision.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/database-migrations.md
</context>

## Artifacts this phase produces (canonical registry — read by the drift pass)

Every NEW symbol/file Phase 5 creates across all 16 plans. Newly-created symbols listed here are excluded from source-grounding drift checks. (Revised 2026-07-12 for the cross-AI review — see 05-REVIEWS.md. **Round 1** added plan 05-14 and restructured the transaction/outbox/relay seams. **Round 2** redesigned the package/executor seam to break Go import cycles and the envelope-before-delta contradiction: the shared value types move to a cycle-neutral leaf package `internal/world/wmodel`, `mutate` returns the delta, and the relay/executor reach storage through consumer-owned interfaces. **Round 3** corrected four ownership contracts and one correctness gap: the WRITER owns envelope finalization (`WriteIntent`), the relay reaches storage through an explicit `Lease` abstraction with at-least-once semantics stated plainly, skip is a `SkipService` owning DB + publisher, the wave-1 caller inventory is mechanically complete, and — the substantive catch — ALL production character creation (registered + guest + bootstrap-admin) routes through one atomic `CharacterGenesisService` (new plan **05-15**), with a `world_genesis_checkpoint` table for durable genesis idempotency and a full-adjacency import guard + composition allowlist. **Round 5** (this revision) folds in the locked scope decisions D-06 + D-07 and six implementation findings: (a) D-06 adds the deletion-side counterpart to 05-15 — a `CharacterReapingService` (new plan **05-16**) routing guest reaping + failed-guest cleanup through a tombstone-emitting deletion; (b) D-07 REMOVES the vestigial world scene-participant WRITE surface (`world.Service.AddSceneParticipant`/`RemoveSceneParticipant` + `scene_repo.go`'s `public.scene_participants` writes) — the removal lands in 05-14 (it owns those files); (c) the `mutate` seam takes a WRITE CLOSURE that identifies + executes the operation (`mutate(ctx, intent, write func(ctx)(*wmodel.MutationDelta, error))`) — writer repos private to the executor (05-06); (d) a durable consumer idempotency table lands in 000050 (05-05) + the reference consumer applies it (05-07); (e) the location-delete cascade preselects cascaded exits so INV-WORLD-2 delta-parity holds (05-02); (f) the envelope→wire adapter is specified concretely (05-07); the movement hook fails as operational degradation not a post-commit command error (05-06); the whole-tree gate uses a new `task build:all` target, not raw `go build ./...` (05-14); and the delete zero-row classifier is simplified to two observable outcomes (05-02/05-03).)

**Migrations:** `000049_world_version_guard.{up,down}.sql` (this plan); `000050_world_outbox.{up,down}.sql` (05-05) — 000050 carries the persistent **feed-epoch** schema (round 2: a per-game `epoch` on `world_feed_counter` + an `epoch` column on `outbox` rows) AND the **`world_genesis_checkpoint`** table (round 3 MEDIUM: PK `(game_id, epoch, aggregate_type, aggregate_id)` — the durable genesis identity that survives outbox pruning; genesis re-runs at the same epoch skip on the PK before allocating a position).

**Cycle-neutral leaf types package `internal/world/wmodel` (round-2 redesign, created in 05-14; imports only stdlib + `internal/core`/`ulid` — imports NONE of `world`/`world/postgres`/`world/outbox`):** `MutationDelta` + `AffectedAggregate` (moved here from the round-1 `internal/world/mutation_delta.go`); `EnvelopeIntent` (the pre-write envelope shape — event ULID, kind, schema_version, actor, causation/correlation, primary aggregate id/type, intent-level new-values-only payload — **excludes** feed_position, epoch, and the affected-aggregates manifest); `Envelope` (the finalized persisted shape = intent + feed_position + epoch + the affected-aggregates manifest built from the `MutationDelta`); the pure `Finalize(intent, delta, epoch, position) *Envelope` constructor whose ONLY production caller is the postgres `WriteIntent` writer (round 3: the writer owns finalization). This package is what makes the graph acyclic: `world`, `world/postgres`, and `world/outbox` all depend on `wmodel` and never on each other for these types. Final import graph (drawn in 05-06/05-07, guarded by the FULL-adjacency meta-test in 05-07 Task 4 — round 3): `world → wmodel`; `postgres → world, wmodel`; `outbox → wmodel`; forbidden edges asserted on production imports: `world→outbox`, `world→postgres`, `outbox→postgres`, `outbox→world`, `postgres→outbox`, `wmodel→world`, `wmodel→postgres`, `wmodel→outbox`; PLUS the composition allowlist — only `cmd/holomush`, `internal/world/setup`, `internal/bootstrap/setup`, `internal/access/setup`, `internal/testsupport/**`, and `internal/world/postgres` itself may import `internal/world/postgres`.

**Schema columns/tables:** `version` column on `locations`/`exits`/`characters`/`objects`; new `outbox` table (with an `epoch` column); new `world_feed_counter` table (locked per-game counter, with an `epoch` column); new `world_genesis_checkpoint` table (round 3 — durable genesis identity); new **`world_consumer_checkpoint`** table (round 5, finding 2 — durable reference-consumer idempotency: PK `(consumer_name, event_id)` + a per-consumer `(epoch, feed_position)` watermark, so exactly-once visible effect survives consumer restart BEYOND JetStream's finite dedup window; the consumer applies its effect + records the row in ONE transaction — 000050 schema in 05-05). Its SQL lives in a new `internal/world/postgres/consumer_checkpoint_store.go` (writer boundary) reached through a consumer-owned `ConsumerCheckpointStore` interface declared in `internal/world/outbox` (05-07, injected — so `outbox` does not import `postgres`, the eight-edge guard stays green). Also a new `internal/world/outbox/relay.go` wire adapter serializing the whole `wmodel.Envelope` into `eventbus.Event.Payload` (round-5 finding 5).

**Go structs/fields:** `Version int` on `world.Location`, `world.Exit`, `world.Character`, `world.Object` (this plan). After a successful create/update the repo refreshes this field to the committed version so a reused struct does not carry a stale version (finding 12; populated in 05-02/05-03).

**Transaction & repository foundation (05-14):** re-entrant `Transactor.InTransaction` + the unexported re-entrant `withTx` seam in `internal/world/postgres`; `MutationDelta` + `AffectedAggregate` types (now in the leaf package `internal/world/wmodel`, round 2) that guarded writes return; read-only reader interface views (`LocationReader`/`ExitReader`/`ObjectReader`/`CharacterReader`/`SceneReader`); redesigned repo write interfaces (`Delete(ctx, id, expectedVersion) (*wmodel.MutationDelta, error)`, writes returning `(*wmodel.MutationDelta, error)`, **and `Move(ctx, objectID, to, expectedVersion)` + `UpdateLocation(ctx, characterID, locationID, expectedVersion)` — round-2 Antigravity: Move/UpdateLocation took no version-bearing struct, so CAS on the move/containment path had no parameter**); `PropertyRepository.Create`/`Update` converted from `r.pool.Exec` to `execerFromCtx` (round-2 property-leak finding — the round-1 sweep missed them); regenerated `worldtest/mock_*Repository.go`. **Interface blast radius (round 2, Codex):** the write-return redesign changes only the concrete `internal/world/postgres` repo methods + the `internal/world` interfaces; the auth-facing `auth.CharacterRepository` (a separate narrow interface, `character_service.go:17`) keeps `Create(...) error` and the production `CharRepoAdapter.Create` (`internal/bootstrap/setup/adapters.go:38`) **discards** the returned delta (`_, err := a.charRepo.Create(...)`) — so unrelated auth signatures do not change. Every compile-time caller of the redesigned methods is enumerated in 05-14.

**Error codes:** `WORLD_CONCURRENT_EDIT` (this plan); `WORLD_FEED_LOCK_TIMEOUT` (05-05 — the counter/mutation lock-timeout signal, round-2 MEDIUM); relay poison/halt codes + the typed stale-lease error (05-07).

**New package `internal/world/outbox` (behavior only — NO shared value types, which live in `wmodel`):** the single leased `relay`; the reference idempotent consumer + bootstrap harness; the `SkipService` (round 3 — owns the leased store AND the JetStream publisher: acquire fenced lease → validate halted row → publish same-position marker → after PubAck mark resolved → resume); genesis emission (checkpoint-idempotent) + feed epoch/reset; the taxonomy registry; subject builders relocated from the deleted `events.go`; non-quarantined seam tests. It declares the **consumer-owned `OutboxStore` interface — its ONLY method is `AcquireLease(ctx, gameID) (Lease, error)` — plus the `Lease` interface** (`Generation()` / `NextUnpublished` / `MarkPublished` / `Prune` / `MarkSkipResolved` / `CurrentEpoch` / `Release`), all relay DB ops living on the Lease so they structurally run on the lock-holding connection (round 3, blocker #2) — the concrete impl lives in `internal/world/postgres` and is injected by setup, so **`outbox` does NOT import `postgres`** (round 2, Codex second cycle). JetStream delivery is **at-least-once**: the generation fence rejects a stale holder's DB ack but cannot un-send wire traffic; `Nats-Msg-Id` dedup + the idempotent consumer are the wire-correctness mechanism (round 3 — no "split-brain impossible" claims). **All world-table + outbox/counter SQL (the outbox writer, the locked per-game counter allocator, the relay's next-unpublished SELECT / mark-published UPDATE / prune / skip-resolve / epoch read+advance, genesis + checkpoint inserts) lives in `internal/world/postgres`** (an outbox store), NOT in `internal/world/outbox` — keeping the writer boundary at the repo package (Codex finding 6).

**Service seam (round-2 redesign, round-3 ownership correction, round-5 finding-1 operation-identity fix — the load-bearing seam):** the compile-time write-requires-envelope executor in package `world` (05-06). `world.Service` holds reader views + a write executor; the writer repos are PRIVATE fields on the executor. **Round-5 finding 1 (the `mutate` seam must identify AND execute the operation):** the round-4 shape `mutate(ctx, entity, expectedVersion, intent)` carried no operation selector or callback — an `entity + version` cannot distinguish create/update/delete/move/preference, so it would collapse to runtime string-dispatch on `intent.kind`. The corrected seam takes a **WRITE CLOSURE** that performs the actual guarded repo write, plus the envelope **INTENT (not a finished envelope)**, and RETURNS the delta: `mutate(ctx, intent wmodel.EnvelopeIntent, write func(ctx context.Context) (*wmodel.MutationDelta, error)) (*wmodel.MutationDelta, error)`. The executor wraps, in one re-entrant `Transactor.InTransaction`: (1) `delta := write(txCtx)` — the closure runs ONE guarded writer-repo method (the closures are constructed by the executor's per-operation methods, which own the private writer repos, so a caller cannot supply arbitrary SQL and the operation is unambiguously identified by which method built the closure — Codex's "keep writer repositories private to the executor"); (2) `OutboxWriter.WriteIntent(txCtx, intent, delta)` — the injected **`world.OutboxWriter` interface** (declared in package `world`): **`WriteIntent(ctx, intent wmodel.EnvelopeIntent, delta *wmodel.MutationDelta) (*wmodel.Envelope, error)`** — the concrete `internal/world/postgres` impl allocates epoch/feed_position from the locked counter (late), FINALIZES via `wmodel.Finalize`, persists, and RETURNS the finalized envelope (round 3, blocker #1: the executor never finalizes — a fully-finalized `Envelope` cannot exist before the writer allocates its storage-owned fields; each envelope field has exactly one owner: intent ← caller, delta ← the closure's repo write, epoch/position ← writer). Because the seam names `wmodel` types, a `world`-owned interface, and a closure returning a `wmodel` type — never `outbox` or `postgres` — package `world` imports NEITHER `outbox` NOR `postgres`, dissolving the round-1 `world → outbox → world` cycle, the envelope-before-delta temporal contradiction, and the round-3 epoch/position ownership contradiction. An intent-less or direct-repo write still does not type-check (the intent + closure are non-optional).

**Taxonomy + enforcement:** versioned taxonomy schema registry (~15–20 envelope kinds, `App-Schema-Version`) = ARCH-04 input; the world-envelope census meta-test (explicit descriptor-set bijection + go/ast cross-check; per round 3 its completeness claim is PAIRED with the reader-view fence, the AST SQL fence, the import-graph composition allowlist, and the 05-15 interface `Create` removal — the bijection alone proves registry consistency, not write-path discovery); the raw-world-SQL **AST/token meta-test** in `test/meta/` that scans production Go for world-table mutation SQL outside `internal/world/postgres` (NOT depguard — depguard matches imported packages, not SQL literals; Codex finding 6); the **full-adjacency import-graph meta-test** (05-07 Task 4 — 8 forbidden edges + the composition allowlist).

**Invariants (new `WORLD` scope in `docs/architecture/invariants.yaml`) — canonical numeric IDs (the parser only recognizes `INV-<SCOPE>-<number>`, `invariant_registry_test.go:163`; Codex finding 3):**
- `INV-WORLD-1` — ATOMIC-FEED (state change + its one envelope commit atomically in the same tx).
- `INV-WORLD-2` — DELTA-PARITY (the affected-aggregates manifest matches the committed delta — before/after versions equal the row's transition).
- `INV-WORLD-3` — FEED-ORDER (relay publishes strictly in feed_position order, gap-free; poison halts).
- `INV-WORLD-4` — WRITER-BOUNDARY (the enforceable boundary, round-3 reconciled, round-5 D-06 extended: world-table mutation SQL is confined to `internal/world/postgres` by the AST fence AND `world.Service` exposes no directly-callable writer repo by the reader-view compile fence AND only composition/test packages may import `internal/world/postgres` per the 05-07 allowlist; the write executor is the sole in-`world` write owner, and there are exactly **TWO sanctioned out-of-`world` writers**, both application services constructed only at composition roots: the **atomic `CharacterGenesisService` (05-15)** on the CREATION side (registered + guest + bootstrap-admin character creation all emit their genesis envelope atomically — round 3, blocker #5), and the **atomic `CharacterReapingService` (05-16)** on the DELETION side (guest reaping + failed-guest cleanup emit one character tombstone envelope per reaped character atomically — round 5, D-06: guest reaping/cleanup previously deleted characters via an FK cascade with no tombstone). Both reach persistence only through `OutboxWriter.WriteIntent` + the guarded `internal/world/postgres` writer repos. Bound by the AST SQL fence (05-09, covering `entity_properties`), the reader-view test (05-11/05-12), AND the import-graph allowlist meta-test (05-07/05-12).
The ADR/one-pager symbolic names (`INV-WORLD-ATOMIC-FEED` etc.) are preserved as the registry `summary`/`legacy:` provenance, not as parser IDs. **INV-WORLD-1 (ATOMIC-FEED) binds to the ALWAYS-RUN STATE+ENVELOPE atomicity test in `internal/world/postgres/outbox_store_test.go` (the normal `task test:int` Integration Test lane), NOT the opt-in resilience suite that skips unless `HOLOMUSH_RUN_QUARANTINED=1` (round-2 MEDIUM); per round 3 the binding test mutates a REAL world row + writes its envelope in one tx (rollback → neither; commit → both; forced outbox failure → state rolls back) — an envelope-only rollback test is an insufficient binding. The resilience spec is supplemental evidence.**

**Relay operational model (05-07, round-3 hardened):** a session-level PostgreSQL advisory lock (single-lease, keyed by game) on a dedicated connection, reached ONLY through the **`Lease` abstraction** — `OutboxStore.AcquireLease(ctx, gameID) (Lease, error)`; every relay DB op (next-unpublished SELECT, mark-published UPDATE, prune, skip-resolve, epoch read) is a `Lease` method bound to the lock-holding connection, with `Generation()` fencing on DB acks (round 3, blocker #2: pool-shaped store methods could not prove connection binding). **At-least-once stated plainly:** the fence rejects a stale holder's DB ack but cannot un-send an already-issued NATS message — `Nats-Msg-Id` dedup + the idempotent consumer are the wire-correctness mechanism. A dedicated `LISTEN` connection + transaction-side `pg_notify`; a periodic sweep fallback; the **`SkipService`** (round 3, blocker #3 — owns the leased store AND the JetStream publisher, because skip must publish a same-feed-position operator-authorized MARKER preserving gap-free order, INV-WORLD-3, before the row is marked resolved); a `holomush outbox skip` **admin CLI subcommand constructed with DB + EventBus config** that drives the SkipService (round-3: a DB-only CLI cannot publish the marker); halt-position health metrics; the `OutboxRelaySubsystem` + its `lifecycle.SubsystemID`; the `cmd/holomush/core.go` composition-root wiring.

**Atomic character creation (round 3, blocker #5 — supersedes the round-2 CoreServer `WithOutboxWriter` design):** a new **`auth.CharacterGenesisService`** (`internal/auth/character_genesis.go`, plan 05-15) owns character insert + optional player↔character binding + outbox genesis envelope in ONE re-entrant transaction, fail-closed (nil deps reject; the `createCharacterAtomic` non-transactional fallback at `auth_handlers.go:521-526` is DELETED). ALL THREE production creation paths route through it: registered (gRPC → `CharacterService.CreateBound`, bind reason `initial_bind`), guest (`GuestService.CreateGuest`, bind reason `initial_bind_guest`, enrolled in its player-create tx), bootstrap-admin (`bootstrap.SeedAdmin` → `CharacterService.Create`, NO binding — behavior preserved). `Create` is REMOVED from `auth.CharacterRepository`, `GuestCharacterRepository`, and `CharRepoAdapter` (the compile-level fence; the wave-1 delta-discarding `CharRepoAdapter.Create` bridge in 05-14 is deliberately temporary). No `CoreServer` `outboxWriter` field exists — the writer lives inside the service, constructed at `cmd/holomush/sub_grpc.go` and `internal/bootstrap/setup/subsystem.go`. The persistent feed-epoch schema lives in migration 000050 with an `internal/world/postgres` epoch read/advance store method that genesis/reset (05-11) drives; genesis-snapshot idempotency is keyed by the `world_genesis_checkpoint` table.

**Deletions (not new symbols):** `internal/world/events.go` (whole file), `internal/world/event_store_adapter.go`, `s.eventEmitter`/`EmitMoveEvent`/`EVENT_EMITTER_MISSING`/`EVENT_EMIT_FAILED`, the `go-retry` import in `events.go`. (Before deleting `EmitExamineEvent`, 05-06 audits for `events.*.examine.*` subscribers — Antigravity finding 16.) **Round-5 D-07 removal (vestigial world scene-participant WRITE surface):** `world.Service.AddSceneParticipant` / `world.Service.RemoveSceneParticipant`; `SceneRepository.AddParticipant` / `SceneRepository.RemoveParticipant` (interface + `internal/world/postgres/scene_repo.go` impls, incl. their `INSERT INTO scene_participants` / `DELETE FROM scene_participants` writes); and these two methods drop out of the regenerated `mock_SceneRepository`. Removed in 05-14 (it owns those files) after a grep confirms only `_test.go` files reference them (verified: no production callers outside `internal/world`). The scene READ surface (`ListParticipants` / `GetScenesFor`) and the `public.scene_participants` TABLE are KEPT — the reads still SELECT/JOIN the table, so the physical table DROP is DEFERRED to issue #4815 (D-07 fallback: remove the Go write surface now, defer the DROP when a read dependency remains). `plugins/core-scenes` and its `plugin_core_scenes.scene_participants` schema are UNTOUCHED (separate table + schema). Because these commands no longer exist, the census bijection (D-01) has nothing to exempt and the D-01↔D-05 scene-participant contradiction disappears (round-5 D-07).

**Atomic guest character DELETION (round-5 D-06, plan 05-16 — the deletion-side counterpart to 05-15):** a new `auth.CharacterReapingService` (`internal/auth/character_reaping.go`) that, for each character of a reaped/cleaned-up guest player, deletes it via the guarded world `CharacterWriter.Delete` (tombstone `wmodel.MutationDelta`) AND emits one character delete/tombstone envelope (the SAME kind DeleteCharacter emits — no new kind) via `world.OutboxWriter.WriteIntent` in ONE re-entrant tx, then deletes the player (own pool, ordered AFTER the tombstones so the `characters.player_id ON DELETE CASCADE` removes no un-tombstoned character). It satisfies `auth.GuestCleaner` (injected into the guest reaper at `cmd/holomush/sub_grpc.go`) and backs `guest_service.go` `cleanupGuestPlayer`. It is the SECOND sanctioned out-of-world writer under INV-WORLD-4 (deletion side); orphan-player is documented compensation outside the invariant. A regression test proves guest expiration cannot produce genesis-without-tombstone feed history.

<tasks>

<task type="auto">
  <name>Task 1: Migration 000049 — version column on the four world tables</name>
  <files>internal/store/migrations/000049_world_version_guard.up.sql, internal/store/migrations/000049_world_version_guard.down.sql</files>
  <read_first>
    - internal/store/migrations/000001_baseline.up.sql (lines ~255-283 — the access_policies version precedent to mirror: `version INTEGER NOT NULL DEFAULT 1`)
    - internal/store/migrations/000048_disable_unconditional_scene_read_seed.up.sql (naming/format of the current highest migration; confirms 000049 is next)
    - .claude/rules/database-migrations.md (paired up/down, idempotent IF EXISTS, no triggers/functions, default satisfies non-null)
  </read_first>
  <action>
    Create the paired migration numbered 000049 named `world_version_guard`. The up migration adds a `version` column of type INTEGER, NOT NULL, DEFAULT 1, using `ADD COLUMN IF NOT EXISTS`, to each of the four world tables: `locations`, `exits`, `characters`, `objects` (exactly these four — NOT `entity_properties`, per one-pager §1). DEFAULT 1 backfills every existing row atomically so no data-migration step is required. The down migration drops the `version` column from all four tables using `DROP COLUMN IF EXISTS`, in reverse table order. Both files carry the SPDX header used by sibling migrations. No triggers, functions, or backfill jobs — logic stays in Go.
  </action>
  <verify>
    <automated>test -f internal/store/migrations/000049_world_version_guard.up.sql && test -f internal/store/migrations/000049_world_version_guard.down.sql && grep -q 'version' internal/store/migrations/000049_world_version_guard.up.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both `000049_world_version_guard.up.sql` and `.down.sql` exist.
    - The up file adds a NOT NULL DEFAULT 1 integer `version` column to `locations`, `exits`, `characters`, `objects` (four ADD COLUMN statements, each guarded IF NOT EXISTS).
    - The down file drops `version` from all four tables (IF EXISTS).
    - `entity_properties` is NOT touched.
    - Migration applies and reverts cleanly under `task test:int` (integration suite runs migrations against a fresh DB).
  </acceptance_criteria>
  <done>The four world tables gain an optimistic-concurrency version column; the migration is idempotent and reversible.</done>
</task>

<task type="auto">
  <name>Task 2: Version field on the four world structs</name>
  <files>internal/world/location.go, internal/world/exit.go, internal/world/character.go, internal/world/object.go</files>
  <read_first>
    - internal/world/location.go (Location struct ~48-58 and NewLocationWithID ~68-76 — struct shape + constructor)
    - internal/world/exit.go, internal/world/character.go, internal/world/object.go (the sibling struct shapes)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md ("Version guard — struct field" section)
  </read_first>
  <action>
    Add an exported `Version int` field to each of the four structs `Location`, `Exit`, `Character`, `Object`. Document it as (a) the read version carried back into a guarded write and (b) the field the repo refreshes to the committed version after a successful create/update, so a struct reused for a later write does not carry a stale version and spuriously conflict (review finding 12 — the refresh itself lands in 05-02/05-03). New entities receive `version=1` from the DB default, so constructors (e.g. `NewLocationWithID`) MUST NOT hand-set `Version` on create — it is populated only by read-path scans and post-write refresh (added in the repo plans 05-02/05-03). Do not modify any scan function in this plan (those live in `internal/world/postgres/*_repo.go`, owned by 05-02/05-03) — this plan only adds the struct field.
  </action>
  <verify>
    <automated>task test -- ./internal/world/</automated>
  </verify>
  <acceptance_criteria>
    - Each of Location/Exit/Character/Object has a `Version int` field.
    - No constructor hand-sets `Version`.
    - `task test -- ./internal/world/` compiles and passes (existing unit tests unaffected; Version defaults to zero value until scans populate it in later plans).
  </acceptance_criteria>
  <done>The four structs can transport a read version; the field is inert until repo scans populate it in 05-02/05-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WORLD_CONCURRENT_EDIT typed error code</name>
  <files>internal/world/errors.go, internal/world/errors_test.go</files>
  <read_first>
    - internal/world/errors.go (existing error sentinels + oops.Code convention in this package)
    - internal/world/postgres/location_repo.go (lines 37, 82 — the `oops.Code("LOCATION_NOT_FOUND").With("id", ...).Wrap(world.ErrNotFound)` style to mirror)
    - .claude/rules/references/plan-review-learnings.md ("errutil.AssertErrorCode" convention; `oops.Code(...).Is` matches any OopsError — assert code identity, not chain-walk)
  </read_first>
  <behavior>
    - A `world.ErrConcurrentEdit` sentinel exists; an error wrapping it via `oops.Code("WORLD_CONCURRENT_EDIT")` reports code `WORLD_CONCURRENT_EDIT` under `errutil.AssertErrorCode`.
    - The conflict error is distinct from `world.ErrNotFound` — `errors.Is(concurrentEditErr, world.ErrNotFound)` is false.
  </behavior>
  <action>
    Add a `WORLD_CONCURRENT_EDIT` conflict error to `internal/world/errors.go`: an exported sentinel (e.g. `ErrConcurrentEdit`) plus the constant code string `WORLD_CONCURRENT_EDIT`, following the existing sentinel + `oops.Code(...).Wrap(...)` idiom in the package. This is the single typed conflict signal the guarded repos will emit (05-02/05-03) and the `world.Service` boundary will propagate unchanged (D-02 — no UX mapping in this phase). Write the RED test in `errors_test.go` first: assert `errutil.AssertErrorCode` reports the code and that the conflict sentinel is NOT `world.ErrNotFound`. Keep the literal code string out of any non-test comment body.
  </action>
  <verify>
    <automated>task test -- ./internal/world/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/world/errors.go` exports a concurrent-edit sentinel and defines the `WORLD_CONCURRENT_EDIT` code.
    - `errors_test.go` asserts the code via `errutil.AssertErrorCode` and asserts the conflict is distinct from `ErrNotFound`.
    - `task test -- ./internal/world/` passes.
  </acceptance_criteria>
  <done>The phase has one typed conflict signal, code-asserted, distinct from not-found.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live schema | An operator applies 000049 to a running DB; a bad column default could reject writes or corrupt existing rows. |
| conflict error → caller | The conflict code crosses the world.Service boundary to callers; it must not leak internal state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | 000049 up migration | low | mitigate | DEFAULT 1 backfills existing rows in place (no NOT NULL-without-default lock storm); idempotent ADD COLUMN IF NOT EXISTS is safe to re-run. |
| T-05-02 | Tampering | version column | medium | mitigate | Column is NOT NULL DEFAULT 1 so no row can exist without a version; the actual CAS enforcement lands in 05-02/05-03 — this plan only establishes the invariant-carrying column. |
| T-05-03 | Information Disclosure | WORLD_CONCURRENT_EDIT error | low | accept | The conflict code is intentionally generic (no row contents in the code/sentinel); `.With("id", ...)` metadata is server-side context, not wire payload. |
</threat_model>

<verification>
- `task test -- ./internal/world/` passes (struct + error changes compile).
- `task test:int` applies and reverts migration 000049 cleanly (run via the `local-check` agent; integration suite runs migrations).
- `grep 'version' internal/store/migrations/000049_world_version_guard.up.sql` confirms the column add.
</verification>

<success_criteria>
- Migration 000049 adds `version INTEGER NOT NULL DEFAULT 1` to exactly the four world tables and reverses cleanly.
- Location/Exit/Character/Object each carry `Version int`.
- `WORLD_CONCURRENT_EDIT` exists and is code-asserted in a test.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-01-SUMMARY.md` when done.
</output>
