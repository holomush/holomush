---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/store/migrations/000049_world_version_guard.up.sql
  - internal/store/migrations/000049_world_version_guard.down.sql
  - internal/world/location.go
  - internal/world/exit.go
  - internal/world/character.go
  - internal/world/object.go
  - internal/world/errors.go
  - internal/world/errors_test.go
autonomous: true
requirements: [MODEL-03]
must_haves:
  truths:
    - "The four world tables (locations, exits, characters, objects) carry a version column defaulting to 1 (per D-03 / MODEL-03)."
    - "The four Go world structs carry a Version field that transports the read version into a guarded write."
    - "A typed WORLD_CONCURRENT_EDIT error code exists as the phase's single conflict signal (D-02)."
  artifacts:
    - path: internal/store/migrations/000049_world_version_guard.up.sql
      provides: "version INTEGER NOT NULL DEFAULT 1 on locations/exits/characters/objects"
      contains: "version"
    - path: internal/store/migrations/000049_world_version_guard.down.sql
      provides: "reverses the version-column add"
    - path: internal/world/errors.go
      provides: "WORLD_CONCURRENT_EDIT typed error + sentinel"
      contains: "WORLD_CONCURRENT_EDIT"
  key_links:
    - from: internal/world/location.go
      to: internal/store/migrations/000049_world_version_guard.up.sql
      via: "Version struct field carries the row's version column value"
      pattern: "Version"
---

<objective>
Lay the version-guard foundation (MODEL-03, slice 1): the schema migration that adds the optimistic-concurrency `version` column to all four world tables, the `Version` field on the four Go structs, and the typed `WORLD_CONCURRENT_EDIT` error code that is the phase's single conflict signal. This is interface-first: every downstream slice-1 plan (repo CAS, RMW threading, M12 flip) builds against the column, the struct field, and the error code defined here.

Purpose: A version-predicated CAS write is impossible without a version column + a struct field to carry the read version + a distinct conflict code to surface. Those three contracts must exist before any repo can be rewritten.
Output: migration 000049 (paired up/down), `Version int` on Location/Exit/Character/Object, `WORLD_CONCURRENT_EDIT` error.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-CONTEXT.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/adr/holomush-i4784-world-state-model-decision.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/database-migrations.md
</context>

## Artifacts this phase produces (canonical registry — read by the drift pass)

Every NEW symbol/file Phase 5 creates across all 14 plans. Newly-created symbols listed here are excluded from source-grounding drift checks. (Revised 2026-07-12 for the cross-AI review — see 05-REVIEWS.md. **Round 1** added plan 05-14 and restructured the transaction/outbox/relay seams. **Round 2** redesigned the package/executor seam to break Go import cycles and the envelope-before-delta contradiction: the shared value types move to a cycle-neutral leaf package `internal/world/wmodel`, `mutate` returns the delta and finalizes the envelope AFTER the write, and the relay/executor reach storage through consumer-owned interfaces — see the round-2 notes below.)

**Migrations:** `000049_world_version_guard.{up,down}.sql` (this plan); `000050_world_outbox.{up,down}.sql` (05-05) — 000050 also carries the persistent **feed-epoch** schema (round 2, Codex "epoch not wired": a per-game `epoch` on `world_feed_counter` + an `epoch` column on `outbox` rows).

**Cycle-neutral leaf types package `internal/world/wmodel` (round-2 redesign, created in 05-14; imports only stdlib + `internal/core`/`ulid` — imports NONE of `world`/`world/postgres`/`world/outbox`):** `MutationDelta` + `AffectedAggregate` (moved here from the round-1 `internal/world/mutation_delta.go`); `EnvelopeIntent` (the pre-write envelope shape — event ULID, kind, schema_version, actor, causation/correlation, primary aggregate id/type, intent-level new-values-only payload — **excludes** feed_position, epoch, and the affected-aggregates manifest); `Envelope` (the finalized persisted shape = intent + feed_position + epoch + the affected-aggregates manifest built from the `MutationDelta`). This package is what makes the graph acyclic: `world`, `world/postgres`, and `world/outbox` all depend on `wmodel` and never on each other for these types. Final import graph (drawn in 05-06/05-07, guarded by a meta-test in 05-07): `world → wmodel`; `postgres → world, wmodel`; `outbox → wmodel`; NONE of `world→outbox`, `world→postgres`, `outbox→postgres`, `outbox→world`, `postgres→outbox`.

**Schema columns/tables:** `version` column on `locations`/`exits`/`characters`/`objects`; new `outbox` table (with an `epoch` column); new `world_feed_counter` table (locked per-game counter, with an `epoch` column).

**Go structs/fields:** `Version int` on `world.Location`, `world.Exit`, `world.Character`, `world.Object` (this plan). After a successful create/update the repo refreshes this field to the committed version so a reused struct does not carry a stale version (finding 12; populated in 05-02/05-03).

**Transaction & repository foundation (05-14):** re-entrant `Transactor.InTransaction` + the unexported re-entrant `withTx` seam in `internal/world/postgres`; `MutationDelta` + `AffectedAggregate` types (now in the leaf package `internal/world/wmodel`, round 2) that guarded writes return; read-only reader interface views (`LocationReader`/`ExitReader`/`ObjectReader`/`CharacterReader`/`SceneReader`); redesigned repo write interfaces (`Delete(ctx, id, expectedVersion) (*wmodel.MutationDelta, error)`, writes returning `(*wmodel.MutationDelta, error)`, **and `Move(ctx, objectID, to, expectedVersion)` + `UpdateLocation(ctx, characterID, locationID, expectedVersion)` — round-2 Antigravity: Move/UpdateLocation took no version-bearing struct, so CAS on the move/containment path had no parameter**); `PropertyRepository.Create`/`Update` converted from `r.pool.Exec` to `execerFromCtx` (round-2 property-leak finding — the round-1 sweep missed them); regenerated `worldtest/mock_*Repository.go`. **Interface blast radius (round 2, Codex):** the write-return redesign changes only the concrete `internal/world/postgres` repo methods + the `internal/world` interfaces; the auth-facing `auth.CharacterRepository` (a separate narrow interface, `character_service.go:17`) keeps `Create(...) error` and the production `CharRepoAdapter.Create` (`internal/bootstrap/setup/adapters.go:38`) **discards** the returned delta (`_, err := a.charRepo.Create(...)`) — so unrelated auth signatures do not change. Every compile-time caller of the redesigned methods is enumerated in 05-14.

**Error codes:** `WORLD_CONCURRENT_EDIT` (this plan); `WORLD_FEED_LOCK_TIMEOUT` (05-05 — the counter/mutation lock-timeout signal, round-2 MEDIUM); relay poison/halt codes (05-07).

**New package `internal/world/outbox` (behavior only — NO shared value types, which live in `wmodel`):** the single leased `relay`; the reference idempotent consumer + bootstrap harness; genesis emission + feed epoch/reset; the taxonomy registry; subject builders relocated from the deleted `events.go`; non-quarantined seam tests. It declares the **consumer-owned `OutboxStore` interface** the relay calls (`NextUnpublished` / `MarkPublished` / `Prune` / `Skip` / `CurrentEpoch` / `AdvanceEpoch` / genesis reads) — the concrete impl lives in `internal/world/postgres` and is injected by setup, so **`outbox` does NOT import `postgres`** (round 2, Codex second cycle). **All world-table + outbox/counter SQL (the outbox writer, the locked per-game counter allocator, the relay's next-unpublished SELECT / mark-published UPDATE / prune / skip-marker / epoch read+advance, genesis inserts) lives in `internal/world/postgres`** (an outbox store), NOT in `internal/world/outbox` — keeping the writer boundary at the repo package (Codex finding 6).

**Service seam (round-2 redesign — the load-bearing fix):** the compile-time write-requires-envelope executor in package `world` (05-06). `world.Service` holds reader views + a write executor. The seam takes an **envelope INTENT (not a finished envelope)** and RETURNS the delta: `mutate(ctx, entity, expectedVersion, intent wmodel.EnvelopeIntent) (*wmodel.MutationDelta, error)` — the executor runs the guarded repo write (which produces the delta), FINALIZES the `wmodel.Envelope` from that returned delta (manifest = the rows the repo actually touched) + a late-allocated feed_position, then persists it through the injected **`world.OutboxWriter` interface** (declared in package `world`, `WriteEnvelope(ctx, *wmodel.Envelope) error`; concrete impl in `internal/world/postgres`, injected by setup). Because the seam names `wmodel` types and a `world`-owned interface — never `outbox` or `postgres` — package `world` imports NEITHER `outbox` NOR `postgres`, dissolving the round-1 `world → outbox → world` cycle and the envelope-before-delta temporal contradiction. An envelope-less (intent-less) or direct-repo write still does not type-check.

**Taxonomy + enforcement:** versioned taxonomy schema registry (~15–20 envelope kinds, `App-Schema-Version`) = ARCH-04 input; the world-envelope census meta-test (structural, derived from the real command surface via `go/ast`/`go/types`); the raw-world-SQL **AST/token meta-test** in `test/meta/` that scans production Go for world-table mutation SQL outside `internal/world/postgres` (NOT depguard — depguard matches imported packages, not SQL literals; Codex finding 6).

**Invariants (new `WORLD` scope in `docs/architecture/invariants.yaml`) — canonical numeric IDs (the parser only recognizes `INV-<SCOPE>-<number>`, `invariant_registry_test.go:163`; Codex finding 3):**
- `INV-WORLD-1` — ATOMIC-FEED (state change + its one envelope commit atomically in the same tx).
- `INV-WORLD-2` — DELTA-PARITY (the affected-aggregates manifest matches the committed delta — before/after versions equal the row's transition).
- `INV-WORLD-3` — FEED-ORDER (relay publishes strictly in feed_position order, gap-free; poison halts).
- `INV-WORLD-4` — WRITER-BOUNDARY (the enforceable boundary, round-2 reconciled: world-table mutation SQL is confined to `internal/world/postgres` by the AST fence AND `world.Service` exposes no directly-callable writer repo by the reader-view compile fence; the write executor is the sole in-`world` write owner, and the one sanctioned out-of-`world` writer — CreateCharacter's registration-flow write — stays inside the repo package and emits its genesis envelope atomically). Bound by BOTH the AST SQL fence (05-09, now covering `entity_properties`) and the reader-view test (05-11/05-12).
The ADR/one-pager symbolic names (`INV-WORLD-ATOMIC-FEED` etc.) are preserved as the registry `summary`/`legacy:` provenance, not as parser IDs. **INV-WORLD-1 (ATOMIC-FEED) binds to the ALWAYS-RUN outbox transaction-rollback atomicity test in `internal/world/postgres/outbox_store_test.go` (the normal `task test:int` Integration Test lane), NOT the opt-in resilience suite that skips unless `HOLOMUSH_RUN_QUARANTINED=1` (round-2 MEDIUM — a bound-but-never-run invariant is a false gate); the resilience spec is supplemental evidence.**

**Relay operational model (05-07, round-2 hardened):** a session-level PostgreSQL advisory lock (single-lease, keyed by game) on a dedicated connection with fencing/reacquire on connection loss; **the relay's DB queries (next-unpublished SELECT, mark-published UPDATE, prune, skip-marker) run through the dedicated advisory-lock lease connection (NOT the general `r.pool`), plus a fencing generation checked in the publish/ack path — so a partition that drops the lease connection cannot double-publish before the process notices (round-2 split-brain finding)**; a dedicated `LISTEN` connection + transaction-side `pg_notify`; a periodic sweep fallback; **a same-feed-position operator skip/recovery affordance — it publishes an operator-authorized poison/skip MARKER at the poison row's OWN feed_position (preserving gap-free order, INV-WORLD-3) BEFORE marking the row published, NOT a silent `published_at` bump that would leave a wire gap (round-2 finding: the round-1 skip as drawn violated INV-WORLD-3)**; a `holomush outbox skip` **admin CLI subcommand** that invokes the skip (so operators clear a poison halt without raw SQL — round-2 finding: skip had DB logic but no caller); halt-position health metrics; the `OutboxRelaySubsystem` + its `lifecycle.SubsystemID`; the `cmd/holomush/core.go` composition-root wiring.

**End-to-end wiring (round 2, Codex):** CreateCharacter same-tx genesis emission threads a `world.OutboxWriter` through `CoreServer` (a new field on `internal/grpc/server.go:152` + a `WithOutboxWriter` `CoreServerOption` in `internal/grpc/auth_handlers.go` + assembly at `cmd/holomush/sub_grpc.go:477`); the persistent feed-epoch schema lives in migration 000050 with an `internal/world/postgres` epoch read/advance store method that genesis/reset (05-11) drives.

**Deletions (not new symbols):** `internal/world/events.go` (whole file), `internal/world/event_store_adapter.go`, `s.eventEmitter`/`EmitMoveEvent`/`EVENT_EMITTER_MISSING`/`EVENT_EMIT_FAILED`, the `go-retry` import in `events.go`. (Before deleting `EmitExamineEvent`, 05-06 audits for `events.*.examine.*` subscribers — Antigravity finding 16.)

<tasks>

<task type="auto">
  <name>Task 1: Migration 000049 — version column on the four world tables</name>
  <files>internal/store/migrations/000049_world_version_guard.up.sql, internal/store/migrations/000049_world_version_guard.down.sql</files>
  <read_first>
    - internal/store/migrations/000001_baseline.up.sql (lines ~255-283 — the access_policies version precedent to mirror: `version INTEGER NOT NULL DEFAULT 1`)
    - internal/store/migrations/000048_disable_unconditional_scene_read_seed.up.sql (naming/format of the current highest migration; confirms 000049 is next)
    - .claude/rules/database-migrations.md (paired up/down, idempotent IF EXISTS, no triggers/functions, default satisfies non-null)
  </read_first>
  <action>
    Create the paired migration numbered 000049 named `world_version_guard`. The up migration adds a `version` column of type INTEGER, NOT NULL, DEFAULT 1, using `ADD COLUMN IF NOT EXISTS`, to each of the four world tables: `locations`, `exits`, `characters`, `objects` (exactly these four — NOT `entity_properties`, per one-pager §1). DEFAULT 1 backfills every existing row atomically so no data-migration step is required. The down migration drops the `version` column from all four tables using `DROP COLUMN IF EXISTS`, in reverse table order. Both files carry the SPDX header used by sibling migrations. No triggers, functions, or backfill jobs — logic stays in Go.
  </action>
  <verify>
    <automated>test -f internal/store/migrations/000049_world_version_guard.up.sql && test -f internal/store/migrations/000049_world_version_guard.down.sql && grep -q 'version' internal/store/migrations/000049_world_version_guard.up.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both `000049_world_version_guard.up.sql` and `.down.sql` exist.
    - The up file adds a NOT NULL DEFAULT 1 integer `version` column to `locations`, `exits`, `characters`, `objects` (four ADD COLUMN statements, each guarded IF NOT EXISTS).
    - The down file drops `version` from all four tables (IF EXISTS).
    - `entity_properties` is NOT touched.
    - Migration applies and reverts cleanly under `task test:int` (integration suite runs migrations against a fresh DB).
  </acceptance_criteria>
  <done>The four world tables gain an optimistic-concurrency version column; the migration is idempotent and reversible.</done>
</task>

<task type="auto">
  <name>Task 2: Version field on the four world structs</name>
  <files>internal/world/location.go, internal/world/exit.go, internal/world/character.go, internal/world/object.go</files>
  <read_first>
    - internal/world/location.go (Location struct ~48-58 and NewLocationWithID ~68-76 — struct shape + constructor)
    - internal/world/exit.go, internal/world/character.go, internal/world/object.go (the sibling struct shapes)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md ("Version guard — struct field" section)
  </read_first>
  <action>
    Add an exported `Version int` field to each of the four structs `Location`, `Exit`, `Character`, `Object`. Document it as (a) the read version carried back into a guarded write and (b) the field the repo refreshes to the committed version after a successful create/update, so a struct reused for a later write does not carry a stale version and spuriously conflict (review finding 12 — the refresh itself lands in 05-02/05-03). New entities receive `version=1` from the DB default, so constructors (e.g. `NewLocationWithID`) MUST NOT hand-set `Version` on create — it is populated only by read-path scans and post-write refresh (added in the repo plans 05-02/05-03). Do not modify any scan function in this plan (those live in `internal/world/postgres/*_repo.go`, owned by 05-02/05-03) — this plan only adds the struct field.
  </action>
  <verify>
    <automated>task test -- ./internal/world/</automated>
  </verify>
  <acceptance_criteria>
    - Each of Location/Exit/Character/Object has a `Version int` field.
    - No constructor hand-sets `Version`.
    - `task test -- ./internal/world/` compiles and passes (existing unit tests unaffected; Version defaults to zero value until scans populate it in later plans).
  </acceptance_criteria>
  <done>The four structs can transport a read version; the field is inert until repo scans populate it in 05-02/05-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WORLD_CONCURRENT_EDIT typed error code</name>
  <files>internal/world/errors.go, internal/world/errors_test.go</files>
  <read_first>
    - internal/world/errors.go (existing error sentinels + oops.Code convention in this package)
    - internal/world/postgres/location_repo.go (lines 37, 82 — the `oops.Code("LOCATION_NOT_FOUND").With("id", ...).Wrap(world.ErrNotFound)` style to mirror)
    - .claude/rules/references/plan-review-learnings.md ("errutil.AssertErrorCode" convention; `oops.Code(...).Is` matches any OopsError — assert code identity, not chain-walk)
  </read_first>
  <behavior>
    - A `world.ErrConcurrentEdit` sentinel exists; an error wrapping it via `oops.Code("WORLD_CONCURRENT_EDIT")` reports code `WORLD_CONCURRENT_EDIT` under `errutil.AssertErrorCode`.
    - The conflict error is distinct from `world.ErrNotFound` — `errors.Is(concurrentEditErr, world.ErrNotFound)` is false.
  </behavior>
  <action>
    Add a `WORLD_CONCURRENT_EDIT` conflict error to `internal/world/errors.go`: an exported sentinel (e.g. `ErrConcurrentEdit`) plus the constant code string `WORLD_CONCURRENT_EDIT`, following the existing sentinel + `oops.Code(...).Wrap(...)` idiom in the package. This is the single typed conflict signal the guarded repos will emit (05-02/05-03) and the `world.Service` boundary will propagate unchanged (D-02 — no UX mapping in this phase). Write the RED test in `errors_test.go` first: assert `errutil.AssertErrorCode` reports the code and that the conflict sentinel is NOT `world.ErrNotFound`. Keep the literal code string out of any non-test comment body.
  </action>
  <verify>
    <automated>task test -- ./internal/world/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/world/errors.go` exports a concurrent-edit sentinel and defines the `WORLD_CONCURRENT_EDIT` code.
    - `errors_test.go` asserts the code via `errutil.AssertErrorCode` and asserts the conflict is distinct from `ErrNotFound`.
    - `task test -- ./internal/world/` passes.
  </acceptance_criteria>
  <done>The phase has one typed conflict signal, code-asserted, distinct from not-found.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live schema | An operator applies 000049 to a running DB; a bad column default could reject writes or corrupt existing rows. |
| conflict error → caller | The conflict code crosses the world.Service boundary to callers; it must not leak internal state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | 000049 up migration | low | mitigate | DEFAULT 1 backfills existing rows in place (no NOT NULL-without-default lock storm); idempotent ADD COLUMN IF NOT EXISTS is safe to re-run. |
| T-05-02 | Tampering | version column | medium | mitigate | Column is NOT NULL DEFAULT 1 so no row can exist without a version; the actual CAS enforcement lands in 05-02/05-03 — this plan only establishes the invariant-carrying column. |
| T-05-03 | Information Disclosure | WORLD_CONCURRENT_EDIT error | low | accept | The conflict code is intentionally generic (no row contents in the code/sentinel); `.With("id", ...)` metadata is server-side context, not wire payload. |
</threat_model>

<verification>
- `task test -- ./internal/world/` passes (struct + error changes compile).
- `task test:int` applies and reverts migration 000049 cleanly (run via the `local-check` agent; integration suite runs migrations).
- `grep 'version' internal/store/migrations/000049_world_version_guard.up.sql` confirms the column add.
</verification>

<success_criteria>
- Migration 000049 adds `version INTEGER NOT NULL DEFAULT 1` to exactly the four world tables and reverses cleanly.
- Location/Exit/Character/Object each carry `Version int`.
- `WORLD_CONCURRENT_EDIT` exists and is code-asserted in a test.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-01-SUMMARY.md` when done.
</output>
