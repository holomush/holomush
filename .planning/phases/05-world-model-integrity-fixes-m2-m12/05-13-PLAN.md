---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 13
type: execute
wave: 12
depends_on: [05-12]
files_modified:
  - CLAUDE.md
  - README.md
  - site/src/content/docs/contributing/reference/coding-standards.md
  - site/src/content/docs/contributing/explanation/architecture.md
  - test/meta/world_model_doc_claim_test.go
autonomous: true
requirements: [MODEL-02]
must_haves:
  truths:
    - "Every doc site that stated the false 'event sourcing / current state derives from replay' principle now describes the decided model (event-driven with an append-only audit log); no doc claims replay-derived world state the code does not provide."
    - "The real client-catch-up / Subscribe replay language is left intact (it IS real and MUST NOT be downgraded) — including README.md:22 'session persistence and event replay', which is reconnect/catch-up language, not a world-state-reconstruction claim (Codex finding 17)."
    - "A doc-claim meta-test asserts the corrected sites no longer state replay-derived world state."
  artifacts:
    - path: test/meta/world_model_doc_claim_test.go
      provides: "grep/assertion that the corrected doc sites no longer claim replay-derived world state"
      contains: "replay|event.sourc"
  key_links:
    - from: CLAUDE.md
      to: docs/adr/holomush-i4784-world-state-model-decision.md
      via: "the downgraded principle reflects the ADR's decided model"
      pattern: "event-driven|append-only"
---

<objective>
Downgrade the false "event sourcing / current state derives from replay" principle at its documentation sites to the decided model ("event-driven with an append-only audit log"), and add a doc-claim meta-test so the corrected claim cannot silently regress. This is slice 4 — the docs land in the same phase the mechanism lands (per the ADR/one-pager).

Purpose: The F1 archaeology established that world-state event sourcing was never built; the docs asserting replay-derived world state are false. MODEL-02 corrects them to the model the code actually provides — while preserving the genuinely-real client catch-up / Subscribe replay language.
Output: corrected CLAUDE.md / README.md / coding-standards.md / architecture.md sites, a re-grep for any missed mdx site, and a doc-claim meta-test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@docs/adr/holomush-i4784-world-state-model-decision.md
@docs/reviews/arch-review/2026-07-11/verification/f1-eventsourcing-why.md
@docs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-grep and downgrade the event-sourcing doc claims</name>
  <files>CLAUDE.md, README.md, site/src/content/docs/contributing/reference/coding-standards.md, site/src/content/docs/contributing/explanation/architecture.md</files>
  <read_first>
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md (MODEL-02 Doc Sites table — CLAUDE.md:274, README.md:18/22, coding-standards.md:344/348, architecture.md:79/295/305/309; the downgrade target phrasing; the client-catch-up language that MUST NOT be downgraded — architecture.md:74)
    - CLAUDE.md (line ~274 — "Event sourcing — actions produce immutable ordered events; state derives from replay")
    - README.md (lines ~18 "event-sourced architecture" — FALSE, downgrade; ~22 "session persistence and event replay" — evaluate: legitimate reconnect/catch-up, likely PRESERVE per Codex finding 17)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (Codex LOW finding 17 — do NOT mechanically downgrade README:22)
    - docs/adr/holomush-i4784-world-state-model-decision.md (the decided model wording to reflect)
  </read_first>
  <action>
    First re-grep to confirm the exact current site set (line numbers drift): search `CLAUDE.md README.md site/src/content/docs/**/*.{md,mdx}` for `event.sourc`, `derives from replay`, `state.*replay`, `replayable`, `append-only`. Downgrade only the ACTUALLY-FALSE world-state-reconstruction claims to the decided model — "event-driven with an append-only audit log" — at: CLAUDE.md (~274), README.md (~18 "event-sourced architecture"), coding-standards.md (~344 heading, ~348), architecture.md (~79, ~295, ~305 heading, ~309). **Scope discipline (Codex finding 17):** `README.md:22` ("session persistence and event replay") is legitimate client reconnect/catch-up language, NOT a false world-state-reconstruction claim — read it in context and PRESERVE it unless it demonstrably asserts world state is rebuilt from replay (record the decision in the summary). Resolve Open Question 4: the researcher found no ES claim in `site/src/content/docs/index.mdx` on this branch — the re-grep confirms whether any `.mdx` marketing site carries the claim; if one surfaces, correct it and add it to `files_modified` in the summary. MUST NOT downgrade the genuinely-real client-catch-up / Subscribe replay language (e.g. architecture.md:74 "Reconnecting clients catch up from their last seen event" is correct — leave it). Run `task lint:markdown` / `task fmt` after edits (rumdl table/format compliance).
  </action>
  <verify>
    <automated>task lint:markdown</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md / README.md / coding-standards.md / architecture.md no longer state that world/current state derives from event replay.
    - The corrected sites describe the decided model (event-driven + append-only audit log).
    - The client catch-up / Subscribe replay language is preserved.
    - A re-grep for `derives from replay` / `event.sourc` over docs surfaces no remaining false world-state claim; `task lint:markdown` passes.
  </acceptance_criteria>
  <done>The false event-sourcing world-state claims are corrected to the decided model; real replay language is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Doc-claim meta-test guarding the correction</name>
  <files>test/meta/world_model_doc_claim_test.go</files>
  <read_first>
    - test/meta/depguard_config_test.go (the read-a-file + assert idiom to mirror)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md (Success criterion → proof map, MODEL-02 row — doc-grep assertion)
  </read_first>
  <action>
    Add `test/meta/world_model_doc_claim_test.go` that reads the corrected doc files and asserts none of them state replay-derived WORLD/current state (the false claim), while allowing the legitimate client-catch-up replay language to remain. The test should fail if a future edit re-introduces a "current state derives from event replay"-style claim for world state at the guarded sites. Model the file-read + assertion on `depguard_config_test.go`. Use a phrasing check narrow enough to distinguish the false world-state claim from the true client-catch-up language (assert on the guarded files/sections, not a blanket repo grep). Keep any literal claim phrasing the test negative-checks out of comment bodies.
  </action>
  <verify>
    <automated>task test -- ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - The meta-test reads the corrected sites and asserts no replay-derived-world-state claim remains.
    - The test would fail if the false claim is re-introduced at a guarded site.
    - The legitimate client-catch-up replay language does not trip the test.
    - `task test -- ./test/meta/` passes.
  </acceptance_criteria>
  <done>The doc correction is regression-guarded by a meta-test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc claim → reader/contributor | A false ES claim misleads contributors into assuming a world-state rebuild path exists. |
| correction → regression | An edit could re-introduce the false claim without a test failing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-37 | Repudiation | false ES doc claim | low | mitigate | The claim is corrected to the decided model and a doc-claim meta-test prevents regression; the real client-catch-up replay language is explicitly preserved so the correction is truthful, not over-broad. |
</threat_model>

<verification>
- `task lint:markdown` passes; `task test -- ./test/meta/` passes (run via `local-check`).
- Re-grep `derives from replay` / `event.sourc` over `CLAUDE.md README.md site/src/content/docs/**` surfaces no remaining false world-state claim.
</verification>

<success_criteria>
- Every doc site stating replay-derived world state now describes the decided model.
- The correction is regression-guarded; the real client-catch-up replay language is intact.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-13-SUMMARY.md` when done.
</output>
