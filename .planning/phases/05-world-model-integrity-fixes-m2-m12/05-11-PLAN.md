---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 11
type: execute
wave: 10
depends_on: [05-10]
files_modified:
  - internal/world/service.go
  - internal/world/service_test.go
  - internal/world/mutator.go
  - internal/world/postgres/scene_repo.go
  - internal/property/entity_mutator.go
  - internal/property/entity_mutator_test.go
  - internal/world/postgres/property_repo.go
  - internal/grpc/auth_handlers.go
  - internal/grpc/auth_handlers_test.go
  - internal/world/outbox/genesis.go
  - internal/world/outbox/genesis_test.go
  - test/meta/world_envelope_census_test.go
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "Every remaining world write command (DeleteCharacter, UpdateCharacterDescription, AddSceneParticipant, RemoveSceneParticipant, the property write path, and CreateCharacter) emits exactly one declared-kind envelope in the same transaction — no un-migrated command remains. Scene-participant writes (scene_participants table) enroll in the mutation tx via execerFromCtx (05-14) so their envelope commits atomically (Codex finding 4: scene_repo.go was absent from the rollout — it is now covered)."
    - "With every command now routed through mutate(), the compile-time write fence is COMPLETED here: world.Service's repo fields are switched to the read-only reader views (05-14) so it has no directly-callable write repo — an envelope-less or direct-repo write is now a type error across the whole service (Codex finding 2; the genuine fence 05-06 opened is closed once coverage is complete)."
    - "CreateCharacter has NO non-transactional escape hatch in production: createCharacterAtomic no longer falls back to a plain create; character creation + its genesis envelope commit in one transaction, and the auth-service/adapter path (characterService.Create), not only the gRPC handler, is covered (Codex finding 9)."
    - "The census meta-test derives the write-command surface STRUCTURALLY (go/ast or go/types over world.Service's write methods + CreateCharacter), NOT from the incomplete world.Mutator subset, and asserts each maps to a declared envelope kind; it passes because coverage is complete (no allow-list) (Codex finding 10)."
    - "Genesis snapshot emission emits one envelope per existing aggregate at cutover, and a feed epoch/reset procedure exists for DB restores/backfills."
  artifacts:
    - path: test/meta/world_envelope_census_test.go
      provides: "census meta-test: every write command → declared envelope kind (no allow-list)"
      contains: "census|envelope"
    - path: internal/world/outbox/genesis.go
      provides: "genesis snapshot emission at cutover + feed epoch/reset procedure"
      contains: "genesis|epoch"
  key_links:
    - from: internal/grpc/auth_handlers.go
      to: internal/world/outbox/taxonomy.go
      via: "CreateCharacter's world-state write emits a character-genesis envelope"
      pattern: "genesis|envelope"
  prohibitions:
    - "No world write command may remain un-migrated — the census meta-test forbids an allow-list of pending commands."
    - "No property write may reach a world-table repo write except through the envelope-carrying seam."
---

<objective>
Complete the D-01 mechanical rollout: wire the remaining world write commands (character, scene-participant, property path, and CreateCharacter), add genesis snapshot emission + a feed epoch/reset procedure, and introduce the census meta-test that structurally forbids any un-migrated command. The census lands here — after coverage is complete — so it passes on introduction (data-first, enforcement-last).

Purpose: This closes the "no allow-list of pending commands" requirement. Once the census enumerates every write command → declared kind and passes, the drift that caused the ADR is structurally non-recurrable. Genesis emission gives the feed a defined origin at cutover; the epoch/reset procedure covers DB restores/backfills.
Output: remaining commands emitting, CreateCharacter genesis, the census meta-test (green), genesis + epoch/reset.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/event-conventions.md
@.claude/rules/gateway-boundary.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Character, scene-participant, and property write commands through the outbox</name>
  <files>internal/world/service.go, internal/world/service_test.go, internal/property/entity_mutator.go, internal/property/entity_mutator_test.go, internal/world/postgres/property_repo.go</files>
  <read_first>
    - internal/world/service.go (DeleteCharacter ~602, UpdateCharacterDescription ~656 (version-threaded in 05-04 — now also emits), AddSceneParticipant ~703, RemoveSceneParticipant ~724)
    - internal/property/entity_mutator.go (SetName/SetDescription — funnel into UpdateLocation/UpdateObject, already emitting via 05-10; confirm the property-side envelope is the parent command's, not a duplicate)
    - internal/world/postgres/property_repo.go (Create ~35, Update ~115, Delete ~153 — child rows guarded by parent version + delta-parity, NOT a 5th version column)
    - internal/world/mutator.go, internal/world/outbox/taxonomy.go
  </read_first>
  <behavior>
    - RED: DeleteCharacter, UpdateCharacterDescription, AddSceneParticipant, RemoveSceneParticipant each emit exactly one declared-kind envelope in the same tx.
    - A property write (SetName/SetDescription) results in exactly one envelope — the parent aggregate's update command envelope — not a duplicate property-level envelope.
    - DeleteCharacter emits a tombstone.
  </behavior>
  <action>
    Route DeleteCharacter, UpdateCharacterDescription, AddSceneParticipant, RemoveSceneParticipant through `mutate()` with their taxonomy-declared kinds and intent-level payloads, each manifest built from the write's returned `MutationDelta`. Scene-participant writes: `SceneRepository.AddParticipant`/`RemoveParticipant` were switched to `execerFromCtx` in 05-14 (they no longer use raw `pool.Exec`), so wrapping them in `mutate()` commits the `scene_participants` row atomically with the outbox envelope (Codex finding 4 — scene_repo.go is now in scope; `scene_participants` is covered by the writer-boundary fence, 05-09). For the property write path: SetName/SetDescription funnel into UpdateLocation/UpdateObject (already emitting from 05-10), so the property write's envelope IS the parent aggregate's update envelope — ensure exactly one envelope per property command, with the parent's before/after version in the manifest; `entity_properties` is a child guarded by the parent's version + delta-parity, not its own version column. Confirm `property_repo.go` + `scene_repo.go` writes stay inside `internal/world/postgres` (writer-boundary fence from 05-09). **Complete the compile-time fence (Codex finding 2):** now that every write command routes through the executor's `mutate()`, switch `world.Service`'s repo fields to the read-only reader views (05-14: `LocationReader`/`ExitReader`/`ObjectReader`/`CharacterReader`/`SceneReader`) in `service.go` + `mutator.go`, so Service holds no directly-callable write repo — the write repos live only on the write executor behind `mutate()`. Add a test asserting Service's repo field types are the reader interfaces (a direct `s.xRepo.Update(...)` would not compile). RED tests first: one envelope per command; property write yields exactly one parent-update envelope; Service exposes no directly-callable write repo.
  </action>
  <verify>
    <automated>task test -- ./internal/world/ ./internal/property/ && task test:int -- -run 'Character|Scene|Property' ./internal/world/...</automated>
  </verify>
  <acceptance_criteria>
    - DeleteCharacter/UpdateCharacterDescription/AddSceneParticipant/RemoveSceneParticipant each emit exactly one declared-kind envelope.
    - A property write yields exactly one parent-update envelope (no duplicate).
    - world.Service's repo fields are the read-only reader views; a test asserts no directly-callable write repo remains (the compile-time fence is complete).
    - DeleteCharacter emits a tombstone; `task test -- ./internal/world/ ./internal/property/` passes.
  </acceptance_criteria>
  <done>All remaining world.Service + property write commands emit through the outbox.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CreateCharacter genesis envelope + genesis snapshot emission + feed epoch/reset</name>
  <files>internal/grpc/auth_handlers.go, internal/grpc/auth_handlers_test.go, internal/world/outbox/genesis.go, internal/world/outbox/genesis_test.go</files>
  <read_first>
    - internal/grpc/auth_handlers.go (CreateCharacter ~476; createCharacterAtomic ~517-538 — the transactional path AND the non-transactional FALLBACK at :521-526 that skips the binding/tx when transactor|bindings is nil; Codex finding 9, Open Question 3)
    - internal/world/outbox/taxonomy.go (the character-genesis kind from 05-09)
    - internal/world/postgres/outbox_store.go, internal/world/outbox/envelope.go (the same-tx store + envelope)
    - docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md (Consistency/lifecycle — genesis snapshot at cutover; feed epoch/reset for DB restores/backfills)
    - .claude/rules/gateway-boundary.md (CreateCharacter lives on the core server; the write path is core-side, not gateway)
  </read_first>
  <behavior>
    - RED: CreateCharacter's world-state write emits exactly one character-genesis envelope in the same tx as the character row insert.
    - RED: in a production configuration, createCharacterAtomic does NOT fall back to a plain non-transactional create — a missing transactor/bindings fails closed (returns an error) rather than silently creating a character without its genesis envelope (Codex finding 9). The envelope is emitted inside the atomic transaction alongside characterService.Create + the binding.
    - Genesis snapshot emission produces exactly one envelope per existing aggregate (idempotent — running it twice does not double-emit).
    - The feed epoch/reset procedure advances the feed epoch so a DB restore/backfill does not replay stale positions.
  </behavior>
  <action>
    Fix `createCharacterAtomic` (`internal/grpc/auth_handlers.go:517-538`): REMOVE the non-transactional fallback at :521-526 for production (a nil transactor/bindings must fail closed with a typed error, not silently create a character without its genesis envelope — Codex finding 9). Inside the transactional path, after `characterService.Create` + the binding, emit exactly one character-genesis envelope through the same-tx outbox store (this is the auth-service/adapter path — `characterService.Create`, not just the gRPC handler shell — so the envelope commits atomically with the character row + binding). If a test-only non-transactional mode must remain, gate it explicitly behind test wiring, never the production default. In `internal/world/outbox/genesis.go`, implement the genesis snapshot emission (one envelope per existing location/exit/character/object at cutover, idempotent) and the feed epoch/reset procedure (advance the epoch so DB restores/backfills either emit normal mutations or bump the epoch — INV-WORLD-4 / WRITER-BOUNDARY's admin/migration clause). RED tests: CreateCharacter emits one genesis envelope atomically; a nil-transactor config fails closed (no silent plain create); genesis emission is idempotent; epoch advance is observable.
  </action>
  <verify>
    <automated>task test -- ./internal/grpc/ ./internal/world/outbox/ && task test:int -- -run 'Genesis|CreateCharacter' ./...</automated>
  </verify>
  <acceptance_criteria>
    - CreateCharacter emits one character-genesis envelope in the same tx as the character write (via characterService.Create, the auth-service path).
    - createCharacterAtomic's production path has no non-transactional fallback: a nil transactor/bindings fails closed (asserted by a test), so no character is created without its genesis envelope.
    - Genesis snapshot emission is one-envelope-per-aggregate and idempotent; a feed epoch/reset procedure exists and is tested.
  </acceptance_criteria>
  <done>The last write path (CreateCharacter) emits; the feed has a defined genesis origin + an epoch/reset for restores.</done>
</task>

<task type="auto">
  <name>Task 3: Census meta-test — every write command maps to a declared envelope kind</name>
  <files>test/meta/world_envelope_census_test.go</files>
  <read_first>
    - internal/world/service.go (the actual write-command surface: CreateLocation/UpdateLocation/DeleteLocation/CreateExit/UpdateExit/DeleteExit/CreateObject/UpdateObject/DeleteObject/MoveObject/DeleteCharacter/UpdateCharacterDescription/MoveCharacter/AddSceneParticipant/RemoveSceneParticipant)
    - internal/grpc/auth_handlers.go (CreateCharacter — the world-state write outside world.Service)
    - internal/world/mutator.go (the Mutator interface — NOTE it is an INCOMPLETE subset, missing deletes/moves/scene/character-update/CreateCharacter; the census MUST NOT derive from it — Codex finding 10)
    - internal/world/outbox/taxonomy.go (the declared-kind registry the census asserts against)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (Codex finding 10 — census source must be structural, not the world.Mutator subset)
  </read_first>
  <action>
    Add `test/meta/world_envelope_census_test.go` that derives the write-command surface STRUCTURALLY — parse `internal/world/service.go` (and the CreateCharacter site in `internal/grpc/auth_handlers.go`) with `go/ast`/`go/types` to enumerate the actual mutating command methods, rather than reading the incomplete `world.Mutator` interface (which omits deletes, moves, scene membership, character update, and CreateCharacter — Codex finding 10). Assert each enumerated write command maps to a declared taxonomy envelope kind — with NO allow-list of pending/exempt commands (D-01). Examine commands (`ExamineLocation/Object/Character`) are reads and MUST be recognized as non-mutating by the structural derivation (or explicitly classified out with a documented reason), not silently omitted. The census must FAIL if a new mutating command is added without a declared kind (a structural derivation auto-detects it — unlike a hard-coded list). Because 05-10 + 05-11 Tasks 1-2 completed coverage, this test passes on introduction. Keep any literal command/kind strings the test negative-checks out of comment bodies.
  </action>
  <verify>
    <automated>task test -- ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - The census derives the write-command surface via go/ast or go/types over world.Service + CreateCharacter (NOT the world.Mutator subset) and asserts a declared kind for each, with no allow-list.
    - The census passes (coverage complete); examine reads are correctly excluded as non-mutating.
    - Adding a hypothetical new mutating command without a declared kind fails the census (auto-detected by the structural derivation).
  </acceptance_criteria>
  <done>The census structurally forbids an un-migrated command; the D-01 full rollout is proven complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CreateCharacter (gRPC flow) → world tables | A character-creation write outside the world writer boundary escapes the guard/feed. |
| new write command → census | An un-migrated command silently omitted from the feed is the exact ADR drift. |
| genesis/epoch → feed origin | A double-genesis or missing epoch corrupts consumer bootstrap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-32 | Tampering | un-migrated command | high | mitigate | The census meta-test derives the command surface structurally (go/ast/go/types over the real write methods, not the incomplete world.Mutator subset — finding 10) and asserts every write command maps to a declared kind with no allow-list; a new un-declared command fails CI — the drift is structurally non-recurrable. |
| T-05-33 | Tampering | genesis idempotency | medium | mitigate | Genesis emission is one-per-aggregate and idempotent (re-run does not double-emit); the epoch/reset covers restores so stale positions are not replayed. |
| T-05-34 | Elevation of Privilege | CreateCharacter non-transactional fallback | high | mitigate | The createCharacterAtomic non-transactional fallback (auth_handlers.go:521-526) is removed for production and fails closed — a character cannot be created without its genesis envelope (Codex finding 9). The write stays inside the writer boundary (fence from 05-09) and emits atomically. |
</threat_model>

<verification>
- `task test -- ./internal/world/ ./internal/property/ ./internal/grpc/ ./internal/world/outbox/ ./test/meta/` passes (run via `local-check`).
- `task lint` green (writer-boundary fence holds with CreateCharacter wired).
- The census meta-test passes and would fail on a hypothetical un-declared command.
</verification>

<success_criteria>
- Every world write command emits exactly one declared-kind envelope; the census passes with no allow-list.
- Genesis snapshot emission + feed epoch/reset exist and are tested.
- The D-01 full mechanical rollout is complete.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-11-SUMMARY.md` when done.
</output>
