---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 11
type: execute
wave: 10
depends_on: [05-10, 05-15]
files_modified:
  - internal/world/service.go
  - internal/world/service_test.go
  - internal/world/mutator.go
  - internal/property/entity_mutator.go
  - internal/property/entity_mutator_test.go
  - internal/world/postgres/genesis_store.go
  - internal/world/postgres/genesis_store_test.go
  - internal/world/outbox/genesis.go
  - internal/world/outbox/genesis_test.go
  - test/meta/world_envelope_census_test.go
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "Every remaining world.Service write command (DeleteCharacter, UpdateCharacterDescription, AddSceneParticipant, RemoveSceneParticipant, and the property write path) emits exactly one declared-kind envelope in the same transaction — no un-migrated command remains. Scene-participant writes (scene_participants) AND property writes (entity_properties, whose Create/Update were moved onto execerFromCtx in 05-14) enroll in the mutation tx so their envelope commits atomically (Codex finding 4 + round-2 property-leak). Character CREATION (registered + guest + bootstrap-admin) is NOT wired here — round-3 blocker #5 moved it to the atomic character-genesis service in 05-15, which closes ALL THREE production creation paths, not just the gRPC one."
    - "With every command now routed through mutate(), the compile-time write fence is COMPLETED here: world.Service's repo fields are switched to the read-only reader views (05-14) so it has no directly-callable write repo, AND the write executor is the sole in-world owner of the writer repos — an envelope-less or direct-repo write is now a type error across the whole service (Codex finding 2). The enforceable INV-WORLD-4 boundary = this reader-view compile fence + the AST SQL fence (05-09, covering entity_properties) + the import-graph composition allowlist (05-07 Task 4) + the single sanctioned out-of-world writer: the atomic character-genesis application service (05-15), through which registered, guest, AND bootstrap-admin character creation all emit their genesis envelope atomically (round-3 blocker #5: reader views alone cannot fence packages that hold a concrete writer repo — guest_service.go and bootstrap/admin.go were bypasses)."
    - "The census meta-test derives the write-command surface from an EXPLICIT closed command construct whose membership is mechanically discoverable — the executor's write-command descriptor set (each descriptor names its taxonomy kind) — NOT name-prefix inference over method declarations or detection of existing mutate() calls (which would miss a NEW direct write). The census proves REGISTRY CONSISTENCY (bijection between registered write commands and declared taxonomy kinds, no allow-list); COMPLETENESS of production world writes is claimed only in combination with the fences that make an unregistered write impossible: the reader-view compile fence, the AST SQL fence (05-09), the internal/world/postgres composition allowlist (05-07), and the removal of Create from the auth-side character-repo interfaces (05-15) (round-3 census MEDIUM: a descriptor bijection alone cannot discover guest/bootstrap direct repo calls — do not overstate it)."
    - "Genesis snapshot emission emits one envelope per existing aggregate at cutover and is idempotent via the DURABLE world_genesis_checkpoint schema (000050, round-3 MEDIUM): each genesis envelope inserts its checkpoint row (game_id, epoch, aggregate_type, aggregate_id) in the SAME transaction; a same-epoch re-run conflicts on the checkpoint PK and skips BEFORE allocating a feed position (no gap, race-safe under the serializing counter lock — a pre-insert existence query alone would be race-prone and outbox uniques cannot help because genesis uses fresh ULIDs and published rows get pruned); a feed epoch/reset procedure advances the persistent per-game epoch (000050 schema) via an internal/world/postgres epoch read/advance store method, legitimately re-opening genesis at the new epoch."
  artifacts:
    - path: test/meta/world_envelope_census_test.go
      provides: "census meta-test: every registered write command → declared envelope kind (explicit closed command construct, no allow-list)"
      contains: "census|envelope"
    - path: internal/world/postgres/genesis_store.go
      provides: "genesis snapshot SQL (one envelope per existing aggregate, checkpoint-idempotent) + persistent feed-epoch read/advance — in the writer boundary"
      contains: "genesis|epoch"
    - path: internal/world/outbox/genesis.go
      provides: "genesis snapshot orchestration (checkpoint-keyed idempotency) + feed epoch/reset procedure (calls the postgres genesis/epoch store)"
      contains: "genesis|epoch"
  key_links:
    - from: internal/world/outbox/genesis.go
      to: internal/world/postgres/genesis_store.go
      via: "genesis orchestration emits one envelope per aggregate through the postgres genesis/epoch store, inserting the world_genesis_checkpoint row in the same tx (idempotency key)"
      pattern: "genesis|checkpoint|epoch"
  prohibitions:
    - "No world write command may remain un-migrated — the census forbids an allow-list of pending commands; membership is derived from the explicit command construct, not name inference."
    - "No property write may reach a world-table repo write except through the envelope-carrying seam; property Create/Update MUST be execerFromCtx-enrolled (05-14) and entity_properties is in the SQL fence (05-09)."
    - "Genesis idempotency MUST NOT rest on a pre-insert existence query alone (race-prone, round-3 MEDIUM) — the world_genesis_checkpoint row is inserted atomically with each genesis envelope and its PK is the durable identity; the existence check runs under the serializing per-game counter lock with the PK as backstop."
    - "The census MUST NOT be presented as proof of production write-path completeness on its own — completeness claims cite the fences (reader views, AST SQL fence, composition allowlist, 05-15 interface Create removal) alongside the bijection (round-3 census MEDIUM)."
---

<objective>
Complete the D-01 mechanical rollout inside `world.Service`: wire the remaining write commands (character update/delete, scene-participant, property path), complete the reader-view write fence + writer-executor ownership, add genesis snapshot emission with DURABLE checkpoint-keyed idempotency + a persistent feed epoch/reset procedure (000050 schema + a postgres epoch store), and introduce the census meta-test built on an explicit closed command construct. The census lands here — after Service coverage is complete — so it passes on introduction (data-first, enforcement-last).

**Round-3 restructure (blocker #5):** CreateCharacter wiring is NO LONGER in this plan. Round 3 found the round-2 shape (an `OutboxWriter` threaded through `CoreServer` for the gRPC handler) covered only ONE of THREE production character-creation paths — guest creation (`internal/auth/guest_service.go:134-146`) and bootstrap-admin creation (`internal/bootstrap/admin.go:85-98` via `CharRepoAdapter`, `adapters.go:38-40`) would still commit character rows WITHOUT genesis envelopes, making INV-WORLD-4 false. ALL character creation now routes through ONE atomic character-genesis application service, built and wired in **05-15** (which supersedes the round-2 CoreServer `WithOutboxWriter` design — same end-to-end guarantee, delivered through the service). This plan keeps: the Service-side rollout, the reader-view fence, genesis snapshot + epoch, and the census.

**Round-3 genesis idempotency (MEDIUM):** genesis uses fresh event ULIDs and published outbox rows are pruned, so no outbox unique can make a re-run idempotent. The durable identity is the `world_genesis_checkpoint` table (000050): each aggregate's genesis envelope inserts its checkpoint row in the same tx; the same-epoch re-run check runs under the serializing per-game counter lock (race-safe) with the checkpoint PK as backstop; an epoch advance re-opens genesis.

Purpose: This closes the "no allow-list of pending commands" requirement. Once the census asserts a bijection between the registered write commands and declared kinds and passes, the drift that caused the ADR is structurally non-recurrable. Genesis emission gives the feed a defined origin at cutover; the persistent epoch/reset covers DB restores/backfills.
Output: remaining Service commands emitting, the reader-view fence complete, the census meta-test (green, explicit-construct, honestly-scoped), genesis + checkpoint idempotency + persistent epoch/reset (with a postgres genesis/epoch store).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/event-conventions.md
@.claude/rules/gateway-boundary.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Character, scene-participant, and property write commands through the outbox + complete the reader-view fence</name>
  <files>internal/world/service.go, internal/world/service_test.go, internal/property/entity_mutator.go, internal/property/entity_mutator_test.go, internal/world/mutator.go</files>
  <read_first>
    - internal/world/service.go (DeleteCharacter ~602, UpdateCharacterDescription ~656 (version-threaded in 05-04 — now also emits), AddSceneParticipant ~703, RemoveSceneParticipant ~724)
    - internal/property/entity_mutator.go (SetName/SetDescription — funnel into UpdateLocation/UpdateObject, already emitting via 05-10; confirm the property-side envelope is the parent command's, not a duplicate)
    - internal/world/postgres/property_repo.go (Create/Update already moved onto execerFromCtx in 05-14 — READ-ONLY here to confirm; child rows guarded by parent version + delta-parity, NOT a 5th version column)
    - internal/world/postgres/scene_repo.go (AddParticipant/RemoveParticipant already on execerFromCtx from 05-14 — READ-ONLY here)
    - internal/world/mutator.go (the mutate() seam from 05-06; the write executor to make sole writer-repo owner), internal/world/outbox/taxonomy.go
  </read_first>
  <behavior>
    - RED: DeleteCharacter, UpdateCharacterDescription, AddSceneParticipant, RemoveSceneParticipant each emit exactly one declared-kind envelope in the same tx.
    - A property write (SetName/SetDescription) results in exactly one envelope — the parent aggregate's update command envelope — not a duplicate property-level envelope.
    - DeleteCharacter emits a tombstone.
    - world.Service exposes no directly-callable write repo (a direct s.xRepo.Update(...) does not compile).
  </behavior>
  <action>
    Route DeleteCharacter, UpdateCharacterDescription, AddSceneParticipant, RemoveSceneParticipant through `mutate()` with their taxonomy-declared kinds and intent-level payloads, each manifest built from the write's returned `wmodel.MutationDelta`. Scene-participant writes: `SceneRepository.AddParticipant`/`RemoveParticipant` were switched to `execerFromCtx` in 05-14, so wrapping them in `mutate()` commits the `scene_participants` row atomically with the outbox envelope (Codex finding 4). For the property write path: `PropertyRepository.Create`/`Update` were moved onto `execerFromCtx` in 05-14 (round-2 property-leak fix), so a property write funnelled through the parent command (SetName/SetDescription → UpdateLocation/UpdateObject, already emitting from 05-10) commits atomically inside the parent's mutation tx; the property write's envelope IS the parent aggregate's update envelope — ensure exactly one envelope per property command, with the parent's before/after version in the manifest; `entity_properties` is a child guarded by the parent's version + delta-parity, not its own version column, and is now in the SQL fence (05-09). Confirm `property_repo.go` + `scene_repo.go` writes stay inside `internal/world/postgres` (writer-boundary fence from 05-09). **Complete the compile-time fence + writer-executor ownership (Codex finding 2 + round-2 write-fence):** now that every write command routes through the executor's `mutate()`, switch `world.Service`'s repo fields to the read-only reader views (05-14: `LocationReader`/`ExitReader`/`ObjectReader`/`CharacterReader`/`SceneReader`) in `service.go` + `mutator.go`, so Service holds no directly-callable write repo — the writer repos live ONLY on the write executor behind `mutate()` (the executor is the sole in-world owner of the writer repos). Add a test asserting Service's repo field types are the reader interfaces (a direct `s.xRepo.Update(...)` would not compile). Note the enforceable INV-WORLD-4 boundary is this reader-view compile fence PLUS the AST SQL fence (05-09) PLUS the import-graph composition allowlist (05-07 Task 4 — only composition/test packages may import `internal/world/postgres`, so no new production package can hold a concrete writer) PLUS the atomic character-genesis service (05-15 — the single sanctioned out-of-world writer; round-3 blocker #5: guest/bootstrap-admin creation had been holding concrete/narrow writer repos that reader views cannot fence). Go cannot make a concrete repo method uncallable from a package holding a pool, so the SQL fence + allowlist are the mechanical backstops that catch any out-of-boundary writer. RED tests first: one envelope per command; property write yields exactly one parent-update envelope; Service exposes no directly-callable write repo.
  </action>
  <verify>
    <automated>task test -- ./internal/world/ ./internal/property/ && task test:int -- -run 'Character|Scene|Property' ./internal/world/...</automated>
  </verify>
  <acceptance_criteria>
    - DeleteCharacter/UpdateCharacterDescription/AddSceneParticipant/RemoveSceneParticipant each emit exactly one declared-kind envelope.
    - A property write yields exactly one parent-update envelope (no duplicate) and commits atomically (property Create/Update on execerFromCtx from 05-14).
    - world.Service's repo fields are the read-only reader views; the write executor is the sole owner of the writer repos; a test asserts no directly-callable write repo remains (the compile-time fence is complete).
    - DeleteCharacter emits a tombstone; `task test -- ./internal/world/ ./internal/property/` passes.
  </acceptance_criteria>
  <done>All remaining world.Service + property write commands emit through the outbox; the reader-view fence + writer-executor ownership are complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Genesis snapshot (checkpoint-idempotent) + feed epoch/reset with the postgres genesis/epoch store</name>
  <files>internal/world/postgres/genesis_store.go, internal/world/postgres/genesis_store_test.go, internal/world/outbox/genesis.go, internal/world/outbox/genesis_test.go</files>
  <read_first>
    - internal/store/migrations/000050_world_outbox.up.sql (05-05 — the outbox + world_feed_counter epoch columns AND the world_genesis_checkpoint table: PK (game_id, epoch, aggregate_type, aggregate_id) — the durable genesis identity)
    - internal/world/postgres/outbox_store.go, internal/world/wmodel/envelope.go (the same-tx WriteIntent + the wmodel.Envelope/EnvelopeIntent shapes)
    - internal/world/outbox/taxonomy.go (the genesis kinds from 05-09)
    - internal/world/postgres/feed_counter.go (05-05 — the serializing per-game counter FOR UPDATE lock the idempotency check runs under)
    - docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md (Consistency/lifecycle — genesis snapshot at cutover; feed epoch/reset for DB restores/backfills)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (round-3 MEDIUM at §"Genesis idempotency lacks a schema-level mechanism" — fresh ULIDs + pruning defeat outbox uniques; a pre-insert existence query alone is race-prone)
  </read_first>
  <behavior>
    - RED: genesis snapshot emission produces exactly one envelope per existing aggregate (location/exit/character/object) at the current epoch, each committed atomically WITH its world_genesis_checkpoint row.
    - RED: running the snapshot twice at the same epoch emits NOTHING the second time (checkpoint PK conflict detected under the counter lock, skipped before any position is allocated — no gap, no duplicate); two concurrent snapshot runs cannot double-emit (the checkpoint PK + the serializing counter lock make the race a constraint no-op, not a duplicate envelope).
    - The feed epoch/reset procedure advances the persistent per-game feed epoch (world_feed_counter.epoch); after an advance, the snapshot legitimately re-emits at the new epoch (the checkpoint key includes epoch).
  </behavior>
  <action>
    **Genesis + epoch store:** in `internal/world/postgres/genesis_store.go`, implement the genesis snapshot SQL (one envelope per existing location/exit/character/object at cutover) AND the persistent per-game epoch read/advance (`CurrentEpoch`/`AdvanceEpoch` against `world_feed_counter.epoch` from the 000050 schema) — all in the writer boundary. **Checkpoint-keyed idempotency (round-3 MEDIUM):** for each aggregate, within one transaction, acquire the per-game counter lock (which serializes all envelope writers for the game), check/insert the `world_genesis_checkpoint` row keyed `(game_id, epoch, aggregate_type, aggregate_id)`, and ONLY when the checkpoint insert actually inserted (no PK conflict) allocate the position and write the genesis envelope — a same-epoch re-run skips before allocating (no gap), a conflicting concurrent run aborts on the PK (rolling back its counter increment — still no gap), and the identity survives outbox pruning because the checkpoint table is never pruned. Do NOT rely on a bare pre-insert existence query (race-prone) or on outbox event_id/position uniques (fresh ULIDs; pruned rows). In `internal/world/outbox/genesis.go`, orchestrate: emit the genesis snapshot via the store, and the feed epoch/reset procedure advances the epoch (so DB restores/backfills either emit normal mutations or bump the epoch — INV-WORLD-4 / WRITER-BOUNDARY's admin/migration clause) by calling the postgres genesis/epoch store (NOT raw SQL in the outbox package — writer boundary). NOTE: per-character genesis-on-CREATE (registered/guest/bootstrap-admin) is 05-15's atomic character-genesis service, not this task — this task is the CUTOVER snapshot for pre-existing aggregates. RED tests: one envelope + one checkpoint row per aggregate, atomically; double-run same-epoch emits nothing new; concurrent double-run cannot double-emit; epoch advance re-opens genesis; the advance is observable through the postgres store.
  </action>
  <verify>
    <automated>task test -- ./internal/world/outbox/ && task test:int -- -run 'Genesis|Epoch|Checkpoint' ./internal/world/postgres/ ./internal/world/outbox/</automated>
  </verify>
  <acceptance_criteria>
    - Genesis snapshot emission is one-envelope-per-aggregate, each atomic with its world_genesis_checkpoint row; a same-epoch re-run emits nothing and leaves no position gap; a concurrent re-run cannot double-emit (checkpoint PK + counter-lock serialization — round-3 MEDIUM).
    - No bare pre-insert existence query is the sole idempotency mechanism; the checkpoint PK is the durable identity and survives outbox pruning.
    - The persistent feed-epoch is read/advanced via `internal/world/postgres/genesis_store.go`; after an epoch advance the snapshot re-emits at the new epoch; the reset procedure is tested.
  </acceptance_criteria>
  <done>The feed has a defined genesis origin with schema-backed, race-safe idempotency + a persistent epoch/reset (with a postgres genesis/epoch store) for restores.</done>
</task>

<task type="auto">
  <name>Task 3: Census meta-test — every write command maps to a declared envelope kind</name>
  <files>test/meta/world_envelope_census_test.go</files>
  <read_first>
    - internal/world/mutator.go (the write executor — where the explicit write-command descriptor set lives; NOTE the old world.Mutator interface is an INCOMPLETE subset, missing deletes/moves/scene/character-update/CreateCharacter; the census MUST NOT derive membership from it — Codex finding 10)
    - internal/world/service.go (the actual write-command surface: CreateLocation/UpdateLocation/DeleteLocation/CreateExit/UpdateExit/DeleteExit/CreateObject/UpdateObject/DeleteObject/MoveObject/DeleteCharacter/UpdateCharacterDescription/MoveCharacter/AddSceneParticipant/RemoveSceneParticipant)
    - internal/auth/character_genesis.go (05-15 — the atomic character-genesis service: the ONE sanctioned out-of-world writer, covering registered + guest + bootstrap-admin creation; its descriptor is the out-of-Service census entry)
    - internal/world/outbox/taxonomy.go (the declared-kind registry the census asserts against)
    - test/meta/world_import_graph_test.go (05-07 Task 4 — the composition allowlist the census's completeness claim leans on)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (round-2 census-discovery finding; round-3 census MEDIUM at §"Census completeness is overstated" — the bijection proves registry consistency, NOT discovery of guest/bootstrap direct repo calls; pair it with the call-graph/import restriction)
  </read_first>
  <action>
    Define an EXPLICIT closed write-command construct whose membership is mechanically discoverable — a `writeCommands` descriptor set on the write executor (`internal/world/mutator.go`), where each descriptor names a command AND its taxonomy `kind` (plus the character-genesis descriptor for the atomic character-genesis service, 05-15 — the one sanctioned out-of-world writer). The executor's `mutate()` dispatch and the taxonomy both key off this same set, so a new write command that isn't registered has no kind. Add `test/meta/world_envelope_census_test.go` that asserts a BIJECTION between this registered descriptor set and the declared taxonomy kinds — every registered write command has exactly one declared kind and every declared world-change kind has exactly one registered command — with NO allow-list of pending/exempt commands (D-01). Cross-check: the structural set of `world.Service` mutating methods (via go/ast, as a secondary signal) equals the registered descriptor set — so a mutating method added to Service without a descriptor is caught. **Honest completeness scoping (round-3 census MEDIUM):** document IN THE TEST that the bijection proves registry consistency, and that completeness of production world writes rests on the paired fences — the reader-view compile fence (Task 1), the AST SQL fence (05-09), the internal/world/postgres composition allowlist in the import-graph meta-test (05-07 Task 4), and the removal of Create from the auth-side character-repo interfaces (05-15) — which together make an unregistered production write either uncompilable or an import/SQL-fence failure; the census alone cannot discover a direct repo call in a package that holds a concrete writer (the guest/bootstrap bypasses were exactly that). Examine commands (`ExamineLocation/Object/Character`) are reads — they are NOT in the write-command set and MUST be classified out with a documented reason. Because 05-10 + 05-11 Tasks 1-2 + 05-15 completed coverage, this test passes on introduction. Keep any literal command/kind strings the test negative-checks out of comment bodies.
  </action>
  <verify>
    <automated>task test -- ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - The census asserts a bijection between the executor's explicit registered write-command descriptor set (incl. the 05-15 character-genesis descriptor) and the declared taxonomy kinds (NOT name-prefix inference or the world.Mutator subset), with no allow-list.
    - A secondary go/ast cross-check asserts the structural set of world.Service mutating methods equals the registered descriptor set, so a mutating method added without a descriptor is caught.
    - The census documents its scope honestly (round-3 MEDIUM): bijection = registry consistency; completeness = bijection + reader-view fence + AST SQL fence + composition allowlist + 05-15 interface Create removal.
    - The census passes (coverage complete); examine reads are correctly excluded as non-mutating with a documented reason.
    - Adding a hypothetical new registered command without a declared kind (or a new Service mutating method without a descriptor) fails the census.
  </acceptance_criteria>
  <done>The census structurally forbids an un-migrated command via an explicit closed command construct + a bijection assertion; the D-01 full rollout is proven complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new write command → census | An un-migrated command silently omitted from the feed is the exact ADR drift. |
| genesis/epoch → feed origin | A double-genesis or missing epoch corrupts consumer bootstrap. |
| out-of-Service writers → fence | Packages holding writer repos (guest/bootstrap) escape reader views — closed by 05-15 + the composition allowlist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-32 | Tampering | un-migrated command | high | mitigate | The census meta-test asserts the bijection over the explicit descriptor set with no allow-list; a new un-declared command fails CI. Completeness is paired (round-3 MEDIUM) with the reader-view fence, AST SQL fence (05-09), composition allowlist (05-07), and the 05-15 interface Create removal — together making an unregistered production write uncompilable or fence-caught. |
| T-05-33 | Tampering | genesis idempotency | medium | mitigate | Genesis idempotency is SCHEMA-BACKED (round-3 MEDIUM): the world_genesis_checkpoint row (PK game_id/epoch/aggregate_type/aggregate_id) commits atomically with each genesis envelope, the check runs under the serializing counter lock (race-safe), and the identity survives outbox pruning; the epoch/reset covers restores so stale positions are not replayed. |
| T-05-34 | Elevation of Privilege | character creation bypassing the feed | high | mitigate | Round 3 found guest + bootstrap-admin creation commit characters without genesis envelopes (INV-WORLD-4 false). Closed in 05-15: ALL production character creation routes through the atomic character-genesis service (insert + binding + genesis in one tx, fail-closed); this plan's fence text names that service as the ONE sanctioned out-of-world writer. |
| T-05-44 | Tampering | feed-epoch state absent | medium | mitigate | The promised feed-epoch advance now has persistent schema (world_feed_counter.epoch in migration 000050) + a postgres read/advance store (genesis_store.go); a DB restore/backfill bumps the epoch so stale positions are not replayed (round-2: 05-11 previously promised an epoch advance with no schema/store). |
</threat_model>

<verification>
- `task build` succeeds (no import cycle) and `task test -- ./internal/world/ ./internal/property/ ./internal/world/outbox/ ./test/meta/` passes (run via `local-check`).
- `task lint` green (writer-boundary fence holds; entity_properties in the fence).
- The census meta-test passes and would fail on a hypothetical un-registered/un-declared command; its doc comment states the completeness pairing (fences + allowlist + 05-15).
- The genesis snapshot double-run/concurrent-run tests prove checkpoint-keyed idempotency with no position gap.
</verification>

<success_criteria>
- Every world.Service write command emits exactly one declared-kind envelope; the census passes with no allow-list and honest completeness scoping.
- Genesis snapshot emission (checkpoint-idempotent, race-safe, prune-surviving) + feed epoch/reset exist and are tested.
- The D-01 Service-side mechanical rollout is complete (character creation completes in 05-15).
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-11-SUMMARY.md` when done.
</output>
