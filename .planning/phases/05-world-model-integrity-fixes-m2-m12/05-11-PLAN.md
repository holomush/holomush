---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 11
type: execute
wave: 10
depends_on: [05-10]
files_modified:
  - internal/world/service.go
  - internal/world/service_test.go
  - internal/world/mutator.go
  - internal/property/entity_mutator.go
  - internal/property/entity_mutator_test.go
  - internal/grpc/server.go
  - internal/grpc/auth_handlers.go
  - internal/grpc/auth_handlers_test.go
  - cmd/holomush/sub_grpc.go
  - internal/world/postgres/genesis_store.go
  - internal/world/postgres/genesis_store_test.go
  - internal/world/outbox/genesis.go
  - internal/world/outbox/genesis_test.go
  - test/meta/world_envelope_census_test.go
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "Every remaining world write command (DeleteCharacter, UpdateCharacterDescription, AddSceneParticipant, RemoveSceneParticipant, the property write path, and CreateCharacter) emits exactly one declared-kind envelope in the same transaction — no un-migrated command remains. Scene-participant writes (scene_participants) AND property writes (entity_properties, whose Create/Update were moved onto execerFromCtx in 05-14) enroll in the mutation tx so their envelope commits atomically (Codex finding 4 + round-2 property-leak: scene_repo.go and property_repo.go are both covered)."
    - "With every command now routed through mutate(), the compile-time write fence is COMPLETED here: world.Service's repo fields are switched to the read-only reader views (05-14) so it has no directly-callable write repo, AND the write executor is the sole in-world owner of the writer repos — an envelope-less or direct-repo write is now a type error across the whole service (Codex finding 2). The enforceable INV-WORLD-4 boundary = this reader-view compile fence + the AST SQL fence (05-09, now covering entity_properties); the one sanctioned out-of-world writer is CreateCharacter's registration-flow write, which stays inside the repo package and emits its genesis envelope atomically (round-2 write-fence reconciliation)."
    - "CreateCharacter has NO non-transactional escape hatch in production AND is wired end-to-end for same-tx emission: createCharacterAtomic no longer falls back to a plain create; an OutboxWriter dependency is threaded through CoreServer (a new field on internal/grpc/server.go + a WithOutboxWriter CoreServerOption + assembly at cmd/holomush/sub_grpc.go:477 constructing the postgres outbox store) so the character insert + binding + genesis envelope commit in one transaction via the auth-service/adapter path (characterService.Create), not only the gRPC handler shell (Codex finding 9 + round-2 end-to-end-wiring finding: editing only auth_handlers.go could not obtain the store)."
    - "The census meta-test derives the write-command surface from an EXPLICIT closed command construct whose membership is mechanically discoverable — the executor's write-command descriptor set (each descriptor names its taxonomy kind) — NOT name-prefix inference over method declarations or detection of existing mutate() calls (which would miss a NEW direct write). Combined with the reader-view + SQL fences (a new un-enveloped write cannot compile / cannot write raw SQL), the census asserts a bijection between the registered write commands and declared taxonomy kinds, with no allow-list (Codex finding 10 + round-2 census-discovery finding)."
    - "Genesis snapshot emission emits one envelope per existing aggregate at cutover, and a feed epoch/reset procedure exists for DB restores/backfills — backed by a persistent per-game epoch (the 000050 schema from 05-05) with an internal/world/postgres epoch read/advance store method (round-2: 05-11 promised to advance the epoch but 000050 had no epoch schema and there was no postgres store for it)."
  artifacts:
    - path: test/meta/world_envelope_census_test.go
      provides: "census meta-test: every registered write command → declared envelope kind (explicit closed command construct, no allow-list)"
      contains: "census|envelope"
    - path: internal/world/postgres/genesis_store.go
      provides: "genesis snapshot SQL (one envelope per existing aggregate) + persistent feed-epoch read/advance — in the writer boundary"
      contains: "genesis|epoch"
    - path: internal/world/outbox/genesis.go
      provides: "genesis snapshot orchestration + feed epoch/reset procedure (calls the postgres genesis/epoch store)"
      contains: "genesis|epoch"
  key_links:
    - from: internal/grpc/auth_handlers.go
      to: internal/world/postgres/outbox_store.go
      via: "CreateCharacter emits a character-genesis envelope through the injected OutboxWriter (WithOutboxWriter) inside the same transaction as the character insert + binding"
      pattern: "OutboxWriter|genesis|envelope"
  prohibitions:
    - "No world write command may remain un-migrated — the census forbids an allow-list of pending commands; membership is derived from the explicit command construct, not name inference."
    - "No property write may reach a world-table repo write except through the envelope-carrying seam; property Create/Update MUST be execerFromCtx-enrolled (05-14) and entity_properties is in the SQL fence (05-09)."
    - "CreateCharacter's production path MUST have no non-transactional fallback and MUST have an OutboxWriter wired (a nil OutboxWriter/transactor fails closed) — editing only auth_handlers.go without the server.go/sub_grpc.go dependency is insufficient (round-2)."
---

<objective>
Complete the D-01 mechanical rollout: wire the remaining world write commands (character, scene-participant, property path, and CreateCharacter — the last END-TO-END wired through a CoreServer `OutboxWriter` dependency, round 2), complete the reader-view write fence + writer-executor ownership, add genesis snapshot emission + a persistent feed epoch/reset procedure (backed by the 000050 epoch schema + a postgres epoch store, round 2), and introduce the census meta-test built on an explicit closed command construct (round 2). The census lands here — after coverage is complete — so it passes on introduction (data-first, enforcement-last).

The round-2 review found three end-to-end gaps this plan now closes: (a) property `Create`/`Update` were converted onto `execerFromCtx` in 05-14 so property writes join the mutation tx (this plan wires their envelope through the parent command); (b) CreateCharacter same-tx emission needs an `OutboxWriter` threaded through `CoreServer` (`server.go`) + the gRPC option assembly (`sub_grpc.go:477`) — editing only `auth_handlers.go` cannot obtain the store; (c) the promised feed-epoch advance had no schema/store — the epoch now lives in migration 000050 (05-05) with a postgres read/advance store. The census derives membership from an explicit registered command-descriptor set (not name inference), so a NEW direct write is caught.

Purpose: This closes the "no allow-list of pending commands" requirement. Once the census asserts a bijection between the registered write commands and declared kinds and passes, the drift that caused the ADR is structurally non-recurrable. Genesis emission gives the feed a defined origin at cutover; the persistent epoch/reset covers DB restores/backfills.
Output: remaining commands emitting, the reader-view fence complete, CreateCharacter end-to-end-wired genesis, the census meta-test (green, explicit-construct), genesis + persistent epoch/reset (with a postgres epoch store).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/event-conventions.md
@.claude/rules/gateway-boundary.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Character, scene-participant, and property write commands through the outbox + complete the reader-view fence</name>
  <files>internal/world/service.go, internal/world/service_test.go, internal/property/entity_mutator.go, internal/property/entity_mutator_test.go, internal/world/mutator.go</files>
  <read_first>
    - internal/world/service.go (DeleteCharacter ~602, UpdateCharacterDescription ~656 (version-threaded in 05-04 — now also emits), AddSceneParticipant ~703, RemoveSceneParticipant ~724)
    - internal/property/entity_mutator.go (SetName/SetDescription — funnel into UpdateLocation/UpdateObject, already emitting via 05-10; confirm the property-side envelope is the parent command's, not a duplicate)
    - internal/world/postgres/property_repo.go (Create/Update already moved onto execerFromCtx in 05-14 — READ-ONLY here to confirm; child rows guarded by parent version + delta-parity, NOT a 5th version column)
    - internal/world/postgres/scene_repo.go (AddParticipant/RemoveParticipant already on execerFromCtx from 05-14 — READ-ONLY here)
    - internal/world/mutator.go (the mutate() seam from 05-06; the write executor to make sole writer-repo owner), internal/world/outbox/taxonomy.go
  </read_first>
  <behavior>
    - RED: DeleteCharacter, UpdateCharacterDescription, AddSceneParticipant, RemoveSceneParticipant each emit exactly one declared-kind envelope in the same tx.
    - A property write (SetName/SetDescription) results in exactly one envelope — the parent aggregate's update command envelope — not a duplicate property-level envelope.
    - DeleteCharacter emits a tombstone.
    - world.Service exposes no directly-callable write repo (a direct s.xRepo.Update(...) does not compile).
  </behavior>
  <action>
    Route DeleteCharacter, UpdateCharacterDescription, AddSceneParticipant, RemoveSceneParticipant through `mutate()` with their taxonomy-declared kinds and intent-level payloads, each manifest built from the write's returned `wmodel.MutationDelta`. Scene-participant writes: `SceneRepository.AddParticipant`/`RemoveParticipant` were switched to `execerFromCtx` in 05-14, so wrapping them in `mutate()` commits the `scene_participants` row atomically with the outbox envelope (Codex finding 4). For the property write path: `PropertyRepository.Create`/`Update` were moved onto `execerFromCtx` in 05-14 (round-2 property-leak fix), so a property write funnelled through the parent command (SetName/SetDescription → UpdateLocation/UpdateObject, already emitting from 05-10) commits atomically inside the parent's mutation tx; the property write's envelope IS the parent aggregate's update envelope — ensure exactly one envelope per property command, with the parent's before/after version in the manifest; `entity_properties` is a child guarded by the parent's version + delta-parity, not its own version column, and is now in the SQL fence (05-09). Confirm `property_repo.go` + `scene_repo.go` writes stay inside `internal/world/postgres` (writer-boundary fence from 05-09). **Complete the compile-time fence + writer-executor ownership (Codex finding 2 + round-2 write-fence):** now that every write command routes through the executor's `mutate()`, switch `world.Service`'s repo fields to the read-only reader views (05-14: `LocationReader`/`ExitReader`/`ObjectReader`/`CharacterReader`/`SceneReader`) in `service.go` + `mutator.go`, so Service holds no directly-callable write repo — the writer repos live ONLY on the write executor behind `mutate()` (the executor is the sole in-world owner of the writer repos). Add a test asserting Service's repo field types are the reader interfaces (a direct `s.xRepo.Update(...)` would not compile). Note the enforceable INV-WORLD-4 boundary is this reader-view compile fence PLUS the AST SQL fence (05-09) — Go cannot make a concrete repo method uncallable from a package holding a pool, so the SQL fence (no world-table mutation SQL outside `internal/world/postgres`) is the mechanical backstop that catches any out-of-boundary writer. RED tests first: one envelope per command; property write yields exactly one parent-update envelope; Service exposes no directly-callable write repo.
  </action>
  <verify>
    <automated>task test -- ./internal/world/ ./internal/property/ && task test:int -- -run 'Character|Scene|Property' ./internal/world/...</automated>
  </verify>
  <acceptance_criteria>
    - DeleteCharacter/UpdateCharacterDescription/AddSceneParticipant/RemoveSceneParticipant each emit exactly one declared-kind envelope.
    - A property write yields exactly one parent-update envelope (no duplicate) and commits atomically (property Create/Update on execerFromCtx from 05-14).
    - world.Service's repo fields are the read-only reader views; the write executor is the sole owner of the writer repos; a test asserts no directly-callable write repo remains (the compile-time fence is complete).
    - DeleteCharacter emits a tombstone; `task test -- ./internal/world/ ./internal/property/` passes.
  </acceptance_criteria>
  <done>All remaining world.Service + property write commands emit through the outbox; the reader-view fence + writer-executor ownership are complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CreateCharacter OutboxWriter wiring + genesis envelope + genesis snapshot + feed epoch/reset (with postgres epoch store)</name>
  <files>internal/grpc/server.go, internal/grpc/auth_handlers.go, internal/grpc/auth_handlers_test.go, cmd/holomush/sub_grpc.go, internal/world/postgres/genesis_store.go, internal/world/postgres/genesis_store_test.go, internal/world/outbox/genesis.go, internal/world/outbox/genesis_test.go</files>
  <read_first>
    - internal/grpc/server.go (CoreServer struct ~152 — the deps: transactor ~175 + bindings ~176; add an outboxWriter field here; CoreServerOption ~233)
    - internal/grpc/auth_handlers.go (CreateCharacter ~476; createCharacterAtomic ~517-538 — the transactional path AND the non-transactional FALLBACK at :521-526 that skips the binding/tx when transactor|bindings is nil; WithTransactor ~138 — the option pattern to mirror for WithOutboxWriter; Codex finding 9, Open Question 3)
    - cmd/holomush/sub_grpc.go (~326 charRepo/transactor construction; ~477 coreServerOpts assembly with WithTransactor/WithBindingRepository — add WithOutboxWriter here, constructing the postgres outbox store on the same pool; round-2 end-to-end-wiring finding)
    - internal/world/postgres/outbox_store.go, internal/world/wmodel/envelope.go (the same-tx WriteEnvelope + the wmodel.Envelope/EnvelopeIntent; the OutboxWriter interface from 05-06)
    - internal/world/outbox/taxonomy.go (the character-genesis kind from 05-09)
    - internal/world/postgres/feed_counter.go (05-05 — world_feed_counter has the epoch column; the genesis/epoch store reads/advances it)
    - docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md (Consistency/lifecycle — genesis snapshot at cutover; feed epoch/reset for DB restores/backfills)
    - .claude/rules/gateway-boundary.md (CreateCharacter lives on the core server; the write path is core-side, not gateway)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (Codex finding 9 + round-2: CreateCharacter/epoch not wired end-to-end — CoreServer deps + sub_grpc assembly + epoch schema/store)
  </read_first>
  <behavior>
    - RED: CreateCharacter's world-state write emits exactly one character-genesis envelope in the same tx as the character row insert, via the injected OutboxWriter.
    - RED: in a production configuration, createCharacterAtomic does NOT fall back to a plain non-transactional create — a missing transactor/bindings/OutboxWriter fails closed (returns an error) rather than silently creating a character without its genesis envelope (Codex finding 9). The envelope is emitted inside the atomic transaction alongside characterService.Create + the binding.
    - Genesis snapshot emission produces exactly one envelope per existing aggregate (idempotent — running it twice does not double-emit).
    - The feed epoch/reset procedure advances the persistent per-game feed epoch (world_feed_counter.epoch) so a DB restore/backfill does not replay stale positions; the advance is observable via the postgres epoch store.
  </behavior>
  <action>
    **Wire the OutboxWriter through CoreServer (round-2 end-to-end finding):** add an `outboxWriter world.OutboxWriter` field to `CoreServer` (`server.go:152`) + a `WithOutboxWriter(w world.OutboxWriter) CoreServerOption` (mirror `WithTransactor` at `auth_handlers.go:138`); in `cmd/holomush/sub_grpc.go` (~477, the coreServerOpts assembly) construct the postgres outbox store on the same `pool` and pass `holoGRPC.WithOutboxWriter(outboxStore)`. **Fix `createCharacterAtomic`** (`auth_handlers.go:517-538`): REMOVE the non-transactional fallback at :521-526 for production (a nil transactor/bindings/outboxWriter must fail closed with a typed error, not silently create a character without its genesis envelope — Codex finding 9). Inside the existing `s.transactor.InTransaction` path, after `characterService.Create` + the binding, build a character-genesis `wmodel.EnvelopeIntent` (character-genesis kind) from the created character and emit it via `s.outboxWriter.WriteEnvelope(txCtx, env)` — so the character insert (through the auth adapter → the execerFromCtx-enrolled CharacterRepository.Create) + the binding + the genesis envelope all commit in ONE transaction. If a test-only non-transactional mode must remain, gate it explicitly behind test wiring, never the production default. **Genesis + epoch store (round-2 epoch finding):** in `internal/world/postgres/genesis_store.go`, implement the genesis snapshot SQL (one envelope per existing location/exit/character/object at cutover, idempotent — keyed so a re-run does not double-emit) AND the persistent per-game epoch read/advance (`CurrentEpoch`/`AdvanceEpoch` against `world_feed_counter.epoch` from the 000050 schema) — all in the writer boundary. In `internal/world/outbox/genesis.go`, orchestrate: emit the genesis snapshot via the store, and the feed epoch/reset procedure advances the epoch (so DB restores/backfills either emit normal mutations or bump the epoch — INV-WORLD-4 / WRITER-BOUNDARY's admin/migration clause) by calling the postgres genesis/epoch store (NOT raw SQL in the outbox package — writer boundary). RED tests: CreateCharacter emits one genesis envelope atomically through the injected OutboxWriter; a nil-OutboxWriter/transactor config fails closed (no silent plain create); genesis emission is idempotent; the epoch advance is observable through the postgres store.
  </action>
  <verify>
    <automated>task test -- ./internal/grpc/ ./cmd/holomush/ ./internal/world/outbox/ && task test:int -- -run 'Genesis|CreateCharacter|Epoch' ./...</automated>
  </verify>
  <acceptance_criteria>
    - CoreServer has an outboxWriter field + a WithOutboxWriter option; sub_grpc.go assembles it with the postgres outbox store on the shared pool.
    - CreateCharacter emits one character-genesis envelope in the same tx as the character write (via characterService.Create + the injected OutboxWriter).
    - createCharacterAtomic's production path has no non-transactional fallback: a nil transactor/bindings/OutboxWriter fails closed (asserted by a test), so no character is created without its genesis envelope.
    - The persistent feed-epoch is read/advanced via `internal/world/postgres/genesis_store.go`; genesis snapshot emission is one-envelope-per-aggregate and idempotent; the epoch reset procedure is tested.
  </acceptance_criteria>
  <done>The last write path (CreateCharacter) emits through an end-to-end-wired OutboxWriter; the feed has a defined genesis origin + a persistent epoch/reset (with a postgres epoch store) for restores.</done>
</task>

<task type="auto">
  <name>Task 3: Census meta-test — every write command maps to a declared envelope kind</name>
  <files>test/meta/world_envelope_census_test.go</files>
  <read_first>
    - internal/world/mutator.go (the write executor — where the explicit write-command descriptor set lives; NOTE the old world.Mutator interface is an INCOMPLETE subset, missing deletes/moves/scene/character-update/CreateCharacter; the census MUST NOT derive membership from it — Codex finding 10)
    - internal/world/service.go (the actual write-command surface: CreateLocation/UpdateLocation/DeleteLocation/CreateExit/UpdateExit/DeleteExit/CreateObject/UpdateObject/DeleteObject/MoveObject/DeleteCharacter/UpdateCharacterDescription/MoveCharacter/AddSceneParticipant/RemoveSceneParticipant)
    - internal/grpc/auth_handlers.go (CreateCharacter — the world-state write outside world.Service, one sanctioned out-of-world writer)
    - internal/world/outbox/taxonomy.go (the declared-kind registry the census asserts against)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (round-2 census-discovery finding: go/ast over method declarations doesn't reveal mutation, and detecting existing mutate() calls misses a NEW direct write — the census needs an explicit closed command construct)
  </read_first>
  <action>
    Define an EXPLICIT closed write-command construct whose membership is mechanically discoverable — a `writeCommands` descriptor set on the write executor (`internal/world/mutator.go`), where each descriptor names a command AND its taxonomy `kind` (plus the CreateCharacter genesis descriptor). The executor's `mutate()` dispatch and the taxonomy both key off this same set, so a new write command that isn't registered has no kind. Add `test/meta/world_envelope_census_test.go` that asserts a BIJECTION between this registered descriptor set and the declared taxonomy kinds — every registered write command has exactly one declared kind and every declared world-change kind has exactly one registered command — with NO allow-list of pending/exempt commands (D-01). This is mechanically sound where name-prefix inference or "detect existing mutate() calls" is not (round-2 finding): a NEW direct write cannot compile (the reader-view fence, Task 1) nor write raw SQL (the AST fence, 05-09), and a new write command that IS registered must carry a kind or the bijection fails. Cross-check: the structural set of `world.Service` mutating methods (via go/ast, as a secondary signal) equals the registered descriptor set — so a mutating method added to Service without a descriptor is caught. Examine commands (`ExamineLocation/Object/Character`) are reads — they are NOT in the write-command set and MUST be classified out with a documented reason. Because 05-10 + 05-11 Tasks 1-2 completed coverage, this test passes on introduction. Keep any literal command/kind strings the test negative-checks out of comment bodies.
  </action>
  <verify>
    <automated>task test -- ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - The census asserts a bijection between the executor's explicit registered write-command descriptor set and the declared taxonomy kinds (NOT name-prefix inference or the world.Mutator subset), with no allow-list.
    - A secondary go/ast cross-check asserts the structural set of world.Service mutating methods equals the registered descriptor set, so a mutating method added without a descriptor is caught.
    - The census passes (coverage complete); examine reads are correctly excluded as non-mutating with a documented reason.
    - Adding a hypothetical new registered command without a declared kind (or a new Service mutating method without a descriptor) fails the census.
  </acceptance_criteria>
  <done>The census structurally forbids an un-migrated command via an explicit closed command construct + a bijection assertion; the D-01 full rollout is proven complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CreateCharacter (gRPC flow) → world tables | A character-creation write outside the world writer boundary escapes the guard/feed. |
| new write command → census | An un-migrated command silently omitted from the feed is the exact ADR drift. |
| genesis/epoch → feed origin | A double-genesis or missing epoch corrupts consumer bootstrap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-32 | Tampering | un-migrated command | high | mitigate | The census meta-test derives the command surface structurally (go/ast/go/types over the real write methods, not the incomplete world.Mutator subset — finding 10) and asserts every write command maps to a declared kind with no allow-list; a new un-declared command fails CI — the drift is structurally non-recurrable. |
| T-05-33 | Tampering | genesis idempotency | medium | mitigate | Genesis emission is one-per-aggregate and idempotent (re-run does not double-emit); the epoch/reset covers restores so stale positions are not replayed. |
| T-05-34 | Elevation of Privilege | CreateCharacter non-transactional fallback | high | mitigate | The createCharacterAtomic non-transactional fallback (auth_handlers.go:521-526) is removed for production and fails closed — a nil transactor/bindings/OutboxWriter errors rather than creating a character without its genesis envelope (Codex finding 9). The OutboxWriter is wired end-to-end through CoreServer (server.go) + sub_grpc.go:477 (round-2), so the write stays inside the writer boundary (fence from 05-09) and emits atomically. |
| T-05-44 | Tampering | feed-epoch state absent | medium | mitigate | The promised feed-epoch advance now has persistent schema (world_feed_counter.epoch in migration 000050) + a postgres read/advance store (genesis_store.go); a DB restore/backfill bumps the epoch so stale positions are not replayed (round-2: 05-11 previously promised an epoch advance with no schema/store). |
</threat_model>

<verification>
- `task build` succeeds (no import cycle) and `task test -- ./internal/world/ ./internal/property/ ./internal/grpc/ ./cmd/holomush/ ./internal/world/outbox/ ./test/meta/` passes (run via `local-check`).
- `task lint` green (writer-boundary fence holds with CreateCharacter wired; entity_properties in the fence).
- The census meta-test passes and would fail on a hypothetical un-registered/un-declared command; the CreateCharacter fail-closed test passes with a nil OutboxWriter.
</verification>

<success_criteria>
- Every world write command emits exactly one declared-kind envelope; the census passes with no allow-list.
- Genesis snapshot emission + feed epoch/reset exist and are tested.
- The D-01 full mechanical rollout is complete.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-11-SUMMARY.md` when done.
</output>
