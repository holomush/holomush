---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 11
type: execute
wave: 10
depends_on: [05-10, 05-15]
files_modified:
  - internal/world/service.go
  - internal/world/service_test.go
  - internal/world/mutator.go
  - internal/property/entity_mutator.go
  - internal/property/entity_mutator_test.go
  - internal/world/postgres/genesis_store.go
  - internal/world/postgres/genesis_store_test.go
  - internal/world/outbox/genesis.go
  - internal/world/outbox/genesis_test.go
  - cmd/holomush/world_genesis.go
  - cmd/holomush/world_genesis_test.go
  - test/meta/world_envelope_census_test.go
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "Every remaining world.Service write command (DeleteCharacter, UpdateCharacterDescription, and the property write path) emits exactly one declared-kind envelope in the same transaction — no un-migrated command remains. Property writes (entity_properties, whose Create/Update were moved onto execerFromCtx in 05-14) enroll in the mutation tx so their envelope commits atomically (round-2 property-leak). SCENE-PARTICIPANT commands are NOT wired here — round-5 D-07 REMOVED the vestigial world.Service.AddSceneParticipant/RemoveSceneParticipant surface in 05-14 (only _test.go referenced them), so they no longer exist to migrate — this resolves the D-01↔D-05 scene-participant contradiction by removal (the round-5 Codex 'D-05 vs outbox' finding). Character CREATION (registered + guest + bootstrap-admin) is NOT wired here — round-3 blocker #5 moved it to the atomic character-genesis service in 05-15. Character DELETION for the GUEST lifecycle (reaper + failed-guest cleanup) is NOT wired here either — round-5 D-06 moved it to the atomic character-reaping service in 05-16 (the deletion-side counterpart to 05-15); world.Service.DeleteCharacter (a normal command) IS wired here and emits the same character delete/tombstone kind the reaping service reuses."
    - "With every command now routed through mutate(), the compile-time write fence is COMPLETED here: world.Service's repo fields are switched to the read-only reader views (05-14) so it has no directly-callable write repo, AND the write executor is the sole in-world owner of the writer repos — an envelope-less or direct-repo write is now a type error across the whole service (Codex finding 2). The enforceable INV-WORLD-4 boundary = this reader-view compile fence + the AST SQL fence (05-09, covering entity_properties) + the import-graph composition allowlist (05-07 Task 4) + the TWO sanctioned out-of-world writers: the atomic character-genesis application service (05-15 — CREATION: registered, guest, AND bootstrap-admin creation emit their genesis envelope atomically; round-3 blocker #5) AND the atomic character-reaping application service (05-16 — DELETION: guest reaping + failed-guest cleanup emit one character tombstone envelope per reaped character atomically; round-5 D-06: guest_reaper.go/guest_service.go cleanup previously deleted characters via FK cascade with no tombstone, and reader views alone cannot fence packages that hold a concrete writer repo)."
    - "The census meta-test derives the write-command surface from an EXPLICIT closed command construct whose membership is mechanically discoverable — the executor's write-command descriptor set (each descriptor names its taxonomy kind) — NOT name-prefix inference over method declarations or detection of existing mutate() calls (which would miss a NEW direct write). The census proves REGISTRY CONSISTENCY (bijection between registered write commands and declared taxonomy kinds, no allow-list); COMPLETENESS of production world writes is claimed only in combination with the fences that make an unregistered write impossible: the reader-view compile fence, the AST SQL fence (05-09), the internal/world/postgres composition allowlist (05-07), and the removal of Create from the auth-side character-repo interfaces (05-15) (round-3 census MEDIUM: a descriptor bijection alone cannot discover guest/bootstrap direct repo calls — do not overstate it)."
    - "Genesis snapshot emission emits one envelope per existing aggregate at cutover and is idempotent via the DURABLE world_genesis_checkpoint schema (000050, round-3 MEDIUM): each genesis envelope inserts its checkpoint row (game_id, epoch, aggregate_type, aggregate_id) in the SAME transaction; a same-epoch re-run conflicts on the checkpoint PK and skips BEFORE allocating a feed position (no gap, race-safe under the serializing counter lock — a pre-insert existence query alone would be race-prone and outbox uniques cannot help because genesis uses fresh ULIDs and published rows get pruned)."
    - "Epoch reset is ONE LOCKED, COMPLETE operation (round-6 Codex MEDIUM — the round-5 AdvanceEpoch updated world_feed_counter.epoch but NOT next_position, did not reject/quarantine unpublished old-epoch outbox rows, and did not coordinate the relay; an epoch increment inheriting the old next_position is not the promised 'restart cleanly', and an epoch change while old rows remain makes relay ordering ambiguous): AdvanceEpoch acquires the per-game world_feed_counter row lock (SELECT ... FOR UPDATE), VERIFIES no unresolved/unpublished old-epoch outbox row remains (published_at IS NULL) — or explicitly QUARANTINES them (records + marks them so the relay does not publish stale-epoch positions) — INCREMENTS epoch, RESETS next_position to the defined origin (so positions restart at the new epoch, not inherit the old counter), records reset metadata, and NOTIFIES/restarts the relay — all under the one lock. An integration test runs the reset with a relay active AND old-epoch unpublished rows present, asserting the old rows are quarantined (not published under the new epoch) and next_position restarts at the origin."
    - "Genesis orchestration reaches its postgres store through a CONSUMER-OWNED GenesisStore interface declared in internal/world/outbox (implemented by internal/world/postgres/genesis_store.go, injected at the composition root) — so internal/world/outbox does NOT import internal/world/postgres (round-4 A3: the round-3 shape wired outbox/genesis.go → postgres/genesis_store.go directly, contradicting 05-07's outbox→postgres prohibition). The 05-07 Task-4 eight-edge import guard MUST stay green after this wiring — the interface keeps the outbox→postgres edge absent, exactly like the OutboxStore/Lease seam."
    - "Genesis snapshot + epoch reset have a REAL operator entry point (round-4 A3: they were library-only with no caller invoking them 'at cutover'): a `holomush world genesis --game <id>` (emit the cutover snapshot) + `holomush world epoch-reset --game <id>` (advance the feed epoch for a DB restore/backfill) admin CLI subcommand — mirroring the 05-07 `holomush outbox skip` pattern — constructs the postgres GenesisStore impl, injects it into the outbox genesis orchestration, and invokes it. It is off crypto/abac surfaces."
  artifacts:
    - path: test/meta/world_envelope_census_test.go
      provides: "census meta-test: every registered write command → declared envelope kind (explicit closed command construct, no allow-list)"
      contains: "census|envelope"
    - path: internal/world/postgres/genesis_store.go
      provides: "genesis snapshot SQL (one envelope per existing aggregate, checkpoint-idempotent) + persistent feed-epoch read/advance — in the writer boundary"
      contains: "genesis|epoch"
    - path: internal/world/outbox/genesis.go
      provides: "genesis snapshot orchestration (checkpoint-keyed idempotency) + feed epoch/reset procedure + the CONSUMER-OWNED GenesisStore interface it depends on (round-4 A3 — impl in postgres, injected; keeps outbox from importing postgres so the 05-07 eight-edge guard stays green)"
      contains: "genesis|epoch|GenesisStore"
    - path: cmd/holomush/world_genesis.go
      provides: "holomush world genesis / world epoch-reset admin CLI (round-4 A3 — the real operator entry point at cutover / DB-restore): constructs the postgres GenesisStore impl, injects it into the outbox genesis orchestration, invokes snapshot/epoch-advance"
      contains: "genesis|epoch"
  key_links:
    - from: internal/world/outbox/genesis.go
      to: internal/world/postgres/genesis_store.go
      via: "genesis orchestration emits one envelope per aggregate through the CONSUMER-OWNED GenesisStore interface (declared in outbox, implemented by postgres, injected at the composition root — round-4 A3: NO direct outbox→postgres import), inserting the world_genesis_checkpoint row in the same tx (idempotency key)"
      pattern: "GenesisStore|genesis|checkpoint|epoch"
    - from: cmd/holomush/world_genesis.go
      to: internal/world/outbox/genesis.go
      via: "the admin CLI constructs the postgres GenesisStore impl and injects it into the outbox genesis orchestration (composition root — the real 'at cutover' caller, round-4 A3)"
      pattern: "genesis|epoch|GenesisStore"
  prohibitions:
    - "No world write command may remain un-migrated — the census forbids an allow-list of pending commands; membership is derived from the explicit command construct, not name inference."
    - "No property write may reach a world-table repo write except through the envelope-carrying seam; property Create/Update MUST be execerFromCtx-enrolled (05-14) and entity_properties is in the SQL fence (05-09)."
    - "Genesis idempotency MUST NOT rest on a pre-insert existence query alone (race-prone, round-3 MEDIUM) — the world_genesis_checkpoint row is inserted atomically with each genesis envelope and its PK is the durable identity; the existence check runs under the serializing per-game counter lock with the PK as backstop."
    - "The census MUST NOT be presented as proof of production write-path completeness on its own — completeness claims cite the fences (reader views, AST SQL fence, composition allowlist, 05-15 interface Create removal, 05-16 guest-deletion routed through the reaping service) alongside the bijection (round-3 census MEDIUM). No scene-participant command may appear in the census (round-5 D-07 removed the surface in 05-14)."
    - "internal/world/outbox/genesis.go MUST NOT import internal/world/postgres (round-4 A3 — it violates 05-07's outbox→postgres prohibition and the eight-edge import guard). Genesis orchestration reaches its store ONLY through the consumer-owned GenesisStore interface declared in internal/world/outbox; the postgres impl is injected at the composition root (the admin CLI / bootstrap). Run the 05-07 Task-4 import-graph guard after this wiring — it MUST stay green."
    - "Genesis snapshot + epoch reset MUST have a real caller (round-4 A3) — the holomush world genesis / world epoch-reset admin CLI. They MUST NOT ship as library-only code with no operator/lifecycle entry point invoking them 'at cutover'."
---

<objective>
Complete the D-01 mechanical rollout inside `world.Service`: wire the remaining write commands (character update/delete, scene-participant, property path), complete the reader-view write fence + writer-executor ownership, add genesis snapshot emission with DURABLE checkpoint-keyed idempotency + a persistent feed epoch/reset procedure (000050 schema + a postgres epoch store) reached through a CONSUMER-OWNED GenesisStore interface (no outbox→postgres import) + a real `holomush world genesis` / `world epoch-reset` admin-CLI operator entry (round-4 A3), and introduce the census meta-test built on an explicit closed command construct. The census lands here — after Service coverage is complete — so it passes on introduction (data-first, enforcement-last).

**Round-3 restructure (blocker #5):** CreateCharacter wiring is NO LONGER in this plan. Round 3 found the round-2 shape (an `OutboxWriter` threaded through `CoreServer` for the gRPC handler) covered only ONE of THREE production character-creation paths — guest creation (`internal/auth/guest_service.go:134-146`) and bootstrap-admin creation (`internal/bootstrap/admin.go:85-98` via `CharRepoAdapter`, `adapters.go:38-40`) would still commit character rows WITHOUT genesis envelopes, making INV-WORLD-4 false. ALL character creation now routes through ONE atomic character-genesis application service, built and wired in **05-15** (which supersedes the round-2 CoreServer `WithOutboxWriter` design — same end-to-end guarantee, delivered through the service). This plan keeps: the Service-side rollout, the reader-view fence, genesis snapshot + epoch, and the census.

**Round-3 genesis idempotency (MEDIUM):** genesis uses fresh event ULIDs and published outbox rows are pruned, so no outbox unique can make a re-run idempotent. The durable identity is the `world_genesis_checkpoint` table (000050): each aggregate's genesis envelope inserts its checkpoint row in the same tx; the same-epoch re-run check runs under the serializing per-game counter lock (race-safe) with the checkpoint PK as backstop; an epoch advance re-opens genesis.

Purpose: This closes the "no allow-list of pending commands" requirement. Once the census asserts a bijection between the registered write commands and declared kinds and passes, the drift that caused the ADR is structurally non-recurrable. Genesis emission gives the feed a defined origin at cutover; the persistent epoch/reset covers DB restores/backfills.
Output: remaining Service commands emitting, the reader-view fence complete, the census meta-test (green, explicit-construct, honestly-scoped), genesis + checkpoint idempotency + persistent epoch/reset (with a postgres genesis/epoch store).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/event-conventions.md
@.claude/rules/gateway-boundary.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Character + property write commands through the outbox + complete the reader-view fence</name>
  <files>internal/world/service.go, internal/world/service_test.go, internal/property/entity_mutator.go, internal/property/entity_mutator_test.go, internal/world/mutator.go</files>
  <read_first>
    - internal/world/service.go (DeleteCharacter ~602, UpdateCharacterDescription ~656 (version-threaded in 05-04 — now also emits); NOTE AddSceneParticipant/RemoveSceneParticipant are REMOVED in 05-14 per round-5 D-07 — they no longer exist here)
    - internal/property/entity_mutator.go (SetName/SetDescription — funnel into UpdateLocation/UpdateObject, already emitting via 05-10; confirm the property-side envelope is the parent command's, not a duplicate)
    - internal/world/postgres/property_repo.go (Create/Update already moved onto execerFromCtx in 05-14 — READ-ONLY here to confirm; child rows guarded by parent version + delta-parity, NOT a 5th version column)
    - internal/world/mutator.go (the mutate(ctx, intent, write-closure) seam from 05-06 — round-5 finding 1: each command calls the executor's per-operation method that builds the write closure; the write executor is the sole writer-repo owner), internal/world/outbox/taxonomy.go
  </read_first>
  <behavior>
    - RED: DeleteCharacter and UpdateCharacterDescription each emit exactly one declared-kind envelope in the same tx (via the executor's per-operation method → mutate(intent, closure), round-5 finding 1).
    - A property write (SetName/SetDescription) results in exactly one envelope — the parent aggregate's update command envelope — not a duplicate property-level envelope.
    - DeleteCharacter emits a character delete/tombstone envelope (the same kind the guest CharacterReapingService reuses, 05-16/D-06).
    - world.Service exposes no directly-callable write repo (a direct s.xRepo.Update(...) does not compile).
    - There is no AddSceneParticipant/RemoveSceneParticipant command to wire (removed in 05-14, round-5 D-07).
  </behavior>
  <action>
    Route DeleteCharacter and UpdateCharacterDescription through the executor's per-operation methods → `mutate(ctx, intent, write)` (round-5 finding 1 — the command builds its `wmodel.EnvelopeIntent` and the executor method builds the write closure calling the guarded writer method; the manifest is built by the WRITER from the closure's returned `wmodel.MutationDelta`). DeleteCharacter uses the character delete/tombstone kind (the SAME kind the guest CharacterReapingService reuses, 05-16/D-06 — consumers treat all character tombstones uniformly). **NO scene-participant wiring (round-5 D-07):** `world.Service.AddSceneParticipant`/`RemoveSceneParticipant` and `SceneRepository.AddParticipant`/`RemoveParticipant` were REMOVED in 05-14 (only `_test.go` referenced them; the vestigial `public.scene_participants` write surface), so there is no scene-participant command to route — do NOT reintroduce them. For the property write path: `PropertyRepository.Create`/`Update` were moved onto `execerFromCtx` in 05-14 (round-2 property-leak fix), so a property write funnelled through the parent command (SetName/SetDescription → UpdateLocation/UpdateObject, already emitting from 05-10) commits atomically inside the parent's mutation tx; the property write's envelope IS the parent aggregate's update envelope — ensure exactly one envelope per property command, with the parent's before/after version in the manifest; `entity_properties` is a child guarded by the parent's version + delta-parity, not its own version column, and is now in the SQL fence (05-09). Confirm `property_repo.go` writes stay inside `internal/world/postgres` (writer-boundary fence from 05-09). **Complete the compile-time fence + writer-executor ownership (Codex finding 2 + round-2 write-fence):** now that every write command routes through the executor's `mutate()`, switch `world.Service`'s repo fields to the read-only reader views (05-14: `LocationReader`/`ExitReader`/`ObjectReader`/`CharacterReader`/`SceneReader`) in `service.go` + `mutator.go`, so Service holds no directly-callable write repo — the writer repos live ONLY on the write executor behind `mutate()` (the executor is the sole in-world owner of the writer repos). Add a test asserting Service's repo field types are the reader interfaces (a direct `s.xRepo.Update(...)` would not compile). Note the enforceable INV-WORLD-4 boundary is this reader-view compile fence PLUS the AST SQL fence (05-09) PLUS the import-graph composition allowlist (05-07 Task 4 — only composition/test packages may import `internal/world/postgres`, so no new production package can hold a concrete writer) PLUS the TWO atomic out-of-world application services — the character-genesis service (05-15, creation) AND the character-reaping service (05-16, guest deletion; round-5 D-06) — the only sanctioned out-of-world writers; round-3 blocker #5 + round-5 D-06: guest/bootstrap-admin creation and guest reaping/cleanup had been holding concrete/narrow writer repos (or relying on an FK cascade) that reader views cannot fence. Go cannot make a concrete repo method uncallable from a package holding a pool, so the SQL fence + allowlist are the mechanical backstops that catch any out-of-boundary writer. RED tests first: one envelope per command; property write yields exactly one parent-update envelope; Service exposes no directly-callable write repo.
  </action>
  <verify>
    <automated>task test -- ./internal/world/ ./internal/property/ && task test:int -- -run 'Character|Scene|Property' ./internal/world/...</automated>
  </verify>
  <acceptance_criteria>
    - DeleteCharacter and UpdateCharacterDescription each emit exactly one declared-kind envelope via the executor's per-operation method → mutate(intent, closure) (round-5 finding 1).
    - There is no AddSceneParticipant/RemoveSceneParticipant on world.Service (removed in 05-14, round-5 D-07): `grep -n 'SceneParticipant' internal/world/service.go` returns nothing.
    - A property write yields exactly one parent-update envelope (no duplicate) and commits atomically (property Create/Update on execerFromCtx from 05-14).
    - world.Service's repo fields are the read-only reader views; the write executor is the sole owner of the writer repos; a test asserts no directly-callable write repo remains (the compile-time fence is complete).
    - DeleteCharacter emits a character delete/tombstone envelope; `task test -- ./internal/world/ ./internal/property/` passes.
  </acceptance_criteria>
  <done>All remaining world.Service + property write commands emit through the outbox; the reader-view fence + writer-executor ownership are complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Genesis snapshot (checkpoint-idempotent) + feed epoch/reset behind a consumer-owned GenesisStore interface + the admin CLI operator entry (round-4 A3)</name>
  <files>internal/world/postgres/genesis_store.go, internal/world/postgres/genesis_store_test.go, internal/world/outbox/genesis.go, internal/world/outbox/genesis_test.go, cmd/holomush/world_genesis.go, cmd/holomush/world_genesis_test.go</files>
  <read_first>
    - internal/store/migrations/000050_world_outbox.up.sql (05-05 — the outbox + world_feed_counter epoch columns AND the world_genesis_checkpoint table: PK (game_id, epoch, aggregate_type, aggregate_id) — the durable genesis identity)
    - internal/world/postgres/outbox_store.go, internal/world/wmodel/envelope.go (the same-tx WriteIntent + the wmodel.Envelope/EnvelopeIntent shapes)
    - internal/world/outbox/taxonomy.go (the genesis kinds from 05-09)
    - internal/world/postgres/feed_counter.go (05-05 — the serializing per-game counter FOR UPDATE lock the idempotency check runs under)
    - docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md (Consistency/lifecycle — genesis snapshot at cutover; feed epoch/reset for DB restores/backfills)
    - internal/world/outbox/store.go, internal/world/outbox/skip.go (05-07 — the OutboxStore/Lease + SkipService pattern: a consumer-owned interface in outbox + a postgres impl injected + a `holomush outbox skip` admin CLI; MIRROR this for GenesisStore + the `holomush world genesis` CLI — round-4 A3)
    - cmd/holomush/outbox_admin.go (05-07 — the admin CLI shape to mirror: cobra subcommand, DB config, constructs the postgres impl and drives the outbox-owned service)
    - test/meta/world_import_graph_test.go (05-07 Task 4 — the eight-edge import guard that MUST stay green: outbox→postgres is forbidden, so genesis MUST reach postgres only through the GenesisStore interface)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (round-3 MEDIUM at §"Genesis idempotency lacks a schema-level mechanism" — fresh ULIDs + pruning defeat outbox uniques; round-4 A3 — the consumer-owned GenesisStore interface to avoid the outbox→postgres edge + the real operator/lifecycle entry point)
  </read_first>
  <behavior>
    - RED: genesis snapshot emission produces exactly one envelope per existing aggregate (location/exit/character/object) at the current epoch, each committed atomically WITH its world_genesis_checkpoint row.
    - RED: running the snapshot twice at the same epoch emits NOTHING the second time (checkpoint PK conflict detected under the counter lock, skipped before any position is allocated — no gap, no duplicate); two concurrent snapshot runs cannot double-emit (the checkpoint PK + the serializing counter lock make the race a constraint no-op, not a duplicate envelope).
    - The feed epoch/reset procedure advances the persistent per-game feed epoch (world_feed_counter.epoch); after an advance, the snapshot legitimately re-emits at the new epoch (the checkpoint key includes epoch).
    - RED (round-6 Codex MEDIUM — complete, locked epoch reset): AdvanceEpoch, under the per-game counter row lock in one operation, (a) verifies/quarantines any unpublished old-epoch outbox row, (b) increments epoch, (c) RESETS next_position to the defined origin, (d) records reset metadata, (e) notifies/restarts the relay — a test with a relay active AND old-epoch unpublished rows present asserts the old rows are NOT published under the new epoch (quarantined) and next_position restarts at the origin (not inherited).
  </behavior>
  <action>
    **Genesis + epoch store:** in `internal/world/postgres/genesis_store.go`, implement the genesis snapshot SQL (one envelope per existing location/exit/character/object at cutover) AND the persistent per-game epoch read/advance (`CurrentEpoch`/`AdvanceEpoch` against `world_feed_counter.epoch` from the 000050 schema) — all in the writer boundary. **AdvanceEpoch is ONE LOCKED, COMPLETE operation (round-6 Codex MEDIUM):** under the per-game `world_feed_counter` row lock (`SELECT ... FOR UPDATE`), it (a) VERIFIES no unpublished old-epoch outbox row remains (`published_at IS NULL`) — or QUARANTINES them (records + marks them so the relay never publishes a stale-epoch position, avoiding the ambiguous-ordering hazard of old rows surviving an epoch change); (b) INCREMENTS `epoch`; (c) RESETS `next_position` to the defined origin (so positions restart cleanly at the new epoch, NOT inherit the old counter — the round-5 gap); (d) records reset metadata; (e) NOTIFIES/restarts the relay (coordinates relay state so it re-reads the counter) — all within the single lock, so no writer allocates a position against a half-reset counter. **Checkpoint-keyed idempotency (round-3 MEDIUM):** for each aggregate, within one transaction, acquire the per-game counter lock (which serializes all envelope writers for the game), check/insert the `world_genesis_checkpoint` row keyed `(game_id, epoch, aggregate_type, aggregate_id)`, and ONLY when the checkpoint insert actually inserted (no PK conflict) allocate the position and write the genesis envelope — a same-epoch re-run skips before allocating (no gap), a conflicting concurrent run aborts on the PK (rolling back its counter increment — still no gap), and the identity survives outbox pruning because the checkpoint table is never pruned. Do NOT rely on a bare pre-insert existence query (race-prone) or on outbox event_id/position uniques (fresh ULIDs; pruned rows). **Consumer-owned GenesisStore interface (round-4 A3 — no outbox→postgres import):** declare a `GenesisStore` interface in `internal/world/outbox` (co-located with `genesis.go`, mirroring the 05-07 `OutboxStore`/`Lease` seam) exposing the snapshot-emit + checkpoint + `CurrentEpoch`/`AdvanceEpoch` operations the orchestration needs; `internal/world/postgres/genesis_store.go` IMPLEMENTS it; the concrete impl is INJECTED at the composition root. In `internal/world/outbox/genesis.go`, orchestrate: emit the genesis snapshot and run the feed epoch/reset procedure (so DB restores/backfills either emit normal mutations or bump the epoch — INV-WORLD-4 / WRITER-BOUNDARY's admin/migration clause) THROUGH the injected `GenesisStore` interface — NOT by importing `internal/world/postgres` (round-4 A3: the round-3 direct wiring violated 05-07's outbox→postgres prohibition; the writer SQL still lives only in `internal/world/postgres/genesis_store.go`). Run the 05-07 Task-4 eight-edge import guard afterward — `outbox→postgres` MUST stay absent. **Operator entry point (round-4 A3):** add a `holomush world genesis --game <id>` (emit the cutover snapshot) + `holomush world epoch-reset --game <id>` (advance the feed epoch after a DB restore/backfill) admin CLI subcommand in `cmd/holomush/world_genesis.go` — register it under the cobra root, mirroring the `holomush outbox skip` shape (05-07): it connects to the DB, constructs the postgres `GenesisStore` impl, injects it into the outbox genesis orchestration, and invokes snapshot / epoch-advance; keep it off crypto/abac surfaces (no `internal/access`/crypto import). NOTE: per-character genesis-on-CREATE (registered/guest/bootstrap-admin) is 05-15's atomic character-genesis service, not this task — this task is the CUTOVER snapshot for pre-existing aggregates. RED tests: one envelope + one checkpoint row per aggregate, atomically; double-run same-epoch emits nothing new; concurrent double-run cannot double-emit; epoch advance re-opens genesis; the advance is observable through the GenesisStore; **the complete epoch reset (round-6 Codex MEDIUM) — a test with a relay active AND old-epoch unpublished outbox rows present asserts AdvanceEpoch quarantines the old rows (they are NOT published under the new epoch), resets next_position to the origin (a post-reset write allocates from the origin, not the inherited counter), and coordinates the relay;** the CLI drives snapshot + epoch-reset (a CLI test constructs the subcommand and invokes it against a real pool).
  </action>
  <verify>
    <automated>task test -- ./internal/world/outbox/ ./cmd/holomush/ ./test/meta/ && task test:int -- -run 'Genesis|Epoch|Checkpoint' ./internal/world/postgres/ ./internal/world/outbox/</automated>
  </verify>
  <acceptance_criteria>
    - Genesis snapshot emission is one-envelope-per-aggregate, each atomic with its world_genesis_checkpoint row; a same-epoch re-run emits nothing and leaves no position gap; a concurrent re-run cannot double-emit (checkpoint PK + counter-lock serialization — round-3 MEDIUM).
    - No bare pre-insert existence query is the sole idempotency mechanism; the checkpoint PK is the durable identity and survives outbox pruning.
    - The persistent feed-epoch is read/advanced via `internal/world/postgres/genesis_store.go`; after an epoch advance the snapshot re-emits at the new epoch; AdvanceEpoch is ONE locked operation that (round-6 Codex MEDIUM) verifies/quarantines unpublished old-epoch rows, resets next_position to the origin, records reset metadata, and coordinates the relay — an integration test with an active relay + old-epoch unpublished rows proves the old rows are not published under the new epoch and next_position restarts at the origin.
    - Genesis orchestration reaches postgres ONLY through the consumer-owned `GenesisStore` interface (round-4 A3): `grep -rn 'internal/world/postgres' internal/world/outbox/genesis.go` returns no import, and the 05-07 Task-4 eight-edge import guard is green (outbox→postgres absent).
    - `holomush world genesis --game <id>` and `holomush world epoch-reset --game <id>` exist as the real operator entry point (round-4 A3), construct the postgres GenesisStore impl, inject it, and drive snapshot / epoch-advance; a CLI test exercises them; the subcommand imports no crypto/abac-gated file.
  </acceptance_criteria>
  <done>The feed has a defined genesis origin with schema-backed, race-safe idempotency + a persistent epoch/reset (with a postgres genesis/epoch store) for restores.</done>
</task>

<task type="auto">
  <name>Task 3: Census meta-test — every write command maps to a declared envelope kind</name>
  <files>test/meta/world_envelope_census_test.go</files>
  <read_first>
    - internal/world/mutator.go (the write executor — where the explicit write-command descriptor set lives; NOTE the old world.Mutator interface is an INCOMPLETE subset, missing deletes/moves/scene/character-update/CreateCharacter; the census MUST NOT derive membership from it — Codex finding 10)
    - internal/world/service.go (the actual write-command surface: CreateLocation/UpdateLocation/DeleteLocation/CreateExit/UpdateExit/DeleteExit/CreateObject/UpdateObject/DeleteObject/MoveObject/DeleteCharacter/UpdateCharacterDescription/MoveCharacter/UpdateCharacterPreferences — the last is the round-4 C5 character_settings fold-in registered in 05-09 Task 2, so it MUST appear in the census bijection with the character_preferences_update kind. NOTE AddSceneParticipant/RemoveSceneParticipant are REMOVED (round-5 D-07) — they are NOT in the surface and MUST NOT be in the census)
    - NOTE (round-9 R6-5 MEDIUM — census split to avoid a wave-10 file race): the OUT-OF-SERVICE producer census (asserting the character-genesis service 05-15 AND the character-reaping service 05-16 each emit a DECLARED kind) is NOT done here — it is done in 05-12, which depends_on BOTH 05-15 and 05-16. This plan's census (this task) asserts ONLY the IN-SERVICE bijection + the go/ast Service-mutating-method cross-check, so it does NOT read `internal/auth/character_reaping.go` (which 05-16 CREATES in the SAME wave 10 — Codex R6 finding: wave-parallel execution could run this census before that file exists). Do NOT read/assert `character_genesis.go` / `character_reaping.go` here.
    - internal/world/outbox/taxonomy.go (the declared-kind registry the census asserts against)
    - test/meta/world_import_graph_test.go (05-07 Task 4 — the composition allowlist the census's completeness claim leans on)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (round-2 census-discovery finding; round-3 census MEDIUM at §"Census completeness is overstated" — the bijection proves registry consistency, NOT discovery of guest/bootstrap direct repo calls; pair it with the call-graph/import restriction)
  </read_first>
  <action>
    Define an EXPLICIT closed write-command construct whose membership is mechanically discoverable — a `writeCommands` descriptor set on the write executor (`internal/world/mutator.go`), where each descriptor names a command AND its taxonomy `kind`, PLUS the UpdateCharacterPreferences descriptor with the character_preferences_update kind (round-4 C5 fold-in registered in 05-09 Task 2). NO scene-participant descriptor exists (round-5 D-07 removed the surface). The executor's per-operation methods and the taxonomy both key off the in-Service descriptor set, so a new write command that isn't registered has no kind. Add `test/meta/world_envelope_census_test.go` that asserts, over the {world.Service write commands} set, a BIJECTION with the declared taxonomy kinds — every registered command has exactly one declared kind and every declared world-change IN-SERVICE kind has exactly one registered in-Service PRODUCER — with NO allow-list of pending/exempt commands (D-01). **The OUT-OF-SERVICE producer assertions (the character-genesis service 05-15 AND the character-reaping service 05-16 each emit a DECLARED kind) are NOT done here — they land in 05-12 (round-9 R6-5 MEDIUM), which depends_on BOTH 05-15 and 05-16, so the census does not read the wave-10 `internal/auth/character_reaping.go` that 05-16 CREATES in the same wave (Codex R6 finding: wave-parallel execution could otherwise run this census before that file exists).** The character delete/tombstone kind has its in-Service producer (DeleteCharacter) asserted here; its additional out-of-Service producer (the reaping service, a sanctioned multi-producer of that one kind) is asserted in 05-12 — so this census's in-Service bijection is over PRODUCERS-of-record and is NOT a bijection violation. Cross-check: the structural set of `world.Service` mutating methods (via go/ast, as a secondary signal) equals the in-Service registered descriptor set — so a mutating method added to Service without a descriptor is caught, and a removed one (AddSceneParticipant/RemoveSceneParticipant) is correctly absent. **Honest completeness scoping (round-3 census MEDIUM):** document IN THE TEST that the bijection proves registry consistency, and that completeness of production world writes rests on the paired fences — the reader-view compile fence (Task 1), the AST SQL fence (05-09), the internal/world/postgres composition allowlist in the import-graph meta-test (05-07 Task 4), and the removal of Create from the auth-side character-repo interfaces (05-15) — which together make an unregistered production write either uncompilable or an import/SQL-fence failure; the census alone cannot discover a direct repo call in a package that holds a concrete writer (the guest/bootstrap bypasses were exactly that). Examine commands (`ExamineLocation/Object/Character`) are reads — they are NOT in the write-command set and MUST be classified out with a documented reason. Because 05-10 + 05-11 Tasks 1-2 + 05-15 completed coverage, this test passes on introduction. Keep any literal command/kind strings the test negative-checks out of comment bodies.
  </action>
  <verify>
    <automated>task test -- ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - The census asserts a bijection between the executor's explicit registered in-Service write-command descriptor set and the declared taxonomy kinds (NOT name-prefix inference or the world.Mutator subset), with no allow-list. The OUT-OF-SERVICE producer assertions (05-15 character-genesis + 05-16 character-reaping each emit a DECLARED kind) are NOT here — they land in 05-12 (round-9 R6-5 MEDIUM), which depends_on both, so this census does NOT read the wave-10 `internal/auth/character_reaping.go` (Codex R6 wave-race fix): `grep -n 'character_reaping' test/meta/world_envelope_census_test.go` returns nothing in the 05-11 census.
    - No scene-participant command or kind is in the census (round-5 D-07 removed the surface); a go/ast cross-check asserts AddSceneParticipant/RemoveSceneParticipant are absent from world.Service.
    - A secondary go/ast cross-check asserts the structural set of world.Service mutating methods equals the in-Service registered descriptor set, so a mutating method added without a descriptor is caught.
    - The census documents its scope honestly (round-3 MEDIUM): bijection = registry consistency; completeness = bijection + reader-view fence + AST SQL fence + composition allowlist + 05-15 + 05-16 interface Create/writer removals.
    - The census passes (coverage complete); examine reads are correctly excluded as non-mutating with a documented reason.
    - Adding a hypothetical new registered command without a declared kind (or a new Service mutating method without a descriptor) fails the census.
  </acceptance_criteria>
  <done>The census structurally forbids an un-migrated command via an explicit closed command construct + a bijection assertion; the D-01 full rollout is proven complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new write command → census | An un-migrated command silently omitted from the feed is the exact ADR drift. |
| genesis/epoch → feed origin | A double-genesis or missing epoch corrupts consumer bootstrap. |
| out-of-Service writers → fence | Packages holding writer repos (guest/bootstrap) escape reader views — closed by 05-15 + the composition allowlist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-32 | Tampering | un-migrated command | high | mitigate | The census meta-test asserts the bijection over the explicit descriptor set with no allow-list; a new un-declared command fails CI. Completeness is paired (round-3 MEDIUM) with the reader-view fence, AST SQL fence (05-09), composition allowlist (05-07), and the 05-15 interface Create removal — together making an unregistered production write uncompilable or fence-caught. |
| T-05-33 | Tampering | genesis idempotency | medium | mitigate | Genesis idempotency is SCHEMA-BACKED (round-3 MEDIUM): the world_genesis_checkpoint row (PK game_id/epoch/aggregate_type/aggregate_id) commits atomically with each genesis envelope, the check runs under the serializing counter lock (race-safe), and the identity survives outbox pruning; the epoch/reset covers restores so stale positions are not replayed. |
| T-05-34 | Elevation of Privilege | character creation bypassing the feed | high | mitigate | Round 3 found guest + bootstrap-admin creation commit characters without genesis envelopes (INV-WORLD-4 false). Closed in 05-15: ALL production character creation routes through the atomic character-genesis service (insert + binding + genesis in one tx, fail-closed); this plan's fence text names that service as a sanctioned out-of-world writer. |
| T-05-59 | Tampering | guest character DELETION bypassing the feed | high | mitigate | Round 5 (D-06) found guest reaping (guest_reaper.go) + failed-guest cleanup (guest_service.go) delete characters via an FK cascade with NO tombstone (genesis-without-tombstone). Closed in 05-16: guest character deletion routes through the atomic character-reaping service (per-character tombstone envelope in one tx, ordered before the player delete); this plan's fence text + census name it the SECOND sanctioned out-of-world writer emitting the character delete/tombstone kind. |
| T-05-44 | Tampering | feed-epoch state absent / incomplete reset | medium | mitigate | The feed-epoch advance has persistent schema (world_feed_counter.epoch in migration 000050) + a postgres read/advance store (genesis_store.go). Round-6 Codex MEDIUM: AdvanceEpoch is now ONE locked operation that also RESETS next_position to the origin, VERIFIES/QUARANTINES unpublished old-epoch outbox rows (so the relay never publishes a stale-epoch position — no ambiguous ordering), records reset metadata, and coordinates the relay; a relay-active-with-old-rows integration test binds it. A DB restore/backfill thus restarts the feed cleanly, not with an inherited counter. |
| T-05-50 | Tampering | outbox→postgres import cycle from genesis wiring | medium | mitigate | Round-4 A3: the round-3 shape wired outbox/genesis.go → postgres/genesis_store.go, contradicting 05-07's outbox→postgres prohibition. The consumer-owned GenesisStore interface (declared in outbox, impl injected from postgres) keeps the edge absent; the 05-07 Task-4 eight-edge import guard fails CI if a future edit reintroduces it. |
| T-05-51 | Denial of Service | genesis never runs (no operator entry) | medium | mitigate | Round-4 A3: genesis/epoch-reset existed only as library code with no caller. The `holomush world genesis` / `world epoch-reset` admin CLI is the real operator entry at cutover / DB-restore; a CLI test proves it constructs + invokes the orchestration. It is operator-authorized, off crypto/abac surfaces. |
</threat_model>

<verification>
- `task build` succeeds (no import cycle) and `task test -- ./internal/world/ ./internal/property/ ./internal/world/outbox/ ./cmd/holomush/ ./test/meta/` passes (run via `local-check`).
- `task lint` green (writer-boundary fence holds; entity_properties in the fence).
- The census meta-test passes and would fail on a hypothetical un-registered/un-declared command; its doc comment states the completeness pairing (fences + allowlist + 05-15).
- The genesis snapshot double-run/concurrent-run tests prove checkpoint-keyed idempotency with no position gap.
- `grep -rn 'internal/world/postgres' internal/world/outbox/genesis.go` returns no import, and the 05-07 Task-4 eight-edge import guard is green (outbox→postgres absent — round-4 A3); `holomush world genesis --help` / `world epoch-reset --help` show the operator subcommands.
</verification>

<success_criteria>
- Every world.Service write command emits exactly one declared-kind envelope; the census passes with no allow-list and honest completeness scoping.
- Genesis snapshot emission (checkpoint-idempotent, race-safe, prune-surviving) + feed epoch/reset exist and are tested.
- The D-01 Service-side mechanical rollout is complete (character creation completes in 05-15).
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-11-SUMMARY.md` when done.
</output>
