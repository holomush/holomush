---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/world/postgres/transactor.go
  - internal/world/postgres/transactor_test.go
  - internal/world/postgres/helpers.go
  - internal/world/postgres/exit_repo.go
  - internal/world/postgres/object_repo.go
  - internal/world/postgres/scene_repo.go
  - internal/world/postgres/property_repo.go
  - internal/world/wmodel/mutation_delta.go
  - internal/world/wmodel/mutation_delta_test.go
  - internal/world/repository.go
  - internal/world/postgres/location_repo.go
  - internal/world/postgres/character_repo.go
  - internal/world/service.go
  - internal/bootstrap/setup/adapters.go
  - internal/testsupport/integrationtest/plugins.go
  - internal/world/worldtest/mock_LocationRepository.go
  - internal/world/worldtest/mock_ExitRepository.go
  - internal/world/worldtest/mock_ObjectRepository.go
  - internal/world/worldtest/mock_CharacterRepository.go
  - internal/world/worldtest/mock_SceneRepository.go
autonomous: true
requirements: [MODEL-03, MODEL-04]
must_haves:
  truths:
    - "Transactor.InTransaction is re-entrant: a nested call inside an ambient transaction reuses that transaction (no second pool.Begin), so a repo method and its caller share one tx and one connection (addresses Codex Agreed-Concern A + Antigravity zero-row-classifier connection-reuse)."
    - "No world repository write method opens its own pool.Begin: exit Create/Delete and object Move enroll in the ambient transaction via a re-entrant seam, and scene AddParticipant/RemoveParticipant route through execerFromCtx — so every world write can commit atomically with the outbox row (addresses Codex: repository-owned transactions escape/nest)."
    - "A MutationDelta contract exists (in the cycle-neutral leaf package internal/world/wmodel) that guarded write methods return, carrying the primary and every affected aggregate (including repository-internal cascade IDs) with before/after versions — the source the outbox manifest is built from, not command inputs (addresses Codex delta-parity: cascade IDs are repository-internal). The types live in wmodel — imported by world, postgres, and outbox but importing none of them — so the round-1 world↔outbox↔postgres import cycles cannot form (round-2 Codex import-cycle finding)."
    - "Repository Delete takes an expectedVersion; ObjectRepository.Move and CharacterRepository.UpdateLocation ALSO take an expectedVersion (round-2 Antigravity: they took no version-bearing struct so the move/containment CAS had no parameter); read-only reader interfaces exist for every repo, so the compile-time write fence (05-06/05-11) can hold and the delete/move guard (05-02/05-03) has its parameter."
    - "PropertyRepository.Create and Update route through execerFromCtx (round-2 property-leak finding: property_repo.go:60/136 still used r.pool.Exec directly and the round-1 sweep missed them), so property writes funnelled through a parent mutation commit atomically instead of on a separate connection."
    - "The write-return redesign compiles as an atomic wave-1 change because every compile-time caller is enumerated (round-2 Codex blast-radius finding): the auth-side auth.CharacterRepository stays Create(...) error and CharRepoAdapter.Create discards the delta; the world.Service ServiceConfig construction sites (setup/subsystem.go, testsupport/integrationtest/plugins.go) still compile."
  prohibitions:
    - "No world repository write method may retain its own pool.Begin/pool.Exec transaction — exit Create/Delete and object Move MUST route through the re-entrant seam; scene AND property writes MUST route through execerFromCtx."
    - "This plan MUST NOT change any observable behavior — it is a pure interface + transaction-enrollment refactor. The version predicate, delta population, and outbox enrollment are added by later plans; here the redesigned methods return a primary-only delta and ignore expectedVersion."
    - "The auth.CharacterRepository interface (character_service.go:17) MUST NOT change signature — the delta-return redesign is absorbed by the concrete CharRepoAdapter.Create discarding the delta, so unrelated auth-facing code does not change (round-2 Codex blast-radius)."
  artifacts:
    - path: internal/world/wmodel/mutation_delta.go
      provides: "cycle-neutral leaf package: MutationDelta + AffectedAggregate types (primary + affected aggregates with before/after versions + tombstone flag); imports none of world/postgres/outbox"
      contains: "MutationDelta|AffectedAggregate"
    - path: internal/world/postgres/transactor.go
      provides: "re-entrant InTransaction that reuses an ambient tx"
      contains: "txFromContext"
  key_links:
    - from: internal/world/postgres/exit_repo.go
      to: internal/world/postgres/transactor.go
      via: "exit Create/Delete run inside the re-entrant seam instead of pool.Begin, so they enroll in an ambient outbox transaction"
      pattern: "execerFromCtx|InTransaction|withTx"
---

<objective>
The transaction & repository foundation the reviews identified as an unaddressed prerequisite (Codex Agreed-Concern A). Three source-grounded blockers make the drawn slice-1/slice-2 mechanism unimplementable as written, and this plan fixes them once, behavior-preservingly, before the version-guard repo rewrites (05-02/05-03) and the outbox (05-05+):

1. `Transactor.InTransaction` always calls `pool.Begin` (`transactor.go:28`) — it is NOT re-entrant, so a repo method that opens a tx while its caller already holds one nests/escapes; and the zero-row classifier's "locked follow-up read in the same tx" cannot reuse the caller's connection.
2. `ExitRepository.Create`/`Delete` (`exit_repo.go:60,178`) and `ObjectRepository.Move` (`object_repo.go:172`) open their OWN `pool.Begin` transactions, and `SceneRepository.AddParticipant`/`RemoveParticipant` (`scene_repo.go:40,57`) use `r.pool.Exec` directly — all four escape any ambient mutation transaction, so the outbox row would commit separately.
3. Delta-parity is unbuildable from command inputs because cascade IDs (the reverse-exit ID at `exit_repo.go:80`, cascaded children) are generated INSIDE the repository; service methods only see `error`. There is no `MutationDelta` contract for repos to return.

The **round-2 review** adds four grounded requirements this plan now also lands:
4. The `MutationDelta`/`AffectedAggregate`/`EnvelopeIntent`/`Envelope` value types must live in a **cycle-neutral leaf package** (`internal/world/wmodel`) so the round-1 `world → outbox → world` and `outbox → postgres → outbox` import cycles cannot form. This plan creates `wmodel` and puts `MutationDelta`/`AffectedAggregate` there; 05-05 adds `EnvelopeIntent`/`Envelope`.
5. `ObjectRepository.Move` (`repository.go:118`) and `CharacterRepository.UpdateLocation` (`repository.go:161`) take no version-bearing struct, so the round-1 redesign gave the move/containment path no CAS parameter — add `expectedVersion int` to both.
6. `PropertyRepository.Create`/`Update` (`property_repo.go:60,136`) still write via `r.pool.Exec` directly — the round-1 `execerFromCtx` sweep missed them; convert both so property writes join the ambient mutation tx.
7. The write-return signature change has a wider compile-time blast radius than the round-1 plan listed — the auth-side `auth.CharacterRepository` (`character_service.go:17`) and the `CharRepoAdapter` (`adapters.go:38`) plus the `ServiceConfig` construction sites must be enumerated so wave 1 compiles atomically.

Purpose: The atomicity guarantee (MODEL-04) and the version guard (MODEL-03) both require every world write to enroll in one caller-owned transaction and to report the rows it actually touched. That is a repository-layer contract change, and it must land before the guard/outbox logic builds on it — cycle-free, and with every caller enumerated.
Output: re-entrant `InTransaction` + a re-entrant `withTx` seam; exit/object/scene/**property** writes refactored onto the ambient executor; the `wmodel` leaf package with the `MutationDelta` contract; redesigned repo write interfaces (`Delete(ctx, id, expectedVersion)`, **`Move`/`UpdateLocation` gaining `expectedVersion`**, writes returning `(*wmodel.MutationDelta, error)`) with read-only reader views; regenerated mocks; every caller (incl. the delta-discarding `CharRepoAdapter` and the `ServiceConfig` sites) compiling. No behavior change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make InTransaction re-entrant + a re-entrant withTx seam</name>
  <files>internal/world/postgres/transactor.go, internal/world/postgres/transactor_test.go, internal/world/postgres/helpers.go</files>
  <read_first>
    - internal/world/postgres/transactor.go (InTransaction :27-42 — always pool.Begin today; the non-reentrant site the review cites at transactor.go:27)
    - internal/world/postgres/helpers.go (txFromContext :33, execerFromCtx :42, txKey{} :29 — the ambient-tx machinery to reuse)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (Codex Agreed-Concern A; Antigravity zero-row-classifier connection-reuse: the classifier SELECT ... FOR UPDATE must run on the execer-provided connection)
  </read_first>
  <behavior>
    - RED: calling InTransaction when a tx is already in context executes the closure within the EXISTING transaction — no second pool.Begin, no second connection. A test proves a nested InTransaction write is rolled back when the OUTER transaction rolls back (shared tx), and committed when the outer commits.
    - RED: a nested InTransaction returning an error surfaces to the outer, which rolls back (the outermost owns commit/rollback).
    - Top-level InTransaction (no ambient tx) begins and commits exactly as today.
  </behavior>
  <action>
    Make `Transactor.InTransaction` re-entrant: if `txFromContext(ctx) != nil`, execute `fn(ctx)` within the existing transaction and return its error WITHOUT beginning a new tx or committing/rolling back (the outermost call owns commit/rollback). Only when no ambient tx is present does it `pool.Begin`, stash the tx via `txKey{}`, and commit-on-nil / rollback-on-error as today. Add an unexported package-level `withTx(ctx, pool, fn)` helper in `helpers.go` with the same re-entrant semantics for the repo methods that today open their own `pool.Begin` (Task 2) — reuse the ambient tx if present, else begin-and-commit a local one; inside, callers use `execerFromCtx(ctx, pool)`. This is the exported-in-spirit transaction-scoped executor Codex asks for, kept unexported because the outbox persistence (05-05) lives in this same `internal/world/postgres` package and can reach it directly (resolving the sibling-package visibility blocker without exporting `execerFromCtx`). Write the RED tests first against a testcontainer pool: nested-share, nested-error-propagation, top-level-commit.
  </action>
  <verify>
    <automated>task test:int -- -run 'Transactor|InTransaction' ./internal/world/postgres/</automated>
  </verify>
  <acceptance_criteria>
    - A nested InTransaction reuses the ambient tx (no second Begin) — proven by an outer-rollback-rolls-back-inner test.
    - Top-level InTransaction still begins + commits/rolls back as before.
    - `withTx` exists and is re-entrant; the classifier + repo methods can share the caller's connection.
    - `task test:int -- -run 'Transactor|InTransaction' ./internal/world/postgres/` passes.
  </acceptance_criteria>
  <done>The transaction seam is re-entrant; a repo method and its caller share one tx and one connection.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor self-transacting repo methods (exit/object/scene/property) onto the ambient executor</name>
  <files>internal/world/postgres/exit_repo.go, internal/world/postgres/object_repo.go, internal/world/postgres/scene_repo.go, internal/world/postgres/property_repo.go</files>
  <read_first>
    - internal/world/postgres/exit_repo.go (Create :50-94 opens pool.Begin :60; Delete :176-258 opens pool.Begin :178; the reverse-exit idgen.New() :80 — a repository-internal cascade ID delta-parity needs)
    - internal/world/postgres/object_repo.go (Move :165 opens pool.Begin :172, with FOR UPDATE locks on object + container)
    - internal/world/postgres/scene_repo.go (AddParticipant :40 and RemoveParticipant :57 use r.pool.Exec directly)
    - internal/world/postgres/property_repo.go (Create :60 and Update :136 use r.pool.Exec directly — the round-2 property-leak sites; Delete :154 / DeleteByParent already use execerFromCtx — mirror them)
    - internal/world/postgres/helpers.go (withTx from Task 1; execerFromCtx :42)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (round-2 Agreed-Concern: PropertyRepository.Create/Update transaction leak; property_repo.go:60/136)
  </read_first>
  <action>
    Replace the internal `r.pool.Begin(...)` in `ExitRepository.Create`, `ExitRepository.Delete`, and `ObjectRepository.Move` with the re-entrant `withTx(ctx, r.pool, func(txCtx) error {...})` seam, and route every statement inside through `execerFromCtx(txCtx, r.pool)` (or `tx` obtained from context) rather than a fresh connection. When called standalone these methods still get their own transaction (via `withTx`); when called inside an ambient mutation tx (05-06+) they enroll in it and commit atomically with the outbox row. Preserve the existing multi-statement semantics exactly: exit Create still atomically inserts the reverse exit when bidirectional; exit Delete still locks with `FOR UPDATE`, cascades the return exit, and returns `*world.BidirectionalCleanupResult` on non-severe issues; object Move still locks object + container `FOR UPDATE` and enforces nesting/circular rules. Switch `SceneRepository.AddParticipant`/`RemoveParticipant` from `r.pool.Exec` to `execerFromCtx(ctx, r.pool).Exec` so scene-participant writes enroll in the ambient tx (their envelope emission lands in 05-11; the writer-boundary fence in 05-09 also covers `scene_participants`). **Also switch `PropertyRepository.Create` (:60) and `PropertyRepository.Update` (:136) from `r.pool.Exec` to `execerFromCtx(ctx, r.pool).Exec` (round-2 property-leak finding)** — property writes funnelled through a parent operation (character delete, entity update) then commit atomically with the parent mutation instead of on a separate pooled connection (which risks lock contention / deadlock); the `entity_properties` writes also land inside the writer-boundary fence (05-09 now covers `entity_properties`). Behavior-preserving: run the existing repo integration tests unchanged.
  </action>
  <verify>
    <automated>task test:int -- -run 'Exit|Object|Scene|Property' ./internal/world/postgres/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'r.pool.Begin' internal/world/postgres/exit_repo.go internal/world/postgres/object_repo.go` returns no hits (writes route through withTx).
    - `grep -n 'r.pool.Exec' internal/world/postgres/scene_repo.go internal/world/postgres/property_repo.go` returns no write-path hits (Create/Update/Delete all via execerFromCtx).
    - A property write inside an ambient InTransaction commits/rolls back with the parent (an integration test rolls back the parent tx and asserts the property write did not persist).
    - Existing exit/object/scene/property integration tests pass unchanged (behavior preserved: bidirectional create/delete, move nesting rules, participant upsert/delete, property visibility defaults).
  </acceptance_criteria>
  <done>Every world write — exit, object, scene, AND property — can enroll in an ambient transaction; no repo method owns an escaping transaction or a pooled write.</done>
</task>

<task type="auto">
  <name>Task 3: wmodel leaf package + MutationDelta contract + redesigned write interfaces + reader views + mock regen + blast-radius callers</name>
  <files>internal/world/wmodel/mutation_delta.go, internal/world/wmodel/mutation_delta_test.go, internal/world/repository.go, internal/world/postgres/location_repo.go, internal/world/postgres/exit_repo.go, internal/world/postgres/character_repo.go, internal/world/postgres/object_repo.go, internal/world/postgres/scene_repo.go, internal/world/service.go, internal/bootstrap/setup/adapters.go, internal/testsupport/integrationtest/plugins.go, internal/world/worldtest/mock_LocationRepository.go, internal/world/worldtest/mock_ExitRepository.go, internal/world/worldtest/mock_ObjectRepository.go, internal/world/worldtest/mock_CharacterRepository.go, internal/world/worldtest/mock_SceneRepository.go</files>
  <read_first>
    - internal/world/repository.go (LocationRepository/ExitRepository/ObjectRepository/CharacterRepository/SceneRepository — Delete :33/75/105/154 take only an id; Create/Update return only error; ObjectRepository.Move :118 takes (objectID, to Containment) with NO version; CharacterRepository.UpdateLocation :161 takes (characterID, locationID) with NO version — the two round-2 Antigravity CAS-parameter gaps)
    - internal/world/service.go (ServiceConfig/Service :30-56 and the write call sites that consume repo returns — every caller must compile against the new signatures)
    - internal/auth/character_service.go (:17-34 — auth.CharacterRepository is a SEPARATE narrow interface with its own Create(ctx, *world.Character) error; it does NOT embed world.CharacterRepository and MUST NOT change)
    - internal/bootstrap/setup/adapters.go (:38 CharRepoAdapter.Create calls the concrete a.charRepo.Create and returns error — the delta-discard site)
    - internal/testsupport/integrationtest/plugins.go (:268-286 constructs world.NewService(ServiceConfig{...}) with the concrete repos — must still compile against the reshaped ServiceConfig)
    - .mockery.yaml (mock generation config; `mockery` regenerates the worldtest mocks)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (round-2 Codex import-cycle + blast-radius findings; round-2 Antigravity Move/UpdateLocation expectedVersion gap)
    - .claude/rules/references/plan-review-learnings.md (verify every method signature against the repo interface; regenerate mockery outputs, do not hand-edit)
  </read_first>
  <action>
    **Create the cycle-neutral leaf package `internal/world/wmodel`** (round-2 import-cycle fix — imports ONLY stdlib + `internal/core`/`ulid`, and MUST import none of `internal/world`, `internal/world/postgres`, `internal/world/outbox`). Define in `internal/world/wmodel/mutation_delta.go`: `AffectedAggregate{ Type (location/exit/character/object/scene), ID, BeforeVersion int, AfterVersion int, Tombstone bool }`; `MutationDelta{ Primary AffectedAggregate; Affected []AffectedAggregate }`. This is the contract the outbox manifest is built from (05-10/05-11), so it must carry every row a command touched INCLUDING repository-internal cascade IDs (reverse exit, cascaded children). (05-05 adds `EnvelopeIntent` + `Envelope` to this same `wmodel` package.) Redesign the five repository interfaces in `repository.go`: `Delete(ctx, id, expectedVersion int) (*wmodel.MutationDelta, error)`; `Create`/`Update` return `(*wmodel.MutationDelta, error)`; **`ObjectRepository.Move(ctx, objectID, to Containment, expectedVersion int) (*wmodel.MutationDelta, error)`** and **`CharacterRepository.UpdateLocation(ctx, characterID, locationID *ulid.ULID, expectedVersion int) (*wmodel.MutationDelta, error)`** — adding the `expectedVersion` the move/containment CAS needs (round-2 Antigravity); scene `AddParticipant`/`RemoveParticipant` return `(*wmodel.MutationDelta, error)`. Add read-only reader interface views (`LocationReader`, `ExitReader`, `ObjectReader`, `CharacterReader`, `SceneReader`) exposing only the Get/List/Find methods — the compile-time write fence (05-06 opens, 05-11 completes) gives `world.Service` these reader types plus a write executor, so an envelope-less direct write does not type-check. Update all four postgres repo impls (`location/exit/character/object_repo.go`) + scene to the new signatures, returning a primary-only `wmodel.MutationDelta` for now (Primary populated with the aggregate id + a placeholder before/after version; `expectedVersion` accepted and IGNORED — the version predicate and real before/after population land in 05-02/05-03; the read version for Move/UpdateLocation is threaded from `service.go` in 05-03/05-11; cascade Affected population lands in 05-10/05-11). **Blast-radius callers (round-2 Codex — enumerate ALL so wave 1 compiles atomically):** (a) update every caller in `service.go` to handle the two return values (discard the delta for now — behavior-preserving); (b) the auth-side `auth.CharacterRepository` interface (`character_service.go:17`) is a SEPARATE narrow interface and MUST NOT change — instead update the concrete `CharRepoAdapter.Create` (`adapters.go:38`) to discard the delta (`_, err := a.charRepo.Create(ctx, char)`), keeping its own `Create(...) error` signature; (c) the `ServiceConfig` construction sites — `internal/world/setup/subsystem.go` and `internal/testsupport/integrationtest/plugins.go` — still pass the concrete repos (which satisfy both the reader and writer interfaces) and must compile against the reshaped `ServiceConfig`; run a repo-wide grep (`.Create(`/`.Update(`/`.Delete(`/`.Move(`/`.UpdateLocation(`/`.AddParticipant(`/`.RemoveParticipant(` on these repo types) to confirm no other production caller is missed. Regenerate the worldtest mocks with `mockery` (do NOT hand-edit generated files; per plan-review-learnings the mock signatures must match the interface exactly). Confirm `task build` (import cycles are compile errors) and `task test -- ./internal/world/... ./internal/bootstrap/... ./internal/testsupport/...` compile and pass with no behavior change.
  </action>
  <verify>
    <automated>task build && task test -- ./internal/world/... && task test:int -- ./internal/world/postgres/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/world/wmodel` exists holding `MutationDelta`/`AffectedAggregate` (before/after versions + tombstone flag) and imports none of `internal/world`, `internal/world/postgres`, `internal/world/outbox` (`go list -deps` / import scan confirms).
    - Every repo `Delete` takes `expectedVersion`; `ObjectRepository.Move` and `CharacterRepository.UpdateLocation` take `expectedVersion`; every `Create`/`Update`/`Move`/`UpdateLocation`/scene write returns `(*wmodel.MutationDelta, error)`; read-only reader interfaces exist.
    - `auth.CharacterRepository` is unchanged (`Create(...) error`); `CharRepoAdapter.Create` discards the delta and still compiles; the `ServiceConfig` sites (subsystem.go, integrationtest/plugins.go) compile.
    - The worldtest mocks are regenerated (mockery) and match the new interfaces; no hand edits.
    - `task build` succeeds (no import cycle) and `task test -- ./internal/world/... ./internal/bootstrap/...` + `task test:int -- ./internal/world/postgres/` pass with no behavior change.
  </acceptance_criteria>
  <done>The `wmodel` leaf package + redesigned repository write API (Delete/Move/UpdateLocation+expectedVersion, writes return `wmodel.MutationDelta`, reader views) exist with regenerated mocks and every blast-radius caller compiling; the delta contract, cycle-neutral types, and compile-time fence substrate exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller tx → repo write | A repo write that opens its own transaction escapes the caller's atomicity guarantee; the re-entrant seam closes this. |
| repo cascade → delta manifest | Cascade IDs generated inside the repo must be reported to the caller or the delta-parity manifest cannot match the committed rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-38 | Tampering | escaping repo transactions | high | mitigate | exit Create/Delete + object Move + scene writes route through the re-entrant seam / execerFromCtx; a `grep` gate + behavior tests forbid a surviving `r.pool.Begin`/`r.pool.Exec` write. A write that split the tx would leave an orphan outbox row (proven impossible in 05-05's atomicity test). |
| T-05-39 | Repudiation | non-reentrant tx double-Begin | high | mitigate | InTransaction reuses the ambient tx; a nested Begin would deadlock the locked follow-up read against a row the outer tx holds — the re-entrancy test proves the shared-connection path. |
| T-05-40 | Tampering | delta manifest cannot match rows | medium | mitigate | The `MutationDelta` contract makes repository-internal cascade IDs (reverse exit, children) reportable to the mutate() seam; the manifest is built from actual touched rows, not command inputs (bound by INV-WORLD-DELTA-PARITY in 05-12). |
</threat_model>

<verification>
- `task test:int -- -run 'Transactor|InTransaction|Exit|Object|Scene|Property' ./internal/world/postgres/` passes (run via `local-check`).
- `grep -n 'r.pool.Begin' internal/world/postgres/exit_repo.go internal/world/postgres/object_repo.go` returns no hits; `grep -n 'r.pool.Exec' internal/world/postgres/scene_repo.go internal/world/postgres/property_repo.go` returns no write-path hits.
- `task build` succeeds (import cycles are compile errors — proves `internal/world/wmodel` is cycle-neutral) and `task test -- ./internal/world/... ./internal/bootstrap/...` compiles + passes (mocks regenerated, delta-discarding adapter + ServiceConfig sites updated, no behavior change).
</verification>

<success_criteria>
- InTransaction is re-entrant; exit/object/scene/property writes enroll in the ambient tx.
- The `wmodel` leaf package + MutationDelta contract + redesigned write interfaces (Delete/Move/UpdateLocation+expectedVersion) + reader views exist; mocks regenerated; every blast-radius caller compiles (auth interface unchanged via delta-discarding adapter).
- No observable behavior change — the guard, delta population, and outbox enrollment build on this in later plans.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-14-SUMMARY.md` when done.
</output>
