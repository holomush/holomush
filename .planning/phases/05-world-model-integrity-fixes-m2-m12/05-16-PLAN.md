---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 16
type: tdd
wave: 10
depends_on: [05-15, 05-09]
files_modified:
  - internal/auth/character_reaping.go
  - internal/auth/character_reaping_test.go
  - internal/auth/guest_reaper.go
  - internal/auth/guest_reaper_test.go
  - internal/auth/guest_service.go
  - internal/auth/guest_service_test.go
  - cmd/holomush/sub_grpc.go
  - test/integration/auth/guest_reaper_tombstone_test.go
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "Guest character DELETION is symmetric with 05-15's creation (round-5 D-06): both guest reaping (guest_reaper.go → GuestCleaner.DeleteGuestPlayer) and failed-guest cleanup (guest_service.go cleanupGuestPlayer → s.players.Delete) route through ONE atomic character-reaping application service that, for each character owned by the guest player, deletes it through the tombstone-emitting world CharacterWriter.Delete AND emits exactly one character delete/tombstone envelope in the SAME transaction, THEN deletes the player. No reaped guest character enters the feed via genesis (05-15) at creation but escapes deletion — genesis-without-tombstone feed history is impossible."
    - "The reaping service is the deletion-side counterpart to 05-15's CharacterGenesisService and the SECOND sanctioned out-of-world writer under INV-WORLD-4 (05-12): its concrete CharacterWriter (the guarded worldpostgres.CharacterRepository, whose Delete returns a tombstone wmodel.MutationDelta and stays in the allowlisted internal/world/postgres) is constructed only at the composition root (cmd/holomush/sub_grpc.go), and its envelope goes through the world.OutboxWriter.WriteIntent contract (05-06) — no raw DELETE FROM characters SQL and no envelope-less character deletion exists in the guest lifecycle."
    - "The atomic unit is exactly {each character delete + its tombstone envelope} in ONE re-entrant world transaction (05-14 transactor + world txKey); a forced rollback removes both the character deletion and the tombstone envelope. The player row DELETE (internal/auth/postgres/player_repo.go DeleteGuestPlayer — its own r.pool) is NOT claimed atomic with the tombstone tx (mirrors 05-15's round-4 B4 discipline): ORDERING deletes the characters through the world tombstone path FIRST (committed), THEN deletes the player, so the characters.player_id ON DELETE CASCADE (000002_player_is_guest.up.sql:9) becomes a no-op (no character rows remain to cascade-delete without a tombstone). Orphan-player (characters tombstoned + deleted but the player DELETE failed) is an ACCEPTED, DOCUMENTED compensation gap reconciled by the next reap cycle — it is OUTSIDE INV-WORLD-4."
    - "The reaper handles a WORLD_CONCURRENT_EDIT on a character delete as retriable operational degradation (a concurrent edit between the version read and the guarded delete) — it is a background best-effort reaper; the character is retried on the next reap cycle, never silently skipped without a tombstone."
  artifacts:
    - path: internal/auth/character_reaping.go
      provides: "the atomic character-reaping application service: DeleteGuestPlayer(ctx, playerID) — lists the player's characters, in one re-entrant world tx deletes each via CharacterWriter.Delete (tombstone delta) + OutboxWriter.WriteIntent (character delete/tombstone kind), then deletes the player; fail-closed constructor; the SECOND sanctioned out-of-world writer (deletion side) under INV-WORLD-4"
      contains: "OutboxWriter|WriteIntent|tombstone"
    - path: internal/auth/guest_reaper.go
      provides: "guest reaper rerouted: its GuestCleaner is the character-reaping service (tombstone-emitting) instead of a raw DeleteGuestPlayer that cascades characters silently"
    - path: test/integration/auth/guest_reaper_tombstone_test.go
      provides: "regression test proving guest expiration emits one character tombstone per reaped character (no genesis-without-tombstone feed history) and that the player-delete FK cascade deletes no un-tombstoned character"
      contains: "tombstone|genesis"
  key_links:
    - from: internal/auth/character_reaping.go
      to: internal/world/mutator.go
      via: "the reaping service persists each tombstone through the world.OutboxWriter interface (WriteIntent — the writer allocates epoch/position, finalizes from the returned tombstone delta, persists, returns) — the same contract 05-15's genesis service uses"
      pattern: "OutboxWriter|WriteIntent"
    - from: internal/auth/guest_reaper.go
      to: internal/auth/character_reaping.go
      via: "the reaper's GuestCleaner dependency is satisfied by the reaping service (DeleteGuestPlayer) — the tombstone-emitting deletion, not player_repo's raw cascade"
      pattern: "GuestCleaner|DeleteGuestPlayer|reaping"
  prohibitions:
    - "No guest-lifecycle code path may delete a character without a tombstone envelope — the reaper and guest_service cleanup MUST route through the reaping service; a raw DeleteGuestPlayer whose FK cascade removes an un-tombstoned character is the exact D-06 hole being closed."
    - "The reaping service MUST NOT begin a second transaction when an ambient tx is in context — it enrolls via the re-entrant transactor (05-14); character delete + tombstone envelope commit together or not at all."
    - "The player-row DELETE MUST NOT be claimed atomic with the tombstone tx (player_repo uses its own r.pool, outside world/postgres's private txKey) — characters are tombstoned + deleted FIRST (committed), THEN the player is deleted; orphan-player is documented compensation, not an atomicity claim."
    - "The reaping service MUST NOT introduce a new envelope kind — a reaped guest character is a character deletion; it reuses the SAME character delete/tombstone taxonomy kind (05-09) that world.Service.DeleteCharacter emits (05-11), so consumers treat all character tombstones uniformly."
---

<objective>
Close the round-5 D-06 hole: the guest reaper and failed-guest cleanup delete characters through an FK cascade with NO tombstone, so a reaped guest's character enters the feed via genesis (05-15) at creation but its deletion is invisible — INV-WORLD-4 / delta-parity / feed-completeness are FALSE for a live production path. This plan is the deletion-side symmetric counterpart to 05-15's `CharacterGenesisService`: one atomic character-reaping service routes ALL guest character deletion through a tombstone-emitting world deletion path, so guest expiration cannot produce genesis-without-tombstone feed history.

**Why this plan exists (round-5 Codex, locked as D-06):** `guest_reaper.go` (`reap()` → `GuestCleaner.DeleteGuestPlayer`) and `guest_service.go` (`cleanupGuestPlayer` → `s.players.Delete`) both delete the guest PLAYER, relying on the `characters.player_id ON DELETE CASCADE` FK (`internal/store/migrations/000002_player_is_guest.up.sql:9`) to remove the guest's characters — with no character tombstone. The AST SQL fence (05-09) cannot catch this: the Go source contains `DELETE FROM players` (an auth-table write, not a world table), and the character deletion happens invisibly at the DB FK layer. Creation is fully covered (05-15 routes registered + guest + bootstrap-admin through the genesis service); deletion was not. This plan fixes it by construction, not convention.

**Design (mirrors 05-15, round-4 B4 discipline for the cross-pool boundary):** a `CharacterReapingService` owns, in ONE re-entrant world transaction, the tombstone-emitting deletion of each of a guest player's characters (guarded world `CharacterWriter.Delete` → tombstone `wmodel.MutationDelta`, then `OutboxWriter.WriteIntent` with the character delete/tombstone kind). The player-row DELETE stays on `player_repo`'s own pool and is ORDERED AFTER the character-tombstone tx commits, so the FK cascade has no un-tombstoned character left to delete. Orphan-player (player DELETE fails after character tombstones commit) is an accepted, documented compensation gap outside INV-WORLD-4 — symmetric to 05-15's orphan-guest-player gap.

Purpose: A character that appeared on the feed at creation (genesis) but vanished from the DB without a tombstone is exactly the feed-completeness drift Phase 5 exists to close. This plan makes tombstone-less guest character deletion impossible by construction.
Output: `auth.CharacterReapingService` (per-character tombstone-emitting deletion + player delete, one re-entrant tx per character, fail-closed), the guest reaper + failed-guest cleanup rerouted through it, composition-root wiring, and a regression test proving no genesis-without-tombstone feed history.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

## Artifacts this phase produces

This plan is registered in the canonical artifact registry in `05-01-PLAN.md` ("Artifacts this phase produces"). NEW symbols this plan creates (excluded from source-grounding drift checks):
- `internal/auth/character_reaping.go`: `CharacterReapingService` + its constructor + its narrow service-declared deps (a `CharacterDeleter`/`CharacterWriter` interface with `Delete(ctx, id, expectedVersion) (*wmodel.MutationDelta, error)`, a character reader that lists a player's characters, the re-entrant transactor, the world player deleter, and `world.OutboxWriter`).
- `test/integration/auth/guest_reaper_tombstone_test.go`: the D-06 regression suite.
- Rerouted (not new symbols): `guest_reaper.go`'s injected `GuestCleaner` is now the reaping service; `guest_service.go` `cleanupGuestPlayer` routes through the reaping service; `cmd/holomush/sub_grpc.go` constructs the reaping service and injects it into both.

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-CONTEXT.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/event-conventions.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The atomic CharacterReapingService (per-character tombstone-emitting deletion, one re-entrant tx)</name>
  <files>internal/auth/character_reaping.go, internal/auth/character_reaping_test.go</files>
  <read_first>
    - internal/auth/character_genesis.go (05-15 — the creation-side sibling to mirror EXACTLY: service-declared narrow deps, fail-closed constructor, re-entrant tx enrollment, OutboxWriter.WriteIntent usage; this plan is its deletion mirror)
    - internal/auth/guest_reaper.go (:18-21 GuestCleaner interface — DeleteGuestPlayer(ctx, playerID) error; the interface this service satisfies as the reaper's cleaner)
    - internal/auth/guest_service.go (:33-48 the narrow service-declared deps idiom — GuestTransactor/GuestBindingCreator; :61 the players PlayerRepository dep)
    - internal/world/mutator.go (05-06 — the world.OutboxWriter interface: WriteIntent(ctx, intent, delta) (*wmodel.Envelope, error); the writer allocates epoch/position + finalizes + persists + returns)
    - internal/world/repository.go + internal/world/postgres/character_repo.go (05-14/05-03 — CharacterRepository.Delete(ctx, id, expectedVersion) (*wmodel.MutationDelta, error): the guarded, tombstone-delta-returning world delete; and a character reader that lists a player's characters with populated Version)
    - internal/world/wmodel/envelope.go, internal/world/wmodel/mutation_delta.go (05-05/05-14 — EnvelopeIntent fields incl. core.NewULID identity; the tombstone MutationDelta the character Delete returns)
    - internal/world/outbox/taxonomy.go (05-09 — the character delete/tombstone kind DeleteCharacter emits; the reaping service REUSES this kind, it does NOT declare a new one)
    - internal/world/postgres/transactor.go + helpers.go (05-14 — the re-entrant InTransaction / txKey the reaping service shares with the character writer for ambient enrollment)
    - internal/auth/postgres/player_repo.go (:331-334 DeleteGuestPlayer — DELETE FROM players ... AND is_guest = true on its own r.pool; the player delete ordered AFTER the tombstone tx)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-CONTEXT.md (D-06 — the locked decision: route guest reaping AND failed-guest cleanup through a character-aware tombstone-emitting deletion in the same tx as the delete, then delete the player)
  </read_first>
  <behavior>
    - RED: DeleteGuestPlayer(ctx, playerID) lists the player's characters; for each, in one re-entrant world transaction, it (1) calls CharacterWriter.Delete(txCtx, char.ID, char.Version) obtaining a tombstone wmodel.MutationDelta, (2) builds a character delete/tombstone wmodel.EnvelopeIntent (the character delete/tombstone taxonomy kind from 05-09, core.NewULID identity, system actor), (3) calls OutboxWriter.WriteIntent(txCtx, intent, delta) — the character deletion and its tombstone envelope commit together.
    - RED: with an ambient tx already in ctx, the service ENROLLS (re-entrant — no second Begin): rolling back the OUTER tx removes both the character deletion and the tombstone envelope.
    - RED: a forced failure (delete or envelope write) rolls the whole per-character deletion back — no character deleted without its tombstone, and no orphan tombstone without a deletion.
    - RED: after the character tombstone tx commits, the player is deleted via the player deleter (own pool); the characters.player_id FK cascade removes no un-tombstoned character (they are already gone). A player with zero characters is deleted cleanly.
    - RED: a WORLD_CONCURRENT_EDIT on a character delete surfaces to the caller as a retriable error (the reaper retries next cycle) — the character is NOT deleted without a tombstone.
    - RED: the constructor fails closed on any nil dep (character reader, character writer, transactor, outbox writer, player deleter) — there is no degraded tombstone-less deletion mode.
  </behavior>
  <action>
    Create `internal/auth/character_reaping.go`: a `CharacterReapingService` with service-declared narrow deps (mirror `guest_service.go`'s idiom and `character_genesis.go`'s structure): (a) a character reader interface listing a player's characters (`ListByPlayer(ctx, playerID) ([]*world.Character, error)` — the Version field is populated by the guarded scans from 05-03); (b) a `CharacterWriter`/`CharacterDeleter` interface — `Delete(ctx, id ulid.ULID, expectedVersion int) (*wmodel.MutationDelta, error)` — satisfied structurally by the concrete `worldpostgres.CharacterRepository` (post-05-14/05-03 signature), injected at composition roots ONLY (05-07 allowlist); (c) the re-entrant transactor (05-14 — the SAME transactor/tx-context convention the character writer's `execerFromCtx` reads, so enrollment does not silently split); (d) `world.OutboxWriter` (05-06 — `WriteIntent`); (e) a player deleter interface — `DeleteGuestPlayer(ctx, playerID) error` — satisfied by `internal/auth/postgres/player_repo.go` (its own pool). Its method `DeleteGuestPlayer(ctx, playerID ulid.ULID) error` (satisfying `auth.GuestCleaner`): list the player's characters; for each, run `InTransaction`: `delta := writer.Delete(txCtx, char.ID, char.Version)`; build a character delete/tombstone `wmodel.EnvelopeIntent` (the character delete/tombstone taxonomy kind from 05-09 — the SAME kind DeleteCharacter uses, NOT a new kind; `core.NewULID()` identity, never `idgen.New()`; a system actor; intent-level payload naming the deleted character); `OutboxWriter.WriteIntent(txCtx, intent, delta)`. After the character tombstone tx(es) commit, call the player deleter `DeleteGuestPlayer(ctx, playerID)` on its own pool (ordered AFTER the tombstones so the FK cascade removes no un-tombstoned character). Surface a `WORLD_CONCURRENT_EDIT` on a character delete as a retriable error (the reaper retries; do NOT swallow it into a tombstone-less success). The constructor rejects nil deps with a typed error (fail-closed). Document on the type that this is the SECOND sanctioned out-of-world writer under INV-WORLD-4 (deletion side), symmetric to 05-15's genesis service (creation side), and that its census disposition is documented in 05-11 (a sanctioned out-of-world writer emitting the already-declared character delete/tombstone kind). Write the RED tests first (unit with mocks for constructor fail-closed, per-character call order, no-character player deletes cleanly, WORLD_CONCURRENT_EDIT surfaces retriable; integration with a real pool for one-tx atomicity per character, ambient-tx enrollment/outer-rollback, forced-step rollback).
  </action>
  <verify>
    <automated>task test -- ./internal/auth/ && task test:int -- -run 'CharacterReaping|Reaper' ./internal/auth/...</automated>
  </verify>
  <acceptance_criteria>
    - CharacterReapingService.DeleteGuestPlayer deletes each character via the guarded world CharacterWriter.Delete AND emits one character delete/tombstone envelope per character in the same transaction; an integration test proves outer-tx rollback removes both (re-entrant enrollment, no second Begin).
    - The envelope intent uses the SAME character delete/tombstone taxonomy kind DeleteCharacter emits (05-09/05-11) and core.NewULID(); `grep -n 'idgen.New' internal/auth/character_reaping.go` returns nothing; no new taxonomy kind is declared.
    - The player is deleted AFTER the character tombstone tx commits (ordered); a test asserts the FK cascade removes no un-tombstoned character; a player with zero characters is deleted cleanly.
    - The constructor fails closed on any nil dep; a WORLD_CONCURRENT_EDIT on a character delete surfaces as a retriable error (no tombstone-less success).
    - `task test -- ./internal/auth/` passes.
  </acceptance_criteria>
  <done>One atomic tombstone-emitting guest character-deletion primitive exists, fail-closed, re-entrant, ordered against the player delete — the deletion-side counterpart to 05-15's genesis service.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reroute the guest reaper + failed-guest cleanup through the reaping service; composition wiring; the D-06 regression gate</name>
  <files>internal/auth/guest_reaper.go, internal/auth/guest_reaper_test.go, internal/auth/guest_service.go, internal/auth/guest_service_test.go, cmd/holomush/sub_grpc.go, test/integration/auth/guest_reaper_tombstone_test.go</files>
  <read_first>
    - internal/auth/guest_reaper.go (:31-51 the GuestReaper struct + NewGuestReaper(config, lister, cleaner GuestCleaner); :78-96 reap() calls r.cleaner.DeleteGuestPlayer — inject the reaping service as the cleaner)
    - internal/auth/guest_service.go (:192-198 cleanupGuestPlayer calls s.players.Delete(ctx, playerID) — the failed-guest cleanup path that also cascades characters silently; :155/:165/:177/:181 the token/session failure call sites of cleanupGuestPlayer)
    - internal/auth/character_reaping.go (Task 1 — the service to inject; it satisfies auth.GuestCleaner)
    - cmd/holomush/sub_grpc.go (:715 s.guestReaper = auth.NewGuestReaper(...) — the reaper composition root; and the 05-15 genesis-service construction near the auth/character-service wiring — mirror it to construct the reaping service from the concrete worldpostgres.CharacterRepository + postgres outbox store + re-entrant transactor + a character reader + player_repo)
    - internal/world/outbox/genesis.go, internal/world/outbox/store.go (05-11/05-07 — the feed-read seam the regression test asserts against, to observe the character create genesis + delete tombstone in feed order)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-CONTEXT.md (D-06 — a regression test MUST prove guest expiration cannot produce genesis-without-tombstone feed history)
  </read_first>
  <behavior>
    - RED: the guest reaper, given an idle guest with a character, reaps it via the reaping service — the feed gains one character delete/tombstone envelope for that character before the player is deleted; the character row is gone and the player row is gone.
    - RED: guest_service cleanupGuestPlayer (invoked on a token/session-creation failure after the guest player + character were committed) routes through the reaping service — the character is tombstoned before the player is cleaned up; no un-tombstoned cascade deletion.
    - RED (the D-06 regression gate): a guest is created (05-15 emits a character genesis create envelope), then reaped — the feed contains BOTH the create envelope AND the delete/tombstone envelope for that character; there is NO genesis-without-tombstone history, and the player-delete FK cascade removes no un-tombstoned character.
  </behavior>
  <action>
    Reroute the guest reaper: in `cmd/holomush/sub_grpc.go:715` (and wherever the reaper's `GuestCleaner` is supplied), construct the `CharacterReapingService` (Task 1) from the concrete `worldpostgres.CharacterRepository` (character reader + guarded Delete), the postgres outbox store (05-05, as the `world.OutboxWriter`), the re-entrant transactor (05-14), and `player_repo` (the player deleter), and pass it as `NewGuestReaper`'s `cleaner` in place of the raw `player_repo`-backed `DeleteGuestPlayer` — mirroring the 05-15 genesis-service composition. Reroute failed-guest cleanup: change `guest_service.go` `cleanupGuestPlayer` (and its call sites at the token/session-failure paths) so it invokes the reaping service's `DeleteGuestPlayer` (tombstone-emitting) instead of `s.players.Delete` — inject the reaping service (or a narrow deleter interface it satisfies) into `GuestService`; keep the best-effort/log-on-failure posture. The raw `player_repo.DeleteGuestPlayer` stays as the player-row delete the reaping service calls AFTER tombstoning; no direct guest-lifecycle caller invokes it for a player that still has un-tombstoned characters. Add the D-06 regression integration test `test/integration/auth/guest_reaper_tombstone_test.go` (`//go:build integration`, Ginkgo per the auth integration suite conventions): create a guest through the production path (05-15 genesis emits the character create envelope), assert the feed has the create envelope, reap the guest through the reaper (reaping service), then assert the feed has exactly one character delete/tombstone envelope for that character, the character row is gone, the player row is gone, and NO character was removed by the FK cascade without a tombstone (i.e. the create-without-tombstone history is impossible). Run the whole-tree gate (`task build:all` — the 05-14 target — plus the auth suites) so the reroute compiles and the reaper/guest-service tests pass.
  </action>
  <verify>
    <automated>task build:all && task test -- ./internal/auth/ ./cmd/holomush/ && task test:int -- -run 'GuestReaper|CleanupGuest|Tombstone' ./internal/auth/... ./test/integration/auth/</automated>
  </verify>
  <acceptance_criteria>
    - The guest reaper's `GuestCleaner` is the tombstone-emitting reaping service (constructed at `cmd/holomush/sub_grpc.go`), not a raw player-cascade delete; `grep -n 'DeleteGuestPlayer' internal/auth/guest_reaper.go` shows the reaper drives the service, and the reaper no longer relies on the FK cascade to remove characters.
    - `guest_service.go` `cleanupGuestPlayer` routes through the reaping service — a failed-guest cleanup tombstones the character before deleting the player; no un-tombstoned FK cascade.
    - The D-06 regression test proves: after create-then-reap, the feed holds BOTH the character create genesis envelope (05-15) AND its delete/tombstone envelope; genesis-without-tombstone feed history is impossible; the player-delete cascade removes no un-tombstoned character.
    - `task build:all` (05-14 target) + `task test -- ./internal/auth/ ./cmd/holomush/` pass.
  </acceptance_criteria>
  <done>Both guest-deletion paths (reaper + failed-guest cleanup) emit character tombstones through the world boundary; the D-06 genesis-without-tombstone hole is closed and regression-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest deletion → world feed | A guest character deleted without a tombstone breaks INV-WORLD-4 / feed completeness — the character appeared at genesis but never left the feed. |
| character tombstone tx ↔ player delete | Two separate pools (world/postgres vs auth player_repo); ordering + compensation, not a single-tx atomicity claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-54 | Tampering | guest-reaper tombstone-less delete | high | mitigate | The reaper's `GuestCleaner` is the reaping service: each character is deleted through the guarded world `CharacterWriter.Delete` AND a tombstone envelope commits in the same tx BEFORE the player is deleted; the FK cascade then removes no un-tombstoned character. The D-06 regression test asserts genesis-without-tombstone is impossible. |
| T-05-55 | Tampering | failed-guest cleanup tombstone-less delete | high | mitigate | `guest_service.go` `cleanupGuestPlayer` routes through the reaping service — a partial-guest cleanup after a token/session failure tombstones the character before deleting the player. |
| T-05-56 | Repudiation | false "one transaction" player+character atomicity | medium | mitigate | The player row (own pool) is NOT claimed atomic with the tombstone tx (mirrors 05-15's B4): characters are tombstoned + deleted FIRST (committed), THEN the player is deleted; orphan-player is documented compensation reconciled by the next reap cycle, outside INV-WORLD-4. |
| T-05-57 | Denial of Service | reaper halts on a concurrent-edit conflict | low | accept | A WORLD_CONCURRENT_EDIT on a character delete is retriable (the reaper retries next cycle) — the character is never deleted without a tombstone; a transient conflict does not corrupt the feed. |
</threat_model>

<verification>
- `task test -- ./internal/auth/ ./cmd/holomush/` passes (run via `local-check`).
- `task test:int -- -run 'CharacterReaping|GuestReaper|CleanupGuest|Tombstone' ./internal/auth/... ./test/integration/auth/` passes (run via `local-check`).
- `grep -n 'idgen.New' internal/auth/character_reaping.go` returns nothing (tombstone events use core.NewULID()).
- The D-06 regression test asserts a reaped guest's character produces a delete/tombstone envelope in the feed (no genesis-without-tombstone).
</verification>

<success_criteria>
- ONE atomic character-reaping service exists (per-character tombstone-emitting deletion + ordered player delete, one re-entrant tx, fail-closed) and BOTH guest-deletion paths (reaper + failed-guest cleanup) route through it.
- Guest expiration cannot produce genesis-without-tombstone feed history (regression-gated); INV-WORLD-4 / feed completeness are genuinely true for the guest deletion lifecycle.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-16-SUMMARY.md` when done.
</output>
