---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 07
type: tdd
wave: 6
depends_on: [05-06]
files_modified:
  - internal/world/outbox/relay.go
  - internal/world/outbox/relay_test.go
  - internal/world/outbox/consumer.go
  - internal/world/outbox/consumer_test.go
  - internal/world/outbox/relay_seams_test.go
  - internal/world/setup/subsystem.go
  - internal/world/setup/subsystem_test.go
  - internal/lifecycle/subsystem.go
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "A single leased relay publishes outbox rows strictly in feed_position order, sets Nats-Msg-Id = event ULID for dedup, is woken by LISTEN/NOTIFY with a periodic sweep fallback, and marks rows published only after PubAck then prunes."
    - "A poison (unpublishable) envelope HALTS the relay and raises a lag/halt alert — the ordered gap-free feed never advances past an unpublishable position (INV-WORLD-FEED-ORDER shape)."
    - "A reference idempotent consumer + bootstrap harness exist; the relay is wired as a lifecycle.Subsystem replacing the dead no-emitter leg in production world.Service construction."
    - "The relay path lives in internal/world/outbox and touches no crypto-gated or internal/access file (crypto/abac reviewers do not trigger)."
  artifacts:
    - path: internal/world/outbox/relay.go
      provides: "single leased position-ordered publisher with halt-and-alert poison posture + lag/halt alerting"
      contains: "feed_position|FeedPosition"
    - path: internal/world/outbox/consumer.go
      provides: "reference idempotent consumer + bootstrap harness"
    - path: internal/world/outbox/relay_seams_test.go
      provides: "non-quarantined seam test giving the relay wiring gating-CI coverage"
  key_links:
    - from: internal/world/setup/subsystem.go
      to: internal/world/outbox/relay.go
      via: "relay registered as a lifecycle.Subsystem, replacing the omitted EventEmitter leg"
      pattern: "outbox|relay|Subsystem"
  prohibitions:
    - "The relay MUST NOT DLQ-and-continue past a poison envelope — it halts and alerts (a feed gap violates ordering)."
    - "The relay MUST NOT re-authorize or re-check ABAC — it publishes already-authorized, already-committed facts; it MUST NOT import internal/access or any crypto-gated file."
---

<objective>
Build the single leased relay (position-ordered publisher with halt-and-alert poison posture + lag/halt alerting), a reference idempotent consumer + bootstrap harness, and wire the relay as a `lifecycle.Subsystem` — replacing the dead no-emitter leg in production `world.Service` construction. Relay alerting lands in THIS slice: a halted ordered feed is otherwise silent.

Purpose: The outbox rows (05-05) are inert without a publisher. The relay is the only writer to JetStream for world changes; it must preserve gap-free order, dedup by `Nats-Msg-Id`, survive broker blips (LISTEN/NOTIFY + sweep, mark-published-after-PubAck), and — unlike the audit projection it mirrors — HALT rather than skip on poison, alerting so operators see a stalled feed.
Output: relay.go, consumer.go, non-quarantined seam test, subsystem wiring, alerting. Includes an automated D-04 gate-confirmation check (Task 3, `type=auto`): a grep proving the relay in `internal/world/outbox` imports no crypto/abac-gated file, so neither the crypto- nor abac-reviewer should trigger at push.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/event-conventions.md
@.claude/rules/event-interfaces.md
@.claude/rules/plugin-runtime-symmetry.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Single leased relay — position order, dedup, LISTEN/NOTIFY + sweep, halt-and-alert</name>
  <files>internal/world/outbox/relay.go, internal/world/outbox/relay_test.go</files>
  <read_first>
    - internal/eventbus/audit/projection.go (lines ~113-138 construction/bounded-DLQ, ~254-276 final-attempt Term — the poison precedent to mirror-but-invert: relay HALTS, does not continue)
    - internal/world/outbox/writer.go, envelope.go (05-05 — the row shape + published marker the relay updates)
    - internal/eventbus (Qualify — prepends events.<game_id>.; the relay's publish subject boundary)
    - internal/world/service.go (~149 observability.RecordEngineFailure — the counter/metric style for lag/halt alerting)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md (Pattern 4; Pitfall 4 DLQ-and-continue creates a gap; Open Question 5 poison posture)
  </read_first>
  <behavior>
    - RED: given three unpublished outbox rows at positions n, n+1, n+2, the relay publishes them in exactly that order and marks each published only after PubAck.
    - Each publish sets Nats-Msg-Id = the envelope's event ULID.
    - A poison row (publish permanently fails past max attempts) HALTS the relay at that position — positions after it are NOT published — and increments a halt/lag alert metric.
    - After a broker outage, the relay resumes from the first unpublished position in order (no skips, no reorder).
    - Only one lease publishes at a time; a second relay instance holding no lease does not publish.
  </behavior>
  <action>
    Implement the relay in `internal/world/outbox/relay.go`: a single leased publisher that scans the outbox for the next unpublished row per game in `feed_position` order, publishes to JetStream via `eventbus.Qualify` with `Nats-Msg-Id` = the envelope's event ULID (JetStream DupeWindow + consumer-side dedup beyond the window), marks the row published (sets the published marker) only after PubAck, and prunes published rows (retention bounding itself is OPS-02/Phase 6 — here: mark + prune-after-PubAck only). Wake on `LISTEN/NOTIFY` with a periodic sweep fallback. Acquire a single lease so exactly one relay publishes (mirror the audit projection's construction/metric shape). CRITICAL DIVERGENCE from the audit projection: on a poison envelope the relay HALTS and alerts — it MUST NOT DLQ-and-continue (that creates a feed gap, violating ordering, Pitfall 4). Add a lag/halt metric mirroring `observability.RecordEngineFailure`, incremented on halt and on lag. Use `core.NewULID()` identity (never `idgen.New()`). Write the RED tests first (order, dedup key, halt-on-poison, resume-after-outage, single-lease) against an embedded NATS harness.
  </action>
  <verify>
    <automated>task test:int -- -run 'Relay' ./internal/world/outbox/</automated>
  </verify>
  <acceptance_criteria>
    - Relay publishes strictly in feed_position order; marks published only after PubAck.
    - Nats-Msg-Id = event ULID on every publish.
    - Poison row HALTS the relay (no advance past it) and increments a halt/lag alert metric.
    - Broker-outage resume is in order with no skips; only the lease holder publishes.
  </acceptance_criteria>
  <done>The ordered gap-free leased relay exists with halt-and-alert poison posture and lag alerting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reference idempotent consumer + bootstrap harness + subsystem wiring</name>
  <files>internal/world/outbox/consumer.go, internal/world/outbox/consumer_test.go, internal/world/outbox/relay_seams_test.go, internal/world/setup/subsystem.go, internal/world/setup/subsystem_test.go, internal/lifecycle/subsystem.go</files>
  <read_first>
    - internal/world/setup/subsystem.go (~51-56 WorldSubsystem registration; ~66-75 production world.Service constructed with NO EventEmitter — the dead leg to replace with relay wiring + the outbox writer from 05-06)
    - internal/lifecycle/subsystem.go (SubsystemID iota + the stringer — a new SubsystemID goes at the END of the const block, then regenerate)
    - internal/testsupport/integrationtest/resilience_seams_test.go (~16-46 — the non-quarantined seam-test pattern to copy so codecov sees the wiring)
    - docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md (Consistency/lifecycle — reference consumer bootstraps from DB snapshot + feed watermark then tails; zero product projections)
  </read_first>
  <behavior>
    - RED: the reference consumer, given a duplicate delivery (same Nats-Msg-Id / event ULID), applies the effect once (idempotent) — a second delivery is a no-op.
    - The bootstrap harness starts from a DB snapshot + feed watermark, then tails live — it does not assume infinite replay.
    - The relay registers as a lifecycle.Subsystem with a SubsystemID and ordered start/stop; production world.Service is constructed with the outbox writer (no EventEmitter).
    - The non-quarantined seam test drives the relay/consumer wiring closures without containers (gating-CI coverage).
  </behavior>
  <action>
    Implement a reference idempotent consumer in `consumer.go` (dedup on the event ULID; second delivery is a no-op) and a bootstrap harness that reads a consistent DB snapshot + feed watermark then tails — the ONLY consumer Phase 5 ships (zero product projections). Register the relay as a `lifecycle.Subsystem`: add a new `SubsystemID` at the END of the const block in `internal/lifecycle/subsystem.go` and regenerate its stringer (`task generate`), then wire the relay subsystem in `internal/world/setup/subsystem.go` with the appropriate `DependsOn`, replacing the omitted-`EventEmitter` dead leg and injecting the outbox `Writer` into the production `world.Service` constructor (the parameter added in 05-06). Add a non-quarantined `relay_seams_test.go` that self-skips off the gating lane but drives the relay/consumer option wiring directly (copy `resilience_seams_test.go`), so codecov/patch counts the seams even though the deep resilience suite is quarantine-gated. Genesis snapshot emission + feed epoch/reset land in slice 3 (05-11); this plan wires the consumer/bootstrap plumbing only.
  </action>
  <verify>
    <automated>task test -- ./internal/world/outbox/ ./internal/world/setup/ && task test:int -- -run 'Consumer' ./internal/world/outbox/</automated>
  </verify>
  <acceptance_criteria>
    - The reference consumer is idempotent (duplicate delivery is a no-op).
    - The bootstrap harness starts from snapshot + watermark, then tails.
    - The relay is registered as a lifecycle.Subsystem; production world.Service is built with the outbox writer, not an EventEmitter.
    - A non-quarantined seam test exercises the relay/consumer wiring; `task test -- ./internal/world/outbox/ ./internal/world/setup/` passes.
  </acceptance_criteria>
  <done>The relay is live in the process lifecycle; a reference idempotent consumer + bootstrap harness exist with gating-CI seam coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the relay path stays off crypto/abac-gated surfaces (D-04)</name>
  <files>internal/world/outbox/relay.go, internal/world/outbox/consumer.go, internal/world/setup/subsystem.go</files>
  <read_first>
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md (Security Domain / D-04 gate confirmation table — the crypto-reviewer + abac-reviewer trigger paths)
    - CLAUDE.md (Pre-Push Review Gates — crypto-reviewer / abac-reviewer trigger file lists)
  </read_first>
  <action>
    Confirm the relay path does NOT import or modify a crypto/abac-gated file (D-04 — one phase PR, gates apply to the whole diff at push). The relay MIRRORS the audit projection's shape in the NEW `internal/world/outbox` package; it must NOT import or edit `internal/eventbus/crypto/`, `internal/eventbus/codec/`, `internal/access/`, `internal/eventbus/history/dispatcher.go`, `internal/eventbus/history/cold_postgres.go`, or `internal/eventbus/audit/projection.go`, and must NOT touch `crypto_keys`/`events_audit` migrations or plugin `crypto.emits`. The relay publishes cleartext intent-level world-change envelopes (no per-event DEK) and never re-authorizes (no `checkAccess`/DEK/decrypt in the relay path). Record the grep result in the plan summary; if any gated import is found, relocate the relay to stay in `internal/world/outbox`.
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'internal/eventbus/crypto\|internal/eventbus/codec\|internal/access\|history/dispatcher\|cold_postgres\|audit/projection\|checkAccess' internal/world/outbox/ 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'internal/eventbus/crypto\|internal/eventbus/codec\|internal/access\|history/dispatcher\|cold_postgres\|audit/projection\|checkAccess' internal/world/outbox/` returns no hits.
    - No changed file under `internal/world/**` appears in the crypto-reviewer/abac-reviewer trigger lists.
    - The summary records the grep result confirming the relay is off gated surfaces.
  </acceptance_criteria>
  <done>The relay path is confirmed off crypto/abac-gated surfaces; neither reviewer triggers (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outbox → JetStream | The relay is the sole publisher of world changes; a second publisher or a reorder corrupts the feed contract. |
| relay poison → feed order | A skipped poison envelope silently breaks gap-free ordering. |
| relay package → gated surfaces | Placement must keep the diff off crypto/abac-gated files (D-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19 | Tampering | feed ordering | high | mitigate | Single lease + strict position-order publish + halt-on-poison; a second relay without the lease does not publish; the relay never advances past an unpublishable position (proven by RED tests). |
| T-05-20 | Denial of Service | silent halted feed | high | mitigate | Halt raises a lag/halt alert metric (`observability.RecordEngineFailure` style) so a stalled feed is visible, not silent — the reason relay alerting lands in this slice. |
| T-05-21 | Spoofing | duplicate delivery | medium | mitigate | Nats-Msg-Id = event ULID (JetStream DupeWindow) + the reference consumer dedups on the ULID beyond the window; a replayed envelope applies once. |
| T-05-22 | Elevation of Privilege | relay bypassing ABAC | medium | mitigate | The relay publishes already-authorized, already-committed facts and MUST NOT import `internal/access` or re-check; the checkpoint confirms it. |
| T-05-23 | Tampering | crypto/abac gate surface | high | mitigate | Task 3's automated grep verifies the relay lives in a NEW `internal/world/outbox` package and imports no crypto-gated / access file (D-04); a gated import fails the verify. |
</threat_model>

<verification>
- `task test:int -- -run 'Relay|Consumer' ./internal/world/outbox/` passes (run via `local-check`).
- `task test -- ./internal/world/setup/` passes; `task generate` produces no uncommitted stringer diff.
- `grep -rn 'internal/eventbus/crypto\|internal/access\|audit/projection' internal/world/outbox/` returns no import hits.
- Automated D-04 gate-confirmation grep passes: the relay in `internal/world/outbox` imports no crypto/abac-gated file.
</verification>

<success_criteria>
- A single leased, position-ordered, dedup-keyed relay with halt-and-alert poison posture publishes world changes.
- A reference idempotent consumer + bootstrap harness exist; the relay is a lifecycle.Subsystem.
- The relay path is confirmed off crypto/abac-gated surfaces.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-07-SUMMARY.md` when done.
</output>
