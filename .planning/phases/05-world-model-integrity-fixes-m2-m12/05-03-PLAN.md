---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 03
type: tdd
wave: 2
depends_on: [05-01, 05-14]
files_modified:
  - internal/world/postgres/character_repo.go
  - internal/world/postgres/character_repo_test.go
  - internal/world/postgres/object_repo.go
  - internal/world/postgres/object_repo_test.go
  - internal/bootstrap/setup/adapters.go
autonomous: true
requirements: [MODEL-03]
must_haves:
  truths:
    - "A stale-version UPDATE/DELETE/MOVE to a character or object surfaces WORLD_CONCURRENT_EDIT, not a silent success and not a false NOT_FOUND (MODEL-03)."
    - "The zero-row classifier is TWO observable outcomes, not three (round-5 Codex MEDIUM): an existing row whose version differs → WORLD_CONCURRENT_EDIT; an absent row → NOT_FOUND (including a concurrent delete that already won). The locked follow-up read runs in the SAME transaction on the caller's connection — proven not to deadlock under a pool of size 1 (finding 14). The round-4 three-way concurrent-delete-via-prior-evidence classification is dropped: it is not observable from an ID-only delete API."
    - "Character/object read paths populate the struct Version field; a successful write refreshes it to the committed version (finding 12) and returns a MutationDelta whose before/after versions match the row transition."
    - "A version-scanning character-list path exists so a subsequent guarded delete/CAS has each character's STORED version (round-6 R6-1, the #1 must-fix — WITHOUT it the guest reaper's CAS Delete in 05-16 permanently conflicts because char.Version==0 vs DB version DEFAULT 1). `worldpostgres.CharacterRepository` gains a CANONICAL version-scanning `ListByPlayer(ctx, playerID) ([]*world.Character, error)` (SELECT includes `version`, scanned into `Character.Version`) — the reaping service (05-16) lists through THIS in-boundary method. The round-6 blocker was that no `ListByPlayer` existed on the world repo at all (character_repo.go:29-135) and the only production `ListByPlayer` (CharRepoAdapter.ListByPlayer, internal/bootstrap/setup/adapters.go:74-118) SELECTed id/player_id/name/description/location_id/created_at with NO `version` — a version-blind list feeding a version-predicated delete."
    - "Version-scan inventory beyond internal/world/postgres/*_repo.go is closed (round-6 R6-3 MEDIUM / grok R6-1): the production auth-facing `CharRepoAdapter.ListByPlayer` (internal/bootstrap/setup/adapters.go) — the actual list path a subsequent write/delete consumes — is updated to SELECT+scan `version` (or delegate to the world repo's version-scanning ListByPlayer) so no path that lists characters for a later write returns Version==0. A RED test asserts listed version == stored version so the reap CAS Delete (05-16) succeeds."
  artifacts:
    - path: internal/world/postgres/character_repo.go
      provides: "version-predicated CAS Update/Delete + zero-row classifier for characters + a CANONICAL version-scanning ListByPlayer (round-6 R6-1 — the in-boundary list the guest reaper's CAS Delete needs)"
      contains: "version"
    - path: internal/world/postgres/object_repo.go
      provides: "version-predicated CAS Update/Delete/Move + zero-row classifier for objects"
      contains: "version"
    - path: internal/bootstrap/setup/adapters.go
      provides: "CharRepoAdapter.ListByPlayer updated to SELECT+scan `version` (round-6 R6-1/R6-3 — the production list path a later write/delete consumes no longer returns Version==0)"
      contains: "version"
  key_links:
    - from: internal/world/postgres/object_repo.go
      to: internal/world/errors.go
      via: "zero-row-with-differing-version returns WORLD_CONCURRENT_EDIT"
      pattern: "WORLD_CONCURRENT_EDIT|ErrConcurrentEdit"
  prohibitions:
    - "No guarded write may use r.pool.Exec directly — every Create/Update/Delete/Move routes through execerFromCtx (Pitfall 1)."
---

<objective>
Fill the CAS logic in the character and object repositories on top of the 05-14 foundation — the same guard, connection-reusing classifier, `MutationDelta` population, and struct-Version refresh as location/exit (05-02). This completes the MODEL-03 guard for all four aggregates. Object has a `Move` write path in addition to Update/Delete; 05-14 already moved object Move off its own `pool.Begin` onto the ambient executor, so here it becomes a guarded write.

Depends on 05-14 (re-entrant tx, redesigned signatures, object-Move enrollment, regenerated mocks) and 05-01 (version column + WORLD_CONCURRENT_EDIT + MutationDelta type). This plan implements guard logic in already-redesigned bodies; it does NOT re-declare interfaces or regenerate mocks.

Purpose: Characters and objects are the other two aggregates the version guard must cover; `MoveObject`/`MoveCharacter` are the M2 movers slice 2 wires through the outbox, so their repo writes must be guarded and tx-enrollable first. The classifier's follow-up read MUST run on the caller's connection (05-14 re-entrant seam), never a fresh pool borrow (finding 14).
Output: guarded character_repo + object_repo with populated MutationDelta + refreshed Version, unit-tested for conflict / concurrent-delete / not-found + a pool-size-1 no-deadlock test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Character repo — version-predicated CAS + zero-row classifier</name>
  <files>internal/world/postgres/character_repo.go, internal/world/postgres/character_repo_test.go</files>
  <read_first>
    - internal/world/postgres/character_repo.go (Get/Create/Update/Delete/Move sites — signatures redesigned in 05-14)
    - internal/world/postgres/helpers.go (execerFromCtx; the re-entrant withTx seam + zero-row classifier helper if 05-02 extracted one — reuse it)
    - internal/world/wmodel/mutation_delta.go (05-14 — the wmodel.MutationDelta to populate; the character move write is CharacterRepository.UpdateLocation, which now takes expectedVersion (05-14))
    - internal/world/postgres/location_repo.go (the 05-02 pattern to mirror — read after 05-02 lands)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md (Pattern 1, Pitfall 1, Pitfall 2)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (findings 5, 12, 14)
  </read_first>
  <behavior>
    - RED: stale-version character Update/Move returns WORLD_CONCURRENT_EDIT; absent character returns the repo's not-found code; successful Update increments version by 1.
    - Get/List/GetByLocation populate `char.Version`.
    - A character move (location change) is version-guarded — a stale move is rejected.
    - RED (round-6 R6-1): a NEW canonical `ListByPlayer(ctx, playerID) ([]*world.Character, error)` on `worldpostgres.CharacterRepository` returns each of the player's characters with `Version` populated from the row's `version` column — a test creates characters (version 1), runs an Update on one (version → 2), then asserts `ListByPlayer` returns the STORED version for each (listed version == stored version), so a guarded CAS `Delete(ctx, id, listedVersion)` against a listed character SUCCEEDS (not a permanent conflict). This is the version source the 05-16 guest reaper needs.
  </behavior>
  <action>
    Apply the 05-02 guard to `CharacterRepository` (signatures already redesigned in 05-14): version column in every scan; version-predicated CAS on `Update` and the character move write `UpdateLocation` (which now takes `expectedVersion` per 05-14 — WHERE matches id + `expectedVersion`; SET increments version); every write through `execerFromCtx`; the TWO-OUTCOME zero-row classifier (locked follow-up read on the caller's connection via `withTx` — an existing differing-version row → `WORLD_CONCURRENT_EDIT`, an absent row → not-found incl. a concurrent delete that already won; finding 14; round-5 Codex MEDIUM: no three-way distinction) reusing the shared helper from 05-02 if present. Refresh `char.Version` after a successful write (finding 12). Populate + return the `wmodel.MutationDelta` (Primary = character with before/after versions; Tombstone on Delete). Preserve tx-cascade semantics on delete. The read version that `UpdateLocation`'s `expectedVersion` needs is threaded from the calling command in `service.go` (05-11 MoveCharacter/character-move rollout); here the repo method enforces the CAS against whatever `expectedVersion` it is given. **Add the canonical version-scanning `ListByPlayer` (round-6 R6-1, the #1 must-fix):** `ListByPlayer(ctx, playerID ulid.ULID) ([]*world.Character, error)` on `worldpostgres.CharacterRepository` — `SELECT id, player_id, name, description, location_id, version, created_at FROM characters WHERE player_id=$1 ORDER BY name` (a READ, so `r.pool.Query` is fine; the SQL fence only fences mutations), scanning `version` into `Character.Version`. This is the in-boundary version-bearing list the 05-16 guest reaper lists through so its CAS `Delete(ctx, id, char.Version)` matches the stored `version DEFAULT 1` (WITHOUT it, `char.Version==0` makes every reap delete a permanent conflict/not-found and D-06 never closes). Add the RED test asserting listed version == stored version.
  </action>
  <verify>
    <automated>task test:int -- -run 'TestCharacter' ./internal/world/postgres/</automated>
  </verify>
  <acceptance_criteria>
    - Stale-version character Update/Move returns WORLD_CONCURRENT_EDIT; absent returns not-found.
    - Successful Update increments version by 1.
    - No character write uses `r.pool.Exec` directly.
    - Scans populate `char.Version`.
    - `worldpostgres.CharacterRepository.ListByPlayer` exists and its SELECT includes `version` scanned into `Character.Version` (round-6 R6-1); a RED test proves listed version == stored version so a subsequent CAS `Delete(ctx, id, listedVersion)` succeeds.
  </acceptance_criteria>
  <done>Character writes are version-guarded and tx-enrollable; a canonical version-scanning ListByPlayer supplies the stored version the guest reaper's CAS Delete needs (round-6 R6-1).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Object repo — version-predicated CAS + zero-row classifier (incl. Move)</name>
  <files>internal/world/postgres/object_repo.go, internal/world/postgres/object_repo_test.go</files>
  <read_first>
    - internal/world/postgres/object_repo.go (Get/Create/Update/Delete/Move sites — Move changes containment)
    - internal/world/postgres/character_repo.go (Task 1 pattern to mirror)
    - internal/world/postgres/object_repo_test.go (existing harness)
  </read_first>
  <behavior>
    - RED: stale-version object Update/Move/Delete returns WORLD_CONCURRENT_EDIT; absent object returns not-found; successful Update increments version by 1.
    - Object move (containment change) is version-guarded.
    - Scans populate `obj.Version`.
  </behavior>
  <action>
    Apply the same guard to `ObjectRepository`, covering `Update`, `Delete`, and the object `Move`/containment write (05-14 already moved Move off its own `pool.Begin`, keeping the object + container `FOR UPDATE` locks inside the ambient tx, and gave `Move` an `expectedVersion` parameter): version column in scans; version-predicated CAS with SET incrementing version and WHERE matching id + `expectedVersion` — including on `Move` (the containment write now carries `expectedVersion` per 05-14); all writes through `execerFromCtx`; the TWO-OUTCOME zero-row classifier via the shared helper on the caller's connection (differing-version → conflict, absent → not-found; finding 14; round-5 Codex MEDIUM); refresh `obj.Version` after a successful write (finding 12); populate + return the `wmodel.MutationDelta`. The object `Move` is a guarded write like Update — a stale move is a conflict; its delta Primary carries the containment change with before/after versions. The read version `Move`'s `expectedVersion` needs is threaded from `service.go` MoveObject (05-10).
  </action>
  <verify>
    <automated>task test:int -- -run 'TestObject' ./internal/world/postgres/</automated>
  </verify>
  <acceptance_criteria>
    - Stale-version object Update/Move/Delete returns WORLD_CONCURRENT_EDIT; absent returns not-found.
    - Successful Update increments version by 1.
    - No object write uses `r.pool.Exec` directly.
    - Scans populate `obj.Version`.
  </acceptance_criteria>
  <done>Object writes (incl. Move) are version-guarded and tx-enrollable; all four aggregates now carry the MODEL-03 guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Version-scan inventory — CharRepoAdapter.ListByPlayer scans version (round-6 R6-1/R6-3)</name>
  <files>internal/bootstrap/setup/adapters.go, internal/world/postgres/character_repo_test.go</files>
  <read_first>
    - internal/bootstrap/setup/adapters.go (:74-118 CharRepoAdapter.ListByPlayer — the production auth-facing list path; its current SELECT `id, player_id, name, description, location_id, created_at` has NO `version` and never sets Character.Version — the round-6 R6-1 gap; :121-129 ListAll delegates to the world repo's ListAll which selects id+name only)
    - internal/world/postgres/character_repo.go (Task 1 — the canonical version-scanning ListByPlayer to delegate to, keeping the world reads in the boundary)
    - .planning/phases/05-world-model-integrity-fixes-m2-m12/05-REVIEWS.md (round-6 R6-1 — the reaper's list has no versioned read on the claimed type; R6-3 MEDIUM — version-scan inventory incomplete beyond internal/world/postgres/*_repo.go: any auth/harness adapter listing characters for a subsequent write/delete must scan version)
  </read_first>
  <behavior>
    - RED: `CharRepoAdapter.ListByPlayer` returns each character with `Version` populated from the row's `version` column (listed version == stored version) — so any caller that lists via the adapter and then issues a guarded write/delete has the correct expected version, not 0.
  </behavior>
  <action>
    Close the version-scan inventory beyond the four `*_repo.go` files (round-6 R6-3 MEDIUM): update `CharRepoAdapter.ListByPlayer` (`internal/bootstrap/setup/adapters.go:74-118`) so its list carries `version`. Prefer DELEGATING to the world repo's canonical version-scanning `ListByPlayer` (Task 1) to keep world reads in the boundary; if the adapter must keep its own SELECT (auth-specific ordering/columns), add `version` to the column list and scan it into `Character.Version`. Confirm no OTHER production character-list path that feeds a subsequent write/delete returns Version==0: audit `CharRepoAdapter.ListAll` (delegates to `worldpostgres.ListAll`, which SELECTs id+name only — a DIRECTORY read, NOT a write-feeding list, so it stays version-blind by design; document that it is directory-only and MUST NOT back a guarded delete) and the integration-test harness character seeders (test-support, out of the production fence — note them but do not gate on them). Add a RED assertion (in `character_repo_test.go` or an adapter test) that the adapter's ListByPlayer scans version. Keep it a READ (no mutation SQL — the fence is unaffected).
  </action>
  <verify>
    <automated>task test:int -- -run 'TestCharacter|ListByPlayer' ./internal/world/postgres/ ./internal/bootstrap/...</automated>
  </verify>
  <acceptance_criteria>
    - `CharRepoAdapter.ListByPlayer` SELECT includes `version` (or delegates to the world repo's version-scanning ListByPlayer) and populates `Character.Version` — `grep -n 'version' internal/bootstrap/setup/adapters.go` shows the version scan in the ListByPlayer path.
    - A test asserts the adapter's listed version == the stored version.
    - `ListAll` (directory picker, id+name only) is documented as directory-only and NOT a guarded-delete feeder; no other production character-list path returns Version==0 for a write-feeding caller.
  </acceptance_criteria>
  <done>Every production character-list path that feeds a subsequent guarded write/delete scans `version` — the round-6 R6-1/R6-3 version-scan gap is closed (the guest reaper lists version-bearing characters).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent replicas → same character/object | Racing move/update writers must be serialized by the guard. |
| repo write → transaction | Object Move must not escape the ambient tx. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | character/object CAS | high | mitigate | Stale move/update affects zero rows (version mismatch); the follow-up read surfaces WORLD_CONCURRENT_EDIT. A crafted `Version` cannot bypass CAS — it must equal the stored value a genuine read returned. |
| T-05-08 | Tampering | object Move enrollment | high | mitigate | Object Move routes through `execerFromCtx` so it commits atomically with the (slice-2) outbox row; a `r.pool.Exec` Move would split the transaction. Grep gate + acceptance criteria forbid it. |
| T-05-09 | Repudiation | zero-row classifier | medium | mitigate | Locked follow-up read in the same tx makes conflict/delete/not-found deterministic. |
</threat_model>

<verification>
- `task test:int -- -run 'TestCharacter|TestObject' ./internal/world/postgres/` passes (run via `local-check`).
- `grep -n 'r.pool.Exec' internal/world/postgres/character_repo.go internal/world/postgres/object_repo.go` returns no write-path hits.
- Conflict / not-found outcomes assert via `errutil.AssertErrorCode`.
</verification>

<success_criteria>
- Character and object Update/Delete/Move are version-predicated CAS with the locked-follow-up-read classifier.
- All writes enroll via `execerFromCtx`.
- All four world aggregates now carry the MODEL-03 guard.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-03-SUMMARY.md` when done.
</output>
