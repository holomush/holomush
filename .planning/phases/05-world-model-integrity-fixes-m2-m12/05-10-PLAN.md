---
phase: 05-world-model-integrity-fixes-m2-m12
plan: 10
type: execute
wave: 9
depends_on: [05-09]
files_modified:
  - internal/world/service.go
  - internal/world/service_test.go
  - internal/world/payloads.go
  - internal/world/payloads_test.go
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "Every location, exit, and object write command (create/update/delete/move) routes through mutate() with exactly one declared-kind envelope committed in the same transaction as its state change."
    - "Delete commands emit one tombstone per aggregate; cascades are subsumed by the command's single envelope; failed/no-op commands emit nothing."
    - "Each envelope's affected-aggregates manifest is built FROM the MutationDelta the guarded write returns (05-14), so the before/after versions and the cascade/reverse-exit aggregate IDs come from the rows the repo actually touched — NOT from command inputs (Codex finding 7: cascade IDs like the reverse exit are repository-internal and unreconstructable from inputs)."
  artifacts:
    - path: internal/world/service.go
      provides: "location/exit/object write commands wired through the outbox envelope"
      contains: "mutate"
  key_links:
    - from: internal/world/service.go
      to: internal/world/outbox/taxonomy.go
      via: "each command constructs an envelope of the taxonomy-declared kind for that command"
      pattern: "kind|Kind"
  prohibitions:
    - "No location/exit/object write command may reach a repo write except through the envelope-carrying mutate() seam."
---

<objective>
Wire the mechanical emission rollout across the location, exit, and object write commands (11 commands): CreateLocation, UpdateLocation, DeleteLocation, CreateExit, UpdateExit, DeleteExit, CreateObject, UpdateObject, DeleteObject, MoveObject — each through the `mutate()` seam with a declared-kind envelope. This is the first half of the D-01 full rollout (character/scene/property + census are 05-11).

Purpose: The compile-time seam + census structurally forbid leaving any command un-migrated (no allow-list). This plan converts 11 of the ~15 commands from "emits nothing / dead emit path" to "one envelope per successful command in the same tx." MoveObject and CreateObject previously emitted via the deleted emit path; the rest emit for the first time.

Slice-3 ordering: this rollout lands BEFORE the coverage census (05-11) + invariant binding (05-12), a deliberate data-first/enforcement-last deviation from the one-pager's normative taxonomy→census→invariants→rollout order (`model-01-consensus-onepager.md:41`) — see 05-09's objective for the full rationale (a census introduced before coverage completes would leave `task test` red at every intermediate rollout commit). Acknowledged, not silent (Codex finding 11).
Output: 11 location/exit/object commands emitting through the outbox; payload builders for their intent-level new-values-only payloads.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-RESEARCH.md
@.planning/phases/05-world-model-integrity-fixes-m2-m12/05-PATTERNS.md
@docs/reviews/arch-review/2026-07-11/verification/model-01-consensus-onepager.md
@.claude/rules/event-conventions.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Location + exit write commands through the outbox</name>
  <files>internal/world/service.go, internal/world/service_test.go, internal/world/payloads.go, internal/world/payloads_test.go</files>
  <read_first>
    - internal/world/service.go (CreateLocation ~205, UpdateLocation ~230, DeleteLocation ~256 incl. the DeleteLocation InTransaction cascade ~270-287; CreateExit ~315, UpdateExit ~343, DeleteExit ~370 incl. bidirectional cascade)
    - internal/world/mutator.go (the mutate() seam from 05-06)
    - internal/world/outbox/taxonomy.go, envelope.go (05-09/05-05 — declared kinds + envelope shape)
    - internal/world/payloads.go (existing payload construction to extend)
  </read_first>
  <behavior>
    - RED: each of Create/Update/Delete Location and Exit, on success, writes exactly one outbox envelope of the taxonomy-declared kind in the same tx; the envelope manifest carries the affected aggregate(s) with correct before/after versions.
    - A delete emits one tombstone per aggregate; a cascade (exit bidirectional, location children) is subsumed by the command's single envelope (affected-aggregates manifest lists them) — not one envelope per cascaded row.
    - A failed/no-op command emits no envelope.
  </behavior>
  <action>
    Route CreateLocation, UpdateLocation, DeleteLocation, CreateExit, UpdateExit, DeleteExit through `mutate()`, each building one envelope of its taxonomy-declared kind with an intent-level new-values-only payload (add builders in `payloads.go`). Build the envelope's affected-aggregates manifest FROM the `MutationDelta` the guarded repo write returns (05-14/05-02) — its Primary + Affected entries carry the before/after versions and the repository-internal cascade IDs (the reverse exit id from `exit_repo.go:80`, cascaded children) — NOT from command inputs; this is what makes delta-parity provable (Codex finding 7; bound by INV-WORLD-2 in 05-12). For deletes, the envelope is a tombstone; a cascade is represented by the cascaded aggregates already present in the delta's Affected list (one envelope per command, not per cascaded row). Reuse the existing `DeleteLocation` `InTransaction` cascade seam so the tombstone envelope commits with the cascade. RED tests first: each command emits exactly one envelope of the right kind whose manifest equals the returned MutationDelta; no-op emits nothing.
  </action>
  <verify>
    <automated>task test -- ./internal/world/ && task test:int -- -run 'Location|Exit' ./internal/world/...</automated>
  </verify>
  <acceptance_criteria>
    - CreateLocation/UpdateLocation/DeleteLocation/CreateExit/UpdateExit/DeleteExit each emit exactly one declared-kind envelope in the same tx.
    - Deletes emit a tombstone; cascades are subsumed by the single envelope's manifest.
    - No-op/failed commands emit nothing.
    - The manifest before/after versions match the row's version transition.
  </acceptance_criteria>
  <done>All six location/exit commands emit through the outbox with correct delta manifests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Object write commands (create/update/delete/move) through the outbox</name>
  <files>internal/world/service.go, internal/world/service_test.go, internal/world/payloads.go, internal/world/payloads_test.go</files>
  <read_first>
    - internal/world/service.go (CreateObject ~445 — previously emitted via EmitObjectCreateEvent; UpdateObject ~473; DeleteObject ~502; MoveObject ~549 — previously emitted via EmitMoveEvent)
    - internal/world/mutator.go, internal/world/outbox/taxonomy.go, envelope.go
    - internal/world/payloads.go (Task 1 additions)
  </read_first>
  <behavior>
    - RED: CreateObject/UpdateObject/DeleteObject/MoveObject each emit exactly one declared-kind envelope in the same tx; MoveObject's manifest reflects the containment change with before/after versions.
    - DeleteObject emits a tombstone.
    - No-op emits nothing.
  </behavior>
  <action>
    Route CreateObject, UpdateObject, DeleteObject, MoveObject through `mutate()` with their taxonomy-declared kinds and intent-level new-values-only payloads. CreateObject and MoveObject previously used the deleted emit path — they now emit through the outbox instead. Build the object payloads/manifests in `payloads.go`. RED tests first: each emits exactly one correct-kind envelope with a correct manifest.
  </action>
  <verify>
    <automated>task test -- ./internal/world/ && task test:int -- -run 'Object' ./internal/world/...</automated>
  </verify>
  <acceptance_criteria>
    - CreateObject/UpdateObject/DeleteObject/MoveObject each emit exactly one declared-kind envelope in the same tx.
    - MoveObject's manifest reflects the containment change with before/after versions.
    - No-op emits nothing; `task test -- ./internal/world/` passes.
  </acceptance_criteria>
  <done>All four object commands emit through the outbox.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| write command → outbox | Each command must emit exactly one envelope atomically; a missed or duplicated envelope breaks the feed contract. |
| cascade → envelope cardinality | A cascade must be one command envelope, not N — over-emission corrupts consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-29 | Tampering | envelope cardinality | medium | mitigate | Each command emits exactly one envelope via `mutate()`; RED tests assert cardinality; the census (05-11) proves no command is un-migrated. |
| T-05-30 | Tampering | delta-parity manifest | medium | mitigate | The manifest is built from the repo's returned MutationDelta (finding 7), so its before/after versions + cascade IDs are the rows the repo actually touched, not command inputs; bound by INV-WORLD-2 (DELTA-PARITY) in 05-12 — tests assert the manifest EQUALS the delta, not mere presence. |
| T-05-31 | Information Disclosure | payloads | low | mitigate | Payloads are intent-level new-values-only (erasure-safe); no secrets. |
</threat_model>

<verification>
- `task test -- ./internal/world/` and `task test:int -- -run 'Location|Exit|Object' ./internal/world/...` pass (run via `local-check`).
- `grep -c 'mutate(' internal/world/service.go` reflects the wired commands (filter comments).
</verification>

<success_criteria>
- All 11 location/exit/object write commands emit exactly one declared-kind envelope in the same transaction.
- Deletes emit tombstones; cascades are single-envelope; no-ops emit nothing.
</success_criteria>

<output>
Create `.planning/phases/05-world-model-integrity-fixes-m2-m12/05-10-SUMMARY.md` when done.
</output>
