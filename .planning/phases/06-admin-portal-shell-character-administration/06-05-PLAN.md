---
phase: 06-admin-portal-shell-character-administration
plan: 05
type: tdd
wave: 4
depends_on: [06-04]
files_modified:
  - api/proto/holomush/adminportal/v1/adminportal.proto
  - api/proto/holomush/web/v1/web.proto
  - internal/access/policy/seed.go
  - internal/access/policy/seed_test.go
  - internal/access/policy/seed_profile_visibility_test.go
  - internal/admin/section/descriptor.go
  - internal/grpc/admin_service.go
  - internal/grpc/admin_characters_write.go
  - internal/grpc/admin_characters_write_test.go
  - internal/grpc/characteraccess_service.go
  - internal/grpc/characteraccess_write_test.go
  - internal/web/admin_handlers.go
  - internal/world/payloads.go
  - internal/world/payloads_test.go
  - internal/world/service.go
  - internal/world/service_test.go
  - internal/world/outbox/taxonomy.go
  - internal/world/outbox/taxonomy_test.go
  - cmd/holomush/sub_grpc.go
  - test/integration/access/admin_characters_write_test.go
  - test/meta/world_sql_fence_test.go
  - test/meta/admin_character_message_role_fence_test.go
  - internal/testsupport/integrationtest/harness.go
  - site/src/content/docs/reference/grpc-api.md
  - docs/architecture/invariants.yaml
  - docs/architecture/invariants.md
autonomous: true
requirements: [ADMIN-04, ADMIN-05, ADMIN-06]

estimate:
  tokens: 132000
  raw_tokens: 132000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ── ROADMAP Success Criterion 2 (write half) ──
    - "AdminUpdateCharacter accepts only an explicit field-mask allowlist of the 13 §10.6 paths — `description` PLUS the twelve `profile.*` paths — asserted by set equality in BOTH directions against the checked-in list; roles is not among them and no admin RPC can mutate a role."
    - "§10.6's designated durable verification exists and is schema-level: a meta-test walking the admin character messages' PROTO DESCRIPTOR fails if any field name ever matches role|grant|permission|capability. A source grep over a hand-written Go file cannot see a field added to a generated message, which is the exact future risk §10.6 names."
    - "The erasure-safe new-values-only property of BuildCharacterProfileUpdatePayload is pinned at TWO layers, deliberately split because neither layer can carry the other's assertion. (a) STRUCTURAL, at the builder: TestCharacterProfileUpdatePayloadExposesNoValueBearingField asserts the marshalled KEY SET and the struct's reflected json tags are exactly {character_id, changed_attributes, section, action} — it declares no old/new sentinel values and asserts no absence, because the builder's parameters are (characterID, changed []string, auditCtx) and carry no values at all, so a sentinel-absence assertion there would pass identically under a builder that leaked everything (cross-AI review C2-31). (b) BEHAVIOURAL, end-to-end, at Task 3 Test 5 and Task 2 Test 6b: real old and new prose flows through AdminUpdateCharacter into an emitted envelope and the SERIALIZED BYTES are asserted with require.NotContains against both values. No such test exists in the tree today (verified: `rg 'BuildCharacterProfileUpdatePayload' -g '*_test.go'` returns zero hits), so this plan AUTHORS both rather than extending one."
    - "Admin retire and un-retire move a character through the SAME lifecycle states as player-initiated retire, reusing world.Service.RetireCharacter / UnretireCharacter; there is no AdminDeleteCharacter, and world.Service.DeleteCharacter is reachable from no admin RPC and no button."
    # ── ROADMAP Success Criterion 3 (read through §14 row 9) ──
    - "Every admin mutation emits its audit envelope in the SAME transaction as its state change through the buildIntent → mutator seam; the events_audit row is PROJECTED from that envelope by the asynchronous audit projection, which stays the only writer to that table."
    - "No INSERT INTO events_audit exists outside internal/eventbus/audit/projection.go."
    - "The envelope Actor is player:<id>, carried verbatim from Caller.subject; the acting character is NOT recorded in the payload."
    - "Every admin envelope payload carries the evaluated section and action."
    # ── D-103 before-values ──
    - "AdminRetireCharacter and AdminUnretireCharacter carry the before-status, and it DIFFERS from the new status on a real transition — an assertion that a status is merely present passes under the unimplemented shape."
    - "AdminUpdateCharacter carries changed attribute NAMES only; no profile prose value, before or after, reaches the payload or events_audit."
    # ── ADMIN-04 edge: empty / encoding (probe) — CORRECTED, see C2-06 ──
    - "An AdminUpdateCharacter carrying an empty update_mask mutates nothing and returns a NO-OP SUCCESS carrying the character's current state and current version — per the normative §9.5 rule 4 (`01-SPEC.md:2197`), restated for admin character editing at `:2534`. It emits no envelope, bumps no version, and writes no audit row. Authorization, existence and the expected_version guard are all still evaluated BEFORE the no-op answer, so an empty mask is not a way to skip a check."
    - "The admin path takes the FACADE'S SHAPE on the empty mask — no-op success carrying the freshly-read character including its version — and DIVERGES from it on ONE clause, deliberately and flagged: a STALE expected_version. `characteraccess_write.go:315-336` answers a stale empty-mask caller with SUCCESS (its own comment: `A STALE CALLER IS ANSWERED, NOT REFUSED`, and the code returns `projectOwner(char, profile)` without comparing `expectedVersion`); the admin path returns `Aborted`. The divergence is intentional and is not the facade's reasoning applied consistently — it is a different threat model. The facade's caller edits its OWN character and a stale version costs it a round trip; the admin caller edits SOMEONE ELSE'S character, and a request whose version guard is stale is a request built against a view of a character that has since changed underneath it. On an audited cross-owner surface, `Aborted` is the answer that makes the operator re-read before acting. The plan does NOT claim the two paths match: an earlier revision asserted `the admin path matches the PLAYER path here … for the same stated reason`, which was false in exactly this clause."
    - "Profile field caps are enforced in BYTES server-side (len of the UTF-8 encoding), so a ~34-rune CJK value that looks short is rejected at the same boundary a 101-byte ASCII value is."
    # ── ADMIN-05 edge: idempotency / concurrency (probe) ──
    - "Running AdminRetireCharacter twice on the same character produces exactly ONE character.retired envelope; the second call is refused by the existing lifecycle guard before any write."
    - "A stale expected_version returns Aborted (WORLD_CONCURRENT_EDIT) and neither the state change nor the envelope lands; expected_version absent or zero is REJECTED, never treated as skip-the-guard."
    # ── ADMIN-06 edge: adjacency / empty / encoding / ordering / idempotency / concurrency (probe) ──
    - "No envelope is emitted for a transition whose before-status equals the requested status — the lifecycle guard refuses first, so before and after can never be equal in an emitted lifecycle payload."
    - "An ADMIN update_mask resolving to zero CHANGED attributes emits no admin envelope, and the suppression happens INSIDE the authoritative transaction, not in a handler precheck. A pre-transaction handler comparison reads values it does not hold a lock on, so a concurrent write between the comparison and the transaction makes the answer wrong in both directions; and it cannot suppress anything anyway, because `UpdateCharacterProfileAttributes` deliberately REWRITES an equal-valued row (`service.go:1112-1134`: the resubmit `DOES rewrite the row … so the write, the CAS and the version bump are unconditional`) while leaving `changed` empty — so a non-empty mask of only equal-valued profile fields bumps the version and emits an empty-changed envelope no matter what the handler decided first."
    - "The mechanism is a THIRD typed option, world.WithSkipUnchangedProperties(), supplied only by the admin handler. Inside the existing transaction, after the rows are read, a requested name whose stored value already equals the requested value is dropped from the `updates` partition. When that empties all three partitions and no description changed, the shipped empty-partition early return (`service.go:1228-1236`) fires and the call is a true no-op: no row rewrite, no version bump, no envelope. The comparison is against rows read inside the transaction, so it is race-safe by construction rather than by timing."
    - "The shared builder is NOT narrowed: BuildCharacterProfileUpdatePayload keeps accepting an empty changed list, because the shipped player path deliberately emits one on an all-identical resubmit and documents that as honest (`service.go:1115-1134`). The PLAYER path supplies no options, so its unconditional-rewrite behaviour is byte-identical after this change — verified by a test that calls it with no options and asserts the row rewrite, the version bump and the empty-changed envelope all still happen."
    - "changed_attributes is sorted, matching BuildCharacterProfileUpdatePayload's existing convention, so the payload is byte-stable for equal input."
    - "Replaying the same envelope through the audit projection writes exactly one events_audit row — the projection's existing dedup key is the event ULID."
    - "A forced rollback of the mutation transaction leaves zero outbox rows and zero events_audit rows for that mutation."
    - statement: "The admin envelope payload is marshalled by the same encoder as every other world payload, so field ordering and escaping match the declared taxonomy contract rather than a bespoke shape."
      verification: backstop
    # ── Taxonomy ratchet — EIGHT places, not six (cross-AI review R-29) ──
    - "The payload widening is applied in all EIGHT places at once: the lifecycle payload struct, its builder, both lifecycle call sites, the taxonomy characterLifecyclePayload field list, both lifecycle kinds' SchemaVersion 1→2, the characterProfilePayload field list (section + action), KindCharacterProfileUpdate's SchemaVersion 1→2, and AppSchemaVersion 3→4. Reverting ANY of the three SchemaVersion bumps or the AppSchemaVersion bump alone fails a taxonomy test."
    - "The admin profile write reuses KindCharacterProfileUpdate rather than minting a distinct admin kind, so no census, no command→kind parity row and no service kind-list entry changes. The player path emits empty section and action on the same widened payload, which is correct: the envelope Actor already distinguishes who acted, and one kind for `a character's profile changed` is the better audit model than two."
    # ── The world-layer authorization gate (cross-AI review R-27) ──
    - "A new seed policy `seed:admin-character-administration` — `permit(principal is player, action in [\"read\", \"write\", \"retire\", \"unretire\"], resource is character) when { \"admin\" in principal.player.roles };` — is what lets the D-104 player-flavoured admin caller pass world.Service.checkAccess. Without it the admin passes the section interceptor and is then DEFAULT-DENIED inside the world service, because seed:admin-full-access requires `principal is character` (seed.go:105-107) and seed:admin-section-access is scoped `resource is admin_section` (seed.go:985-987)."
    - "`delete` is DELIBERATELY absent from that policy's action list. A player-principal admin is denied `delete` on a character resource by the engine, so world.Service.DeleteCharacter stays unreachable from the admin boundary at the POLICY layer as well as at the RPC layer. A test asserts both: the compiled policy's action list has exactly four members, and a real engine denies a player-admin subject `delete` on a character."
    # ── The description / profile.* write, under ONE version (cross-AI review R-28) ──
    - "One AdminUpdateCharacter request is ONE domain write, ONE version bump, ONE transaction and ONE envelope, even when its mask names `description` alongside `profile.*` paths. It is served by the EXISTING world.Service.UpdateCharacterProfileAttributes extended with an optional description channel — never by calling UpdateCharacterDescription as a second write, which would consume the single expected_version twice and could partially commit."
    - "Making that method variadic changes an INTERFACE, not just a function: `characterAccessWorldMutator` (internal/grpc/characteraccess_service.go:65) declares the exact non-variadic signature, so `*world.Service` stops satisfying it unless the interface changes in the same commit. That file is in files_modified, its method is updated to match, and its two call sites (characteraccess_create.go:205, characteraccess_write.go:347) are asserted to pass ZERO options — the facade's deliberate narrowness is preserved by assertion rather than by the signature alone."
    - "A description-only mask (update_mask naming ONLY `description`) applies the description and bumps the version by exactly one. The three attribute partitions are empty in that case, and the shipped method returns early on all-empty partitions (`service.go:1228-1236`), so the description write MUST be placed where a supplied-and-differing WithDescription defeats that early return — otherwise the one path the whole combined-write fix exists to serve silently does nothing."
    - "The admin description write MUST NOT route through UpdateCharacterDescription for a second, independent reason: that method emits kindCharacterUpdated, whose declared payload carries a `description` STRING field (taxonomy.go:189-192) — the prose VALUE. Routing an admin description edit through it would write player-authored prose into the retained events_audit, which is exactly what D-103 forbids and what this plan's own INV-PRIVACY registration asserts cannot happen."
    - "`description` appears in the emitted payload only as the NAME `description` inside the sorted changed_attributes list; its value reaches no payload field."
  artifacts:
    - path: "internal/grpc/admin_characters_write.go"
      provides: "AdminUpdateCharacter / AdminRetireCharacter / AdminUnretireCharacter with the 13-path allowlist"
      contains: "expectedVersion"
    - path: "internal/world/payloads.go"
      provides: "CharacterLifecycleChangePayload.BeforeStatus and the admin section/action fields"
      contains: "BeforeStatus"
    - path: "internal/world/outbox/taxonomy.go"
      provides: "widened characterLifecyclePayload, SchemaVersion 2, AppSchemaVersion 4"
      contains: "AppSchemaVersion = 4"
  key_links:
    - from: "internal/grpc/admin_characters_write.go"
      to: "internal/world/service.go"
      via: "the admin handlers call the SAME world.Service lifecycle methods the player path calls"
      pattern: "(Retire|Unretire)Character"
    - from: "internal/world/service.go"
      to: "internal/world/outbox/taxonomy.go"
      via: "buildIntent emits a declared kind; an undeclared kind is WORLD_TAXONOMY_UNKNOWN_KIND"
      pattern: "buildIntent"

  prohibitions:
    - requirement_id: ADMIN-06
      category: privacy
      status: resolved
      verification: judgment
      statement: "The admin audit trail MUST NOT accumulate player-authored profile prose, nor a durable player-to-character-alt linkage, in the retained events_audit table — an audit record must not become a permanent copy of the content it describes, nor a map of which characters a player plays. Widening the payload later is additive and cheap; narrowing it is not, because prose already written into retained partitions cannot be recalled."
---

<objective>
Give the `characters` admin section its write surface, and make every admin mutation audited in
the same transaction as its state change — with before-values scoped away from player prose.

Purpose: ADMIN-06 as written says "recording before-values", and read to the letter that would
copy player-authored profile prose into `events_audit`, a **retained** table, undoing the
erasure-safe property `BuildCharacterProfileUpdatePayload` was designed with
(`payloads.go:445-466` — designed with it in its doc comment, but **pinned by no test**: this plan
authors the first one, Task 1 Test 4). D-103 splits before-values by field
kind instead: changed attribute **names** for prose, a real before-`status` for lifecycle
transitions, because a lifecycle enum is not player-authored content. An auditor still learns
who, when, which character, which fields, and for a lifecycle transition what the value was.

Per **D-105**, emission goes through the transactional outbox seam in the same transaction as
the state change (§9.3, INV-WORLD-1), reusing the shape `UpdateCharacterDescription` already
uses; the `events_audit` row is **projected** from that envelope by the asynchronous audit
projection, which stays the only writer to that table. There is **no** direct
`INSERT INTO events_audit` — §14 row 9 is explicit that no transactional write path into that
table exists and that building one would bypass the codec / `dek_ref` / dedup contract
`writeAuditRow` maintains. Per **D-104** the envelope Actor is `player:<id>` and the acting
character is omitted. Per **D-103** before-values are split by field kind.

Output: three write RPCs with the 13-path allowlist and the `expected_version` guard; the
eight-step taxonomy widening carrying `before_status`, `section` and `action`; the seed policy
that lets the player-flavoured admin caller pass the world-layer `checkAccess`; and the
integration proof that the envelope is transactional while the `events_audit` row is projected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-portal-shell-character-administration/06-CONTEXT.md
@.planning/phases/06-admin-portal-shell-character-administration/06-RESEARCH.md
@.planning/phases/06-admin-portal-shell-character-administration/06-PATTERNS.md
@.claude/rules/grpc-errors.md
@.claude/rules/event-conventions.md
@.claude/rules/invariants.md
</context>

<reversibility_gate_note>
`verify plan-structure` emits one warning on this plan: Task 1 is rated `one-way` with no
preceding `checkpoint:decision`. That is **deliberate and correct**, not an oversight.
`gsd-core/references/planner-reversibility.md` names "gating a decision already made" as an
anti-pattern: `06-CONTEXT.md` D-103 records the maintainer choosing this exact payload shape,
including that encrypting the prose was **considered and rejected** (retained-is-still-retained;
encryption is not erasure; crypto-shredding is a separate mechanism this milestone does not
ship). The door is already walked through. The rating stays for the record so a later phase can
see which door this was; re-asking it here would be checkpoint fatigue on a settled decision.
</reversibility_gate_note>

<!-- planner-discipline-allow: roles, DeleteCharacter, glob, SchemaVersion: 1, %v, UpdateCharacterDescription, delete, permitCount -->
<!-- Each literal above is negative-grepped by an acceptance criterion AND named in an action
     body, because the action must tell the executor which field, method, matching style or
     stale constant is forbidden. The greps target Go source files, not this plan. -->

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: The eight-step payload widening — before-status, section, action</name>
  <reversibility rating="one-way">Once admin envelopes with this payload shape are projected into `events_audit`, the rows sit in retained partitions and cannot be recalled. Widening later is additive; NARROWING is not. D-103 already records the human choosing this shape, so no checkpoint re-asks it — the rating is here for the record.</reversibility>
  <files>internal/world/payloads.go, internal/world/payloads_test.go, internal/world/service.go, internal/world/outbox/taxonomy.go, internal/world/outbox/taxonomy_test.go</files>
  <read_first>
    - `internal/world/payloads.go:314-317` (`CharacterLifecycleChangePayload` — today
      `CharacterID` + `Status`, **no** before-status), `:434-443`
      (`BuildCharacterLifecyclePayload`), `:445-466`
      (`BuildCharacterProfileUpdatePayload` and its doc comment at `:449-453`: *"The VALUES are
      deliberately absent. Profile prose is player-authored personal content and the taxonomy's
      payload rule is new-values-only AND erasure-safe"*).
    - `internal/world/payloads_test.go:964-978` — `TestBuildLocationPayload_NewValuesOnly`. Read it
      as the **shape** to follow (unmarshal, assert the fields present, `assert.Len` on the map),
      but note it is a **LOCATION** test: it asserts `got["description"] == "a bright hall"` and
      `Len == 3`. It pins nothing about character profiles.
    - **There is NO existing test for `BuildCharacterProfileUpdatePayload`.** Verified against the
      tree: `rg 'BuildCharacterProfileUpdatePayload' -g '*_test.go'` returns **zero** hits. The
      erasure-safe new-values-only property the whole D-103 design rests on is therefore currently
      **untested**. Task 1 AUTHORS that test; it does not extend one. Do not graft
      character-profile assertions onto `TestBuildLocationPayload_NewValuesOnly` — that test is
      about a different builder and a different struct.
    - `internal/world/outbox/taxonomy.go:22-29` (`AppSchemaVersion = 3` and its doc requiring a
      bump), `:143-145` (the two lifecycle kind rows at `SchemaVersion: 1`), `:200-204`
      (`characterLifecyclePayload`), `:221-235` (`Lookup`/`IsDeclared`/`Kinds` and
      `WORLD_TAXONOMY_UNKNOWN_KIND`).
    - `internal/world/service.go:1308-1375` (`RetireCharacter` — the read at `:1325` whose
      comment at `:1324` says *"Version arms the precheck, Status arms the lifecycle guard"*,
      the switch at `:1345`, the payload build at `:1365`, the `buildIntent → mutator` commit
      at the end) and the `UnretireCharacter` equivalent from `:1408`.
    - `internal/world/service.go:52-58` (character envelope kinds) and
      `internal/world/mutator.go:100-106` (the command→kind parity table).
    - `internal/world/caller.go:42-45` — *"its byte identity is an audit requirement, not only
      an authz one"*, which is why the payload need not duplicate the player id.
    - `internal/world/postgres/character_repo.go:502-553` — `SetStatus` uses
      `RETURNING version` only and **cannot** yield the old status; that is fine because the
      service layer already holds it. Do NOT add a repository round-trip.
    - `test/meta/world_envelope_census_test.go` — the existing envelope census, so the widening
      is made in a way that census already covers rather than duplicated.
  </read_first>
  <behavior>
    RED first:
    - Test 1: `BuildCharacterLifecyclePayload` with before `active` and after `retired` marshals
      a payload whose `before_status` is `active` and whose `status` is `retired`, and the two
      **differ**. An assertion that a status is merely present passes under today's shape.
    - Test 2: the taxonomy declares `before_status`, `section` and `action` on
      `characterLifecyclePayload`, and `Lookup(KindCharacterRetired).SchemaVersion == 2`.
    - Test 3: `AppSchemaVersion == 4`, and `Lookup(KindCharacterProfileUpdate).SchemaVersion == 2`
      with `section` and `action` declared on `characterProfilePayload`.
    - Test 3b: `RetireCharacter` called with **no** options produces a payload whose `section`
      and `action` are empty — the player path is unchanged by the widening.
    - Test 4 (**new test, authored here — SHAPE CORRECTED, see C2-31**):
      `TestCharacterProfileUpdatePayloadExposesNoValueBearingField` in
      `internal/world/payloads_test.go`. This is a **structural** assertion, because the builder's
      signature is `BuildCharacterProfileUpdatePayload(characterID, changed []string, auditCtx)`
      and it **takes no values at all** — so a test that declares local `oldBiography` /
      `newBiography` sentinels and asserts they are absent from the marshalled bytes is vacuous by
      construction: those strings never enter the builder, and the assertion passes identically
      whether or not the payload could carry values. That was the previous revision's shape and it
      proved nothing.

      Instead: call the builder with two changed attributes in NON-sorted order, marshal, and
      unmarshal into `map[string]any`. Assert the key set is **exactly**
      `{character_id, changed_attributes, section, action}` — `require.Len` on the map plus
      `require.Contains` per key — so a value-bearing field added later fails immediately.
      Assert `changed_attributes` unmarshals to the **sorted** slice and that each element is a
      bare attribute NAME. Then reflect over `CharacterProfileUpdateChangePayload`'s struct
      fields and assert the same four `json` tags and no others; `require.NotEmpty` on the
      reflected field set so a reflection call returning nothing cannot pass vacuously.

      Prove it RED: temporarily add `Values map[string]string \`json:"values"\`` to the struct,
      run, observe both the map-key and the reflected-field assertions fail, revert. Record it.

      **The value-absence property is proven end-to-end by Test 6b instead**, where real old and
      new prose actually flows through `AdminUpdateCharacter` into an emitted envelope and the
      serialized bytes are inspected with `require.NotContains`. That test can carry the sentinel
      values because that path actually receives them; this one cannot, so it asserts the schema
      property that makes the leak impossible rather than performing a search that must succeed.
    - Test 5 (**relocated, see C2-25**): the zero-changed refusal is an **admin-handler** precheck
      and is tested in `internal/grpc/admin_characters_write_test.go`, not here. In THIS file,
      assert the opposite for the shared builder: `BuildCharacterProfileUpdatePayload(id, nil)`
      and `(id, []string{})` both **succeed**, marshalling `changed_attributes` as an empty list.
      That is the shipped player behaviour on an all-identical resubmit, documented at
      `service.go:1115-1134` as "both representable and honest", and a builder that errored on it
      would turn a live player success into a failure.
    - Test 6: `RetireCharacter` on an already-`retired` character is refused by the lifecycle
      guard and emits nothing — so before and after can never be equal in an emitted payload.
  </behavior>
  <action>
Apply the widening in **all eight places at once** — the taxonomy ratchet is all-or-none. (An
earlier revision said six and enumerated only the lifecycle half; the profile-update half needs
its own two steps or a partial widening there stays green — cross-AI review R-29.)

1. `internal/world/payloads.go:314-317` — add `BeforeStatus string \`json:"before_status"\`` to
   `CharacterLifecycleChangePayload`, plus `Section string \`json:"section"\`` and
   `Action string \`json:"action"\`` carrying the evaluated admin section and action (§10.7).
   For a player-initiated transition, `Section` and `Action` are empty; document that.
2. `internal/world/payloads.go:434-443` — `BuildCharacterLifecyclePayload` gains a
   `beforeStatus Status` parameter and an admin-context parameter (a small **exported**
   `AuditContext{Section, Action string}` value, zero for the player path). Refuse to build
   when `beforeStatus == status` — the guard upstream should already have refused, and a
   builder that would emit an equal-valued transition is a bug the builder can catch.
3. `internal/world/service.go:1365` and the `UnretireCharacter` equivalent (`:1461`) — pass
   `char.Status` read at step (2). It is already live at the payload-build site; no repository
   change and no second read.

   **The typed API by which the admin handler supplies that context** (cross-AI review R-30 — the
   plan previously said the shared methods "gain an admin audit context" without saying how):
   `RetireCharacter` and `UnretireCharacter` gain a trailing **variadic option**
   `opts ...LifecycleOption`, with one option `WithAuditContext(AuditContext) LifecycleOption`.
   Variadic means every existing caller compiles unchanged and the player path supplies nothing,
   yielding the zero `AuditContext` and empty `section`/`action` — so no non-admin caller is
   touched, `Caller` is not overloaded with an admin concern, and the public signature does not
   grow a parameter every caller must reason about. Census impact: none. The envelope census
   (`test/meta/world_envelope_census_test.go`) covers the payload shape, which the widening
   changes and which step 4 declares; no census asserts world-method signatures.
4. `internal/world/outbox/taxonomy.go:200-204` — extend `characterLifecyclePayload` with
   `{Name: "before_status", Type: "string"}`, `{Name: "section", Type: "string"}`,
   `{Name: "action", Type: "string"}`.
5. `internal/world/outbox/taxonomy.go:143-145` — both lifecycle kind rows move
   `SchemaVersion: 1` → `2`.
6. `internal/world/outbox/taxonomy.go:207-213` — extend `characterProfilePayload` with
   `{Name: "section", Type: "string"}` and `{Name: "action", Type: "string"}`.
7. `internal/world/outbox/taxonomy.go:146` — `KindCharacterProfileUpdate` moves
   `SchemaVersion: 1` → `2`.
8. `internal/world/outbox/taxonomy.go:29` — `AppSchemaVersion` 3 → 4, required by the
   constant's own doc comment at `:22-24`.

**Reuse `KindCharacterProfileUpdate`; do not mint an admin kind.** The choice is settled here
rather than deferred to a "decide by reading" hedge (cross-AI review R-29). Reuse means no
census row (`test/meta/world_envelope_census_test.go`), no command→kind parity row
(`mutator.go:100-106`) and no service kind-list entry (`service.go:52-58`) changes — a distinct
kind would force all three, none of which is in `files_modified`. The player path then emits
**empty** `section` and `action` on the widened payload, which is correct and must be documented
as such: the envelope `Actor` already distinguishes who acted, and one kind for "a character's
profile changed" is a better audit model than two kinds split by actor flavour.

`BuildCharacterProfileUpdatePayload` gains the same `AuditContext` parameter and keeps its
existing shape otherwise: `{character_id, changed_attributes []string (sorted), section, action}`
and nothing else. Its doc comment keeps D-103's reasoning in the terms the next reader needs —
the values are absent because `events_audit` is retained, encryption was considered and rejected
(retained-is-still-retained, encryption is not erasure, and crypto-shredding is a separate
mechanism this milestone does not ship), and an auditor still learns who, when, which character
and which fields — and gains one sentence saying `description` appears in
`changed_attributes` as a **name** when an admin edits it, and that its value reaches no field.

The **Actor** is untouched: `Caller.subject` already carries `player:<id>` verbatim into the
envelope, so the payload records no player id and no acting character (D-104). Do not add
either.
  </action>
  <verify>
    <automated>task test -- ./internal/world/... ./test/meta/ && task test:int -- ./test/integration/...</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'AppSchemaVersion = 4' internal/world/outbox/taxonomy.go` matches; `rg -n 'AppSchemaVersion = 3'` returns zero matches.
    - `rg -n 'SchemaVersion: 1' internal/world/outbox/taxonomy.go` no longer matches either lifecycle kind row **nor** the `KindCharacterProfileUpdate` row.
    - Reverting **each** of the three `SchemaVersion` bumps individually, and the `AppSchemaVersion` bump, makes a taxonomy test FAIL — four demonstrations, recorded. The all-or-none claim is only true if each step is individually load-bearing; an earlier six-step enumeration left the profile-update half able to drift green.
    - `git diff internal/world/outbox/taxonomy.go` shows **no** new `Kind*` constant and no new entry in the kind list — the admin profile write reuses `KindCharacterProfileUpdate`. `git diff --exit-code internal/world/mutator.go test/meta/world_envelope_census_test.go` shows no parity-table or census row was added.
    - `rg -n 'WithAuditContext|LifecycleOption' internal/world/service.go` matches, and `git diff internal/world/service.go` shows `RetireCharacter`/`UnretireCharacter` gained only a trailing variadic parameter — every existing caller compiles unmodified. A test calls each with no options and asserts empty `section`/`action`.
    - The before-status test asserts `require.NotEqual(t, payload.BeforeStatus, payload.Status)` on a real active→retired transition; an assertion of mere presence would pass under the pre-widening shape and is insufficient.
    - `task test -- ./test/meta/ -run TestWorldEnvelopeCensus` passes with the widened payload — the existing census covers it, and no sibling census was added.
    - `rg -n 'profile\.' internal/world/payloads.go` shows no `profile.*` VALUE reaching any payload struct field. **The `description|pronouns` arms are deliberately dropped from this grep** (cross-AI review C2-27): `CharacterUpdateChangePayload.Description` (`payloads.go:300-305`) is a real, legitimately shipped prose-carrying field on the `character_updated` payload, which this plan leaves in place and merely refuses to route admin edits through. A grep that flags it invites an executor to "fix" the wrong thing. The property that admin edits carry no prose is proven by Task 1 Test 4 (marshalled-bytes `NotContains`) and Test 6b, not by this grep.
    - Reverting step 6 alone (leaving `AppSchemaVersion` at 3) makes a taxonomy test FAIL — demonstrate and record.
    - `task test -- ./internal/world/ -run TestCharacterProfileUpdatePayloadExposesNoValueBearingField -v` output contains the line `--- PASS: TestCharacterProfileUpdatePayloadExposesNoValueBearingField`. **Asserting the exit code alone is insufficient**: `-run` matching zero tests exits 0, so a criterion phrased as "the run passes" is satisfied identically whether or not the test was ever written. The `--- PASS:` line is what proves it ran.
    - That test asserts the payload's **key set** and the struct's **reflected `json` tags** are exactly `{character_id, changed_attributes, section, action}`, with `require.Len` and `require.NotEmpty` on both sides. Temporarily adding `Values map[string]string` to `CharacterProfileUpdateChangePayload` makes it FAIL; demonstrate and record. **It does NOT declare local old/new sentinel strings and assert their absence** (cross-AI review C2-31): the builder's parameters are `(characterID, changed []string, auditCtx)` and carry no values, so such an assertion would pass identically under a builder that leaked everything. The value-absence property is carried by Test 6b, at the layer where values actually flow.
    - `TestBuildLocationPayload_NewValuesOnly` is **unmodified** — `git diff internal/world/payloads_test.go` shows added functions only, no edits inside that test body.
  </acceptance_criteria>
  <done>The lifecycle payload carries a differing before-status plus the evaluated section and action; the profile payload carries names plus section and action and still no values; the admin context reaches the lifecycle methods through a typed variadic option that leaves every player caller untouched; and all eight ratchet steps landed together, each demonstrated individually load-bearing.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Three write RPCs — the 13-path allowlist, the version guard, the same lifecycle</name>
  <files>api/proto/holomush/adminportal/v1/adminportal.proto, api/proto/holomush/web/v1/web.proto, internal/access/policy/seed.go, internal/access/policy/seed_test.go, internal/access/policy/seed_profile_visibility_test.go, internal/admin/section/descriptor.go, internal/grpc/admin_service.go, internal/grpc/admin_characters_write.go, internal/grpc/admin_characters_write_test.go, internal/grpc/characteraccess_service.go, internal/grpc/characteraccess_write_test.go, internal/web/admin_handlers.go, internal/world/service.go, internal/world/service_test.go, cmd/holomush/sub_grpc.go, internal/testsupport/integrationtest/harness.go, test/meta/admin_character_message_role_fence_test.go, site/src/content/docs/reference/grpc-api.md</files>
  <read_first>
    - `internal/grpc/characteraccess_write.go:108-119` (the `profileMaskField` doc block and struct,
      accessor ON the entry so each path name is written exactly once), `:121-141` (the
      `updateCharacterProfileMaskablePaths` doc block: exact-string map lookup, no
      prefix/wildcard/glob, an unlisted path is **rejected** not ignored; and the byte caps —
      `world.MaxNameLength` for the seven short fields, `world.MaxDescriptionLength` for the five
      long ones, both measured in BYTES), `:142-179` (the twelve entries themselves — note that
      doc block says *"each of the **twelve** §7.2 names"*: this sibling does NOT carry
      `description`, which is why the admin allowlist is 13 and not 12),
      `:196-205` (internal-propagates-verbatim so an outage is not laundered into an
      authorization answer), `:212-226` (`requireGuardedVersion` — `expectedVersion > 0` or
      refuse).
    - `internal/world/service.go:1308` (`RetireCharacter`, `checkAccess` action `"retire"` at
      `:1321-1322`) and `:1408` (`UnretireCharacter`, action `"unretire"` at `:1421`) — the
      **canonical** lifecycle methods. **Phrasing correction** (cross-AI review R-31): these are
      not "the methods the player path calls", because
      `rg '\.RetireCharacter\(' -g '!*_test.go'` finds **zero** production callers — player
      self-retire is deferred (IDENT-04). They are the one lifecycle path, which is why reusing
      them keeps admin and future player transitions from diverging. Do not go looking for a
      player caller that does not exist.
    - `internal/world/service.go:1090-1091` — `UpdateCharacterProfileAttributes`'s own
      `checkAccess(caller, "write", character:<id>)`, and `:1074-1082` where it **rejects**
      `description` with `CHARACTER_PROFILE_ATTRIBUTE_UNKNOWN` because its name set is the closed
      §7.2 twelve.
    - `internal/world/service.go:1054-1060` — its signature
      `(ctx, caller, characterID, expectedVersion, attributes map[string]string)`. It is
      extended, not replaced.
    - `internal/access/policy/seed.go:105-107` — `seed:admin-full-access`,
      `permit(principal is character, action, resource) when { "admin" in principal.character.roles }`.
      It requires a **character** principal and therefore never fires for the D-104
      player-flavoured admin caller.
    - `internal/access/policy/seed.go:985-987` — `seed:admin-section-access`, the only
      player-principal policy, scoped `resource is admin_section`. It does not reach a
      `character:` resource.
    - `internal/world/outbox/taxonomy.go:189-192` — `characterUpdatePayload`, the payload
      `kindCharacterUpdated` declares. **It carries `{Name: "description", Type: "string"}` — the
      prose VALUE.** This is why the admin description write must not route through
      `UpdateCharacterDescription`.
    - `internal/grpc/characteraccess_write.go:315-336` — the player path treats an **empty mask**
      as a no-op success returning current state, with a doc comment warning not to "fix" the
      inconsistency. The admin path deliberately diverges (`InvalidArgument`); see the action.
    - `internal/world/service.go:816-828` — `buildIntent` → `mutator.*`, the one emission seam
      (INV-WORLD-1).
    - `internal/grpc/admin_sections.go` — `mapAdminSectionError` and the static-message consts.
    - `01-SPEC.md` §9.4–§9.5 — `expected_version` as an `int32` scalar on every guarded
      mutation (absent or zero is **rejected**, never "skip the guard") and the four
      update-mask rules; §10.6 — the 13-path allowlist and the four exclusions.
    - `06.1-UI-SPEC.md` (in the Phase 06.1 directory) → "The edit Sheet" and "The mutation loop"
      — the client contract these RPCs must satisfy (response must carry the NEW version so the
      row can update in place without a refetch). Read as a **wire-shape constraint**; the Sheet
      and the loop themselves are Phase 06.1's (`06.1-04`), a downstream consumer in a later
      phase.
  </read_first>
  <behavior>
    RED first:
    - Test 1: `AdminUpdateCharacter` with `update_mask` naming one of the 13 paths applies it and
      returns the new `version`.
    - Test 2: `update_mask` naming a path outside the 13 — including `roles`, `name`, `status`
      and `version` — returns `codes.InvalidArgument`; the field is **not** silently ignored.
    - Test 3 (**contract corrected, C2-06**): an **empty** `update_mask` returns **success**
      carrying the character's current state and current version, mutates nothing, bumps no
      version and emits **zero** envelopes — per §9.5 rule 4 (`01-SPEC.md:2197`, restated for
      admin character editing at `:2534`). Assert all four: the OK status, the returned version
      equal to the pre-call version, a subsequent read showing every field unchanged, and an
      outbox row count of zero for the request. A handler returning `InvalidArgument` fails the
      first; a handler that skipped the checks and returned early fails Test 3b.
    - Test 3b: an empty `update_mask` with a **stale** `expected_version` still returns
      `codes.Aborted`, and an empty mask for a **nonexistent** character still returns
      `codes.NotFound` — the no-op answer comes after the guards, not instead of them.
    - Test 4: `expected_version` absent or `0` returns `codes.InvalidArgument`, never a
      guard-free write.
    - Test 5: a stale `expected_version` returns `codes.Aborted` with oops
      `WORLD_CONCURRENT_EDIT`, and a subsequent read shows the row unchanged.
    - Test 6 (**two caps, not one**): the short fields cap at `world.MaxNameLength` = **100**
      and the long fields at `world.MaxDescriptionLength` = **4000**
      (`internal/world/validation.go:20-21`). For a short field assert 99 accepted, 100 accepted,
      101 rejected; for a long field assert 3999 accepted, 4000 accepted, 4001 rejected. A CJK
      value crossing **each** respective cap by UTF-8 byte length while its rune count does not
      is rejected at the same boundary. Asking a 4000-byte field to reject 101 bytes would fail
      against correct server behaviour (cross-AI review R-33).
    - Test 6b (**the mixed mask — the case that previously had no implementation**): a mask
      naming `description` AND `profile.biography` in one request, with one `expected_version`,
      succeeds; a subsequent read shows **both** values changed and `version` incremented by
      exactly **one**; and exactly **one** envelope was emitted, of kind
      `KindCharacterProfileUpdate`, whose sorted `changed_attributes` is
      `["description", "profile.biography"]` and whose bytes contain **neither** value.
      Implementing this as two domain calls fails on the version increment, the envelope count,
      and the prose-absence assertion at once.
    - Test 6c: a `world.HumanCaller(access.PlayerSubject(adminPlayerID))` is PERMITTED `write`,
      `retire` and `unretire` on a `character:` resource by a real engine over the full seed
      corpus, and is **DENIED** `delete`. Removing `seed:admin-character-administration` makes
      the first three FAIL; adding `"delete"` to its action list makes the fourth FAIL.
      Demonstrate all four and record.
    - Test 7: `AdminRetireCharacter` moves an active character to `retired` through
      `world.Service.RetireCharacter`; a second call is refused and emits nothing.
    - Test 8: `AdminUnretireCharacter` returns a retired character to `active` through
      `world.Service.UnretireCharacter`.
    - Test 9: each of the three, called by a non-admin over the wire, is denied with top-level
      `DENY_ADMIN_SECTION` and a paired admin positive control.
  </behavior>
  <action>
Add three RPCs to `holomush.adminportal.v1.AdminPortalService`: `AdminUpdateCharacter`,
`AdminRetireCharacter`, `AdminUnretireCharacter`. Each request carries
`string character_id`, `string player_session_token` and `int32 expected_version`, all with
`buf.validate` minimums. `AdminUpdateCharacterRequest` additionally carries the 13 optional
profile fields plus `google.protobuf.FieldMask update_mask`. Each response carries the updated
`AdminCharacter` **including the new version**, because D-110 requires the row to update in
place from the response rather than refetching.

There is **no** `AdminDeleteCharacter`. `world.Service.DeleteCharacter` MUST NOT be reachable
from any admin RPC (§4.4, §10.6). There is no rename in v0.13 (D-44).

Add three `AdminDescriptors` entries, each `{SectionID: "characters", Action: ActionWrite}`.
The write action is what makes the ABAC decision differ from the read RPCs' — do not reuse
`ActionRead`.

`internal/grpc/admin_characters_write.go`:
- Copy `characteraccess_write.go:143-179`'s **accessor-on-the-entry** map shape for the allowlist so
  each path name is written exactly once and drift between the list and the reader is impossible.
  Restate its `:123-137` doc block: exact-string map lookup; no prefix, wildcard or glob; an
  unlisted path is rejected, not ignored (§9.5 rule 2). Role mutation is out of scope (PORTAL-08,
  #4899).
- **The allowlist is 13 keys and its shape is: `description` PLUS twelve `profile.*` paths.**
  §10.6 (`01-SPEC.md:2546-2560`) lists them in order: `description`, then `profile.pronouns`,
  `profile.concept`, `profile.species`, `profile.age`, `profile.faction`, `profile.appearance`,
  `profile.personality`, `profile.biography`, `profile.rumors`, `profile.currently`,
  `profile.rp_preferences`, `profile.timezone`. `description` is the in-world description and it is
  easy to lose because every *other* entry shares the `profile.` prefix — the shipped sibling
  `updateCharacterProfileMaskablePaths` carries only the twelve (`rg -c '"profile\.'
  internal/grpc/characteraccess_write.go` = 12), because that RPC does not own `description`.
  Dropping `description` here is a real ADMIN-04 scope loss, not a cosmetic difference. **Never
  assert this list by counting a `profile.` prefix** — 13 paths yield 12 such matches, so a count
  of 13 is unsatisfiable by a correct implementation and the only ways to "fix" it are to invent a
  thirteenth `profile.*` path or to delete `description`. Assert set equality instead (below).
- Copy `requireGuardedVersion` (`:212-226`) verbatim in shape for all three mutations.
- Enforce the byte caps with `len()` over the UTF-8 encoding, matching
  `characteraccess_write.go:119-153` exactly, so the admin Sheet's counter and the server agree.
- Retire and un-retire call `world.Service.RetireCharacter` / `UnretireCharacter` — the
  **canonical** lifecycle methods, so admin and any future player-initiated transition cannot
  diverge (ADMIN-05). Pass the evaluated `section` and `action` through
  `world.WithAuditContext(world.AuditContext{Section: "characters", Action: "write"})`.
- **`AdminUpdateCharacter` is ONE domain write, even for a mixed mask.** Call the **existing**
  `world.Service.UpdateCharacterProfileAttributes`, extended in this task with two variadic
  options: `world.WithDescription(string)` and `world.WithAuditContext(AuditContext)`. See the
  world-service edits below. Do **not** call `UpdateCharacterDescription` as a second write.
- **An empty mask is a NO-OP SUCCESS** (cross-AI review C2-06). The normative source says so
  twice — `01-SPEC.md:2197` (§9.5 rule 4: *"A request with no paths changes nothing and returns
  success"*) and `:2534`, which applies §9.5's four rules to admin character editing by name. A
  previous revision specified `InvalidArgument` and "documented the divergence" in a handler
  comment; **a doc comment does not amend a normative spec**, and unlike ADMIN-02 and ADMIN-06
  this was never on the phase's list of user-approved divergences. It is withdrawn.

  The behaviour: evaluate authorization, existence and the `expected_version` guard **first** —
  an empty mask is not a way to skip a check — then return success carrying the character's
  current state and current version. No envelope, no version bump, no audit row.
  `AdminUpdateCharacterResponse` carries the updated `AdminCharacter` including its version, so it
  has the channel `service.go:1218-1227` says the world **method** lacks (that method refuses
  because it returns only an error and cannot hand the version back). Cite that comment in the
  handler's doc block.

  **One clause DIVERGES from the player facade, and the plan says so rather than claiming
  parity** (cross-AI review C3-27). An earlier revision asserted *"There is no divergence"* and
  *"the admin path matches the PLAYER path here … for the same stated reason"*. That is false on
  a **stale** `expected_version`: `characteraccess_write.go:315-336` answers a stale empty-mask
  caller with **success** — its own comment reads *"A STALE CALLER IS ANSWERED, NOT REFUSED —
  deliberately"*, and the code returns `projectOwner(char, profile)` without ever comparing
  `expectedVersion` — while Test 3b requires the admin path to return `Aborted`. Both reviewers
  caught the contradiction independently, from opposite sides.

  **Keep the `Aborted`, drop the parity claim.** The facade's reasoning does not transfer, because
  the facade's caller is editing their **own** character and the worst case is one wasted round
  trip. The admin caller is editing **someone else's** character on an audited surface, and a
  stale version guard means the request was composed against a view of that character that has
  since changed — possibly by the owner, possibly by another operator. Refusing is what makes the
  operator re-read before acting on a stale picture. Write **that** into the handler's doc block —
  the divergence, its one clause, and this reason — rather than a claim of parity the shipped
  facade contradicts.

**`internal/world/service.go` — extend the profile write, do not build a parallel one**
(cross-AI review R-28). The 13-path mask is `description` plus twelve `profile.*` paths, but
`UpdateCharacterProfileAttributes` **rejects** `description` (`:1074-1082`) and `description` has
its own domain method. Two writes would each bump `characters.version` and each consume the
caller's single `expected_version`, so the second fails its precheck, one RPC would emit two
envelopes in two transactions, and a partial commit is reachable. Worse, `UpdateCharacterDescription`
emits `kindCharacterUpdated`, whose declared payload carries a `description` **string** field
(`taxonomy.go:189-192`) — routing an admin description edit through it writes player-authored
prose into the retained `events_audit`, which is precisely what D-103 forbids and what this
plan's own `INV-PRIVACY` registration asserts cannot happen.

So:

- Change the signature to
  `UpdateCharacterProfileAttributes(ctx, caller, characterID, expectedVersion, attributes map[string]string, opts ...ProfileUpdateOption)`.
  Variadic ⇒ every existing **call site** compiles unchanged and behaves identically.

  **But a variadic tail changes the METHOD SET, so an interface that declares the old signature
  stops being satisfied** (cross-AI review C2-05). `internal/grpc/characteraccess_service.go:65`
  declares `characterAccessWorldMutator` with the exact non-variadic
  `UpdateCharacterProfileAttributes(ctx context.Context, caller world.Caller, characterID ulid.ULID, expectedVersion int, attributes map[string]string) error`,
  and `*world.Service` is assigned to it. Leaving that alone is a **compile break**, not a
  runtime surprise, and the file was absent from `files_modified` in every prior revision.

  So: **edit `characteraccess_service.go:65` in the same commit**, adding the same
  `opts ...world.ProfileUpdateOption` tail to the interface method. Keep the interface's doc
  block (`:44-63`) and its "one interface per capability" reasoning **verbatim** — the change is
  a signature, not a widening. Then preserve the narrowness the doc block is about by
  **assertion**: `characteraccess_create.go:205` and `characteraccess_write.go:347` pass **zero**
  options, and an acceptance criterion greps them to prove it. The facade names the two commands
  and nothing beneath them; a variadic tail it never uses does not change that, and the grep is
  what keeps it true.

  **And edit the EXISTING TEST DOUBLE in the same commit, because the interface has an
  implementor the previous revision missed** (cross-AI review C3-25). `recordingWorldMutator`
  (`internal/grpc/characteraccess_write_test.go:87`) declares the **non-variadic**
  `UpdateCharacterProfileAttributes(_ context.Context, caller world.Caller, characterID ulid.ULID, expectedVersion int, attributes map[string]string) error`
  and is assigned to `characterAccessWorldMutator` by every character-access write test. Changing
  only the production interface and `*world.Service` makes **every test using that double fail to
  compile** — a build break inside `internal/grpc`, not a runtime surprise. The C2-05 repair
  scheduled the interface and the implementation and stopped there; the file was in neither
  `files_modified` nor Task 2's `<files>`. Both now carry it. Add the same
  `opts ...world.ProfileUpdateOption` tail to the double's method and — the reason this is worth
  more than a mechanical signature edit — have the double **assert the facade supplies none**:
  `m.t.Helper(); require.Empty(m.t, opts, "character-access callers MUST pass no ProfileUpdateOption")`.
  That converts the two greps on the call sites into a runtime check on every character-access
  test that exercises the mutator, so the facade's narrowness is pinned by behaviour as well as by
  text. `task test -- ./internal/grpc/` inside this task is what surfaces it either way.

  Run `task test -- ./internal/grpc/` as part of this task, not only at the end: the
  character-access tests are the ones this signature change can break.
- `WithDescription(v string) ProfileUpdateOption` supplies the in-world description through a
  **separate channel**, so `description` never enters `attributes` and the closed §7.2 name-set
  validation at `:1074-1082` is untouched.
- `WithAuditContext(AuditContext) ProfileUpdateOption` supplies `section`/`action`.
- **`WithSkipUnchangedProperties() ProfileUpdateOption` is what actually delivers the admin
  no-empty-envelope guarantee** (cross-AI review C3-26). An earlier revision relocated that
  guarantee to an admin-handler precheck. That does not work, for two independent reasons:
  (a) the shipped method **rewrites an equal-valued row unconditionally** —
  `service.go:1112-1134` says so in its own comment (*"a resubmit of the value already stored
  DOES rewrite the row (it lands in `updates`, so the write, the CAS and the version bump are
  unconditional) but changes nothing"*) — so a non-empty mask naming only equal-valued
  `profile.*` fields bumps the version and emits an empty-`changed` envelope regardless of what
  the handler decided beforehand; and (b) a pre-transaction comparison reads rows it holds no
  lock on, so a concurrent write between the comparison and the transaction makes it wrong in
  both directions. The option moves the decision **inside** the transaction, where the rows are
  already read and the CAS already protects them: when supplied, a requested name whose stored
  value equals the requested value is dropped from the `updates` partition before the partitions
  are evaluated. If that empties all three and `!descriptionChanged`, the shipped early return at
  `:1228-1236` fires and nothing happens at all — no row rewrite, no version bump, no envelope.
  **The admin handler supplies it; the player facade supplies nothing**, so
  `characteraccess_write.go`'s unconditional-rewrite behaviour and its "both representable and
  honest" empty-`changed` envelope are byte-identical after this change. Do **not** make the skip
  unconditional: that would silently change a shipped player contract this plan has no
  authorization to change.
- **Place the description write relative to the empty-partition early return, explicitly**
  (cross-AI review C2-26). `service.go:1228-1236` returns `nil` when `creates`, `updates` and
  `deletes` are all empty, after only the version check. A description-only mask puts **nothing**
  in `attributes`, so all three partitions are empty, the early return fires, and the description
  is never written — silently, for exactly the mask the whole R-28 combined-write fix exists to
  serve. The rule:
  - Compute `descriptionChanged` — the option was supplied **and** its value differs from
    `char.Description` — **before** the early-return check.
  - The early return fires only when all three partitions are empty **and**
    `!descriptionChanged`. Its version-mismatch refusal (`CodeConcurrentEdit`) is unchanged and
    still runs first.
  - When `descriptionChanged` is true, execution continues into the transaction as if a partition
    were non-empty: the CAS, the single version bump, `"description"` in `changed`, and the one
    envelope all happen exactly once.
  - Option supplied but **equal** to the stored value ⇒ `descriptionChanged` is false ⇒ the
    documented no-op, same as any other unchanged field.
  Write those four sentences into the method's doc comment. An executor who has to improvise
  guard ordering on the phase's most sensitive method is one who will get it wrong quietly.
- Inside the method's existing transaction, after the existing attribute partition, write the
  description when `descriptionChanged` — reusing whatever repository primitive
  `UpdateCharacterDescription` already uses, in the SAME transaction, under the SAME single
  version bump. Read `UpdateCharacterDescription`'s body (`:919-975`) for that primitive; do not
  duplicate its authorization, its precheck or its intent build — this method already has all three.
- Include `"description"` in `changed` (and therefore in the sorted `changed_attributes` of the
  emitted payload) when and only when the description actually changed. Its **value** reaches no
  payload field.
- The method still emits exactly ONE `KindCharacterProfileUpdate` envelope through the one
  `buildIntent → mutator` seam.

**`internal/access/policy/seed.go` — the world-layer authorization gate. Without this the whole
write path is default-denied** (cross-AI review R-27). Every world write the admin traverses runs
its own `checkAccess` on a `character:<id>` resource: `"retire"` (`:1321-1322`), `"unretire"`
(`:1421`), `"write"` (`:1090-1091`). D-104 forces a **player**-flavoured caller, because
`Caller.subject` is carried verbatim into the envelope Actor. But `seed:admin-full-access`
requires `principal is character` (`seed.go:105-107`) and never fires for a player principal, and
`seed:admin-section-access` is scoped `resource is admin_section` (`:985-987`) and does not reach
a character resource. The admin passes the section interceptor and is then denied inside the
world service.

Add one seed policy. **The DSL text is written here, in the plan, so `abac-reviewer` can gate it
on paper rather than discover it in a diff:**

```
{
    Name:        "seed:admin-character-administration",
    Description: "Admin players may read, write and move the lifecycle of any character, scoped to the character resource type (ADMIN-03/04/05, §10.4). Not delete.",
    DSLText:     `permit(principal is player, action in ["read", "write", "retire", "unretire"], resource is character) when { "admin" in principal.player.roles };`,
    SeedVersion: 1,
},
```

The scoping choices, each of which is the answer to a question `abac-reviewer` will ask:

- **`principal is player`** — required, not preferred. D-104 makes the caller player-flavoured so
  the envelope Actor is `player:<id>`; a character-flavoured caller would put the acting-character
  id back into the retained audit trail, which D-104 exists to remove. This mirrors the shipped
  `seed:admin-section-access`, which is player-flavoured for the same family of reason
  (`seed.go:975-987`'s own comment: one answer to "is this human an admin", not two).
- **`resource is character`** — narrower than `seed:admin-full-access`'s unrestricted `resource`.
- **The four actions are exhaustive over what the admin RPCs traverse**, and no more.
- **`delete` is deliberately absent.** `world.Service.DeleteCharacter` is irreversible and
  cascades `entity_properties` (§4.4). There is no `AdminDeleteCharacter` RPC, and this omission
  makes the same guarantee hold one layer down: a player-principal admin is denied `delete` on a
  character by the **engine**, not only by the absence of a button. Two tests pin it — the
  compiled policy's action list has exactly four members, and a real engine over the full seed
  corpus DENIES a player-admin subject `delete` on a `character:` resource. Adding `"delete"` to
  the DSL makes both FAIL; demonstrate and record.

Pin the new policy the way `seed:admin-section-access` is pinned, in
`internal/access/policy/seed_profile_visibility_test.go` beside
`TestSeedAdminSectionAccessIsTypeScopedAndPlayerFlavored`: assert it compiles, that its target
resource is the character **type** with `Target.ResourceExact` nil, that its principal kind is
`player`, and that its action list is exactly the four.

**Schedule the seed-census bookkeeping the new policy breaks** (cross-AI review C2-24, corrected
by C3-28). Adding a policy turns **three** shipped exhaustive assertions RED — an earlier revision
scheduled only two, and the third fails on a *correct* implementation exactly like the other two:

1. `internal/access/policy/seed_test.go:37` — `assert.Len(t, seeds, 64, "expected 64 seed
   policies (54 permit, 10 forbid)")`. Bump the count to **65** and the parenthetical to
   `(55 permit, 10 forbid)`.
2. `internal/access/policy/seed_test.go:214` — `assert.ElementsMatch(t, expectedNames, seedNames)`
   against the hardcoded inventory beginning at `:99`. Append
   `"seed:admin-character-administration"`, following the file's own ordering and
   one-line-provenance convention.
3. **`TestSeedPoliciesEffectDistribution` (`internal/access/policy/seed_test.go:81-95`)** — a
   SEPARATE function that compiles every seed and asserts `assert.Equal(t, 54, permitCount, …)`
   and `assert.Equal(t, 10, forbidCount, …)`. The new policy is a **permit**, so `permitCount`
   becomes **55** and its long provenance parenthetical gains one clause naming this phase
   (`+1 v0.13 phase-06 admin character administration ADMIN-03/04/05`), matching the convention
   every other clause in that string follows. `forbidCount` stays **10**. This is the site the
   previous revision missed, and it is invisible to a search for `Len(t, seeds` or
   `expectedNames` — which is precisely why the pre-edit sweep below must include `permitCount`.

- **Before editing, run
  `rg -n 'SeedPolicies|expectedNames|Len\(t, seeds|permitCount|forbidCount' internal/access --glob '*_test.go'`
  and record what it returned.** The `permitCount|forbidCount` arms are the C3-28 addition: the
  earlier sweep pattern could not have surfaced site 3. Verified at revision time that
  `internal/access/setup/seed_coverage_test.go` and
  `buildabacstack_seed_coverage_integ_test.go` carry no count or name inventory, so **three** sites
  in one file is the expectation — but confirm rather than assume.

Scheduling this matters beyond tidiness: the executor's own `task test -- ./internal/access/...`
goes red immediately after a **correct** implementation, and an executor who was not told the
bump is owed may conclude the policy is wrong and weaken it. An unscheduled red on an
authorization census is exactly the improvisation this plan's standards exist to prevent.

**`AdminPortalServer` gains a world-service dependency, so the constructor and the composition root are
in scope** (cross-AI review R-22): add `WithAdminCharacterWriter(w AdminCharacterWriter)` to the
`AdminPortalServerOption` seam, where `AdminCharacterWriter` is a narrow interface over the three
`world.Service` methods this plan calls; wire it at `cmd/holomush/sub_grpc.go` from the world
service already constructed there, and at the harness. A nil writer returns `codes.Internal`,
never a silent success.
- Map errors through `mapAdminSectionError` plus: `WORLD_CONCURRENT_EDIT` → `codes.Aborted`;
  `world.ErrNotFound` → `codes.NotFound` with a static message; an unlisted mask path or a
  zero `expected_version` → `codes.InvalidArgument` with a static message; anything else →
  `errutil.LogErrorContext` then `status.Errorf(codes.Internal, "internal error")`. Translate at
  this one layer only; never `%v` an inner error into a status message.

**§10.6's designated durable verification — two tests, both required.** `01-SPEC.md:2576-2583`
names it explicitly: *"the durable verification is schema-level: a meta-test that fails if the admin
character message ever gains a field whose name matches `role|grant|permission|capability`, paired
with an allowlist test asserting set equality against the checked-in list above."* Neither exists in
the tree. Ship both; a source grep over one hand-written Go file is not a substitute for either,
because by construction it cannot see a field added to a **generated** proto message — the exact
future risk §10.6 names.

1. **Allowlist set equality** — `TestAdminProfileMaskAllowlistMatchesSpec` in
   `internal/grpc/admin_characters_write_test.go` (same package, so it can read the unexported map).
   Check in the 13 paths above as a literal `[]string` in the test, with a comment citing
   `01-SPEC.md` §10.6 as the source of truth. `require.NotEmpty` on **both** the map key set and the
   literal, then assert `setSymmetricDifference` between them is empty in **both directions** — a
   missing path and an EXTRA path must each fail. Prove both directions RED: delete `description`
   from the map (MISSING fires), then add `profile.nickname` (EXTRA fires). Revert after proving.
2. **Proto-descriptor role fence** — `TestAdminCharacterMessagesCarryNoRoleBearingField` in
   `test/meta/admin_character_message_role_fence_test.go`. Walk the **descriptors**, not the source:
   for each of `AdminUpdateCharacterRequest`, `AdminCharacter` and each admin character response
   message, take `msg.ProtoReflect().Descriptor().Fields()` and collect every field's
   `Name()` — recursing into nested message fields so a role field hidden one level down is still
   seen. **Bound the recursion with a visited set** keyed on each message descriptor's
   `protoreflect.FullName`, skipping any descriptor already visited: proto descriptor graphs may
   cycle (a self-referential message, or `google.protobuf.Struct`/`Value`/`ListValue`), and an
   unbounded walk over one HANGS the test rather than failing it. Today's enumerated set is scalars
   plus `google.protobuf.FieldMask` and is acyclic, so this is future-proofing — but a hang is a
   far worse failure mode than a red, and the guard is three lines.
   `require.NotEmpty` on the collected field set so a reflection call that returned nothing
   fails rather than passing vacuously. Assert no collected name matches
   `(?i)(role|grant|permission|capability)`. Prove it RED by temporarily adding
   `repeated string roles = 90;` to `AdminCharacter`, running `task proto`, and confirming the test
   fails naming that field; revert the proto and regenerate. Record the failure text. Its doc
   comment states why it walks descriptors rather than grepping source: the risk is a *future*
   field on a *generated* message, which no source grep over `admin_characters_write.go` can see.

Add the three `WebAdmin*` proxies to `internal/web/admin_handlers.go` (five-part shape,
computes nothing). `task proto && task web:generate`; commit generated artifacts.
  </action>
  <verify>
    <automated>task test -- ./internal/grpc/... ./internal/world/... && task test:int -- ./test/integration/access/...</automated>
  </verify>
  <acceptance_criteria>
    - `TestAdminProfileMaskAllowlistMatchesSpec` passes, asserting `setSymmetricDifference` between the allowlist map keys and the checked-in §10.6 13-path literal is empty in BOTH directions, with `require.NotEmpty` on each side. Deleting `description` from the map makes it FAIL (MISSING direction live); adding a `profile.nickname` entry makes it FAIL (EXTRA direction live). Demonstrate both and record. **Do NOT gate this on a `profile.` prefix count** — the allowlist is `description` plus twelve `profile.*` paths, so a correct implementation yields 12 such matches and a count of 13 is unsatisfiable.
    - `TestAdminCharacterMessagesCarryNoRoleBearingField` passes, walking `ProtoReflect().Descriptor().Fields()` (recursing into nested messages, bounded by a visited set keyed on `protoreflect.FullName` so a cyclic descriptor graph cannot hang the walk) with `require.NotEmpty` on the collected field set, and rejecting any name matching `(?i)(role|grant|permission|capability)`. Temporarily adding `repeated string roles = 90;` to `AdminCharacter` and running `task proto` makes it FAIL naming that field — demonstrate, record the failure text, then revert and regenerate.
    - Secondary source-hygiene check only, never a substitute for the descriptor fence: `rg -v '^\s*//' internal/grpc/admin_characters_write.go | rg -q 'Roles'` exits non-zero. Comments are filtered out because the file's own doc block explains which paths are excluded and an unfiltered grep would be self-invalidating.
    - `rg -n 'DeleteCharacter' internal/grpc/ internal/web/` returns no match inside any admin file.
    - Mask matching is exact-string map access, asserted **behaviourally**: a Vitest-equivalent Go case sends `update_mask: ["profile."]`, one sends `["profile.*"]`, and one sends `["profile.biograph"]` (a proper prefix of a real path), and all three return `codes.InvalidArgument`. A prefix, glob or `Contains` implementation accepts at least one of them. **The previous grep, `rg -n 'HasPrefix|strings.Contains|glob|\*' …`, could not print "no match"** (cross-AI review C2-28): `\*` matches every Go pointer, and the file is full of `*adminportalv1.AdminUpdateCharacterRequest`. A criterion whose output is guaranteed non-empty carries no signal.
    - The byte-cap test covers **both** caps in one table: 99/100/101 against `world.MaxNameLength` (100) for a short field and 3999/4000/4001 against `world.MaxDescriptionLength` (4000) for a long field, plus a CJK value crossing each respective cap. `rg -n 'MaxNameLength|MaxDescriptionLength' internal/grpc/admin_characters_write_test.go` matches both — a table that names only one cap is insufficient and, applied to a long field, would assert behaviour the server correctly refuses to exhibit.
    - `rg -v '^\s*//' internal/grpc/admin_characters_write.go | rg -q 'UpdateCharacterDescription'` exits **non-zero** — the admin path never routes a description edit through the method whose payload declares a `description` string field (`taxonomy.go:189-192`). Wiring it there makes Test 6b's prose-absence assertion FAIL; demonstrate and record. **The `^\s*//` filter is load-bearing, not decoration** (cross-AI review C3-31): this plan *requires* the handler's doc block to explain why the admin path must not route through `UpdateCharacterDescription`, so an unfiltered grep is invalidated by the very comment the plan mandates — the same self-invalidation the `Roles` criterion below already filters for. Keep the explanation in a full-line doc comment, not a trailing one.
    - `rg -n 'WithDescription|ProfileUpdateOption' internal/world/service.go` matches, and `git diff internal/world/service.go` shows `UpdateCharacterProfileAttributes` gained only a trailing variadic parameter — every existing caller compiles unmodified.
    - Test 6b's single-envelope assertion counts outbox rows for the mutation and asserts exactly **1**; an implementation that made two domain calls emits 2 and fails.
    - `rg -n 'seed:admin-character-administration' internal/access/policy/seed.go` matches exactly once, its DSL text is byte-identical to the block in the action, and its action list contains no `"delete"`. `task test -- ./internal/access/... -run TestSeedAdminCharacterAdministration` passes.
    - `rg -n 'WithAdminCharacterWriter' internal/grpc/admin_service.go cmd/holomush/sub_grpc.go internal/testsupport/integrationtest/harness.go` matches in all three.
    - The empty-mask **no-op success** is stated in the handler's doc block, citing `01-SPEC.md` §9.5 rule 4 and `service.go:1218-1227`'s response-channel reasoning; `rg -n 'InvalidArgument' internal/grpc/admin_characters_write.go` shows it used for an **unlisted mask path** and a **zero `expected_version`**, and NOT for an empty mask.
    - Test 3 asserts the empty-mask call returns OK, returns the pre-call version, leaves every field unchanged on a subsequent read, and produces **zero** outbox rows. Test 3b asserts an empty mask with a stale version still returns `Aborted` and for a missing character still returns `NotFound` — the guards run before the no-op answer. A handler that returned `InvalidArgument` fails Test 3; one that returned early before the guards fails Test 3b.
    - `rg -n 'UpdateCharacterProfileAttributes' internal/grpc/characteraccess_service.go` shows the interface method carrying the same `opts ...world.ProfileUpdateOption` tail as the implementation, and `rg -n 'UpdateCharacterProfileAttributes\(' internal/grpc/characteraccess_create.go internal/grpc/characteraccess_write.go` shows **both** call sites passing zero options — the facade's declared narrowness is preserved by assertion, not merely by the old signature. `task test -- ./internal/grpc/` passes as part of this task's own verify, not only at the phase gate.
    - `git diff internal/grpc/characteraccess_service.go` shows a signature change only: the doc block at `:44-63` and the `characterAccessProfileVisibility` interface below it are byte-identical.
    - **A description-only mask works.** A test sends `update_mask: ["description"]` with a changed value and asserts the description changed, `version` incremented by exactly **one**, and exactly **one** envelope was emitted whose `changed_attributes` is `["description"]`. Placing the `WithDescription` write after the empty-partition early return (`service.go:1228-1236`) makes it FAIL with the description unwritten and no version bump — demonstrate and record. This is the path the whole combined-write fix exists to serve and the one an unguarded early return silently drops.
    - A test sends `update_mask: ["description"]` with the **same** value already stored and asserts the documented no-op: no version bump, no envelope, success.
    - **The equal-valued `profile.*` case is tested, and it is the one the admin no-empty-envelope guarantee actually turns on** (cross-AI review C3-26). A test sends a **non-empty** `update_mask` naming only `profile.biography` whose value is byte-identical to the stored row, and asserts: `codes.OK`, the version **unchanged**, **zero** outbox rows, and zero `events_audit` rows. Removing `world.WithSkipUnchangedProperties()` from the admin handler's option list makes it FAIL with the version incremented and **one** envelope carrying an empty `changed_attributes` — demonstrate and record, because that is the shipped behaviour of `UpdateCharacterProfileAttributes` (`service.go:1112-1134`: the equal-valued rewrite is *unconditional*) and the reason a handler precheck cannot deliver this guarantee. Only `description`-same-value was covered before, and `description` travels a different code path (`descriptionChanged`), so it could not observe this bug.
    - **The player path is proven UNCHANGED by the same option.** A test calls `world.Service.UpdateCharacterProfileAttributes` with **no** options and an all-identical attribute map, and asserts the shipped behaviour still holds: the row IS rewritten, the version IS bumped, and ONE envelope is emitted with an empty `changed_attributes` — the *"both representable and honest"* contract at `service.go:1115-1134`. Making the skip unconditional instead of option-gated makes this FAIL; demonstrate and record. This is what stops the admin fix from silently changing a live player contract.
    - `rg -n 'WithSkipUnchangedProperties' internal/world/service.go internal/grpc/admin_characters_write.go` matches in **both**, and `rg -n 'WithSkipUnchangedProperties' internal/grpc/characteraccess_create.go internal/grpc/characteraccess_write.go` returns **zero** matches — the option is admin-only by construction.
    - `rg -n 'UpdateCharacterProfileAttributes' internal/grpc/characteraccess_write_test.go` shows `recordingWorldMutator`'s method carrying the same `opts ...world.ProfileUpdateOption` tail as the interface, and its body asserting `require.Empty(m.t, opts, …)`. **Omitting this file is a compile break, not a runtime surprise** (cross-AI review C3-25): the double at `:87` declares the old non-variadic signature and every character-access write test assigns it to `characterAccessWorldMutator`. Deleting the `require.Empty` and passing an option from a character-access call site makes the character-access suite FAIL — demonstrate and record; that is what converts the two call-site greps into a behavioural check.
    - **All THREE seed-census sites are updated, not two** (cross-AI review C3-28). `task test -- ./internal/access/...` passes, and `internal/access/policy/seed_test.go` shows: (1) `assert.Len(t, seeds, 65, "expected 65 seed policies (55 permit, 10 forbid)")`; (2) `"seed:admin-character-administration"` in `expectedNames`; and (3) `TestSeedPoliciesEffectDistribution` (`:81-95`) asserting `assert.Equal(t, 55, permitCount, …)` with a new provenance clause naming this phase, and `forbidCount` still `10`. Site 3 is a separate function invisible to a search for `Len(t, seeds` or `expectedNames`, which is why the previous revision missed it and why the sweep pattern below now includes the count variables. `rg -n 'assert\.Equal\(t, 54, permitCount' internal/access/policy/seed_test.go` returns **zero** matches after the edit. The SUMMARY records what `rg -n 'SeedPolicies|expectedNames|Len\(t, seeds|permitCount|forbidCount' internal/access --glob '*_test.go'` returned, so a fourth enumeration is ruled out by evidence rather than by assumption.
    - `rg -n 'RetireCharacter|UnretireCharacter' internal/grpc/admin_characters_write.go` shows the calls go to the `world.Service` methods, not to a repository or an admin-only lifecycle path.
    - Each of the three has a wire-level denial test with a paired positive control.
    - `task lint:proto` exits 0; generated artifacts committed.
  </acceptance_criteria>
  <done>Three gated write RPCs enforce a 13-path exact-string allowlist (`description` plus the twelve `profile.*` paths) proven by set equality in both directions against the checked-in §10.6 list; §10.6's schema-level descriptor fence rejects any role-bearing field on the admin character messages, demonstrated RED; the RPCs refuse an absent or zero `expected_version`, agree with the client on byte caps, and route both lifecycle transitions through the same `world.Service` methods the player path uses.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Prove the envelope is transactional and the audit row is projected</name>
  <precondition>Docker is running and `task test:int` can start both the Postgres testcontainer and the embedded NATS JetStream server — the transactional boundary and the asynchronous projection are only observable against both.</precondition>
  <files>test/integration/access/admin_characters_write_test.go, test/meta/world_sql_fence_test.go, docs/architecture/invariants.yaml, docs/architecture/invariants.md</files>
  <read_first>
    - `internal/eventbus/audit/projection.go:304` (`writeAuditRow`) and its `INSERT INTO
      events_audit` at `:376` — the single writer, plus the retention-partition mover.
    - `internal/world/service.go:816-828` — the `buildIntent` → `mutator.*` same-transaction
      seam (INV-WORLD-1).
    - `internal/testsupport/integrationtest/harness.go` and `real_abac.go` — check for an
      existing helper that stands up the world service plus the audit projection before
      building one.
    - `test/meta/world_envelope_census_test.go` and `test/meta/world_sql_fence_test.go` — the
      existing envelope and SQL fences; extend rather than duplicate.
    - `01-SPEC.md` §14 row 9 — the amendment that makes the envelope transactional and the row
      projected, which ROADMAP criterion 3 must be read through.
  </read_first>
  <behavior>
    RED first, in `test/integration/access/admin_characters_write_test.go`:
    - Test 1: a successful `AdminRetireCharacter` produces exactly one outbox envelope whose
      `Actor` is `player:<the acting player's id>`, whose payload carries `before_status:
      "active"`, `status: "retired"`, `section: "characters"` and `action: "write"`, and whose
      payload carries **no** character-actor field and no profile prose.
    - Test 2: forcing the mutation transaction to roll back leaves **zero** outbox rows and
      **zero** `events_audit` rows for that character.
    - Test 3: the `events_audit` row appears **asynchronously**, via the projection —
      `Eventually` on the row's presence after the RPC has already returned.
    - Test 4: replaying the same envelope through the projection twice leaves exactly one
      `events_audit` row.
    - Test 5: a successful `AdminUpdateCharacter` on two profile fields produces a payload whose
      `changed_attributes` is a sorted two-element list and whose serialized bytes contain
      **neither** the old nor the new value of either field — **and whose envelope `Actor` equals
      `player:<the acting player's id>`** (cross-AI review C3-32). The D-104 Actor identity was
      asserted only on the *retire* path (Test 1); the **update** path reaches the envelope through
      a different world method with a different option set, so an Actor regression there would
      have gone unobserved. It is one line in a test that already has the envelope in hand.
  </behavior>
  <action>
Write the integration proofs above, reusing `integrationtest.Start(t, …)` rather than building a
harness; if no option stands up the audit projection alongside the world service, add the
smallest one to the existing harness file and state in its doc comment what it provides that
the existing options do not.

**Name the rollback seam and the replay driver here, because "force a rollback" and "replay the
envelope twice" are not instructions** (cross-AI review C3-30). Both were left as verbs with no
named mechanism, and the nearest in-tree precedent does not fit as-is.

- **Test 2's rollback.** The precedent is
  `test/integration/world/character_retire_atomicity_test.go:146-175`, which forces the failure by
  manually duplicating an outbox intent inside a real transaction so the second insert violates a
  unique constraint. **Read it first.** It does not exercise the admin RPC, so it cannot be reused
  verbatim: this test must drive `AdminRetireCharacter` end-to-end and fail the transaction from
  *inside*. Take the cheaper of two routes and state in the SUMMARY which you took and why:
  (a) **extend the precedent's shape** — pre-insert a conflicting outbox row for the intent the
  admin mutation will emit, so the mutation's own insert collides and the whole transaction rolls
  back; this needs no production seam and is the default. (b) If (a) cannot be arranged because
  the intent's key is not knowable before the call, add a **test-only fault-injection option on
  the harness** (not on production types) that wraps the mutator and returns an error after the
  state write and before commit, with a doc comment saying it exists for this proof. **Do not add
  a production hook.** Either way the assertion is unchanged: zero outbox rows AND zero
  `events_audit` rows for that character.
- **Test 4's replay.** "Replay the same envelope twice" means re-delivering **the same event
  ULID** to the projection, because the ULID is the dedup key. Drive it at the projection, not
  through the RPC: capture the emitted envelope from the outbox after Test 1, then invoke the
  audit projection's delivery path a second time with that same envelope — the same call the
  subscriber makes. Read `internal/eventbus/audit/projection.go` around `writeAuditRow` (`:304`)
  and its caller to find the exact entry point and name it in the test's doc comment. Re-issuing
  the **RPC** is not a replay: it produces a new ULID and legitimately writes a second row, so a
  test built that way asserts the opposite of the property. Assert exactly one `events_audit` row
  after the second delivery.

Extend `test/meta/world_sql_fence_test.go` with the `events_audit` single-writer fence.
**Scope it correctly the first time — the naive statement is FALSE at HEAD** (cross-AI review
R-34): `INSERT INTO events_audit` appears today in a dozen non-`projection.go` files, essentially
all test fixtures — `internal/store/events_audit_test.go:76`,
`internal/testsupport/holomushtest/server.go:560,782`,
`internal/eventbus/crypto/dek/rekey_phase3_integration_test.go:223` among them. A fence phrased
"anywhere outside `projection.go`" fails on the existing baseline and the executor would discover
that mid-task and weaken it ad hoc.

The property is about **production writers**:

> Within non-test production Go source, the only file containing an `INSERT INTO events_audit`
> is `internal/eventbus/audit/projection.go`.

**The allowlist is that one file and nothing else** (cross-AI review C2-29, evidence corrected by
C3-29). An earlier revision added "plus the retention-partition mover it already shares the table
with — enumerate that file explicitly", which names a file that can never appear in the match set:
the mover is migration SQL and partition DDL, not a Go `INSERT`. That correction stands.

**What that revision recorded as its evidence does NOT reproduce, so do not expect it to.** It
claimed `rg -n 'INSERT INTO events_audit' --glob '*.go' -g '!*_test.go'` returns *"exactly one
hit, `projection.go:376`"*. Orchestrator-verified at HEAD: it returns **three** —

```
internal/eventbus/audit/projection.go:376
internal/testsupport/holomushtest/server.go:560
internal/testsupport/holomushtest/server.go:782
```

The two extras are test-support fixtures whose filenames do not end in `_test.go`, so the
filename filter alone does not exclude them. **The fence's property is unaffected**, and this is
why the mechanism matters more than the count: `worldFenceSkipDir` (pinned at
`world_sql_fence_test.go:325-328`) skips `internal/testsupport`, so both extras are out of the
inspected set by the shipped allowlist rather than by the grep. Run the `rg`, **record all three
lines**, and state in the SUMMARY that two are excluded by `worldFenceSkipDir`'s
`internal/testsupport` entry — not by the `_test.go` filter, which would have missed them. An
executor told to expect one hit and seeing three will otherwise conclude the plan is wrong and
weaken the fence. A second allowlist entry for a file that carries no insert remains dead
configuration that reads to a later author as permission.

Reuse the shipped mechanism rather than inventing a second one. `world_sql_fence_test.go` already
carries `worldFenceSkipDir` — pinned at `:325-328` to skip `plugins`, `internal/testsupport` and
`test` — and `isWriterBoundaryPath`, pinned at `:333` as the positive allowlist. Extend those
helpers and add a `_test.go` filename filter; cite `:333` in the new test's doc comment as the
mechanism, so the next reader finds the allowlist rather than adding a third one.

Enumerate the matching files rather than counting lines, `require.NotEmpty` on the inspected file
set so a parse that found nothing fails, and prove it goes RED by temporarily adding such an
insert to a scratch **non-test** file. Also prove it does **not** fire on a `_test.go` file, so
the exclusion is a deliberate scope and not an accidental hole. Record both demonstrations.

Register two invariants in `docs/architecture/invariants.yaml`, taking the next free `N` in
each existing scope (no `INV-ADMIN` scope exists and none is created):
- `INV-WORLD-<next>`: "An admin mutation's audit envelope commits or rolls back with its state
  change in one transaction; the `events_audit` row is projected from that envelope
  asynchronously and `internal/eventbus/audit/projection.go` remains the only writer to that
  table." `binding: bound`, `asserted_by` the integration test and the SQL fence.
- `INV-PRIVACY-<next>`: "An admin audit payload carries changed attribute names and lifecycle
  enum values only; no player-authored profile prose and no acting-character id reaches
  `events_audit`." `binding: bound`, `asserted_by` the payload tests and the integration test.
Add the matching `// Verifies:` annotations to the genuinely-asserting tests only — never to a
test that merely touches the code. Regenerate with `go run ./cmd/inv-render`.

**Finally, run the ABAC domain gate — it is a step of this task, not a note in a checklist**
(plan-checker WARNING). Invoke `/holomush-dev:review-abac` over this plan's changed files and
record its binary verdict in the SUMMARY. This plan **does** touch `internal/access/policy/seed.go`,
so the repo's `remind-pre-action-review.sh` hook would nudge — but a nudge is not a schedule, and
Task 2's whole `seed:admin-character-administration` design was written into the plan expressly so
`abac-reviewer` could *"gate it on paper rather than discover it in a diff"*. Hand the reviewer the
DSL text, the four scoping arguments (`principal is player` forced by D-104, `resource is
character`, the four exhaustive actions, and the deliberate absence of `delete`), and the two tests
that pin them. A NOT READY verdict blocks the phase; do not proceed past it.
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/access/... && task test -- ./test/meta/ && go run ./cmd/inv-render && git diff --exit-code docs/architecture/invariants.md</automated>
    <human-check>`/holomush-dev:review-abac` has been run over this plan's changed files — including the new `seed:admin-character-administration` policy in `internal/access/policy/seed.go` — and returned **READY**. Its verdict text is recorded in the SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - The rollback test asserts BOTH `0` outbox rows and `0` `events_audit` rows — a test asserting only the audit table passes under a non-transactional envelope write.
    - The projection test uses `Eventually` on the `events_audit` row rather than asserting it synchronously after the RPC — a synchronous assertion would pass under a forbidden direct insert.
    - The prose-absence assertion greps the **serialized payload bytes** for the literal field values, not the struct fields: `require.NotContains(t, string(payload), oldValue)` and the same for the new value.
    - The fence passes **at HEAD before any new code lands** — run it first against the unmodified tree. A fence whose stated property is false on the existing baseline is not a fence.
    - Temporarily adding an `INSERT INTO events_audit` to a scratch **non-test** Go file makes the fence FAIL, and adding the same string to a `_test.go` file does **not** — demonstrate both and record the failure text for the first. The second demonstration is what proves the test-file exclusion is a scoped decision rather than a hole.
    - `rg -n 'isWriterBoundaryPath|worldFenceSkipDir' test/meta/world_sql_fence_test.go` shows the new assertion reuses the shipped allowlist helpers rather than re-spelling a walker, and the new test's doc comment cites `test/meta/world_sql_fence_test.go:333`.
    - `task test -- -run 'TestEveryRegistryInvariantHasBinding|TestBoundInvariantsAreGenuinelyAsserted|TestProvenanceGuard' ./test/meta/` passes.
    - `go run ./cmd/inv-render && git diff --exit-code docs/architecture/invariants.md` exits 0.
    - **`/holomush-dev:review-abac` has been invoked and returned READY, with its verdict text recorded in the SUMMARY** (plan-checker WARNING). Task 2's seed-policy DSL was written into the plan expressly so this gate could review it on paper; leaving the invocation to a hook nudge rather than scheduling it is what turns "reviewed on paper" into an assumption. A NOT READY verdict blocks the phase.
  </acceptance_criteria>
  <done>The envelope and the state change are proven to commit or roll back together; the audit row is proven to arrive asynchronously through the sole writer; the payload is proven to carry no prose; both properties are registered invariants with real bindings; and the ABAC domain gate has run over the new seed policy and returned READY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → `AdminUpdateCharacter` | An operator writing to a character they do not own. The field mask is the only thing bounding what they may write. |
| mutation transaction → `events_audit` | A retained table. Anything written here cannot be recalled, so the payload shape is a data-protection decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-28 | Elevation of Privilege | an admin mutating a role through the profile mask | critical | mitigate | The allowlist is a 13-entry exact-string map with the accessor on the entry; an unlisted path is rejected, not ignored; `TestAdminProfileMaskAllowlistMatchesSpec` asserts set equality against the checked-in §10.6 list in BOTH directions, demonstrated RED each way. |
| T-06-28b | Elevation of Privilege | a **future** field named `role`/`grant`/`permission`/`capability` added to a generated admin character message, invisible to any source grep over the hand-written handler | high | mitigate | §10.6's designated schema-level fence: `TestAdminCharacterMessagesCarryNoRoleBearingField` walks `ProtoReflect().Descriptor().Fields()` recursively with `require.NotEmpty` on the inspected set, demonstrated RED against a planted `repeated string roles` field. |
| T-06-28c | Tampering | the allowlist silently losing `description` because a criterion counted the `profile.` prefix and 13 paths yield only 12 matches | medium | mitigate | Set-equality against the checked-in list replaces every count-based assertion; the `description` entry is named explicitly in the action, and the MISSING direction is demonstrated RED by deleting exactly it. |
| T-06-29 | Tampering | irreversible `DeleteCharacter` reachable from an admin RPC or button | critical | mitigate | No `AdminDeleteCharacter` exists; `rg 'DeleteCharacter'` asserted absent from every admin file; the UI contract forbids the words delete/remove/purge/erase as a character action. |
| T-06-30 | Information Disclosure | player-authored profile prose written into the retained `events_audit` | high | mitigate | Changed attribute **names** only (D-103); the prose-absence assertion greps the serialized payload bytes for the literal values; registered as an `INV-PRIVACY` invariant. |
| T-06-31 | Information Disclosure | a durable player↔character-alt linkage written into a retained table | high | mitigate | The acting character is omitted (D-104); the envelope Actor is `player:<id>` from `Caller.subject`, and the payload adds no actor field. |
| T-06-32 | Repudiation | a mutation landing with no audit envelope | high | mitigate | Emission goes through the one `buildIntent → mutator` seam in the same transaction (INV-WORLD-1); the rollback test asserts zero envelopes AND zero rows. |
| T-06-33 | Tampering | a second writer to `events_audit` bypassing the codec / `dek_ref` / dedup contract | high | mitigate | SQL fence asserting no `INSERT INTO events_audit` outside `projection.go`, demonstrated RED; §14 row 9. |
| T-06-34 | Tampering | lost update — two operators editing one character concurrently | high | mitigate | `expected_version` is required (`> 0`) on every guarded mutation; a stale version returns `Aborted` and neither the write nor the envelope lands. |
| T-06-35 | Tampering | a byte-cap divergence letting a CJK value through the client and failing at the server, or vice versa | medium | mitigate | Caps enforced in bytes on both sides against the same numbers; the four-case boundary table (99/100/101/CJK). |
| T-06-36 | Repudiation | the payload widening applied partially, so the audit record's schema and its declaration disagree | medium | mitigate | All **eight** ratchet steps in one task; each of the three `SchemaVersion` bumps and the `AppSchemaVersion` bump is individually demonstrated to fail a taxonomy test when reverted alone. |
| T-06-37a | Elevation of Privilege | the admin write path default-denied by `world.Service.checkAccess` on `character:<id>`, forcing the executor to invent an authorization policy mid-execution — or, worse, to "fix" it with a raw repository call or a boolean `isAdmin` that bypasses the one decision point | critical | mitigate | `seed:admin-character-administration` is written **in the plan** with its DSL text so `abac-reviewer` gates it on paper: `principal is player` (forced by D-104), `resource is character`, four actions and no more. `internal/access/policy/seed.go` is in `files_modified`; the policy is pinned the way `seed:admin-section-access` is; and a real-engine test proves permit for the three actions and DENY for a fourth. No bypass of `checkAccess` exists on any admin path. |
| T-06-37b | Elevation of Privilege | the irreversible `DeleteCharacter` reachable because a broad admin policy granted `delete` on `character` alongside the actions actually needed | critical | mitigate | `delete` is omitted from the policy's action list, so the engine denies it for a player-principal admin regardless of what any future RPC does. Two tests pin it (exact four-member action list; a real engine denying `delete`), and adding `"delete"` is a demonstrated RED. This is the same guarantee as "no `AdminDeleteCharacter` exists", made one layer lower where an RPC-level omission cannot be quietly undone. |
| T-06-38 | Information Disclosure | an admin `description` edit routed through `UpdateCharacterDescription`, whose declared payload `characterUpdatePayload` carries a `description` **string** — writing player-authored prose into the retained `events_audit` | high | mitigate | The admin write never calls that method (grep-asserted zero matches); `description` travels as a `WithDescription` option into the extended `UpdateCharacterProfileAttributes`, which emits the names-only `KindCharacterProfileUpdate` payload. Test 6b asserts the emitted bytes contain neither value, and wiring the old route is a demonstrated RED. |
| T-06-39 | Tampering | a mixed `description` + `profile.*` mask served as two domain writes, each consuming the one `expected_version` — so the second fails its precheck and the request **partially commits**, emitting two envelopes in two transactions for one operator action | high | mitigate | One extended domain method, one `checkAccess`, one version bump, one transaction, one envelope. Test 6b asserts both values changed, `version` incremented by exactly one, and exactly one outbox row — three assertions a two-call implementation fails simultaneously. |
| T-06-40b | Repudiation | an `events_audit` fence whose stated property is false at HEAD, so it is weakened ad hoc during execution and stops constraining anything | medium | mitigate | The fence is scoped to non-test production source up front, reuses the shipped `worldFenceSkipDir` / `isWriterBoundaryPath` allowlist (`world_sql_fence_test.go:325-333`) rather than a second mechanism, is required to pass against the unmodified tree first, and is demonstrated both RED on a production file and NOT-red on a `_test.go` file. |
</threat_model>

<artifacts_this_phase_produces>
- Proto: `AdminUpdateCharacter`, `AdminRetireCharacter`, `AdminUnretireCharacter` and their
  request/response messages; the mirrored `WebAdmin*` RPCs and messages.
- Go: `internal/grpc/admin_characters_write.go` — the three handlers, the 13-entry
  `adminProfileMaskablePaths` allowlist, the admin `requireGuardedVersion` use;
  three `AdminDescriptors` entries with `Action: ActionWrite`.
- Go: `internal/world/payloads.go` — `CharacterLifecycleChangePayload.BeforeStatus`,
  `.Section`, `.Action`; a widened `BuildCharacterLifecyclePayload` signature; the admin
  profile-update payload builder and its `AuditContext` value.
- Go: `internal/world/service.go` — `ProfileUpdateOption` and the three options
  `WithDescription`, `WithAuditContext`, `WithSkipUnchangedProperties`; `LifecycleOption` and
  `WithAuditContext`'s lifecycle form; the exported `AuditContext` value type.
- Go: `internal/world/outbox/taxonomy.go` — widened `characterLifecyclePayload`, both
  lifecycle kinds at `SchemaVersion: 2`, `AppSchemaVersion = 4`.
- Tests: `internal/grpc/admin_characters_write_test.go` (including
  `TestAdminProfileMaskAllowlistMatchesSpec`); `test/meta/admin_character_message_role_fence_test.go`
  (`TestAdminCharacterMessagesCarryNoRoleBearingField` — §10.6's designated schema-level fence);
  `TestCharacterProfileUpdatePayloadExposesNoValueBearingField` newly authored in
  `internal/world/payloads_test.go` (no predecessor existed — the name
  `TestBuildCharacterProfileUpdatePayloadCarriesNamesNotValues` appeared in an earlier revision
  and is obsolete: C2-31 restructured the test from a values-absence assertion, which was vacuous
  at that layer, into a structural key-set + reflected-tag assertion); additions to
  `internal/world/outbox/taxonomy_test.go`;
  `test/integration/access/admin_characters_write_test.go`; and the extended
  `events_audit` single-writer SQL fence in `test/meta/world_sql_fence_test.go`.
- Docs: two new invariant ids in the existing `INV-WORLD` and `INV-PRIVACY` scopes.
</artifacts_this_phase_produces>

<verification>
- `task test -- ./internal/world/... ./internal/grpc/... ./test/meta/` green.
- `task test:int -- ./test/integration/...` green (Docker + embedded NATS).
- `task lint`, `task lint:proto` green; `task fmt` run and edits committed (this task edits
  aligned Go `const`/`struct` blocks, which is the exact shape that passes `task build` and
  fails `task fmt:check` in CI).
- `/holomush-dev:review-abac` MUST run before push. `crypto-reviewer` is **not** triggered:
  D-103 deliberately keeps prose out of the payload and no `crypto.emits` declaration changes.
  If that decision is ever revisited toward encrypting the payload, `crypto-reviewer` fires.
</verification>

<review_dispositions>
Cross-AI review findings owned by this plan.

| Ref | Finding | Severity | Disposition |
|---|---|---|---|
| R-27 | Admin writes are default-denied by a second ABAC gate no task addresses: every reused world method runs `checkAccess` on `character:<id>` first, `seed:admin-full-access` requires `principal is character`, and `seed.go` is absent from every `files_modified`. The executor would invent an authorization policy mid-execution — the decision `abac-reviewer` is meant to gate on paper. | HIGH (both reviewers) | **INCORPORATED — option (a), a narrowly-scoped player-principal policy.** `seed:admin-character-administration` with its DSL text written into the action so it can be reviewed before it is implemented; `principal is player` (forced by D-104's Actor requirement), `resource is character`, exactly four actions, **`delete` deliberately excluded**. `seed.go` and its pin test added to `files_modified`. Two engine-level tests plus four demonstrated REDs. Threats `T-06-37a` / `T-06-37b`. Option (b) — a typed authorization result threaded into purpose-built world methods — was rejected: it creates a second trust channel into `checkAccess` and changes shared signatures every player caller uses, for no gain over one narrow policy. No `checkAccess` bypass, no raw repository call, no boolean `isAdmin` appears anywhere. |
| R-28 | The 13-path mask splits across two domain writes that one `expected_version` cannot fund: `UpdateCharacterProfileAttributes` rejects `description`, which has its own method; each bumps `version`; one RPC would emit two envelopes in two transactions. | HIGH (both reviewers) | **INCORPORATED — the combined-write option, and for a reason the reviewers did not have.** Reject-mixed-masks was rejected because it moves a partial-commit hazard onto the client and makes an audited surface's Save half-succeed. But the decisive fact is stronger: `UpdateCharacterDescription` emits `kindCharacterUpdated`, whose declared payload carries a `description` **string** field (`taxonomy.go:189-192`), so routing an admin description edit through it writes prose into the retained `events_audit` — a direct D-103 violation this plan's own `INV-PRIVACY` registration asserts is impossible. The resolution extends the **existing** `UpdateCharacterProfileAttributes` with `WithDescription` and `WithAuditContext` variadic options rather than building a parallel command: one `checkAccess`, one precheck, one transaction, one version bump, one names-only envelope. Test 6b is the RED test, asserting all three properties a two-call implementation breaks at once. Threats `T-06-38` / `T-06-39`. |
| R-29 | The taxonomy ratchet is under-enumerated: the "six places" cover only the lifecycle payload; the profile-update half needs its own widening and bump, or a partial widening there stays green. Task 1's "decide by reading" hedge on the kind question compounds it. | MEDIUM (opencode) | **INCORPORATED.** Eight steps, enumerated. The kind question is **settled in the plan, not deferred**: reuse `KindCharacterProfileUpdate` (so no census, parity-table or kind-list row changes — none of which were in `files_modified`), widen `characterProfilePayload` with `section`/`action`, bump its `SchemaVersion` 1→2. The player path emitting empty `section`/`action` is documented as correct. Each of the four version bumps is individually demonstrated load-bearing. |
| R-30 | The lifecycle audit-context widening is unreconciled with non-admin callers — no typed API says how the admin handler supplies context without changing the shared signature or overloading `Caller`. | MEDIUM (codex) | **INCORPORATED.** A trailing variadic `opts ...LifecycleOption` with `WithAuditContext(AuditContext)`. Every existing caller compiles unchanged and supplies nothing, yielding the zero value and empty fields; `Caller` is not overloaded; census impact is stated (none — no census asserts world-method signatures). A test pins the no-options player behaviour. |
| R-33 | The long-cap boundary is wrong: short fields use `MaxNameLength` (100), long fields `MaxDescriptionLength` (4000). Asking a 4000-byte field to reject 101 bytes cannot pass against correct server behaviour. | HIGH (codex) | **INCORPORATED.** Test 6 covers both caps with their own triples (99/100/101 and 3999/4000/4001) and a CJK value crossing **each** respective cap. A grep asserts both constants appear in the test file. Mirrored in 06.1-04's E2E. |
| R-34 | The `events_audit` fence is false at HEAD as literally stated — `INSERT INTO events_audit` appears in a dozen non-`projection.go` test fixtures — and never references the existing allowlist mechanism at `world_sql_fence_test.go:333`. | MEDIUM (opencode) | **INCORPORATED.** The property is restated as production-only, reuses the shipped `worldFenceSkipDir` / `isWriterBoundaryPath` helpers with a `_test.go` filter, must pass against the unmodified tree first, and is demonstrated both RED on a production file and not-red on a test file. Threat `T-06-40b`. |
| R-31 | "the same methods the player path calls" overstates the tree — `rg '\.RetireCharacter\('` finds zero production callers; player self-retire is deferred (IDENT-04). | LOW (opencode) | **INCORPORATED.** Reworded to "the canonical lifecycle methods" with the absence stated explicitly in `read_first`, so an executor does not hunt for a caller that does not exist. The reuse argument is unchanged and still correct. |
| R-32 | The empty-mask divergence from the player path (`InvalidArgument` vs no-op success, `characteraccess_write.go:315-336`) is deliberate but unflagged, so a checker may read a false contradiction or an executor may "align" the two. | LOW (opencode) | **INCORPORATED.** The divergence and its reason are stated in the handler's doc block with the citation, and a grep asserts the note is present. |
| R-22 | `AdminPortalServer` needs a world-service dependency; `admin_service.go` and `sub_grpc.go` are absent from `files_modified`. | HIGH (codex) | **INCORPORATED.** `WithAdminCharacterWriter` on the `AdminPortalServerOption` seam, wired at both composition roots, asserted present at all three sites; a nil writer returns `codes.Internal`. |
| R-32 (2nd) | The empty-mask divergence from the player path is undocumented. | MEDIUM (codex) | **SUPERSEDED by C2-06, then PARTLY RESTORED by C3-27.** The cycle-1 repair documented a divergence in a handler comment; cycle 2 established that documenting it does not authorize it, and the admin path now returns the normative no-op **success**. Cycle 3 found that one clause does still diverge — a **stale** `expected_version` — and that C2-06's "there is no divergence" wording was false about it. See C3-27: the divergence is kept, narrowed to that one clause, and flagged with its own reason instead of claimed away. |
| R-04 | `task docs:proto` omitted. | MEDIUM | **INCORPORATED** in `files_modified` and the regen criteria. **Row relocated in cycle 3:** it previously sat OUTSIDE a `</review_dispositions>` closing tag that was then repeated, leaving it outside the table for any tooling that parses the block. |

**Convergence cycle 2.**

| Ref | Finding | Severity | Disposition |
|---|---|---|---|
| C2-05 | **[REGRESSION-FROM-FIX]** The combined-write signature breaks an existing compile fence: adding `opts ...ProfileUpdateOption` means `*world.Service` no longer satisfies `characterAccessWorldMutator`, whose method is non-variadic at `internal/grpc/characteraccess_service.go:63` (interface method verified at `:65`) — a file absent from 06-05's `files_modified`, tasks and tests. The plan asserts "every caller compiles unchanged", which is true of call sites and false of the interface. | HIGH (codex), verified against `characteraccess_service.go:64-66` | **INCORPORATED.** `internal/grpc/characteraccess_service.go` is added to `files_modified` and to Task 2's `<files>`, the interface method gains the same `opts ...world.ProfileUpdateOption` tail, and the distinction (call sites vs method set) is written into the action so it is not re-lost. The interface's "one interface per capability" doc block is preserved byte-identical, and the narrowness it argues for is kept by **assertion** instead: both call sites (`characteraccess_create.go:205`, `characteraccess_write.go:347`) are grep-asserted to pass **zero** options. `task test -- ./internal/grpc/` runs inside this task, not only at the phase gate, because the character-access tests are what this change can break. |
| C2-06 | 06-05 contradicts the normative empty-mask contract without an authorized decision: `01-SPEC.md:2197` (§9.5 rule 4) says an empty mask is a no-op success, `:2534` repeats it for admin character editing, and the plan requires `InvalidArgument`. Documenting the divergence in a handler comment does not supersede the phase's normative source, and unlike ADMIN-02/ADMIN-06 this is not a listed user-approved divergence. | HIGH (codex), verified at `01-SPEC.md:2192-2200` | **INCORPORATED — the divergence is WITHDRAWN.** `AdminUpdateCharacter` returns a no-op success carrying current state and current version, with authorization, existence and the `expected_version` guard all evaluated first. The rationale is not deference: `service.go:1218-1227` already explains that the facade answers with success **because its response carries the freshly-read version**, while the world method refuses **because it returns only an error** — and `AdminUpdateCharacterResponse` carries the version, so it is on the facade's side of that line. Test 3 asserts OK + unchanged version + unchanged fields + zero outbox rows; Test 3b asserts the guards still fire (stale version → `Aborted`, missing character → `NotFound`) so the no-op is not a bypass. No amendment is owed: the plan now agrees with the SPEC. |
| C2-24 | **[REGRESSION-FROM-FIX]** The new seed policy breaks two exhaustive seed censuses the plan never schedules: `seed_test.go:37` asserts `Len(seeds, 64)` and `:206-214` asserts `ElementsMatch` against a hardcoded inventory. Neither file is in `files_modified`; the executor's own verify goes red after a correct implementation. | MEDIUM (opencode), verified at `seed_test.go:37,99,214` | **INCORPORATED.** `internal/access/policy/seed_test.go` added to `files_modified` and to Task 2's `<files>`; the action schedules the `64 → 65` bump with its `(55 permit, 10 forbid)` parenthetical and the `expectedNames` append, and mandates a pre-edit `rg` sweep for a third enumeration with its result recorded. The reason it is scheduled rather than left to discovery is stated: an unscheduled red on an authorization census invites an executor to weaken the policy. Verified at revision time that `internal/access/setup/`'s two coverage tests carry no count or inventory. |
| C2-25 | **[REGRESSION-FROM-FIX]** "The builder refuses zero changed attributes" contradicts deliberately-documented shipped behaviour: `service.go:1120-1132` documents the all-identical resubmit as shipping an empty `changed` list alongside a real row write, calling it "both representable and honest", and that path calls the same builder. A builder that errors converts a live player success into an error. | MEDIUM (opencode), verified at `service.go:1115-1134` | **INCORPORATED.** The refusal is relocated to the **admin handler** as a precheck; the shared builder keeps accepting an empty list and Task 1 Test 5 now asserts that it does. The must-have is narrowed from "changed_attributes is never an empty array in an emitted payload" to "no **admin** envelope is emitted for zero changed attributes", which is the property actually wanted and the one that leaves the player path alone. |
| C2-26 | **[REGRESSION-FROM-FIX]** The `WithDescription` extension never reconciles with the empty-partition early return: `service.go:1222-1233` returns `nil` when creates/updates/deletes are all empty, so a description-only mask puts nothing in `attributes`, the early return fires, and the description is never written — Task 2 Test 1 goes red for exactly the path the R-28 fix exists to serve. The action never says where the write sits relative to that return. | MEDIUM (opencode), verified at `service.go:1228-1236` | **INCORPORATED.** The action now specifies four rules by name: compute `descriptionChanged` before the early-return check; the early return fires only when all three partitions are empty **and** `!descriptionChanged`, with its version-mismatch refusal unchanged and still first; a true `descriptionChanged` continues into the transaction for one CAS, one version bump, one `"description"` entry in `changed` and one envelope; a supplied-but-equal value is the documented no-op. Two new acceptance criteria pin it — a description-only mask bumping version by exactly one with a one-element `changed_attributes`, demonstrated RED by placing the write after the early return, and a same-value case asserting the no-op. |
| C2-27 | Task 1's payload grep `rg -n 'description\|pronouns\|profile\.' internal/world/payloads.go` matches `CharacterUpdateChangePayload.Description` (`payloads.go:300-305`), a legitimately shipped prose-carrying field this plan deliberately leaves in place. Judgment-phrased, so it cannot false-fail — but it invites fixing the wrong thing. | LOW (opencode) | **INCORPORATED.** Narrowed to `rg -n 'profile\.' internal/world/payloads.go`, with the `description`/`pronouns` arms' removal and its reason recorded inline, and with the actual property (admin edits carry no prose) attributed to Test 4 and Test 6b where it is really proven. |
| C2-28 | Task 2's wildcard grep `rg -n 'HasPrefix\|strings.Contains\|glob\|\*' …` always matches: `\*` matches every Go pointer, and the handler file is full of `*adminportalv1.…Request`. It cannot print "no match". | LOW (opencode) | **INCORPORATED.** Replaced with three behavioural cases — `update_mask` of `["profile."]`, `["profile.*"]` and `["profile.biograph"]` all returning `InvalidArgument` — which a prefix, glob or `Contains` implementation fails on at least one. A criterion whose output is guaranteed non-empty carries no signal and is retired. |
| C2-29 | The fence's "retention-partition mover" file does not contain the fenced string: the only production non-test `INSERT INTO events_audit` is `projection.go:376`, and the mover lives in migration SQL, so "enumerate that file explicitly" names a file that never appears in the match set. | LOW (opencode) | **INCORPORATED.** Task 3's action states the allowlist as "`projection.go` alone", with the `rg` evidence that establishes it, and drops the instruction to enumerate a mover file that carries no insert. |
| C2-30 | The scalar-count repair does not deliver its claimed snapshot consistency — this belongs to 06-04. | MEDIUM (codex) | **Owned and dispositioned by 06-04** (see that plan's `<review_dispositions>`); recorded here only because Task 3's transactional-boundary proofs sit adjacent to it. |
| C2-31 | The builder-level prose-absence test cannot carry the old/new values it claims to exclude: the builder's shape is `{character_id, changed_attributes, section, action}` and it takes no value parameters, so declaring local sentinels and asserting their absence is vacuous. | MEDIUM (codex) | **INCORPORATED — the test is restructured, not merely renamed.** Task 1 Test 4 becomes `TestCharacterProfileUpdatePayloadExposesNoValueBearingField`: a **structural** assertion over the marshalled key set and the reflected `json` tags, `require.Len` + `require.NotEmpty` on both sides, demonstrated RED by planting a `Values map[string]string` field. The value-absence property moves to Test 6b, the end-to-end admin-update path, which is the only layer where real old and new prose actually flows and where a `NotContains` on serialized bytes can fail. |

**Convergence cycle 3.**

Findings scoped to `06-03`/`06-06`/`06-07`/`06-08` are **MOVED TO 06.1** (see `06.1-REVIEWS.md`)
and are listed rather than omitted, because silence reads as an oversight.

| Ref | Finding | Severity | Disposition |
|---|---|---|---|
| C3-25 | **[REGRESSION-FROM-FIX]** The C2-05 variadic repair still breaks the test suite: `recordingWorldMutator.UpdateCharacterProfileAttributes` (`internal/grpc/characteraccess_write_test.go:87`) declares the old non-variadic signature and no longer satisfies the modified interface, and that file is absent from both `files_modified` and Task 2's `<files>`. *"Updating only `characterAccessWorldMutator` makes every test using that double fail to compile … no task authorizes the required edit."* | HIGH (codex-B) | **INCORPORATED.** Orchestrator-verified at `characteraccess_write_test.go:87`: the double's signature is exactly the non-variadic one. `internal/grpc/characteraccess_write_test.go` is added to `files_modified` and to Task 2's `<files>`, and the action names the edit. It also takes codex-B's stronger suggestion: the double's method asserts `require.Empty(m.t, opts, …)`, which converts the two call-site greps into a **behavioural** check running on every character-access test that touches the mutator — so the facade's narrowness is pinned by execution, not only by text. A new acceptance criterion pins the signature, the assertion, and a demonstrated RED. |
| C3-26 | **C2-25's admin no-empty-envelope guarantee is not actually enforced.** The proposed handler precheck cannot establish "zero changed attributes" for profile fields under concurrency, and the domain method intentionally emits an empty-`changed` envelope for equal-valued row rewrites (`service.go:1123-1134`). No acceptance criterion sends a non-empty mask containing only equal-valued `profile.*` fields; only same-value `description` is tested, and `description` travels a different code path. Either withdraw the guarantee or add a typed domain option that suppresses equal-valued property rewrites inside the authoritative transaction, with a race-aware test. | HIGH (codex-B) | **INCORPORATED — option (b), the typed domain option.** Orchestrator-verified at `service.go:1112-1134`, whose own comment says the equal-valued resubmit *"DOES rewrite the row (it lands in `updates`, so the write, the CAS and the version bump are unconditional)"* while leaving `changed` empty — so a handler precheck could not have suppressed anything even absent the race. A third option, `world.WithSkipUnchangedProperties()`, drops equal-valued names from the `updates` partition **inside** the transaction, after the rows are read and under the CAS, which is race-safe by construction; when that empties all three partitions and no description changed, the shipped early return at `:1228-1236` fires and the call is a true no-op. The option is **admin-only**: the player facade supplies nothing and its unconditional-rewrite behaviour is byte-identical, pinned by its own criterion. Withdrawing the guarantee instead was rejected — ADMIN-06's audit contract is what makes "no envelope for no change" meaningful, and a version bump with an empty-`changed` envelope on an audited cross-owner surface is exactly the noise the guarantee exists to prevent. Truths rewritten; three new acceptance criteria (the equal-valued `profile.*` case with its RED, the player-path-unchanged control, and the admin-only option grep). |
| C3-27 | The empty-mask "no divergence" language is false. The admin path requires stale-version rejection (`Aborted`); the player path intentionally returns success for a stale empty mask (`characteraccess_write.go:315-336`, whose comment reads *"A STALE CALLER IS ANSWERED, NOT REFUSED"* and whose code never compares `expectedVersion`). *"State the remaining divergence explicitly and ground it in the admin contract instead of claiming complete parity."* | MEDIUM (codex-B) + MEDIUM (opencode-3 Concern 1, independently, from the other side — it noted the plan cites the very comment that argues **against** its own Test 3b) | **INCORPORATED — the `Aborted` is KEPT and the parity claim is DROPPED.** Orchestrator-verified at `characteraccess_write.go:313-336`: the empty-mask branch returns `projectOwner(char, profile)` without comparing `expectedVersion`. Both reviewers were right that the two cannot both be true. The behaviour that ships is the refusal, on a reason that is the admin contract's and not the facade's: the facade's caller edits their **own** character and staleness costs one round trip, while the admin caller edits **someone else's** on an audited surface, and a stale guard means the request was composed against a view that has since changed — refusing is what makes the operator re-read. The truth, the action and the handler doc block now state the divergence, its single clause (stale `expected_version`) and that reason, instead of asserting a parity the shipped facade contradicts. |
| C3-28 | **[REGRESSION-FROM-FIX]** Seed census bookkeeping remains incomplete: C2-24 scheduled `Len(seeds, 64)` and `expectedNames` but missed `TestSeedPoliciesEffectDistribution` (`internal/access/policy/seed_test.go:81-95`), which independently asserts exactly 54 permit policies. The new policy is a permit, so that must become 55, and the proposed pre-edit census grep omits `permitCount`. | MEDIUM (codex-B) | **INCORPORATED.** Orchestrator-verified at `seed_test.go:81-95`: `assert.Equal(t, 54, permitCount, …)` and `assert.Equal(t, 10, forbidCount, …)` in a separate function. The action now enumerates **three** sites with site 3 spelled out (bump to 55, add a provenance clause naming this phase, `forbidCount` unchanged) and explains why site 3 is invisible to the old sweep pattern. The pre-edit grep gains `permitCount|forbidCount`, and the acceptance criterion asserts `assert.Equal(t, 54, permitCount` returns zero matches after the edit. |
| C3-29 | **C2-29's recorded verification does not reproduce.** The disposition claims `rg -n 'INSERT INTO events_audit' --glob '*.go' -g '!*_test.go'` returns *"exactly one hit, `projection.go:376`"*; it returns **three** — plus `internal/testsupport/holomushtest/server.go:560,782`, both non-`_test.go` filenames. The design survives because `worldFenceSkipDir` skips `internal/testsupport`, but Task 3 tells the executor to run that `rg` and record the output, which will now contradict the plan. | MEDIUM (opencode-3 Concern 2) + implicit (codex-B) | **INCORPORATED.** Orchestrator-verified: three hits, exactly as reported. Task 3's action now prints all three expected lines verbatim, attributes the two extras to `worldFenceSkipDir`'s `internal/testsupport` entry (`world_sql_fence_test.go:325-328`) rather than to the `_test.go` filter — which would **not** have excluded them, and saying which mechanism does the work is the point — and instructs the executor to record all three and state the exclusion reason. The allowlist stays the single file. |
| C3-30 | Task 3's rollback and replay mechanisms remain underspecified: the plan asks to "force" an RPC mutation rollback and replay the same envelope twice but names no injection seam or projection API. The existing rollback precedent (`character_retire_atomicity_test.go:146-175`) manually duplicates an outbox intent inside a real transaction and does not exercise the admin RPC. | MEDIUM (codex-B) | **INCORPORATED.** The action now names the precedent as required reading, gives two concrete routes for the rollback (extend the precedent's conflicting-outbox-row shape — the default; or a **test-only** harness fault-injection option, explicitly not a production hook) and requires the SUMMARY to record which was taken and why. For the replay it names the actual mechanism: re-deliver the **same event ULID** to the projection's delivery path (read around `projection.go:304`), and it states the trap explicitly — re-issuing the **RPC** is not a replay, because it mints a new ULID and legitimately writes a second row, so a test built that way asserts the opposite of the property. |
| C3-31 | The `UpdateCharacterDescription` zero-match grep can be self-invalidated by the doc comment the plan itself mandates: the plan requires a doc block explaining why the admin path must not route through that method, and the unfiltered grep would match it. | LOW (opencode-3 Concern 3) | **INCORPORATED.** The criterion gains the `rg -v '^\s*//'` filter, mirroring the `Roles` criterion in the same block, with the reason stated inline and an instruction to keep the explanation in a full-line doc comment rather than a trailing one. |
| C3-32 | Suggestion: assert in Test 6b that the single emitted envelope's `Actor` equals `player:<id>` — the D-104 identity claim is currently covered only by Task 3 Test 1 (retire), not the update path. | LOW (opencode-3, suggestion 4) | **INCORPORATED, placed at Task 3 Test 5** (which already holds the envelope, so it is one line). The update path reaches the envelope through a different world method with a different option set than retire does, so an Actor regression there would otherwise go unobserved. |
| C3-33 | Stale plan metadata: the must-have at `:47` still describes the builder-level test as checking old/new values, contradicting the corrected structural test; the artifacts section names the obsolete `TestBuildCharacterProfileUpdatePayloadCarriesNamesNotValues`. | LOW (codex-B) | **INCORPORATED.** The must-have is rewritten to describe the deliberate **two-layer** split — structural at the builder (no sentinels, because the builder takes no values), behavioural end-to-end at Task 3 Test 5 / Task 2 Test 6b (where prose actually flows) — and says why neither layer can carry the other's assertion. The artifacts entry names `TestCharacterProfileUpdatePayloadExposesNoValueBearingField` and records the obsolete name with the reason it changed, so a reader of an old SUMMARY can resolve it. The artifacts section also gains the `internal/world/service.go` option types, which were produced but unlisted. |
| C3-34 | Malformed review-disposition markup: `</review_dispositions>` closes before the `R-04` row and then closes again, which may break plan tooling or leave R-04 outside the table. (The same defect appears in 06-04.) | LOW (codex-B) | **INCORPORATED.** The `R-04` row is moved inside the cycle-1 table and the duplicate closing tag removed. The duplicated `R-32` row is also disambiguated — the second occurrence is now labelled `R-32 (2nd)` and its disposition updated for C3-27. |
| C3-35 | Five Svelte component tests in the wrong Vitest project; the `<768px` drawer half; the C2-32 exclusion glob; `setPlayerProfile` call sites; amendment 11; `.mobilebar`; `[section]/+page.svelte`. | HIGH ×2 + MEDIUM cluster (codex-C, opencode-4a/4b1/4b2) | **MOVED TO 06.1.** Scoped to plans that became `06.1-01`…`06.1-04` on 2026-08-13; carried in `06.1-REVIEWS.md`. Not this phase's; not re-filed here. |
| C3-39 | The `abac-reviewer` gate is named but never scheduled — `/holomush-dev:review-abac` appears only in the prose `<verification>` list, in no task, `<verify>` or `<done>`, across all four plans. (Raised against `06-04` and `06-05` jointly; `06-04` carries the primary row.) | **WARNING** (plan-checker) | **INCORPORATED in this plan too.** Unlike `06-04`, this plan **does** touch `internal/access/policy/seed.go`, so the repo's `remind-pre-action-review.sh` hook would nudge — but a nudge is not a schedule, and Task 2's action states outright that the `seed:admin-character-administration` DSL was written into the plan so `abac-reviewer` could *"gate it on paper rather than discover it in a diff"*. Leaving the invocation to a hook is what turns that into an assumption. The gate becomes a scheduled step of Task 3: named in the action with the four scoping arguments to hand the reviewer, a `<verify><human-check>`, an acceptance criterion, and a clause in `<done>`. A NOT READY verdict blocks the phase. |
</review_dispositions>

<success_criteria>
- ROADMAP Success Criterion 2's write half holds: 13-path allowlist excluding roles, and
  admin lifecycle transitions reusing the canonical lifecycle methods.
- The admin caller passes the world-layer `checkAccess` through one narrow, reviewed policy —
  and is denied `delete` by that same policy.
- A mixed `description` + `profile.*` mask is one write, one version, one transaction, one envelope.
- A non-empty mask naming only equal-valued fields is a true no-op — no row rewrite, no version
  bump, no envelope — enforced inside the transaction by an admin-only option, with the player
  path's shipped unconditional-rewrite behaviour proven byte-identical.
- The one place the admin empty-mask answer diverges from the player facade (a stale
  `expected_version` → `Aborted`) is stated as a divergence with its own reason, not claimed as
  parity.
- ROADMAP Success Criterion 3 holds as amended by §14 row 9: envelope transactional, row
  projected, one writer.
- No prose and no alt linkage reaches `events_audit`.
</success_criteria>

<output>
Create `.planning/phases/06-admin-portal-shell-character-administration/06-05-SUMMARY.md` when done
</output>
