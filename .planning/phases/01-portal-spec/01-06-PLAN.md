---
phase: 01-portal-spec
plan: 06
type: execute
wave: 6
depends_on: ["01-05"]
files_modified:
  - .planning/phases/01-portal-spec/01-SPEC.md
  - CLAUDE.md
  - .claude/rules/invariants.md
autonomous: true
requirements: [PORTAL-10]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every backticked path:line citation in 01-SPEC.md resolves: the named construct is present in the named file at the named lines, checked at authoring time and recorded with the commit the check ran against."
    - "Section 16 lists the grounding citations as a flat, re-verifiable list a reviewer can walk without reading the whole SPEC."
    - "Section 16 states the git ref and date the citation sweep ran against, so a future reader knows citations are point-in-time and expected to drift."
    - "CLAUDE.md states in BOTH of its spec-location passages that GSD milestone specs live under the planning phase directories, with the historical spec tree named as historical."
    - "The invariants rule file auto-loads on GSD phase SPEC files, because its frontmatter paths list gains a matching glob."
    - "The invariants rule file gains one sentence noting a planning-tree SPEC is likewise not auto-caught by the orphan check and must be hand-registered."
    - "All sixteen SPEC sections are authored; no placeholder line naming a filling plan remains anywhere in 01-SPEC.md."
    - "Every requirement id PORTAL-01 through PORTAL-10 is traceable to at least one named SPEC section, recorded as a table in the plan SUMMARY."
    - "Every INV- id declared in SPEC section 13 exists as an id value in docs/architecture/invariants.yaml, and every v0.13 registry entry whose origin_spec names the SPEC path is declared in section 13 — the correspondence holds in both directions."
  prohibitions:
    - "The pointer edit MUST NOT change the invariants rule prose to claim the orphan check walks the planning tree — it does not, and a false gate claim is worse than a documented gap."
    - "The pointer edit MUST NOT rewrite links to specific historical design specs; those files still exist at those paths and relocating them is deferred work touching live CI gates."
    - "The citation sweep MUST NOT silently drop an unresolvable citation; an unresolvable citation is corrected or removed, and either way recorded."
  artifacts:
    - .planning/phases/01-portal-spec/01-SPEC.md
    - CLAUDE.md
    - .claude/rules/invariants.md
  key_links:
    - "SPEC section 13 invariant ids <-> docs/architecture/invariants.yaml entries whose origin_spec is the SPEC path (bidirectional set equality)"
    - "CLAUDE.md Documentation Structure passage <-> CLAUDE.md Spec-Driven Development passage (a partial edit leaves the file contradicting itself)"
    - "invariants rule frontmatter paths <-> the SPEC's own location (the rule must auto-load when a GSD SPEC is edited)"
---

<objective>
Close the phase: verify **every** `path:line` citation in the SPEC resolves, apply the D-19
spec-location pointer edit to both `CLAUDE.md` passages and the invariants rule, and run the
completeness gate over the whole document.

Purpose: `.claude/rules/references/design-review-learnings.md` and its plan-review sibling record
unverified or invented `path:line` citations as one of the most frequent review failures in this
repo — a plan that cites `manager.go:844` when the function starts at 849 sends a subagent to the
wrong place. The SPEC is the instruction source for five downstream phases; a wrong citation in it
propagates five times.

Output: a citation-verified SPEC with a grounding trace, and a repo whose spec-location convention
matches where this SPEC actually lives.
</objective>

<artifacts_this_phase_produces>
Authored by this plan: `01-SPEC.md` section `## 16. Grounding Trace`.
Modified by this plan: `CLAUDE.md` (the Documentation Structure passage and the Spec-Driven
Development passage), `.claude/rules/invariants.md` (frontmatter `paths:` plus one prose sentence).
Produced as records, not files: the requirement-to-section traceability table and the bidirectional
invariant correspondence check, both recorded in `01-06-SUMMARY.md`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-portal-spec/01-CONTEXT.md
@.planning/phases/01-portal-spec/01-PATTERNS.md
@.planning/REQUIREMENTS.md
@.claude/rules/invariants.md
@.claude/rules/references/design-review-learnings.md
@.claude/rules/references/plan-review-learnings.md
@.planning/phases/01-portal-spec/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify every path:line citation in the SPEC and author section 16 as the grounding trace</name>
  <files>.planning/phases/01-portal-spec/01-SPEC.md</files>
  <read_first>
    - `01-SPEC.md` in full — this task's subject is every citation in it
    - `.planning/phases/01-portal-spec/01-PATTERNS.md` sections "Grounding-trace section" and "path:line citation" under Shared Patterns
    - `.claude/rules/references/design-review-learnings.md` (the recurring unverified-citation failures)
    - `.claude/rules/references/plan-review-learnings.md` (the specific off-by-N and invented-symbol examples)
    - `.planning/REQUIREMENTS.md` lines 12-15 (the idiom for recording that citations were verified at a named ref)
  </read_first>
  <action>
    Extract every backticked citation in `01-SPEC.md` that names a file with a line or line range.
    Build the list mechanically from the document rather than from memory — a citation the extraction
    misses is a citation nobody checks.

    For each extracted citation, open the named file at the named lines and confirm the construct the
    surrounding SPEC prose claims is there is actually there. A line number that lands on a blank
    separator, a comment, or a different symbol is a failure, not a rounding error. Where a citation
    is off, correct the line numbers in the SPEC. Where a citation names a symbol, file, or task
    target that does not exist, remove the claim or restate it against something that does — never
    leave an unresolvable citation in place, and never silently delete one without recording it.

    Pay specific attention to the failure shapes the learnings catalogue: a cited range whose start
    is off by one because of a preceding blank line or doc comment; a symbol name that is plausible
    but is not the real identifier; a task name or command that does not exist in the Taskfile; and a
    file path that is the logical path rather than the on-disk path.

    Replace the section 16 placeholder with the grounding trace: a flat list of the citations,
    grouped by the area they ground (access control, world model, proto surface, admin surface,
    verification and invariants), each with a one-line parenthetical naming the construct at that
    line. Model it on the analog's grounding-trace section so a reviewer can walk the list without
    reading the whole SPEC.

    Open section 16 with a sentence stating the git ref and the date the sweep ran against, following
    the idiom REQUIREMENTS.md already uses, and stating explicitly that citations are point-in-time
    and are expected to drift as the tree moves — this SPEC is a design record, not a file whose
    citations a CI gate keeps green.

    Record in the SUMMARY: the total number of citations extracted, the number that resolved
    unchanged, the number corrected with before-and-after line numbers, and the number removed with
    the reason for each.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && rg -o '`[a-zA-Z0-9_./-]+\.(go|sql|proto|yaml|md|ts|lua):[0-9]+(-[0-9]+)?`' .planning/phases/01-portal-spec/01-SPEC.md | tr -d '`' | sed 's/:.*//' | LC_ALL=C sort -u > /tmp/gsd-cited-files.txt && test "$(wc -l < /tmp/gsd-cited-files.txt | tr -d ' ')" -ge 15 && while read -r f; do test -f "$f" || { echo "MISSING FILE: $f"; exit 1; }; done < /tmp/gsd-cited-files.txt && echo "all cited files exist"</automated>
  </verify>
  <acceptance_criteria>
    - Every file named in a `path:line` citation in `01-SPEC.md` exists on disk (the automated check exits 0 with the confirmation line).
    - The extraction is non-empty: the check asserts a floor on the number of distinct cited files before testing any of them, so a broken extraction regex cannot report success over an empty set. A loop that iterates zero times reports the same green as one that checked everything.
    - For every citation, the SUMMARY records one of three dispositions: resolved unchanged, corrected with before and after line numbers, or removed with a reason. The three counts sum to the total extracted.
    - Section 16 exists and groups the citations into the areas named in the action.
    - Section 16's opening sentence names a git ref and a date.
    - Section 16 states that citations are point-in-time and expected to drift.
    - No citation in `01-SPEC.md` points at a Taskfile target, a Go symbol, or a proto method that does not exist — spot-verified for at least every citation naming an executable target or an exported identifier.
  </acceptance_criteria>
  <done>Every citation in the SPEC has been opened and checked, corrections are recorded with before-and-after line numbers, and section 16 is a reviewer-walkable grounding trace stamped with the ref it was verified against.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the D-19 spec-location pointer edit to both CLAUDE.md passages and the invariants rule</name>
  <files>CLAUDE.md, .claude/rules/invariants.md</files>
  <read_first>
    - `.planning/phases/01-portal-spec/01-CONTEXT.md` D-19 and the Deferred Ideas entry describing the full retirement sweep and the live gates it touches
    - `.planning/phases/01-portal-spec/01-PATTERNS.md` section 4 in full — it quotes all three target passages verbatim and lists the files to leave alone
    - `CLAUDE.md` line 22 (the Documentation Structure passage) and lines 50-52 (the Spec-Driven Development passage)
    - `.claude/rules/invariants.md` lines 6-15 (frontmatter `paths:`) and the two prose passages describing the orphan check's walk root
    - `test/meta/invariant_registry_test.go` line 341 (the hard-coded walk root the prose describes)
  </read_first>
  <action>
    Make three scoped edits. Never rewrite a whole file.

    First, in the Spec-Driven Development passage, state that GSD milestone specs live under
    `.planning/phases/<phase>/<NN>-SPEC.md` and name the existing spec directories as historical.
    Keep every other clause in that passage intact — the RFC2119 requirement, the invariant-registry
    capture requirement, and the do-not-mint-ad-hoc-families clause all survive unchanged.

    Second, make the equivalent change in the Documentation Structure passage near the top of the
    file. This passage makes the same claim in different words, and an edit that lands in only one
    of the two leaves the file contradicting itself — which is precisely the pattern-mapper finding
    this task exists to close. Verify after editing that both passages now agree.

    Third, in the invariants rule, add a glob matching GSD phase SPEC files to the frontmatter
    `paths:` list, so the rule auto-loads whenever one is edited. Then append ONE sentence to the
    escape-hatch prose noting that a SPEC living under the planning tree is likewise not auto-caught
    by the orphan check and must be hand-registered.

    Leave the two existing prose passages describing the orphan check's walk root exactly as they
    are. They describe a live gate accurately, and D-18 relies on that gate's documented limitation.
    Do NOT edit them to claim any broader walk root than the code actually implements — confirm
    against the test file's walk root line before and after your edit that the prose still matches
    the code.

    Leave alone every file that links to a specific historical design spec by name. Those files
    point at documents that still exist at those paths, and relocating them is deferred work that
    touches the docs fast-lane classification gates.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(rg -c '\.planning/phases' CLAUDE.md)" -ge 2 && rg -q 'SPEC\.md' .claude/rules/invariants.md && rg -n 'docs.*superpowers.*specs' test/meta/invariant_registry_test.go | rg -q '341|WalkDir' && echo "pointer edit consistent with the live gate"</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` names the GSD phase SPEC location in BOTH the Documentation Structure passage and the Spec-Driven Development passage — at least two matches for the planning phase path.
    - The Spec-Driven Development passage still contains its RFC2119 clause, its invariant-registry capture clause, and its do-not-mint-ad-hoc-families clause.
    - `.claude/rules/invariants.md` frontmatter `paths:` contains a glob that matches `.planning/phases/01-portal-spec/01-SPEC.md`.
    - `.claude/rules/invariants.md` gains exactly one new sentence about the planning-tree SPEC not being auto-caught.
    - The two existing prose passages describing the orphan-check walk root are byte-identical to their pre-edit state, and the walk root they name still matches the root in `test/meta/invariant_registry_test.go`.
    - `git diff --stat` shows exactly two files changed by this task, `CLAUDE.md` and `.claude/rules/invariants.md`, and no file linking to a specific historical design spec is touched.
  </acceptance_criteria>
  <done>Both CLAUDE.md passages agree on the new spec location, the invariants rule auto-loads on GSD SPECs and documents the escape hatch, and no prose falsely widens a live gate.</done>
</task>

<task type="auto">
  <name>Task 3: Run the SPEC completeness gate — sections, requirement traceability, and invariant correspondence</name>
  <files>.planning/phases/01-portal-spec/01-SPEC.md</files>
  <read_first>
    - `01-SPEC.md` in full
    - `.planning/REQUIREMENTS.md` PORTAL-01 through PORTAL-10
    - `.planning/ROADMAP.md` the Phase 1 success criteria block (all five)
    - `docs/architecture/invariants.yaml` (every entry whose `origin_spec` names the SPEC path)
    - all five prior plan SUMMARYs in `.planning/phases/01-portal-spec/`
  </read_first>
  <action>
    Run three closure checks and record all three in the SUMMARY as tables, not prose.

    First, section completeness: confirm all sixteen numbered sections exist with the exact titles
    plan 01-01 fixed, in that order, and that no placeholder line naming a filling plan survives
    anywhere in the document. A surviving placeholder means a section was skipped, which is the one
    failure this gate exists to catch.

    Second, requirement traceability: build a table mapping each of PORTAL-01 through PORTAL-10 to
    the SPEC section or sections that discharge it, and mapping each of the five roadmap Phase 1
    success criteria to the same. Every requirement and every criterion MUST have at least one
    section. A requirement with no section is a gap; record it and stop rather than papering over it.

    Third, invariant correspondence in both directions: every `INV-` id declared in section 13 MUST
    exist as an `id:` value in the registry, and every registry entry whose `origin_spec` names the
    SPEC path MUST be declared in section 13. Set inequality in either direction is a failure. Record
    both sets and their comparison. Confirm each of those entries carries a pending binding and no
    asserted-by list, since none of their asserting tests exists yet.

    Re-run the registry meta-tests and the render drift check one final time over the finished state.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go run ./cmd/inv-render -check && task test -- -run 'TestRegistrySchemaParsesOwnershipFields|TestEveryRegistryInvariantHasBinding|TestRegistryBindingChecks|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted|TestOwnedPathsPartition' ./test/meta/ && test "$(rg -c '^## [0-9]+\. ' .planning/phases/01-portal-spec/01-SPEC.md)" = "16"</automated>
  </verify>
  <acceptance_criteria>
    - `01-SPEC.md` contains exactly sixteen `## N. Title` headings with the titles and order fixed by plan 01-01.
    - No line in `01-SPEC.md` names a filling plan as a placeholder.
    - The SUMMARY contains a table mapping each of PORTAL-01 through PORTAL-10 to at least one SPEC section, with no unmapped requirement.
    - The SUMMARY contains a table mapping each of the five roadmap Phase 1 success criteria to at least one SPEC section, with no unmapped criterion.
    - The SUMMARY records the set of `INV-` ids declared in section 13 and the set of registry entries whose `origin_spec` names the SPEC path, and states they are equal.
    - Every one of those registry entries shows a pending binding and carries no asserted-by list.
    - `go run ./cmd/inv-render -check` exits 0 and all six named meta-tests pass.
  </acceptance_criteria>
  <done>The SPEC is complete against its own skeleton, every requirement and success criterion maps to a section, and the declared and registered invariant sets are equal in both directions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this SPEC's citations -> five downstream phase executors | a wrong citation sends a subagent to the wrong code and propagates once per phase |
| CLAUDE.md's spec-location claim -> every future contributor and agent | two passages disagreeing means whichever is read first wins |
| the invariants rule prose -> a maintainer relying on the orphan check | prose claiming a broader walk root than the code implements is a false gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-33 | Tampering | SPEC citation accuracy | high | mitigate | Extract citations mechanically from the document and open every one; record each as resolved, corrected with line numbers, or removed with a reason, so no citation is checked by assumption. |
| T-01-34 | Spoofing | Invariants rule prose vs the live gate | high | mitigate | Leave the walk-root prose byte-identical and verify it still matches the code's walk root; add the planning-tree note as a new sentence rather than by widening an existing claim. |
| T-01-35 | Repudiation | Partial pointer edit | medium | mitigate | Require both CLAUDE.md passages to change together and assert at least two matches, because a single-passage edit leaves the file self-contradicting with no signal. |
| T-01-36 | Information disclosure | Invariant set drift | medium | mitigate | Compare the declared and registered invariant sets in both directions; a registered entry not declared in the SPEC is an invariant nobody can find the origin of. |
| T-01-37 | Denial of service | Citation drift over time | low | accept | SPEC citations will rot as the tree moves. Accepted deliberately: the sweep stamps the ref it ran against and states citations are point-in-time, rather than committing a checker that would force citation maintenance on a frozen design record. |
</threat_model>

<verification>
- Every cited file exists and every cited line range was opened and checked; dispositions are recorded and their counts sum to the extracted total.
- Section 16 exists, is grouped by area, and names the ref and date of the sweep.
- Both CLAUDE.md spec-location passages agree; the invariants rule auto-loads on GSD SPECs; the walk-root prose is unchanged and still matches the code.
- All sixteen sections are authored with no surviving placeholder.
- PORTAL-01 through PORTAL-10 and all five roadmap Phase 1 success criteria each map to at least one section.
- The declared and registered invariant sets are equal in both directions, all pending, none with an asserted-by list.
- `go run ./cmd/inv-render -check` exits 0 and the six registry meta-tests pass.
</verification>

<success_criteria>
The SPEC is committed, complete, citation-verified against a named ref, and the repo's own
spec-location convention points at where it lives. A reviewer can walk section 16 and re-verify the
grounding without reading the document.
</success_criteria>

<output>
Create `.planning/phases/01-portal-spec/01-06-SUMMARY.md` when done.
</output>
