---
phase: 02-abac-schema-vocabulary
plan: 11
type: execute
wave: 5
depends_on: ["02-02", "02-04", "02-05", "02-06", "02-08", "02-09", "02-10"]
files_modified:
  - .planning/phases/01-portal-spec/01-SPEC.md
  - .planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md
autonomous: true
requirements: [IDENT-06, IDENT-07, IDENT-08, IDENT-09, PROFILE-11, EXT-07]

estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "01-SPEC.md §8.5.1.1 records option 2 as REJECTED with its reason, and records D-01's viewer-flavored twins as the settled shape — so a later reader does not re-open the choice the phase already made."
    - "01-SPEC.md §11.3's permitted sort/filter table gains a last_active_at row and a joined players.username row; the existing characters.player_id row ('never an ordering') stays unchanged, because A2 is a different claim rather than a contradiction of it."
    - "01-SPEC.md's stale internal/store/migrations/*.up.sql citations are corrected to the current single-file goose paths and line numbers, so a later planner transcribing one does not produce an Edit-tool failure."
    - "01-SPEC.md §6.1.2 records that Mechanism A is implemented over the Script property from stdlib unicode.Scripts rather than Script_Extensions, that the approximation can only miss a rejection and never add one, and that a rune with no script assignment is rejected fail-closed."
    - "Every amendment row is APPLIED to its own artifact, and each application is proven by a recorded search for the superseded string rather than asserted — recording an amendment is not applying it."
    - "02-VALIDATION.md's Per-Task Verification Map is filled with every task in this phase, its frontmatter status is set to validated, and nyquist_compliant reflects the observed state rather than an aspiration."
    - "abac-reviewer has reviewed the phase diff and returned READY before the phase merges."
  artifacts:
    - .planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md
  key_links:
    - "01-SPEC.md §14's amendments table gains one row per amendment, each quoting the superseded text verbatim alongside its artifact and location, because the quoting is what forces the search that catches a string still live somewhere nobody inventoried."
    - "Each amendment's search target is a substring lying on a SINGLE source line — a multi-line quote searched line-wise returns zero matches whether or not the text survives, which would be a proof that cannot fail."
---

<objective>
Close the phase: apply the four SPEC amendments this phase owes, fill the
validation map, and route the diff to `abac-reviewer`.

Purpose: §14's own preamble records the failure this plan exists to avoid —
*recording an amendment is not applying it.* A downstream planner reads the
artifact directly, so a §8.5.1.1 that still presents option 2 as live will be
planned against regardless of what a table elsewhere says about it.
`.claude/rules/references/design-review-learnings.md` names exactly this — an
amendment row whose superseded string is still live in a sibling artifact — as a
recurring review failure.
Output: four applied amendments with recorded search proofs, a filled
`02-VALIDATION.md`, and an `abac-reviewer` verdict.

**This plan writes only to `.planning/` artifacts.** It ships no code, no schema and
no policy. Its requirement list carries all six phase requirement IDs because it is
the plan that records their discharge and closes the traceability loop, not because
it implements any of them — every one is implemented in plans `02-01` through
`02-10`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md
@.planning/phases/02-abac-schema-vocabulary/02-RESEARCH.md
@.planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md
@.planning/phases/01-portal-spec/01-SPEC.md
@.claude/rules/planning-artifacts.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Apply the four SPEC amendments and prove each landed</name>
  <files>.planning/phases/01-portal-spec/01-SPEC.md</files>
  <read_first>
    - .planning/phases/01-portal-spec/01-SPEC.md §14 in full — especially the preamble ("recording an amendment is not applying it"), the note on quotations and single-line search targets, and the ten existing rows as the format to extend
    - .planning/phases/01-portal-spec/01-SPEC.md §8.5.1.1 (the section being amended), §11.3 (the permitted sort/filter table), §6.1.2 (Mechanism A), and every `internal/store/migrations/*.up.sql` citation in the file
    - .planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md D-05, D-26
    - .planning/phases/02-abac-schema-vocabulary/02-RESEARCH.md § Concerns items 1 and 5
    - .planning/phases/02-abac-schema-vocabulary/02-02-SUMMARY.md and 02-07-SUMMARY.md (what actually shipped, so the amendments describe the code rather than the plan)
    - .claude/rules/planning-artifacts.md (do not invent structure in a tool-owned file; §14's table is the sanctioned shape and is what to extend)
  </read_first>
  <action>
Apply four amendments, each **in place in §14's table AND in the affected section**,
and record a single-line search proof for each in the SUMMARY.

**Amendment A — §8.5.1.1, term B's shape (D-05).** Rewrite the section to record
that **option 2 is REJECTED**: term B evaluating against a co-located character
subject and DENYING where one does not exist violates §8.8 / INV-PRIVACY-10 at the
`anonymous` rung. An anonymous viewer has no character; `profile.pronouns` is an
`entity_properties` row seeded at the `anonymous` floor; so term B would deny it and
a reachable profile would carry `name` without `pronouns`. The failure is **closed**,
so no test in the suite would catch it — it surfaces as "the public profile looks
bare", which is precisely the symptom §8.5.1.1 warns provokes the forbidden repair.
Record D-01's viewer-flavored twins as the settled shape, name the five shipped
twin policies, and record that `seed:property-owner-write` deliberately has none.
Keep the section's core prohibition — removing term B is a normative violation, not
a tuning decision — verbatim.

**Amendment B — §11.3, two new sort/filter rows (D-26).** Add a `characters.last_active_at`
row (Sort: Yes, Filter: Yes) with its safety rationale: it is intrinsic row metadata
carrying no profile content, and the `0` epoch sentinel sorts never-active rows last
under most-recent-first without a `NULLS LAST` clause. Add a joined
`players.username` row for the linkage the admin list already sorts by and §11.3
never enumerated. **Leave the existing `characters.player_id` row unchanged** — its
"never an ordering" verdict stays correct, and the new row is a different claim
rather than a contradiction of it. Say so explicitly in the new row, because a
reader seeing two player-linkage rows will otherwise read one as superseding the
other.

**Amendment C — stale migration citations.** The goose cutover collapsed every
`.up.sql`/`.down.sql` pair into one `.sql` file and shifted every line number, so
**every** `internal/store/migrations/*.up.sql` citation in `01-SPEC.md` is stale in
both filename and line number. Enumerate them with a search, re-derive each against
the current tree, and correct them. Do not transcribe corrected values from
`02-RESEARCH.md` — re-derive, because the tree has moved again since it was written.
Record the count found and the count corrected.

**Amendment D — §6.1.2's Script-vs-Script_Extensions divergence.** Record that
Mechanism A is implemented over the **Script** property from Go's stdlib
`unicode.Scripts`, not over **Script_Extensions**, which UTS #39's Moderately
Restrictive profile is defined over; that no Go module surveyed supplies
Script_Extensions and `golang.org/x/text` has no script package at all; that the
approximation can only ever **miss** a rejection the faithful profile would make and
never add one, and is closed-direction for §6.1.2's three named rejections because
Cyrillic and Greek letters carry real Script values; and that a rune with no script
assignment — the Unicode-15-stdlib versus Unicode-17-confusables skew — is
**rejected** fail-closed. Record it as a divergence with its rationale, in the shape
§14 row 10 uses for the grid-parity divergence, so a reviewer reads it as a choice
rather than an oversight.

**Prove each amendment landed.** For each, pick a search target that is a substring
lying on a **single** source line — a multi-line quote searched line-wise returns
zero matches whether or not the text survives, which would be a proof that cannot
fail — run the search, and record the target and its result in the SUMMARY.
  </action>
  <verify>
    <automated>rg -c 'migrations/[0-9]*_[a-z_]*\.up\.sql' .planning/phases/01-portal-spec/01-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'migrations/[0-9]*_[a-z_]*\.up\.sql' .planning/phases/01-portal-spec/01-SPEC.md` returns 0
    - `rg -n 'option 2' .planning/phases/01-portal-spec/01-SPEC.md` shows it described as rejected, and the section names the five shipped viewer twin policies
    - §11.3's table contains a `characters.last_active_at` row and a `players.username` row, and its `characters.player_id` row is byte-identical to its previous content
    - §6.1.2 contains the words `Script_Extensions` and a statement that the approximation can only miss a rejection
    - §14's table has four new rows, each quoting its superseded text verbatim alongside artifact and location
    - The SUMMARY records, for each of the four amendments, the single-line search target used and its result
    - `.planning/ROADMAP.md` and `.planning/STATE.md` are NOT edited by this task — no version-bearing or check-marked `###` heading is added anywhere, per `.claude/rules/planning-artifacts.md`
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation edits to a phase artifact; reversible by a revert.</reversibility>
  <done>Four amendments are applied in place and in §14, each proven landed by a recorded single-line search rather than asserted.</done>
</task>

<task type="execute">
  <name>Task 2: Fill the validation map and set its status honestly</name>
  <files>.planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md (the whole file — the Per-Task Verification Map's column set, the RED-first table, the Wave 0 checklist and the sign-off list)
    - .planning/phases/02-abac-schema-vocabulary/02-RESEARCH.md § Validation Architecture → "Phase Requirements → Test Map" (the 31 rows every one of which must land on a task)
    - Every `02-*-SUMMARY.md` produced by plans 02-01 through 02-10
  </read_first>
  <action>
Fill `02-VALIDATION.md`'s Per-Task Verification Map with one row per task across
plans `02-01` through `02-10`, carrying: task id, plan, wave, requirement, threat
ref (the `T-02-NN` id from that plan's threat register), secure behavior, test type,
automated command, whether the file existed, and observed status.

Then walk **all 31 rows** of `02-RESEARCH.md`'s "Phase Requirements → Test Map" and
confirm each lands on a task in the filled table. Record the mapping explicitly.
Any row that does **not** land is a gap: record it as a gap with the reason, and file
a GitHub issue (`gh issue create -R holomush/holomush --label bug`) rather than
quietly dropping it or marking the phase compliant.

Update the RED-first table with the three demonstrations and their observed
outcomes: the name-uniqueness gate against the staged `000053` schema (plan
`02-06`), the fourth-rung clearing test against an ordinal implementation (plan
`02-08`), and the additive-permit regression with term B removed (plan `02-08`).
Each row records the recorded non-zero exit, not a claim that one would have
occurred.

Set the frontmatter `status: validated` and set `nyquist_compliant` to what was
actually observed. If Docker was unavailable for any integration tier, set it
`false` and say which criteria were therefore unproven — the field distinguishes
NOT-VALIDATED from PARTIAL for the milestone audit, and a `true` that was not
observed defeats that distinction.

Do not edit `.planning/ROADMAP.md` or `.planning/STATE.md` in this task. The
orchestrator owns ROADMAP plan-list updates through `gsd roadmap`, and inventing
structure in a tool-owned parsed file silently changes what the tool sees.
  </action>
  <verify>
    <automated>rg -c 'pending — allocated when' .planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'pending — allocated when' .planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md` returns 0 — the placeholder row is gone
    - The Per-Task Verification Map has one row per task across plans 02-01..02-10, each carrying a `T-02-NN` threat ref and an automated command
    - The SUMMARY records the 31-row mapping from RESEARCH's test map to tasks, with any unmapped row named and its issue number recorded
    - The RED-first table's three rows each record an observed outcome, not a projected one
    - The frontmatter carries `status: validated` and a `nyquist_compliant` value matching what the SUMMARY reports as observed
    - `git diff --stat .planning/ROADMAP.md .planning/STATE.md` shows no change from this task
  </acceptance_criteria>
  <reversibility rating="reversible">A planning artifact edit.</reversibility>
  <done>Every task has a verification row, all 31 test-map rows are accounted for, and the compliance flag reports the observed state.</done>
</task>

<task type="execute">
  <name>Task 3: Route the phase to abac-reviewer and run the phase gate</name>
  <files>(no files created or modified — review pass and gate run)</files>
  <read_first>
    - .planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md D-05 (the explicit routing obligation and why abac-reviewer specifically — it identified the §8.5.1.1 residual and has the context to confirm the rejection does not trade one hole for another)
    - .planning/phases/02-abac-schema-vocabulary/02-07-SUMMARY.md and 02-08-SUMMARY.md and 02-09-SUMMARY.md (the ABAC surface the reviewer is being asked about)
    - .claude/rules/landing-the-plane.md
    - CLAUDE.md § Pre-Push Review Gates
  </read_first>
  <action>
Run `abac-reviewer` (`/holomush-dev:review-abac`) over the phase diff. It is
**mandatory**: the diff touches `internal/access/` throughout, and D-05 routes the
§8.5.1.1 finding to it by name. Brief it explicitly on the three questions this
phase resolved that it is best placed to check:

1. **D-02's rejection of option 2** — does the viewer-flavored-twins shape close the
   §8.5.1.1 residual without trading one hole for another?
2. **The term-A/term-B separation by action token** (`read_profile_attribute` vs
   `read`) — does it actually keep the two families in separate evaluations, given
   `combineDecisions` combines permits disjunctively?
3. **Open Question 1's resolution** — two `seed:profile-public-read-*` entries under
   one conceptual name, with a bare-`resource` single policy explicitly rejected.

Address every finding it returns: fix, or record why not applicable. Do not merge on
a NOT READY.

Note that `crypto-reviewer` is **not** triggered: nothing in this phase touches
`internal/eventbus/crypto/`, `codec/`, `history/dispatcher.go`,
`cold_postgres.go`, `event_emitter.go::Emit`, `audit/projection.go`, a
`crypto.emits` manifest declaration, or a `crypto_keys` / `events_audit` migration.
Record that as a stated absence rather than silently skipping the gate.

Then run the phase gate: `task pr-prep` **inline in the parent session** — this is
the one command exempt from the `local-check` delegation rule — and `task test:int`.
Commit any files `task fmt` mutated (SPDX headers on this phase's many new files
are the expected case), because uncommitted `fmt` output is a common cause of red
CI on an otherwise-green branch.
  </action>
  <verify>
    <automated>task pr-prep</automated>
    <human-check>Read `abac-reviewer`'s verdict in full. It MUST be a binary READY or NOT READY with every finding citing `path:line`. Do not proceed on "mostly ready with minor concerns" — the agent is constructed to return a binary verdict, so an equivocal one is itself a finding.</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records `abac-reviewer`'s verdict verbatim and, for each finding, whether it was fixed or why it is not applicable
    - The SUMMARY states explicitly that `crypto-reviewer` is not triggered and why
    - `task pr-prep` exits 0, run inline in the parent session
    - `task test:int` exits 0, or the SUMMARY records which integration criteria could not run and why
    - `git status` shows a clean working tree after `task fmt`'s edits are committed
    - No commit on this branch carries a `[ci skip]` or equivalent directive
  </acceptance_criteria>
  <reversibility rating="reversible">A review pass and a gate run.</reversibility>
  <done>`abac-reviewer` has returned READY on the phase's authorization surface, the phase gate is green, and formatter output is committed.</done>
</task>

</tasks>

<verification_integrity>
The six PORTAL-10 / §12.1 rules at their fixed numbering, specialized to this plan.

1. **Census with set equality.** This plan ships no census. Phase 2's rule-1 census
   scope is the character-name write-site census in plan `02-06`, and this plan's
   Task 2 records that it landed. EXT-04's admin section-registry ↔ descriptor
   census remains Phase 6's; Task 2 records that Phase 2 did not claim it. The
   31-row test-map reconciliation in Task 2 is a coverage check over a hand-written
   list, not a census, and is not offered as one.
2. **Paired positive control on every denial test.** This plan ships **no tests at
   all** — it edits planning artifacts and runs gates. Stated explicitly rather
   than omitted: rule 2 is not satisfied here by the absence of the tests it
   governs. Task 2's obligation is to confirm the pairing landed in the plans that
   do ship denial tests — `02-02`, `02-04`, `02-05`, `02-06`, `02-08`, `02-09` and
   `02-10` — and to record any that did not.
3. **Assertions against marshaled response bytes.** Does not apply — no code and no
   response shape in this plan. Task 2 records that the milestone's wire-level
   absence assertion (INV-ACCESS-11) is correctly deferred to Phase 4 rather than
   claimed here.
4. **Gates demonstrated RED against the pre-fix state.** This plan's own gate is
   Amendment C's citation check, which MUST be observed failing before the
   corrections land — run the `.up.sql` search first, record the non-zero count, then
   correct and record zero. Task 2 additionally verifies the phase's three
   substantive RED demonstrations were **observed and recorded**, not projected; a
   row in the RED-first table that says "would have failed" is itself a finding.
5. **Wire-level assertion of every opacity and authorization contract.** No wire
   surface in this plan. Its contribution to rule 5 is Amendment A, which records
   why option 2's failure mode is invisible to the test suite — a closed failure
   with no symptom — so that a later reader understands why the D-04 regression test
   in plan `02-08` is load-bearing rather than redundant.
6. **Invariant-scope discipline.** This plan registers no invariant and writes no
   `// Verifies:` annotation. It records the phase's final registry state: three
   entries bound (INV-WORLD-5 and INV-WORLD-6 in plan `02-04`, INV-PRIVACY-11 in
   plan `02-09`), and five staying `pending` with no `asserted_by` — INV-ACCESS-10,
   INV-ACCESS-11, INV-ACCESS-12, INV-PRIVACY-9 and INV-PRIVACY-10 all bind in
   Phase 4, and INV-WORLD-7 in Phase 4 with its commands landing in Phase 3. All
   sit in existing scopes; no family was minted.
</verification_integrity>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact → downstream planner | A superseded statement left live in a directly-read artifact is planned against regardless of any amendment table. |
| phase diff → merge | The last point at which an authorization regression can be caught before it is in `main`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-67 | Tampering | unapplied amendment | high | mitigate | §8.5.1.1 left presenting option 2 as live would be planned against in Phase 4, reintroducing the §8.8 violation D-02 rejected. Closed by applying each amendment in place and proving it with a recorded single-line search rather than asserting it. |
| T-02-68 | Elevation of privilege | unreviewed authorization surface | high | mitigate | The phase's entire subject is the authorization boundary. Closed by the mandatory `abac-reviewer` gate with a binary verdict and a briefing naming the three questions this phase resolved. |
| T-02-69 | Repudiation | projected rather than observed RED demonstrations | high | mitigate | A gate never seen failing is indistinguishable from a gate that cannot fail, and the phase carries three of them. Closed by Task 2 verifying each recorded a non-zero exit, and by treating "would have failed" as a finding. |
| T-02-70 | Tampering | invented structure in a parsed planning file | medium | mitigate | A version-bearing or check-marked `###` heading in `ROADMAP.md` truncates `extractCurrentMilestone`'s scope silently, producing confident wrong answers with no error. Closed by this plan writing only to `01-SPEC.md` and `02-VALIDATION.md`, asserted by a no-diff criterion on ROADMAP and STATE. |
| T-02-71 | Repudiation | overstated compliance flag | medium | mitigate | A `nyquist_compliant: true` that was not observed defeats the milestone audit's NOT-VALIDATED-versus-PARTIAL distinction. Closed by setting it from the observed state and naming any unproven criteria. |
| T-02-72 | Tampering | `[ci skip]` on a ship-note commit | medium | mitigate | A CI-skip directive on a branch tip suppresses the docs stand-in too, leaving required checks with no run for that SHA and a merge blocked on checks that will never arrive. Closed by the explicit prohibition and its acceptance criterion. |
</threat_model>

<verification>
- `task pr-prep` green, run inline in the parent session.
- `task test:int` green, or its gaps recorded.
- `abac-reviewer` READY.
- `rg -c 'migrations/[0-9]*_[a-z_]*\.up\.sql' .planning/phases/01-portal-spec/01-SPEC.md` returns 0.
- `02-VALIDATION.md` has no placeholder row and carries `status: validated`.
- `git status` clean.
</verification>

<success_criteria>
- Four amendments applied in place and recorded in §14, each proven by a recorded single-line search.
- Every stale `.up.sql` citation in `01-SPEC.md` corrected.
- All 31 RESEARCH test-map rows mapped to tasks, with any gap filed rather than dropped.
- The three RED-first demonstrations recorded as observed.
- `abac-reviewer` READY; `crypto-reviewer` stated as not triggered.
- `task pr-prep` green with formatter output committed.
</success_criteria>

## Artifacts this plan produces

No new Go symbols, no schema, no policy. Edits only:
`.planning/phases/01-portal-spec/01-SPEC.md` (§6.1.2, §8.5.1.1, §11.3, §14, and
every stale migration citation) and
`.planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md` (the Per-Task
Verification Map, the RED-first table, and the frontmatter status flags).

<output>
Create `.planning/phases/02-abac-schema-vocabulary/02-11-SUMMARY.md` when done.
</output>
</content>
