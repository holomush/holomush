---
phase: 02-abac-schema-vocabulary
plan: 06
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-05"]
files_modified:
  - internal/charname/admission.go
  - internal/charname/admission_test.go
  - test/meta/character_name_admission_test.go
  - internal/world/repository.go
  - internal/world/postgres/character_repo.go
  - internal/world/postgres/character_repo_test.go
  - internal/world/postgres/negative_path_test.go
  - internal/world/postgres/identity_backfill.go
  - internal/world/postgres/identity_backfill_test.go
  - internal/world/worldtest/mock_CharacterRepository.go
  - internal/auth/character_genesis.go
  - internal/auth/character_service.go
  - internal/auth/character_service_test.go
  - internal/auth/guest_service.go
  - internal/auth/guest_service_test.go
  - internal/auth/mocks/mock_CharacterRepository.go
  - internal/auth/mocks/mock_GuestCharacterRepository.go
  - internal/bootstrap/setup/adapters.go
  - internal/testsupport/integrationtest/harness.go
  - test/integration/auth/auth_suite_test.go
autonomous: true
requirements: [IDENT-09]

estimate:
  tokens: 116000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A character display name can be written to the database ONLY by passing a charname.Admitted value, and charname.Admitted has exactly one constructor — (*charname.Gate).Admit — so a write site that does not run the gate cannot be expressed in Go rather than merely being caught by a test."
    - "characters.name is not writable through CharacterRepository.Update: after this plan Update's statement writes description and location_id only, so the pre-existing ungated name write at internal/world/postgres/character_repo.go:79 no longer exists."
    - "Every SQL statement in internal/world/postgres that writes characters.name writes normalized_name, name_skeleton and name_skeleton_unicode_version in the SAME statement, so a display name can never be updated while its identity columns go stale — which would silently defeat the UNIQUE index over normalized_name."
    - "The admission census is derived from the tree by parsing internal/world/postgres and internal/charname with go/parser, never from a second hand-maintained list, and its expected set of name-admitting methods is compared by set equality with a symmetric-difference diff; inequality in either direction is RED."
    - "The census is demonstrated RED against the tree as it actually stood before this plan — where Update writes name with no charname.Admitted in scope and without the identity columns — and the recorded non-zero exit appears in this plan's SUMMARY."
    - "Uniqueness of a character name is asked as exact byte equality on the stored normalized key, not as LOWER(name): all five ExistsByName implementations move together to ExistsByNormalizedName over normalized_name = $1."
    - "The guest provisioning path runs the same gate the player create path runs: acquireUniqueName obtains a charname.Admitted before it can persist anything, and a generated name the gate rejects is retried by the loop that already exists."
    - "internal/world/postgres does not import internal/store, so the writer boundary stays importable from internal/store/migrations without an import cycle."
  artifacts:
    - internal/charname/admission.go
    - test/meta/character_name_admission_test.go
    - internal/world/postgres/identity_backfill.go
    - internal/world/repository.go
  key_links:
    - "charname.Admitted is the seam that converts criterion 1's 'at rename' half from a promise into a compile-time fact: Phase 3's RenameCharacter must call CharacterRepository.Rename, whose only name parameter is a charname.Admitted, whose only constructor is the gate."
    - "test/meta/character_name_admission_test.go is the COMPLEMENT of test/meta/world_sql_fence_test.go, not a duplicate: the fence proves no characters mutation SQL lives outside internal/world/postgres; this census proves that inside that boundary, every name write is gated and identity-coherent. Neither is sufficient alone, and this plan runs both."
    - "All five ExistsByName sites move together: internal/bootstrap/setup/adapters.go (the shared query), internal/auth/character_service.go and internal/auth/guest_service.go (the two interface declarations), internal/testsupport/integrationtest/harness.go (the harness adapter), and test/integration/auth/auth_suite_test.go:297 (a FIFTH adapter carrying its own independent LOWER(name) query)."
  prohibitions:
    - "MUST NOT add a second exported way to construct a charname.Admitted — no FromString, no Unchecked, no Raw, no struct literal with exported fields. The type's guarantee IS its single constructor; a convenience escape hatch voids the whole plan."
    - "MUST NOT satisfy the identity-coherence rule by adding a database trigger or generated column. .claude/rules/database-migrations.md forbids triggers and functions outright; the coherence lives in the single SQL statement inside the writer boundary."
---

<objective>
Make `charname.Gate` the **only** way a character display name can reach the
database, structurally rather than by convention: introduce the `charname.Admitted`
admission token, make it the sole name parameter of every name-writing method in
the sanctioned writer boundary, and prove both facts with a tree-derived census
demonstrated RED first.

Purpose: IDENT-09 and success criterion 1 require the name checks at create **and**
at rename. No rename path exists yet — `RenameCharacter` is Phase 3's — so the
rename half can only be discharged **structurally**. The previous shape of this
plan tried to discharge it with a census over SQL string literals, which cannot
work: a Phase 3 `RenameCharacter` that calls the pre-existing
`CharacterRepository.Update` introduces no new SQL literal, so the census would
stay green while the new write site bypassed the gate — exactly the case it exists
to catch. This plan replaces that with a type the gate alone can mint, and demotes
the census to a backstop over the remaining freedom.
Output: `charname.Admitted` + `Gate.Admit`; a writer boundary whose `Create` and
new `Rename` take the token and whose `Update` no longer writes `name` at all; the
five-site `ExistsByNormalizedName` migration; and the admission census in
`test/meta/`.

**Writer-boundary constraint (the reason this plan is shaped this way).**
`test/meta/world_sql_fence_test.go` is the RAW-WORLD-SQL FENCE binding
`INV-WORLD-4` / the writer-boundary invariant. Its Go scan (`:188-232`) walks the
whole tree and exempts ONLY `internal/world/postgres` (`writerBoundaryDir`,
`:134`), `_test.go` files, generated files, and a skip set that its own self-test
at `:306-309` proves **excludes `internal/store`** and does not cover `cmd/`.
Critically, the `world-sql-fence:allow` escape hatch exists only in
`scanMigrationForWorldDML` (`:113-129`), which globs `*.sql` — **there is no marker
path for `.go` files.** So any `characters` mutation SQL this phase plants in
`internal/store/migrations/*.go` or in `cmd/` is an unexemptable fence violation.
Everything that mutates `characters` therefore lands as a method inside
`internal/world/postgres`, and the migration and the CLI (plan `02-12`) become thin
callers with no SQL literal of their own. The fence itself is NOT modified: it is a
bound-invariant guard, and weakening it to accommodate this phase would be trading
a durable guarantee for a convenience.

**Pre-existing ungated write path (the reason `Update` changes).**
`internal/world/postgres/character_repo.go:79` is
`UPDATE characters SET name = $2, description = $3, location_id = $4, version = version + 1`
— a generic `Update` that writes `characters.name` today through neither
`world.NormalizeCharacterName` nor the gate. Left alone it would also write a
`name` whose `normalized_name` went stale, silently defeating the UNIQUE index
plan `02-12` lands over that column. Its ONLY production caller is
`internal/world/service.go:798` `UpdateCharacterDescription`, which reads the
character, sets `Description`, and round-trips the just-read `name` back. Dropping
`name` from that statement is therefore **behaviour-preserving for the only caller**
and closes the hole.

**Scope note (recorded, not a reduction).** This plan was split out of a single
oversized `02-06`. The migration chain (`000055` Go backfill + `000056`
`SET NOT NULL` then `CREATE UNIQUE INDEX`), the operator resolution CLI, the
RED-first concurrent-claim proof and the NFKC-only duplicates spec all land in
**plan `02-12`** (wave 5), which depends on this one. Success criteria 1 and 2 are
**not** amended; they are discharged across the pair.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md
@.planning/phases/02-abac-schema-vocabulary/02-RESEARCH.md
@.planning/phases/02-abac-schema-vocabulary/02-PATTERNS.md
@.planning/phases/01-portal-spec/01-SPEC.md
@.claude/rules/testing.md
@.claude/rules/invariants.md
</context>

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: The admission token, and the census demonstrated RED against the pre-fix tree</name>
  <files>internal/charname/admission.go, internal/charname/admission_test.go, test/meta/character_name_admission_test.go</files>
  <read_first>
    - test/meta/world_sql_fence_test.go — the WHOLE file, and before writing anything. It is both the constraint this plan works within and the worked example for the census: `scanGoStringLiteralsForWorldWriteSQL` (`:84-106`) is the parse-Go-not-grep technique to copy, `worldWriteSQLRegexp` (`:75-78`) the word-boundary-anchored matcher shape, `isWriterBoundaryPath` (`:138-141`) the boundary predicate to reuse in substance, and `TestWorldSQLFenceFlagsMutationOutsideBoundary` / `TestWorldSQLFenceIgnoresReadsAndComments` (`:256-288`) the synthetic-fixture positive-control style every new rule must carry
    - test/meta/meta_helpers_test.go lines 1-60 (`skipDirs` and `findRepoRoot`, both of which the census reuses)
    - internal/charname/gate.go (`Gate`, its `SkeletonLookup` and `BlockList` dependencies, and `Check`'s exact signature as left by plans 02-01, 02-02 and 02-05 — `Admit` wraps it and MUST NOT change it)
    - internal/charname/pipeline.go and internal/charname/skeleton.go (`Normalize`, the `Normalized` type and its key field, `Skeleton`, `UnicodeVersion`)
    - internal/world/postgres/character_repo.go lines 46-110 (`Create` and `Update` as they stand TODAY — this is the state the census must go RED against)
    - .planning/phases/01-portal-spec/01-SPEC.md §6.1.1, §6.1.3 and §12.1 rules 1 and 4 (the census definition with its non-vacuity clause, and the RED-first requirement with its non-vacuity clause)
  </read_first>
  <behavior>
    - `(*Gate).Admit` on a name the gate accepts returns an `Admitted` whose `Display()` is the submitted name's display form, whose `Key()` is the §6.1.1 normalized key, whose `Skeleton()` is the UTS #39 skeleton, and whose `UnicodeVersion()` is `charname.UnicodeVersion`.
    - `(*Gate).Admit` on a name the gate rejects returns the zero `Admitted` and the error `Check` produced, unwrapped and un-re-coded — `NAME_CONFUSABLE`, `NAME_BLOCKED` and the mixed-script code all survive `Admit` unchanged.
    - The zero `Admitted` reports `IsZero() == true`; an `Admitted` returned by a successful `Admit` reports `IsZero() == false`.
    - The census flags a synthetic writer-boundary function that writes `characters.name` and has no `charname.Admitted` parameter.
    - The census flags a synthetic writer-boundary function that takes a `charname.Admitted` and writes `characters.name` without also writing `normalized_name`, `name_skeleton` and `name_skeleton_unicode_version` in the same statement.
    - The census flags a synthetic second function in `internal/charname` whose results include `Admitted`.
    - The census does NOT flag a statement that writes the three derived identity columns without writing `name` (the backfill shape), nor a `SELECT` over `characters`, nor a comment mentioning the columns.
    - The census does NOT flag a `_test.go` fixture that writes `characters.name` with no `charname.Admitted` in scope — `_test.go` files are outside the scanned set, exactly as they are for the fence.
  </behavior>
  <action>
Write `internal/charname/admission.go`. Define `Admitted` as an **opaque value
type**: its populated form is unexported state, so the only value any other package
can build by hand is the zero value, and the zero value is detectable via
`func (a Admitted) IsZero() bool`. Expose read-only accessors `Display()`, `Key()`,
`Skeleton()` and `UnicodeVersion()`. Define `func (g *Gate) Admit(ctx context.Context, submitted string) (Admitted, error)`
as the SOLE constructor: it calls `g.Check`, returns the zero value and `Check`'s
error verbatim on rejection, and mints the populated value on acceptance.

`Admit` is additive — do **not** change `Gate.Check`'s signature or behaviour.
`Check` remains the verdict function that plans 02-01, 02-02 and 02-05 built and
tested; `Admit` is the admission token minted from a successful verdict. Record
that split in the file's doc comment, along with the reason the type exists: it is
the compile-time half of success criterion 1's "at rename", in a phase where no
rename path exists to assert against.

Write `internal/charname/admission_test.go` covering each `<behavior>` row that
concerns `Admit`, using the gate doubles plan 02-01 established.

Write `test/meta/character_name_admission_test.go` as **Phase 2's §12.1 rule-1
census**. It parses the tree with `go/parser` — never a second hand-maintained list
— and enforces four rules, each with its own synthetic-source positive control in
the style of `TestWorldSQLFenceFlagsMutationOutsideBoundary`.

**Scanned file set — state it in the rules, do not leave it implied.** All four
rules scan `.go` files only, excluding `_test.go` and excluding generated files,
mirroring the fence's own Go scan (`world_sql_fence_test.go:211` is the `_test.go`
skip, `:222` the generated-file skip; reuse `isGeneratedGo` in substance). Inherit
that filter deliberately rather than by luck: `character_repo_test.go`'s
stale-version CAS fixture issues a direct versioned `UPDATE` assigning the `name`
column from a test function that takes no `charname.Admitted`, so a rule A that
scanned `_test.go` would go RED on a legitimate fixture — and the cheap responses
to that RED are widening the rule or deleting the fixture, both of which cost more
than they save. Task 2 re-witnesses that particular fixture onto `description`, so
it may stop writing `name` at all; scope the filter correctly regardless, because
the next fixture will.

- **Rule A (gated).** Every SQL string literal in `internal/world/postgres` that
  writes the `name` column of `characters` — an `INSERT INTO characters` whose
  column list contains `name`, or an `UPDATE characters SET` assigning `name` —
  sits in a function whose parameter list includes a `charname.Admitted`. Match
  `name` on a word boundary so `normalized_name` and `name_skeleton` do not
  false-positive.
- **Rule B (identity-coherent).** Every such literal also writes
  `normalized_name`, `name_skeleton` and `name_skeleton_unicode_version` in the
  SAME statement. One-directional: a statement writing the three derived columns
  WITHOUT `name` is the backfill shape and is permitted.
- **Rule C (single constructor).** Exactly one function declared anywhere under
  `internal/charname` has `Admitted` among its results, and it is `(*Gate).Admit`.
- **Rule D (set equality).** The set of `internal/world/postgres` methods taking a
  `charname.Admitted`, derived from the tree, equals the checked-in expected set —
  after Task 2 that is `(*CharacterRepository).Create` and
  `(*CharacterRepository).Rename` — compared order-independently on exact-string
  keys with a symmetric-difference diff on failure. Inequality in **either**
  direction is RED.

The file's doc comment states what the census is FOR and what it deliberately does
not do: it is the complement of `world_sql_fence_test.go`, which already proves no
`characters` mutation SQL exists outside `internal/world/postgres`; this census
proves that inside that boundary every `name` write is gated and identity-coherent.
It also states the Phase 3 obligation: `RenameCharacter` must obtain its
`charname.Admitted` from the gate, and rule D turns RED if a third name-admitting
method appears without a deliberate update here.

**Then run the census against the tree as it actually stands, before Task 2.**
Rules A and B both fire on `internal/world/postgres/character_repo.go:79`, which
writes `name` with no `Admitted` in scope and without the identity columns.
**Record the command and its non-zero exit in the plan SUMMARY.** A census whose
RED state was assumed rather than observed fails §12.1 rule 4, and so does one
demonstrated against a hypothetical pre-fix state constructed for the
demonstration — the tree at this commit is the tree as it actually stood.
  </action>
  <verify>
    <automated>task test -- ./internal/charname/ ./test/meta/</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'func \(g \*Gate\) Admit' internal/charname/admission.go` matches
    - Census rule C is GREEN and non-vacuous: it reports exactly one function under `internal/charname` whose results include `Admitted`, and that function is `(*Gate).Admit`. This is the authoritative form — a grep for constructor-shaped names can only catch the ones someone thought to enumerate
    - `internal/charname/admission.go` declares no exported field on `Admitted`, so no other package can build a populated value with a struct literal — asserted by a test in `internal/charname/admission_test.go` that a zero-value `Admitted` reports `IsZero() == true`, plus census rule C above
    - `test/meta/character_name_admission_test.go` uses `go/parser` and contains four distinct synthetic-source positive-control tests, one per rule
    - The census's scanned file set is explicit in the source and asserted by a control: it walks `.go` files excluding `_test.go` and generated files, mirroring `world_sql_fence_test.go`'s Go scan, and a synthetic `_test.go` fixture writing `characters.name` with no `charname.Admitted` in scope is NOT flagged. A census that goes RED on a legitimate test fixture is a census someone will widen
    - The rule-D expected set is a literal in the test file and the comparison is order-independent with a symmetric-difference diff
    - The census run BEFORE Task 2 exits non-zero, and the SUMMARY records the command and the failure output
    - `task test -- ./internal/charname/` exits 0
  </acceptance_criteria>
  <reversibility rating="reversible">A new type and a new meta-test, both additive; nothing is deployed and no schema changes. Reverting is a revert.</reversibility>
  <done>`charname.Admitted` exists with exactly one constructor, and a tree-derived four-rule census was observed failing against the pre-fix writer boundary with its non-zero exit recorded.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 2: The gated writer boundary — Create takes the token, Update stops writing names, Rename appears</name>
  <files>internal/world/repository.go, internal/world/postgres/character_repo.go, internal/world/postgres/identity_backfill.go, internal/world/postgres/identity_backfill_test.go, internal/world/postgres/character_repo_test.go, internal/world/postgres/negative_path_test.go, internal/world/worldtest/mock_CharacterRepository.go</files>
  <read_first>
    - internal/world/postgres/character_repo.go — the WHOLE file. `Create` (`:52-65`) and its `char.Version` refresh-on-success pattern, `Update` (`:77-110`) and its `classifyCASZeroRow` zero-row classifier, `UpdateLocation` (`:200-230`) and `UpdatePreferences` (`:244-274`) — the two closest shape precedents for a new single-purpose versioned writer, including `withTx` / `txFromContext` / `primaryDeltaVersioned` and the `expectedVersion == 0 means unversioned` convention
    - internal/world/repository.go lines 190-222 (the `CharacterRepository` interface and its per-method doc-comment style)
    - internal/world/service.go lines 798-836 (`UpdateCharacterDescription` — the ONLY production caller of `Update`, which round-trips the just-read name; confirm for yourself that dropping `name` from the statement is behaviour-preserving for it)
    - internal/world/postgres/character_repo_test.go lines 682-758 (`TestCharacterRepository_UpdateVersionGuard`, whose doc comment binds MODEL-03, and specifically its `stale-version update returns WORLD_CONCURRENT_EDIT and does not overwrite` subtest at `:729-748` — read it BEFORE editing anything in this file, because this task silently defangs it and the action below says what to do about that; note `characterDBVersion` at `:726` as the second witness available)
    - internal/world/mutator.go lines 235-275 (`updateCharacter` / `updateCharacterPreferences` — the envelope seam `Update` sits behind, and the shape a versioned rename would use if it needed one)
    - internal/store/migrations/000054_character_identity_and_lifecycle.sql (written by 02-01 — the nullable `normalized_name`, `name_skeleton`, `name_skeleton_unicode_version` columns this task writes)
    - .mockery.yaml lines 38-49 (the `internal/world` block: `CharacterRepository` is generated into `internal/world/worldtest`, so a signature change requires regeneration)
  </read_first>
  <behavior>
    - `Create` persists `name`, `normalized_name`, `name_skeleton` and `name_skeleton_unicode_version` from the supplied `charname.Admitted`, in one statement, and refreshes `char.Name` to the admitted display form on success — mirroring the existing `char.Version` refresh.
    - `Create` called with a zero `charname.Admitted` returns an error carrying `CHARACTER_NAME_NOT_ADMITTED` and issues no SQL.
    - `Update` no longer writes `name`: a character whose row is updated for description keeps its stored `name`, `normalized_name`, `name_skeleton` and `name_skeleton_unicode_version` byte-identical, even when the in-memory `Character.Name` passed in differs from the stored one.
    - `Rename` writes all four identity columns in one statement, bumps `version`, and returns a `wmodel.MutationDelta`.
    - `Rename` with a non-zero `expectedVersion` that does not match affects zero rows and surfaces `WORLD_CONCURRENT_EDIT`; against an absent id it surfaces `CHARACTER_NOT_FOUND` — the same two-outcome classification `Update` and `UpdateLocation` already use.
    - `Rename` called with a zero `charname.Admitted` returns `CHARACTER_NAME_NOT_ADMITTED` and issues no SQL.
    - `BackfillCharacterIdentity` over rows with NULL identity columns computes and writes `normalized_name`, `name_skeleton` and `name_skeleton_unicode_version` from each row's existing `name`, and writes no `name`.
    - `BackfillCharacterIdentity` returns the collision sets — every group of two or more rows sharing a normalized key, each member carrying id, name, `player_id` and `created_at` — and writes nothing else about them; it resolves nothing.
    - Running `BackfillCharacterIdentity` twice over the same rows produces identical column values the second time.
  </behavior>
  <action>
Change `world.CharacterRepository.Create` to take the admission token alongside the
character: `Create(ctx, char *Character, name charname.Admitted)`. The INSERT binds
the four identity columns from `name` and does **not** read `char.Name`; document
that and refresh `char.Name` from `name.Display()` on success, exactly as the method
already refreshes `char.Version`. Reject a zero `Admitted` before issuing SQL.

Drop `name = $2` from `Update`'s statement so it writes `description`,
`location_id` and the version bump only. Renumber its placeholders and its CAS
`AND version = $n` accordingly — an off-by-one here silently writes the wrong
column. Rewrite the method's doc comment to say plainly that `characters.name` is
NOT writable through `Update` and to point at `Rename`, and record why: a generic
updater that also carried the name is how an ungated, identity-incoherent name
write survived into this phase.

Add `Rename(ctx context.Context, id ulid.ULID, name charname.Admitted, expectedVersion int) (*wmodel.MutationDelta, error)`
to the interface and the implementation, shaped on `UpdateLocation`: one
version-predicated statement writing all four identity columns plus the version
bump, `classifyCASZeroRow` for the zero-row case, `primaryDeltaVersioned` for the
delta, and the `expectedVersion == 0 means unversioned` convention. Writing the
four columns together is what rule B enforces and what keeps `normalized_name`
from going stale under the UNIQUE index `02-12` creates over it.

Add `internal/world/postgres/identity_backfill.go` exposing
`BackfillCharacterIdentity` plus the collision-set type it returns. It exists here
and not in `internal/store/migrations` for one reason, which the file's doc comment
must state: the fence has no `.go` marker path, so the SQL must live inside the
writer boundary and the migration must be a caller. Because goose Go migrations run
on a `database/sql` `*sql.Tx` while this package is pgx, the function takes a
**minimal executor interface declared in this package** — the two methods it
actually uses, consumer-side — rather than importing goose or a concrete type.
Compute the keys with `charname.Normalize` and `charname.Skeleton` directly: this
is a backfill of names already admitted under the old rules, so it MUST NOT run the
Gate, MUST NOT consult the block list, and MUST NOT reject anything — per D-17 it
detects and reports and resolves nothing.

**Re-witness the CAS test — this is not mechanical repair, and it is the one edit
in this task that a green suite will not prompt you to make.**
`TestCharacterRepository_UpdateVersionGuard`'s stale-version subtest proves its
"does not overwrite" half using the stored `name` as its SOLE witness: a concurrent
winner advances the row and sets a name by direct SQL, the loser calls `Update`
carrying a different in-memory `Name`, and the only post-condition reads the name
back. The moment `Update` stops writing `name`, that post-condition holds whether
or not the CAS predicate fired — a subtest that can no longer fail, on a test whose
doc comment binds MODEL-03, and it will still be green when you look at it. Move
the witness onto state `Update` does still write: have the concurrent winner set
`description` instead of the name, give the losing in-memory character a differing
`Description`, and assert BOTH that the stored description is still the winner's
AND that the stored version did not advance a second time — `characterDBVersion` is
already the helper for that read. Then delete the name post-condition outright:
after this task `got.Name` MUST NOT be the CAS witness, because it reports
unchanged either way. Keep the doc comment's MODEL-03 binding only if the
re-witnessed assertions genuinely fail when the predicate does not fire; a binding
attached to an assertion that no longer exercises the guarantee is worse than no
binding, because it reads as covered.

Regenerate the mock: `mockery`. Update the rest of the in-package repo tests
(`character_repo_test.go`, `negative_path_test.go`) for the new `Create` signature
and the `Update` behaviour change, and add `identity_backfill_test.go` for the
`<behavior>` rows above. Enumerate the compile breaks rather than guessing at them:
`rg -n 'repo\.Create\(|\.Create\(ctx, char' --type go` and `go build ./... 2>&1`.

**Import-direction constraint.** `internal/world/postgres` gains an import of
`internal/charname`. It MUST NOT gain an import of `internal/store`: plan `02-12`'s
migration lives in `internal/store/migrations`, which `internal/store` blank-imports,
so `internal/world/postgres → internal/store` would close a cycle. `internal/charname`
itself is safe today because plan 02-05 declares the block list as a consumer-side
interface in `charname` rather than importing `internal/charname/blocklist`; assert
that rather than assume it.
  </action>
  <verify>
    <automated>task test -- ./internal/world/... ./test/meta/ &amp;&amp; task test:int -- ./internal/world/postgres/</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Rename\(ctx context.Context' internal/world/repository.go` matches and the parameter type is `charname.Admitted`
    - `rg -v '^\s*//' internal/world/postgres/character_repo.go | rg -c 'UPDATE characters SET name'` returns 0 — `Update` no longer assigns `name`. Comment lines are stripped first, so a doc comment explaining the removal cannot invalidate its own check
    - Census rules A and B are GREEN over `internal/world/postgres`, which is the mechanical form of "every `name` write is gated and writes all four identity columns"; the census runs in this task's verify command, not as a separate manual step
    - `rg -n 'CHARACTER_NAME_NOT_ADMITTED' internal/world/postgres/character_repo.go` matches on both the `Create` and the `Rename` guard
    - `rg -n '"winner", got\.Name' internal/world/postgres/character_repo_test.go` returns no match — the stale-version CAS subtest no longer witnesses "does not overwrite" on the stored name, which after this task reports unchanged whether or not the CAS predicate fired
    - That subtest's post-conditions instead assert the stored `description` is the concurrent winner's value AND that `characterDBVersion` still reads the winner's version — two witnesses on state `Update` does write, so the MODEL-03 binding in `TestCharacterRepository_UpdateVersionGuard`'s doc comment stays honest rather than vacuous
    - `go list -deps ./internal/world/postgres | rg -c 'holomush/internal/store$'` returns 0 — no import cycle with `internal/store/migrations`
    - `rg -n 'charname.Gate|\.Check\(' internal/world/postgres/identity_backfill.go` returns no match — the backfill does not run the gate
    - `mockery` produces no diff after it is run and committed, and `internal/world/worldtest/mock_CharacterRepository.go` carries `Rename`
    - `task test -- ./internal/world/... ./test/meta/` exits 0 — the census rules A, B and D are now GREEN, having been observed RED in Task 1
    - `task test:int -- ./internal/world/postgres/` exits 0
  </acceptance_criteria>
  <reversibility rating="costly">The `CharacterRepository` interface change ripples through the generated mock and every caller in `internal/auth`; reverting means reverting all of them together. No schema or deployed state changes here, so it is costly rather than one-way.</reversibility>
  <done>`characters.name` is writable only by `Create` and `Rename`, both of which take an admission token and write all four identity columns in one statement, and the census that observed this as RED is now GREEN.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 3: Every admission call site, the guest path's gate, and five existence checks on the normalized key</name>
  <files>internal/auth/character_genesis.go, internal/auth/character_service.go, internal/auth/character_service_test.go, internal/auth/guest_service.go, internal/auth/guest_service_test.go, internal/auth/mocks/mock_CharacterRepository.go, internal/auth/mocks/mock_GuestCharacterRepository.go, internal/bootstrap/setup/adapters.go, internal/testsupport/integrationtest/harness.go, test/integration/auth/auth_suite_test.go</files>
  <read_first>
    - internal/auth/character_genesis.go lines 40-70 and 160-200 (the `writer` interface whose `Create` mirrors the world repository's, and `CharacterGenesisService.Create`'s transaction body at `:178` — both take the token after this task)
    - internal/auth/character_service.go lines 20-30 and 100-135 (the repo interface declaration and `createWithMaxAndBind` as rewired by plan 02-01 to call `charname.Gate.Check`)
    - internal/auth/guest_service.go lines 35-45 and 215-245 (the second repo interface, and `acquireUniqueName`'s retry loop at `:227`, which builds a display name with `strings.ReplaceAll(name, "_", " ")` and calls `ExistsByName` directly — this path never runs the pipeline at all)
    - internal/bootstrap/setup/adapters.go lines 24-50 (`CharRepoAdapter`, its `ExistsByName` with the shared `SELECT EXISTS(SELECT 1 FROM characters WHERE LOWER(name) = LOWER($1))`, and its `CHARACTER_EXISTS_CHECK_FAILED` code)
    - internal/testsupport/integrationtest/harness.go around lines 1115-1125 and 1545-1555 (`authCharRepoAdapter.ExistsByName` and the comment recording that the production guest service relies on it to retry on collision)
    - test/integration/auth/auth_suite_test.go lines 255-300 (the FIFTH `ExistsByName` adapter at `:297`, carrying its own independent `LOWER(name)` query, and the `charRepo.Create` fixture calls at `:262` and `:293`)
    - plugins/core-scenes/publish_store.go lines 155-170 (`isUniqueViolation` — the SQLSTATE 23505 plus constraint-name check to copy in substance)
  </read_first>
  <behavior>
    - `createWithMaxAndBind` obtains a `charname.Admitted` from the gate and passes it to genesis; a name the gate rejects never reaches a database write.
    - `acquireUniqueName` obtains a `charname.Admitted` for each generated candidate; a candidate the gate rejects — because a block-list pattern matches it, or because its skeleton collides — releases the name and retries through the loop that already exists, and exhausting the retries surfaces the existing exhaustion error rather than a gate error.
    - The existence check asks `normalized_name = $1` with the admitted key; a stored name whose display form differs only in case or in NFKC-collapsible codepoints is reported as existing, where the `LOWER(name)` form reported it as free.
    - The create path maps SQLSTATE 23505 on constraint `characters_normalized_name_key` to the existing `CHARACTER_NAME_TAKEN` code; any other 23505 is not swallowed.
    - All five existence-check implementations answer identically for the same key, including the two test-side adapters.
  </behavior>
  <action>
Thread the token through the create path. `CharacterGenesisService.Create` and its
`writer` interface both take the `charname.Admitted` and hand it to the repository;
`createWithMaxAndBind` in `internal/auth/character_service.go` obtains it by calling
`Gate.Admit` where plan 02-01 left a `Gate.Check` call, and passes it down. There is
now exactly one way to reach a name write from `internal/auth`, and it runs the gate.

**Route the guest path through the same gate.** In `internal/auth/guest_service.go`'s
`acquireUniqueName`, replace the bare `strings.ReplaceAll` plus `ExistsByName` pair
with a `Gate.Admit` call on the display form followed by the normalized-key
existence check, and persist the admitted key and skeleton exactly as the player
path does. This path is automatic and high-volume; a gate installed only in
`CharacterService` would have left it wide open, which is `T-02-31`.

**Replace the existence check with a normalized-key lookup at all FIVE sites,
together.** Rename the method to `ExistsByNormalizedName(ctx, key string)` and
change the query to `SELECT EXISTS(SELECT 1 FROM characters WHERE normalized_name = $1)`
— exact byte equality on the case-folded key, not `LOWER(name)`. The five sites are
the shared adapter in `internal/bootstrap/setup/adapters.go`, both interface
declarations (`internal/auth/character_service.go`, `internal/auth/guest_service.go`),
the harness adapter in `internal/testsupport/integrationtest/harness.go`, and a
FIFTH adapter at `test/integration/auth/auth_suite_test.go:297` carrying its own
independent `LOWER(name)` query. **The fifth site's query body moves too** — a
mechanical rename that leaves `LOWER(name)` in place at one site reproduces exactly
the divergence this change closes, and it would diverge in the integration tier,
which is where this phase's criteria are asserted. Regenerate the auth mocks with
`mockery` and update the roughly twenty `ExistsByName` expectations across
`internal/auth/character_service_test.go` and `internal/auth/guest_service_test.go`.

Keep the check a **UX affordance only**. §6.1.3 is explicit that uniqueness is the
database constraint; the pre-check exists to produce a friendly error most of the
time and it does not close the race. Add 23505 handling on the insert path in
`internal/auth/character_service.go`, copying `isUniqueViolation`'s shape and
matching the constraint name `characters_normalized_name_key`, mapped to the
existing `CHARACTER_NAME_TAKEN` code. The index that makes this fire is created in
plan `02-12`; the handler lands here so the create path is complete when it does,
and `02-12`'s concurrent-claim spec is what proves it fires.
  </action>
  <verify>
    <automated>task test -- ./internal/auth/ ./internal/bootstrap/... ./test/meta/ &amp;&amp; task test:int -- ./test/integration/auth/</automated>
  </verify>
  <acceptance_criteria>
    - `rg --type go -l 'ExistsByName' | wc -l` returns 0 — every site moved, including the generated mocks. `ExistsByNormalizedName` does not contain `ExistsByName` as a substring, so this grep is unambiguous; do NOT weaken it by writing a Go comment that mentions the old name, and do not leave a migration note naming it in Go source
    - `rg -n 'ExistsByNormalizedName' --type go` matches at exactly five declaration sites: the shared adapter, both auth interfaces, the harness adapter, and `test/integration/auth/auth_suite_test.go`
    - For every Go file, `rg -v '^\s*//' <file> | rg -c 'LOWER\(name\)'` returns 0 — the case-folding query is gone from live code at all five sites, and stripping comment lines first means a comment recording what it was replaced with cannot invalidate the check
    - `rg -n 'characters_normalized_name_key' internal/auth/character_service.go` matches — the 23505 handler names the index
    - `rg -n 'charname\.' internal/auth/guest_service.go` matches — the guest path runs the gate
    - `rg -n 'Admit\(' internal/auth/character_service.go internal/auth/guest_service.go` matches in both
    - `mockery` produces no diff after it is run and committed
    - `task test -- ./internal/auth/ ./internal/bootstrap/... ./test/meta/` exits 0
    - `task test:int` exits 0 in full — this task changes shared interfaces, which the untagged lane does not compile
  </acceptance_criteria>
  <reversibility rating="costly">The interface rename touches six packages including the integration harness and two generated mock files; reverting means reverting all of them together.</reversibility>
  <done>Both production name-admission paths run the gate, the guest path is no longer the hole, and all five existence checks ask exact byte equality on the normalized key.</done>
</task>

</tasks>

<verification_integrity>
The six PORTAL-10 / §12.1 rules at their fixed numbering, specialized to this plan.

1. **Census with set equality.** **This plan owns Phase 2's rule-1 census**: the
   character-name admission census in `test/meta/character_name_admission_test.go`.
   Its derived side comes from parsing `internal/world/postgres` and
   `internal/charname` with `go/parser`, never from a second hand-maintained list.
   Rule D is the set-equality half — the tree-derived set of name-admitting methods
   against a checked-in expected set, order-independent, symmetric-difference diff,
   RED in either direction — and it is non-vacuous from the moment Task 2 lands
   (two members). Phase 2 does **not** claim EXT-04's admin section-registry ↔
   authorization-descriptor census, which §12.2 assigns to Phase 6.
   **The census is a backstop, not the whole guarantee.** The primary mechanism is
   the type: `charname.Admitted` has one constructor, so an ungated name write is
   not expressible. That is deliberate — a census that must be kept honest is
   weaker than a shape that cannot be wrong, and the previous SQL-literal census
   was provably unable to catch a Phase 3 `RenameCharacter` reusing the existing
   `Update`.
2. **Paired positive control on every denial test.** Each of the census's four
   rules ships a synthetic-source fixture that MUST be flagged, in the style of
   `TestWorldSQLFenceFlagsMutationOutsideBoundary`, plus a fixture that must NOT be
   flagged (the backfill's three-derived-columns-without-`name` shape, a `SELECT`,
   and a comment). Rule D is paired in both directions: removing a member from the
   expected set is RED, and adding an unlisted name-admitting method is RED.
   `Create`'s and `Rename`'s zero-`Admitted` refusals are paired with the
   corresponding success on the same fixture, so the refusal cannot pass because
   the write is broken.
3. **Assertions against marshaled response bytes.** Does not apply — this plan
   ships no RPC and withholds no field. Stated as not applying rather than omitted.
4. **Gates demonstrated RED against the pre-fix state.** This plan carries a rule-4
   instance: the admission census is demonstrated RED against **the tree as it
   actually stood**, where `internal/world/postgres/character_repo.go:79` writes
   `name` with no `charname.Admitted` in scope and without the identity columns.
   The recorded non-zero exit goes in the SUMMARY. This is a DIFFERENT instance
   from the milestone's named one — the name-uniqueness gate RED against today's
   unindexed schema — which plan `02-12` carries and which this plan does not
   claim.
5. **Wire-level assertion of every opacity and authorization contract.** This plan
   asserts no opacity contract; `CHARACTER_NAME_TAKEN` is a create-path error, not
   an authorization refusal, and it deliberately discloses that the name is taken —
   that is the feature. `errutil.AssertErrorCode` is used to assert which internal
   code fired and is not cited as evidence about a caller-visible response.
6. **Invariant-scope discipline.** This plan pins no registry invariant and writes
   no `// Verifies:` annotation. It works WITHIN `INV-WORLD-4`'s writer-boundary
   fence rather than amending it; `test/meta/world_sql_fence_test.go` is not
   modified and its allow-marker is not extended to `.go` files. `INV-WORLD-6`'s
   name-reservation guarantee is bound in plan `02-04`; `INV-WORLD-7` stays
   `pending` for Phase 4. No ad-hoc `INV-NAME-*` family is minted.
</verification_integrity>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any caller → `characters.name` | The display name is in-world identity; every path that can set it is a trust boundary. |
| guest provisioning → `characters.name` | An automatic, high-volume path that today bypasses the normalization pipeline entirely. |
| future phase → the writer boundary | Phase 3 adds `RenameCharacter`; nothing in Phase 2 can review that code, so the constraint has to be structural. |
| `internal/world/postgres` ↔ `internal/store` | An import-cycle boundary: plan `02-12`'s migration must be able to call the writer boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-30 | Spoofing | `LOWER(name)` existence query | high | mitigate | ASCII-oriented folding over a column that permits any script never rejected an NFKC-collapsible pair, so a visually identical name was never even a candidate for rejection. Closed by exact byte equality on the stored normalized key at all five sites, the fifth of which carries its own independent copy of the old query. |
| T-02-31 | Spoofing | guest provisioning path | high | mitigate | The guest path never ran the pipeline, so a gate installed only in `CharacterService` left the automatic, high-volume path open. Closed by `acquireUniqueName` obtaining a `charname.Admitted`, with the existing retry loop absorbing a rejection. |
| T-02-73 | Tampering | `CharacterRepository.Update` writing `name` | high | mitigate | A generic updater that also wrote `characters.name` is an ungated admission path reachable by any caller holding the repository — and it is live today at `character_repo.go:79`. Closed by removing `name` from `Update`'s statement entirely; the only production caller round-trips the just-read name, so the removal is behaviour-preserving. Asserted by census rule A and by a behaviour test that mutates the in-memory `Name` and observes the stored one unchanged. |
| T-02-74 | Tampering | display name written without its identity columns | high | mitigate | A `name` write that leaves `normalized_name` stale defeats the UNIQUE index `02-12` creates over that column — silently, because the index is over a column nobody updated, so the constraint holds while the data it constrains is wrong. Closed by census rule B: every statement writing `name` writes all four identity columns together, and `Rename` is the only shape that can. |
| T-02-75 | Elevation of privilege | a second `charname.Admitted` constructor | high | mitigate | A convenience escape hatch — `FromString`, `Unchecked`, an exported field, a struct literal — would void the entire guarantee at zero apparent cost, and would look like a small refactor in review. Closed by making the populated form unexported state and by census rule C, which asserts exactly one function in `internal/charname` returns the type. Recorded as an explicit prohibition in this plan's front matter. |
| T-02-76 | Spoofing | a future write site bypassing the gate | high | mitigate | Phase 3's `RenameCharacter` is the concrete case: reusing the pre-existing `Update` would have introduced no new SQL literal, so a literal-scanning census would have stayed green while the new site bypassed the gate. Closed structurally — `Rename`'s only name parameter is a `charname.Admitted`, so a bypass does not compile — with census rule D as the backstop against a third method appearing. |
| T-02-77 | Denial of service | `internal/world/postgres` → `internal/store` import cycle | medium | mitigate | `internal/store` blank-imports `internal/store/migrations`, so an import of `internal/store` from the writer boundary would close a cycle and make plan `02-12`'s migration unbuildable — discovered at the worst moment, mid-migration-authoring. Closed by the consumer-side executor interface in `identity_backfill.go` and asserted by a `go list -deps` criterion. |
</threat_model>

<verification>
- `task test -- ./internal/charname/ ./internal/world/... ./internal/auth/ ./internal/bootstrap/... ./test/meta/` green.
- `./test/meta/` specifically: BOTH `TestNoRawWorldSQLOutsideWriterBoundary` and the new admission census must be green. This plan moves SQL toward the writer boundary and adds a complementary fence; the meta lane is where either mistake surfaces, and it is not reached by a package-scoped run.
- `task test:int` green in full — this plan changes shared interfaces, which the untagged lane does not compile.
- `mockery` run and its output committed; `task lint` green; `task fmt` run and its edits committed.
</verification>

## Artifacts this plan produces

New files: `internal/charname/admission.go`, `internal/charname/admission_test.go`,
`test/meta/character_name_admission_test.go`,
`internal/world/postgres/identity_backfill.go`,
`internal/world/postgres/identity_backfill_test.go`.

New exported symbols: `charname.Admitted` with `IsZero`, `Display`, `Key`,
`Skeleton`, `UnicodeVersion`; `(*charname.Gate).Admit`;
`world.CharacterRepository.Rename`;
`worldpostgres.BackfillCharacterIdentity` and its collision-set type and executor
interface.

Changed signatures: `world.CharacterRepository.Create` and
`(*worldpostgres.CharacterRepository).Create` gain a `charname.Admitted`;
`auth.CharacterGenesisService.Create` and its `writer` interface do the same.

Changed behaviour: `(*worldpostgres.CharacterRepository).Update` no longer writes
`characters.name`.

Renamed symbol: `ExistsByName` → `ExistsByNormalizedName` across five sites, with
the query body changing from `LOWER(name) = LOWER($1)` to `normalized_name = $1`
at both sites that carry their own copy of it.

New error code: `CHARACTER_NAME_NOT_ADMITTED`.

Regenerated: `internal/world/worldtest/mock_CharacterRepository.go`,
`internal/auth/mocks/mock_CharacterRepository.go`,
`internal/auth/mocks/mock_GuestCharacterRepository.go`.

<success_criteria>
- `charname.Admitted` has exactly one constructor and no exported field, asserted by census rule C.
- `characters.name` is written by exactly two methods, both inside `internal/world/postgres`, both taking a `charname.Admitted`, both writing all four identity columns in one statement.
- `CharacterRepository.Update` no longer writes `characters.name`, and a description update leaves all four identity columns byte-identical.
- The admission census was observed failing against the pre-fix tree and its non-zero exit is recorded; it is green after Task 2 and stays green through Task 3.
- No `ExistsByName` and no `LOWER(name)` remains anywhere in the tree.
- The guest path runs the gate.
- `TestNoRawWorldSQLOutsideWriterBoundary` is still green and `test/meta/world_sql_fence_test.go` is unmodified.
</success_criteria>

<output>
Create `.planning/phases/02-abac-schema-vocabulary/02-06-SUMMARY.md` when done.
</output>
