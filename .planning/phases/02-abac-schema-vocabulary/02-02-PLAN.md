---
phase: 02-abac-schema-vocabulary
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/charname/mixedscript.go
  - internal/charname/mixedscript_test.go
  - internal/charname/gate.go
  - internal/charname/gate_test.go
  - internal/world/validation.go
  - internal/world/validation_test.go
  - internal/auth/player_test.go
autonomous: true
requirements: [IDENT-06, IDENT-08]

estimate:
  tokens: 72000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A name whose script set is Latin+Cyrillic, Latin+Greek, or Cyrillic+Greek is rejected; a name whose script set is a single script, or Latin+Japanese, Latin+Han+Bopomofo, or Latin+Han+Hangul, is permitted — all eight rows of 01-SPEC.md §6.1.2 Mechanism A are asserted."
    - "Codepoints of script Common and Inherited are excluded from the script set and never cause a rejection on their own."
    - "A name containing a rune with no assignment in the stdlib unicode.Scripts tables is rejected (fail-closed), because Go's stdlib is Unicode 15.0.0 while the confusables data is 17.0.0."
    - "Player usernames still reject non-ASCII input and a leading non-letter: internal/auth/player.go's ^[a-zA-Z][a-zA-Z0-9_]*$ rule is pinned by a regression guard and is not re-implemented."
    - "internal/charname imports no internal/auth symbol — the character-name pipeline cannot reach the username validator, in either its own code or its tests' production dependencies."
    - "internal/auth/player.go imports no internal/charname symbol, and no function reachable from ValidateUsername calls into charname — the username policy is implemented by its own regex and nothing else."
    - "The guard does NOT forbid internal/auth as a package from importing internal/charname: character-name admission IS the responsibility of the character-creation services in that package, and plan 02-06 makes them call Gate.Admit. A package-wide ban would be RED by design in wave 4."
    - statement: "Mechanism A is implemented over the Script property (stdlib unicode.Scripts), not Script_Extensions, so a name whose only second-script evidence is a Common-category codepoint with a restricted scx set is permitted where the faithful UTS #39 profile would reject it; the divergence is closed-direction for §6.1.2's three named rejections and is recorded in the amendment pass."
      verification: backstop
  artifacts:
    - internal/charname/mixedscript.go
    - internal/charname/mixedscript_test.go
    - internal/auth/player_test.go
  key_links:
    - "charname.Gate.Check evaluates the mixed-script verdict after Normalize and before the skeleton query, so a mixed-script name never reaches a database round trip."
    - "internal/world/validation.go's characterNameRegex remains the letters-and-spaces shape; the mixed-script rule is additive to it, not a replacement."
  prohibitions:
    - "The mixed-script restriction MUST NOT become a de-facto English-only name policy. A name written wholly in a single non-Latin script is permitted, and no rule added here may reject a non-Latin name that its Latin-script equivalent would pass."
---

<objective>
Complete IDENT-06 by adding §6.1.2 Mechanism A — the UTS #39 Moderately
Restrictive mixed-script restriction — to the gate the tracer established, and pin
IDENT-08's username rule with the regression guard §6.2 asks for.

The two land together deliberately. §6 opens by stating that character-name
normalization and player-username validation are two separate policies that MUST
NOT share an implementation; IDENT-08's guard exists precisely to fail if a future
change to the character-name pipeline reaches the username path. The plan that
grows the character-name pipeline is therefore the right plan to install the guard
that watches it.

Purpose: close cross-script splicing (`раypal` — Cyrillic а and р inside a Latin
word), which the skeleton check alone does not catch early, and make the
two-policy separation mechanically enforced.
Output: `charname.MixedScript`, its eight-row verdict table, and an extended
`internal/auth/player_test.go`.

**Research blocker incorporated (RESEARCH § Concerns 1).** `02-CONTEXT.md` states
`golang.org/x/text` supplies script extensions for UTS #24. It does not — x/text
has no script package at all. Go's stdlib `unicode.Scripts` supplies the **Script**
property at Unicode 15.0.0, not **Script_Extensions**, which UTS #39's Moderately
Restrictive profile is defined over. This plan takes RESEARCH's disposition (a):
implement §6.1.2's closed eight-row table over `unicode.Scripts`, and record the
divergence. Disposition (b) — generating a second Script_Extensions table — is
**deferred**, with the reason recorded: the marginal fidelity gain is confined to
names whose only second-script evidence is a Common-category codepoint, the
divergence is closed-direction for all three of §6.1.2's named rejections (Cyrillic
and Greek letters carry real `sc` values), and Mechanism B is the defense against
whole-script confusables regardless.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md
@.planning/phases/02-abac-schema-vocabulary/02-RESEARCH.md
@.planning/phases/01-portal-spec/01-SPEC.md
@.planning/phases/02-abac-schema-vocabulary/02-01-SUMMARY.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: The eight-row mixed-script verdict table</name>
  <files>internal/charname/mixedscript.go, internal/charname/mixedscript_test.go, internal/charname/gate.go, internal/charname/gate_test.go</files>
  <read_first>
    - internal/charname/pipeline.go and internal/charname/gate.go (written by 02-01 — the composition point and the `Normalized` shape)
    - .planning/phases/01-portal-spec/01-SPEC.md §6.1.2 (the eight-row table is the specification; transcribe its rows, do not paraphrase them)
    - .planning/phases/02-abac-schema-vocabulary/02-RESEARCH.md § P0 Finding 2 (the sc-vs-scx table and its closed-direction argument)
    - internal/world/validation.go (`characterNameRegex` — the existing letters-and-spaces shape the new rule is additive to)
  </read_first>
  <behavior>
    - `MixedScript("Alaric")` → permitted (single script, Latin).
    - `MixedScript` on a name written wholly in Cyrillic → permitted (single script). This is the row Mechanism B exists to cover; Mechanism A MUST NOT reject it.
    - `MixedScript` on a name written wholly in Han, wholly in Hiragana, wholly in Greek, wholly in Hangul, wholly in Arabic → permitted in each case.
    - `MixedScript` on Latin + Hiragana, Latin + Katakana, Latin + Han → permitted (Japanese row).
    - `MixedScript` on Latin + Han + Bopomofo → permitted (Chinese row).
    - `MixedScript` on Latin + Han + Hangul → permitted (Korean row).
    - `MixedScript` on Latin + Cyrillic → rejected with code `NAME_MIXED_SCRIPT`.
    - `MixedScript` on Latin + Greek → rejected with code `NAME_MIXED_SCRIPT`.
    - `MixedScript` on Cyrillic + Greek → rejected with code `NAME_MIXED_SCRIPT`.
    - `MixedScript` on Latin + Arabic (an "any other combination" case) → rejected with code `NAME_MIXED_SCRIPT`.
    - `MixedScript("Anne-Marie O'Neill")` — ASCII punctuation and spaces are script `Common` and are excluded from the set, so the verdict is single-script Latin, permitted.
    - `MixedScript` on a Latin name plus a combining mark (script `Inherited`) → permitted; `Inherited` is excluded from the set.
    - `MixedScript` on a string containing a rune assigned in Unicode 16 or 17 and therefore in no `unicode.Scripts` range → rejected with code `NAME_UNASSIGNED_SCRIPT`.
    - The rejection error message names the offending script combination but does not name any existing character.
  </behavior>
  <action>
Create `internal/charname/mixedscript.go` with
`func ScriptSet(s string) ([]string, error)` and
`func MixedScript(s string) error`.

`ScriptSet` walks the runes of `s`, and for each rune finds the `unicode.Scripts`
range table it belongs to by `unicode.Is`. Runes in `Common` or `Inherited` are
script-neutral and excluded from the set. A rune matching **no** script table is
the Unicode-version-skew case (RESEARCH P-12): return an error carrying
`oops.Code("NAME_UNASSIGNED_SCRIPT")`. Reject is the fail-closed choice and it is
consistent with `characterNameRegex`'s existing letters-only shape, which already
rejects most such runes; document that reasoning in the function's doc comment so
the rule is explicit rather than emergent (RESEARCH Open Question 4). Return the
set as a sorted, deduplicated `[]string` so the verdict is order-independent and
the error message is stable.

`MixedScript` evaluates the resulting set against §6.1.2's table as a closed
enumeration — set size 0 or 1 permits; the three permitted multi-script sets are
`{Latin}` unioned with a non-empty subset of `{Han, Hiragana, Katakana}`,
`{Latin, Han, Bopomofo}` and its subsets containing Bopomofo, and
`{Latin, Han, Hangul}` and its subsets containing Hangul; every other set of size
≥ 2 is rejected with `oops.Code("NAME_MIXED_SCRIPT")`. Express the permitted
combinations as explicit set membership, never as an ordered comparison or a
"count the scripts" heuristic — a heuristic re-derives the table in a second place.

Add a package doc comment on `mixedscript.go` recording, in one paragraph: that
this is UTS #39's Moderately Restrictive profile; that it is computed over the
**Script** property from stdlib `unicode.Scripts` (Unicode `unicode.Version`) and
not over **Script_Extensions**; that the approximation can only ever *miss* a
rejection the faithful profile would make, never add one; and that the confusables
data is at a different Unicode version, which is why an unassigned-script rune is
rejected rather than treated as neutral.

Then extend `charname.Gate.Check` to evaluate `MixedScript(n.Display)` after
`Normalize` and **before** the skeleton lookup, so a mixed-script name never
reaches a database round trip.

Write the tests as a table-driven suite with one row per `<behavior>` case, ACE
sentence names, and non-ASCII fixtures spelled as `\uXXXX` escapes rather than
literal characters.
  </action>
  <verify>
    <automated>task test -- -run 'MixedScript|ScriptSet' ./internal/charname/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/charname/mixedscript.go` contains `func ScriptSet(` and `func MixedScript(`
    - `[ "$(rg -v '^\s*//' internal/charname/mixedscript.go | rg -o 'Script_Extensions' | wc -l)" -eq 0 ]` — the approximation is documented in comments, not claimed in code. Comment lines are stripped first so the documenting comment cannot invalidate its own check, and the count uses `-o | wc -l` with a numeric `-eq` because `rg -c` prints nothing and exits 1 on the zero matches that mean success, and `wc -l` emits leading whitespace so a string comparison against `"0"` also fails
    - `rg -n 'unicode.Scripts' internal/charname/mixedscript.go` matches
    - `task test -- -run 'MixedScript|ScriptSet' ./internal/charname/` exits 0 with at least 14 subtests, one per `<behavior>` row
    - The suite contains a permitted case for a name written wholly in Cyrillic and a permitted case for a name written wholly in Arabic — the prohibition's mechanical guard
    - `rg -n 'NAME_UNASSIGNED_SCRIPT' internal/charname/mixedscript.go` matches
    - `internal/charname/gate_test.go` asserts a Latin+Cyrillic submission fails without the `SkeletonLookup` being called (use a lookup double that fails the test if invoked)
  </acceptance_criteria>
  <reversibility rating="reversible">The verdict table is a pure function with no stored state; tightening or loosening it later is a code edit with no migration.</reversibility>
  <done>All eight §6.1.2 rows are asserted, single-script non-Latin names are permitted, an unassigned-script rune is rejected fail-closed, and the gate short-circuits before any database round trip.</done>
</task>

<task type="tdd">
  <name>Task 2: Empty-normal-form and pipeline rejections reach the world validator</name>
  <files>internal/world/validation.go, internal/world/validation_test.go, internal/charname/pipeline.go, internal/charname/pipeline_test.go</files>
  <read_first>
    - internal/world/validation.go (the whole file — `ValidateCharacterName`'s ordered checks and the `&ValidationError{Field, Message}` return style)
    - internal/world/validation_test.go (the existing table shape and its ACE names)
    - internal/charname/gate.go (written by 02-01, extended by Task 1)
    - .planning/phases/01-portal-spec/01-SPEC.md §6.1.1 (the empty-normal-form rejection) and §6.1.5 (why the regex's existing strictness is not relied on)
  </read_first>
  <behavior>
    - `ValidateCharacterName` still rejects an empty string, a name over `MaxCharacterNameLength` runes, a name with leading or trailing spaces, and a name with consecutive spaces.
    - `ValidateCharacterName` still rejects a name containing a digit or an ASCII apostrophe, per the unchanged `characterNameRegex`.
    - A submission that normalizes to the empty string produces a user-visible rejection distinguishable from "you left the box blank": the message tells the submitter their input contained no visible characters, per the roadmap's name-pipeline sketch finding.
    - A submission whose display form still contains a `Cf` codepoint after `charname.Normalize` is impossible — the pipeline removed it — and a test asserts the stored display form is `Cf`-free for a `Cf`-padded submission.
  </behavior>
  <action>
Leave `characterNameRegex` and the ordered checks in `ValidateCharacterName`
unchanged — §6.1.5 is explicit that the regex's existing strictness stays, and the
pipeline's `Cf` stripping is an independent guarantee rather than a replacement.

Add to `internal/charname` a distinct message for the empty-normal-form case that
does not read as "please enter a name": the roadmap's name-pipeline sketch finding
records that a name of only invisibles looks like an empty box to the player, so
the two cases need different wording. Wire it through so the caller receives it.

Extend `internal/world/validation_test.go` with the regression rows above,
asserting the pre-existing behaviour is unchanged by this phase's pipeline
replacement. Extend `internal/charname/pipeline_test.go` with the `Cf`-free stored
display assertion.
  </action>
  <verify>
    <automated>task test -- ./internal/world/ ./internal/charname/</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'characterNameRegex' internal/world/validation.go` still matches the `^[\p{L}]+( [\p{L}]+)*$` literal — the regex is unchanged by this phase
    - `task test -- ./internal/world/ ./internal/charname/` exits 0
    - `internal/world/validation_test.go` contains subtests for each pre-existing rejection listed in `<behavior>`
    - A test asserts the empty-normal-form message differs from the blank-input message
  </acceptance_criteria>
  <done>The pre-existing validator behaviour is pinned by regression rows, and a name of only invisibles produces a message a player can act on.</done>
</task>

<task type="tdd">
  <name>Task 3: IDENT-08 — pin the player-username rule</name>
  <files>internal/auth/player_test.go</files>
  <read_first>
    - internal/auth/player.go lines 18-40 and 150-175 (`MinUsernameLength`, `MaxUsernameLength`, `usernameRegex`, and `ValidateUsername` where it is applied)
    - internal/auth/player_test.go (the existing test shape and naming)
    - .planning/phases/01-portal-spec/01-SPEC.md §6.2 (the "do not touch" instruction and what the guard is for)
  </read_first>
  <behavior>
    - `ValidateUsername("José")` is rejected — non-ASCII.
    - `ValidateUsername` rejects a username written in Cyrillic, and one containing a fullwidth Latin letter.
    - `ValidateUsername` rejects a username containing a `Cf` codepoint.
    - `ValidateUsername("1alaric")` is rejected — leading non-letter.
    - `ValidateUsername("_alaric")` is rejected — leading underscore.
    - `ValidateUsername("alaric_01")` is accepted — the positive control that makes each rejection above non-vacuous.
    - `ValidateUsername("ab")` is rejected (below `MinUsernameLength`) and a 31-character username is rejected (above `MaxUsernameLength`); a 3-character and a 30-character username are accepted.
    - A test asserts `internal/charname` imports no `github.com/holomush/holomush/internal/auth` path — direction one of the separation, and permanently true.
    - A test asserts the file `internal/auth/player.go` imports no `github.com/holomush/holomush/internal/charname` path — direction two, scoped to the FILE that owns the username rule rather than to the whole package.
    - The same test asserts no identifier `charname` appears anywhere in the call graph rooted at `ValidateUsername` within `internal/auth/player.go`.
    - A control asserts the guard does NOT fire when some OTHER file in `internal/auth` imports `internal/charname`: build a synthetic two-file fixture (one `player.go`-shaped, one `character_service.go`-shaped with the import) and assert only the first is flagged. Without this control the guard is indistinguishable from the package-wide ban that plan `02-06` would make RED.
  </behavior>
  <action>
Write **no new username validation.** IDENT-08 is discharged by a regression guard
over the rule that already exists, not by re-implementing it — §6.2 is explicit.

Extend `internal/auth/player_test.go` with a table-driven suite covering every
`<behavior>` row, each ACE-named as a sentence. Each rejection row is paired on the
same fixture with the accepted `alaric_01` control, so a rejection assertion cannot
pass because the validator rejects everything.

Add the separation guard as a small `go/parser` test in the same file. **Scope it
narrowly — this is the fix for cross-AI review HIGH #3 (02-REVIEWS.md), and the
earlier package-wide form was a genuine cross-plan contradiction, not a style
nit.** The earlier draft asserted "no file under `internal/auth/` imports
`internal/charname`" and made it a standing success criterion. Plan `02-06` makes
`internal/auth/character_service.go` and `internal/auth/guest_service.go` call
`Gate.Admit` — deliberately, because character-name admission is exactly those
services' job — so the package-wide guard would be RED by design in wave 4 and the
cheap repair would be deleting the guard, losing the protection entirely.

Assert instead the two things the §6 two-policy separation actually needs, both of
which stay true forever:

1. **`internal/charname` MUST NOT import `internal/auth`.** Parse every non-test
   `.go` file under `internal/charname/` (including sub-packages) and assert no
   import path has the `github.com/holomush/holomush/internal/auth` prefix. This is
   the direction that matters most: it makes it impossible for the character-name
   pipeline to start consulting the credential validator.
2. **`internal/auth/player.go` MUST NOT reach `charname`.** Parse that one file and
   assert it imports no `internal/charname` path, and that no identifier `charname`
   appears in any function reachable from `ValidateUsername` within it. Scoping to
   the file that owns the rule is what distinguishes "the username policy is
   separate" from "this package may not do character-name work".

Write both with `go/parser` (not `rg`) so the assertion is over parsed import
declarations rather than over text, and give each the synthetic-fixture control the
`<behavior>` rows name — in particular the control proving a `character_service.go`-
shaped file carrying the import is NOT flagged. Record in the SUMMARY that the
guard is file-scoped by design and why, so a future reader does not "tighten" it
back into the contradiction.

Non-ASCII fixtures are spelled as `\uXXXX` escapes.
  </action>
  <verify>
    <automated>task test -- -run TestValidateUsername ./internal/auth/</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat internal/auth/player.go` shows no change — the rule is pinned, not edited
    - `task test -- -run TestValidateUsername ./internal/auth/` exits 0
    - `internal/auth/player_test.go` contains subtests asserting rejection for a non-ASCII, a Cyrillic, a fullwidth, a `Cf`-bearing, a leading-digit and a leading-underscore username
    - Every rejection subtest is paired with the accepting `alaric_01` control on the same fixture
    - A `go/parser` test asserts no non-test file under `internal/charname/` imports any `github.com/holomush/holomush/internal/auth` path, and it fails when such an import is added to a synthetic fixture
    - A `go/parser` test asserts `internal/auth/player.go` imports no `internal/charname` path and that `charname` appears nowhere in `ValidateUsername`'s call graph within that file
    - A control asserts a synthetic `internal/auth` file that is NOT `player.go` may import `internal/charname` without being flagged — the criterion that proves this guard will still be green after plan `02-06` lands `Gate.Admit` in `character_service.go` and `guest_service.go`
    - `[ "$(rg -o 'internal/auth' internal/charname/ --type go -g '!*_test.go' | wc -l)" -eq 0 ]` — no non-test file under `internal/charname/` names the auth package. The "non-test files" scope is carried mechanically by the `-g '!*_test.go'` glob rather than left to the reader, and the count uses `-o | wc -l` with a numeric `-eq` because `rg -c` prints nothing and exits 1 on the zero matches that mean success, and `wc -l` emits leading whitespace so a string comparison against `"0"` also fails
  </acceptance_criteria>
  <reversibility rating="reversible">A test-only addition; removing it loses the guard but changes no behaviour.</reversibility>
  <done>The existing `^[a-zA-Z][a-zA-Z0-9_]*$` rule is pinned by paired regression assertions, and a directional, file-scoped guard fails if a future change unifies the two name policies — while leaving plan `02-06`'s legitimate `internal/auth → internal/charname` admission calls green.</done>
</task>

</tasks>

<verification_integrity>
The six PORTAL-10 / §12.1 rules at their fixed numbering, specialized to this plan.

1. **Census with set equality.** This plan ships no census. Phase 2's rule-1 census
   scope is the character-name write-site census in plan `02-06`; EXT-04's admin
   registry census remains Phase 6's.
2. **Paired positive control on every denial test.** Applies to both suites here.
   Every mixed-script rejection row is paired with a permitted row on the same
   fixture shape (notably: whole-Cyrillic and whole-Arabic names are *permitted*,
   which is what proves the rule is not an English-only filter). Every username
   rejection row is paired with the accepting `alaric_01` control — without it,
   `err != nil` cannot distinguish "rejected as non-ASCII" from "rejected for
   length".
3. **Assertions against marshaled response bytes.** Does not apply — no RPC and no
   projection in this plan. Stated as not applying rather than omitted.
4. **Gates demonstrated RED against the pre-fix state.** The new gates are
   `MixedScript` and the two directional separation guards. `MixedScript` MUST be
   observed failing against the pre-Task-1 gate (a Latin+Cyrillic name is accepted
   there). Each separation guard MUST be observed failing against its own synthetic
   fixture — an `internal/auth` import planted in an `internal/charname` file, and a
   `charname` import planted in a `player.go`-shaped file — never against the real
   tree, because a deliberate import added to real `internal/auth` code and reverted
   would demonstrate the package-wide ban this plan explicitly does not install.
   Both recorded non-zero exits go in this plan's SUMMARY. The username rows are
   pinned against behaviour that already passes and therefore cannot be shown RED;
   say so in the SUMMARY rather than claiming a demonstration.
5. **Wire-level assertion of every opacity and authorization contract.** The one
   contract here is that a mixed-script rejection names the script combination but
   no existing character. Assert over the message the caller receives.
   `errutil.AssertErrorCode` is used to assert *which* internal code fired and is
   not cited as evidence about what a caller saw.
6. **Invariant-scope discipline.** No registry invariant is pinned in this plan and
   no `// Verifies:` annotation is written. The sc-vs-scx divergence is recorded as
   a SPEC amendment in plan `02-11`, not as a new invariant — it is a documented
   approximation, not a guarantee.
</verification_integrity>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player → `CharacterService.Create` | An attacker-chosen name crosses into an in-world identity other characters read and act on. |
| player → account registration | An attacker-chosen username crosses into a credential handle backed by a database UNIQUE constraint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Spoofing | `charname.MixedScript` | high | mitigate | Cross-script splicing (Cyrillic а and р inside a Latin word) is closed by the Moderately Restrictive verdict table, with all three of §6.1.2's named rejection pairs asserted. |
| T-02-07 | Spoofing | `charname.ScriptSet` unassigned-rune branch | medium | mitigate | A rune assigned after Unicode 15.0.0 has no stdlib script and would otherwise be treated as neutral, letting a two-script name read as single-script. Rejected fail-closed with `NAME_UNASSIGNED_SCRIPT`. |
| T-02-08 | Elevation of privilege | username ↔ character-name policy merge | medium | mitigate | A future "unification" of the two policies would relax the ASCII-only credential rule. Closed by the IDENT-08 paired regression pin plus two DIRECTIONAL guards: `internal/charname` may not import `internal/auth` at all, and `internal/auth/player.go` may not reach `charname`. Deliberately NOT a package-wide ban — plan `02-06` legitimately calls `Gate.Admit` from `character_service.go` and `guest_service.go`, and a guard that is RED by design would be deleted rather than obeyed. |
| T-02-09 | Information disclosure | mixed-script rejection message | low | accept | The message names the offending script combination, which is derived entirely from the submitter's own input and discloses nothing about existing characters. |
| T-02-10 | Denial of service | over-broad script rejection | medium | mitigate | An over-tight Mechanism A would reject legitimate non-Latin names — a fairness failure, not a security one. Closed by the prohibition and its mechanical guard: whole-Cyrillic and whole-Arabic permitted cases are required suite rows. |
</threat_model>

<verification>
- `task test -- ./internal/charname/ ./internal/world/ ./internal/auth/` green.
- `task lint` green.
- `task fmt` run and its edits committed.
- `task test:int` run after this plan because it touches shared types reachable from integration files.
</verification>

<success_criteria>
- All eight rows of §6.1.2's verdict table are asserted, with the permitted rows including at least two single-script non-Latin names.
- An unassigned-script rune is rejected rather than silently treated as neutral.
- `internal/auth/player.go` is unmodified and its rule is pinned by paired assertions.
- Two directional guards hold: `internal/charname` imports no `internal/auth` symbol, and `internal/auth/player.go` reaches no `charname` symbol. Neither forbids `internal/auth`'s character-creation services from importing `charname`, and a control proves it.
- The sc-vs-scx divergence is written down in `internal/charname/mixedscript.go`'s doc comment and queued for the §14 amendment pass.
</success_criteria>

## Artifacts this phase produces (this plan's share)

New files: `internal/charname/mixedscript.go`, `internal/charname/mixedscript_test.go`.

New exported symbols: `charname.ScriptSet`, `charname.MixedScript`.

New error codes: `NAME_MIXED_SCRIPT`, `NAME_UNASSIGNED_SCRIPT`.

Extended files: `internal/charname/gate.go` (mixed-script step added to `Check`),
`internal/world/validation_test.go`, `internal/auth/player_test.go`.

<output>
Create `.planning/phases/02-abac-schema-vocabulary/02-02-SUMMARY.md` when done.
</output>
