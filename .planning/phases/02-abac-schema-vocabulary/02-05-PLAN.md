---
phase: 02-abac-schema-vocabulary
plan: 05
type: tdd
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - internal/charname/blocklist/blocklist.go
  - internal/charname/blocklist/blocklist_test.go
  - internal/charname/blocklist/cache.go
  - internal/charname/blocklist/cache_test.go
  - internal/charname/blocklist/poller.go
  - internal/charname/blocklist/poller_test.go
  - internal/charname/gate.go
  - internal/charname/gate_test.go
  - internal/store/postgres.go
  - internal/bootstrap/setup/subsystem.go
  - test/integration/charname/name_blocklist_test.go
autonomous: true
requirements: [IDENT-07]

estimate:
  tokens: 88000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A character name matching any pattern in the configured block list is rejected server-side at the single gate every character-name admission routes through, so create and every future admission point are covered by one evaluation."
    - "Block-list patterns are evaluated against the §6.1.1 normalized form (post-case-fold), never against the raw submission and never against the display name."
    - "A pattern that fails to compile is a hard startup failure whose error names the offending entry; the whole list is validated and compiled at boot."
    - "An empty block list — an absent settings row, or the JSON value `[]` — compiles to an empty snapshot that rejects nothing. It is not a startup failure and is not read as 'block everything'."
    - "Pattern evaluation order is the stored list order and is stable across reloads; the first matching pattern decides the rejection."
    - "Two patterns that match the same name are not merged and not deduplicated: the name is rejected once, on the first match, and the rejection identifies no pattern to the submitter."
    - "Compiling the same list value twice yields snapshots that accept and reject identically; a reload that observes an unchanged version indicator does not swap the snapshot."
    - "A reload whose new list contains an uncompilable pattern fails and leaves the last valid snapshot in force — never a partial update and never an empty list."
    - "A reload racing an in-flight check never exposes a partially-compiled snapshot: a check reads one immutable snapshot for the whole of its evaluation."
    - "The block list is not versioned, not audited, and not retroactive: a name already admitted under an older list is never re-validated, and this is a stated property rather than an oversight."
  artifacts:
    - internal/charname/blocklist/blocklist.go
    - internal/charname/blocklist/cache.go
    - internal/charname/blocklist/poller.go
    - test/integration/charname/name_blocklist_test.go
  key_links:
    - "charname.Gate.Check consults the block-list snapshot after Normalize and MixedScript and before the skeleton lookup, so a blocked name never reaches a database round trip."
    - "The poller's version indicator is the pair (updated_at, hash(value)) for the core.character.name.blocklist row, mirroring policy.VersionQuerier's two-signal design — a bare updated_at poll does not detect a direct-SQL edit."
    - "internal/bootstrap wires the cache and poller into startup so a bad pattern aborts boot rather than being discovered by the first player whose name slips through."
  prohibitions:
    - "The seeded default block list MUST NOT encode content judgements the operator did not make. Migration 000054 seeds it as the empty JSON array `[]`, and this phase MUST NOT add a starter set of patterns — the shipped list is what the operator wrote and nothing else."
---

<objective>
Land IDENT-07: an operator-configurable block/disallow list of regular expressions,
evaluated server-side against the normalized character name at the one gate every
admission routes through.

Purpose: the list is DB-backed configuration with no in-process writer (v0.13 ships
no editing surface per §8.12, so edits are direct SQL) in a multi-replica
deployment. That combination is already solved in-tree by `policy.Cache` +
`policy.Poller` — an immutable compiled snapshot behind a read barrier, refreshed
by polling a cheap version indicator. This plan mirrors that rather than inventing
invalidation, because in-process invalidation would only fix the replica that
served the write.
Output: `internal/charname/blocklist` with a compiled snapshot, a poller, boot
validation, and its wiring into the gate.

**Research blocker incorporated (RESEARCH § Concerns 3 / P-5).** D-16 specifies
polling `holomush_system_info.updated_at` for the key. That indicator does not work
as assumed: the column is bumped only by `SetSystemInfo`'s `ON CONFLICT` clause,
migrations forbid triggers, and D-16 itself states the only v0.13 edit path is
direct SQL — so a plain `UPDATE holomush_system_info SET value = …` leaves
`updated_at` unchanged and the poller never notices. The **decision** (mirror Cache
+ Poller) is right; the **indicator** is corrected here to the pair
`(updated_at, hash(value))`, which is the shape closest to the pattern being
mirrored: `policy.VersionQuerier` already returns two signals
(`LatestPolicyVersion` returns a timestamp **and** a count) for exactly this
reason — a deletion may not move `MAX(updated_at)`.

**Assumption-delta checkpoint answered.** The delta detector fired on
`configurable`: the name policy moves from compile-time `regexp.MustCompile`
allowlists — the repo-wide shape today, with no configurable regex list anywhere in
the tree — to an operator-editable settings key. **Has a configuration decision
become a modeling decision? No.** The block list stays a config value: it has no
lifecycle of its own — no version history, no audit trail, no editor, no "who may
edit" model beyond database access. What IS promoted is the **compiled snapshot**:
`blocklist.Snapshot` becomes the primary representation the gate consumes, and the
raw `[]string` is demoted to a storage detail of one variant (the settings row).
The one consequence worth writing down is non-retroactivity: a name admitted under
an older list is never re-validated, so the list governs admissions and not the
existing corpus. That is a stated property, recorded in the package doc comment,
and an explicit truth above — not an oversight to be discovered later.

**Note on ReDoS.** Go's `regexp` is RE2 — linear time, no backtracking — so
operator-supplied patterns carry no ReDoS risk. The residual risk is a *wrong*
(over-broad) pattern, not a slow one. Do NOT add timeouts, complexity analysis, or
any other backtracking-defense machinery.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md
@.planning/phases/02-abac-schema-vocabulary/02-RESEARCH.md
@.planning/phases/02-abac-schema-vocabulary/02-PATTERNS.md
@.planning/phases/01-portal-spec/01-SPEC.md
@.claude/rules/logging.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Compile a block list into an immutable snapshot</name>
  <files>internal/charname/blocklist/blocklist.go, internal/charname/blocklist/blocklist_test.go, internal/charname/gate.go, internal/charname/gate_test.go</files>
  <read_first>
    - internal/access/policy/cache.go lines 1-120 (the three-part shape: an immutable `Snapshot` safe for concurrent reads without locking, the one-shot `readBarrier` whose `err` is written before `close(done)` so Go's memory model guarantees visibility, and the `Cache` struct's `mu` / `snapshot` / `barrierMu` / `barrier` / `dirty` fields)
    - internal/access/policy/cache_test.go lines 150-200 (`TestCacheReloadFailsOnCompilationError` — the reload-failure-does-not-partially-update assertion this plan must reproduce with a non-empty prior snapshot)
    - internal/settings/game.go lines 110-165 (`StringSliceN`'s lenient `(nil,false)` + `DebugContext` parse-failure path, and `SetStringSlice`'s namespace validation and JSON marshal)
    - internal/settings/namespaces.go (the `RegisteredNamespaces` allowlist — `core` is already admitted, so no change is needed)
    - internal/store/migrations/000054_character_identity_and_lifecycle.sql (written by 02-01 — the seeded `core.character.name.blocklist` key and its `[]` value)
    - internal/charname/gate.go (the composition point, as extended by 02-02)
  </read_first>
  <behavior>
    - `blocklist.Compile(nil)` and `blocklist.Compile([]string{})` each return an empty snapshot and no error; `snapshot.Match("alaric")` returns no match.
    - `blocklist.Compile([]string{"(unclosed"})` returns an error whose message contains the offending pattern text verbatim, so the operator who wrote it can find it.
    - `blocklist.Compile([]string{"^ok$", "(unclosed", "^also$"})` fails and reports the offending entry, not merely "a pattern failed".
    - `Compile` on a two-pattern list where both match the same input returns the FIRST matching pattern's index — evaluation is in stored list order and the first match decides.
    - Compiling the same list twice produces snapshots whose `Match` verdicts agree for a fixed corpus of inputs.
    - `snapshot.Match` is evaluated against the caller-supplied string as given; the gate is what supplies the normalized key, and a gate test asserts a pattern written in lowercase matches a mixed-case submission because the key is case-folded.
    - `snapshot.Match` returns a boolean and the matched pattern's index; it does NOT return the pattern text to the caller, so a rejection cannot echo operator configuration back to a submitter.
  </behavior>
  <action>
Create `internal/charname/blocklist/blocklist.go` with
`type Snapshot struct { … }` (immutable, safe for concurrent reads without
locking — copy `policy.Snapshot`'s doc comment rationale in substance),
`func Compile(patterns []string) (*Snapshot, error)`, and
`func (s *Snapshot) Match(normalized string) (bool, int)`.

`Compile` compiles each pattern with `regexp.Compile` in list order, and on the
first failure returns `oops.Code("BLOCKLIST_PATTERN_INVALID").With("pattern", p).With("index", i).Wrap(err)`
so the error names the offending entry (D-15). A nil or empty input is valid and
produces an empty snapshot — the absence of configuration is not "block everything"
and is not a startup failure.

`Match` iterates in stored order and returns on the first hit. It returns the index
rather than the pattern text so the rejection path cannot echo operator
configuration back to a submitter.

Write the package doc comment recording the three properties this list does NOT
have, so a later reader does not assume them: it is **not versioned** (there is no
history of what the list said when a name was admitted), **not audited** (edits are
direct SQL and leave no trail in this system), and **not retroactive** (a name
admitted under an older list is never re-validated). State also that Go's `regexp`
is RE2 and therefore carries no ReDoS risk, so no backtracking-defense machinery
belongs here.

Then extend `charname.Gate` with a `BlockList` dependency — an interface with a
single `Match(normalized string) (bool, int)` method so the gate depends on the
snapshot's shape, not on the cache — and evaluate it in `Check` **after**
`Normalize` and `MixedScript` and **before** the skeleton lookup, returning
`oops.Code("NAME_BLOCKED")`. Pass `Normalized.Key` (the case-folded form), never
the display name and never the raw submission. A nil `BlockList` means no list is
configured and matches nothing.
  </action>
  <verify>
    <automated>task test -- ./internal/charname/...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/charname/blocklist/blocklist.go` contains `func Compile(` and `func (s *Snapshot) Match(`
    - `rg -v '^\s*//' internal/charname/blocklist/ | rg -c 'time.After|context.WithTimeout|backtrack'` returns 0 — no ReDoS machinery
    - `rg -n 'BLOCKLIST_PATTERN_INVALID' internal/charname/blocklist/blocklist.go` matches
    - `task test -- ./internal/charname/...` exits 0 with a subtest per `<behavior>` row
    - A gate test asserts `Check` passes `Normalized.Key` to the block list, by using a `BlockList` double that records the string it was given and asserting it equals the case-folded key
    - A gate test asserts the `NAME_BLOCKED` error message contains neither the matched pattern text nor its index
  </acceptance_criteria>
  <reversibility rating="reversible">Pure compilation over a config value; the snapshot shape can change without a migration.</reversibility>
  <done>A block list compiles to an immutable snapshot, an empty list is valid, a bad pattern names itself, and the gate evaluates the list against the normalized key before any database round trip.</done>
</task>

<task type="tdd">
  <name>Task 2: Cache, two-signal poller, and boot validation</name>
  <files>internal/charname/blocklist/cache.go, internal/charname/blocklist/cache_test.go, internal/charname/blocklist/poller.go, internal/charname/blocklist/poller_test.go, internal/store/postgres.go, internal/bootstrap/setup/subsystem.go</files>
  <read_first>
    - internal/access/policy/cache.go (the whole file — `Reload`'s compile-then-swap ordering, the read barrier, and the `dirty` flag for an `Invalidate` racing an active reload)
    - internal/access/policy/poller.go lines 15-140 (the `VersionQuerier` two-signal interface and its doc comment explaining WHY it returns two signals, `Reloadable`, `PollerConfig`'s nil-collaborator rejection, the 10s `Interval` default, `Run`'s immediate first poll, and the "only mark initialized AFTER reload succeeds" comment)
    - internal/store/postgres.go lines 80-100 (`SetSystemInfo`'s `INSERT … ON CONFLICT (key) DO UPDATE SET value = $2, updated_at = …` — the only writer that maintains `updated_at`, and the reason a direct-SQL edit does not move it)
    - internal/access/policy/bootstrap.go lines 25-60 (the fail-closed boot posture: validate the whole set, wrap with the offending entry's identity, return the error, let startup abort)
    - internal/access/setup/setup.go lines 320-345 (how `policy.NewPoller` is constructed and handed its collaborators)
    - internal/bootstrap/setup/subsystem.go (where boot-time seeding and validation steps are sequenced)
    - .claude/rules/logging.md
  </read_first>
  <behavior>
    - `Cache.Reload` on a valid list installs a new snapshot; `Cache.Snapshot` returns it.
    - `Cache.Reload` on a list containing an uncompilable pattern returns an error AND leaves the previously installed, non-empty snapshot in force — asserted with a prior snapshot that is non-empty, so "no partial update" is distinguishable from "reset to empty".
    - `Cache.Snapshot` called concurrently with `Reload` returns either the old or the new snapshot, never a partially-populated one.
    - The version querier returns `(updatedAt int64, valueHash string, err error)` for `core.character.name.blocklist`; a change to the value with `updated_at` unchanged still moves the hash and therefore triggers a reload.
    - The version querier returning "no such row" is not an error: it yields the zero indicator and an empty list.
    - `NewPoller` rejects a nil querier, a nil reloader and a nil tracker; a non-positive interval defaults to 10s.
    - `Poller.Run` performs an immediate first poll before entering the ticker loop, and marks itself initialized only after the first reload succeeds, so a failed first reload is retried.
    - A poll whose indicator is unchanged does not call `Reload`.
    - Boot validation compiles the configured list and returns an error naming the offending entry when a pattern is uncompilable; startup aborts.
  </behavior>
  <action>
Create `internal/charname/blocklist/cache.go` mirroring `policy.Cache`'s three-part
shape: an immutable snapshot field guarded by an `RWMutex`, a one-shot
`readBarrier` for reload-in-progress (writing `err` before `close(done)` so the
memory model guarantees visibility without extra synchronization), and functional
options. `Reload` reads the settings value, compiles it, and swaps **only on
success** — a compile failure returns the error and leaves the prior snapshot
untouched, which is D-16's "last valid list stays in force".

Create `internal/charname/blocklist/poller.go` mirroring `policy.Poller`:
a `VersionQuerier` interface, a `Reloadable` subset, a `PollerConfig` rejecting nil
collaborators and defaulting `Interval` to 10s, and a `Run` doing an immediate
first poll then a ticker loop, marking `initialized` only after a successful
reload.

The version indicator is the **pair** `(updated_at, hash(value))` for the key,
queried in one statement — for example
`SELECT updated_at, md5(value) FROM holomush_system_info WHERE key = $1`. Carry a
doc comment recording why both signals are needed: `updated_at` is maintained only
by `SetSystemInfo`'s `ON CONFLICT` clause, migrations forbid triggers, and v0.13's
only edit path for this key is direct SQL, which leaves `updated_at` untouched.
This mirrors `policy.VersionQuerier`'s own two-signal design and its stated reason.

Add the boot step: compile the configured list once at startup and abort on
failure, following `policy.Bootstrap`'s posture — validate the whole set, wrap with
the offending entry's identity, return the error, do not log-and-continue. Wire the
cache and poller into the same startup sequence, and hand the cache's snapshot
accessor to the `charname.Gate` constructed for the create path.

Use `slog.*Context` / `errutil.LogErrorContext` throughout; a bare `slog.Warn` in
this package is a sloglint failure.
  </action>
  <verify>
    <automated>task test -- ./internal/charname/... &amp;&amp; task test:int -- ./test/integration/charname/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/charname/blocklist/poller.go`'s `VersionQuerier` returns two signals plus an error, and its doc comment states why a bare `updated_at` poll is insufficient
    - `rg -n 'md5\(value\)|hash' internal/charname/blocklist/poller.go` matches — the indicator includes a content hash
    - `rg -v '^\s*//' internal/charname/blocklist/ | rg -c 'slog\.(Info|Warn|Error|Debug)\(' ` returns 0 — every log call is a `*Context` variant
    - `task test -- ./internal/charname/...` exits 0
    - A cache test installs a NON-EMPTY snapshot, then reloads with an uncompilable pattern, and asserts the snapshot still matches what the prior list matched — distinguishing "unchanged" from "reset to empty"
    - A poller test asserts an unchanged indicator does not call `Reload`, using a reloader double that counts invocations
    - A boot test asserts startup returns an error naming the offending pattern when the configured list is uncompilable
  </acceptance_criteria>
  <reversibility rating="costly">`core.character.name.blocklist` becomes operator-facing configuration once seeded; renaming the key later strands an operator's existing value.</reversibility>
  <done>The compiled list refreshes on a two-signal indicator that a direct-SQL edit actually moves, a bad pattern aborts boot naming itself, and a failed reload leaves the last valid list in force.</done>
</task>

<task type="tdd">
  <name>Task 3: Block-list rejection proven against real Postgres</name>
  <files>test/integration/charname/name_blocklist_test.go</files>
  <read_first>
    - test/integration/charname/name_confusable_test.go (written by 02-01 — the suite's fixture helpers and cleanup ordering)
    - internal/charname/blocklist/cache.go and poller.go (written in Task 2)
    - internal/settings/game.go (`SetStringSlice` — how the test writes a list value through the supported path)
  </read_first>
  <behavior>
    - With `core.character.name.blocklist` set to a pattern matching a chosen name, creating a character with that name fails with code `NAME_BLOCKED`.
    - Paired positive control: the SAME fixture creating a character with a name the pattern does not match SUCCEEDS — so the rejection cannot pass because creation is broken.
    - With the key set to `[]`, the same blocked name is accepted — proving the empty list rejects nothing.
    - A pattern written in lowercase blocks a submission typed in mixed case, because the list is evaluated against the case-folded key.
    - After the list value is changed by a direct `UPDATE holomush_system_info SET value = …` that does NOT touch `updated_at`, the poller still observes the change within one interval and the new list takes effect — the assertion that closes RESEARCH § Concerns 3.
  </behavior>
  <action>
Add `test/integration/charname/name_blocklist_test.go` to the suite plan `02-01`
created. Drive the create path end to end, writing the list value through
`settings.SetStringSlice` for the ordinary cases and through a direct `UPDATE` that
leaves `updated_at` alone for the poller case.

For the poller case, construct the poller with a short `Interval` rather than
sleeping for the 10s default, and use Gomega's `Eventually` for the observation.

Every rejection assertion is paired with the accepting control on the same fixture.
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/charname/</automated>
    <human-check>If Docker is unavailable, record which of the five behaviours were therefore not observed and do not claim the poller case as proven.</human-check>
  </verify>
  <acceptance_criteria>
    - `test/integration/charname/name_blocklist_test.go` exists with `//go:build integration`
    - `task test:int -- ./test/integration/charname/` exits 0
    - The spec contains a direct `UPDATE holomush_system_info SET value` that does not set `updated_at`, followed by an `Eventually` asserting the new list took effect
    - Each blocked-name assertion is paired with an accepted-name assertion on the same fixture
    - A spec asserts that with the key set to `[]` the previously blocked name is accepted
  </acceptance_criteria>
  <done>The block list rejects and permits correctly against real Postgres, and a direct-SQL edit that leaves `updated_at` untouched is still observed.</done>
</task>

</tasks>

<verification_integrity>
The six PORTAL-10 / §12.1 rules at their fixed numbering, specialized to this plan.

1. **Census with set equality.** This plan ships no census. Phase 2's rule-1 census
   scope is the character-name write-site census in plan `02-06`, which is the
   census that proves this block list is reached from every admission point;
   without it, "evaluated at both create and rename" would rest on inspection.
   EXT-04's admin registry census remains Phase 6's.
2. **Paired positive control on every denial test.** Every blocked-name assertion —
   unit and integration — is paired on the same fixture with an accepted name, and
   with the empty-list case that accepts the previously blocked name. Without the
   pair, a rejection cannot distinguish "blocked by the list" from "creation is
   broken" or from "everything is blocked".
3. **Assertions against marshaled response bytes.** Does not apply — this plan
   ships no RPC and withholds no field. The related discipline that does apply is
   that the rejection MUST NOT echo operator configuration: assert the pattern text
   and its index are absent from the returned message.
4. **Gates demonstrated RED against the pre-fix state.** The new gates are the
   block-list rejection, the reload-failure-preserves-last-valid assertion, and the
   direct-SQL-edit poller observation. Each MUST be observed failing first: the
   rejection against the pre-Task-1 gate, the reload assertion against a cache that
   swaps before compiling, and the poller observation against a bare `updated_at`
   indicator. That last one is the point of the RED demonstration — a bare
   `updated_at` poller passes every other test in this plan. Record all three
   non-zero exits in the SUMMARY.
5. **Wire-level assertion of every opacity and authorization contract.** The
   contract here is that a block-list rejection reveals nothing about the
   configuration. Assert over the message the caller receives that neither the
   pattern text nor its index appears. `errutil.AssertErrorCode` is used to assert
   `NAME_BLOCKED` fired and is not cited as evidence about what the caller saw.
6. **Invariant-scope discipline.** This plan pins no registry invariant and writes
   no `// Verifies:` annotation. The block list's guarantees are configuration
   behaviour rather than system invariants; minting an `INV-BLOCKLIST-*` family
   would be exactly the ad-hoc-family debt the registry exists to prevent.
</verification_integrity>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player → character-name admission | An attacker-chosen name crosses into an in-world identity; the block list is one of the server-side checks on that crossing. |
| operator → `holomush_system_info` row | Operator-authored regular expressions become compiled, executing configuration. In v0.13 the only edit path is direct database access. |
| database → replica set | Every replica must converge on the same compiled list without an in-process writer to hook. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-22 | Spoofing | block-list evaluation input | high | mitigate | Evaluating the raw submission or the display name instead of the case-folded key lets a mixed-case or compatibility-encoded variant walk past a pattern. Closed by passing `Normalized.Key` and asserting it with a recording double. |
| T-02-23 | Elevation of privilege | client-side evaluation | high | mitigate | §6.1.4 is explicit that client-side evaluation is not evaluation. This phase ships no client copy of the list; the only evaluation point is the server-side gate. |
| T-02-24 | Tampering | `Cache.Reload` swap ordering | high | mitigate | A cache that swaps before compiling leaves an empty list in force after a bad edit — silently unenforced. Closed by compile-then-swap and by the reload-failure test asserted against a non-empty prior snapshot. |
| T-02-25 | Tampering | poll indicator | high | mitigate | A bare `updated_at` indicator never observes the only edit path v0.13 has, so an operator's block list silently never takes effect on any replica. Closed by the `(updated_at, hash(value))` pair and its RED-first demonstration. |
| T-02-26 | Denial of service | over-broad operator pattern | medium | accept | RE2 is linear-time, so this is not ReDoS; the risk is a pattern that rejects legitimate names. D-15's boot validation surfaces a malformed pattern to the operator who wrote it, and D-16's last-valid-list-stays-in-force bounds the blast radius of a bad edit. An over-broad but valid pattern is an operator concern with no system defense, and adding one would require the system to judge names. |
| T-02-27 | Information disclosure | rejection message | medium | mitigate | Echoing the matched pattern back to a submitter discloses operator configuration and helps an attacker enumerate the list. Closed by returning the index internally and asserting neither pattern nor index appears in the caller-visible message. |
| T-02-28 | Repudiation | unaudited list edits | low | accept | Edits are direct SQL and leave no trail in this system; there is no editing surface to audit in v0.13 (§8.12). Recorded as a stated non-property of the list rather than left to be discovered. |
</threat_model>

<verification>
- `task test -- ./internal/charname/...` green.
- `task test:int -- ./test/integration/charname/` green.
- `task lint` green, including sloglint over the new package.
- `task fmt` run and its edits committed.
</verification>

<success_criteria>
- A name matching a configured pattern is rejected server-side at the one gate; a non-matching name on the same fixture is accepted.
- An empty list rejects nothing and is not a startup failure; an uncompilable pattern aborts boot naming itself.
- A failed reload leaves the last valid non-empty list in force.
- A direct-SQL edit that does not touch `updated_at` is still observed within one poll interval.
- The rejection message discloses no operator configuration.
</success_criteria>

## Artifacts this plan produces

New package: `internal/charname/blocklist` — files `blocklist.go`, `cache.go`,
`poller.go` and their tests; plus
`test/integration/charname/name_blocklist_test.go`.

New exported symbols: `blocklist.Snapshot`, `blocklist.Compile`,
`(*blocklist.Snapshot).Match`, `blocklist.Cache`, `blocklist.NewCache`,
`(*blocklist.Cache).Reload`, `(*blocklist.Cache).Snapshot`,
`blocklist.VersionQuerier`, `blocklist.Reloadable`, `blocklist.PollerConfig`,
`blocklist.NewPoller`, `(*blocklist.Poller).Run`, and a `BlockList` interface on
`charname.Gate`.

New error codes: `BLOCKLIST_PATTERN_INVALID`, `NAME_BLOCKED`.

Consumes (created by plan `02-01`): settings key `core.character.name.blocklist`.

<output>
Create `.planning/phases/02-abac-schema-vocabulary/02-05-SUMMARY.md` when done.
</output>
</content>
