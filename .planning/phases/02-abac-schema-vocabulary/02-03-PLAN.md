---
phase: 02-abac-schema-vocabulary
plan: 03
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/access/prefix.go
  - internal/access/prefix_test.go
  - internal/access/policy/attribute/viewer.go
  - internal/access/policy/attribute/viewer_test.go
autonomous: true
requirements: [EXT-07, PROFILE-11]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "access.ParseEntityRef accepts viewer:, profile: and admin_section: references and returns their type and id; all three prefixes are members of knownPrefixes."
    - "ViewerSubject, ProfileResource and AdminSectionResource each panic on an empty identifier, so no call site can produce a bare prefix that would silently bypass access control."
    - "ViewerTierProvider.ResolveSubject emits tier on every rung; player_id is present on the guest and player rungs and ABSENT — never an empty string — on the anonymous rung; has_player_id is emitted true or false on every code path."
    - "ViewerTierProvider emits roles as a string list resolved PER PLAYER, with a has_roles witness on every code path; roles is ABSENT on the anonymous rung, ABSENT when no role lookup is configured, and ABSENT when the lookup errors."
    - "ViewerTierProvider.Schema declares tier, player_id, has_player_id, roles and has_roles, so the resolver does not silently drop them."
    - "ViewerTierProvider.ResolveResource returns (nil, nil) — a viewer is a Subject, never a Resource."
    - "The viewer tier token is derived from server-side session state at the point the subject string is constructed; no constructor in internal/access accepts a tier value sourced from a request header, query parameter, cookie or request field."
  artifacts:
    - internal/access/policy/attribute/viewer.go
    - internal/access/policy/attribute/viewer_test.go
  key_links:
    - "attribute.PlayerRoleLookup is DECLARED here and consumed by both ViewerTierProvider (this plan) and PlayerAttributeProvider (plan 02-13); its implementation over PostgresRoleStore.PlayerRoles and its production wiring both land in 02-13."
    - "viewer.roles is what makes seed:viewer-property-admin-read expressible at all: the shipped admin read policy gates on `\"admin\" in principal.character.roles` (internal/access/policy/seed.go:126), and a viewer-flavored twin has no roles attribute to test without this."
    - "internal/access/prefix_test.go's known-prefix table proves membership indirectly via ParseEntityRef."
---

<objective>
Land the ABAC *subject and resource vocabulary* every later plan in this phase and
Phase 4 gate on: the three new entity prefixes with their panic-on-empty
constructors, and the `viewer:` subject namespace with its attribute provider —
including the `roles` attribute without which a viewer-flavored admin read policy
cannot be written at all.

No policies are seeded here — plan `02-07` owns `internal/access/policy/seed.go`
in a single append. This plan ships only the subjects, resources and attributes
those policies will reference, so that the policies land against a vocabulary that
already resolves.

**Scope split (cross-AI review, 02-REVIEWS.md HIGH #4 + the viewer-identity
finding).** The earlier shape of this plan also owned the player-scoped role store
query, the `PlayerAttributeProvider` role attribute, and provider registration in
`internal/access/setup/setup.go`. Two things forced a split. First, the review
established that the specified wiring does not compile: `BuildABACStack` takes
`RoleStore store.RoleStore` (`internal/access/setup/setup.go:89`) and that
**interface** carries only `GetRoles`/`AddRole`/`RemoveRole`/`PlayerHasRole`
(`internal/store/role_store.go:15-23`) — no `PlayerRoles` — while the concrete
`*PostgresRoleStore` is in scope only at `internal/access/setup/subsystem.go:95`,
a file this plan did not own. Second, closing the viewer-identity defect requires
new derived attributes on `PropertyProvider` plus a character→player resolver. Both
now live in plan **`02-13`**, which owns `role_store.go`, `player.go`, `property.go`,
`setup.go` and `subsystem.go` together. This plan declares the `PlayerRoleLookup`
seam those pieces plug into.

Purpose: an unregistered attribute namespace does not error — `principal.viewer.tier`
is simply absent from the bag, every condition referencing it evaluates false, and
the whole tier-floor family silently default-denies in production while a unit test
that stubs the bag stays green (RESEARCH P-7). Shipping the provider *before* the
policies exist makes that failure mode unreachable by construction; plan `02-13`
registers it, before `02-07` seeds anything that references it.
Output: `internal/access/policy/attribute/viewer.go` and three prefix families.

**Dependency note.** This plan shares no file with the tracer (`02-01`) — it touches
`internal/access/` and `internal/store/role_store.go`, the tracer touches
`internal/charname/`, `internal/world/`, `internal/auth/` and the migrations. The
`depends_on` is an **ordering** dependency, not a file one: tracer-first means the
end-to-end slice is verified before any expansion task runs, and this plan is
expansion.

**Open Question 2 settled here.** RESEARCH flagged the principal type for
`seed:admin-section-access` as the phase's highest-risk unresolved detail: a wrong
principal type makes the policy match nothing and criterion 5 pass vacuously via
default-deny. §10.5's verdict is normative — *the admin gate is evaluated PER
PLAYER* — and the tree's shipped operator path already reads roles per player
(`PostgresRoleStore.PlayerHasRole`, "true iff any character of playerID has role").
The `player` namespace today carries `id`, `grants`, `is_guest`, `has_is_guest`
and **no roles**, so `principal is player` gated on roles is not expressible until
this plan lands it. `principal is character` with `principal.character.roles` is
rejected: it would put two different answers to "is this caller an admin" over one
table, the operator socket saying yes and the web saying no for the same human at
the same moment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md
@.planning/phases/02-abac-schema-vocabulary/02-RESEARCH.md
@.planning/phases/02-abac-schema-vocabulary/02-PATTERNS.md
@.planning/phases/01-portal-spec/01-SPEC.md
@.claude/rules/abac-providers.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Three entity prefixes and their panic-on-empty constructors</name>
  <files>internal/access/prefix.go, internal/access/prefix_test.go</files>
  <read_first>
    - internal/access/prefix.go (the whole file — the subject and resource constant blocks, `knownPrefixes`, `PlayerSubject`'s panic guard and the doc comment explaining why it exists, `CharacterResource`, `CharacterDirectoryResource`, and `ParseEntityRef`)
    - internal/access/prefix_test.go lines 550-610 (the known-prefix table and its comment explaining that `ParseEntityRef` proves membership indirectly)
    - .planning/phases/01-portal-spec/01-SPEC.md §8.4.1 Phase-2 obligations 2 and 3, §8.4.2 Phase-2 obligation, §10.4 (the `admin_section:` row)
  </read_first>
  <behavior>
    - `ParseEntityRef("viewer:anonymous")` returns type `viewer`, id `anonymous`, no error.
    - `ParseEntityRef("viewer:player:01ARZ3NDEKTSV4RRFFQ69G5FAV")` returns type `viewer` and the remainder as the id — `SplitN(ref, ":", 2)` semantics, so the rung and ULID travel together in the id.
    - `ParseEntityRef("profile:01ARZ3NDEKTSV4RRFFQ69G5FAV")` and `ParseEntityRef("admin_section:characters")` each parse with the expected type.
    - `ViewerSubject(ViewerTierAnonymous, "")` returns `viewer:anonymous`.
    - `ViewerSubject(ViewerTierGuest, "01ARZ…")` returns `viewer:guest:01ARZ…`; `ViewerSubject(ViewerTierPlayer, "01ARZ…")` returns `viewer:player:01ARZ…`.
    - `ViewerSubject(ViewerTierPlayer, "")` panics — a player-rung viewer with no identifier would produce `viewer:player:`, whose id half is empty.
    - `ViewerSubject("spectator", "01ARZ…")` panics — the token is not one of the three tiers, and an unrecognized tier MUST NOT be silently carried into a subject string.
    - `ProfileResource("")` panics; `AdminSectionResource("")` panics.
    - `ProfileResource("01ARZ…")` returns `profile:01ARZ…`; `AdminSectionResource("characters")` returns `admin_section:characters`.
  </behavior>
  <action>
Add to `internal/access/prefix.go`, following the existing constant-block and
constructor conventions exactly:

Subject side — `SubjectViewer = "viewer:"`, plus the three tier tokens
`ViewerTierAnonymous = "anonymous"`, `ViewerTierGuest = "guest"`,
`ViewerTierPlayer = "player"` as exported string constants so no call site spells a
tier as a literal.

Resource side — `ResourceProfile = "profile:"` and
`ResourceAdminSection = "admin_section:"`. `admin_section` is an underscore-bearing
resource type; `ResourceCharacterDirectory` is the in-tree precedent that such a
type parses and matches through `resource is <type>`.

Append all three to `knownPrefixes`.

Constructors — `ViewerSubject(tier, playerID string) string`,
`ProfileResource(characterID string) string`,
`AdminSectionResource(sectionID string) string`. Each panics on an empty
identifier, and `ViewerSubject` additionally panics on a tier token that is not one
of the three constants. Copy `PlayerSubject`'s doc-comment rationale in substance —
empty subject strings would silently bypass access control if returned as the bare
prefix — because that reason is the whole point of the guard. `ViewerSubject`
returns `SubjectViewer + tier` on the anonymous rung (which carries no player
identifier, per §8.4.1's table) and `SubjectViewer + tier + ":" + playerID`
otherwise; the anonymous rung therefore ignores `playerID` and MUST panic if a
non-empty one is supplied, because that would signal a caller confusion about which
rung it is on.

Add a doc comment on `ViewerSubject` recording §8.4.1's trust requirement: the tier
token MUST be derived from the server-side session state the gateway already
authenticated, and MUST NOT be read from a client-supplied header, query parameter,
cookie value or request field. This constructor sits in the same trust position
`access.PlayerSubject` occupies today.

Extend `internal/access/prefix_test.go`'s known-prefix table with one row per new
constant, and add constructor tests including the panic cases (use
`assert.Panics`).
  </action>
  <verify>
    <automated>task test -- ./internal/access/</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'SubjectViewer|ResourceProfile|ResourceAdminSection' internal/access/prefix.go` matches all three constants
    - All three constants appear inside the `knownPrefixes` slice literal
    - `rg -n 'func ViewerSubject|func ProfileResource|func AdminSectionResource' internal/access/prefix.go` matches all three
    - `task test -- ./internal/access/` exits 0
    - `internal/access/prefix_test.go` contains `assert.Panics` assertions for `ViewerSubject` with an empty id on the player rung, `ViewerSubject` with an unrecognized tier, `ProfileResource("")` and `AdminSectionResource("")`
    - The known-prefix table has three new rows, each exercising `ParseEntityRef`
  </acceptance_criteria>
  <reversibility rating="costly">The prefix strings become part of every stored `access_policies` row's target and of Phase 4's subject construction; renaming one later means re-seeding policies and re-deriving fixtures.</reversibility>
  <done>All three prefixes parse, are registered, and cannot be constructed empty.</done>
</task>

<task type="tdd">
  <name>Task 2: ViewerTierProvider</name>
  <files>internal/access/policy/attribute/viewer.go, internal/access/policy/attribute/viewer_test.go</files>
  <read_first>
    - internal/access/policy/attribute/property.go (the whole file — the four-method provider surface, the omit-don't-sentinel `owner` branch with its `has_owner` witness on both paths and its ADR citation, `Schema()`, and the `oops.Code(...).With(...).Wrapf(...)` boundary style)
    - internal/access/policy/attribute/player.go lines 55-185 (`NewPlayerAttributeProvider`'s functional-option shape, `Namespace()`, `ResolveResource` returning nil/nil, `Schema()`, and `ResolveSubject`'s `SplitN` parse plus its early `return nil, nil` for a foreign entity type)
    - internal/access/policy/attribute/resolver.go lines 40-80 (how un-namespaced provider keys are namespaced at merge time, and where undeclared keys are dropped and counted)
    - .claude/rules/abac-providers.md (omit, do not sentinel; the `has_X` witness convention)
    - .planning/phases/01-portal-spec/01-SPEC.md §8.4.1 (the subject-form table and the three-key attribute table)
  </read_first>
  <behavior>
    - `Namespace()` returns `"viewer"`.
    - `ResolveResource(ctx, anything)` returns `(nil, nil)`.
    - `ResolveSubject(ctx, "character:01ARZ…")` returns `(nil, nil)` — a foreign entity type is not this provider's business, matching `PlayerAttributeProvider`'s early return.
    - `ResolveSubject(ctx, "viewer:anonymous")` returns a bag with `tier == "anonymous"` and `has_player_id == false`, and the key `player_id` is **absent** from the map.
    - `ResolveSubject(ctx, "viewer:guest:01ARZ…")` returns `tier == "guest"`, `player_id == "01ARZ…"`, `has_player_id == true`.
    - `ResolveSubject(ctx, "viewer:player:01ARZ…")` returns `tier == "player"`, `player_id == "01ARZ…"`, `has_player_id == true`.
    - `ResolveSubject(ctx, "viewer:spectator:01ARZ…")` returns an error carrying `oops.Code("INVALID_VIEWER_TIER")` — an unrecognized tier is rejected, never carried through as a tier value.
    - `ResolveSubject(ctx, "viewer:")` and `ResolveSubject(ctx, "viewer:player:")` each return an error carrying `oops.Code("INVALID_ENTITY_ID")`.
    - `ResolveSubject(ctx, "viewer:guest:not-a-ulid")` returns an error — the player identifier is validated as a ULID, matching `PlayerAttributeProvider`.
    - `Schema()` declares exactly `tier` (string), `player_id` (string), `has_player_id` (bool), `roles` (string list) and `has_roles` (bool).
    - A test asserts the returned map for the anonymous rung does **not** contain the key `player_id` — asserted by key presence, not by comparing the value to `""`.
    - Constructed with no role lookup, `ResolveSubject` on the player rung emits `has_roles == false` and **omits** the `roles` key entirely.
    - Constructed `WithViewerRoleLookup`, `ResolveSubject` on the player rung emits `roles` as a string list and `has_roles == true`; on the guest rung it does the same, because a guest is a player row too and §10.5's verdict is per player.
    - On the **anonymous** rung `roles` is omitted and `has_roles == false` — there is no player to resolve roles for, and an empty list would be a sentinel that `containsAny`-shaped conditions could match against.
    - When the role lookup returns an error, `ResolveSubject` logs at warn with the ctx and emits `has_roles == false` with `roles` **omitted** — fail-safe, matching `PlayerAttributeProvider`'s `is_guest` branch. It does NOT fail the whole resolution.
    - A test asserts the `roles` key is absent (comma-ok form) in each of the three omit cases, and that `has_roles` is present in all five cases.
  </behavior>
  <action>
Create `internal/access/policy/attribute/viewer.go` with
`type ViewerTierProvider struct{}` and `func NewViewerTierProvider() *ViewerTierProvider`,
implementing the four-method `AttributeProvider` surface.

`ResolveSubject` splits the subject on the first colon, returns `(nil, nil)` when
the entity type is not `viewer`, then parses the remainder: a remainder equal to
`access.ViewerTierAnonymous` is the anonymous rung; a remainder of the form
`<tier>:<ulid>` is the guest or player rung. Reject any other tier token with
`oops.Code("INVALID_VIEWER_TIER")`, an empty id half with
`oops.Code("INVALID_ENTITY_ID")`, and a non-ULID player identifier with
`oops.Code("INVALID_VIEWER_PLAYER_ID")`.

Emit `tier` on every successful path. Emit `player_id` **only** on the guest and
player rungs; on the anonymous rung the key is absent, never `""`. Emit
`has_player_id` on **both** branches. Carry the ADR holomush-ti1b rationale in a
comment at the branch, following `PropertyProvider`'s `owner` handling: an
empty-string sentinel satisfies `"" == ""` against any other unresolved peer
attribute and creates a fail-open match in a default-deny system.

**Add the `roles` attribute and its lookup seam — this closes half of the review's
viewer-identity defect (02-REVIEWS.md, 02-07 HIGH).** The shipped admin read policy
is `permit(principal is character, …) when { resource.property.visibility == "admin"
&& "admin" in principal.character.roles }`
(`internal/access/policy/seed.go:124-128`). Plan `02-07` must twin it for a
`viewer:` subject, and the viewer namespace as §8.4.1 declares it has **no roles
attribute at all** — so `principal.viewer.roles` would resolve to nothing and the
admin twin could never permit anyone, silently, because a missing key is false
(`internal/access/policy/dsl/evaluator.go:216`). Nothing in the suite would catch a
policy that can only deny. Fix it at the source rather than in the policy.

Declare in this file:

    type PlayerRoleLookup func(ctx context.Context, playerID string) ([]string, error)

as an **exported package-level type in `package attribute`** — plan `02-13` reuses
the same type for `PlayerAttributeProvider`'s `WithPlayerRoleLookup`, so both
providers answer "what roles does this player hold" through one signature and one
implementation. Add `WithViewerRoleLookup(PlayerRoleLookup)` as a functional option
mirroring `WithPlayerKindLookup`'s shape exactly (`player.go:55-185`).

Emit `roles` on the guest and player rungs when the lookup is configured and
succeeds; omit it (with `has_roles == false`) on the anonymous rung, when no lookup
is configured, and when the lookup errors — logging the error at warn with the ctx
and continuing, exactly as the `is_guest` branch does. Never emit an empty slice as
a stand-in for "unknown": an empty list is a *resolved* value that a future
`containsAny`-shaped condition would evaluate against, which is the list-flavored
version of the empty-string sentinel ADR holomush-ti1b forbids.

Record in a doc comment that the semantics are **per player** — the union of roles
held by any character of that player — matching §10.5's normative verdict and the
shipped `PlayerHasRole` join, and that this deliberately gives the same answer the
operator socket gives for the same human at the same moment.

`Schema()` declares all five keys — the resolver drops and counts any provider
attribute whose key is not in the namespace schema, so an undeclared `roles` is
silently absent rather than loudly wrong, which is precisely the invisible
fail-closed the review flagged.

**SPEC deviation to record.** §8.4.1's attribute table declares exactly three viewer
keys. This plan ships five. That is a deliberate amendment, not a drift: the table
is amended in plan `02-11` (Amendment F) with this rationale. Note it in the
SUMMARY so the amendment pass has the text it needs.

Write the tests against the real provider (not a double), with ACE sentence names,
asserting key absence via the two-value map lookup rather than value comparison.
  </action>
  <verify>
    <automated>task test -- ./internal/access/policy/attribute/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/access/policy/attribute/viewer.go` contains `func (p *ViewerTierProvider) Namespace() string` returning `"viewer"`
    - `rg -v '^\s*//' internal/access/policy/attribute/viewer.go | rg -c 'player_id\"\] = \"\"'` returns 0 — no empty-string sentinel is ever assigned
    - `rg -v '^\s*//' internal/access/policy/attribute/viewer.go | rg -c 'roles\"\] = \[\]string\{\}|roles\"\] = nil'` returns 0 — no empty-list sentinel either
    - `Schema()` declares exactly the five keys `tier`, `player_id`, `has_player_id`, `roles`, `has_roles`
    - `rg -n 'type PlayerRoleLookup func' internal/access/policy/attribute/viewer.go` matches, and `rg -n 'func WithViewerRoleLookup' internal/access/policy/attribute/viewer.go` matches
    - `task test -- ./internal/access/policy/attribute/` exits 0 with a subtest per `<behavior>` row
    - A subtest asserts anonymous-rung `player_id` absence using the comma-ok map form, and its failure message would distinguish "absent" from "present and empty"
    - Three subtests assert `roles` absence (comma-ok) for the anonymous rung, the no-lookup case and the lookup-error case, each paired with a configured-lookup case on the same fixture that DOES emit `roles`
  </acceptance_criteria>
  <reversibility rating="costly">The attribute names become policy text in every tier-floor and viewer-twin policy seeded by plan `02-07`; renaming one means re-seeding.</reversibility>
  <done>`ViewerTierProvider` resolves all three rungs, omits `player_id` on the anonymous rung, resolves `roles` per player behind an omit-don't-sentinel lookup seam, and declares every key it emits.</done>
</task>

<!-- Task 3 (player-scoped roles, PropertyProvider identity peers, provider
registration) MOVED to plan 02-13 in this revision. See the objective's scope-split
note: the specified `ABACConfig.RoleStore` wiring did not compile, and the fix needs
`internal/access/setup/subsystem.go` plus `property.go`, neither of which this plan
could own without a same-wave file collision. -->


</tasks>

<verification_integrity>
The six PORTAL-10 / §12.1 rules at their fixed numbering, specialized to this plan.

1. **Census with set equality.** This plan ships no census. Phase 2's rule-1 census
   scope is the character-name write-site census in plan `02-06`; EXT-04's admin
   section-registry ↔ descriptor census remains Phase 6's and is not claimed here.
2. **Paired positive control on every denial test.** This plan has no
   authorization denial tests — it ships subjects and attributes, not policies.
   Stated explicitly rather than omitted. Its nearest analogue is the panic and
   error assertions, each of which is paired with the succeeding call on the same
   fixture (`ViewerSubject` panics empty / returns for a valid id;
   `ResolveSubject` errors on a bad tier / resolves for a good one), so a rejection
   assertion cannot pass because the constructor rejects everything.
3. **Assertions against marshaled response bytes.** Does not apply — no RPC and no
   projection. The related discipline that *does* apply here is omit-don't-sentinel
   one layer in: attribute absence is asserted by key presence on the returned map,
   never by comparing a value to its zero.
4. **Gates demonstrated RED against the pre-fix state.** The new gate is the
   known-prefix table rows, which MUST be observed failing before the constants are
   added, with the recorded non-zero exit in this plan's SUMMARY.
   **The seed-coverage `viewer` no-WARN assertion has MOVED to plan `02-07`.** The
   review found the demonstration vacuous here (02-REVIEWS.md, 02-03 MEDIUM) and it
   is right: `warnOnMissingSeedCoverage` scans `policy.SeedPolicies()`
   (`internal/access/setup/seed_coverage.go:28`), and no seed references the viewer
   namespace until `02-07` seeds one. Removing the provider registration in this
   wave produces no viewer WARN either way, so the assertion could not distinguish a
   registered provider from an unregistered one — it would have been a gate that
   cannot fail. It belongs in the wave where viewer-referencing seeds exist.
5. **Wire-level assertion of every opacity and authorization contract.** No wire
   surface exists in this plan. The contract asserted instead is the trust-position
   one from §8.4.1: a test asserts `ViewerSubject` panics on an unrecognized tier
   token, which is what stops a client-supplied string from reaching a subject.
   `errutil.AssertErrorCode` is used to assert which internal code fired and is not
   cited as evidence about a caller-visible response.
6. **Invariant-scope discipline.** No registry invariant is pinned here and no
   `// Verifies:` annotation is written. INV-ACCESS-10 and INV-ACCESS-11 stay
   `pending` — they bind in Phase 4, against the read path this vocabulary serves.
</verification_integrity>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web request → viewer subject construction | The viewer's tier is decided here. A tier sourced from anything the client controls is an authorization decision made by the attacker. |
| `character_roles` storage → player-scoped role read | A character-scoped table read with player-scoped semantics; the blast radius of a role is the player, not the character. |
| policy DSL → attribute bag | An attribute the provider omits evaluates false; an attribute the provider sentinels can match a peer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Elevation of privilege | `ViewerSubject` | high | mitigate | A client-supplied tier would let a logged-out visitor present as `player`. Closed by constructing the subject only from server-side session state, by panicking on any token outside the three tier constants, and by a doc comment fixing the trust position. |
| T-02-12 | Elevation of privilege | `ViewerTierProvider` anonymous rung | high | mitigate | An empty-string `player_id` sentinel satisfies `"" == ""` against any other unresolved peer attribute and creates a fail-open match in a default-deny system. Closed by omitting the key and asserting its absence by key presence. |
| T-02-13 | Elevation of privilege | unregistered `viewer` namespace | high | mitigate | An unregistered provider makes every `principal.viewer.*` condition false and the whole family default-deny — fail-closed, but invisible, and the tempting repair is to widen the policies. Registration itself is plan `02-13`'s (it owns `setup.go`); this plan's contribution is shipping a provider whose `Schema()` declares every key it emits, so a registered provider cannot silently drop one. |
| T-02-14 | Spoofing | `ViewerTierProvider` id parse | medium | mitigate | A non-ULID player identifier is rejected rather than carried into the bag, matching `PlayerAttributeProvider`. |
| T-02-15 | Elevation of privilege | player-scoped role union semantics | medium | accept | A role held by any one character grants the player that role everywhere, on the web as on the operator socket. This is the shipped, documented semantics of `PlayerHasRole` and §10.5's normative verdict; matching it removes a second source of truth about a trust boundary. Tracked upstream as issue #4899. |
| T-02-16 | Information disclosure | role lookup failure | low | mitigate | A lookup error omits `roles` and emits `has_roles=false` rather than emitting a partial or empty list that could match a policy; logged at warn with ctx. |
| T-02-81 | Information disclosure | missing `viewer.roles` attribute | high | mitigate | Without a `roles` key on the viewer namespace, `seed:viewer-property-admin-read` would reference an attribute nothing supplies. A missing key is FALSE (`dsl/evaluator.go:216`), so the admin twin would deny every caller forever — silently, fail-closed, with no error and no failing test. The visible symptom is "admins see a bare profile", and the cheap repair is widening the policy. Closed by shipping `roles` here, declaring it in `Schema()`, and asserting each omit branch against a configured-lookup control. |
</threat_model>

<verification>
- `task test -- ./internal/access/...` green.
- `task lint` green.
- `task fmt` run and its edits committed.
</verification>

<success_criteria>
- `viewer:`, `profile:` and `admin_section:` parse, are in `knownPrefixes`, and cannot be constructed with an empty identifier.
- The anonymous rung's `player_id` key is absent, asserted by key presence.
- `principal.viewer.roles` resolves per player behind a lookup seam, making a viewer-flavored admin read policy expressible at all; it is omitted rather than emptied on every unresolved path.
- `Schema()` declares all five viewer keys, so the resolver cannot silently drop one.
</success_criteria>

## Artifacts this plan produces

New files: `internal/access/policy/attribute/viewer.go`,
`internal/access/policy/attribute/viewer_test.go`.

New exported symbols: `access.SubjectViewer`, `access.ResourceProfile`,
`access.ResourceAdminSection`, `access.ViewerTierAnonymous`,
`access.ViewerTierGuest`, `access.ViewerTierPlayer`, `access.ViewerSubject`,
`access.ProfileResource`, `access.AdminSectionResource`,
`attribute.ViewerTierProvider`, `attribute.NewViewerTierProvider`,
`attribute.PlayerRoleLookup`, `attribute.WithViewerRoleLookup`.

New attribute keys: `viewer.tier`, `viewer.player_id`, `viewer.has_player_id`,
`viewer.roles`, `viewer.has_roles`.

New error codes: `INVALID_VIEWER_TIER`, `INVALID_VIEWER_PLAYER_ID`.

Moved out of this plan in the reviews revision (now plan `02-13`):
`(*store.PostgresRoleStore).PlayerRoles`, `attribute.WithPlayerRoleLookup`,
`player.roles` / `player.has_roles`, the `PropertyProvider` identity peers, and all
provider registration in `internal/access/setup/`.

<output>
Create `.planning/phases/02-abac-schema-vocabulary/02-03-SUMMARY.md` when done.
</output>
