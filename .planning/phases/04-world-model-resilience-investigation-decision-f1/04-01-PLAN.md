---
phase: 04-world-model-resilience-investigation-decision-f1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/testsupport/integrationtest/options.go
  - internal/testsupport/integrationtest/harness.go
  - test/integration/resilience/resilience_suite_test.go
  - test/integration/resilience/chaos_helpers_test.go
  - test/integration/resilience/boot_smoke_test.go
autonomous: true
requirements: [OPS-05]
tags: [resilience, integration-test, harness, eventbus, nats]
must_haves:
  truths:
    - "Two in-process CoreServer replicas boot over ONE real NATS JetStream container and ONE shared Postgres database (D-03)"
    - "The resilience suite self-skips on the gating Integration Test lane (env unset) and runs when HOLOMUSH_RUN_QUARANTINED=1 (D-05)"
    - "An event published from replica A is observable on the shared broker from replica B's connection"
    - "TestQuarantineRegistryBijection stays green — the gate registers no quarantine marker"
  artifacts:
    - "internal/testsupport/integrationtest/options.go — WithExternalNATS(url) and WithSharedDatabase(connStr) StartOptions"
    - "internal/testsupport/integrationtest/harness.go — option resolution moved before DB/bus construction; ConnStr() and Bus() accessors"
    - "test/integration/resilience/resilience_suite_test.go — TestWorldModelResilience entry gated by quarantinetest.Enabled()"
    - "test/integration/resilience/chaos_helpers_test.go — startExternalNATS, startReplica, pauseBroker, unpauseBroker helpers"
    - "test/integration/resilience/boot_smoke_test.go — two-replica boot smoke spec"
  key_links:
    - "Start() resolves StartOptions BEFORE the Postgres and bus construction blocks, or the new seams are dead code"
    - "Both replicas build eventbus.Config identically (Mode+URL then .Defaults()) so CreateOrUpdateStream is idempotent on second boot (subsystem.go:264)"
    - "Suite gate uses quarantinetest.Enabled() with a message that does NOT match markerLineRE (test/meta/quarantine_registry_test.go:31) — no test/quarantine.yaml row"
---

<objective>
Build the two-replica resilience harness substrate for OPS-05: extend `integrationtest.Start` with external-NATS and shared-database seams (per D-03 — real `natstest` JetStream container + Postgres testcontainer + two in-process `CoreServer` replicas; the D-04 alternatives, docker-compose and single-process simulation, stay rejected and are NOT built), create the `test/integration/resilience/` suite skeleton gated off the PR lane per D-05 (seam (b): `quarantinetest.Enabled()` without a quarantine-registry marker), and prove two replicas genuinely share one broker + one database with a boot smoke spec.

Purpose: every later verdict (M12 in plan 02, M2 in plan 03) stands on this substrate being real — one DB, one broker, two production-path replicas — and on the suite never flaking the required `Integration Test` PR gate.
Output: harness StartOptions + accessors, gated suite skeleton, chaos helpers, passing two-replica boot smoke spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-CONTEXT.md
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-RESEARCH.md
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-PATTERNS.md
@internal/testsupport/integrationtest/harness.go
@internal/testsupport/integrationtest/options.go
@internal/testsupport/natstest/nats.go
@internal/testsupport/quarantinetest/quarantinetest.go
</context>

## Artifacts this phase produces (plan 01 slice)

New symbols created by this plan (source-grounding: exclude from drift verification):

| Artifact | Kind | Location |
|----------|------|----------|
| `WithExternalNATS(url string) StartOption` | option func | `internal/testsupport/integrationtest/options.go` |
| `WithSharedDatabase(connStr string) StartOption` | option func | `internal/testsupport/integrationtest/options.go` |
| `startConfig.externalNATSURL string` | config field | `internal/testsupport/integrationtest/harness.go` |
| `startConfig.sharedConnStr string` | config field | `internal/testsupport/integrationtest/harness.go` |
| `Server.connStr string` (field) + `func (s *Server) ConnStr() string` | accessor | `internal/testsupport/integrationtest/harness.go` |
| `func (s *Server) Bus() *eventbustest.Embedded` | accessor | `internal/testsupport/integrationtest/harness.go` |
| `TestWorldModelResilience` | Ginkgo suite entry | `test/integration/resilience/resilience_suite_test.go` |
| `startExternalNATS(ctx) *natstest.NATSEnv` | test helper | `test/integration/resilience/chaos_helpers_test.go` |
| `startReplica(t, url, connStr string, extra ...integrationtest.StartOption) *integrationtest.Server` | test helper | `test/integration/resilience/chaos_helpers_test.go` |
| `pauseBroker(ctx, env)` / `unpauseBroker(ctx, env)` | test helpers | `test/integration/resilience/chaos_helpers_test.go` |
| Two-replica boot smoke `Describe` | Ginkgo spec | `test/integration/resilience/boot_smoke_test.go` |
| `test/integration/resilience` package | new integration test package | (whole directory) |

<tasks>

<task type="auto">
  <name>Task 1: Add WithExternalNATS / WithSharedDatabase StartOptions and Server accessors to integrationtest</name>
  <files>internal/testsupport/integrationtest/options.go, internal/testsupport/integrationtest/harness.go</files>
  <read_first>
    - internal/testsupport/integrationtest/harness.go — Start body (lines ~291-400: KEK setup, DB at ~321-323, bus at ~367-368, option resolution at ~370-386), Server struct (~100-200), busEventAppenderAdapter (~1237-1291), Pool()/GameID() accessors (~888, ~1202)
    - internal/testsupport/integrationtest/options.go — the WithExtraPluginDir StartOption shape to copy exactly
    - internal/eventbus/eventbustest/embedded.go — Embedded struct (lines 48-80): exported fields Bus (*eventbus.Subsystem), JS, Conn; New() construction to mirror
    - internal/eventbus/subsystem.go — EnsureStream/desiredStreamConfig/verifyStream (lines 253-315: CreateOrUpdateStream is idempotent for identical config), Start (86), Stop (362), JS()/Conn()/GameID() accessors (405-417)
    - internal/eventbus/config.go — Config fields (Mode, URL, StoreDir, StreamMaxAge, DupeWindow, Provision, GameID) and Defaults()
    - test/integration/eventbus_external/external_boot_test.go — lines 55-102: the proven ModeExternal Config construction against natstest
  </read_first>
  <action>
    Implement the two harness seams from 04-RESEARCH.md "Recommended Project Structure" and 04-PATTERNS.md, per D-03.

    1. **Move option resolution to the top of Start.** The cfg-resolution block currently at harness.go ~373-385 (`cfg := &startConfig{accessEngine: &allowAllPolicyEngine{}}`, the opts loop, and the three compose-guard panics) must move to immediately after `ctx := context.Background()` (~line 294), BEFORE the KEK block. Leave `pe := cfg.accessEngine` (~386) and the withRealABAC block (~388-394) where they are — they only need cfg in scope. Without this move the new options are resolved after the DB/bus are already built and are dead code.

    2. **startConfig fields.** Add `externalNATSURL string` and `sharedConnStr string` to startConfig (harness.go, alongside extraPluginDirs).

    3. **options.go: two new StartOptions** following the WithExtraPluginDir shape exactly (exported func returning a closure that sets the field). `WithExternalNATS(url string)` — doc comment states: swaps the embedded eventbustest bus for a production external-mode `eventbus.Subsystem` dialing url; REQUIRES a running JetStream broker (normally `natstest.StartNATS`); safe for multiple replicas against one broker because EnsureStream uses CreateOrUpdateStream with identical config (cite subsystem.go). `WithSharedDatabase(connStr string)` — doc comment states: joins an existing per-test database instead of creating a fresh one; used by two-replica resilience suites (D-03, #4791); a second Start on a shared DB re-seeds its own guest location (benign — distinct ULIDs) and re-runs versioned plugin migrations (no-op); KEK env vars are re-pointed at the newest replica's ephemeral keyfile (benign while the resilience suite avoids WithPluginCrypto).

    4. **DB seam** (harness.go ~321-323): when `cfg.sharedConnStr != ""`, skip `testutil.SharedPostgres`/`testutil.FreshDatabase` and use `cfg.sharedConnStr` as connStr; otherwise keep the existing two lines unchanged. Retain connStr in a new Server field `connStr` (set where the Server literal is populated) and add accessor `func (s *Server) ConnStr() string`.

    5. **Bus seam** (harness.go ~367-368): when `cfg.externalNATSURL != ""`, instead of `eventbustest.New(t)` construct `eventbus.NewSubsystem` with `eventbus.Config{Mode: eventbus.ModeExternal, URL: cfg.externalNATSURL}.Defaults()` (the external_boot_test.go precedent; do NOT set StreamMaxAge/DupeWindow explicitly — relying on Defaults() guarantees every replica presents an identical desiredStreamConfig, keeping CreateOrUpdateStream a no-op on second boot). `require.NoError` on `sub.Start(ctx)`; register `t.Cleanup` that calls `sub.Stop(context.Background())` and logs (not fails) on error — deliberate chaos may have wedged the connection. Wrap as `&eventbustest.Embedded{Bus: sub, JS: sub.JS(), Conn: sub.Conn()}` and assign to the existing `bus` variable so ALL downstream harness wiring (publisher, subscriber, historyReader, GameID) works unchanged. Add accessor `func (s *Server) Bus() *eventbustest.Embedded { return s.bus }` with a doc comment naming its consumers (resilience suite stream inspection + M2 emitter wiring).

    6. Doc-comment style per WithFocusDelivery (harness.go ~239-254): state what the option changes, what it REQUIRES, and why default behavior is untouched (zero blast radius for existing suites — both fields default empty and both branches preserve the current code path verbatim).

    Run `task fmt` after edits and include its mutations in the commit.
  </action>
  <verify>
    <automated>task test:int -- -run TestNoSuchTestZZZ ./internal/testsupport/integrationtest/ ./test/integration/privacy/</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "func WithExternalNATS\(url string\) StartOption" internal/testsupport/integrationtest/options.go` — exactly 1 match
    - `rg -n "func WithSharedDatabase\(connStr string\) StartOption" internal/testsupport/integrationtest/options.go` — exactly 1 match
    - `rg -n "func \(s \*Server\) ConnStr\(\) string|func \(s \*Server\) Bus\(\) \*eventbustest.Embedded" internal/testsupport/integrationtest/harness.go` — 2 matches
    - Source order assertion: in harness.go, the line matching `cfg := &startConfig{` appears at a LOWER line number than the line matching `testutil.SharedPostgres` (option resolution precedes DB construction)
    - `task lint` exits 0
    - The verify command exits 0 (both integrationtest and an existing consumer package compile under -tags=integration with no behavior change; -run filter matches nothing so no specs execute)
  </acceptance_criteria>
  <done>Both StartOptions exist with the documented semantics, option resolution precedes both seams, default (option-less) Start behavior is byte-for-byte the old path, and existing integration packages still compile.</done>
</task>

<task type="auto">
  <name>Task 2: Create the gated resilience suite skeleton, chaos helpers, and two-replica boot smoke spec</name>
  <files>test/integration/resilience/resilience_suite_test.go, test/integration/resilience/chaos_helpers_test.go, test/integration/resilience/boot_smoke_test.go</files>
  <read_first>
    - .planning/phases/04-world-model-resilience-investigation-decision-f1/04-PATTERNS.md — the verbatim suite-entry, gate, helper, and Ordered/BeforeAll patterns to copy
    - test/integration/eventbus_external/external_boot_test.go — full file: suite entry shape, startExternalNATS helper, independent-conn stream inspection (lines 85-93)
    - test/integration/crypto/cache_invalidation_test.go — Ordered + BeforeAll/AfterAll one-container-per-suite topology (lines 28-82)
    - internal/testsupport/quarantinetest/quarantinetest.go — EnvVar, Enabled(); note Skip() is the marker-registering variant this suite must NOT call
    - test/meta/quarantine_registry_test.go — markerLineRE (line 31): the three marker patterns the suite files must not contain
    - internal/testsupport/integrationtest/session.go — Session struct exported fields (SessionID, CharacterID, LocationID), EmitDirectEvent (~770), MoveTo (~303)
    - internal/testsupport/natstest/nats.go — StartNATS, NATSEnv.URL, Conn(t), Terminate, Container field
  </read_first>
  <action>
    Create the new `test/integration/resilience` package (package name `resilience_test`). All three files carry the SPDX header pair + `//go:build integration` tag (04-PATTERNS.md "Shared Patterns").

    1. **resilience_suite_test.go** — package doc comment enumerating what the suite proves (two-replica world-model resilience: M12 lost-update verdict, M2 dual-write window, broker flap, replica restart, client reconnect; #4791, OPS-05) and WHY it is opt-in (D-05: two-replica chaos is CI-resource-sensitive; it runs on the existing nightly Quarantine Health lane via HOLOMUSH_RUN_QUARANTINED=1 and locally on demand; it is deliberately NOT a quarantine-registry entry because it is not a flaky spec — cite the D-05 NB from 04-CONTEXT.md). `TestWorldModelResilience(t *testing.T)` gates FIRST: if `!quarantinetest.Enabled()` then `t.Skipf("resilience harness is nightly/opt-in: set %s=1 to run (#4791)", quarantinetest.EnvVar)`, then RegisterFailHandler(Fail) + RunSpecs(t, "World-Model Resilience Suite"). CRITICAL: nowhere in this package may any comment, string, or call match markerLineRE (test/meta/quarantine_registry_test.go:31) — do not call the marker-registering skip helper and do not reproduce any of the three marker-pattern literals in prose; refer to them only by reference to the regex source line.

    2. **chaos_helpers_test.go** — package-private helpers in the external_boot_test.go style (Expect + DeferCleanup inside helpers):
       - `startExternalNATS(ctx context.Context) *natstest.NATSEnv` — StartNATS + Expect + DeferCleanup(Terminate). Copy from external_boot_test.go.
       - `startReplica(t *testing.T, url, connStr string, extra ...integrationtest.StartOption) *integrationtest.Server` — composes `integrationtest.Start(t, append([]integrationtest.StartOption{integrationtest.WithExternalNATS(url)}, extra...)...)`, adding `integrationtest.WithSharedDatabase(connStr)` when connStr is non-empty. Doc comment: replica 1 passes connStr="" (creates the fresh DB); replica 2 passes replica 1's ConnStr().
       - `pauseBroker(ctx context.Context, env *natstest.NATSEnv)` / `unpauseBroker(...)` — via `testcontainers.NewDockerClientWithOpts(ctx)` then `cli.ContainerPause(ctx, env.Container.GetContainerID())` / `ContainerUnpause` (04-RESEARCH.md Pattern 2, verified against pinned testcontainers-go v0.43.0). Doc comment: pause freezes the broker with networking intact (port-stable) — the primary flap primitive per RESEARCH "Alternatives Considered"; Stop/Start restart fidelity is a plan-03+ experiment.
    3. **boot_smoke_test.go** — one `Describe("Two-replica substrate", Ordered, ...)` with BeforeAll starting NATS via startExternalNATS, then:
       - Boot replica A: `startReplica(t, env.URL, "")`; boot replica B: `startReplica(t, env.URL, replicaA.ConnStr())`. Neither passes WithInTreePlugins (keep the smoke boot light).
       - It("replicas share one database"): create a location via `replicaA.NewLocation(ctx)`, then SELECT its row via `replicaB.Pool()` (`SELECT name FROM locations WHERE id = $1` — the harness escape-hatch idiom, direct SQL + descriptive require/Expect message) and assert it is found.
       - It("replicas share one broker"): connect a guest on replica A (`replicaA.ConnectGuest(ctx)`), record the EVENTS stream LastSeq via an INDEPENDENT connection (`env.Conn(t)` + `jetstream.New` + `js.Stream(ctx, eventbus.StreamName)` per external_boot_test.go:85-93), publish one event from replica A (`sess.EmitDirectEvent` on stream `character.<sess.CharacterID>` with a builtin type — verify the exact EmitDirectEvent signature and an accepted builtin type against session.go before writing), then poll the SAME independent connection until LastSeq advances past the recorded value (bounded Eventually, no sleeps). This proves one broker serves both replicas' subsystems.
       - It("replicas agree on GameID"): `Expect(replicaA.GameID()).To(Equal(replicaB.GameID()))`.
       - Watch for replica-2 boot failures from shared-DB re-seeding (RESEARCH Pitfall 3 / assumption A4). If replica B's boot fails on a duplicate-key or migration error, fix forward by making the failing seeding step tolerant under sharedConnStr in harness.go (document which step) — do NOT paper over with retries.
    Run `task fmt`; commit its mutations.
  </action>
  <verify>
    <automated>HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience -timeout 15m ./test/integration/resilience/</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 with all boot-smoke specs passed (judge by exit code; gotestsum summary is secondary confirmation)
    - Env-unset lane safety: `task test:int -- -run TestWorldModelResilience ./test/integration/resilience/` (WITHOUT the env var) exits 0 and executes 0 specs (skip observed in gotestsum output as secondary confirmation) — this is the D-05 load-bearing gate check (RESEARCH Pitfall 1)
    - `rg -n 'quarantined:|quarantinetest\.Skip\(|@quarantine|Label\("quarantine"' test/integration/resilience/` exits with NO matches (rg exit code 1) — the suite registers no quarantine marker
    - `task test -- -run TestQuarantineRegistryBijection ./test/meta/` exits 0 — the marker↔registry bijection is undisturbed
    - All three new files contain both the SPDX header and the integration build tag: `rg -L -c "go:build integration" test/integration/resilience/*.go` reports 3 files each with count ≥1
  </acceptance_criteria>
  <done>The gated suite runs only with HOLOMUSH_RUN_QUARANTINED=1, the boot smoke proves one DB + one broker + two replicas, and the quarantine registry meta-test is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testsupport → production | test-only allow-all ABAC engine and harness options must never be importable from production code |
| resilience suite → gating CI | an ungated two-replica chaos suite would land inside the required `Integration Test` PR check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | integrationtest allow-all engine / new StartOptions | medium | mitigate | all new code stays under `//go:build integration` in `internal/testsupport/` + `test/`; the existing depguard rule forbids production imports of eventbustest/coretest/natstest/quarantinetest — no depguard change in this phase |
| T-04-02 | Denial of Service | required `Integration Test` PR gate (CI resources) | high | mitigate | `quarantinetest.Enabled()` gate in the suite entry; Task 2 acceptance explicitly verifies the env-unset run executes 0 specs |
| T-04-03 | Tampering (supply chain) | dependencies | low | accept | zero new packages installed (RESEARCH Package Legitimacy Audit: not applicable); all libraries already pinned in go.mod |
</threat_model>

<verification>
- `task test` green (unit tier untouched)
- `task lint` green
- `task test:int -- -run TestWorldModelResilience ./test/integration/resilience/` (env unset) exits 0 with 0 specs run
- `HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience -timeout 15m ./test/integration/resilience/` exits 0, smoke specs green
- `task test -- -run TestQuarantineRegistryBijection ./test/meta/` exits 0
</verification>

<success_criteria>
- Two replicas boot over one natstest broker + one shared database and prove it (smoke spec green)
- D-05 gate verified in both directions (skips without env, runs with env)
- Default `integrationtest.Start` behavior unchanged for every existing suite (compile + scoped run of an existing consumer package)
</success_criteria>

<output>
Create `.planning/phases/04-world-model-resilience-investigation-decision-f1/04-01-SUMMARY.md` when done. Record: exact option/accessor names added, any Pitfall-3/A4 re-seeding fixes applied to harness.go, and the observed smoke-suite runtime (feeds plan 02/03 timeout budgets).
</output>
