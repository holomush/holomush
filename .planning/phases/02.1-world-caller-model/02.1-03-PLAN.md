---
phase: 02.1-world-caller-model
plan: 03
type: execute
wave: 3
depends_on: [02.1-02]
files_modified:
  - test/meta/world_caller_census_test.go
  - docs/architecture/invariants.yaml
  - docs/architecture/invariants.md
autonomous: true
requirements: [AUTHZ-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A go/ast census test in test/meta asserts that every *Service method whose body references s.checkAccess declares world.Caller in its second parameter slot — the structural predicate catches a future 24th command regressing to a bare subject string, which the compiler would happily accept (D-65)."
    - "world.Service.UpdateCharacterPreferences is named explicitly in the census test's exemption mechanism with the D-05 rationale cited, and the test asserts the structural predicate agrees with that named exemption rather than merely tolerating it (D-68)."
    - "INV-WORLD-8 exists in docs/architecture/invariants.yaml with binding: bound and asserted_by listing both the census test and the internal/world attribute-forwarding proof; its summary carves out UpdateCharacterPreferences explicitly so the summary claims no more than the binding proves (the INV-WORLD-6 lesson)."
    - "The INV-WORLD scope's description and boundary in docs/architecture/invariants.yaml are amended in the same change to describe the world.Service command-API caller-argument shape the scope now contains, and the invariant stays in INV-WORLD rather than moving to INV-ACCESS (D-67)."
    - "docs/architecture/invariants.md is regenerated by go run ./cmd/inv-render and no byte inside a BEGIN/END GENERATED region was hand-edited."
    - "The four registry meta-tests pass: TestEveryRegistryInvariantHasBinding, TestProvenanceGuard, TestBoundInvariantsAreGenuinelyAsserted, TestRegistryBindingChecks."
    - "abac-reviewer returns READY for the phase's diff even though no policy text changed (ROADMAP criterion 5)."
    - "task pr-prep is green inline before the phase is handed off."
    - statement: "The census test compares parameter TYPE expressions, not parameter names, and handles a grouped parameter declaration where one ast.Field carries two Names — so 'params list index 1' is not conflated with 'parameter 1'."
      verification: backstop
  artifacts:
    - test/meta/world_caller_census_test.go
    - docs/architecture/invariants.yaml
    - docs/architecture/invariants.md
  key_links:
    - "test/meta/world_caller_census_test.go -> the // Verifies: INV-WORLD-8 annotation -> the registry's binding ratchet (test/meta/invariant_registry_test.go's Verifies parser)."
    - "bodyReferencesSelector(fn.Body, recvName, \"checkAccess\") -> the self-maintaining discriminator between authorizing commands and the D-05 exemption."
    - "docs/architecture/invariants.yaml -> go run ./cmd/inv-render -> docs/architecture/invariants.md generated regions."
  prohibitions:
    - "MUST NOT fabricate a binding: the // Verifies: annotation goes only on a test that genuinely asserts INV-WORLD-8. If no such test exists, leave the entry pending and file a coverage issue."
    - "MUST NOT bind INV-WORLD-8 to a Skip-only or placeholder test."
    - "MUST NOT list asserted_by while the entry is pending."
    - "MUST NOT hand-edit inside the BEGIN GENERATED / END GENERATED regions of docs/architecture/invariants.md."
    - "MUST NOT renumber or move an existing INV-WORLD id; renaming a registered id is a migration, not an edit."
    - "MUST NOT move this invariant to the INV-ACCESS scope — D-67 pins it to INV-WORLD and amends the scope text instead."
    - "MUST NOT write an invariant summary that claims every public world.Service command takes the caller value without carving out UpdateCharacterPreferences — that is literally false and reproduces the INV-WORLD-6 defect D-65 exists to avoid."
    - "MUST NOT register the action schema namespace (02.2 D-59)."
    - "MUST NOT hand-edit .planning/ROADMAP.md or .planning/STATE.md."
---

<objective>
Pin the guarantee so it cannot silently regress, then close the phase's gates.

Mint and bind `INV-WORLD-8` with a census-style go/ast test in `test/meta/` that walks
`internal/world/service.go` and asserts every authorizing `*Service` method's second parameter type
is `world.Caller`. Amend the `INV-WORLD` scope's own description and boundary in the same change so
the registry does not describe a scope its newest member falls outside of (D-67). Then run the
phase's two human-facing gates: the `abac-reviewer` domain review (ROADMAP criterion 5) and
`task pr-prep`.

Purpose: the flip is enforced by the compiler only for code that exists today. A future 24th
`world.Service` command written with a bare `subjectID string` parameter compiles fine and quietly
reopens the defect this whole phase closed. The census test is the only thing that catches it.

Output: `test/meta/world_caller_census_test.go`, the `INV-WORLD-8` registry entry, the regenerated
invariants document, and READY from `abac-reviewer`.

**D-64 commit mapping.** This plan is landmark (3) — proofs, invariant binding, and cleanups. The
criterion-2 proof landed early in Plan 01 because the tracer needed it as a real end-to-end verify;
this plan re-runs it post-flip rather than re-authoring it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-world-caller-model/02.1-CONTEXT.md
@.planning/phases/02.1-world-caller-model/02.1-RESEARCH.md
@.planning/phases/02.1-world-caller-model/02.1-PATTERNS.md
@.planning/phases/02.1-world-caller-model/02.1-01-SUMMARY.md
@.planning/phases/02.1-world-caller-model/02.1-02-SUMMARY.md
@.claude/rules/invariants.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Census test binding INV-WORLD-8 — every authorizing world.Service command takes the caller value</name>

  <files>test/meta/world_caller_census_test.go (new)</files>

  <read_first>
- `test/meta/world_envelope_census_test.go` in full — this is the machinery to copy, and it is
  `package meta` with no build tag, so it runs under `task test`. Read specifically:
  `outOfServiceOnlyKinds` at :49-60 (the exemption-as-named-function idiom, one commented member per
  justifying decision, never an inline name comparison); `serviceMutatingMethods` at :111-141, which
  already parses `internal/world/service.go` and already returns an `allMethods` set that the
  existing test discards; `serviceReceiverName` at :145-158, which isolates `*Service` methods;
  `bodyReferencesSelector` at :162-180, the generic `ast.Inspect` selector walker; and
  `TestWorldEnvelopeCensusMatchesServiceMutatingMethods` at :187-207 for the two-direction
  set-equality assertion shape with failure messages that name both the failure and the decision.
  **Note the path: the machinery is at `test/meta/world_envelope_census_test.go`. CONTEXT.md D-65
  cites `internal/world/world_envelope_census_test.go`, which does not exist — do not plan against
  that path.**
- `test/meta/meta_helpers_test.go` line 33 — `findRepoRoot`, the helper the parse machinery uses.
- `internal/world/service.go` lines 845-870 — `UpdateCharacterPreferences` and its doc comment
  stating it deliberately runs no `checkAccess` because it is the settings-subsystem persistence
  primitive (D-05). This is the single exemption.
- `internal/world/service.go` lines 1090-1100 and 1140-1150 — the two commands whose parameters were
  grouped before the flip, so you can see how the post-flip declarations actually read and confirm
  the test walks type expressions rather than name positions.
- `internal/access/policy/seed_smoke_test.go` line 958 — the in-repo `// Verifies: INV-<SCOPE>-N`
  annotation precedent.
- `test/meta/invariant_registry_test.go` lines 155-175 — the `Verifies:` regexp the binding ratchet
  parses, and lines 225-245, 370-390, 850-870, 1120-1140 for the four gate tests this must satisfy.
- `.claude/rules/invariants.md` — the define / respect / bind / regenerate workflow and the
  never-fabricate-a-binding rule.
  </read_first>

  <behavior>
Write the test first and watch it go red before the annotation and registry entry exist, then green.

`TestWorldServiceCallerParamIsTypedOnEveryAuthorizingCommand` (the name prefix-matches the
`TestWorldServiceCallerParam` run-filter seeded in `02.1-VALIDATION.md`):

- Parses `internal/world/service.go` with go/ast and collects, for each `*Service` method, whether
  its body references the receiver's `checkAccess` selector — the structural signal for "this method
  performs an authorization check".
- For every method in that authorizing set, asserts the second parameter's TYPE expression is the
  identifier `Caller`. The assertion is on the type expression, never on the parameter name, and it
  handles the case where one `*ast.Field` carries two names — so a grouped declaration cannot slip
  through by shifting the index.
- Asserts that the set of exported `*Service` methods that are NOT in the authorizing set equals
  exactly the documented exemption set. Failing that assertion means either a command lost its
  authorization check or a new non-authorizing command appeared undocumented; both deserve a human
  look.
- The exemption set is a named function returning a set with exactly one member,
  `UpdateCharacterPreferences`, carrying a comment citing D-05 and `internal/world/service.go`'s own
  doc comment as the justification. This is the `outOfServiceOnlyKinds` idiom — never an inline name
  comparison.
- Failure messages name both the failure and the decision, in the style of the existing census
  (for example: this method performs an authorization check but does not take the caller value —
  AUTHZ-01 / INV-WORLD-8 forbid a bare subject-string parameter).

A second assertion in the same file or a sibling test: no `*Service` method declares a `string`
type in its second parameter slot. This is the "no escape hatch" half of criterion 1 and it holds
for `UpdateCharacterPreferences` without an exemption, because its second parameter is a ULID.
  </behavior>

  <action>
Create `test/meta/world_caller_census_test.go`, `package meta`, with no build tag so it runs in the
untagged `task test` lane alongside the existing census.

Reuse the existing parse machinery rather than duplicating it. `serviceMutatingMethods` already
parses `internal/world/service.go`, already isolates `*Service` methods via `serviceReceiverName`,
and already builds the `allMethods` set that today's caller discards with a blank identifier. Write
a sibling collector in the new file that reuses `serviceReceiverName`, `bodyReferencesSelector`, and
`findRepoRoot`, and returns the authorizing set plus the full exported-method set together with each
method's `*ast.FuncDecl` so the parameter walk has something to inspect. Do not modify
`test/meta/world_envelope_census_test.go`.

Use `bodyReferencesSelector(fn.Body, recvName, "checkAccess")` as the authorizing predicate. This is
the structurally correct discriminator and it is self-maintaining: it needs no list of the 23
command names, and if someone later adds an authorization check to a command that lacks one, the
predicate follows automatically. It is also exactly what distinguishes the D-05 exemption, whose
body contains no such call.

Walk parameters by flattening `fn.Type.Params.List` into a per-parameter sequence, expanding any
field carrying multiple `Names` into that many entries and carrying the shared type expression on
each. Assert on the flattened index-1 entry's type expression. Do not index `Params.List[1]`
directly: after the flip the declarations are ungrouped, but a future grouped declaration would
silently shift the meaning of that index, and the whole point of this test is to survive future
edits.

Add the exemption as a named function in the same file returning a set whose single member is
`UpdateCharacterPreferences`, with a comment naming D-05 and pointing at the doc comment in
`internal/world/service.go` that records why that command deliberately runs no authorization check.
Then assert set equality between "exported `*Service` methods not in the authorizing set" and that
exemption set — so the exemption is not merely tolerated but pinned, and adding a `checkAccess` call
to `UpdateCharacterPreferences` or introducing a second unauthorized command both fail loudly. D-68
requires the exemption be named explicitly rather than left implicit; pairing the structural
predicate with a named, asserted exemption satisfies both halves.

Annotate the test with the `// Verifies: INV-WORLD-8` line immediately above the test function, in
the form the registry's parser matches (the precedent is `internal/access/policy/seed_smoke_test.go`
line 958). The annotation is what flips the registry entry from pending to bound in Task 2 — do not
add it to any test that does not genuinely assert the invariant.

Follow the repo's ACE test-naming rule: the function name is a sentence stating action, condition,
and expectation.
  </action>

  <verify>
    <automated>task test -- -run 'TestWorldServiceCallerParam' ./test/meta/</automated>
    <automated>task test -- ./test/meta/</automated>
    <automated>task lint</automated>
  </verify>

  <acceptance_criteria>
- `test/meta/world_caller_census_test.go` exists, declares `package meta`, and carries no build tag
  (`rg -c '^//go:build' test/meta/world_caller_census_test.go` returns 0).
- `task test -- -run 'TestWorldServiceCallerParam' ./test/meta/` exits 0.
- `rg -n '// Verifies: INV-WORLD-8' test/meta/world_caller_census_test.go` returns exactly one line,
  and the line immediately following it is a test function declaration.
- `rg -n 'bodyReferencesSelector\([^,]+, [^,]+, "checkAccess"\)' test/meta/world_caller_census_test.go`
  returns at least one line — the authorizing predicate is the structural selector walk, not a
  hardcoded list of the 23 command names.
- `rg -n 'UpdateCharacterPreferences' test/meta/world_caller_census_test.go` returns a line inside a
  named exemption function whose preceding comment cites D-05.
- The test fails when the invariant is violated. Prove it: temporarily change one migrated
  `world.Service` command's second parameter back to a bare string, confirm
  `task test -- -run 'TestWorldServiceCallerParam' ./test/meta/` exits non-zero, then revert.
  Record the observed failure message in the plan summary.
- `git diff test/meta/world_envelope_census_test.go` is empty — the existing census was not modified.
- `task test -- ./test/meta/` exits 0 and `task lint` exits 0.
  </acceptance_criteria>

  <reversibility rating="reversible">A meta test is additive; deleting it costs only the regression guard.</reversibility>

  <done>
The census test exists, is annotated `// Verifies: INV-WORLD-8`, asserts the caller-parameter type
on every structurally-identified authorizing command, pins the single D-05 exemption by name, and
was demonstrated to go red against a deliberately reverted signature.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register INV-WORLD-8, amend the INV-WORLD scope text (D-67), and regenerate the rendered registry</name>

  <files>docs/architecture/invariants.yaml, docs/architecture/invariants.md</files>

  <read_first>
- `docs/architecture/invariants.yaml` lines 778-800 — the `INV-WORLD` scope block: its
  `description`, its MODEL-01-derived `boundary` (which currently says nothing about authorization
  plumbing), its `status: pending` note and the provenance rationale embedded in it, its
  `origin_specs`, and its `owned_paths` (`internal/world/**`, `internal/world/outbox/**`,
  `internal/world/postgres/**`).
- `docs/architecture/invariants.yaml` lines 5018-5125 — the seven existing `INV-WORLD` entries at
  :5025, 5036, 5047, 5057, 5084, 5095, 5108. `INV-WORLD-4` at :5057-5083 is the `bound`-form entry
  to copy, and note its `asserted_by` freely lists `test/meta/world_sql_fence_test.go`, a file
  outside the scope's `owned_paths` — so a `test/meta/` binding is precedented.
- `docs/architecture/invariants.yaml` lines 565-585 — the sibling `INV-ACCESS` scope, whose boundary
  ("Access control evaluation") is the alternative D-67 explicitly declines.
- `.claude/rules/invariants.md` — the hand-registration requirement for ids minted in a GSD phase
  artifact (a `.planning/phases/**/*-SPEC.md` or CONTEXT is outside the orphan-check walk root, so
  nothing auto-catches a missing entry), and the binding ratchet's five steps.
- `internal/world/service.go` lines 845-870 — the D-05 rationale the summary must cite by name.
- `cmd/inv-render/` — the renderer invoked as `go run ./cmd/inv-render`; `Taskfile.yaml` lines
  225-232 for the wrapping task target if one exists.
  </read_first>

  <action>
Hand-add an `INV-WORLD-8` entry to `docs/architecture/invariants.yaml`, copying the shape of the
`bound`-form `INV-WORLD-4` entry: `id`, `scope: INV-WORLD`, an `origin_spec` pointing at this
phase's artifacts, `summary`, `binding: bound`, and `asserted_by`.

Write the summary so it claims no more than the binding proves. The naive phrasing — every public
`world.Service` command takes the typed caller value — is literally false, because
`UpdateCharacterPreferences` takes no subject at all and deliberately runs no authorization check.
That is exactly the defect D-65 cites INV-WORLD-6 for. Phrase the summary as: every public
`world.Service` command that performs an authorization check takes the typed caller value in its
second parameter slot rather than a bare subject string, and `checkAccess` forwards that caller's
attribute channel to the ABAC request rather than a hardcoded nil — with
`UpdateCharacterPreferences` named as the single deliberate exception and D-05 plus
`internal/world/service.go`'s own doc comment cited as its justification (D-68). Prefix the summary
with a short uppercase tag in the style the neighbouring entries already use.

`asserted_by` must list both halves, because the census test proves the parameter-shape half and the
`internal/world` unit test from Plan 01 proves the attribute-forwarding half:
`test/meta/world_caller_census_test.go` and `internal/world/caller_test.go`. Do not claim a binding
for anything neither file asserts; if the summary grows a clause with no assertion behind it, narrow
the summary rather than the truth.

Amend the `INV-WORLD` scope block in the same change (D-67). Its `description` and `boundary` are
MODEL-01-derived and describe world-model write integrity only; INV-WORLD-8 is about the shape of
`world.Service`'s command API and its authorization plumbing, which is world-owned but is not "born
from the MODEL-01 ADR". Extend the `description` to name the command-API caller-argument shape, and
extend the `boundary` to state explicitly that the scope now includes the `world.Service` command-API
caller argument and `checkAccess`'s forwarding of the caller's execution context to the ABAC request
(AUTHZ-01, phase 02.1), while continuing to exclude ABAC policy-evaluation semantics, the S1 gate,
and attribute-provider behavior, which belong to `INV-ACCESS`. Keep the invariant in `INV-WORLD`;
do not move it to `INV-ACCESS`, and do not renumber any existing id. Preserve the existing
`status: pending` note and its provenance rationale verbatim — it explains a residual-walk
misattribution and removing it would break the provenance guard.

The description and boundary are folded YAML scalars spanning multiple lines. Edit them carefully
enough that the file still parses; the registry meta-tests will catch a malformed edit, but so will
a quick `go run ./cmd/inv-render` before you commit.

Regenerate the rendered document with `go run ./cmd/inv-render` (or the `Taskfile.yaml` target that
wraps it). Never hand-edit anything inside the generated regions of
`docs/architecture/invariants.md`; the generate-and-diff meta-test exists precisely to catch that.

Then run the four registry gate tests. If `TestBoundInvariantsAreGenuinelyAsserted` fails, the
binding is fabricated — fix the test or downgrade the entry to pending and file a coverage issue
with `gh issue create -R holomush/holomush`; do not paper over it.
  </action>

  <verify>
    <automated>go run ./cmd/inv-render &amp;&amp; git diff --exit-code docs/architecture/invariants.md</automated>
    <automated>task test -- -run 'TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted|TestRegistryBindingChecks|TestOwnedPathsPartition' ./test/meta/</automated>
    <automated>task test -- ./test/meta/</automated>
  </verify>

  <acceptance_criteria>
- `rg -o '^  - id: INV-WORLD-[0-9]+' docs/architecture/invariants.yaml` includes `INV-WORLD-8` and
  the maximum is 8; no pre-existing id changed.
- The `INV-WORLD-8` entry has `binding: bound` and an `asserted_by` list containing both
  `test/meta/world_caller_census_test.go` and `internal/world/caller_test.go`.
- The `INV-WORLD-8` summary names `UpdateCharacterPreferences` as the single exception and cites
  D-05 (`rg -n 'UpdateCharacterPreferences' docs/architecture/invariants.yaml` returns a line inside
  the new entry).
- `rg -n -A2 'name: INV-WORLD$' docs/architecture/invariants.yaml` shows a `description` that names
  the command-API caller argument, and the `boundary` names AUTHZ-01 / 02.1 and still excludes ABAC
  policy evaluation.
- `rg -c 'INV-ACCESS' docs/architecture/invariants.yaml` is unchanged from before this task — the
  invariant did not move scopes.
- `go run ./cmd/inv-render` followed by `git diff --exit-code docs/architecture/invariants.md` exits
  0 (the checked-in render is current).
- `git diff docs/architecture/invariants.md` contains no change outside a
  BEGIN GENERATED / END GENERATED region.
- `task test -- -run 'TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted|TestRegistryBindingChecks|TestOwnedPathsPartition' ./test/meta/` exits 0.
- `task test -- ./test/meta/` exits 0.
  </acceptance_criteria>

  <reversibility rating="costly">Registering an invariant id is effectively permanent — renaming or renumbering a registered id is a migration, not an edit (`.claude/rules/invariants.md`), and the id is referenced from the census test and this phase's artifacts.</reversibility>

  <done>
`INV-WORLD-8` is registered with `binding: bound`, an honest summary that carves out the D-05
exception, and an `asserted_by` list covering both halves of the claim. The `INV-WORLD` scope's
description and boundary describe what the scope now contains. The rendered registry is regenerated
and current, and all registry gate tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the phase gates — full-suite re-verification, abac-reviewer READY, pr-prep</name>

  <files>(no source changes expected; this task verifies and reports)</files>

  <read_first>
- `.planning/phases/02.1-world-caller-model/02.1-VALIDATION.md` — the requirement-to-test map, the
  sampling rate, and the manual-only verification rows (the `abac-reviewer` gate and the per-site
  subject-mapping checklist).
- `.planning/phases/02.1-world-caller-model/02.1-RESEARCH.md` § "Q2" — the 27-row per-site subject
  map, which is the human checklist for the byte-identity review.
- `CLAUDE.md` § "Pre-Push Review Gates" — the `abac-reviewer` contract: grounded findings citing
  `path:line`, a binary READY / NOT READY verdict, read-only by construction.
- `site/src/content/docs/contributing/how-to/pr-prep.md` — how to read the `pr-prep` result (exit
  code first; go-task collapses failures to 201; the authoritative verdict is in the
  `pr-prep result:` file). Judge success by exit code, never by grepping stdout for a string.
  </read_first>

  <action>
Re-run the phase's full verification surface now that the invariant and census are in place, then
close the two gates.

Run `task fmt`, `task lint`, `task test`, and `task test:int` in that order. `task test:int` needs
Docker and is not optional: 43 of this phase's migrated call sites live behind
`//go:build integration` and are invisible to the untagged lane.

Re-run the two proofs authored in Plan 01 explicitly, so the phase summary records them as green
against the fully-flipped tree rather than against the tracer slice: the attribute-forwarding proof
(ROADMAP criterion 2) and the system-caller S1 proof (ROADMAP criterion 4), plus the nine
empty-subject subtests of `TestWorldService_MalformedAccessParams`.

Re-prove D-63's review contract one last time on the final tree: re-run all four committed
`scripts/codemod` rules with `-U` on a clean working directory and confirm `git diff --exit-code` is
clean.

Then run the `abac-reviewer` domain gate — invoke the `holomush-dev:review-abac` skill over this
phase's changed files (ROADMAP criterion 5). It must return READY. It fires on this phase even
though no ABAC policy text changed, because the refactor changes how the subject and its execution
context reach the engine. Feed it the 27-row subject map from RESEARCH § Q2 as the byte-identity
checklist and the four bootstrap sites as the highest-risk rows. If it returns NOT READY, address
every finding and re-run — do not proceed with an unresolved finding, and do not skip the gate.

Finally run `task pr-prep` inline in this session (not via an offload agent — the final gate before
push runs in the parent per the repo rule). Read the exit code first; go-task collapses any failure
to 201, and the authoritative verdict lives in the `pr-prep result:` file. Distinguish lock
contention from failure by behavior, never by matching a string in the output.

Record in the plan summary: the `abac-reviewer` verdict verbatim, the `pr-prep` result, whether
Docker was available for `task test:int`, and the residual follow-ups this phase deliberately did
not take — retiring `access.WithSystemSubject` (now zero production producers outside the caller
type), deleting the Deprecated `hostfunc.WorldService`, and adding `ast-grep` to `task setup`.
  </action>

  <verify>
    <automated>task fmt &amp;&amp; task lint</automated>
    <automated>task test</automated>
    <automated>task test:int</automated>
    <automated>task test -- -run 'TestWorldServiceCallerAttributesReachActionBag|TestWorldServiceSystemCallerSatisfiesTheS1GateWithoutAmbientContext|TestWorldService_MalformedAccessParams' ./internal/world/</automated>
    <automated>task pr-prep</automated>
    <human-check>`holomush-dev:review-abac` returned READY over this phase's changed files, with no unresolved finding. Paste the verdict line into the plan summary.</human-check>
  </verify>

  <acceptance_criteria>
- `task fmt` leaves `git status --porcelain` clean; `task lint` exits 0.
- `task test` exits 0.
- `task test:int` exits 0 (Docker available). If Docker is unavailable, the task halts and reports
  rather than declaring the phase verified.
- `task test -- -run 'TestWorldServiceCallerAttributesReachActionBag|TestWorldServiceSystemCallerSatisfiesTheS1GateWithoutAmbientContext|TestWorldService_MalformedAccessParams' ./internal/world/` exits 0.
- Re-running all four `scripts/codemod` rules with `-U` on a clean tree leaves
  `git diff --exit-code` clean.
- `task pr-prep` exits 0, judged by exit code, and the `pr-prep result:` file records a pass.
- The `abac-reviewer` gate returned READY, and the verdict is quoted in
  `.planning/phases/02.1-world-caller-model/02.1-03-SUMMARY.md`.
- `rg -n 'access\.WithSystemSubject\(' --glob '*.go' --glob '!*_test.go' internal/ cmd/ | rg -v '^internal/access/context\.go'` returns exactly one line, inside `internal/world/caller.go` — confirming ROADMAP criterion 4 across the whole tree.
- The plan summary records the three deliberate follow-ups (retire `access.WithSystemSubject`,
  delete the Deprecated `hostfunc.WorldService`, add `ast-grep` to `task setup`) as candidates, not
  as omissions.
  </acceptance_criteria>

  <precondition>Docker is running and available to testcontainers (`docker info` exits 0) — `task test:int` and `task pr-prep`'s integration surface depend on it.</precondition>

  <reversibility rating="reversible">Verification only; no source change.</reversibility>

  <done>
`task fmt`, `task lint`, `task test`, `task test:int`, and `task pr-prep` are all green inline; both
ROADMAP proofs and the nine empty-subject subtests pass against the fully-flipped tree; the codemod
re-run is diff-clean; `abac-reviewer` returned READY with its verdict recorded; the residual
follow-ups are written down.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future contributor → `world.Service` | A 24th command written with a bare subject string compiles cleanly; only the census test stands between that and a silently reopened defect. |
| invariant registry → CI | A fabricated or over-broad binding makes the registry assert a guarantee no test proves. |
| the phase's diff → `abac-reviewer` | The last human-facing check on an access-control surface change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-09 | Elevation of Privilege | a future `world.Service` command regressing to a bare subject string | high | mitigate | `test/meta/world_caller_census_test.go` asserts the caller-parameter type on every method structurally identified as authorizing, and is demonstrated to go red against a deliberately reverted signature before it is trusted. |
| T-02.1-10 | Repudiation | the `INV-WORLD-8` registry entry | medium | mitigate | The summary is narrowed to what the two `asserted_by` files actually prove and names the D-05 exception explicitly; `TestBoundInvariantsAreGenuinelyAsserted` rejects a Skip-only binding; a coverage issue is filed rather than a binding fabricated. |
| T-02.1-01 | Elevation of Privilege | the whole phase diff, reviewed for subject drift | high | mitigate | `abac-reviewer` (ROADMAP criterion 5) is a blocking gate with a binary verdict, fed the 27-row byte-identity checklist and pointed at the four `system:bootstrap` rows as the highest-risk mapping. |
| T-02.1-08 | Elevation of Privilege | integration-tier coverage | high | mitigate | `task test:int` is re-run on the final tree with an explicit Docker precondition; the task halts rather than declaring the phase verified without it. |
| T-02.1-05 | Denial of Service | `internal/access/policy/compiler.go` `action` namespace | medium | mitigate | Out of scope by decision (02.2 D-59); this plan registers nothing in the schema registry and the prohibition is carried in `must_haves`. |
| T-02.1-SC | Tampering | package-manager installs | low | accept | No npm/pip/cargo package is installed. No Package Legitimacy Audit is applicable. |
</threat_model>

<verification>
- `task fmt`, `task lint`, `task test`, `task test:int`, and `task pr-prep` all exit 0.
- `go run ./cmd/inv-render` produces no diff against the committed `docs/architecture/invariants.md`.
- The four registry gate tests pass.
- The census test was observed failing against a deliberately reverted signature and passing after
  the revert was undone.
- `abac-reviewer` returned READY.
</verification>

<success_criteria>
- `INV-WORLD-8` is registered, rendered, and genuinely bound by a test that goes red on regression.
- The `INV-WORLD` scope text describes the caller-argument shape it now contains (D-67).
- `UpdateCharacterPreferences` is carved out explicitly in both the summary and the census
  exemption (D-68).
- ROADMAP criterion 5: `abac-reviewer` returns READY.
- All five ROADMAP success criteria are verified green against the fully-flipped tree.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind |
|----------|------|
| `test/meta/world_caller_census_test.go` | new file (`package meta`, no build tag) |
| `TestWorldServiceCallerParamIsTypedOnEveryAuthorizingCommand` | new test (criterion 1, binds INV-WORLD-8) |
| the named exemption function returning `{UpdateCharacterPreferences}` | new unexported helper in that file |
| the authorizing-set collector reusing `serviceReceiverName` / `bodyReferencesSelector` / `findRepoRoot` | new unexported helper in that file |
| `INV-WORLD-8` | new invariant id in `docs/architecture/invariants.yaml` |
| the amended `INV-WORLD` scope `description` / `boundary` | changed registry scope text |
| regenerated `docs/architecture/invariants.md` generated regions | changed generated output |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02.1-world-caller-model/02.1-03-SUMMARY.md` when done.
</output>
