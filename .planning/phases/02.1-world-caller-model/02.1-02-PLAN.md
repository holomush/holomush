---
phase: 02.1-world-caller-model
plan: 02
type: execute
wave: 2
depends_on: [02.1-01]
files_modified:
  - internal/world/service.go
  - internal/world/mutator.go
  - internal/world/grpc_server.go
  - internal/command/types.go
  - internal/plugin/hostfunc/adapter.go
  - internal/plugin/hostfunc/helpers.go
  - internal/plugin/hostfunc/world_write.go
  - internal/plugin/hostcap/world.go
  - internal/bootstrap/setting.go
  - internal/property/registry.go
  - internal/property/entity_mutator.go
  - internal/property/definition_name.go
  - internal/property/definition_description.go
  - internal/world/service_test.go
  - internal/world/movement_hook_test.go
  - internal/world/mutator_test.go
  - internal/world/service_helpers_test.go
  - internal/world/grpc_server_test.go
  - internal/bootstrap/setting_test.go
  - internal/access/policy/attribute/character_test.go
  - internal/plugin/building_integration_test.go
  - internal/plugin/hostcap/world_test.go
  - internal/plugin/hostcap/property_test.go
  - internal/plugin/hostfunc/adapter_test.go
  - internal/plugin/hostfunc/functions_test.go
  - internal/plugin/hostfunc/world_test.go
  - internal/plugin/hostfunc/world_write_test.go
  - internal/plugin/lua/corebuilding_brokered_command_test.go
  - internal/plugin/lua/dispatch_propagation_test.go
  - internal/plugin/goplugin/dispatch_propagation_test.go
  - internal/property/entity_mutator_test.go
  - test/integration/resilience/m2_dualwrite_test.go
  - test/integration/resilience/m12_lastwritewins_test.go
  - test/integration/resilience/restart_reconnect_test.go
  - test/integration/access/concurrent_engine_test.go
  - test/integration/access/seed_policies_test.go
  - test/integration/world/character_lifecycle_test.go
autonomous: true
requirements: [AUTHZ-01]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 23 public world.Service commands that perform an authorization check take world.Caller in their second parameter slot; none takes a bare subject string, and no overload, variadic option, or alias preserves the old shape (ROADMAP criterion 1)."
    - "The six redeclaring interfaces move in lockstep: world.Mutator, command.WorldService, grpc.WorldQuerier, hostfunc.WorldService, property.WorldMutator, and bootstrap.worldSeeder — plus property.EntityMutator, property.Definition.Set, and the hostfunc.withMutatorContext callback type."
    - "internal/property threads world.Caller through all three hops — EntityMutator.SetName/SetDescription, Definition.Set, and the mutator.UpdateLocation/UpdateObject call sites — with no conversion back to a bare subject string at the world.Mutator boundary (D-66)."
    - "The four internal/bootstrap/setting.go call sites map to HumanCaller with the literal system:bootstrap subject and remain normally policy-evaluated; none becomes SystemCaller."
    - "Every migrated production call site produces a byte-identical ABAC subject string to the pre-flip tree — byte equality of the Go string, not case-folded or normalized comparison — so the outbox envelope Actor written by buildIntent and buildMoveIntent is unchanged."
    - "The 14 compile-time interface assertions still compile and none is weakened, deleted, or converted to a comment."
    - "Every existing test in the migrated packages passes with its assertions unmodified: only call shapes and parameter types changed (ROADMAP criterion 3)."
    - "task test:int passes — the 43 build-tagged integration call sites compile and run, which task test alone cannot prove."
    - "Re-running the four committed scripts/codemod rules against the post-flip tree leaves git diff --exit-code clean (D-63's review contract)."
    - "The 21 grouped subjectID parameter declarations are hand-migrated where in scope and demonstrably not truncated: no migrated declaration loses a parameter name."
    - statement: "No world.Service command's behavior depends on the iteration order of the caller's attribute map; the engine merges it key-by-key into bags.Action and callers built by HumanCaller/SystemCaller carry an empty map, so NewAccessRequest normalizes it to a nil Attributes field."
      verification: backstop
  artifacts:
    - internal/world/service.go
    - internal/world/mutator.go
    - internal/command/types.go
    - internal/property/entity_mutator.go
    - internal/property/registry.go
    - internal/plugin/hostfunc/helpers.go
  key_links:
    - "world.Service signatures -> the six redeclaring interfaces -> the 14 compile-time assertions (the flip's loud tripwires)."
    - "internal/property EntityMutator -> Definition.Set -> world.Mutator.UpdateLocation/UpdateObject (D-66's three-hop chain, where subjectID sits at position 4, 4, and 2 respectively)."
    - "hostfunc.withMutatorContext -> its callback type -> the four world_write.go call sites (the caller is built once in helpers.go and threaded, never re-wrapped)."
    - "scripts/codemod rules -> ~349 test call sites and ~162 parameter declarations (the mechanically verifiable bulk of the diff)."
  prohibitions:
    - "MUST NOT map any system:bootstrap call site to SystemCaller — the S1 gate is exact string equality, so that would silently convert four policy-evaluated bootstrap writes into total ABAC bypasses."
    - "MUST NOT add an overload, variadic option, type alias, or deprecated shim that preserves the bare-subject-string shape on any public world.Service command."
    - "MUST NOT convert the caller value back to a bare subject string anywhere inside internal/property (D-66)."
    - "MUST NOT migrate hostfunc.PropertyAccess or hostfunc.WorldQueryAccess (internal/plugin/hostfunc/cap_property.go, cap_world_query.go) — they are all-string Lua-facing facades with no production implementer and are out of scope for 02.1."
    - "MUST NOT rewrite internal/world/mutator.go's repository UpdateLocation call, internal/world/postgres/** repository calls, internal/plugin/hostfunc/world.go's adapter call, or internal/plugin/hostcap/world.go's subject-free wrapper call — all four are verified codemod false positives."
    - "MUST NOT change, reword, add, or delete any assertion in an existing test (ROADMAP criterion 3)."
    - "MUST NOT weaken or delete any compile-time interface assertion to make the tree compile."
    - "MUST NOT add a JobCaller constructor or any job-provenance vocabulary (D-62, Phase 02.2)."
    - "MUST NOT register the action schema namespace anywhere (02.2 D-59)."
    - "MUST NOT retire, delete, or deprecate access.WithSystemSubject or access.IsSystemContext."
    - "MUST NOT hand-edit .planning/ROADMAP.md or .planning/STATE.md."
---

<objective>
The atomic flip. With the architecture proven on one path in Plan 01, migrate everything that
remains: the other 21 `world.Service` command signatures, all six redeclaring interfaces and the
three supporting signatures the ROADMAP never named, the 31 production call sites, the three-hop
`internal/property` chain (D-66), and the ~349 test call sites plus ~162 parameter declarations via
the committed codemod.

Purpose: ROADMAP criterion 1 forbids any overload or variadic escape hatch, so there is no
incremental path across the public surface — the remaining commands move together. Criterion 3
requires the existing suites to pass with their assertions untouched, which is what makes the bulk
diff trustworthy: the compiler and the unmodified suite are the proof, and D-63's re-runnable rules
are how a reviewer checks the bulk mechanically instead of reading ~500 hunks.

Output: a fully migrated tree where no public `world.Service` command carries a bare subject string,
green on `task lint`, `task test`, and `task test:int`.

**D-64 commit mapping.** All three tasks below land as ONE commit — D-64 landmark (2), the atomic
flip. Do the work task by task, verifying as you go, but do not commit until task 3's full gate
(`task fmt`, `task lint`, `task test`, `task test:int`) is green. If an intermediate state must be
saved, stage without committing.

**Corrected blast radius.** The ROADMAP §02.1 "Verified blast radius" block is stale and was
superseded on 2026-08-08; a supersession record was written to STATE.md. Plan against these numbers,
each reproduced by a named command in `02.1-RESEARCH.md`: **23** commands (not 21); **six**
redeclaring interfaces plus one callback type plus three supporting signatures (not "3+"); **31**
arg-2 production call sites in 12 files (not 47); **~332** true test call sites (not 347);
**~162** parameter declarations across 20 files; and **no mockery-generated mock is affected — there
is no mock regeneration step in this phase.**
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-world-caller-model/02.1-CONTEXT.md
@.planning/phases/02.1-world-caller-model/02.1-RESEARCH.md
@.planning/phases/02.1-world-caller-model/02.1-PATTERNS.md
@.planning/phases/02.1-world-caller-model/02.1-01-SUMMARY.md
@.claude/rules/testing.md
@.claude/rules/logging.md
@.claude/rules/plugin-runtime-symmetry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the remaining 21 command signatures and the whole declaration surface, including the internal/property chain (D-66)</name>

  <files>
internal/world/service.go,
internal/world/mutator.go,
internal/command/types.go,
internal/plugin/hostfunc/adapter.go,
internal/plugin/hostfunc/helpers.go,
internal/bootstrap/setting.go,
internal/property/registry.go,
internal/property/entity_mutator.go,
internal/property/definition_name.go,
internal/property/definition_description.go
  </files>

  <read_first>
- `internal/world/service.go` — the 23 command declarations at lines 257, 278, 311, 365, 400, 424,
  460, 498, 546, 562, 583, 619, 659, 699, 745, 780, 799, 933, 953, 985, 1095, 1114, 1144. Two are
  grouped forms: `FindLocationByName` at :1095 and `ListPropertiesByParent` at :1144. Also read
  `UpdateCharacterPreferences` at :845-870 — it takes NO subject and deliberately runs no
  `checkAccess` (D-05); it must stay exactly as it is.
- `internal/world/service.go` lines 340-370 and 1060-1090 — `buildIntent` (`Actor: actor`) and
  `buildMoveIntent` (`Actor: subjectID` at :1079). Both take a plain `string`; after the flip they
  are fed from the caller's unexported subject field by direct field access inside `package world`.
  There are 12 `buildIntent` call sites; the one at :921 already passes
  `access.CharacterSubject(characterID.String())` and is not a caller-derived subject — leave it.
- `internal/world/mutator.go` lines 14-80 — the `Mutator` interface (10 subject-carrying methods, of
  which `GetLocation` and `GetExitsByLocation` already moved in Plan 01), its prose doc comment at
  :20-21, the grouped `FindLocationByName` at :57, and the compile-time assertion at :60-61.
- `internal/command/types.go` lines 36-115 — `WorldService`, 15 subject-carrying methods at
  :40,43,46,49,52,55,58,61,64,67,70,73,76,79,83 (`:73` is the grouped form), and the compile-time
  assertion at :110.
- `internal/plugin/hostfunc/adapter.go` lines 18-80 — the Deprecated `hostfunc.WorldService`
  interface (4 methods at :24-27), the `WorldMutator` type alias at :32 that migrates for free, and
  `SubjectID()` at :67-69.
- `internal/plugin/hostfunc/helpers.go` lines 100-125 — `withMutatorContext`, its callback type at
  :104-108, and the `subjectID := access.PluginSubject(pluginName)` construction at :121.
- `internal/bootstrap/setting.go` lines 28-36 — the `worldSeeder` interface (3 methods,
  `FindLocationByName` at :32 is the grouped form). This interface is named in NO prior artifact;
  it is a sixth redeclaring interface found during planning.
- `internal/property/registry.go` lines 45-60 — `WorldMutator` (2 methods) and `Definition.Set`
  (subject at parameter position 4). Note this file already imports `internal/world`.
- `internal/property/entity_mutator.go` lines 1-35 and 130-215 — the `EntityMutator` interface
  (`SetName` / `SetDescription`, subject at parameter position 4) and the four
  `mutator.UpdateLocation` / `mutator.UpdateObject` call sites at :142, :162, :188, :208 where the
  subject is at position 2. This file does NOT currently import `internal/world`; threading the
  caller type adds that import edge.
- `internal/property/definition_name.go` lines 30-45 and
  `internal/property/definition_description.go` lines 30-45 — the two `Definition.Set` implementers,
  both grouped declarations.
- `.planning/phases/02.1-world-caller-model/02.1-RESEARCH.md` § "The interface + assertion cascade"
  — the full lockstep table and the 14 compile-time assertion sites.
  </read_first>

  <action>
Flip the remaining 21 `world.Service` command declarations so the second parameter is `Caller`
instead of a bare subject string, leaving every other parameter, the return types, and the whole
body unchanged apart from the `s.checkAccess` argument. Delete the inline `HumanCaller(...)`
wrappers Plan 01 placed at those 21 `s.checkAccess` call sites — each now passes the command's own
caller parameter through directly. `UpdateCharacterPreferences` is untouched: it takes no subject,
runs no `checkAccess`, and is the deliberate D-05 exception this phase's invariant carves out.

Two of the 21 use the grouped declaration form — `FindLocationByName(ctx, subjectID, name string)`
and `ListPropertiesByParent(ctx, subjectID, parentType string, parentID ulid.ULID)`. Split each by
hand into a `Caller`-typed parameter followed by the remaining string parameter(s); the committed
declaration codemod deliberately excludes grouped forms because a naive rewrite drops the second
name.

At the `buildIntent` and `buildMoveIntent` call sites, source the actor string from the caller's
unexported subject field by direct field access, not through an accessor method. Both helpers take a
plain `string` parameter, so a method-value mistake is a compile error rather than the silent
`any`-boundary bug the repo has been bitten by before — keeping direct field access preserves that
property. The resulting envelope `Actor` must be byte-identical to the pre-flip string at every one
of the 12 sites.

Migrate the declaration surface in lockstep. Use the committed
`scripts/codemod/world-caller-decl.yml` and `world-caller-decl-internal.yml` rules for the
ungrouped declarations, and hand-migrate every grouped one:
- `world.Mutator` (`internal/world/mutator.go`) — the 8 remaining subject-carrying methods plus the
  grouped `FindLocationByName` at :57. Amend the interface doc comment at :20-21, which currently
  describes the bare subject parameter in prose and the `plugin:<name>` convention; restate it in
  terms of the caller value and name `world.HumanCaller` / `world.SystemCaller` as the construction
  path.
- `command.WorldService` (`internal/command/types.go`) — the 14 remaining methods plus the grouped
  `FindLocationByName` at :73.
- `hostfunc.WorldService` (`internal/plugin/hostfunc/adapter.go`) — the 3 remaining methods. It is
  already marked Deprecated but has a live implementer assertion and live call sites, so it must be
  migrated, not skipped. Note deleting it as a candidate follow-up in the plan summary rather than
  doing it here.
- `bootstrap.worldSeeder` (`internal/bootstrap/setting.go:30-34`) — 2 ungrouped methods plus the
  grouped `FindLocationByName` at :32.
- `grpc.WorldQuerier` migrated completely in Plan 01; verify it needs no further change.
- `hostfunc.WorldMutator` and `hostcap.WorldMutator` are type aliases to `world.Mutator` and migrate
  for free — do not edit them.

D-66: thread `world.Caller` through the whole `internal/property` chain rather than converting back
to a bare subject string at the `world.Mutator` boundary. Migrate all three hops —
`EntityMutator.SetName` and `SetDescription` (`internal/property/entity_mutator.go:28,30`, subject at
parameter position 4), `Definition.Set` (`internal/property/registry.go:58`, position 4) and its two
implementers in `definition_name.go:34` and `definition_description.go:34` (both grouped
declarations, hand-migrate), and `property.WorldMutator` (`registry.go:50-51`, position 2) — plus
the four `mutator.UpdateLocation` / `mutator.UpdateObject` call sites in `entity_mutator.go` at
:142, :162, :188, :208, which pass the threaded caller value directly and are NEVER wrapped in
`HumanCaller`. Because the subject position differs per hop (4, 4, then 2), the positional codemod
rules do not apply to the grouped hops; migrate those by hand. `internal/property/registry.go`
already imports `internal/world`; `internal/property/entity_mutator.go` does not, so add that import
and confirm `go build ./...` reports no import cycle — the dependency runs in the direction the
package already depends.

Migrate `hostfunc.withMutatorContext`'s callback type (`internal/plugin/hostfunc/helpers.go:104-108`)
so its third parameter is `world.Caller`, and change the construction at :121 to build the caller
once from the existing `access.PluginSubject(pluginName)` expression. The subject string itself is
unchanged; only its wrapper is new. Its four consumers in `internal/plugin/hostfunc/world_write.go`
are Task 2's.

Do not migrate `hostfunc.PropertyAccess` (`cap_property.go:26-35`) or `hostfunc.WorldQueryAccess`
(`cap_world_query.go:36-42`). They are all-string Lua-facing facades whose subject arrives from a
Lua script via `L.CheckString(1)`, they have no production implementer, and they do not reach
`world.Service`. Record in the summary that they were deliberately left alone and that the check for
a missed wiring path is a grep for their constructor names outside test files.

Keep every compile-time interface assertion intact — there are 14, and they are the flip's loud
tripwires. If one fails to compile, fix the implementation, never the assertion.

Run `task fmt` after this task: the interface edits touch aligned Go declaration blocks, which can
pass `task build` and still fail `task fmt:check` in CI.
  </action>

  <verify>
    <automated>task test -- ./internal/world/ ./internal/command/ ./internal/property/ ./internal/bootstrap/ ./internal/plugin/...</automated>
    <automated>task lint</automated>
  </verify>

  <acceptance_criteria>
- `rg -c 'func \(s \*Service\) [A-Z][A-Za-z]*\(ctx context\.Context, subjectID( string|,)' internal/world/service.go` returns 0.
- `rg -c 'Caller' internal/world/service.go` returns at least 23 (the 23 command signatures plus
  the `checkAccess` parameter).
- `rg -n 'UpdateCharacterPreferences' internal/world/service.go` shows the declaration unchanged —
  no `Caller` parameter was added and no `checkAccess` call was introduced.
- `rg -n 'subjectID' internal/world/mutator.go internal/command/types.go internal/grpc/server.go internal/plugin/hostfunc/adapter.go internal/bootstrap/setting.go internal/property/registry.go internal/property/entity_mutator.go internal/property/definition_name.go internal/property/definition_description.go internal/plugin/hostfunc/helpers.go` returns 0 lines.
- `rg -n 'world\.Caller|Caller\)' internal/property/entity_mutator.go` shows the caller value
  reaching the `mutator.UpdateLocation` / `mutator.UpdateObject` call sites unwrapped — there is no
  `HumanCaller(` anywhere under `internal/property/`.
- `rg -c 'HumanCaller\(' internal/property/` returns 0.
- `go vet`-equivalent compilation succeeds: `task build` exits 0.
- Every grouped declaration that was migrated retains all of its original parameters: for each of
  `FindLocationByName` and `ListPropertiesByParent` and the two `Definition.Set` implementers,
  the declaration's parameter count is unchanged from the pre-flip tree.
- `rg -n 'var _ (Mutator|WorldService|WorldQuerier|WorldMutator) = ' --glob '*.go' internal/ | wc -l`
  matches the pre-flip count of 14; no assertion was deleted or commented out.
- `rg -n 'PropertyAccess|WorldQueryAccess' internal/plugin/hostfunc/cap_property.go internal/plugin/hostfunc/cap_world_query.go` shows those interfaces unchanged.
- `task test -- ./internal/world/ ./internal/command/ ./internal/property/ ./internal/bootstrap/ ./internal/plugin/...` exits 0.
- `task lint` exits 0 and `task fmt` leaves `git status --porcelain` clean afterwards.
  </acceptance_criteria>

  <reversibility rating="costly">Reverting the 23-signature flip and the six-interface lockstep means unwinding a cross-package refactor; D-56 records the maintainer's reasoning for choosing this shape over the variadic that would have been reversible.</reversibility>

  <done>
No public `world.Service` command carries a bare subject string; the six redeclaring interfaces, the
callback type, and the three supporting signatures all take `world.Caller`; `internal/property`
threads the caller value through all three hops with no bare-string hop remaining; every grouped
declaration kept all its parameters; all 14 compile-time assertions still compile; the touched
packages' unit suites pass with assertions unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate the 31 production call sites against the verified per-site subject map</name>

  <files>
internal/bootstrap/setting.go,
internal/plugin/hostcap/world.go,
internal/plugin/hostfunc/adapter.go,
internal/plugin/hostfunc/world_write.go,
internal/world/grpc_server.go
  </files>

  <read_first>
- `.planning/phases/02.1-world-caller-model/02.1-RESEARCH.md` § "Q2. Subject string formats at the
  production call sites" — the 27-row table naming every site, its current arg-2 expression, what it
  evaluates to, and what it maps to. This table is the review checklist for this task; work row by
  row rather than by file.
- `internal/bootstrap/setting.go` lines 125-145 and 265-340 — the four sites at :134, :272, :276,
  :331, each passing the literal `"system:bootstrap"`.
- `internal/access/policy/engine.go` lines 88-105 — the S1 gate. The subject comparison at :92 is
  exact string equality, not a prefix match, which is precisely why `"system:bootstrap"` is a
  normally policy-evaluated subject and must not become a system caller.
- `internal/plugin/hostcap/world.go` lines 140-150 and 215-395 — the four true sites at :227, :288,
  :330, :385 (each with its `subject := access.PluginSubject(s.pluginName)` local one line above),
  and the verified false positive at :143 whose second argument is an id, not a subject.
- `internal/plugin/hostfunc/adapter.go` lines 60-150 — `SubjectID()` at :67-69 and the four call
  sites at :75, :99, :123, :146. `:75` already migrated in Plan 01.
- `internal/plugin/hostfunc/world_write.go` lines 40-60, 100-120, 180-215, and 310-350 — the four
  callback bodies at :48, :108, :185, :209 plus `setEntityProperty` at :315-320 which forwards the
  subject into `opts.definition.Set`. After Task 1 the callback's third parameter is already a
  caller, so these sites pass it through directly.
- `internal/world/grpc_server.go` lines 35-110 — the four sites at :44, :62, :80, :101, each with
  its `subjectID := access.CharacterSubject(...)` local one line above. This file is `package world`,
  so the constructor is spelled unqualified.
- `internal/grpc/auth_handlers.go` line 972 and `internal/grpc/location_follow.go` — both fully
  migrated in Plan 01; confirm no residue.
  </read_first>

  <action>
Migrate the 31 arg-2 production call sites, working row by row against the 27-row table in
RESEARCH § Q2 rather than file by file, because the mapping is not uniform and one row is a
security-relevant judgment call.

`internal/bootstrap/setting.go` at :134, :272, :276, :331 — wrap the literal `"system:bootstrap"`
in `world.HumanCaller`. These stay normally policy-evaluated. Do not reach for the system
constructor here: the S1 bypass branch is entered only on exact equality with the bare `system`
subject, so mapping these four to a system caller would silently convert four policy-evaluated
bootstrap writes into total ABAC bypasses — the single highest-risk judgment call in this migration,
and one that no existing test necessarily catches. A diff hunk in this file containing the system
constructor is the warning sign.

`internal/plugin/hostcap/world.go` at :227, :288, :330, :385 — wrap the existing `subject` local in
`world.HumanCaller`. Leave :143 alone; its second argument is an id, and the interface it calls
(`capabilities.go:46`) declares a subject-free signature.

`internal/plugin/hostfunc/adapter.go` at :99, :123, :146 — wrap `a.SubjectID()` in
`world.HumanCaller`. Do not change `SubjectID()` itself; it stays a string method and other code
depends on it.

`internal/plugin/hostfunc/world_write.go` at :48, :108, :185, :209 — after Task 1 the callback's
third parameter is already a `world.Caller`, so pass it straight through with no wrapping. `:209`
constructs its own subject inline (`access.PluginSubject(pluginName)`) outside a callback; wrap that
one. `setEntityProperty` at :315 forwards the value into `opts.definition.Set`, whose signature Task
1 already migrated, so its parameter type changes and its body does not.

`internal/world/grpc_server.go` at :44, :62, :80, :101 — wrap the existing `subjectID` local. This
file is `package world`, so use the unqualified constructor spelling; the `world.`-qualified form
inside `package world` produces a doubled package qualifier.

At every site the constructor receives the *already-built* subject expression verbatim. Never
re-derive, re-prefix, or normalize it: the same string is written into the world-change outbox
envelope's actor field via `buildIntent` and `buildMoveIntent`, `wmodel` performs no validation on
it, and any drift silently alters persisted audit-bearing payloads.

Leave `internal/plugin/hostfunc/cap_property.go` at :74, :140 and
`internal/plugin/hostfunc/cap_world_query.go` at :84, :119 untouched — the Lua-facing all-string
facades. Before finishing, run a grep for `NewPropertyCapability` and `NewWorldQueryCapability`
outside test files to confirm the "no production implementer" assumption still holds, and record the
result in the summary. If either turns out to be wired, stop and flag it rather than migrating it.

After this task, `rg` for the world command names taking a bare string second argument across
production files should return only the deliberately-excluded facades.
  </action>

  <verify>
    <automated>task build</automated>
    <automated>task test -- ./internal/bootstrap/ ./internal/plugin/... ./internal/world/ ./internal/grpc/</automated>
    <automated>task lint</automated>
  </verify>

  <acceptance_criteria>
- `rg -n 'SystemCaller' internal/bootstrap/` returns 0 lines.
- `rg -c 'HumanCaller\("system:bootstrap"\)' internal/bootstrap/setting.go` returns 4.
- `rg -n 'SystemCaller\(\)' --glob '*.go' --glob '!*_test.go' internal/ cmd/` returns exactly the two
  lines in `internal/grpc/location_follow.go`.
- `rg -c 'HumanCaller\(' internal/world/grpc_server.go` returns 4, and none of those occurrences is
  spelled with a `world.` qualifier.
- `rg -c 'HumanCaller\(' internal/plugin/hostcap/world.go` returns 4; `internal/plugin/hostcap/world.go:143`
  is unchanged from the pre-flip tree.
- `rg -c 'HumanCaller\(' internal/plugin/hostfunc/world_write.go` returns 1 (only the site that
  builds its own subject inline); the four callback sites pass the threaded caller unwrapped.
- `git diff` over the migrated production files shows, for every hunk, that the expression handed to
  the constructor is character-for-character the pre-flip second argument.
- `rg -n 'NewPropertyCapability|NewWorldQueryCapability' --glob '*.go' --glob '!*_test.go'` returns 0
  lines, confirming the out-of-scope facades have no production implementer; the result is recorded
  in the plan summary.
- `task build` exits 0; `task test -- ./internal/bootstrap/ ./internal/plugin/... ./internal/world/ ./internal/grpc/` exits 0; `task lint` exits 0.
  </acceptance_criteria>

  <reversibility rating="reversible">Individual call-site mappings are one-line changes; the 27-row table makes each independently re-checkable.</reversibility>

  <done>
All 31 production arg-2 call sites carry a caller value; the four bootstrap sites use the human
constructor with the unchanged `system:bootstrap` literal; the only system-constructor sites in
production are the two in `location_follow.go`; every constructor argument is byte-identical to the
pre-flip expression; the two out-of-scope Lua facades are untouched and their no-implementer
assumption is re-verified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the committed codemod over the test tier, then gate the flip on the full and integration suites</name>

  <files>
internal/world/service_test.go,
internal/world/movement_hook_test.go,
internal/world/mutator_test.go,
internal/world/service_helpers_test.go,
internal/world/grpc_server_test.go,
internal/bootstrap/setting_test.go,
internal/access/policy/attribute/character_test.go,
internal/plugin/building_integration_test.go,
internal/plugin/hostcap/world_test.go,
internal/plugin/hostcap/property_test.go,
internal/plugin/hostfunc/adapter_test.go,
internal/plugin/hostfunc/functions_test.go,
internal/plugin/hostfunc/world_test.go,
internal/plugin/hostfunc/world_write_test.go,
internal/plugin/lua/corebuilding_brokered_command_test.go,
internal/plugin/lua/dispatch_propagation_test.go,
internal/plugin/goplugin/dispatch_propagation_test.go,
internal/property/entity_mutator_test.go,
test/integration/resilience/m2_dualwrite_test.go,
test/integration/resilience/m12_lastwritewins_test.go,
test/integration/resilience/restart_reconnect_test.go,
test/integration/access/concurrent_engine_test.go,
test/integration/access/seed_policies_test.go,
test/integration/world/character_lifecycle_test.go
  </files>

  <read_first>
- `scripts/codemod/README.md` — the run order, the preview and apply commands, the enumerated
  hand-migrated surfaces, and the grouped-declaration probe.
- `.planning/phases/02.1-world-caller-model/02.1-PATTERNS.md` § "Test-double declarations" — the
  three shapes the declaration rule must handle, and specifically
  `internal/property/entity_mutator_test.go`, which holds both a subject-carrying
  `fakeVersionMutator` and a subject-free `fakeVersionQuerier`: a correct rule touches the first and
  leaves the second alone. Treat that file as the codemod's regression fixture.
- `internal/world/service_test.go` header (it is `package world_test`) and
  `internal/world/mutator_test.go` / `internal/world/service_helpers_test.go` headers (both
  `package world`) — this is the qualification split the two rule variants exist for.
- `.planning/phases/02.1-world-caller-model/02.1-RESEARCH.md` § "Integration-test exposure" — every
  `test/integration/**` file touched here carries `//go:build integration` and is invisible to
  `task test`. 43 of the call sites live behind that tag.
- `internal/world/service_test.go` lines 6070-6260 — the nine empty-subject subtests, whose call
  expressions the codemod rewrites and whose assertions must not move.
  </read_first>

  <action>
Run the four committed rules from `scripts/codemod/` in the order documented in
`scripts/codemod/README.md`, previewing each with `ast-grep scan -r <rule> .` before applying with
`ast-grep scan -r <rule> -U .`. Apply the externally-qualified variants first, then the
`package world` variants, so no file is rewritten twice.

Before applying, run the grouped-declaration probe documented in the codemod README to re-enumerate
the 21 grouped `subjectID, ...` parameter declarations repo-wide. Of those, the in-scope ones not
already hand-migrated by Task 1 live in `internal/plugin/hostfunc/world_write_test.go` (4) and
`internal/plugin/hostcap/world_test.go` (1); hand-migrate those. The remainder —
`internal/access/policy/attribute/viewer.go`, `internal/plugin/hostfunc/cap_property.go` and its
test, `internal/plugin/hostfunc/cap_world_query.go` and its test — are out of scope and stay as
they are.

After applying, inspect the diff for the four known false-positive surfaces and confirm the
`ignores:` scoping held: no hunk in `internal/world/mutator.go`, none under
`internal/world/postgres/`, none in `internal/plugin/hostfunc/world.go`, none at
`internal/plugin/hostcap/world.go:143`. If any appeared, revert it and fix the rule's scoping rather
than the file, because the rule is the committed review artifact and must reproduce the diff.

Fix whatever the compiler surfaces that the rules could not reach: the 14 compile-time interface
assertions are designed to fail loudly here, and residual test doubles whose method names sit
outside the 23-command regex (for example a double implementing only part of an interface) need hand
edits. Under no circumstances change an assertion to make a test pass — the whole value of criterion
3 is that the assertion set is the unchanged control. If a test genuinely cannot pass without an
assertion change, stop and report it as a behavioral regression rather than editing it.

Run `task fmt` — the mass parameter-type rewrite touches aligned declaration blocks.

Then gate the flip. `task test` alone is not sufficient: 43 of the migrated call sites sit behind
`//go:build integration` and are never compiled by the untagged lane, so a broken integration-tier
signature would ship silently. `task test:int` is mandatory here and needs Docker; if Docker is
unavailable, stop and report rather than committing an unverified flip.

Finally, prove D-63's review contract: with the tree fully migrated and the working directory clean,
re-run all four rules with `-U` and confirm `git diff --exit-code` reports no change. That is the
idempotency claim the rule headers make and the mechanism by which a reviewer verifies ~500 hunks
without reading them.

Commit everything from Tasks 1, 2, and 3 as a single commit — D-64 landmark (2), the atomic flip.
  </action>

  <verify>
    <automated>task fmt && task lint</automated>
    <automated>task test</automated>
    <automated>task test:int</automated>
    <automated>ast-grep scan -r scripts/codemod/world-caller-arg2.yml -U . &amp;&amp; ast-grep scan -r scripts/codemod/world-caller-arg2-internal.yml -U . &amp;&amp; ast-grep scan -r scripts/codemod/world-caller-decl.yml -U . &amp;&amp; ast-grep scan -r scripts/codemod/world-caller-decl-internal.yml -U . &amp;&amp; git diff --exit-code</automated>
  </verify>

  <acceptance_criteria>
- `task test` exits 0.
- `task test:int` exits 0. This is non-negotiable: 43 migrated call sites are behind
  `//go:build integration` and `task test` does not compile them.
- `git diff internal/world/service_test.go` contains no added, removed, or reworded `assert.*`,
  `require.*`, or `errutil.Assert*` line — only call-expression and parameter-type changes.
- The same holds across every migrated test file: `git diff -- '*_test.go' | rg -c '^[+-]\s*(assert|require|errutil\.Assert)\.'` returns 0.
- `task test -- -run 'TestWorldService_MalformedAccessParams' ./internal/world/` exits 0 with its
  nine subtests intact.
- The codemod produced no hunk in `internal/world/mutator.go`, none under `internal/world/postgres/`,
  none in `internal/plugin/hostfunc/world.go`, and none at `internal/plugin/hostcap/world.go:143`.
- `internal/property/entity_mutator_test.go`'s `fakeVersionMutator` methods migrated and its
  `fakeVersionQuerier` methods are unchanged.
- Re-running all four rules with `-U` on the clean post-flip tree leaves `git diff --exit-code`
  clean (D-63's review contract).
- `rg -n 'subjectID' --glob '*_test.go' internal/ test/ | rg -v 'cap_property_test|cap_world_query_test'` returns 0 lines.
- `task fmt` leaves `git status --porcelain` clean; `task lint` exits 0.
- Tasks 1, 2, and 3 are committed as a single commit (D-64 landmark 2), and `git log --oneline -1`
  shows a conventional-commit message scoped to this phase.
  </acceptance_criteria>

  <precondition>Docker is running and available to testcontainers (`docker info` exits 0) — `task test:int` cannot compile or run the 43 build-tagged integration call sites without it.</precondition>

  <reversibility rating="costly">The flip is a single large commit by D-64; reverting it is one `git revert` but re-landing it means re-running the whole migration.</reversibility>

  <done>
The full tree is migrated. `task lint`, `task test`, and `task test:int` are all green. No test
assertion changed anywhere. Re-running the committed codemod rules against the migrated tree yields
an empty diff. Tasks 1-3 are committed together as the single atomic flip commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| call site → `world.Service` | The 31 production sites; each supplies caller identity that becomes both the ABAC subject and the persisted outbox actor. |
| `internal/bootstrap` → ABAC engine | A `system:bootstrap` subject that must remain policy-evaluated, one character away from the S1 total-bypass literal. |
| `internal/property` → `world.Mutator` | The three-hop chain where a bare subject string would otherwise survive the phase. |
| codemod → the tree | ~500 mechanical rewrites across an authorization surface, reviewed by rule reproducibility rather than by reading hunks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-01 | Elevation of Privilege | `internal/bootstrap/setting.go` (4 sites) | high | mitigate | Task 2 works row by row against RESEARCH § Q2's 27-row table; the acceptance criteria assert the human constructor at all four sites and zero occurrences of the system constructor in `internal/bootstrap/`. `engine.go:92` is exact string equality, so the distinction is load-bearing. |
| T-02.1-04 | Elevation of Privilege | the ~500-site mass rewrite | high | mitigate | The rules wrap the already-built subject expression verbatim and never re-derive it; every surface where a blind wrap would be wrong is excluded by `ignores:` and hand-migrated; the unchanged assertion set of the existing suites is the behavioral control; D-63's re-run-to-empty-diff proves the diff is exactly what the committed rules produce. |
| T-02.1-06 | Repudiation | outbox envelope `Actor` at 12 `buildIntent` sites and 1 `buildMoveIntent` site | medium | mitigate | The actor is sourced by direct field access on the caller inside `package world` into a plain `string` parameter, so a method-value mistake is a compile error. Byte identity of every migrated constructor argument is an acceptance criterion. |
| T-02.1-07 | Tampering | grouped parameter declarations | medium | mitigate | A naive declaration rule was observed silently dropping the second name of `subjectID, name string`. The committed rule excludes comma-bearing declarations by construction; the 21 grouped sites are re-enumerated by a documented probe and hand-migrated; an acceptance criterion asserts no migrated declaration lost a parameter. |
| T-02.1-08 | Elevation of Privilege | integration-tier call sites invisible to `task test` | high | mitigate | 43 of the migrated sites are `//go:build integration`. `task test:int` is a mandatory gate on this plan with a stated Docker precondition; the plan halts rather than committing an unverified flip. |
| T-02.1-05 | Denial of Service | `internal/access/policy/compiler.go` `action` namespace | medium | mitigate | Out of scope by decision (02.2 D-59) and captured as a prohibition; this plan touches no schema registration. |
| T-02.1-SC | Tampering | package-manager installs | low | accept | No npm/pip/cargo package is installed by this plan. No Package Legitimacy Audit is applicable. |
</threat_model>

<verification>
- `task fmt`, `task lint`, `task test`, and `task test:int` all exit 0.
- `git diff --stat internal/access/` is empty — no ABAC policy text, seed, or engine behavior
  changed.
- Re-running the four committed codemod rules on the migrated tree yields `git diff --exit-code`
  clean.
- No assertion in any pre-existing test changed.
- The 14 compile-time interface assertions still compile, unmodified.
</verification>

<success_criteria>
- ROADMAP criterion 1: no public `world.Service` command takes a bare subject string; no overload,
  variadic, or alias preserves the old shape.
- ROADMAP criterion 3: the existing suites pass at both tiers with assertions untouched.
- D-66: `internal/property` threads the caller value through all three hops.
- D-63: the committed rules reproduce the bulk diff and re-run to an empty diff.
- D-64: the flip is one commit, and every commit on the branch is green.
</success_criteria>

<artifacts_this_phase_produces>
Plan 02 creates no new exported symbols. It consumes `world.Caller`, `world.HumanCaller`, and
`world.SystemCaller` (created in Plan 01) and changes existing signatures in place. The signature
changes that a symbol-surface diff will flag as "new" are:

| Artifact | Kind |
|----------|------|
| `world.Mutator` (10 methods now taking `world.Caller`) | changed interface |
| `command.WorldService` (15 methods now taking `world.Caller`) | changed interface |
| `grpc.WorldQuerier` (2 methods) | changed interface |
| `hostfunc.WorldService` (4 methods, Deprecated) | changed interface |
| `bootstrap.worldSeeder` (3 methods) | changed unexported interface (not named in any prior artifact) |
| `property.WorldMutator` (2 methods) | changed interface |
| `property.EntityMutator` (`SetName`, `SetDescription`) | changed interface |
| `property.Definition.Set` | changed interface method |
| `hostfunc.withMutatorContext` callback type | changed function type |
| the 23 `world.Service` command signatures | changed methods |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02.1-world-caller-model/02.1-02-SUMMARY.md` when done.
</output>