---
phase: 02.1-world-caller-model
plan: 02
type: execute
wave: 2
depends_on: [02.1-01]
files_modified:
  - internal/world/service.go
  - internal/world/mutator.go
  - internal/world/grpc_server.go
  - internal/command/types.go
  - internal/plugin/hostfunc/adapter.go
  - internal/plugin/hostfunc/helpers.go
  - internal/plugin/hostfunc/world_write.go
  - internal/plugin/hostcap/world.go
  - internal/plugin/hostcap/property.go
  - internal/bootstrap/setting.go
  - internal/property/registry.go
  - internal/property/entity_mutator.go
  - internal/property/definition_name.go
  - internal/property/definition_description.go
  - internal/world/service_test.go
  - internal/world/movement_hook_test.go
  - internal/world/mutator_test.go
  - internal/world/service_helpers_test.go
  - internal/world/grpc_server_test.go
  - internal/bootstrap/setting_test.go
  - internal/access/policy/attribute/character_test.go
  - internal/plugin/building_integration_test.go
  - internal/plugin/hostcap/world_test.go
  - internal/plugin/hostcap/property_test.go
  - internal/plugin/hostfunc/adapter_test.go
  - internal/plugin/hostfunc/functions_test.go
  - internal/plugin/hostfunc/world_test.go
  - internal/plugin/hostfunc/world_write_test.go
  - internal/plugin/lua/corebuilding_brokered_command_test.go
  - internal/plugin/lua/dispatch_propagation_test.go
  - internal/plugin/goplugin/dispatch_propagation_test.go
  - internal/property/entity_mutator_test.go
  - internal/property/registry_test.go
  - internal/property/definition_error_test.go
  - internal/plugin/objects_integration_test.go
  - internal/world/postgres/cascade_delete_test.go
  - test/integration/resilience/m2_dualwrite_test.go
  - test/integration/resilience/m12_lastwritewins_test.go
  - test/integration/resilience/restart_reconnect_test.go
  - test/integration/access/concurrent_engine_test.go
  - test/integration/access/seed_policies_test.go
  - test/integration/world/character_lifecycle_test.go
autonomous: true
requirements: [AUTHZ-01]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 23 public world.Service commands that perform an authorization check take world.Caller in their second parameter slot; none takes a bare subject string, and no overload, variadic option, or alias preserves the old shape (ROADMAP criterion 1)."
    - "The six redeclaring interfaces move in lockstep: world.Mutator, command.WorldService, grpc.WorldQuerier, hostfunc.WorldService, property.WorldMutator, and bootstrap.worldSeeder — plus property.EntityMutator, property.Definition.Set, and the hostfunc.withMutatorContext callback type."
    - "internal/property threads world.Caller through all three hops — EntityMutator.SetName/SetDescription, Definition.Set, and the mutator.UpdateLocation/UpdateObject call sites — with no conversion back to a bare subject string at the world.Mutator boundary (D-66)."
    - "The ENTIRE position-4 surface migrates, test files included, and its completeness is proven by four commands that cover _test.go by construction. Definition.Set: 4 declarations (internal/property/definition_name.go:34, internal/property/definition_description.go:34, internal/property/registry_test.go:29, internal/plugin/hostcap/property_test.go:40) plus internal/property/registry.go:58, and 4 call sites (internal/plugin/hostcap/property.go:84, internal/plugin/hostfunc/world_write.go:319, internal/property/definition_error_test.go:22 and :34). EntityMutator.SetName/SetDescription: 10 declarations and 8 call sites, six of the latter at internal/property/entity_mutator_test.go :73/:80/:90/:100/:110/:119 and two more declarations at :137/:145. None of these is visible to the arg-2 census, to any committed codemod rule, or to probe-subject-param.yml. The earlier completeness command `rg -n '\\.Set\\(ctx' --glob '*.go' -g '!*_test.go' internal/` was blind twice over — it excluded _test.go AND anchored on the identifier ctx — and hid four sites in three files (registry_test.go, definition_error_test.go, hostcap/property_test.go) that appeared in no plan."
    - "The four internal/bootstrap/setting.go call sites map to HumanCaller with the literal system:bootstrap subject and remain normally policy-evaluated; none becomes SystemCaller."
    - "Every migrated production call site produces a byte-identical ABAC subject string to the pre-flip tree — byte equality of the Go string, not case-folded or normalized comparison — so the outbox envelope Actor written by buildIntent and buildMoveIntent is unchanged."
    - "The 13 compile-time interface assertions still compile and none is weakened, deleted, or converted to a comment. 13 is the count returned by `rg -n --glob '*.go' '^\\s*(var\\s+)?_ [A-Za-z_.]*(Mutator|WorldService|WorldQuerier) = ' internal/ | wc -l` on the pre-flip tree, measured 2026-08-08; it counts the grouped `var ( _ WorldService = ... )` form at internal/command/types.go:110 and the package-qualified spellings, both of which a `var _ `-anchored pattern misses."
    - "Every existing test in the migrated packages passes with its assertions unmodified: only call shapes and parameter types changed (ROADMAP criterion 3)."
    - "task test:int passes — the build-tagged integration call sites compile and run, which task test alone cannot prove. Re-measured 2026-08-08: this phase has **51** true arg-2 call sites behind //go:build integration — 48 across the six test/integration/** files (m2_dualwrite 17, concurrent_engine 13, m12_lastwritewins 11, seed_policies 4, restart_reconnect 2, character_lifecycle 1) plus 3 in internal/world/postgres/cascade_delete_test.go. Plan 01's scoped test:int gate covers 15 of them; the other 36 first compile here. A further 6 matches in internal/world/postgres/character_repo_test.go are repository false positives excluded by the codemod's receiver constraint. The figure '43' used by earlier drafts of this plan does not reproduce under any reading and is superseded."
    - "Re-running the four committed scripts/codemod rules against the post-flip tree leaves git diff --exit-code clean (D-63's review contract)."
    - "The 21 grouped subjectID parameter declarations are hand-migrated where in scope and demonstrably not truncated: no migrated declaration loses a parameter name."
    - statement: "No world.Service command's behavior depends on the iteration order of the caller's attribute map; the engine merges it key-by-key into bags.Action and callers built by HumanCaller/SystemCaller carry an empty map, so NewAccessRequest normalizes it to a nil Attributes field."
      verification: backstop
  artifacts:
    - internal/world/service.go
    - internal/world/mutator.go
    - internal/command/types.go
    - internal/property/entity_mutator.go
    - internal/property/registry.go
    - internal/plugin/hostfunc/helpers.go
  key_links:
    - "world.Service signatures -> the six redeclaring interfaces -> the 13 compile-time assertions (the flip's loud tripwires)."
    - "internal/property EntityMutator -> Definition.Set -> world.Mutator.UpdateLocation/UpdateObject (D-66's three-hop chain, where subjectID sits at position 4, 4, and 2 respectively)."
    - "hostfunc.withMutatorContext -> its callback type -> the four world_write.go call sites (the caller is built once in helpers.go and threaded, never re-wrapped)."
    - "scripts/codemod rules -> the 309 remaining call sites and 162 parameter declarations (the mechanically verifiable bulk of the diff). TWO BASES; label every quotation. Basis A (rule CONSTRAINTS only, `ignores:` NOT applied) = the migration surface: 355 true arg-2 sites (351 external + 4 `package world`), minus Plan 01's 46 tracer sites = **309** remaining for this plan. Basis B (what the COMMITTED rules print, `ignores:` applied) = 341 pre-flip (337 external + 4 internal); after Plan 01's tracer the same rules emit **297** (295 external + 2 internal, re-measured 2026-08-08 by dropping the two tracer method names from the METHOD regex). 297 + 12 = 309, where the 12 are the ignored-path residual after location_follow.go's 2 are already done: cap_property.go 2 + cap_world_query.go 2 (deliberately untouched) + hostfunc/world_write.go 4 + property/entity_mutator.go 4, all hand-migrated by Tasks 1-2. Declarations: 132 external + 30 internal = 162, no `ignores:` gap. Every earlier figure for the call-site quantity — '~349', '~332', CONTEXT D-63's '347-site', CONTEXT's '~400-site' — is a pre-research estimate and MUST NOT be re-propagated."
    - "The ENTIRE position-4 surface is migrated, production and test alike: Definition.Set has 4 declarations (definition_name.go:34, definition_description.go:34, registry_test.go:29, hostcap/property_test.go:40) plus its registry.go:58 interface line, and 4 call sites (hostcap/property.go:84, hostfunc/world_write.go:319, definition_error_test.go:22, definition_error_test.go:34); EntityMutator.SetName/SetDescription has 10 declarations and 8 call sites, six of the latter in entity_mutator_test.go. NONE of these is visible to the arg-2 census, to any committed codemod rule, or to probe-subject-param.yml, so the compiler at Task 3's gate is the only backstop — which is why they are enumerated in Task 1's read_first with four completeness commands that cover _test.go by construction. The '31 production arg-2 call sites' figure counts only arg-2-shaped production sites and does not include any of these."
  prohibitions:
    - "MUST NOT map any system:bootstrap call site to SystemCaller — the S1 gate is exact string equality, so that would silently convert four policy-evaluated bootstrap writes into total ABAC bypasses."
    - "MUST NOT add an overload, variadic option, type alias, or deprecated shim that preserves the bare-subject-string shape on any public world.Service command."
    - "MUST NOT convert the caller value back to a bare subject string anywhere inside internal/property's PRODUCTION code (D-66). This is a prohibition on unwrapping a threaded caller, not on a test constructing one: internal/property/definition_error_test.go :22/:34 and internal/property/entity_mutator_test.go :73/:80/:90/:100/:110/:119 legitimately build world.HumanCaller(\"subject\") at position 4, so the completeness grep for HumanCaller under internal/property/ MUST carry --glob '!*_test.go'."
    - "MUST NOT migrate hostfunc.PropertyAccess or hostfunc.WorldQueryAccess (internal/plugin/hostfunc/cap_property.go, cap_world_query.go) — they are all-string Lua-facing facades with no production implementer and are out of scope for 02.1."
    - "MUST NOT rewrite the verified codemod false positives, which are identified by RECEIVER SHAPE, not by file: internal/world/mutator.go:231 (m.characterWriter), internal/plugin/hostfunc/world.go:119 and internal/plugin/hostfunc/adapter_test.go :230/:241/:254/:266/:277/:414 (adapter), internal/plugin/hostcap/world.go:143 (querier), internal/world/postgres/character_repo_test.go :543/:571/:582/:825/:841/:849 (repo), and the five mockery EXPECT() sites (internal/world/service_test.go :4016/:4054/:4208/:5065, internal/world/movement_hook_test.go:83)."
    - "MUST NOT treat internal/world/postgres/** as a repository-call-only false-positive surface. Of its 9 arg-2 matches only 6 are false positives; internal/world/postgres/cascade_delete_test.go holds THREE REAL world.Service calls — svc.DeleteLocation(ctx, \"system\", locID) at :338, svc.DeleteObject(ctx, \"system\", objID) at :377, svc.DeleteCharacter(ctx, \"system\", charID) at :416, all against world.NewService(...) at :328/:367/:406. The file carries //go:build integration at :4, so a missed migration surfaces only at task test:int."
    - "MUST NOT map internal/world/postgres/cascade_delete_test.go's three bare \"system\" literals to SystemCaller(). They map to world.HumanCaller(\"system\") — see Task 2's mapping row and its rationale."
    - "MUST NOT change, reword, add, or delete any assertion in an existing test (ROADMAP criterion 3)."
    - "MUST NOT weaken or delete any compile-time interface assertion to make the tree compile."
    - "MUST NOT add a JobCaller constructor or any job-provenance vocabulary (D-62, Phase 02.2)."
    - "MUST NOT register the action schema namespace anywhere (02.2 D-59)."
    - "MUST NOT retire, delete, or deprecate access.WithSystemSubject or access.IsSystemContext."
    - "MUST NOT hand-edit .planning/ROADMAP.md or .planning/STATE.md."
---

<objective>
The atomic flip. With the architecture proven on one path in Plan 01, migrate everything that
remains: the other 21 `world.Service` command signatures, all six redeclaring interfaces and the
three supporting signatures the ROADMAP never named, the **25** arg-2 production call sites this
plan still owns (17 in Task 2 + 4 threaded by Task 1 in `internal/property` + 4 left untouched by
decision — the other 6 of the phase's 31 were Plan 01's tracer), the **whole position-4 surface**
that no arg-2 census can see (`Definition.Set` 4 declarations + 4 call sites,
`EntityMutator.SetName`/`SetDescription` 10 declarations + 8 call sites, spanning production AND
test), the three-hop `internal/property` chain (D-66), and the 309 remaining Basis-A call sites
plus 162 parameter declarations via the committed codemod.

Purpose: ROADMAP criterion 1 forbids any overload or variadic escape hatch, so there is no
incremental path across the public surface — the remaining commands move together. Criterion 3
requires the existing suites to pass with their assertions untouched, which is what makes the bulk
diff trustworthy: the compiler and the unmodified suite are the proof, and D-63's re-runnable rules
are how a reviewer checks the bulk mechanically instead of reading ~500 hunks.

Output: a fully migrated tree where no public `world.Service` command carries a bare subject string,
green on `task lint`, `task test`, and `task test:int`.

**D-64 commit mapping.** All three tasks below land as ONE commit — D-64 landmark (2), the atomic
flip. Do the work task by task, verifying as you go, but do not commit until task 3's full gate
(`task fmt`, `task lint`, `task test`, `task test:int`) is green. If an intermediate state must be
saved, stage without committing.

**Corrected blast radius — single reconciled figure set, re-measured 2026-08-08 on this tree with
`ast-grep 0.45.1` and the corrected rules.** The ROADMAP §02.1 "Verified blast radius" block is
stale and was superseded on 2026-08-08; a supersession record was written to STATE.md. Several
figures used by earlier drafts of these plans are ALSO superseded here. Plan against this table and
nothing else; every row is reproducible by the command in the third column.

| Quantity | Value | How measured |
|---|---|---|
| Public `world.Service` commands with an arg-2 subject | **23** (not 21) | `rg -n '^func \(s \*Service\) [A-Z]' internal/world/service.go` → 25 exported methods = 23 commands + `UpdateCharacterPreferences`:856 + `SetMovementHook`:160 |
| Redeclaring interfaces | **six** + one callback type + three supporting signatures (not "3+") | RESEARCH § "The interface + assertion cascade" |
| **Basis A** — total true arg-2 call sites, whole tree (rule CONSTRAINTS only, `ignores:` NOT applied) | **355** = 351 external + 4 `package world` | the two arg-2 rules with their `ignores:` block deleted, `rg -c '│\+'`. This is the migration SURFACE, not what the committed files print. |
| **Basis B** — what the COMMITTED arg-2 rules print pre-flip (`ignores:` applied) | **341** = 337 external + 4 `package world` | `ast-grep scan -r scripts/codemod/world-caller-arg2.yml . \| rg -c '│\+'` → **337**. A → B loses exactly 14: `location_follow.go` 2, `cap_property.go` 2, `cap_world_query.go` 2, `hostfunc/world_write.go` 4, `property/entity_mutator.go` 4. **Never assert 351 against the committed rule** — it is short by those 14. |
| …of which Plan 01 (tracer) migrated | **46** across 9 files (Basis A) | Plan 01's tracer rule |
| …remaining for this plan (Basis A) | **309** | 355 − 46. Supersedes "~349", "~332", CONTEXT D-63's "347-site" and CONTEXT's "~400-site" — all pre-research estimates. |
| …of which this plan's **codemod run** rewrites (Basis B, post-tracer) | **297** = 295 external + 2 internal | the committed rules re-run with `GetLocation`/`GetExitsByLocation` dropped from the METHOD regex, measured 2026-08-08. The remaining **12** of the 309 are hand-migrated or deliberately untouched: `cap_property.go` 2 + `cap_world_query.go` 2 (out of scope) + `hostfunc/world_write.go` 4 (Task 2) + `property/entity_mutator.go` 4 (Task 1, D-66). 297 + 12 = 309. |
| Parameter declarations rewritten by the two decl rules | **162** = 132 external + 30 internal | `rg -c '│\+'` per rule |
| Arg-2 **production** call sites | **31**, in **10** files (not 12, and not 47) | 34 raw production matches − 3 receiver-shape false positives. "12 files" is RESEARCH's raw-match file count, which includes the three files whose only match is a false positive. |
| **Position-4 surface, invisible to every arg-2 census** — `Definition.Set` | **4 declarations + 4 call sites** (2 of each are in `_test.go`) | `rg -n '\) Set\(.*WorldQuerier.*WorldMutator' --glob '*.go' internal/ test/ plugins/` → 4; callers via an `ast-grep` arity pattern `$X.Set($A, $B, $C, $D, $E, $F, $G)` → 4. **Enumerated in Task 1's `<read_first>`.** |
| **Position-4 surface** — `EntityMutator.SetName` / `SetDescription` | **10 declarations + 8 call sites** (4 declarations and 6 call sites are in `_test.go`) | `rg -n '(SetName\|SetDescription)\(.*WorldQuerier.*WorldMutator' --glob '*.go' internal/ test/ plugins/` → 10; callers via `ast-grep` `$X.SetName($A, $B, $C, $D, $E, $F)` / `$X.SetDescription(…)` → 8. **Enumerated in Task 1's `<read_first>`.** |
| Build-tagged (`//go:build integration`) true arg-2 sites | **51** (48 across the six `test/integration/**` files + 3 in `internal/world/postgres/cascade_delete_test.go`); 15 belong to Plan 01 | per-file `ast-grep` count. Supersedes "43". |
| Grouped `subjectID, …` declarations | **21** | `ast-grep` `parameter_declaration` regex `^subjectID, ` |
| Grouped `_, _ string` declarations on the 23 names | **7** — same truncation hazard, invisible to the `subjectID`-anchored probe | see Task 3 |
| Fully-unnamed declarations (invisible to probe AND codemod) | **8** repo-wide, of which **7** remain for this plan (Plan 01 takes `corebuilding…:52`) — `corebuilding_brokered_command_test.go` :52/:56/:60/:64/**:89**/**:93**/**:97** and `objects_integration_test.go:63` | `rg -n '^func \(m \*recordingWorldMutator\)' internal/plugin/lua/corebuilding_brokered_command_test.go` / `sed -n '60,75p' internal/plugin/objects_integration_test.go`. **:89/:93/:97 corrects an earlier replan's :84/:88/:92 for `UpdateLocation`/`UpdateObject`/`FindLocationByName`** — re-measured 2026-08-08; :84 is inside `CreateObject`'s body, not a declaration. |
| RESEARCH § Q2 rows | the main table is **27** rows (`:351-377`); Q2 continues to **row 31** in a second table (rows 28-31 = the `cap_*` façades) | read. Working "row by row against the 27-row table" would never surface rows 28-31. |
| mockery-generated mocks affected | **none** — there is no mock regeneration step in this phase | — |
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-world-caller-model/02.1-CONTEXT.md
@.planning/phases/02.1-world-caller-model/02.1-RESEARCH.md
@.planning/phases/02.1-world-caller-model/02.1-PATTERNS.md
@.planning/phases/02.1-world-caller-model/02.1-01-SUMMARY.md
@.claude/rules/testing.md
@.claude/rules/logging.md
@.claude/rules/plugin-runtime-symmetry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the remaining 21 command signatures and the whole declaration surface, including the internal/property chain (D-66)</name>

  <files>
internal/world/service.go,
internal/world/mutator.go,
internal/command/types.go,
internal/plugin/hostfunc/adapter.go,
internal/plugin/hostfunc/helpers.go,
internal/bootstrap/setting.go,
internal/property/registry.go,
internal/property/entity_mutator.go,
internal/property/definition_name.go,
internal/property/definition_description.go
  </files>

  <read_first>
- `internal/world/service.go` — the 23 command declarations at lines 257, 278, 311, 365, 400, 424,
  460, 498, 546, 562, 583, 619, 659, 699, 745, 780, 799, 933, 953, 985, 1095, 1114, 1144. Two are
  grouped forms: `FindLocationByName` at :1095 and `ListPropertiesByParent` at :1144. Also read
  `UpdateCharacterPreferences` at :845-870 — it takes NO subject and deliberately runs no
  `checkAccess` (D-05); it must stay exactly as it is.
- `internal/world/service.go` lines 340-370 and 1060-1090 — `buildIntent` (`Actor: actor`) and
  `buildMoveIntent` (`Actor: subjectID` at :1079). Both take a plain `string`; after the flip they
  are fed from the caller's unexported subject field by direct field access inside `package world`.
  **There are exactly 12 `buildIntent` call sites — :302, :332, :386, :448, :481, :513, :610, :643,
  :680, :728, :769, :825 — and ALL TWELVE pass a bare `subjectID`** (measured 2026-08-08:
  `rg -n 'buildIntent\(' internal/world/service.go`). An earlier draft of this plan said one of the
  12 was at `:921` and already passed `access.CharacterSubject(characterID.String())`; that is
  wrong and internally impossible. `:921` is inside **`buildPreferencesIntent`** (declared `:905`),
  a **third** envelope-actor helper the plans never named. It belongs to
  `UpdateCharacterPreferences` — the D-05 exemption — so it is not migration-affected, but it IS on
  the same audit-bearing outbox-`Actor` surface as T-02.1-06, and it must be named as deliberately
  out of scope rather than left silently absent. Do not "leave `:921` alone" as if it were one of
  the 12; leave `buildPreferencesIntent` alone as a whole.
- `internal/world/mutator.go` lines 14-76 — the `Mutator` interface, its prose doc comment at
  :20-21, the grouped `FindLocationByName` at :57, and the compile-time assertion at :60-61.
  **`Mutator` declares exactly ten methods and `GetExitsByLocation` is NOT one of them** (measured
  2026-08-08; `GetExitsByLocation` lives only in `command.WorldService` `types.go:43` and
  `grpc.WorldQuerier` `server.go:130`). So exactly ONE of `Mutator`'s ten — `GetLocation` at :26 —
  moved in Plan 01, and nine remain here: the 8 ungrouped plus the grouped `FindLocationByName` at
  :57. This plan's `<action>` arithmetic ("the 8 remaining … plus the grouped `FindLocationByName`")
  is the correct one.
- `internal/command/types.go` lines 36-115 — `WorldService`, 15 subject-carrying methods at
  :40,43,46,49,52,55,58,61,64,67,70,73,76,79,83 (`:73` is the grouped form), and the compile-time
  assertion at :110.
- `internal/plugin/hostfunc/adapter.go` lines 18-80 — the Deprecated `hostfunc.WorldService`
  interface (4 methods at :24-27), the `WorldMutator` type alias at :32 that migrates for free, and
  `SubjectID()` at :67-69.
- `internal/plugin/hostfunc/helpers.go` lines 100-125 — `withMutatorContext`: its doc comment at
  :100-104 (whose LAST line, :104, is the prose describing the subject parameter this task deletes),
  its declaration at :105-109 with the callback type on the single line :108, and the
  `subjectID := access.PluginSubject(pluginName)` construction at :121.
- `internal/bootstrap/setting.go` lines 28-36 — the `worldSeeder` interface (3 methods,
  `FindLocationByName` at :32 is the grouped form). This interface is named in NO prior artifact;
  it is a sixth redeclaring interface found during planning.
- `internal/property/registry.go` lines 45-60 — `WorldMutator` (2 methods) and `Definition.Set`
  (subject at parameter position 4). Note this file already imports `internal/world`.
- `internal/property/entity_mutator.go` lines 1-35 and 130-215 — the `EntityMutator` interface
  (`SetName` / `SetDescription`, subject at parameter position 4) and the four
  `mutator.UpdateLocation` / `mutator.UpdateObject` call sites at :142, :162, :188, :208 where the
  subject is at position 2. This file does NOT currently import `internal/world`; threading the
  caller type adds that import edge.
- `internal/property/definition_name.go` lines 30-45 and
  `internal/property/definition_description.go` lines 30-45 — the two `Definition.Set` implementers,
  both grouped declarations.
- **THE COMPLETE POSITION-4 CENSUS — read this table before touching `Definition.Set` or
  `EntityMutator`.** The subject sits at parameter position 4 on both, so NO arg-2 census, NO
  committed codemod rule, and NOT `probe-subject-param.yml` can see any of these. An earlier draft
  enumerated only the production half, because its completeness command
  (`rg -n '\.Set\(ctx' --glob '*.go' -g '!*_test.go' internal/`) excluded `_test.go` **and**
  anchored on the identifier `ctx`, so it was blind twice over — it missed four sites in three files
  that the compiler would only surface at Task 3's gate. Re-derive both halves yourself with the
  commands in the third column; work from your own output, not from this table.

  | Surface | Declarations | Call sites |
  |---|---|---|
  | `Definition.Set` (subject at position 4) | `internal/property/definition_name.go:34` (grouped), `internal/property/definition_description.go:34` (grouped), **`internal/property/registry_test.go:29`** (`testDefinition.Set`, all-blank names, `package property`), **`internal/plugin/hostcap/property_test.go:40`** (`fakePropertyDef.Set`, all-blank names, `package hostcap`-side, qualified `property.WorldQuerier`) — plus the interface declaration at `internal/property/registry.go:58` | `internal/plugin/hostcap/property.go:84` (production, Task 2's), `internal/plugin/hostfunc/world_write.go:319` (production, via `setEntityProperty`), **`internal/property/definition_error_test.go:22`** and **`:34`** (both `def.Set(context.Background(), nil, nil, "subject", "widget", ulid.ULID{}, "value")`) |
  | `EntityMutator.SetName` / `SetDescription` (subject at position 4) | `internal/property/entity_mutator.go:28` and `:30` (interface), `:136`/`:156` (`locationEntityMutator`), `:182`/`:202` (`objectEntityMutator`), **`internal/property/entity_mutator_test.go:137`** and **`:145`** (`testEntityMutator`, all-blank names), `internal/plugin/objects_integration_test.go:63` (fully unnamed) and `:71` | `internal/property/definition_name.go:39`, `internal/property/definition_description.go:39`, and **`internal/property/entity_mutator_test.go:73`, `:80`, `:90`, `:100`, `:110`, `:119`** — six calls, each passing the literal `"subject"` at position 4 |

  **Completeness commands (run all four; they cover test files by construction):**
  - `Definition.Set` declarations → `rg -n '\) Set\(.*WorldQuerier.*WorldMutator' --glob '*.go' internal/ test/ plugins/ | wc -l` returns **4** (measured 2026-08-08). Do NOT substitute a bare `rg '\) Set\('` — the repo has eleven other unrelated `Set` methods (`session.Store`, `MetadataStore`, `KVStore`, `natsHeaderCarrier`, mockery mocks).
  - `Definition.Set` call sites → an `ast-grep` rule with `pattern: $X.Set($A, $B, $C, $D, $E, $F, $G)` (seven single metavariables, no `$$$`) returns **4**. Arity is the discriminator; a text regex cannot express it because `context.Background()` and `s.host.WorldMutator()` contain their own parentheses.
  - `EntityMutator` declarations → `rg -n '(SetName|SetDescription)\(.*WorldQuerier.*WorldMutator' --glob '*.go' internal/ test/ plugins/ | wc -l` returns **10**. The `WorldQuerier.*WorldMutator` anchor is what separates these from `world.Character.SetName` / `SetDescription`, which take a single string and MUST NOT be touched.
  - `EntityMutator` call sites → an `ast-grep` rule with `any:` of `pattern: $X.SetName($A, $B, $C, $D, $E, $F)` and `pattern: $X.SetDescription($A, $B, $C, $D, $E, $F)` returns **8**.

  Migrate every row. The six `entity_mutator_test.go` call sites and the two
  `definition_error_test.go` call sites each pass a bare `"subject"` / `"subject"`-shaped literal at
  position 4 and become `world.HumanCaller("subject")` — a test constructing a caller, which is NOT
  the D-66 prohibition (that forbids converting a threaded caller *back* to a bare string in
  production code, and none of these does).
- `.planning/phases/02.1-world-caller-model/02.1-RESEARCH.md` § "The interface + assertion cascade"
  — the full lockstep table and the compile-time assertion sites. **RESEARCH's assertion
  enumeration is corrected to 13 sites (11 in tests); its earlier "14 sites; 10 in tests" header was
  wrong and its list omitted `internal/world/mutator.go:61`. Re-derive the set yourself with
  `rg -n --glob '*.go' '^\s*(var\s+)?_ [A-Za-z_.]*(Mutator|WorldService|WorldQuerier) = ' internal/`
  rather than trusting any written figure.**
  </read_first>

  <action>
Flip the remaining 21 `world.Service` command declarations so the second parameter is `Caller`
instead of a bare subject string, leaving every other parameter, the return types, and the whole
body unchanged apart from the `s.checkAccess` argument. Delete the inline `HumanCaller(...)`
wrappers Plan 01 placed at those 21 `s.checkAccess` call sites — each now passes the command's own
caller parameter through directly. `UpdateCharacterPreferences` is untouched: it takes no subject,
runs no `checkAccess`, and is the deliberate D-05 exception this phase's invariant carves out.

Two of the 21 use the grouped declaration form — `FindLocationByName(ctx, subjectID, name string)`
and `ListPropertiesByParent(ctx, subjectID, parentType string, parentID ulid.ULID)`. Split each by
hand into a `Caller`-typed parameter followed by the remaining string parameter(s); the committed
declaration codemod deliberately excludes grouped forms because a naive rewrite drops the second
name.

At the `buildIntent` and `buildMoveIntent` call sites, source the actor string from the caller's
unexported subject field by direct field access, not through an accessor method. Both helpers take a
plain `string` parameter, so a method-value mistake is a compile error rather than the silent
`any`-boundary bug the repo has been bitten by before — keeping direct field access preserves that
property. The resulting envelope `Actor` must be byte-identical to the pre-flip string at every one
of the 12 sites.

Migrate the declaration surface in lockstep. Use the committed
`scripts/codemod/world-caller-decl.yml` and `world-caller-decl-internal.yml` rules for the
ungrouped declarations, and hand-migrate every grouped one:
- `world.Mutator` (`internal/world/mutator.go`) — the 8 remaining subject-carrying methods plus the
  grouped `FindLocationByName` at :57. Amend the interface doc comment at :20-21, which currently
  describes the bare subject parameter in prose and the `plugin:<name>` convention; restate it in
  terms of the caller value and name `world.HumanCaller` / `world.SystemCaller` as the construction
  path.
- `command.WorldService` (`internal/command/types.go`) — the 14 remaining methods plus the grouped
  `FindLocationByName` at :73.
- `hostfunc.WorldService` (`internal/plugin/hostfunc/adapter.go`) — the 3 remaining methods. It is
  already marked Deprecated but has a live implementer assertion and live call sites, so it must be
  migrated, not skipped. Note deleting it as a candidate follow-up in the plan summary rather than
  doing it here.
- `bootstrap.worldSeeder` (`internal/bootstrap/setting.go:30-34`) — 2 ungrouped methods plus the
  grouped `FindLocationByName` at :32.
- `grpc.WorldQuerier` migrated completely in Plan 01; verify it needs no further change.
- `hostfunc.WorldMutator` and `hostcap.WorldMutator` are type aliases to `world.Mutator` and migrate
  for free — do not edit them.

D-66: thread `world.Caller` through the whole `internal/property` chain rather than converting back
to a bare subject string at the `world.Mutator` boundary. Migrate all three hops —
`EntityMutator.SetName` and `SetDescription` (`internal/property/entity_mutator.go:28,30`, subject at
parameter position 4), `Definition.Set` (`internal/property/registry.go:58`, position 4) and its two
implementers in `definition_name.go:34` and `definition_description.go:34` (both grouped
declarations, hand-migrate), and `property.WorldMutator` (`registry.go:50-51`, position 2) — plus
the four `mutator.UpdateLocation` / `mutator.UpdateObject` call sites in `entity_mutator.go` at
:142, :162, :188, :208, which pass the threaded caller value directly and are NEVER wrapped in
`HumanCaller`. Because the subject position differs per hop (4, 4, then 2), the positional codemod
rules do not apply to the grouped hops; migrate those by hand. `internal/property/registry.go`
already imports `internal/world`; `internal/property/entity_mutator.go` does not, so add that import
and confirm `go build ./...` reports no import cycle — the dependency runs in the direction the
package already depends.

Migrate `hostfunc.withMutatorContext`'s callback type (`internal/plugin/hostfunc/helpers.go:105-109`)
so its third parameter is `world.Caller`, and change the construction at :121 to build the caller
once from the existing `access.PluginSubject(pluginName)` expression. The subject string itself is
unchanged; only its wrapper is new. Its four consumers in `internal/plugin/hostfunc/world_write.go`
are Task 2's.

**Amend `withMutatorContext`'s prose doc comment at `internal/plugin/hostfunc/helpers.go:104`**, the
same discipline Plan 01 applied to `internal/world/service.go:216-219` and `world.Mutator`'s
interface comment. It currently reads `// The subjectID for ABAC is constructed as
"plugin:<pluginName>".` — a sentence describing a parameter this task deletes. Restate it in terms
of the caller value: the callback receives a `world.Caller` built once here from
`access.PluginSubject(pluginName)`, so the ABAC subject is still `plugin:<pluginName>` and the
callback never re-derives or re-wraps it. A stale comment describing a removed subject parameter is
what the `abac-reviewer` gate reads as a regression.

**Verified 2026-08-08 — this is the ONLY such prose comment left in this task's ten files.** Across
`mutator.go`, `command/types.go`, `grpc/server.go`, `hostfunc/adapter.go`, `bootstrap/setting.go`,
`property/registry.go`, `property/entity_mutator.go`, `property/definition_name.go`,
`property/definition_description.go`, and `hostfunc/helpers.go`, exactly two lines mention
`subjectID` outside a signature: `helpers.go:104` (above) and `internal/world/mutator.go:20`
(already amended earlier in this task). `property/registry.go` and `property/entity_mutator.go`
carry no prose mentioning the subject parameter — their interface doc comments are one-liners about
read/write access — so there is nothing to restate there. Confirm with
`rg -n 'subjectID' <the ten files> | rg '//'` before you start: it returns two lines, and zero after.

Do not migrate `hostfunc.PropertyAccess` (`cap_property.go:26-35`) or `hostfunc.WorldQueryAccess`
(`cap_world_query.go:36-42`). They are all-string Lua-facing facades whose subject arrives from a
Lua script via `L.CheckString(1)`, they have no production implementer, and they do not reach
`world.Service`. Record in the summary that they were deliberately left alone and that the check for
a missed wiring path is a grep for their constructor names outside test files.

Keep every compile-time interface assertion intact — there are 13, and they are the flip's loud
tripwires. If one fails to compile, fix the implementation, never the assertion.

Run `task fmt` after this task: the interface edits touch aligned Go declaration blocks, which can
pass `task build` and still fail `task fmt:check` in CI.
  </action>

  <verify>
    <automated>task test -- ./internal/world/ ./internal/command/ ./internal/property/ ./internal/bootstrap/ ./internal/plugin/...</automated>
    <automated>task lint</automated>
  </verify>

  <acceptance_criteria>
- `rg -n 'func \(s \*Service\) [A-Z][A-Za-z]*\(ctx context\.Context, subjectID( string|,)' internal/world/service.go | wc -l` returns 0.
  **`rg -c ... returns 0` is never a valid criterion in this phase** — on no match `rg -c` prints
  nothing and exits 1, so an executor judging by exit code reads a passing tree as a failure. This
  plan already explains the hazard for its assertion-set criterion below; apply it everywhere. Use
  `| wc -l` returns 0, or `! rg -q ...`.
- `rg -c 'Caller' internal/world/service.go` returns at least 23 (the 23 command signatures plus
  the `checkAccess` parameter). (`rg -c` is fine here: the expected result is non-zero.)
- `rg -n 'UpdateCharacterPreferences' internal/world/service.go` shows the declaration unchanged —
  no `Caller` parameter was added and no `checkAccess` call was introduced.
- `rg -n 'subjectID' internal/world/mutator.go internal/command/types.go internal/grpc/server.go internal/plugin/hostfunc/adapter.go internal/bootstrap/setting.go internal/property/registry.go internal/property/entity_mutator.go internal/property/definition_name.go internal/property/definition_description.go internal/plugin/hostfunc/helpers.go | wc -l` returns 0.
  Reaching zero requires the **prose** hits, not only the signatures: on the pre-flip tree that
  command returns 56 lines, two of which are doc comments —
  `internal/plugin/hostfunc/helpers.go:104` and `internal/world/mutator.go:20` (verified
  2026-08-08). Both must be restated in terms of the caller value, not merely deleted.
- `rg -n 'world\.Caller' internal/plugin/hostfunc/helpers.go` shows the callback type's third
  parameter and a doc comment that still records `plugin:<pluginName>` as the resulting ABAC
  subject — the guarantee did not silently disappear along with the parameter it described.
- `rg -n 'world\.Caller|Caller\)' internal/property/entity_mutator.go` shows the caller value
  reaching the `mutator.UpdateLocation` / `mutator.UpdateObject` call sites unwrapped — there is no
  `HumanCaller(` anywhere in `internal/property/`'s **production** code.
- `rg -n 'HumanCaller\(' internal/property/ --glob '!*_test.go' | wc -l` returns 0. **The
  `--glob '!*_test.go'` is load-bearing and an unscoped form of this criterion is unsatisfiable**:
  the position-4 census above requires `internal/property/definition_error_test.go` :22/:34 and
  `internal/property/entity_mutator_test.go` :73/:80/:90/:100/:110/:119 to construct a caller from
  the literal `"subject"`, which is eight legitimate `world.HumanCaller(` occurrences under that
  directory. D-66 forbids converting a *threaded* caller back to a bare string in production code;
  it does not forbid a test constructing one. For completeness assert the test side too:
  `rg -n 'HumanCaller\("subject"\)' internal/property/ | wc -l` returns **8**.
- `go vet`-equivalent compilation succeeds: `task build` exits 0.
- Every grouped declaration that was migrated retains all of its original parameters: for each of
  `FindLocationByName` and `ListPropertiesByParent` and the two `Definition.Set` implementers,
  the declaration's parameter count is unchanged from the pre-flip tree.
- The compile-time assertion set is intact. Both halves must hold:
  - `rg -n --glob '*.go' '^\s*(var\s+)?_ [A-Za-z_.]*(Mutator|WorldService|WorldQuerier) = ' internal/ | wc -l`
    returns **13**, the pre-flip count measured on this tree 2026-08-08 with this exact command. The
    `(var\s+)?` prefix is load-bearing: `internal/command/types.go:110` sits inside a grouped
    `var ( _ WorldService = ... )` block and a `var _ `-anchored pattern cannot match it. The
    `[A-Za-z_.]*` prefix is likewise load-bearing: 10 of the 13 are package-qualified
    (`hostfunc.WorldMutator`, `world.Mutator`).
  - `git diff -- '*.go' | rg '^-\s*(//\s*)?(var\s+)?_ [A-Za-z_.]*(Mutator|WorldService|WorldQuerier) = ' | wc -l`
    returns 0 — no assertion line was deleted, and none was commented out. This is the property that
    actually matters; the count alone cannot distinguish "unchanged" from "one deleted, one added".
    Pipe through `wc -l`, not `rg -c`: `rg -c` prints nothing and exits 1 on no match, so a bare
    `rg -c ... ` can never literally "return 0".
- `rg -n 'PropertyAccess|WorldQueryAccess' internal/plugin/hostfunc/cap_property.go internal/plugin/hostfunc/cap_world_query.go` shows those interfaces unchanged.
- `task test -- ./internal/world/ ./internal/command/ ./internal/property/ ./internal/bootstrap/ ./internal/plugin/...` exits 0.
- `task lint` exits 0 and **`task fmt:check` exits 0**. Do not phrase this as "`task fmt` leaves
  `git status --porcelain` clean" — the intended changes make the worktree dirty before the planned
  commit, so that criterion cannot hold. `task fmt:check` (`Taskfile.yaml:1033`) is the repo's
  non-mutating gate.
  </acceptance_criteria>

  <reversibility rating="costly">Reverting the 23-signature flip and the six-interface lockstep means unwinding a cross-package refactor; D-56 records the maintainer's reasoning for choosing this shape over the variadic that would have been reversible.</reversibility>

  <done>
No public `world.Service` command carries a bare subject string; the six redeclaring interfaces, the
callback type, and the three supporting signatures all take `world.Caller`; `internal/property`
threads the caller value through all three hops with no bare-string hop remaining; every grouped
declaration kept all its parameters; all 13 compile-time assertions still compile; the touched
packages' unit suites pass with assertions unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate this task's 17 arg-2 production call sites, the position-4 Definition.Set site, and the three real cascade-delete service calls, against the verified per-site subject map</name>

  <files>
internal/bootstrap/setting.go,
internal/plugin/hostcap/world.go,
internal/plugin/hostcap/property.go,
internal/plugin/hostfunc/adapter.go,
internal/plugin/hostfunc/world_write.go,
internal/world/grpc_server.go,
internal/world/postgres/cascade_delete_test.go
  </files>

  <read_first>
- `.planning/phases/02.1-world-caller-model/02.1-RESEARCH.md` § "Q2. Subject string formats at the
  production call sites" — the table naming every site, its current arg-2 expression, what it
  evaluates to, and what it maps to. This is the review checklist for this task; work row by row
  rather than by file. **Q2 is TWO tables, not one:** the main table is rows 1-27 (`:351-377`) and a
  second table carries rows **28-31** (the `cap_*` façades). Read to row 31. Three further sites
  have no Q2 row at all and are specified in this task's `<action>` instead — the `Definition.Set`
  caller at `internal/plugin/hostcap/property.go:84` and the three
  `internal/world/postgres/cascade_delete_test.go` calls.
- `internal/bootstrap/setting.go` lines 125-145 and 265-340 — the four sites at :134, :272, :276,
  :331, each passing the literal `"system:bootstrap"`.
- `internal/access/policy/engine.go` lines 88-105 — the S1 gate. The subject comparison at :92 is
  exact string equality, not a prefix match, which is precisely why `"system:bootstrap"` is a
  normally policy-evaluated subject and must not become a system caller.
- `internal/plugin/hostcap/world.go` lines 140-150 and 215-395 — the four true sites at :227, :288,
  :330, :385 (each with its `subject := access.PluginSubject(s.pluginName)` local one line above),
  and the verified false positive at :143 whose second argument is an id, not a subject.
- `internal/plugin/hostfunc/adapter.go` lines 60-150 — `SubjectID()` at :67-69 and the four call
  sites at :75, :99, :123, :146. `:75` already migrated in Plan 01.
- `internal/plugin/hostfunc/world_write.go` lines 40-60, 100-120, 180-215, and 310-350.
  **Corrected enumeration, measured 2026-08-08 with
  `rg -n 'withMutatorContext|withQueryContext' internal/plugin/hostfunc/world_write.go`:** the four
  `withMutatorContext` call sites are `:46`, `:106`, `:183`, **`:341`**, with callback bodies at
  `:48`, `:108`, `:185`, **`:343`**. `:209` is **not** a `withMutatorContext` callback — it sits
  inside a **`withQueryContext`** callback opened at `:207`, which builds its own subject inline at
  `:208` (`subjectID := access.PluginSubject(pluginName)`) and calls
  `f.worldMutator.FindLocationByName(ctx, subjectID, name)` at `:209`. An earlier draft listed the
  callbacks as `:48, :108, :185, :209`, which silently dropped the genuine fourth — the
  `set_property` path at `:343`, which is also the site that forwards into `opts.definition.Set`
  via `setEntityProperty` (declared `:315`, called `:343`, forwarding at `:319`). After Task 1 the
  `withMutatorContext` callback's third parameter is already a caller, so :48/:108/:185/:343 pass
  it through unwrapped; :209 is the one site in this file that wraps.
- `internal/plugin/hostcap/property.go` lines 78-90 — `propertyServer.SetProperty` builds
  `subjectID := access.PluginSubject(s.pluginName)` at `:83` and calls
  `def.Set(ctx, querier, s.host.WorldMutator(), subjectID, …)` at `:84`. The subject is at
  **parameter position 4**, so the arg-2 census never saw this site and no prior artifact
  enumerated it. Task 1 flips `property.Definition.Set` (`registry.go:58`), so this does not
  compile unless it is migrated here.
- `internal/world/postgres/cascade_delete_test.go` lines 325-420 — three genuine `world.Service`
  calls against `world.NewService(...)`: `svc.DeleteLocation(ctx, "system", locID)` at `:338`,
  `svc.DeleteObject(ctx, "system", objID)` at `:377`, `svc.DeleteCharacter(ctx, "system", charID)`
  at `:416`. The file carries `//go:build integration` at `:4`. Note the second argument is the
  **bare `"system"` literal** — the exact string the S1 gate compares.
- `internal/world/grpc_server.go` lines 35-110 — the four sites at :44, :62, :80, :101, each with
  its `subjectID := access.CharacterSubject(...)` local one line above. This file is `package world`,
  so the constructor is spelled unqualified.
- `internal/grpc/auth_handlers.go` line 972 and `internal/grpc/location_follow.go` — both fully
  migrated in Plan 01; confirm no residue.
  </read_first>

  <action>
**Scope arithmetic first — "all 31 production arg-2 call sites" is NOT this task's responsibility,
and an earlier draft's `<done>` said so while the same block declared four of the 31 untouched.**
The phase has **31** arg-2 production call sites in 10 files (Basis A). They partition, with zero
overlap, into four owners; re-measured 2026-08-08 by intersecting the arg-2 rule's production
matches with the tracer rule's:

| Owner | Sites | Where |
|---|---|---|
| **This task** | **17** | `internal/bootstrap/setting.go` :134/:272/:276/:331 (4); `internal/plugin/hostcap/world.go` :227/:288/:330/:385 (4); `internal/plugin/hostfunc/adapter.go` :99/:123/:146 (3); `internal/plugin/hostfunc/world_write.go` :48/:108/:185/:209 (4); `internal/world/grpc_server.go` :62/:80 (2) |
| Plan 01 (tracer, already done) | 6 | `internal/grpc/location_follow.go` :203/:216; `internal/grpc/auth_handlers.go:972`; `internal/world/grpc_server.go` :44/:101; `internal/plugin/hostfunc/adapter.go:75` |
| Task 1 of this plan (D-66 threading) | 4 | `internal/property/entity_mutator.go` :142/:162/:188/:208 — the caller is threaded through unwrapped, never `HumanCaller`-wrapped |
| Deliberately untouched (Q2 rows 28-31) | 4 | `internal/plugin/hostfunc/cap_property.go` :74/:140; `internal/plugin/hostfunc/cap_world_query.go` :84/:119 |

17 + 6 + 4 + 4 = 31. **`internal/world/grpc_server.go` :44 and :101 are Plan 01's `GetLocation` /
`GetExitsByLocation` — verify them, do not re-edit them**; only :62 and :80 are yours. Likewise
`internal/plugin/hostfunc/adapter.go:75`. The file-level acceptance criteria below assert the
FINAL state (4 `HumanCaller(` in `grpc_server.go`, for example), which is Plan 01's contribution
plus yours; a count short by 2 there means you re-wrote a tracer site instead of verifying it.

Beyond the 17, this task also owns **1** position-4 production site
(`internal/plugin/hostcap/property.go:84`) and **3** integration-tagged real service calls
(`internal/world/postgres/cascade_delete_test.go` :338/:377/:416) — 21 sites in the 7 files listed
above. Work row by row against RESEARCH § Q2 (**rows 1-31 across its two tables**, not just the
27-row main table) rather than file by file, because the mapping is not uniform and three rows are
security-relevant judgment calls.

`internal/bootstrap/setting.go` at :134, :272, :276, :331 — wrap the literal `"system:bootstrap"`
in `world.HumanCaller`. These stay normally policy-evaluated. Do not reach for the system
constructor here: the S1 bypass branch is entered only on exact equality with the bare `system`
subject, so mapping these four to a system caller would silently convert four policy-evaluated
bootstrap writes into total ABAC bypasses — the single highest-risk judgment call in this migration,
and one that no existing test necessarily catches. A diff hunk in this file containing the system
constructor is the warning sign.

`internal/plugin/hostcap/world.go` at :227, :288, :330, :385 — wrap the existing `subject` local in
`world.HumanCaller`. Leave :143 alone; its second argument is an id, and the interface it calls
(`capabilities.go:46`) declares a subject-free signature.

`internal/plugin/hostfunc/adapter.go` at :99, :123, :146 — wrap `a.SubjectID()` in
`world.HumanCaller`. Do not change `SubjectID()` itself; it stays a string method and other code
depends on it.

`internal/plugin/hostfunc/world_write.go` at **:48, :108, :185, :343** — the four
`withMutatorContext` callback bodies. After Task 1 the callback's third parameter is already a
`world.Caller`, so pass it straight through with no wrapping. **`:343` is the `set_property`
callback and is the one an earlier draft dropped by listing `:209` in its place; it is also the
site that forwards into `opts.definition.Set` via `setEntityProperty`.** `:209` is a separate
construct — a `withQueryContext` callback (opened `:207`) that builds its own subject inline at
`:208`; wrap that one, and only that one, in this file. `setEntityProperty` (declared `:315`,
forwarding at `:319`) has its parameter type changed by Task 1's `Definition.Set` flip; its body
does not change.

`internal/plugin/hostcap/property.go` at **:84** — wrap the existing `subjectID` local (built at
`:83` as `access.PluginSubject(s.pluginName)`) in `world.HumanCaller`, giving
`def.Set(ctx, querier, s.host.WorldMutator(), world.HumanCaller(subjectID), …)`. **This site has no
Q2 row and is invisible to the arg-2 census** because `Definition.Set` carries its subject at
parameter position 4. It is one of exactly two production `Definition.Set` callers; the other is
`internal/plugin/hostfunc/world_write.go:319`. Confirm the pair before you start with
`rg -n '\.Set\(ctx' --glob '*.go' -g '!*_test.go' internal/` and check that both appear in this
task's diff. The resulting ABAC subject is byte-identical — `plugin:<pluginName>` — because only
the wrapper is new. This file requires a `world` import if it does not already have one.

`internal/world/postgres/cascade_delete_test.go` at **:338, :377, :416** — map the bare `"system"`
literal to **`world.HumanCaller("system")`**, not `world.SystemCaller()`. This is a decision, not a
default, and it is recorded here so it is not improvised at the gate:

- Behaviour preservation is the deciding factor. These three calls run against
  `policytest.AllowAllEngine()` with a plain `context.Background()`, so today they are permitted by
  the test engine, never by the S1 bypass. `world.HumanCaller("system")` reproduces exactly that:
  the subject string stays byte-identical and the engine still evaluates it normally.
- `world.SystemCaller()` would be a *behaviour change disguised as a rename*. It stamps
  `access.WithSystemSubject` on the derived evaluation context, and combined with the bare `system`
  subject that is precisely the S1 total-bypass branch (`internal/access/policy/engine.go:92-93`,
  exact string equality AND the context marker). That would create three test-visible ABAC bypasses
  where none exists today — the same one-character hazard the four `system:bootstrap` sites are
  fenced against, in the one place where the literal actually *is* `system`.
- These three are also the only sites in the whole migration where the pre-flip second argument is
  the bare `system` literal. Treat a `SystemCaller()` appearing in this file's diff as the warning
  sign, exactly as for `internal/bootstrap/`.

The file is `//go:build integration`, so this migration compiles for the first time at Task 3's
`task test:int` gate — the most expensive place to discover a mistake. Do it deliberately here.
Note that the committed arg-2 codemod DOES reach these three sites (the receiver is `svc`, which is
not in the receiver blocklist), so if the codemod has already run they will read
`world.HumanCaller("system")` — verify rather than re-edit.

`internal/world/grpc_server.go` at :44, :62, :80, :101 — wrap the existing `subjectID` local. This
file is `package world`, so use the unqualified constructor spelling; the `world.`-qualified form
inside `package world` produces a doubled package qualifier.

At every site the constructor receives the *already-built* subject expression verbatim. Never
re-derive, re-prefix, or normalize it: the same string is written into the world-change outbox
envelope's actor field via `buildIntent` and `buildMoveIntent`, `wmodel` performs no validation on
it, and any drift silently alters persisted audit-bearing payloads.

Leave `internal/plugin/hostfunc/cap_property.go` at :74, :140 and
`internal/plugin/hostfunc/cap_world_query.go` at :84, :119 untouched — the Lua-facing all-string
facades (Q2 rows 28-31). Before finishing, confirm the "no production implementer" assumption still
holds. **Search call expressions and exclude the two defining files** — a bare identifier grep
returns 4, matching the constructors' own declarations, and can never be 0. Run
`rg -n 'NewPropertyCapability\(|NewWorldQueryCapability\(' --glob '*.go' --glob '!*_test.go' internal/`
piped through `rg -v 'cap_property\.go|cap_world_query\.go'` and then `wc -l`; it must return 0.
Record the result in the summary. If either turns out to be wired, stop and flag it rather than
migrating it.

After this task, `rg` for the world command names taking a bare string second argument across
production files should return only the deliberately-excluded facades.
  </action>

  <verify>
    <automated>task build</automated>
    <automated>task test -- ./internal/bootstrap/ ./internal/plugin/... ./internal/world/ ./internal/grpc/ ./internal/property/</automated>
    <automated>task lint</automated>
  </verify>

  <acceptance_criteria>
- `rg -n 'SystemCaller' internal/bootstrap/ | wc -l` returns 0.
- `rg -n 'HumanCaller\("system:bootstrap"\)' internal/bootstrap/setting.go | wc -l` returns 4.
- `rg -n 'SystemCaller\(\)' --glob '*.go' --glob '!*_test.go' internal/ cmd/` returns exactly the two
  lines in `internal/grpc/location_follow.go`.
- `rg -n 'HumanCaller\(' internal/world/grpc_server.go | wc -l` returns 4, and none of those
  occurrences is spelled with a `world.` qualifier.
- `rg -n 'HumanCaller\(' internal/plugin/hostcap/world.go | wc -l` returns 4 — the sites at :227,
  :288, :330, :385. `internal/plugin/hostcap/world.go:143` is unchanged from the pre-flip tree
  (`git diff internal/plugin/hostcap/world.go` contains no hunk touching it); it is excluded by the
  codemod's `querier` receiver constraint, NOT by a path ignore, because the same file holds the
  four real sites.
- `rg -n 'HumanCaller\(' internal/plugin/hostfunc/world_write.go | wc -l` returns 1, and that one
  occurrence is at the `withQueryContext` site near :209 — **not** in any of the four
  `withMutatorContext` callback bodies at :48, :108, :185, :343, which pass the threaded caller
  unwrapped. Confirm the callback set with
  `rg -n 'withMutatorContext|withQueryContext' internal/plugin/hostfunc/world_write.go`, which
  returns call sites at :46, :106, :183, :341 (mutator) and :207, :366 (query).
- `rg -n 'HumanCaller\(' internal/plugin/hostcap/property.go | wc -l` returns 1, at :84, and
  `rg -n '\.Set\(ctx' --glob '*.go' -g '!*_test.go' internal/ | rg 'definition\.Set|def\.Set' | wc -l`
  returns 2 — both `Definition.Set` production callers are accounted for
  (`hostcap/property.go:84`, `hostfunc/world_write.go:319`).
- `rg -n 'HumanCaller\("system"\)' internal/world/postgres/cascade_delete_test.go | wc -l` returns
  3 (at :338, :377, :416), and `rg -n 'SystemCaller' internal/world/postgres/ | wc -l` returns 0.
  Three test-visible ABAC bypasses is what the wrong mapping here would cost.
- `git diff` over the migrated production files shows, for every hunk, that the expression handed to
  the constructor is character-for-character the pre-flip second argument.
- **The out-of-scope façades still have no production caller.** The check must search *call
  expressions* and exclude the two defining files — a bare
  `rg -n 'NewPropertyCapability|NewWorldQueryCapability' --glob '*.go' --glob '!*_test.go' internal/`
  returns **4**, not 0, because it matches the constructors' own declarations and doc comments at
  `cap_world_query.go:54,55` and `cap_property.go:46,47` (measured 2026-08-08). Use instead:
  `rg -n 'NewPropertyCapability\(|NewWorldQueryCapability\(' --glob '*.go' --glob '!*_test.go' internal/ | rg -v 'cap_property\.go|cap_world_query\.go' | wc -l`
  → returns 0. Record the result in the plan summary. If either turns out to be wired, stop and flag
  it rather than migrating it.
- `task build` exits 0; `task test -- ./internal/bootstrap/ ./internal/plugin/... ./internal/world/ ./internal/grpc/ ./internal/property/` exits 0; `task lint` exits 0. **`./internal/property/` is load-bearing** — this task's `internal/plugin/hostcap/property.go:84` migration is one end of the `Definition.Set` position-4 flip whose other end (`registry_test.go`, `definition_error_test.go`, `entity_mutator_test.go`) lives in that package, and Task 1's gate already scopes to it; leaving it out of this task's gate means a broken `internal/property` test surfaces one task later than it should.
  </acceptance_criteria>

  <reversibility rating="reversible">Individual call-site mappings are one-line changes; Q2's rows 1-31 plus this task's three no-Q2-row sites make each independently re-checkable.</reversibility>

  <done>
This task's **17** arg-2 production call sites carry a caller value, as do the position-4
`Definition.Set` site at `internal/plugin/hostcap/property.go:84` and the three real
`world.Service` calls in `internal/world/postgres/cascade_delete_test.go` — 21 sites in 7 files.
Together with Plan 01's 6 tracer sites and Task 1's 4 `internal/property` threading sites that
accounts for **27** of the phase's 31 arg-2 production sites; the remaining **4** are the
`cap_property.go` / `cap_world_query.go` façades (Q2 rows 28-31), which this task deliberately
leaves untouched and re-verifies as unwired. The four bootstrap sites use the human
constructor with the unchanged `system:bootstrap` literal; the only system-constructor sites in
production are the two in `location_follow.go`; every constructor argument is byte-identical to the
pre-flip expression; the two out-of-scope Lua facades are untouched and their no-implementer
assumption is re-verified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the committed codemod over the test tier, then gate the flip on the full and integration suites</name>

  <files>
internal/world/service_test.go,
internal/world/movement_hook_test.go,
internal/world/mutator_test.go,
internal/world/service_helpers_test.go,
internal/world/grpc_server_test.go,
internal/bootstrap/setting_test.go,
internal/access/policy/attribute/character_test.go,
internal/plugin/building_integration_test.go,
internal/plugin/hostcap/world_test.go,
internal/plugin/hostcap/property_test.go,
internal/plugin/hostfunc/adapter_test.go,
internal/plugin/hostfunc/functions_test.go,
internal/plugin/hostfunc/world_test.go,
internal/plugin/hostfunc/world_write_test.go,
internal/plugin/lua/corebuilding_brokered_command_test.go,
internal/plugin/lua/dispatch_propagation_test.go,
internal/plugin/goplugin/dispatch_propagation_test.go,
internal/property/entity_mutator_test.go,
internal/property/registry_test.go,
internal/property/definition_error_test.go,
internal/plugin/objects_integration_test.go,
test/integration/resilience/m2_dualwrite_test.go,
test/integration/resilience/m12_lastwritewins_test.go,
test/integration/resilience/restart_reconnect_test.go,
test/integration/access/concurrent_engine_test.go,
test/integration/access/seed_policies_test.go,
test/integration/world/character_lifecycle_test.go
  </files>

  <read_first>
- `scripts/codemod/README.md` — the run order, the preview and apply commands, the enumerated
  hand-migrated surfaces, and the grouped-declaration probe.
- `.planning/phases/02.1-world-caller-model/02.1-PATTERNS.md` § "Test-double declarations" — the
  three shapes the declaration rule must handle, and specifically
  `internal/property/entity_mutator_test.go`, which holds both a subject-carrying
  `fakeVersionMutator` and a subject-free `fakeVersionQuerier`: a correct rule touches the first and
  leaves the second alone. Treat that file as the codemod's regression fixture.
- `internal/world/service_test.go` header (it is `package world_test`) and
  `internal/world/mutator_test.go` / `internal/world/service_helpers_test.go` headers (both
  `package world`) — this is the qualification split the two rule variants exist for.
- `.planning/phases/02.1-world-caller-model/02.1-RESEARCH.md` § "Integration-test exposure" — every
  `test/integration/**` file touched here carries `//go:build integration` and is invisible to
  `task test`. **51** true call sites live behind that tag (48 across the six `test/integration/**`
  files + 3 in `internal/world/postgres/cascade_delete_test.go`), of which Plan 01's scoped
  `task test:int` gate already covered 15.
- `internal/world/service_test.go` lines 6079-6284 — `TestWorldService_MalformedAccessParams`, nine
  subtests, **all nine supplying an empty subject**, whose call expressions the codemod
  rewrites and whose assertions must not move. The ninth case's NAME says "valid subject" but its
  body at `:6252` passes `""`; an earlier replan mis-corrected this to "eight of nine". **Do not
  edit that name** — criterion 3 freezes these subtests and renaming a table case is a test change.
- `internal/plugin/lua/corebuilding_brokered_command_test.go` lines 48-100 — the
  `recordingWorldMutator` double. **Seven of its ten methods declare unnamed parameters** and are
  therefore invisible to both the declaration codemod and `probe-subject-param.yml`: `GetLocation`
  :52 (hand-migrated by Plan 01), `GetCharacter` :56, `GetCharactersByLocation` :60, `GetObject`
  :64, `UpdateLocation` **:89**, `UpdateObject` **:93**, `FindLocationByName` **:97**. Only the
  three `Create*` methods (:68, :75, :82) use `_ string` and match the rule. Re-derive the
  coordinates before editing with
  `rg -n '^func \(m \*recordingWorldMutator\)' internal/plugin/lua/corebuilding_brokered_command_test.go`;
  an earlier replan wrote :84/:88/:92 for the last three, which are body lines, not declarations.
- `internal/plugin/objects_integration_test.go` lines 60-75 — `recordingCharacterMutator`
  implements `property.EntityMutator`: `SetName(context.Context, property.WorldQuerier,
  property.WorldMutator, string, ulid.ULID, string)` at `:63` (**unnamed** — invisible to codemod
  and probe) and `SetDescription(_ context.Context, _ property.WorldQuerier, _
  property.WorldMutator, subjectID string, entityID ulid.ULID, value string)` at `:71`. Both must
  change when Task 1 flips `EntityMutator`. It is registered via `Register(characterMutator)` at
  `:96`, so the compiler catches it — but only after an unplanned failure at the gate.
  </read_first>

  <action>
Run the four committed rules from `scripts/codemod/` in the order documented in
`scripts/codemod/README.md`, previewing each with `ast-grep scan -r <rule> .` before applying with
`ast-grep scan -r <rule> -U .`. Apply the externally-qualified variants first, then the
`package world` variants, so no file is rewritten twice.

Before applying, run the grouped-declaration probe documented in the codemod README to re-enumerate
the 21 grouped `subjectID, ...` parameter declarations repo-wide. Of those, the in-scope ones not
already hand-migrated by Task 1 live in `internal/plugin/hostfunc/world_write_test.go` (4) and
`internal/plugin/hostcap/world_test.go` (1); hand-migrate those. The remainder —
`internal/access/policy/attribute/viewer.go`, `internal/plugin/hostfunc/cap_property.go` and its
test, `internal/plugin/hostfunc/cap_world_query.go` and its test — are out of scope and stay as
they are.

**The `subjectID`-anchored grouped probe under-reports by seven.** A grouped `_, _ string`
declaration carries the identical truncation hazard — a naive `has: {field: name}` rule matches it
and the fix drops the second name exactly as it would for `subjectID, name string` — but the probe
anchors on the identifier `subjectID` and cannot see it. Measured 2026-08-08 with an
`ast-grep` `parameter_declaration` rule on `regex: '^_, _ string$'` scoped to the 23 command names,
there are exactly **seven**, all in files already in this plan's `files_modified`:
`internal/plugin/building_integration_test.go:101`,
`internal/plugin/hostcap/property_test.go:104`,
`internal/plugin/hostfunc/functions_test.go:77`,
`internal/plugin/hostfunc/world_test.go:82` and `:592`,
`internal/plugin/lua/dispatch_propagation_test.go:72`,
`internal/plugin/goplugin/dispatch_propagation_test.go:90`.
All seven are `FindLocationByName`. Hand-migrate them, and verify none lost a parameter name.

**Hand-migrate the SEVEN fully-unnamed declarations this task owns** — eight exist repo-wide and
Plan 01 already took `corebuilding_brokered_command_test.go:52`, so seven remain here. (An earlier
replan headed this same seven-item list with "eight", which reads as a missing item; the repo-wide
total is eight, the count for this task is seven.) These are `parameter_declaration` nodes with no
`name` field at all:
- `internal/plugin/lua/corebuilding_brokered_command_test.go` — six remaining after Plan 01's `:52`:
  `GetCharacter` :56, `GetCharactersByLocation` :60, `GetObject` :64, `UpdateLocation` **:89**,
  `UpdateObject` **:93**, `FindLocationByName` **:97**. Re-derive with
  `rg -n '^func \(m \*recordingWorldMutator\)' internal/plugin/lua/corebuilding_brokered_command_test.go`
  rather than trusting these numbers; an earlier replan wrote :84/:88/:92, which are body lines.
- `internal/plugin/objects_integration_test.go:63` — `recordingCharacterMutator.SetName`.

**Hand-migrate the position-4 TEST surface — no rule and no probe reaches it, and three of its five
files appeared in no plan before this revision.** Work from the census table in Task 1's
`<read_first>`; the four completeness commands there cover `_test.go` by construction, unlike the
earlier `-g '!*_test.go'` command that hid these:
- `internal/property/registry_test.go:29` — `testDefinition.Set`, all-blank parameter names,
  `package property`, so the fourth parameter becomes `_ world.Caller`.
- `internal/plugin/hostcap/property_test.go:40` — `fakePropertyDef.Set`, all-blank names,
  qualified `property.WorldQuerier` / `property.WorldMutator`; fourth parameter becomes
  `_ world.Caller`.
- `internal/property/definition_error_test.go:22` and `:34` — both
  `def.Set(context.Background(), nil, nil, "subject", "widget", ulid.ULID{}, "value")`; the
  position-4 `"subject"` becomes `world.HumanCaller("subject")`.
- `internal/property/entity_mutator_test.go:137` and `:145` — `testEntityMutator.SetName` /
  `SetDescription`, all-blank names; position-4 becomes `_ world.Caller`. Its six call sites at
  `:73`, `:80`, `:90`, `:100`, `:110`, `:119` each pass the literal `"subject"` at position 4 and
  become `world.HumanCaller("subject")`.
Adding `world.HumanCaller(` under `internal/property/` in these test files does NOT violate D-66 —
that prohibition is on unwrapping a threaded caller in production code, and the completeness grep
for it carries `--glob '!*_test.go'` for exactly this reason.

Also hand-migrate `internal/plugin/objects_integration_test.go:71`
(`SetDescription(… subjectID string …)`): it is a named `property.EntityMutator` parameter but at
position 4 under a method name outside the 23-command regex, so no committed rule reaches it. **This
file is NOT in the "legitimately keeps the identifier" list below** — that claim is true of its
local variables and false of these two declarations, and it is in this plan's `files_modified`.

After applying, inspect the diff and confirm the constraint scoping held. Expect:
- **no** hunk in `internal/world/mutator.go`, `internal/plugin/hostfunc/world.go`, at
  `internal/plugin/hostcap/world.go:143`, at `internal/plugin/hostfunc/adapter_test.go`
  :230/:241/:254/:266/:277/:414, at `internal/world/postgres/character_repo_test.go`
  :543/:571/:582/:825/:841/:849, or at the five mockery `EXPECT()` sites
  (`internal/world/service_test.go` :4016/:4054/:4208/:5065,
  `internal/world/movement_hook_test.go:83`);
- **hunks present** at `internal/plugin/hostcap/world.go` :227/:288/:330/:385 and at
  `internal/world/postgres/cascade_delete_test.go` :338/:377/:416 — their absence means the
  false-positive constraints over-suppressed and the rule needs fixing.

If a wrong hunk appeared, revert it and fix the rule's constraints rather than the file, because the
rule is the committed review artifact and must reproduce the diff.

Fix whatever the compiler surfaces that the rules could not reach: the 13 compile-time interface
assertions are designed to fail loudly here, and residual test doubles whose method names sit
outside the 23-command regex (for example a double implementing only part of an interface) need hand
edits. Under no circumstances change an assertion to make a test pass — the whole value of criterion
3 is that the assertion set is the unchanged control. If a test genuinely cannot pass without an
assertion change, stop and report it as a behavioral regression rather than editing it.

Run `task fmt` — the mass parameter-type rewrite touches aligned declaration blocks.

Then gate the flip. `task test` alone is not sufficient: **51** of the migrated call sites sit
behind `//go:build integration` and are never compiled by the untagged lane, so a broken
integration-tier signature would ship silently. The **unscoped** `task test:int` is mandatory here
and needs Docker; if Docker is unavailable, stop and report rather than committing an unverified
flip. (For reference: `task test:int` DOES accept `--` package args —
`Taskfile.yaml:289` is `-coverpkg=./... {{.CLI_ARGS | default "./..."}}` — which is what let Plan 01
gate its tracer commit on a scoped subset. Here the whole tree moves, so run it unscoped. Do not
confuse `task test:int:focus`, a Ginkgo `-ginkgo.focus=` filter pinned to
`./test/integration/plugin`, with scoping.)

Then run the committed census probe to prove no bare subject-string parameter survives on a world
command: `ast-grep scan -r scripts/codemod/probe-subject-param.yml . | rg -c '┌─ '` must return
exactly 8, and all 8 must be in `cap_property.go`, `cap_property_test.go`, `cap_world_query.go`, or
`cap_world_query_test.go`. Any other file in that output is an unmigrated site — fix it before
committing. Do **not** substitute a repo-wide `rg` for the identifier `subjectID`: the identifier
survives legitimately as a local variable at hundreds of sites (503 in
`internal/world/service_test.go` alone) and at six out-of-scope files, so such a grep can never
reach zero and chasing it either stalls the task or edits out-of-scope ABAC tests. The probe is
`-U`-free — never apply it.

Finally, prove D-63's review contract: with the tree fully migrated and the working directory clean,
re-run all four **rewrite** rules with `-U` and confirm `git diff --exit-code` reports no change.
The probe carries no `fix:` and is excluded from this re-run. That is the
idempotency claim the rule headers make and the mechanism by which a reviewer verifies ~500 hunks
without reading them.

Commit everything from Tasks 1, 2, and 3 as a single commit — D-64 landmark (2), the atomic flip.
  </action>

  <verify>
    <automated>task fmt && task fmt:check && task lint</automated>
    <automated>task test</automated>
    <automated>task test:int</automated>
    <automated>task test -- -run 'TestWorldCallerSubjectReachesTheOutboxActorByteForByte' ./internal/world/</automated>
    <automated>test "$(ast-grep scan -r scripts/codemod/probe-subject-param.yml . | rg -c '┌─ ')" = 8 &amp;&amp; test "$(ast-grep scan -r scripts/codemod/probe-subject-param.yml . | rg -o '┌─ ([^:]+:[0-9]+)' -r '$1' | rg -v 'cap_property|cap_world_query' | wc -l | tr -d ' ')" = 0</automated>
    <automated>ast-grep scan -r scripts/codemod/world-caller-arg2.yml -U . &amp;&amp; ast-grep scan -r scripts/codemod/world-caller-arg2-internal.yml -U . &amp;&amp; ast-grep scan -r scripts/codemod/world-caller-decl.yml -U . &amp;&amp; ast-grep scan -r scripts/codemod/world-caller-decl-internal.yml -U . &amp;&amp; git diff --exit-code</automated>
  </verify>

  <acceptance_criteria>
- `task test` exits 0.
- `task test:int` exits 0, run unscoped. This is non-negotiable: 51 migrated call sites are behind
  `//go:build integration` and `task test` does not compile them.
- **Assertion-set immutability, measured against the pre-flip commit rather than the mutable index.**
  Capture `BASE=$(git rev-parse HEAD)` before any work in this plan, then:
  `git diff "$BASE" -- '*_test.go' | rg '^[+-]\s*(assert|require|errutil\.Assert)\.' | wc -l`
  returns 0. Do NOT use a bare `git diff -- '*_test.go'`: it inspects only *unstaged* changes, and
  this plan explicitly permits staging intermediate work ("If an intermediate state must be saved,
  stage without committing"), so the criterion would silently pass once the diff is staged. If a
  base SHA was not captured, `--cached` plus the unstaged form together are the fallback. Pipe
  through `wc -l`, never `rg -c`.
- **Byte-identity of the persisted outbox `Actor` is proven by an automated test, not by reading a
  diff.** T-02.1-06 is this phase's highest-severity threat and its only control today is a human
  confirming each constructor argument is "character-for-character" unchanged — which cannot detect
  a caller subject being normalized inside the caller value *before* `buildIntent` reads it, since
  every existing suite would still pass. Add a table-driven test in `internal/world` (`package
  world`, so it can read the caller's unexported subject field and drive `buildIntent` /
  `buildMoveIntent` directly) asserting the resulting `wmodel.IntentParams.Actor` is byte-for-byte
  equal to the input subject string across at least these four representative shapes:
  `character:<ulid>`, `plugin:<name>`, `system:bootstrap`, and the bare `system` produced by
  `SystemCaller()`. Assert with `require.Equal` on the raw Go strings — byte equality, not
  case-folded or normalized comparison. Name it per the repo's ACE rule (for example
  `TestWorldCallerSubjectReachesTheOutboxActorByteForByte`). This test is additive, so it does not
  violate criterion 3's "no existing assertion changes".
- `task test -- -run 'TestWorldService_MalformedAccessParams' ./internal/world/` exits 0 with its
  nine subtests intact — **all nine supply an empty subject**; only the ninth case's NAME says
  otherwise, and that name MUST NOT be edited (criterion 3).
- The codemod produced no hunk in `internal/world/mutator.go`, none in
  `internal/plugin/hostfunc/world.go`, none at `internal/plugin/hostcap/world.go:143`, none at
  `internal/plugin/hostfunc/adapter_test.go` :230/:241/:254/:266/:277/:414, none in
  `internal/world/postgres/character_repo_test.go`, and none at the five mockery `EXPECT()` sites.
- The codemod DID produce hunks at `internal/plugin/hostcap/world.go` :227/:288/:330/:385 and at
  `internal/world/postgres/cascade_delete_test.go` :338/:377/:416 — the over-suppression check.
  `internal/world/postgres/` as a whole is NOT an exclusion surface; only
  `character_repo_test.go`'s six `repo.UpdateLocation` calls are.
- The seven grouped `_, _ string` `FindLocationByName` declarations are migrated with no parameter
  name lost: `internal/plugin/building_integration_test.go:101`,
  `internal/plugin/hostcap/property_test.go:104`, `internal/plugin/hostfunc/functions_test.go:77`,
  `internal/plugin/hostfunc/world_test.go:82` and `:592`,
  `internal/plugin/lua/dispatch_propagation_test.go:72`,
  `internal/plugin/goplugin/dispatch_propagation_test.go:90`.
- All eight fully-unnamed declarations are migrated (seven by this task, one by Plan 01):
  `rg -n '(GetCharacter|GetCharactersByLocation|GetObject|UpdateLocation|UpdateObject|FindLocationByName)\(context\.Context, string' internal/plugin/lua/corebuilding_brokered_command_test.go | wc -l`
  returns 0, and
  `rg -n 'SetName\(context\.Context, property\.WorldQuerier, property\.WorldMutator, string' internal/plugin/objects_integration_test.go | wc -l`
  returns 0.
- `internal/property/entity_mutator_test.go`'s `fakeVersionMutator` methods migrated and its
  `fakeVersionQuerier` methods are unchanged.
- **The whole position-4 surface is migrated, and all four completeness commands return zero
  survivors.** This is the surface no arg-2 census, no committed rule, and not
  `probe-subject-param.yml` can see, so nothing but these greps and the compiler stands behind it:
  - `rg -n '\) Set\(.*WorldQuerier.*WorldMutator.*, _? ?string,' --glob '*.go' internal/ test/ plugins/ | wc -l`
    returns 0, while
    `rg -n '\) Set\(.*WorldQuerier.*WorldMutator' --glob '*.go' internal/ test/ plugins/ | wc -l`
    still returns **4** — every `Definition.Set` declaration survives and none keeps a `string` at
    position 4. The four are `internal/property/definition_name.go:34`,
    `internal/property/definition_description.go:34`, `internal/property/registry_test.go:29`, and
    `internal/plugin/hostcap/property_test.go:40`. The last two were in NO plan before this
    revision; a count below 4 means one was deleted rather than migrated.
  - `rg -n 'world\.Caller|Caller,' internal/property/registry_test.go internal/property/definition_error_test.go internal/plugin/hostcap/property_test.go | wc -l`
    is non-zero for each of the three files.
  - `rg -n '(SetName|SetDescription)\(.*WorldQuerier.*WorldMutator' --glob '*.go' internal/ test/ plugins/ | wc -l`
    still returns **10**, and none of the 10 carries `string` in the fourth slot.
  - The six `internal/property/entity_mutator_test.go` call sites at :73/:80/:90/:100/:110/:119 and
    the two `internal/property/definition_error_test.go` sites at :22/:34 each pass a constructed
    caller: `rg -n 'HumanCaller\("subject"\)' internal/property/ | wc -l` returns **8**.
- Re-running all four rules with `-U` on the clean post-flip tree leaves `git diff --exit-code`
  clean (D-63's review contract).
- **No subject-string parameter declaration survives on any of the 23 world command names.** Run the
  committed census probe:
  `ast-grep scan -r scripts/codemod/probe-subject-param.yml . | rg -c '┌─ '` returns exactly **8**,
  and every one of those 8 is one of the deliberately out-of-scope Lua-facing façades —
  `ast-grep scan -r scripts/codemod/probe-subject-param.yml . | rg -o '┌─ ([^:]+:[0-9]+)' -r '$1' | rg -v 'cap_property|cap_world_query' | wc -l`
  returns 0. Pre-flip that probe returns 185 across 24 files (measured 2026-08-08).
  Do **not** substitute a bare `rg -n 'subjectID' --glob '*_test.go' internal/ test/` census: measured
  on this tree that returns **615 lines across 17 files** (re-measured 2026-08-08; an earlier draft
  said 607/15, which omitted the two `cap_*_test.go` façade files enumerated as out-of-scope
  elsewhere in this plan — 4 lines each), of which
  `internal/world/service_test.go` alone holds 503 — overwhelmingly local variables
  (`subjectID := access.CharacterSubject(...)`) that MUST survive the flip, because they are exactly
  what gets wrapped in `HumanCaller`. **Five** of those files are out of this plan's scope entirely
  and legitimately keep the identifier: `internal/access/policy/attribute/plugin_provider_test.go`,
  `player_test.go`, `resolver_test.go`, `test/integration/plugin/binary_plugin_test.go`,
  `test/integration/resilience/chaos_helpers_test.go`. **`internal/plugin/objects_integration_test.go`
  was listed here in an earlier draft and has been removed: the claim is true of its local variables
  but false of its two `property.EntityMutator` declarations at `:63` and `:71`, which must migrate.
  It is now in this plan's `files_modified`.** The identifier is not the invariant; the parameter
  type is.
- The probe's blind spot is closed by the compiler, not by the probe: an **unnamed** parameter is
  invisible to it (see `scripts/codemod/README.md`). There are **eight** known instances repo-wide,
  not one: `internal/plugin/lua/corebuilding_brokered_command_test.go` :52 (hand-migrated in
  Plan 01), :56, :60, :64, **:89**, **:93**, **:97**, and
  `internal/plugin/objects_integration_test.go:63` — the last **seven** hand-migrated in this task.
  (Eight repo-wide, seven for this task; and :89/:93/:97 corrects an earlier replan's :84/:88/:92.)
  `task build` and `task test:int` are what prove no other one survives. Do not state that Plan 01
  closed this blind spot.
- `task fmt:check` exits 0 and `task lint` exits 0. (Not "`task fmt` leaves `git status --porcelain`
  clean" — the flip's own changes make the worktree dirty before the planned commit.)
- Tasks 1, 2, and 3 are committed as a single commit (D-64 landmark 2), and `git log --oneline -1`
  shows a conventional-commit message scoped to this phase.
  </acceptance_criteria>

  <precondition>Docker is running and available to testcontainers (`docker info` exits 0) — `task test:int` cannot compile or run the 51 build-tagged integration call sites without it.</precondition>

  <reversibility rating="costly">The flip is a single large commit by D-64; reverting it is one `git revert` but re-landing it means re-running the whole migration.</reversibility>

  <done>
The full tree is migrated. `task lint`, `task test`, and `task test:int` are all green. No test
assertion changed anywhere. Re-running the committed codemod rules against the migrated tree yields
an empty diff. Tasks 1-3 are committed together as the single atomic flip commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| call site → `world.Service` | The 31 arg-2 production sites (10 files) plus the entire position-4 surface — `Definition.Set` (4 declarations + 4 call sites) and `EntityMutator.SetName`/`SetDescription` (10 declarations + 8 call sites), spanning production and test, all invisible to the arg-2 census, to every committed rule, and to `probe-subject-param.yml`; each supplies caller identity that becomes both the ABAC subject and the persisted outbox actor. |
| `internal/world/postgres/cascade_delete_test.go` → `world.Service` | Three integration-tagged calls whose arg-2 is the bare `"system"` literal — the only sites in the migration one character from the S1 total-bypass branch. |
| `internal/bootstrap` → ABAC engine | A `system:bootstrap` subject that must remain policy-evaluated, one character away from the S1 total-bypass literal. |
| `internal/property` → `world.Mutator` | The three-hop chain where a bare subject string would otherwise survive the phase. |
| codemod → the tree | ~500 mechanical rewrites across an authorization surface, reviewed by rule reproducibility rather than by reading hunks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-01 | Elevation of Privilege | `internal/bootstrap/setting.go` (4 sites) | high | mitigate | Task 2 works row by row against RESEARCH § Q2's **rows 1-31 across both its tables**; the acceptance criteria assert the human constructor at all four sites and zero occurrences of the system constructor in `internal/bootstrap/`. `engine.go:92` is exact string equality, so the distinction is load-bearing. |
| T-02.1-11 | Elevation of Privilege | `internal/world/postgres/cascade_delete_test.go` :338/:377/:416 — the only sites in the migration whose pre-flip arg-2 is the **bare `"system"` literal** | high | mitigate | Task 2 pins the mapping to `world.HumanCaller("system")` with its behaviour-preservation rationale, and prohibits `SystemCaller()` in `internal/world/postgres/`. Mapping these to the system constructor would create three test-visible total ABAC bypasses (`engine.go:92-93` — exact equality on the subject AND the context marker). The file is `//go:build integration`, so a wrong choice surfaces only at Task 3's gate; the decision is therefore made in the plan, not at the gate. Acceptance criteria assert 3 `HumanCaller("system")` and 0 `SystemCaller` in that directory. |
| T-02.1-12 | Tampering | the arg-2 codemod's own idempotency | high | mitigate | A `world.Caller` reaches argument 2 in **three** syntactic shapes — constructor call, composite literal, bare identifier — and a single-clause `SUBJ: not: regex: '(HumanCaller\|SystemCaller)\('` guard covers only the first. Re-measured 2026-08-08, that single-clause rule still rewrites `svc.GetObject(ctx, world.Caller{}, id)` and `svc.GetExitsByLocation(ctx, c, id)`, breaking D-63's re-run-to-empty-diff contract — the sole mechanism by which a reviewer trusts ~500 unread hunks on an authorization surface. Plan 01 Task 2 now pins a **three-clause** `SUBJ` guard (positive `(?i)subj\|^"` allowlist + the constructor negative + `not: kind: composite_literal`) with retention re-verified at 351/4, adds `internal/world/caller_test.go` to the `-internal` rule's `ignores:`, and proves idempotency by running each rule TWICE over a four-shape fixture. This task's re-run gate is what detects a regression; the standing remedy for any hunk it produces is an `ignores:` entry or an inline constructor at the call site, never accepting the double-wrap. |
| T-02.1-04 | Elevation of Privilege | the ~500-site mass rewrite | high | mitigate | The rules wrap the already-built subject expression verbatim and never re-derive it; every surface where a blind wrap would be wrong is excluded by `ignores:` and hand-migrated; the unchanged assertion set of the existing suites is the behavioral control; D-63's re-run-to-empty-diff proves the diff is exactly what the committed rules produce. |
| T-02.1-06 | Repudiation | outbox envelope `Actor` at 12 `buildIntent` sites (`internal/world/service.go` :302, :332, :386, :448, :481, :513, :610, :643, :680, :728, :769, :825 — all twelve pass a bare `subjectID`) and 1 `buildMoveIntent` site (`:1079`) | medium | mitigate | The actor is sourced by direct field access on the caller inside `package world` into a plain `string` parameter, so a method-value mistake is a compile error. Byte identity of every migrated constructor argument is an acceptance criterion, **and Task 3 adds an automated detector** — see the row below, which is the mitigation that does not depend on a human reading a diff. |
| T-02.1-07 | Tampering | grouped parameter declarations | medium | mitigate | A naive declaration rule was observed silently dropping the second name of `subjectID, name string`. The committed rule excludes comma-bearing declarations by construction; the 21 grouped sites are re-enumerated by a documented probe and hand-migrated; an acceptance criterion asserts no migrated declaration lost a parameter. |
| T-02.1-08 | Elevation of Privilege | integration-tier call sites invisible to `task test` | high | mitigate | **51** of the migrated sites are `//go:build integration` (48 across six `test/integration/**` files + 3 in `internal/world/postgres/cascade_delete_test.go`); 15 were covered by Plan 01's scoped gate. The unscoped `task test:int` is a mandatory gate on this plan with a stated Docker precondition; the plan halts rather than committing an unverified flip. |
| T-02.1-13 | Repudiation | outbox envelope `Actor` — the automated detector T-02.1-06 previously lacked | medium | mitigate | Task 3 adds a table-driven `internal/world` test asserting the persisted `Actor` is byte-for-byte equal to the caller's subject across `character:<ulid>`, `plugin:<name>`, `system:bootstrap`, and bare `system`. Human diff-reading alone cannot detect normalization happening inside the caller value before `buildIntent` reads it — every existing suite would still pass. `buildPreferencesIntent` (`internal/world/service.go:905`, `Actor:` at `:921`) is a third envelope-actor helper on this same surface; it belongs to the D-05 exemption and is deliberately out of scope. |
| T-02.1-05 | Denial of Service | `internal/access/policy/compiler.go` `action` namespace | medium | mitigate | Out of scope by decision (02.2 D-59) and captured as a prohibition; this plan touches no schema registration. |
| T-02.1-SC | Tampering | package-manager installs | low | accept | No npm/pip/cargo package is installed by this plan. No Package Legitimacy Audit is applicable. |
</threat_model>

<verification>
- `task fmt`, `task lint`, `task test`, and `task test:int` all exit 0.
- `git diff --stat internal/access/` is empty — no ABAC policy text, seed, or engine behavior
  changed.
- Re-running the four committed codemod rules on the migrated tree yields `git diff --exit-code`
  clean. This is only achievable because the arg-2 rules carry the `SUBJ` idempotency guard
  (Plan 01 Task 2); without it the re-run double-wraps every migrated site.
- No assertion in any pre-existing test changed, measured against the pre-flip commit
  (`git diff "$BASE" -- '*_test.go' | rg '^[+-]\s*(assert|require|errutil\.Assert)\.' | wc -l` → 0),
  not against the mutable index.
- The 13 compile-time interface assertions still compile, unmodified.
- `TestWorldCallerSubjectReachesTheOutboxActorByteForByte` passes — the outbox `Actor` byte-identity
  guarantee has an automated detector, not only a human diff read.
</verification>

<success_criteria>
- ROADMAP criterion 1: no public `world.Service` command takes a bare subject string; no overload,
  variadic, or alias preserves the old shape.
- ROADMAP criterion 3: the existing suites pass at both tiers with assertions untouched.
- D-66: `internal/property` threads the caller value through all three hops.
- D-63: the committed rules reproduce the bulk diff and re-run to an empty diff.
- D-64: the flip is one commit, and every commit on the branch is green.
</success_criteria>

<artifacts_this_phase_produces>
Plan 02 creates no new exported symbols. It consumes `world.Caller`, `world.HumanCaller`, and
`world.SystemCaller` (created in Plan 01) and changes existing signatures in place. The one net-new
artifact is a test. The signature changes that a symbol-surface diff will flag as "new" are:

| Artifact | Kind |
|----------|------|
| `TestWorldCallerSubjectReachesTheOutboxActorByteForByte` (Task 3) | new test in `internal/world`, `package world` — the automated T-02.1-06 detector |
| `world.Mutator` (10 methods now taking `world.Caller`) | changed interface |
| `command.WorldService` (15 methods now taking `world.Caller`) | changed interface |
| `grpc.WorldQuerier` (2 methods) | changed interface |
| `hostfunc.WorldService` (4 methods, Deprecated) | changed interface |
| `bootstrap.worldSeeder` (3 methods) | changed unexported interface (not named in any prior artifact) |
| `property.WorldMutator` (2 methods) | changed interface |
| `property.EntityMutator` (`SetName`, `SetDescription`) | changed interface |
| `property.Definition.Set` | changed interface method |
| `hostfunc.withMutatorContext` callback type | changed function type |
| the 23 `world.Service` command signatures | changed methods |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02.1-world-caller-model/02.1-02-SUMMARY.md` when done.
</output>