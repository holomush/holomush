---
phase: 02.1
reviewers: [codex, pi]
reviewed_at: 2026-08-08T17:01:09Z
plans_reviewed: [02.1-01-PLAN.md, 02.1-02-PLAN.md, 02.1-03-PLAN.md]
---

# Cross-AI Plan Review — Phase 02.1 (World Caller Model)

Both lanes were prompt-fed with the full plan set and given repo read access; both produced
source-grounded reviews. Neither carries the `[reviewed-without-repo-access]` marker and neither is
a diff-only reviewer, so both count at full consensus weight.

---

## Codex Review

## 1. Summary

The plans have a strong authorization model and correctly preserve the critical distinction between `"system"` and `"system:bootstrap"`. However, they are not execution-ready. I found two source-grounded blockers: the codemod excludes three real `world.Service` calls under `internal/world/postgres/**`, and the proposed invariant census classifies `SetMovementHook` as an undocumented non-authorizing exported method. Several acceptance commands are also unsatisfiable or vacuous as written. Overall risk is **HIGH** until these are corrected.

## 2. Strengths

- The security model for `SystemCaller` is correct. The engine requires exact subject equality with `"system"` and a marked context at [internal/access/policy/engine.go:92](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/access/policy/engine.go:92), while the marker is independently tested through `IsSystemContext` at [internal/access/context.go:17](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/access/context.go:17). Deriving the marker inside `checkAccess` preserves the double gate while removing ambient marking from the call site.

- Plan 02 correctly protects `"system:bootstrap"` from accidental elevation. All four sites pass that exact literal at [internal/bootstrap/setting.go:134](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/bootstrap/setting.go:134), [internal/bootstrap/setting.go:272](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/bootstrap/setting.go:272), [internal/bootstrap/setting.go:276](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/bootstrap/setting.go:276), and [internal/bootstrap/setting.go:331](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/bootstrap/setting.go:331). Exact equality at `engine.go:92` means `HumanCaller("system:bootstrap")` remains normally evaluated.

- The attribute-forwarding design matches the real substrate. `NewAccessRequest` validates and clones the fourth argument at [internal/access/policy/types/types.go:143](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/access/policy/types/types.go:143), and the engine overlays it into `bags.Action` at [internal/access/policy/engine.go:258](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/access/policy/engine.go:258). A paired permit/deny test is an appropriate proof.

- The plans correctly treat subject preservation as audit-sensitive. `buildIntent` writes the actor verbatim at [internal/world/service.go:355](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/service.go:355), and character moves do likewise at [internal/world/service.go:1079](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/service.go:1079).

- The principal interface cascade is real and broader than the stale roadmap: `world.Mutator` at [internal/world/mutator.go:22](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/mutator.go:22), `command.WorldService` at [internal/command/types.go:38](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/command/types.go:38), `grpc.WorldQuerier` at [internal/grpc/server.go:129](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/grpc/server.go:129), `hostfunc.WorldService` at [internal/plugin/hostfunc/adapter.go:23](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/hostfunc/adapter.go:23), `bootstrap.worldSeeder` at [internal/bootstrap/setting.go:29](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/bootstrap/setting.go:29), and `property.WorldMutator` at [internal/property/registry.go:49](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/property/registry.go:49).

- D-66 is well grounded. The property chain really carries the subject through four world mutation calls at [internal/property/entity_mutator.go:142](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/property/entity_mutator.go:142), `:162`, `:188`, and `:208`. Threading `Caller` without unpacking it is the correct design.

- The plan correctly identifies the unnamed tracer implementer at [internal/plugin/lua/corebuilding_brokered_command_test.go:52](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/lua/corebuilding_brokered_command_test.go:52). It is guarded by the alias-based interface relationship from [internal/plugin/hostfunc/adapter.go:32](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/hostfunc/adapter.go:32).

- The compiler behavior behind the out-of-scope `action` registration decision is accurate: unknown namespaces are skipped at [internal/access/policy/compiler.go:154](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/access/policy/compiler.go:154), while unknown keys become errors only after the namespace is registered.

## 3. Concerns

- **HIGH — Plan 02, Tasks 2–3: the blanket `internal/world/postgres/**` codemod exclusion hides three real `world.Service` calls.**  
  The plan calls that directory a repository-call-only false-positive surface at [02.1-02-PLAN.md:85](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md:85). But `cascade_delete_test.go` constructs an actual `world.Service` and calls:
  
  - `svc.DeleteLocation(ctx, "system", ...)` at [internal/world/postgres/cascade_delete_test.go:338](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/postgres/cascade_delete_test.go:338)
  - `svc.DeleteObject(ctx, "system", ...)` at [internal/world/postgres/cascade_delete_test.go:377](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/postgres/cascade_delete_test.go:377)
  - `svc.DeleteCharacter(ctx, "system", ...)` at [internal/world/postgres/cascade_delete_test.go:416](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/postgres/cascade_delete_test.go:416)

  Those calls will not compile after the signature flip. The file is absent from Plan 02’s `files_modified`. Because these calls use a plain background context and an `AllowAllEngine`, they should likely become `world.HumanCaller("system")`, preserving the prior test-engine behavior without creating an S1 context marker. This mapping needs an explicit decision.

- **HIGH — Plan 03, Task 1: the census’s asserted exemption set is incomplete.**  
  The plan says all exported `*Service` methods not referencing `checkAccess` must equal exactly `{UpdateCharacterPreferences}` at [02.1-03-PLAN.md:140](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-03-PLAN.md:140). But `SetMovementHook` is an exported `*Service` method at [internal/world/service.go:160](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/service.go:160), has no `checkAccess`, and is not a public world command. The proposed test therefore fails on the intended final tree.

  The collector must first define the command surface structurally—for example exported `*Service` methods whose first flattened parameter is `context.Context`—then partition that set by `checkAccess`. Alternatively, explicitly exclude lifecycle/configuration methods such as `SetMovementHook` before enforcing the D-05 exemption.

- **HIGH — Plan 01’s second commit is not integration-green under repository rules.**  
  Plan 01 edits four build-tagged integration files but explicitly defers their compilation until Plan 02. The project rule requires `task test:int` for shared-type refactors, and `task test` does not compile integration-tagged code. `task test:int` builds with `-tags=integration` at [Taskfile.yaml:265](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/Taskfile.yaml:265). Therefore “every commit green” is only true under a narrowed unit-only definition, not the repository’s required refactor gate.

  This is especially risky because the tracer changes shared interfaces. Add `task test:int` to Plan 01’s second commit gate, or move all integration-file tracer edits and the signature flip into a commit that is immediately integration-tested.

- **MEDIUM — Plan 02, Task 2: the “no production implementer” command necessarily fails.**  
  The acceptance command at [02.1-02-PLAN.md:455](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md:455) searches for `NewPropertyCapability|NewWorldQueryCapability` in all non-test Go files and expects zero. It necessarily finds their declarations at [internal/plugin/hostfunc/cap_property.go:47](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/hostfunc/cap_property.go:47) and [internal/plugin/hostfunc/cap_world_query.go:55](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/hostfunc/cap_world_query.go:55). Search for call expressions while excluding the defining files, or use structural matching to distinguish declarations from calls.

- **MEDIUM — Plans 01–03: several “returns 0” criteria misuse `rg -c`.**  
  Examples include [02.1-01-PLAN.md:422](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md:422), [02.1-02-PLAN.md:304](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md:304), `:320`, `:587`, and [02.1-03-PLAN.md:208](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-03-PLAN.md:208). On no match, `rg -c` prints nothing and exits 1. These criteria are not literally satisfiable. Use `if rg -n ...; then exit 1; fi`, or pipe `rg ... | wc -l` where pipeline exit behavior is explicitly handled.

- **MEDIUM — Plan 03, Task 2 violates the repository’s task-only command rule.**  
  Its automated verification invokes bare `go run ./cmd/inv-render` at [02.1-03-PLAN.md:307](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-03-PLAN.md:307). A task wrapper already exists as `invariants:render` at [Taskfile.yaml:228](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/Taskfile.yaml:228), with `lint:invariants` providing the check at [Taskfile.yaml:222](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/Taskfile.yaml:222). Use `task invariants:render` followed by `task lint:invariants`.

- **MEDIUM — Plan 03’s renderer check fails precisely when regeneration is required.**  
  `go run ./cmd/inv-render && git diff --exit-code docs/architecture/invariants.md` expects no diff at [02.1-03-PLAN.md:307](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-03-PLAN.md:307). During the task, adding `INV-WORLD-8` should change the rendered Markdown, so this exits nonzero until the generated file is committed or staged against a suitable baseline. The executable freshness check is: render once, record/stage the intended generated change, render again, then assert the second render creates no additional diff.

- **MEDIUM — The byte-identity acceptance check is manual and incomplete.**  
  The persisted value is security/audit relevant at [internal/world/service.go:355](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/service.go:355) and [internal/world/service.go:1079](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/service.go:1079), but Plan 02 relies on visually checking that constructor arguments are character-for-character unchanged. Existing assertions passing cannot detect a caller field accidentally being normalized before `buildIntent`. Add table-driven tests covering representative human, plugin, bootstrap, and system subjects and assert the resulting outbox `Actor` byte-for-byte.

- **LOW — “`task fmt` leaves `git status --porcelain` clean” conflates formatting cleanliness with worktree cleanliness.**  
  This appears at [02.1-01-PLAN.md:439](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md:439), [02.1-02-PLAN.md:339](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md:339), and `:618`. Before the planned commit, the intended changes themselves make the worktree dirty. Capture a pre-format diff/checksum and assert a second `task fmt` produces no additional change, or run `task fmt:check`.

- **LOW — Assertion-diff checks can become vacuous after staging.**  
  `git diff -- '*_test.go'` only inspects unstaged changes. Since the plan explicitly permits staging intermediate work, the criterion at [02.1-02-PLAN.md:587](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md:587) can pass after the relevant diff has been staged. Compare against the pre-flip commit (`git diff <base> -- '*_test.go'`) or include `--cached`.

## 4. Suggestions

1. Add `internal/world/postgres/cascade_delete_test.go` to Plan 02 and hand-migrate its three real service calls. Narrow codemod ignores to known repository-call shapes instead of the entire directory.

2. Define the Plan 03 census universe as “exported `*Service` command methods,” not every exported method. A practical structural discriminator is a first flattened parameter of `context.Context`; then assert all such methods either reference `checkAccess` or equal the named D-05 exemption.

3. Run `task test:int` before committing Plan 01’s tracer signature change. If that is intentionally impossible, combine the tracer interface flip with the full flip rather than knowingly landing an integration-uncompiled commit.

4. Replace every zero-match `rg -c` criterion with a shell condition or `wc -l` check that has defined exit behavior.

5. Replace bare renderer commands with:

   ```sh
   task invariants:render
   task lint:invariants
   ```

6. Change the constructor-wiring check to search only call sites, excluding `cap_property.go` and `cap_world_query.go` declarations.

7. Add an outbox actor-preservation test matrix. At minimum cover:

   - `character:<id>`
   - `plugin:<name>`
   - `system:bootstrap`
   - bare `system` through `SystemCaller`

8. Compare assertion changes and codemod results against the pre-flip commit, not the mutable index/worktree state.

## 5. Source-grounding table

Phase-produced artifacts were excluded as requested.

| Plan reference or symbol | Status | Source evidence |
|---|---|---|
| `Service.checkAccess` and hardcoded `nil` | VERIFIED | [internal/world/service.go:209](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/service.go:209), `NewAccessRequest(..., nil)` at `:214` |
| `NewAccessRequest` fourth argument, validation, clone | VERIFIED | [internal/access/policy/types/types.go:143](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/access/policy/types/types.go:143) |
| S1 exact-equality double gate | VERIFIED | [internal/access/policy/engine.go:92](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/access/policy/engine.go:92), context half at `:93` |
| Attribute overlay into `bags.Action` | VERIFIED | [internal/access/policy/engine.go:258](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/access/policy/engine.go:258) |
| `WithSystemSubject` / `IsSystemContext` | VERIFIED | [internal/access/context.go:12](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/access/context.go:12), `:18` |
| Panic-on-empty access subject constructors | VERIFIED | [internal/access/prefix.go:97](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/access/prefix.go:97), `:107`, `:121`, `:153` |
| 23 subject-carrying `Service` commands | VERIFIED | Begin at [internal/world/service.go:257](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/service.go:257), end at `:1144`; `UpdateCharacterPreferences` at `:856` lacks a subject |
| `SetMovementHook` exported non-command method | VERIFIED; omitted by Plan 03 census design | [internal/world/service.go:160](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/service.go:160) |
| `buildIntent` actor persistence | VERIFIED | [internal/world/service.go:350](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/service.go:350), assignment at `:355` |
| `buildMoveIntent` actor persistence | VERIFIED | [internal/world/service.go:1062](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/service.go:1062), assignment at `:1079` |
| `world.Mutator` | VERIFIED | [internal/world/mutator.go:22](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/mutator.go:22), assertion at `:61` |
| `command.WorldService` | VERIFIED | [internal/command/types.go:38](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/command/types.go:38) |
| `grpc.WorldQuerier` | VERIFIED | [internal/grpc/server.go:129](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/grpc/server.go:129) |
| Deprecated `hostfunc.WorldService` | VERIFIED | [internal/plugin/hostfunc/adapter.go:23](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/hostfunc/adapter.go:23) |
| `hostfunc.WorldMutator` alias | VERIFIED | [internal/plugin/hostfunc/adapter.go:32](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/hostfunc/adapter.go:32) |
| `bootstrap.worldSeeder` | VERIFIED | [internal/bootstrap/setting.go:29](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/bootstrap/setting.go:29) |
| `property.WorldMutator`, `Definition` | VERIFIED | [internal/property/registry.go:49](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/property/registry.go:49), `:55` |
| `property.EntityMutator` | VERIFIED | [internal/property/entity_mutator.go:25](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/property/entity_mutator.go:25) |
| Property three-hop mutation calls | VERIFIED | [internal/property/entity_mutator.go:142](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/property/entity_mutator.go:142), `:162`, `:188`, `:208` |
| `withMutatorContext` | VERIFIED | [internal/plugin/hostfunc/helpers.go:105](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/hostfunc/helpers.go:105), plugin subject at `:121` |
| `location_follow` ambient system context and two calls | VERIFIED | [internal/grpc/location_follow.go:197](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/grpc/location_follow.go:197), calls at `:203` and `:216` |
| Four `"system:bootstrap"` sites | VERIFIED | [internal/bootstrap/setting.go:134](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/bootstrap/setting.go:134), `:272`, `:276`, `:331` |
| Unnamed `recordingWorldMutator.GetLocation` | VERIFIED | [internal/plugin/lua/corebuilding_brokered_command_test.go:52](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/lua/corebuilding_brokered_command_test.go:52) |
| Existing census helpers in `test/meta` | VERIFIED | [test/meta/world_envelope_census_test.go:145](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/test/meta/world_envelope_census_test.go:145), selector walker at `:162`, set comparison at `:187` |
| `internal/world/world_envelope_census_test.go` | MISSING, correctly corrected by Plan 03 | Actual file is `test/meta/world_envelope_census_test.go` |
| `validateAttributes` | VERIFIED | [internal/access/policy/compiler.go:149](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/access/policy/compiler.go:149) |
| Registry `Verifies:` parser | VERIFIED | [test/meta/invariant_registry_test.go:167](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/test/meta/invariant_registry_test.go:167) |
| `task test:int` behavior | VERIFIED | [Taskfile.yaml:265](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/Taskfile.yaml:265); current tree does interpolate `CLI_ARGS` at `:283`, contrary to the supplied “hardcoded/no args” warning |
| Renderer task | VERIFIED | `lint:invariants` at [Taskfile.yaml:222](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/Taskfile.yaml:222), `invariants:render` at `:228` |
| `NewPropertyCapability` / `NewWorldQueryCapability` definitions | VERIFIED; invalidates zero-result criterion | [internal/plugin/hostfunc/cap_property.go:47](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/hostfunc/cap_property.go:47), [internal/plugin/hostfunc/cap_world_query.go:55](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/hostfunc/cap_world_query.go:55) |
| `internal/world/postgres/**` as repository-only false positives | MISSING/INCORRECT | Three actual service calls at [cascade_delete_test.go:338](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/postgres/cascade_delete_test.go:338), `:377`, `:416` |

I did not independently reproduce every pinned numeric ast-grep census because the rule files are phase-produced and do not yet exist. I also did not verify future summary files, `INV-WORLD-8`, or generated output that the phase explicitly creates. All existing source symbols and material line claims used by the plans were checked; minor line drift that still landed within the cited function was treated as verified.

## 6. Risk Assessment

**Overall: HIGH.**

The core design is sound, particularly around the S1 double gate and preserving `"system:bootstrap"` as policy-evaluated. But the current plans would either fail compilation due to the three excluded cascade-delete service calls or fail Plan 03’s census because of `SetMovementHook`. The deferred integration compilation also conflicts with the repository’s required green-per-commit gate. Once those blockers and the unsatisfiable acceptance commands are fixed, the residual implementation risk should fall to **MEDIUM**, driven mainly by the size of the mechanical migration and the lack of automated outbox-actor byte-identity coverage.


---

## pi Review

# Cross-AI Plan Review — Phase 02.1 (World Caller Model)

## 1. Summary

These are unusually well-grounded plans: the S1 double-gate mechanics, the `system:bootstrap` hazard, the grouped-declaration codemod trap, the unnamed-parameter blind spot, and the census-test reuse of `test/meta` machinery all check out against the source, and the key security invariants (exact-equality bypass, byte-identical subject strings, marker confined to a derived context) are correctly understood and correctly mitigated. However, source verification surfaced **three genuine completeness gaps the plans' own enumeration machinery misses**: (a) `internal/plugin/hostcap/property.go:84` is a *production* caller of `property.Definition.Set` that is in no plan's file list or 31-site census; (b) `internal/plugin/objects_integration_test.go:63,71` implements `property.EntityMutator` and is simultaneously declared "out of scope, keeps the identifier"; (c) Plan 03's census exemption set of exactly one member is falsified by `Service.SetMovementHook`, an exported non-authorizing method. All three are compiler-caught at execution time, but they contradict the plans' "mechanically complete" review story. There is also a factual error about the `recordingWorldMutator` double's unnamed-parameter methods, and several acceptance criteria use the `rg -c ... returns 0` shape that can never be literally satisfied.

## 2. Strengths

- **S1 gate mechanics verified exactly as described.** `internal/access/policy/engine.go:92-93` is exact string equality `req.Subject == "system"` AND `access.IsSystemContext(ctx)`, with a hard `SYSTEM_SUBJECT_REJECTED` on bare subject without marker. The plans' central security reasoning (mapping `system:bootstrap` to `SystemCaller` would create four total bypasses; the exact-equality gate is what makes `HumanCaller("system:bootstrap")` safe) is correct. The four `system:bootstrap` sites exist at `internal/bootstrap/setting.go:134,272,276,331` as cited.
- **The derived-context design preserves semantics.** `IsSystemContext` has exactly one non-test consumer in the tree (`engine.go:93`), so confining the marker to a derived ctx passed only to `Engine.Evaluate` changes nothing downstream — repositories and the outbox never read it. Plan 01's "never reassign the incoming ctx" rule is the right shape.
- **The `location_follow.go` criterion-4 migration is fully verified.** `const systemSubjectID = "system"` at `:28`, `sysCtx := access.WithSystemSubject(ctx)` at `:197`, world calls at `:203`/`:216` — all as cited; the plan's warning against `HumanCaller(systemSubjectID)` is correct (it would hard-fail S1).
- **The codemod hazard analysis is real.** The grouped forms exist exactly where cited: `service.go:1095` (`subjectID, name string`), `service.go:1144` (`subjectID, parentType string`), `mutator.go` `FindLocationByName`, `command/types.go` `FindLocationByName`, `bootstrap/setting.go:32`, and both `Definition.Set` implementers (`definition_name.go:34`, `definition_description.go:34`, grouped `subjectID, entityType string`). Excluding comma-bearing declarations is the right call.
- **The unnamed-parameter blind spot is real and correctly identified.** `internal/plugin/lua/corebuilding_brokered_command_test.go:52` declares `GetLocation(context.Context, string, ulid.ULID)` under the `var _ hostfunc.WorldMutator = (*recordingWorldMutator)(nil)` assertion at `:50`, and `hostfunc.WorldMutator` is a type alias (`adapter.go:32`, `type WorldMutator = world.Mutator`), so the double is a full `world.Mutator` implementer and the assertion is a genuine compile tripwire.
- **The `checkAccess` seam and its fail-closed path are as cited.** `service.go:209` signature, hardcoded `nil` as the 4th arg to `types.NewAccessRequest` at `:214`, the defensive comment at `:216-219` (which does become false once `HumanCaller("")` stops panicking — the plan's mandate to rewrite it is correct), and the deny/eval-failure metric split. `NewAccessRequest` rejects empty subject (`types/types.go:144-146`), rejects reserved keys, and normalizes an empty attr map to nil (`:160-172`) — so the plan's "empty map ⇒ byte-identical nil `Attributes`" claim holds.
- **`test:int` non-scopability claim verified.** `Taskfile.yaml:282-283` explicitly comments "No package enumeration" for `test:int`, so Plan 01's decision to gate only on `task test` and defer integration compilation to Plan 02 Task 3 matches reality; `task test` does interpolate `CLI_ARGS` (`Taskfile.yaml:189`).
- **Census machinery exists as cited in `test/meta/`.** `serviceMutatingMethods` returns `(mutating, allMethods)` with `allMethods` discarded by the current caller (`world_envelope_census_test.go:188` `mutating, _ :=`), `serviceReceiverName`, `bodyReferencesSelector`, and the named-exemption idiom (`outOfServiceOnlyKinds` at `:53-60`) all exist, and `findRepoRoot` is at `meta_helpers_test.go:33`. Plan 03's reuse strategy is sound.
- **The out-of-scope façade analysis is correct.** `NewPropertyCapability`/`NewWorldQueryCapability` appear only in `cap_property.go`, `cap_world_query.go`, and their tests — no production implementer; the plans' exclusion is safe and the "re-grep before finishing" step is a good control.
- **13 compile-time assertions verified.** The exact `rg` command in Plan 02 Task 1's acceptance criteria returns 13 on this tree, including the grouped `var ( _ WorldService = ... )` form at `command/types.go:110`.

## 3. Concerns

- **[HIGH] Plan 02 — `internal/plugin/hostcap/property.go:83-84` is an unenumerated production call site of the D-66 chain.** `propertyServer.SetProperty` builds `subjectID := access.PluginSubject(s.pluginName)` (`:83`) and calls `def.Set(ctx, querier, s.host.WorldMutator(), subjectID, ...)` (`:84`). Once Plan 02 Task 1 flips `property.Definition.Set` (`registry.go:58`) to take `world.Caller` at position 4, this site does not compile — yet `hostcap/property.go` is in **no** plan's `files_modified`, not in the 31-site production census, and not in RESEARCH's Q2 table as far as the plans reveal (Task 2 enumerates only setting.go, hostcap/world.go, hostfunc/adapter.go, hostfunc/world_write.go, world/grpc_server.go). The compiler catches it, so the flip cannot silently ship wrong — but the fix will be an improvised `HumanCaller` wrap made under time pressure, outside the byte-identity review checklist, and it falsifies the plan's "every production call site is enumerated and mapped row-by-row" claim. It also means the "31 production call sites in 12 files" figure is wrong (≥32 in 13 files).
- **[MEDIUM] Plan 02 — `internal/plugin/objects_integration_test.go:63,71` is both out-of-scope-listed and migration-required.** `recordingCharacterMutator` implements `property.EntityMutator`: `SetName(context.Context, property.WorldQuerier, property.WorldMutator, string, ulid.ULID, string)` (`:63`, unnamed — invisible to codemod *and* probe) and `SetDescription(_ context.Context, ..., subjectID string, ...)` (`:71`). Plan 02 Task 3's acceptance criteria explicitly name this file among "six out-of-scope files that legitimately keep the identifier" — true for its local variables, false for these two declarations, which must change when `EntityMutator.SetName/SetDescription` flip. The file is absent from `files_modified`. Compile-caught via `Register(characterMutator)` (`:96`), but the plan's scoping statement is wrong and the probe cannot see the `:63` form.
- **[MEDIUM] Plan 03 Task 1 — the one-member exemption set is falsified by `Service.SetMovementHook`.** The behavior spec asserts "the set of exported `*Service` methods NOT in the authorizing set equals exactly {`UpdateCharacterPreferences`}", and `must_haves` repeats "exactly one member". But `service.go:160` declares exported `func (s *Service) SetMovementHook(h MovementHook)` — no ctx, no `checkAccess`. As specified, the census test fails on first run. The fix is trivial (exclude methods with no `context.Context` parameter, or add `SetMovementHook` to the exemption with its own justification), but the plan as written instructs the executor to build a test that cannot pass, and the INV-WORLD-8 summary wording ("every public `world.Service` command that performs an authorization check") needs the same carve-out precision. This is exactly the class of over-claim D-65/D-68 exist to prevent.
- **[MEDIUM] Plan 01 Task 1 — the `recordingWorldMutator` claim is factually wrong in a way that matters for Plan 02.** Plan 01 states only `GetLocation` is unnamed and "its other methods on that double use the `_ string` form and DO match the rule." In reality **six more** methods on that double are unnamed: `GetCharacter` (`:56`), `GetCharactersByLocation` (`:60`), `GetObject` (`:64`), `UpdateLocation` (`:84`), `UpdateObject` (`:88`), `FindLocationByName` (`:92`). Only the three `Create*` methods use `_ string`. Plan 02 never enumerates these six hand-migrations; its safety net is "the compiler surfaces them" (true, via the `:50` alias assertion) — but the probe-post-flip-equals-8 gate cannot see them, and the plan's "the known instance … was hand-migrated in Plan 01" sentence implies the blind spot is closed when it is not.
- **[LOW] Multiple plans — `rg -c ... returns 0` acceptance criteria are not literally satisfiable.** Plan 01 Task 1: "`rg -c 'func \(s \*Service\) (GetLocation|GetExitsByLocation)\(ctx…subjectID string' … returns 0". Plan 02 Task 1: "`rg -c … subjectID( string|,)' … returns 0" and "`rg -c 'HumanCaller\(' internal/property/` returns 0". Plan 02 Task 3: "`git diff -- '*_test.go' | rg -c '^[+-]\s*(assert|require|errutil\.Assert)\.' returns 0". On no-match, `rg -c` prints nothing and exits 1. Plan 02 demonstrably knows this (its own `wc -l` note in the assertion-set criterion), so this is inconsistent application, not ignorance — but an executor judging by exit code will read a *passing* condition as a failure and may "fix" a green tree.
- **[LOW] Plan 01 Task 1 verify block includes packages the plan doesn't touch** (`./internal/bootstrap/ ./internal/property/`). Harmless (they compile unmodified), but it weakens the signal that the verify set is derived from the change set, and it silently passes even if Plan 01 forgot a file that only Plan 02 packages would expose.

## 4. Suggestions

1. **Plan 02 Task 2:** add `internal/plugin/hostcap/property.go:83-84` to `files_modified` and to the row-by-row mapping (maps to `world.HumanCaller(access.PluginSubject(s.pluginName))`, byte-identical). Update the production-site count to 32 and re-run the census command before execution; make "the Q2 table covers every `Definition.Set` caller" an explicit check (`rg -n '\.Set\(ctx' internal/ --glob '!*_test.go'` currently returns exactly two relevant hits: `hostcap/property.go:84` and `hostfunc/world_write.go:319` — only one is planned).
2. **Plan 02 Task 3:** remove `objects_integration_test.go` from the "legitimately keep the identifier" list, add it to `files_modified`, and add a hand-migration note for its two `EntityMutator` methods (one unnamed, one named). Do the same for the six unnamed `recordingWorldMutator` methods in `corebuilding_brokered_command_test.go`.
3. **Plan 03 Task 1:** change the census collector to restrict the exemption set-equality to *commands* — e.g. exported `*Service` methods whose first parameter is `context.Context` — or add `SetMovementHook` as a second named exemption with justification. Mirror whatever predicate is chosen in the INV-WORLD-8 summary text.
4. **All plans:** replace every `rg -c … returns 0` criterion with `… | wc -l` returns 0 (or `! rg -q …`), matching the convention Plan 02 already uses for the assertion-set check.
5. **Plan 01:** correct the `recordingWorldMutator` description (only the three `Create*` methods are `_ string`; six more are unnamed) so Plan 02 doesn't inherit the error.
6. **Plan 02 Task 3 acceptance:** the "no assertion changed" diff check should also exclude added/removed lines matching `t.Run(` names? No — keep as-is; but do fix its `rg -c` per (4).

## 5. Source-grounding table

| Citation (as plans claim) | Verdict | Evidence |
|---|---|---|
| `engine.go:92` exact-equality S1 gate + `IsSystemContext` | VERIFIED | `engine.go:92-93`: `if req.Subject == "system" { if !access.IsSystemContext(ctx) {` |
| `engine.go:252-265` attribute overlay onto `bags.Action`, gated `len>0` | VERIFIED | overlay loop with `if len(req.Attributes) > 0` guard at ~`:258` |
| `service.go:209/214/216-219` checkAccess, hardcoded nil, stale defensive comment | VERIFIED | quoted above |
| 23 command declarations at the 23 cited line numbers | VERIFIED | `rg '^func \(s \*Service\) [A-Z]'` matches all 23 lines incl. grouped `:1095`, `:1144`; `UpdateCharacterPreferences` at `:856` with no-checkAccess D-05 comment |
| `location_follow.go:28,197,203,216` | VERIFIED | quoted above |
| `bootstrap/setting.go:30-34` worldSeeder (3 methods, grouped FindLocationByName); sites `:134,272,276,331` | VERIFIED | quoted above |
| `mutator.go` 10 subject methods, doc comment `:20-21`, assertion `:60-61` | VERIFIED | quoted above |
| `command/types.go` WorldService 15 methods, grouped `:73`, grouped-var assertion `:110` | VERIFIED | quoted above |
| `grpc/server.go` WorldQuerier = exactly the tracer's 2 methods | VERIFIED | `server.go:128-132` |
| `hostfunc/adapter.go` Deprecated WorldService (4 methods), `WorldMutator` alias `:32`, `SubjectID()` `:67-69`, sites `:75,99,123,146` | VERIFIED | quoted above |
| `hostfunc/helpers.go:104-121` withMutatorContext prose + callback + PluginSubject construction | VERIFIED | quoted above |
| `hostfunc/world_write.go` 4 callback sites + inline-subject findLocationFn + `setEntityProperty` `:315-320` | VERIFIED | quoted above |
| property chain: `registry.go:50-51,58`, `entity_mutator.go:28,30,142,162,188,208`, grouped `definition_*.go:34` | VERIFIED | quoted above |
| `access/context.go:10-18` WithSystemSubject/IsSystemContext; single non-test consumer | VERIFIED | full file read; `rg` shows only `engine.go:93` consumes it |
| `access/prefix.go:95-168` four panic-on-empty constructors | VERIFIED | PluginSubject/CharacterSubject/PlayerSubject/ViewerSubject all panic |
| `types/types.go:143-172` NewAccessRequest: 4th param, empty-subject rejection, empty-map→nil | VERIFIED | quoted above |
| `compiler.go:149-171` validateAttributes skips unregistered `action` namespace | VERIFIED | `if !c.schema.HasNamespace(ref.namespace) { continue }` before the action check |
| `corebuilding_brokered_command_test.go:50,52` alias assertion + unnamed GetLocation | VERIFIED — but plan's "only unnamed method" claim is FALSE | six further unnamed methods at `:56,60,64,84,88,92` |
| `test/meta/world_envelope_census_test.go` machinery (`outOfServiceOnlyKinds`, `serviceMutatingMethods`, `serviceReceiverName`, `bodyReferencesSelector`) | VERIFIED | quoted above |
| `meta_helpers_test.go:33` findRepoRoot | VERIFIED | present |
| `seed_smoke_test.go:958` `// Verifies:` precedent | VERIFIED | `:958` `// Verifies: INV-PLUGIN-50` |
| INV-WORLD-1..7 exist; next free id is 8; scope block ~`:778-800`; INV-WORLD-4 bound-form at `:5057` | VERIFIED | `rg 'INV-WORLD-[0-9]'` ids at `:5025…:5108`; scope block read |
| 13 compile-time assertions via the plan's exact `rg` command | VERIFIED | returns 13 |
| `Taskfile.yaml` `test:int` hardcodes packages; `task test` interpolates `--` | VERIFIED | `:282` "No package enumeration"; `:189` `{{.CLI_ARGS}}` |
| `TestWorldService_MalformedAccessParams` at `service_test.go:6079` | VERIFIED | function at `:6079` |
| `auth_handlers.go:~972` `subj := access.CharacterSubject(...)` + GetLocation | VERIFIED | at `:971-972` |
| cap façades: interfaces at cited lines; no production implementer | VERIFIED | `rg -l NewPropertyCapability\|NewWorldQueryCapability` = 4 cap files only |
| `hostcap/world.go` 4 sites + `:143` false positive (id, not subject) | VERIFIED | sites with `subject := access.PluginSubject` locals; `:143`-region call passes `id` |
| `hostcap/property.go:84` production `Definition.Set` caller | **MISSING from plans** | not in any files_modified or census |
| `objects_integration_test.go:63,71` EntityMutator double | **Plan statement wrong** | listed as out-of-scope identifier-keeper; its declarations must migrate |
| Exported non-authorizing `*Service` methods = only UpdateCharacterPreferences | **FALSE** | `SetMovementHook` at `service.go:160` |
| Probe counts (185 pre-flip, 8 post-flip, 21 grouped, 46 call sites, 23 declarations, ~332 test sites) | UNCHECKABLE | probe rules don't exist yet (phase-produced); I spot-checked shapes but did not recount the tree-wide totals |
| RESEARCH.md per-site table (27 rows), PATTERNS.md, VALIDATION.md | NOT CHECKED | reviewed only via the plans' quotations |

## 6. Risk Assessment

**MEDIUM.** The architecture and authorization reasoning are sound and verified — the two ways this phase could silently create an ABAC bypass (`system:bootstrap` → `SystemCaller`; marker leakage into repo/outbox ctx) are both correctly understood and explicitly fenced, and the byte-identity discipline is right. What keeps this from LOW is a cluster of enumeration incompleteness in exactly the machinery the plans stake their review story on: one missed production call site (`hostcap/property.go:84`), one mis-scoped test double file, six mis-described unnamed-parameter methods, and a census exemption set that fails as written. Every one is compiler-caught at execution time, so the probability of a *silent* wrong merge is low — but the phase's stated value proposition is "the bulk diff is mechanically verifiable and completely enumerated," and the enumeration currently isn't. Fixes are small, localized plan edits (Section 4); none require re-design.


---

## Consensus Summary

Both lanes ran **prompt-fed and source-grounded** (each was given the repo root and read it);
neither carries the `[reviewed-without-repo-access]` marker, so both verdicts count at full
consensus weight. Neither is a diff-only reviewer.

Both reviewers independently reached the same top-level judgment: **the authorization design is
correct and well-grounded, but the plans' enumeration — the very thing their review story is staked
on — is incomplete.** Every gap found is compiler-caught at execution time, so the probability of a
*silent* wrong merge is low; the cost is that an executor hits unplanned compile failures at the
phase's most expensive gate and improvises fixes outside the byte-identity review checklist.

Risk assessment: Codex **HIGH**, pi **MEDIUM**. The delta is scope, not substance — Codex weights
"the plan as written cannot execute" as blocking; pi weights "the compiler catches all of it" as
mitigating. Both agree the fixes are small, localized plan edits requiring no re-design.

### Agreed Strengths

- **The S1 double-gate reasoning is correct and verified by both.** `internal/access/policy/engine.go:92-93`
  is exact string equality `req.Subject == "system"` **AND** `access.IsSystemContext(ctx)`, with a
  hard `SYSTEM_SUBJECT_REJECTED` for a bare subject without the marker. Both reviewers confirmed the
  plans' central security claim: because the comparison is exact equality and not a prefix match,
  `HumanCaller("system:bootstrap")` stays normally policy-evaluated, and mapping those four sites to
  `SystemCaller()` would create four total ABAC bypasses. Both verified all four sites at
  `internal/bootstrap/setting.go:134,272,276,331`.
- **The derived-context design preserves downstream semantics.** pi independently established that
  `access.IsSystemContext` has exactly one non-test consumer in the tree (`engine.go:93`), so
  confining the system marker to a derived context passed only to `Engine.Evaluate` changes nothing
  reaching repositories or the outbox. Plan 01's "never reassign the incoming `ctx`" rule is the
  right shape, and `TestSystemCallerStampsTheSystemMarkerOnlyOnTheDerivedContext` is the right proof.
- **The attribute-forwarding seam matches the real substrate.** `types.NewAccessRequest` takes the
  attribute channel as its 4th parameter, rejects an empty subject, rejects reserved keys, and
  normalizes an empty map to a nil `Attributes` field (`internal/access/policy/types/types.go:143-172`);
  the engine overlays it into `bags.Action` at `internal/access/policy/engine.go:258`. Both confirmed
  the plans' "empty map ⇒ byte-identical `nil`" claim holds, and both endorsed the paired permit/deny
  proof shape as the correct discriminator.
- **Byte-identity of the subject string is correctly treated as audit-relevant.** Both verified
  `buildIntent` writes the actor verbatim (`internal/world/service.go:355`) and `buildMoveIntent`
  likewise (`:1079`), confirming the plans' insistence that the constructor receive the already-built
  subject expression without re-deriving or re-prefixing.
- **The interface cascade is real and broader than the stale ROADMAP.** Both independently confirmed
  all six redeclaring interfaces exist, including the previously-unnamed
  `bootstrap.worldSeeder` (`internal/bootstrap/setting.go:29`) and `property.WorldMutator`
  (`internal/property/registry.go:49`). pi independently re-ran Plan 02's own assertion-count command
  and got **13**, confirming that pinned number.
- **The codemod's grouped-declaration hazard is real and correctly excluded by construction.** pi
  verified the grouped forms exist exactly where cited — `service.go:1095`, `service.go:1144`,
  `mutator.go` `FindLocationByName`, `command/types.go`, `bootstrap/setting.go:32`, and both
  `Definition.Set` implementers at `definition_name.go:34` / `definition_description.go:34`.
- **The unnamed-parameter blind spot is genuine.** Both confirmed
  `internal/plugin/lua/corebuilding_brokered_command_test.go:52` declares `GetLocation` with unnamed
  parameters under the alias assertion at `:50`, making the double a full `world.Mutator` implementer
  and a real compile tripwire.
- **D-66 (threading the caller through `internal/property`) is well grounded** — both verified the
  four `mutator.UpdateLocation`/`UpdateObject` sites at `entity_mutator.go:142,162,188,208`.
- **Plan 03's reuse of the existing `test/meta` census machinery is sound.** pi confirmed
  `serviceMutatingMethods` really does return `(mutating, allMethods)` with `allMethods` discarded by
  the current caller (`world_envelope_census_test.go:188` `mutating, _ :=`), so the "reuse, do not
  duplicate" instruction is executable. Both confirmed Plan 03's correction of CONTEXT.md D-65's
  wrong path.

### Agreed Concerns

**1. [HIGH — both reviewers, independently] Plan 03 Task 1's census exemption set of exactly one
member is falsified by `Service.SetMovementHook`.**
`internal/world/service.go:160` declares exported `func (s *Service) SetMovementHook(h MovementHook)`
— no `context.Context`, no `checkAccess`. Plan 03's `<behavior>` asserts set equality between
"exported `*Service` methods NOT in the authorizing set" and `{UpdateCharacterPreferences}`, and
`must_haves` repeats "exactly one member". **The test fails on its first run against the intended
final tree.** Worse, Plan 03's second assertion ("no `*Service` method declares a `string` type in
its second parameter slot") indexes a flattened parameter position 1 that `SetMovementHook` does not
have — a one-parameter method — so the described algorithm also needs an arity guard. This is
precisely the over-claim class D-65/D-68 exist to prevent. Fix: restrict the census universe to
*commands* (e.g. exported `*Service` methods whose first flattened parameter is `context.Context`),
or add `SetMovementHook` as a second named exemption with its own justification — and mirror
whichever predicate is chosen in the INV-WORLD-8 summary text.

**2. [HIGH — Codex; corroborated by this session] Plan 02's blanket `internal/world/postgres/**`
codemod exclusion hides three real `world.Service` calls.**
`internal/world/postgres/cascade_delete_test.go` constructs a genuine `world.NewService(...)` and
calls `svc.DeleteLocation(ctx, "system", locID)` at `:338`, `svc.DeleteObject(ctx, "system", objID)`
at `:377`, and `svc.DeleteCharacter(ctx, "system", charID)` at `:416`. Plan 02 characterizes that
whole directory as repository-call-only false positives (prohibition at `02.1-02-PLAN.md:85`), and
the file appears in **no** plan's `files_modified`. Two compounding factors:
- The file carries `//go:build integration` (`:4`), so the break surfaces only at `task test:int` —
  Plan 02 Task 3's final gate, the most expensive possible place to discover it.
- These three sites pass the **bare literal `"system"`** — the exact string the S1 gate compares.
  This is the same "one character away from a total bypass" judgment call the plans correctly fence
  for `system:bootstrap`, but here it has no row in the Q2 mapping table and no plan guidance. An
  executor who reaches for `SystemCaller()` creates three test-visible ABAC bypasses; the
  behavior-preserving mapping under the file's `policytest.AllowAllEngine()` is
  `world.HumanCaller("system")`. **This mapping needs an explicit decision in the plan, not an
  improvised one at the gate.**

**3. [HIGH — pi; distinct site] `internal/plugin/hostcap/property.go:83-84` is an unenumerated
production call site of the D-66 chain.**
`propertyServer.SetProperty` builds `subjectID := access.PluginSubject(s.pluginName)` at `:83` and
calls `def.Set(ctx, querier, s.host.WorldMutator(), subjectID, ...)` at `:84`. Once Plan 02 Task 1
flips `property.Definition.Set` (`registry.go:58`) to take `world.Caller` at position 4, this site
does not compile — yet `hostcap/property.go` is in no plan's `files_modified` and not in the 31-site
production census. Consequence: the pinned "**31** production call sites in 12 files" figure is
wrong (≥32 in 13 files), and the plan's "every production call site is enumerated and mapped row by
row" claim — the review story for a security-relevant migration — does not hold.

**4. [MEDIUM — both] Acceptance criteria phrased "`rg -c …` returns 0" are not literally
satisfiable.** `rg -c` prints nothing and **exits 1** on no match. Codex cites
`02.1-01-PLAN.md:422`, `02.1-02-PLAN.md:304`, `:320`, `:587`, `02.1-03-PLAN.md:208`; pi cites the
same family. Both note the inconsistency is self-evident *within Plan 02*, which correctly explains
the hazard for its assertion-set criterion ("Pipe through `wc -l`, not `rg -c`") and then reuses the
broken shape elsewhere. An executor judging by exit code reads a **passing** condition as a failure
and may "fix" a green tree. Fix: `… | wc -l` returns 0, or `! rg -q …`.

**5. [MEDIUM — Codex] Plan 02 Task 2's "no production implementer" criterion necessarily fails.**
`rg -n 'NewPropertyCapability|NewWorldQueryCapability' --glob '*.go' --glob '!*_test.go'` expecting 0
lines necessarily matches the **declarations** at `internal/plugin/hostfunc/cap_property.go:47` and
`cap_world_query.go:55`. The intent (no *caller*) is right; the command tests the wrong thing. Fix:
search call expressions while excluding the defining files, or use structural matching. (pi reached
the same conclusion about the underlying fact — no production implementer — by a different command,
which is why it recorded the fact as VERIFIED while Codex flagged the *criterion*. Both are right.)

**6. [MEDIUM — Codex] Plan 03's renderer check fails precisely when regeneration is required, and
bypasses an existing task wrapper.** `go run ./cmd/inv-render && git diff --exit-code
docs/architecture/invariants.md` (`02.1-03-PLAN.md:307`) expects no diff — but adding `INV-WORLD-8`
*should* change the rendered Markdown, so it exits non-zero until the generated file is committed or
staged. Separately, the bare `go run` violates the repo's task-only rule when wrappers already
exist: `task invariants:render` (`Taskfile.yaml:228`) and `task lint:invariants`
(`Taskfile.yaml:222`, which runs `go run ./cmd/inv-render -check`). Fix: `task invariants:render`
then `task lint:invariants`.

**7. [MEDIUM — Codex] The byte-identity guarantee has no automated detector.** The outbox `Actor` is
audit-bearing (`internal/world/service.go:355`, `:1079`) and `wmodel` performs no validation, yet
Plan 02's only control is a human reading `git diff` to confirm each constructor argument is
"character-for-character" unchanged. The existing suites passing cannot detect a caller subject
being normalized before `buildIntent`. Fix: add a table-driven test asserting the resulting outbox
`Actor` byte-for-byte across representative `character:<id>`, `plugin:<name>`, `system:bootstrap`,
and bare-`system` subjects. This is the phase's stated highest-severity threat (T-02.1-06) with the
weakest verification.

**8. [MEDIUM — pi] Plan 01 misdescribes the `recordingWorldMutator` double, and Plan 02 inherits the
error.** Plan 01 states only `GetLocation` is unnamed and "its other methods on that double use the
`_ string` form and DO match the rule." pi found **six more** unnamed methods on that double —
`GetCharacter` `:56`, `GetCharactersByLocation` `:60`, `GetObject` `:64`, `UpdateLocation` `:84`,
`UpdateObject` `:88`, `FindLocationByName` `:92` — with only the three `Create*` methods using
`_ string`. Plan 02 never enumerates those six hand-migrations and its safety net is "the compiler
surfaces them"; meanwhile the post-flip `probe-subject-param.yml == 8` gate **cannot see any of
them** (the probe's documented blind spot), so the plan's sentence implying the blind spot is closed
by Plan 01's single hand-migration is false.

**9. [MEDIUM — pi] Plan 02 Task 3 simultaneously lists `internal/plugin/objects_integration_test.go`
as out-of-scope and requires its migration.** `recordingCharacterMutator` implements
`property.EntityMutator`: `SetName(context.Context, property.WorldQuerier, property.WorldMutator,
string, ulid.ULID, string)` at `:63` (unnamed — invisible to both the codemod **and** the probe) and
`SetDescription(_ context.Context, …, subjectID string, …)` at `:71`. Plan 02's acceptance criteria
name this file among "six out-of-scope files that legitimately keep the identifier" — true for its
local variables, false for these two declarations, which must change when `EntityMutator` flips. The
file is absent from `files_modified`.

**10. [LOW — Codex] Worktree-cleanliness criteria conflate two properties.** "`task fmt` leaves
`git status --porcelain` clean" (`02.1-01-PLAN.md:439`, `02.1-02-PLAN.md:339`, `:618`) cannot hold
before the planned commit — the intended changes themselves make the worktree dirty. Use
`task fmt:check`, or assert a *second* `task fmt` produces no additional change.

**11. [LOW — Codex] Assertion-diff checks go vacuous after staging.** `git diff -- '*_test.go'`
(`02.1-02-PLAN.md:587`) inspects only unstaged changes, and Plan 02 explicitly permits staging
intermediate work ("If an intermediate state must be saved, stage without committing"). The
criterion silently passes once the diff is staged. Fix: compare against the pre-flip commit
(`git diff <base> -- '*_test.go'`) or add `--cached`.

### Divergent Views

**`task test:int` scopability — the reviewers directly contradict each other. Codex is correct.**

- **Codex:** `task test:int` *does* interpolate `CLI_ARGS`, so Plan 01's justification for deferring
  integration compilation is unfounded.
- **pi:** cited the `# No package enumeration` comment at `Taskfile.yaml:282` and marked the plan's
  non-scopability claim VERIFIED.

**Adjudicated in this session by direct read.** `Taskfile.yaml:289` is:

```
-coverpkg=./... {{.CLI_ARGS | default "./..."}}"
```

`test:int` **does** interpolate `{{.CLI_ARGS}}`. The `# No package enumeration` comment pi relied on
explains why the Taskfile no longer needs an explicit package *list* (it uses `./...`) — it does
**not** say `CLI_ARGS` is ignored. pi misread the comment as the mechanism.

Two consequences follow:

- **Plan 01's `<verification>` block contains a factually false justification.** It states
  `task test:int` "hard-codes its package list in `Taskfile.yaml` and silently ignores a
  `-- ./pkg/...` argument", and uses that to justify not gating the tracer on the integration tier.
  The premise is false on this tree: `task test:int -- ./test/integration/resilience/...
  ./test/integration/access/...` would compile exactly the four integration files Plan 01 edits. The
  narrowing may still be *defensible* on cost grounds, but it must be re-argued on true premises —
  and Codex's independent point stands that the repo's own rule ("MUST run `task test:int` on
  refactors — `task test` does NOT compile integration files, so refactors of shared types break
  silently") applies squarely to a tracer that changes shared interfaces.
- **The false premise is inherited from stale repo documentation**, not invented by the planner:
  `.claude/rules/references/plan-review-learnings.md:574-575` still asserts "`task test:int` does NOT
  accept `--` package args — its package list is hard-coded in `Taskfile.yaml:93-111`". That repo doc
  is stale and should be corrected separately; it will keep re-seeding this error into future plans.

**Risk-level divergence (Codex HIGH vs pi MEDIUM)** is a weighting difference, not a factual one —
see the note at the top of this summary. Both agree on every underlying fact and on the remedy set.

---

## Additional Findings — Reviewing Session

Raised by this session's own source-grounding pass, not by either external lane. Everything below
was **empirically demonstrated on this tree** (`ast-grep 0.45.1`, `ripgrep 15.2.0`) rather than
argued from reading.

### [HIGH] AF-1 — Plan 01 Task 2's `ignores:` list and Plan 02 Task 2's action directly contradict each other on `internal/plugin/hostcap/world.go`

Plan 01 Task 2 instructs: *"Add `ignores:` scoping to every rule for the four verified false-positive
surfaces (… `internal/plugin/hostcap/world.go`'s subject-free wrapper)"*. But that file has **five**
arg-2 matches under the 23-name rule, and only `:143` is subject-free:

```
internal/plugin/hostcap/world.go:227:  loc, err := mutator.FindLocationByName(ctx, subject, req.GetName())
internal/plugin/hostcap/world.go:288:  if err := mutator.CreateLocation(ctx, subject, loc); err != nil {
internal/plugin/hostcap/world.go:330:  if err := mutator.CreateExit(ctx, subject, exit); err != nil {
internal/plugin/hostcap/world.go:385:  if err := mutator.CreateObject(ctx, subject, obj); err != nil {
internal/plugin/hostcap/world.go:143:  chars, err := querier.GetCharactersByLocation(ctx, id, opts)   ← the only false positive
```

**`ast-grep`'s `ignores:` is path-glob scoped, not node scoped** — "the subject-free wrapper" is not
expressible as an ignore. Ignoring the file suppresses four genuine migration targets. And Plan 02
Task 2 explicitly requires exactly those four to be wrapped ("`internal/plugin/hostcap/world.go` at
`:227`, `:288`, `:330`, `:385` — wrap the existing `subject` local in `world.HumanCaller`"), with an
acceptance criterion asserting `rg -c 'HumanCaller\(' internal/plugin/hostcap/world.go` returns 4.
The two plans cannot both be executed as written.

### [HIGH] AF-2 — Plan 01 Task 2's `ignores:` list contradicts Plan 01 Task 2's *own* acceptance criteria, twice

- The action ignores `internal/property/**` on **every** rule; criterion: *"A dry run of
  `world-caller-decl.yml` against `internal/property/entity_mutator_test.go` rewrites the two
  `fakeVersionMutator` methods and leaves every `fakeVersionQuerier` method untouched."* Unsatisfiable
  under the ignore.
- The action ignores `internal/world/mutator.go` on **every** rule; criterion: *"A dry run of
  `world-caller-decl.yml` against `internal/world/mutator.go` produces no hunk that removes a
  parameter name."* Also, `internal/world/mutator.go:22-58` is one of the four interfaces Task 1
  **must** migrate (`GetLocation` at `:26`). The verified false positive in that file is a *call*
  (`:231`), not a declaration — so the ignore is over-broad for the two declaration rules and should
  be scoped per-rule, not applied to "every rule".

### [HIGH] AF-3 — The prescribed call-argument pattern matches mockery `EXPECT()` expectations, in files that cannot be excluded by path

Plan 01 Task 2 prescribes `pattern: $RECV.$METHOD($CTX, $SUBJ, $$$REST)` with `METHOD` constrained to
the 23 command names. `$RECV` is a single-node metavariable and `mockCharRepo.EXPECT()` **is** a
single node (`call_expression`), so the pattern matches mock expectations. Demonstrated:

```
$ ast-grep scan -r <plan's pattern, METHOD=UpdateLocation> internal/world/movement_hook_test.go
help[probe-arg2]:
   ┌─ internal/world/movement_hook_test.go:83:2
83 │  fix.charRepo.EXPECT().UpdateLocation(context.Background(), charID, &toLocID, mock.Anything)...
   │  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
```

Five such sites, all on the repository-shaped `UpdateLocation(ctx, charID, &locID, version)`:
`internal/world/service_test.go:4016`, `:4054`, `:4208`, `:5065`, and
`internal/world/movement_hook_test.go:83`. The rule rewrites `charID` (a `ulid.ULID`) to
`world.HumanCaller(charID)`.

It fails **loudly** (type error) — good. The damage is to **D-63's review contract**: Plan 02 Task 3
and Plan 03 Task 3 both gate on re-running all four rewrite rules with `-U` on the clean post-flip
tree and asserting `git diff --exit-code` is clean. After the executor hand-reverts these five sites,
**the re-run re-applies the wrong rewrite and the contract fails** — and that contract is the entire
mechanism by which a reviewer is meant to trust ~500 unread hunks. `ignores:` cannot fix it at file
granularity: both files also hold in-scope `svc.UpdateLocation(ctx, subjectID, loc)` calls that
**must** be rewritten (`service_test.go:354`, `:376`, `:5530`, …). The rule needs a negative
constraint (e.g. exclude a `$RECV` whose text ends in `.EXPECT()`).

### [HIGH] AF-4 — The two rule variants are separated by *package declaration*, but `ignores:` is a *path glob*, and `internal/world/` mixes both packages

Plan 01 Task 2 says only *"Scope the two variants against each other so no file is matched by both"*,
deferring the mechanism. It cannot be done with path globs, because `internal/world/` holds both:

- `package world` (22 files) — `service.go`, `mutator.go`, `grpc_server.go`, `mutator_test.go`,
  `service_helpers_test.go`, …
- `package world_test` (11 files) — `service_test.go`, `movement_hook_test.go`,
  `grpc_server_test.go`, …

No path glob separates them. Emitting `world.HumanCaller` inside `package world` produces the
doubled-qualifier failure the plan itself warns about; emitting the unqualified form outside
`package world` does not compile. Plan 01's `<action>` names `service_helpers_test.go` and
`mutator_test.go` as unqualified-spelling files and `service_test.go` as `world_test` — so it is
aware of the split without specifying a mechanism that can express it.

**The mechanism exists** — verified working on 0.45.1, so this is a specification gap, not an
impossibility:

```yaml
rule:
  pattern: $RECV.$METHOD($CTX, $SUBJ, $$$REST)
  inside:
    kind: source_file
    stopBy: end
    has:
      kind: package_clause
      regex: '^package world$'
```

So scoped, the rule matched only `internal/world/grpc_server.go` and `internal/world/mutator.go` and
correctly skipped every `package world_test` file. Plan 01 Task 2 should pin this construct.

### [HIGH] AF-5 — Plan 03 Task 2's two acceptance criteria are mutually exclusive, and its verify block fails by construction

- Criterion: *"`go run ./cmd/inv-render` followed by `git diff --exit-code
  docs/architecture/invariants.md` exits 0"* — asserts an **empty** diff.
- Criterion: *"`git diff docs/architecture/invariants.md` contains no change outside a BEGIN
  GENERATED / END GENERATED region"* — presupposes a **non-empty** diff.

Both cannot hold. And the `<automated>` block `go run ./cmd/inv-render && git diff --exit-code
docs/architecture/invariants.md` fails by construction: Task 2 edits `invariants.yaml`, so the
renderer **writes** the new INV-WORLD-8 row into `invariants.md`, and `git diff --exit-code` then
compares the working tree to HEAD and exits 1 on the uncommitted change. The plan never says to
commit between render and diff.

The repo's own idiom is the `-check` flag — `cmd/inv-render` supports *"verify the markdown is up to
date **without writing** (exit 1 on drift)"* — already wrapped as `task lint:invariants`
(`Taskfile.yaml:222-225`, `go run ./cmd/inv-render -check`) and wired into `task lint`. Correct
sequence: `task invariants:render`, then `task lint:invariants`.

### [MEDIUM] AF-6 — `access.ViewerSubject` does **not** panic on an empty subject; the plan mandates writing that claim into a doc comment the `abac-reviewer` will read

Plan 01 instructs the `HumanCaller` doc comment to *"name the four `access.PluginSubject` /
`CharacterSubject` / `PlayerSubject` / `ViewerSubject` panic-on-empty constructors as the repo
convention"*. Three do (`internal/access/prefix.go:97-102`, `:107-112`, `:121-126`). `ViewerSubject`
(`:153-168`) is the opposite for one tier:

```go
case ViewerTierAnonymous:
    if playerID != "" {
        panic("access.ViewerSubject: anonymous rung takes no playerID — caller is on the wrong rung")
    }
    return SubjectViewer + tier
```

An **empty** `playerID` is the *correct* call for the anonymous rung; it panics on a **non-empty**
one. So the convention is 3-for-4, not 4-for-4, and the deviation `HumanCaller` documents is smaller
than claimed. Writing "four panic-on-empty constructors" into a comment that a domain reviewer reads
as ground truth is a durable inaccuracy.

### [MEDIUM] AF-7 — Plan 03's second census assertion is unscoped and will flag unexported helpers

Plan 03 `<behavior>`: *"no `*Service` method declares a `string` type in its second parameter slot"* —
stated over *all* `*Service` methods, not exported or authorizing ones. Two unexported methods have a
`string` second parameter today: `checkAccess(ctx context.Context, subject, action, resource string,
prefix entityPrefix)` (`internal/world/service.go:209`) and `buildMoveIntent`, whose first parameter
is `subjectID string`. Plan 01 flips the `checkAccess` seam, but nothing in any plan says
`buildMoveIntent` flips — indeed Plan 02 explicitly keeps it taking a plain `string` ("Both helpers
take a plain `string` parameter, so a method-value mistake is a compile error"). As literally
written, the assertion flags a construct the plans deliberately preserve.

### [MEDIUM] AF-8 — `serviceMutatingMethods`' `allMethods` return is **not** dead; Plan 03 (and the pi lane) describe it as discarded

Plan 03: *"already builds the `allMethods` set that today's caller discards with a blank identifier"*.
It has **two** call sites in `test/meta/world_envelope_census_test.go`:

- `:188` `mutating, _ := serviceMutatingMethods(t)` — discards it (as described)
- `:221` `_, allMethods := serviceMutatingMethods(t)` — **consumes** it, in
  `TestWorldEnvelopeCensusHasNoSceneParticipantSurface`

Reusing it is still correct; but the framing "the return value is dead, just pick it up" understates
the coupling — a change to what `allMethods` contains now affects an existing green test.

### [MEDIUM] AF-9 — Plan 03's `rg -n -A2 'name: INV-WORLD$'` criterion cannot show what it claims, and produces a **false positive** by matched-string

The criterion is: *"shows a `description` that names the command-API caller argument, and the
`boundary` names AUTHZ-01 / 02.1"*. The pattern matches (`docs/architecture/invariants.yaml:780`), but
`-A2` yields only `:780-782` — all `description`. The `boundary:` key begins at `:783` and its scalar
runs to `:789`, so `-A10` is the minimum. Worse: the `-A2` output **already contains the word
"boundary"** as the trailing word of the description string ("…and the raw-world-write boundary."), so
a naive confirmation grep returns a false positive. That is precisely the "judge by matched output
string" antipattern `.claude/rules/search-tools.md` forbids.

### [MEDIUM] AF-10 — Plan 03 Task 3's Docker precondition misstates what `task pr-prep` is

Precondition: *"Docker is running and available to testcontainers … `task test:int` **and `task
pr-prep`'s integration surface** depend on it."* `Taskfile.yaml` describes `pr-prep` as the *"Fast
pre-push gate (schema/license/lint/fmt/unit/build/bats; **no Docker, no concurrency lock**)"*, with
`pr-prep:full` as the opt-in int+e2e lane. The fast lane has no integration surface. Docker is
genuinely required for `task test:int` only. (Everything else the plan says about reading the
`pr-prep` result — exit code first, authoritative verdict in the `▸ pr-prep result:` file — is
correct.)

### [MEDIUM] AF-11 — Two pinned figures in Plan 01 do not reproduce

| Figure | Plan | Measured |
|---|---|---|
| "43 build-tagged call sites the tracer does not touch" | 43 | 57 integration-tagged arg-2 matches under the 23-name rule; `57 − 15 = 42`. Excluding the 9 `internal/world/postgres/` matches: 33. **Neither reading yields 43.** |
| "~349 call sites … Plan 02 rewrites" | ~349 | 375 total arg-2 matches; `375 − 46 = 329` remainder. The sibling figure on the same line ("~162 declaration sites") *does* reproduce exactly (`185 − 23 = 162`), so the two were not derived the same way. |

Related artifact drift, worth a single reconciling pass: `02.1-CONTEXT.md` D-63 says "347-site",
`02.1-CONTEXT.md:193` says "~400-site", Plan 01 says "~349", Plan 02 says "~332 true test call
sites". Four figures for one quantity.

### [LOW] AF-12 — "the nine **empty-subject** subtests" is 8, not 9

`TestWorldService_MalformedAccessParams` (`internal/world/service_test.go:6079`) has nine subtests,
but the ninth is `"GetCharactersByLocation - valid subject but tests access check boundary"` — a
valid-subject case. The count "nine subtests" is right; "nine **empty-subject** subtests" (Plan 01
`<read_first>`, `<behavior>`, `must_haves`) is wrong by one. Immaterial to execution, but it is the
kind of claim the invariant summary will inherit.

### [LOW] AF-13 — `scripts/gen-event-docs.sh` does not have the shape the criterion cites it for

Criterion: *"Each rule file's first two lines are the SPDX Apache-2.0 identifier and the HoloMUSH
copyright line, matching `scripts/gen-event-docs.sh`."* That file's `:1` is `#!/usr/bin/env bash`;
SPDX is `:2`, copyright `:3`. The requirement is fine for a YAML file with no shebang — the
*justification by analogy* is what is wrong.

### [LOW] AF-14 — `D-05` is an ambiguous token repo-wide

Plans 01/03 cite "D-05" for the `UpdateCharacterPreferences` rationale, correctly grounded in
`internal/world/service.go:840` and `:854`. But `.planning/phases/01-portal-spec/01-CONTEXT.md:57`
defines a *different* D-05, and no `**D-05:**` definition exists in the 02.1 artifacts. A reader
chasing the bare token lands on the wrong decision; the invariant summary Plan 03 mandates will carry
it. Cite it as "D-05 (per `internal/world/service.go:854`)".

### [LOW] AF-15 — Plan 01 lists two files with zero tracer sites

`internal/world/service_helpers_test.go` and `internal/world/mutator_test.go` appear in Plan 01's
"files declaring `package world` … take the unqualified spelling" list, but `rg` for
`GetLocation|GetExitsByLocation` returns nothing in either; neither is in the 46-site or
16-file inventories, nor in `files_modified`. Their real relevance (correctly cited elsewhere in the
plan) is only their `package world` header, which is where the criterion-2 proof can live.

### [INFO] AF-16 — Verified non-issues, recorded so they are not re-raised

- **`ast-grep scan` on 0.45.1 does emit the `┌─ ` framing** the pinned-count commands parse
  (`… | rg -c '┌─ '`), and the `rg -o '┌─ ([^:]+:[0-9]+)' -r '$1'` pipeline runs correctly. The
  census machinery is sound as written.
- **`constraints:` as a top-level sibling of `rule:` loads and runs on 0.45.1** — the plan's schema
  warning is correct and was confirmed against all five reconstructed rules.
- **No inter-task ordering hazard from the `// Verifies:` annotation landing before the registry
  entry.** `checkRegistryBindings` (`test/meta/invariant_registry_test.go:175-226`) iterates
  `reg.Invariants` only — it never walks the bindings map keys — and the orphan pass (`:340-372`)
  scans `specs/`, not test files. Plan 03 Task 1's gates therefore pass before Task 2 registers the
  id. Confirmed safe.
- **`go run ./cmd/inv-render` is not a CLAUDE.md violation.** `.claude/rules/invariants.md` (which
  auto-loads on `docs/architecture/invariants.yaml`) prescribes the raw invocation verbatim twice —
  *"Generated from the YAML by `go run ./cmd/inv-render`"* and *"4. Regenerate: `go run
  ./cmd/inv-render`"*. This **partially corrects the Codex lane's finding 6**: the bare invocation
  follows the domain rule. The wrapper (`task invariants:render`) satisfies both rules at once and is
  preferable, but the *real* defect in that verify block is AF-5, not the choice of invocation.
- **`reapingActor = "system"`** (`internal/auth/character_reaping.go:32`, used only at `:294` as
  `Actor:`) and **`genesisActor = "system"`** (`internal/world/postgres/genesis_store.go:40`, used
  only at `:146`) are outbox **envelope actors** written at the repository layer. Neither passes
  through `world.Service`, so neither is a caller-migration site and neither is an S1 hazard.
- **`internal/grpc/location_follow.go:197` is the only production `access.WithSystemSubject` call
  site** in the tree (all 21 other occurrences are `_test.go`), and `access.IsSystemContext` has
  exactly one non-test consumer (`internal/access/policy/engine.go:93`). ROADMAP criterion 4 and the
  derived-context design are both well-founded.
- **`hostfunc.WorldService` really is marked Deprecated** (`internal/plugin/hostfunc/adapter.go:20-22`)
  yet has a live implementer assertion — Plan 02's "migrate it, don't skip it" call is right.
- **The `abac-reviewer` gate fires outside its documented auto-trigger.** `CLAUDE.md` scopes it to
  "changes touching `internal/access/`"; this phase's diff is centred on `internal/world/`. ROADMAP
  criterion 5 mandates it regardless, so the gate is correctly *required* — but the executor should
  invoke `/holomush-dev:review-abac` explicitly and not expect `remind-pre-action-review.sh` to fire.

### [HIGH] AF-17 — Plan 02 Task 2 migrates the wrong fourth `withMutatorContext` consumer, silently omitting the `set_property` site

Task 2 enumerates the `world_write.go` callback bodies as `:48`, `:108`, `:185`, `:209`. Measured,
the four `withMutatorContext` call sites are `:46`, `:106`, `:183`, **`:341`**, with callback bodies
at `:48`, `:108`, `:185`, **`:343`**:

```
internal/plugin/hostfunc/world_write.go:343:
    if err := setEntityProperty(ctx, adapter, mutator, subjectID, opts, value); err != nil {
```

`:209` is inside a **`withQueryContext`** callback (`:207`), whose subject is built locally at
`:208` — a different construct entirely. The plan's own `<action>` even says ":209 … constructs its
own subject inline … outside a callback", contradicting its own `<read_first>`. Net effect: the
acceptance criterion *"the four callback sites pass the threaded caller unwrapped"* is satisfied by
three real callbacks plus one non-callback, while the genuine fourth — the `set_property` path, which
is also the site that forwards into `opts.definition.Set` — is not covered by any row.

### [MEDIUM] AF-18 — Plan 02's `:921` carve-out points at the wrong construct, and a third envelope-actor site is unmentioned

Plan 02 `<read_first>`: *"There are 12 `buildIntent` call sites; the one at :921 already passes
`access.CharacterSubject(characterID.String())` and is not a caller-derived subject — leave it."*
Measured: the 12 `buildIntent` call sites are at `internal/world/service.go` **302, 332, 386, 448,
481, 513, 610, 643, 680, 728, 769, 825**, and **all twelve pass bare `subjectID`**. Line 921 is
inside `buildPreferencesIntent` (declared `:905`) — a **third** envelope-construction helper the
plans never name:

```
917:  return wmodel.NewEnvelopeIntent(wmodel.IntentParams{
921:      Actor:         access.CharacterSubject(characterID.String()),
```

So the count "12 sites of which one is `:921`" is internally impossible, and the carve-out protects
a construct that was never at risk. The plan's own threat-model row T-02.1-06 gets the shape right
("12 `buildIntent` sites and 1 `buildMoveIntent` site") — but neither mentions
`buildPreferencesIntent`, which is on the same audit-bearing outbox-`Actor` surface the phase's
highest-severity threat covers. It is not migration-affected (it belongs to
`UpdateCharacterPreferences`, the D-05 exemption), but it should be named as such rather than
silently absent.

### [MEDIUM] AF-19 — `world.Mutator` does not declare `GetExitsByLocation`; two plans say it does

`internal/world/mutator.go:22-58` declares exactly ten methods, and `GetExitsByLocation` is **not**
among them: `GetLocation`:26, `GetCharacter`:29, `GetCharactersByLocation`:33, `GetObject`:36,
`CreateLocation`:41, `CreateExit`:44, `CreateObject`:47, `UpdateLocation`:50, `UpdateObject`:53,
`FindLocationByName`:57. `GetExitsByLocation` lives only in `command.WorldService`
(`internal/command/types.go:43`) and `grpc.WorldQuerier` (`internal/grpc/server.go:130`).

Two consequences:
- Plan 02 `<read_first>` says *"the `Mutator` interface (10 subject-carrying methods, of which
  `GetLocation` **and `GetExitsByLocation`** already moved in Plan 01)"* — wrong, and it disagrees
  with Plan 02's own `<action>` arithmetic ("the 8 remaining … plus the grouped `FindLocationByName`
  at `:57`" = 9 of 10), which is correct only if **one** method moved. The `<action>` is right; the
  `<read_first>` is wrong.
- Plan 01 says *"The four interfaces that redeclare them"* — only two (`command.WorldService`,
  `grpc.WorldQuerier`) redeclare **both** tracer methods; `world.Mutator` and the Deprecated
  `hostfunc.WorldService` declare `GetLocation` only. All four must still be edited, so the
  instruction's effect is right, but the count reads as a completeness claim it does not make.
  (Related artifact drift: `02.1-CONTEXT.md` D-64 says "3 interfaces", the ROADMAP says "six
  redeclaring interfaces", Plan 01 says "four".)

### [MEDIUM] AF-20 — Seven grouped-**and-underscore-named** declarations are invisible to the `subjectID`-anchored probe

The pinned count of **21** grouped `subjectID, …` declarations is exactly right (independently
re-measured: 21). But threat row T-02.1-07's mitigation — *"the 21 grouped sites are re-enumerated by
a documented probe and hand-migrated"* — does not cover seven further declarations that carry the
**same truncation hazard** in a form the `subjectID`-anchored probe cannot see, all in files the plan
already lists in `files_modified`:

```
internal/plugin/building_integration_test.go:101        FindLocationByName(_ context.Context, _, _ string)
internal/plugin/hostcap/property_test.go:104            FindLocationByName(_ context.Context, _, _ string)
internal/plugin/hostfunc/functions_test.go:77           FindLocationByName(_ context.Context, _, _ string)
internal/plugin/goplugin/dispatch_propagation_test.go:90
internal/plugin/hostfunc/world_test.go:82
internal/plugin/hostfunc/world_test.go:592
internal/plugin/lua/dispatch_propagation_test.go:72
```

A grouped `_, _ string` declaration is matched by a naive `has: {field: name}` rule exactly as
`subjectID, name string` is, and the fix drops the second name identically — but the enumeration
probe the plan prescribes anchors on the identifier `subjectID`, so the residual set is
under-reported by seven. Compounding this: `corebuilding_brokered_command_test.go` holds **seven**
fully-unnamed world-command declarations (`:52`, `:56`, `:60`, `:64`, `:89`, `:93`, `:97`), which the
plans call "the known instance", singular.
---

## Source-Grounding Pass

`plan_review.source_grounding` is `true`, so this pass ran in addition to the two reviewer lanes.
Every symbol, `path:line` citation, CLI command and numeric claim in all three PLAN.md files was
enumerated and checked against this tree.

**Effective authority:** `intel`
(`node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" drift-guard authority --raw` → `intel`).

**Severity mapping**, resolved from the seam rather than applied by hand
(`gsd-tools drift-guard severity --status <verdict> --authority intel`):

| Verdict | severity | hardBlock |
|---|---|---|
| VERIFIED | `none` | false |
| AMBIGUOUS | `MEDIUM` | false |
| MISSING | `needs-acknowledgement` | false |
| UNCHECKABLE | `INFO` | false |

**No verdict returned `hardBlock: true`**, so the cycle was not stopped. Under `intel` authority a
MISSING symbol is `needs-acknowledgement`, not a hard block.

**Signature-mismatch rule observed.** Under `grep`/`intel` authority a *signature* mismatch cannot be
asserted — it requires a compile. Every finding of the form "this call will not typecheck after the
flip" is therefore recorded **UNCHECKABLE** below, even where the reasoning is strong. Those same
facts are reported as *concerns* above, because their **enumeration-completeness** half is statically
provable and that is what the concerns assert.

**Exclusions applied** (phase-produced artifacts, per each plan's `<artifacts_this_phase_produces>`):
`internal/world/caller.go`, `internal/world/caller_test.go`, `world.Caller`, `world.HumanCaller`,
`world.SystemCaller`, the `Caller` context-derivation method, Plan 01's eight new `Test*` names, all
five `scripts/codemod/*.yml`, `scripts/codemod/README.md`, `test/meta/world_caller_census_test.go`,
`TestWorldServiceCallerParamIsTypedOnEveryAuthorizingCommand`, the census exemption helper,
`INV-WORLD-8`, the amended `INV-WORLD` scope text, and the three `*-SUMMARY.md` files.

### MISSING (`needs-acknowledgement`)

| # | Plan claim | Actual state |
|---|---|---|
| 1 | Plan 02 prohibition: "MUST NOT rewrite … `internal/world/postgres/**` repository calls … all four are verified codemod false positives" | `internal/world/postgres/cascade_delete_test.go` holds three **real `world.Service`** calls: `svc.DeleteLocation(ctx, "system", locID)` `:338`, `svc.DeleteObject(ctx, "system", objID)` `:377`, `svc.DeleteCharacter(ctx, "system", charID)` `:416`, against `world.NewService(...)` at `:328`/`:367`/`:406`. `//go:build integration` at `:4`. In no plan's `files_modified`. |
| 2 | Plan 01 Task 2: ignore "`internal/plugin/hostcap/world.go`'s subject-free wrapper" | That file has **five** arg-2 matches; only `:143` is subject-free. `:227`, `:288`, `:330`, `:385` each carry a real `subject` local — and Plan 02 Task 2 requires all four to be wrapped. `ignores:` is path-glob scoped, so "the subject-free wrapper" is not expressible. |
| 3 | Plan 01 Task 2 action: ignore `internal/property/**` and `internal/world/mutator.go` on **every** rule | Contradicts the same task's own criteria, which require decl-rule dry runs against `internal/property/entity_mutator_test.go` and `internal/world/mutator.go`. `mutator.go:22-58` is also one of the four interfaces Task 1 must migrate; its verified false positive is a *call* (`:231`), not a declaration. |
| 4 | Plan 02 Task 2 `read_first`: the four `world_write.go` callback bodies are `:48, :108, :185, :209` | `withMutatorContext` call sites are `:46, :106, :183, :341`; callback bodies `:48, :108, :185, **:343**`. `:209` is inside a **`withQueryContext`** callback (`:207`). The `set_property` site is uncovered. |
| 5 | Plan 02 Task 2 enumerates the production sites across five files; "31 production call sites in 12 files" | `internal/plugin/hostcap/property.go:84` (`def.Set(ctx, querier, s.host.WorldMutator(), subjectID, …)`, subject built `:83`) is a further production caller of the D-66 chain. `rg -n '\.Set\(ctx' --glob '*.go' internal/ \| rg -v '_test.go'` returns exactly two `Definition.Set` callers — `hostcap/property.go:84` and `hostfunc/world_write.go:319` — only the second is planned. |
| 6 | Plan 03: the exemption set has "exactly one member, `UpdateCharacterPreferences`" | `internal/world/service.go:160` declares exported `func (s *Service) SetMovementHook(h MovementHook)` — no ctx, no `checkAccess`, one parameter. Exported `*Service` methods total **25** = 23 commands + `UpdateCharacterPreferences` `:856` + `SetMovementHook` `:160`. |
| 7 | Plan 01: "[`recordingWorldMutator`'s] other methods … use the `_ string` form and DO match the rule" | **Seven** of its ten methods are unnamed: `:52`, `:56`, `:60`, `:64`, `:89`, `:93`, `:97`. Only the three `Create*` (`:68`, `:75`, `:82`) use `_ string`. |
| 8 | Plan 01 `<verification>`: "`task test:int` hard-codes its package list … and silently ignores a `-- ./pkg/...` argument" | `Taskfile.yaml:289` interpolates `{{.CLI_ARGS \| default "./..."}}`. Root cause: `.claude/rules/references/plan-review-learnings.md:574-575` still carries this claim against a `Taskfile.yaml:93-111` range that no longer holds — **stale repo documentation**, inherited rather than invented. |
| 9 | Plan 02 Task 3 acceptance: `internal/plugin/objects_integration_test.go` "legitimately keeps the identifier" | True of its locals, false of its declarations: `recordingCharacterMutator.SetName(context.Context, property.WorldQuerier, property.WorldMutator, string, ulid.ULID, string)` `:63` (unnamed) and `SetDescription(_ context.Context, …, subjectID string, …)` `:71` implement `property.EntityMutator`. Absent from `files_modified`. |
| 10 | Plan 02 Task 2 acceptance: `rg -n 'NewPropertyCapability\|NewWorldQueryCapability' --glob '*.go' --glob '!*_test.go'` "returns 0 lines" | Returns **4** — the constructors' own declarations at `cap_world_query.go:54,55` and `cap_property.go:46,47`. The underlying fact (no production **caller**) is VERIFIED; the criterion is what is wrong. |
| 11 | Plan 02 `read_first`: "12 `buildIntent` call sites; the one at `:921` already passes `access.CharacterSubject(...)`" | The 12 sites are `:302,332,386,448,481,513,610,643,680,728,769,825` and **all pass bare `subjectID`**. `:921` is inside `buildPreferencesIntent` (decl `:905`) — an unnamed third envelope-actor helper. |
| 12 | Plan 02 `read_first`: `world.Mutator` has "10 subject-carrying methods, of which `GetLocation` **and `GetExitsByLocation`** already moved" | `GetExitsByLocation` is not in `world.Mutator` (`mutator.go:22-58`). It exists only in `command.WorldService` `types.go:43` and `grpc.WorldQuerier` `server.go:130`. Plan 02's own `<action>` arithmetic (9 of 10 remaining) is the correct one. |
| 13 | Plan 01 Task 2 action: "Scope the two variants against each other so no file is matched by both" | No mechanism given, and path globs provably cannot express it — `internal/world/` mixes 22 `package world` files with 11 `package world_test` files. A package-clause `inside:`/`has:` rule **does** work on 0.45.1 (demonstrated); the gap is specification, not capability. |
| 14 | Plan 03 Task 2: the `description` and `boundary` are "**folded** YAML scalars" | `docs/architecture/invariants.yaml:781` and `:783` are **double-quoted flow scalars**, not block-folded (`>`/`>-`). Practical difference the plan misses: a literal `"` or `\` in added text must be escaped, so an amendment quoting e.g. `"system"` breaks the parse. |
| 15 | Plan 01 Task 2 criterion: rule files' "first two lines are the SPDX identifier and the copyright line, **matching `scripts/gen-event-docs.sh`**" | That file's `:1` is `#!/usr/bin/env bash`; SPDX `:2`, copyright `:3`. The requirement is fine for a shebang-less YAML file; the justification-by-analogy is wrong. |

### AMBIGUOUS (`MEDIUM`)

| Plan claim | Finding |
|---|---|
| Plan 01: "the four `access.*Subject` **panic-on-empty** constructors" | 3-for-4. `ViewerSubject` (`internal/access/prefix.go:153-168`) panics on a **non-empty** `playerID` for `ViewerTierAnonymous`; an **empty** one is the correct call. The plan mandates writing "four panic-on-empty" into a doc comment the `abac-reviewer` reads. |
| Plans 01/02: "the **nine empty-subject** subtests" | Nine subtests exist (`service_test.go:6079`), but the ninth is `"GetCharactersByLocation - valid subject but tests access check boundary"` — **eight** are empty-subject. |
| Plan 03: `serviceMutatingMethods` "already returns an `allMethods` set that the existing test **discards**" | Two call sites: `world_envelope_census_test.go:188` discards it, `:221` **consumes** it (`TestWorldEnvelopeCensusHasNoSceneParticipantSurface`). Reuse is still correct; the coupling is understated. |
| Plan 03: "no `*Service` method declares a `string` type in its second parameter slot" | Unscoped over *all* methods. Two unexported ones qualify today — `checkAccess` (`service.go:209`) and `buildMoveIntent` (first param `subjectID string`), and Plan 02 explicitly **preserves** `buildMoveIntent`'s plain-`string` shape. |
| Plan 02: "31 production call sites in **12 files**" | 31 sites is exact; they live in **10** files. 12 is RESEARCH's *raw-match* file count (`02.1-RESEARCH.md:346`, "34 raw production matches in 12 files"), which includes the three false-positive files. |
| Plan 02: "the **27-row** table in RESEARCH § Q2" | The main table is 27 rows (`:351-377`) ✓, but Q2 continues to **row 31** in a second table (rows 28-31 = the `cap_*` façades). A reader working "row by row against the 27-row table" never sees them. |
| Plans: "**43** build-tagged integration call sites" | Measured ~42 true sites across the six listed files; the 43 basis also counts `test/integration/privacy/privacy_test.go` (2), whose hits are `ts.DeleteCharacter(ctx, guestA.CharacterID)` — a test-suite helper whose arg-2 is a character id, not a subject. Omitting `privacy_test.go` from `files_modified` is probably correct; the number is unreconciled. |
| Plan 01: "~349 call sites … Plan 02 rewrites" | Measured 375 total arg-2 matches; `375 − 46 = 329`. The sibling figure on the same line ("~162 declarations") reproduces exactly (`185 − 23 = 162`), so the two were not derived the same way. Related drift: `02.1-CONTEXT.md` D-63 "347-site", `02.1-CONTEXT.md:193` "~400-site", Plan 02 "~332". |
| Plan 03 `read_first`: "`invariants.yaml` lines 778-800 — the `INV-WORLD` scope block" | Block is `:780-799`; `:778-779` are the preceding INV-CHANNELS `owned_paths` + blank. Usable; the plan's own `rg -n 'name: INV-WORLD$'` criterion matches correctly at `:780`. |
| Plan 03 `read_first`: "`invariants.yaml` lines 565-585 — the `INV-ACCESS` scope" | Block starts `:571`; `:565-570` are INV-CLUSTER `shared_files`. Boundary text at `:573` matches verbatim. |
| Plan 01/02/03 minor range overshoots | `service.go` `GetLocation` cited `:257-264`, actually `:256-273`; `mutator.go` `WriteCommandDescriptor` cited `:63-80`, actually `:63-76`; `service_test.go` cited `:6070-6260`, the test spans `:6079-6284`; `UpdateCharacterPreferences` cited `:845-870`, declaration at `:856`; `world_write.go` "`:209` constructs its own subject" — construction is at `:208`. All land inside the intended construct. |
| Plan 03: INV-WORLD-6 as "the cautionary example of an over-broad summary" | Substantively fair but imprecisely aimed: INV-WORLD-6's summary is *self-aware* (it already warns "an enumeration proven on one path while claiming two is a fabricated guarantee"). The real defect is that it claims two asserted paths while `asserted_by` (`:5106-5107`) lists a **single** file. Cite the `asserted_by`-vs-summary gap. |
| Plan 03 vs D-67's recorded text | `02.1-RESEARCH.md:1016-1017` records D-67 as amending the scope **boundary**; the plan amends **both** `description` and `boundary`. Defensible, exceeds the decision's text. |
| Plan 01: "The **four** interfaces that redeclare them" | Only two redeclare both tracer methods. All four still need editing, so the effect is right; the count reads as a completeness claim it does not make. `02.1-CONTEXT.md` D-64 says "3 interfaces", ROADMAP says "six" — three figures across artifacts. |
| Plan 01 criterion `rg -n '^\t[A-Z][A-Za-z]* ' internal/world/caller.go` returns 0 | Loose proxy for "no exported struct field" — also matches any tab-indented statement starting with a capitalized identifier. Non-blocking. |
| Plan 01: `service_helpers_test.go` / `mutator_test.go` in the unqualified-spelling file list | Neither contains a tracer site (`rg` for `GetLocation\|GetExitsByLocation` returns nothing); neither is in `files_modified`. Their relevance is only the `package world` header, correctly cited elsewhere. |
| Plans 01/03: bare token "D-05" | Correctly grounded in `internal/world/service.go:840`/`:854`, but `.planning/phases/01-portal-spec/01-CONTEXT.md:57` defines a **different** D-05 and no `**D-05:**` definition exists in the 02.1 artifacts. |

### UNCHECKABLE (`INFO`)

Recorded rather than resolved. **None was treated as verified or as missing.**

| Item | Why it cannot be checked now |
|---|---|
| Every claim about `scripts/codemod/*.yml` — `ignores:` scoping, run order, idempotency, the grouped-declaration probe, and D-63's re-run-to-empty-diff contract | The rule files are phase-produced; `ls scripts/codemod/` → no such directory. (Findings AF-1…AF-4 concern what the plans *specify* about them, which is checkable; the rules' behaviour is not.) |
| "Pre-flip the probe returns 185 across 24 files"; "post-flip exactly 8" | Defined *by* a rule that does not exist. Both were reproduced against a best-faith reconstruction of the rule as specified (185/24 and 8 at the exact cited `cap_*` lines), which is corroborating but not authoritative. |
| Every "this will not compile after the flip" claim — `cascade_delete_test.go` ×3, `hostcap/property.go:84`, `objects_integration_test.go:63,71`, the 7 unnamed `recordingWorldMutator` methods, the 4 `hostcap/world.go` sites | **Signature-compatibility** claims. Under `intel` authority a signature mismatch cannot be asserted without a compile, and this pass is read-only. Reported above as enumeration-completeness defects, which *are* statically provable. |
| Every post-Plan-01 state Plan 02 asserts — "`:75` already migrated", "`grpc.WorldQuerier` migrated completely", "`auth_handlers.go:972` and `location_follow.go` fully migrated", "the callback's third parameter is already a caller" | Post-change state. All four *locations* were VERIFIED to exist and are pre-flip on this tree (`helpers.go:108` still declares `subjectID string`). |
| Plan 01's `hostfunc/world_write.go` ignore rationale ("its callback receives an already-typed caller from `withMutatorContext`") | Describes a post-Plan-02 state; `helpers.go` is in neither Plan 01's `files_modified` nor its `<files>`. |
| All `must_haves` truths asserting Go type identity (e.g. "a partially-migrated tree compiles only when every declaring interface AND every implementer move together") | Correct Go semantics, not statically verifiable here. |
| Byte-identity of the ABAC subject string post-migration; the outbox `Actor` guarantee | Requires the migrated code. |
| Plan 03's `service.go:1090-1100` / `:1140-1150` framed as "how the **post-flip** declarations read" | Correct **pre-flip** (`FindLocationByName` `:1095`, `ListPropertiesByParent` `:1144`, both grouped), but Plan 02 rewrites `service.go`, so the line numbers drift before Plan 03 runs. |
| "43 of this phase's migrated call sites live behind `//go:build integration`" | Depends on the completed flip. |
| `abac-reviewer` READY (ROADMAP criterion 5); `task pr-prep` green; `task test:int` green | Post-implementation gates; `test:int` additionally needs Docker. |
| `02.1-01-SUMMARY.md` / `02.1-02-SUMMARY.md` (`<context>` blocks of Plans 02/03) | Not on disk; produced by earlier plans. Confirmed absent. |
| `02.1-PATTERNS.md` / `02.1-VALIDATION.md` in full | Anchors confirmed to exist (`02.1-PATTERNS.md:492` "Test-double declarations", `:159`; `02.1-VALIDATION.md:46` seeds the `TestWorldServiceCallerParam` run-filter verbatim), but neither document was read end to end by any lane. |

### VERIFIED (`none`) — pinned numbers and load-bearing citations

Every pinned figure that is measurable today was re-measured. **Most reproduce exactly** — the plans'
re-measurement discipline is genuinely good, which is why the misses above are worth naming.

| Claim | Plan | Measured | Instrument |
|---|---|---|---|
| Compile-time interface assertions | 13 | **13 ✓** (10 package-qualified, 11 in tests — both sub-claims ✓) | Plan 02's exact `rg … \| wc -l` |
| `command/types.go:110` inside a grouped `var ( … )` | yes | **✓** `:109 var (` / `:110 _ WorldService = …` / `:111 )` | `sed` |
| Public `world.Service` commands with an arg-2 subject | 23 | **23 ✓ at exactly the 23 cited lines, zero misses** | `rg -n '^func \(s \*Service\) '` |
| …each running `checkAccess` (1:1) | implied | **23 ✓** | `rg -c 's\.checkAccess\('` |
| Redeclaring interfaces | 6 | **6 ✓** all located | `rg -n 'type (…) interface'` |
| `buildIntent` call sites | 12 | **12 ✓** | `rg -n 'buildIntent\('` |
| Production arg-2 call sites | 31 | **31 ✓** (file count is 10, not 12 — see AMBIGUOUS) | ast-grep over the 23 names, non-test |
| Tracer arg-2 call sites | 46 across 9 files | **46 / 9 ✓** | Plan 01's exact ast-grep rule |
| Tracer parameter declarations | 23 across 16 files | **23 / 16 ✓** | Plan 01's exact `rg` chain |
| Tracer sites behind `//go:build integration` | 15 (9/3/2/1) | **15 ✓, each per-file count exact** | per-file ast-grep |
| Grouped `subjectID, …` declarations | 21 | **21 ✓** (13 files; breakdown 7 Task-1 + 4 + 1 + 9 out-of-scope closes) | ast-grep `parameter_declaration` |
| Declaration-rule coverage of the 23 tracer declarations | 22 of 23, missing only the unnamed double | **✓ empirically** — rule matches 22; zero matches in `corebuilding_brokered_command_test.go` | ast-grep |
| `internal/world/postgres/**` arg-2 matches | 9 | **9 ✓** (but 3 are real Service calls — MISSING #1) | ast-grep |
| `rg -n 'subjectID' <ten Task-1 files> \| wc -l` | 56 | **56 ✓** | verbatim |
| …piped through `rg '//'` | 2 (`helpers.go:104`, `mutator.go:20`) | **2 ✓, exactly those** | verbatim |
| `rg -n 'subjectID' --glob '*_test.go' internal/ test/` | 615 / 17 files | **615 / 17 ✓** | verbatim |
| …`service_test.go` alone | 503 | **503 ✓** | `rg -c` |
| …the six named out-of-scope files; the "607/15 earlier draft" reconciliation | 5+5+3+2+2+1; 615−8=607, 17−2=15 | **✓ all** | `rg -c` |
| Max existing `INV-WORLD` id (so 8 is next) | 7 | **7 ✓**; entries at `:5025,5036,5047,5057,5084,5095,5108` all exact | `rg -o 'id: INV-WORLD-[0-9]+'` |
| INV-WORLD-4 is the `bound` form and `asserted_by` lists a `test/meta/` file | yes | **✓** `binding: bound` `:5077`; `test/meta/world_sql_fence_test.go` `:5080`, file exists | read |
| RESEARCH § Q2 main table | 27 rows | **27 ✓** (`:351-377`) | read |
| `ast-grep` version | ≥ 0.45.1 | **0.45.1 ✓** | `ast-grep --version` |
| `ast-grep scan` emits `┌─ ` framing (non-TTY, piped) so `\| rg -c '┌─ '` works | assumed | **✓ demonstrated**, exit 0; `rg -o '┌─ ([^:]+:[0-9]+)' -r '$1'` also works. Caveat: framing is `--report-style rich`-dependent and needs a non-`off` severity | `ast-grep scan \| rg` |
| `constraints:` as a top-level sibling of `rule:` loads and runs | yes | **✓** on all five reconstructed rules | ast-grep |

Load-bearing security citations, all VERIFIED by at least two of the four independent passes:
`engine.go:92` exact equality + `:93` context half + `SYSTEM_SUBJECT_REJECTED` at `:100`;
`service.go:209` `checkAccess`, `:214` hardcoded `nil`, `:216-219` defensive comment, `:226-253`
deny-vs-eval-failure split with `RecordEngineFailure` at `:230`/`:242` and **absent** on the `:248`
deny return; `types/types.go:143` 4th param, `:144-146` empty-subject rejection, `:153-158` reserved
keys, `:160-172` empty-map→nil; `engine.go:252-265` overlay gated at `:258`; `service.go:355`
`Actor: actor` and `:1079` `Actor: subjectID`; `location_follow.go:28,197,203,216`;
`bootstrap/setting.go:134,272,276,331`; `compiler.go:154-156` unregistered-namespace skip;
`corebuilding_brokered_command_test.go:50,52`; the whole `test/meta` census machinery
(`serviceMutatingMethods` `:115-141`, `serviceReceiverName` `:145-158`, `bodyReferencesSelector`
`:162-180` whose real signature `func bodyReferencesSelector(body *ast.BlockStmt, recv, field string) bool`
matches Plan 03's call exactly, `findRepoRoot` `meta_helpers_test.go:33`); all four registry gate
tests plus `TestOwnedPathsPartition`; `Taskfile.yaml:222` `lint:invariants` and `:228`
`invariants:render`.

Additional independently-verified facts: `location_follow.go:197` is the **only** production
`access.WithSystemSubject` site (21 other occurrences are `_test.go`); `access.IsSystemContext` has
exactly **one** non-test consumer (`engine.go:93`); `reapingActor`/`genesisActor` `"system"` literals
are outbox envelope actors written at the repository layer and never reach `world.Service`; no
inter-task ordering hazard exists from the `// Verifies:` annotation landing before the registry
entry (`checkRegistryBindings` iterates `reg.Invariants` only, and the orphan pass scans `specs/`,
not test files); `go run ./cmd/inv-render` is **not** a CLAUDE.md violation because
`.claude/rules/invariants.md` prescribes it verbatim twice.

### Verification coverage statement

A clean line here does **not** mean "nothing was checked", and it does not mean "everything was
checked".

- **Checked:** every `path:line`, symbol, CLI command and numeric claim in all three PLAN.md files
  that refers to code existing on this tree — by four independent passes (Codex lane, pi lane, and
  three per-plan verification agents) plus direct re-measurement in this session of every pinned
  figure that is measurable today.
- **Not checked — enumerated as UNCHECKABLE above:** everything defined by the phase-produced
  `scripts/codemod` rules (their scoping, run order, idempotency, and the D-63 re-run contract);
  every signature-compatibility consequence, because `intel` authority forbids asserting one without
  a compile; every post-Plan-01 state Plan 02 asserts; byte-identity of the migrated subject strings;
  the `abac-reviewer`, `task pr-prep` and `task test:int` gates; and `02.1-PATTERNS.md` /
  `02.1-VALIDATION.md` end to end (anchors confirmed, full text not read).
- **Deliberately excluded, not skipped:** the phase-produced artifact set enumerated at the top of
  this pass.

---

# Cycle 2 — Cross-AI Plan Review (post-replan verification)

- **reviewed_at:** 2026-08-08T18:07:33Z
- **reviewers:** codex (codex-cli 0.147.0), pi (0.83.0)
- **tree under review:** `0b3b6f47f` — `docs(02.1): incorporate cross-AI review feedback into phase plans`
- **plans_reviewed:** `02.1-01-PLAN.md`, `02.1-02-PLAN.md`, `02.1-03-PLAN.md`
- **tooling:** `ast-grep 0.45.1`

Cycle 1 raised 10 HIGH + 22 actionable non-HIGH; the replan at `0b3b6f47f` addressed all 32.
Everything above this heading is cycle 1 and is retained as audit trail — **do not re-count a
cycle-1 finding that the current plans already fix.** This cycle's job was (a) re-running every
claim the replan asserts rather than reading its assertion, and (b) hunting what the replan broke
or missed.

Both lanes were prompt-fed the full plan set with repo read access and produced source-grounded
reviews. Neither carries the `[reviewed-without-repo-access]` marker; neither is a diff-only
reviewer. Both count at full consensus weight.

> **Process incident (no code impact).** The `pi` lane self-disclosed that it ran one
> `ast-grep scan` with `-U`, applying the arg-2 codemod to the worktree, then restored with
> `git checkout -- .`. Independently verified after the run: `git status --porcelain` is empty and
> HEAD is still `0b3b6f47f`. The lane's read-only posture denies its `edit`/`write` tools but does
> not cover write-capable subprocesses invoked through its shell. Worth tightening before the next
> cycle; nothing in the tree changed.

---

## Codex Review (cycle 2)

## Summary

The replan fixes most cycle-1 mechanics, including the receiver guard, package-clause split, declaration rules, `Definition.Set` site, integration count, and `HumanCaller("system")` mapping. However, cycle 2 found three unresolved contradictions: the committed arg-2 rule’s required `ignores:` reduce its actual baseline from 351 to 337, the “31 migrated production sites” count includes four intentionally untouched façades, and Plan 01’s first “additive, green” commit includes tests that require the second commit’s signature changes. The plans need another convergence cycle.

## Claim verification

| # | Claim | Command run | Observed result | Verdict |
|---|---|---|---|---|
| 1 | 25 exported `*Service` methods; command census excludes `SetMovementHook` structurally | `rg -n '^func \(s \*Service\) [A-Z]' internal/world/service.go` | 25 methods: 23 subject-bearing commands, `UpdateCharacterPreferences`, and `SetMovementHook`. The proposed `context.Context`-first predicate correctly separates the latter. | VERIFIED |
| 2 | Cascade-delete calls should become `HumanCaller("system")` | Inspected `cascade_delete_test.go:326-416`, `policytest/helpers.go:18-24`, and `engine.go:91-103` | All three use plain `context.Background()` and `policytest.AllowAllEngine()`, whose mock ignores both subject and context. `SystemCaller()` would introduce S1 semantics absent today; `HumanCaller("system")` preserves subject bytes and mock-engine behavior. | VERIFIED |
| 3 | Exactly two production `Definition.Set` callers | `rg -n '\.Set\(ctx' --glob '*.go' -g '!*_test.go' internal/` plus source inspection | The relevant calls are [hostcap/property.go:84](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/hostcap/property.go:84) and [world_write.go:319](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/plugin/hostfunc/world_write.go:319). Other `.Set` hits are unrelated stores. | VERIFIED |
| 4 | `$RECV` guard keeps hostcap sites 227/288/330/385, excludes 143 | Ran proposed inline ast-grep rule and filtered JSON by file/line | Exactly 227, 288, 330, 385; line 143 absent. | VERIFIED |
| 5 | Declaration rule rewrites only `fakeVersionMutator` in `entity_mutator_test.go` | Ran proposed external declaration rule with its ignores | 132 external matches total; exactly lines 44 and 53 in that file, both `_ string`; no querier method matched. | VERIFIED |
| 6 | `EXPECT()` guard changes 375 → 370; full receiver guard gives 355 | Ran naive rule, `EXPECT()`-only rule, then full receiver rule | Counts were exactly 375 → 370 → 355. The five expectation-builder matches disappeared; ordinary service calls remained. | VERIFIED |
| 7 | Package split gives internal 4, external 351, total 355 | Ran the package-clause rules without `ignores:`; then ran the proposed external rule with its required ignores | Structural split alone gives 4 internal, all in `grpc_server.go`, plus 351 external. But the actual planned external rule with its per-rule ignores returns **337**, not 351. | REFUTED as an acceptance claim |
| 8 | Invariant rendering uses task wrappers | Inspected `Taskfile.yaml:222-230` | `lint:invariants` runs `go run ./cmd/inv-render -check`; `invariants:render` runs the renderer. The former broken `git diff --exit-code` freshness test is removed. | VERIFIED |
| 9 | Plan 01 runs scoped integration tests and Taskfile interpolates args | Inspected Plan 01 and `Taskfile.yaml:282-289` | The scoped command and Docker precondition are present; `CLI_ARGS` is interpolated. Actual future execution is not runnable before implementation. | VERIFIED structurally |
| 10 | Correct `withMutatorContext`/`withQueryContext` inventory | `rg -n 'withMutatorContext|withQueryContext' world_write.go`, then source inspection | Mutator calls 46/106/183/341, bodies 48/108/185/343; line 209 belongs to the query callback opened at 207. | VERIFIED |
| 11 | Naive rule double-wraps; `SUBJ` guard is pre-flip no-op | Ran a three-call stdin fixture and compared repository counts with/without guard | Naive replacements included `HumanCaller(HumanCaller(x))` and `HumanCaller(SystemCaller())`. Full pre-flip count remained 355 with the guard. | VERIFIED |
| 12 | Reconciled counts: 355, 162, 31/10, 51, Q2 row 31, INV-ACCESS 29 | ast-grep JSON counts; `rg -c 'INV-ACCESS'`; source/table inspection | Structural candidates 355; declaration matches 132+30=162; production-shaped candidates 31 in 10 files; integration list sums 51; Q2 has rows 1–31; baseline is 29. But 355 and 31 include four deliberately untouched façade calls. | PARTIALLY REFUTED semantically |
| 13 | Eight empty-subject subtests | Inspected [service_test.go:6079-6255](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/service_test.go:6079) | All **nine** invoke a service method with `""`. The ninth has a misleading “valid subject” name, but its comment explicitly says it tests an empty subject at line 6251. | REFUTED |
| 14 | `ViewerSubject` differs from the three ordinary subject constructors | Inspected [prefix.go:95-167](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/access/prefix.go:95) | Plugin, character, and player constructors panic on empty. Anonymous viewer requires empty ID; guest/player reject empty. | VERIFIED |
| 15 | New outbox-actor detector closes T-02.1-06 | `rg -n 'TestWorldCallerSubjectReachesTheOutboxActorByteForByte'` | The test is specified in the plans but does not exist yet, as expected before execution. Its runtime effectiveness cannot be tested during plan review. | UNCHECKABLE |

## Concerns

- **HIGH — The committed rule cannot satisfy its own 351-match acceptance criterion.** The plan requires `world-caller-arg2.yml` to ignore `location_follow.go`, `internal/property/**`, `world_write.go`, and both `cap_*` façade families ([02.1-01-PLAN.md:680-692](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md:680)), but later requires that same rule to emit 351 external rewrites ([02.1-01-PLAN.md:807](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md:807)). Running the exact structural rule with those ignores produced **337**. Consequently, “355 − 46 = 309 remaining codemod sites” at [02.1-02-PLAN.md:132-134](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md:132) is not the committed rule’s executable baseline.

- **HIGH — “Migrate all 31 production sites” is impossible under the plan’s own scope.** The 31 candidates include four Lua-facing façade calls at research rows 28–31, but Plan 02 explicitly leaves those untouched. Nevertheless, Task 2 says to migrate 31 at [02.1-02-PLAN.md:459](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md:459) and declares all 31 caller-typed at lines 594–600. The consistent figures are 31 structural production candidates, **27 in-scope arg-2 migrations**, four intentionally retained façade calls, plus the position-4 `Definition.Set` site. This also means “355 true sites” is misleading: four are intentionally not true migration targets.

- **HIGH — Plan 01’s first commit cannot be both additive and green as written.** The commit mapping puts all of `caller_test.go` in the “additive, zero consumers” commit ([02.1-01-PLAN.md:122-127](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md:122)). Task 1 requires that file to contain tests calling `Service.GetLocation` with `Caller` and exercising the migrated `checkAccess` seam. Those tests cannot compile until the second commit changes the service signature. This violates D-64’s per-commit green requirement.

- **MEDIUM — The empty-subject correction is itself incorrect.** The plans repeatedly assert eight empty-subject cases and one valid case, including [02.1-01-PLAN.md:53](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md:53) and [02.1-03-PLAN.md:523-524](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-03-PLAN.md:523). Source shows the ninth case also calls with `""` at [service_test.go:6250-6252](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/internal/world/service_test.go:6250). The case name is stale, not the input.

- **MEDIUM — Plan 03’s final codemod diff-clean check lacks explicit commit/staging order.** Task 3 requires a “clean working directory” at [02.1-03-PLAN.md:526-528](/Volumes/Code/github.com/holomush/.worktrees/v013-phase-03/.planning/phases/02.1-world-caller-model/02.1-03-PLAN.md:526), but Tasks 1–2 have just created the census and changed the invariant registry. Unless those changes are committed first, `git diff --exit-code` fails for unrelated planned changes. Plan 03 does not state where landmark commit 3 occurs relative to this check.

## Suggestions

- Separate three ast-grep metrics in every plan:

  - 355 structurally valid candidates before per-rule path exclusions.
  - 337 matches emitted by the committed external rule plus four by the internal rule on the pre-flip tree.
  - The actual post-tracer residual after accounting for hand-migrated and threaded surfaces, derived by rerunning the committed rules against the Plan 01 tree rather than by `355 − 46`.

- Replace “31 migrated production sites” with “31 structural candidates: 27 in scope and four intentionally retained façade calls.” Update the objective, threat boundary, completion text, and acceptance criteria consistently.

- Split `caller_test.go` across the two Plan 01 commits, or put only constructor-only tests in commit 1 and add the service/DSL/S1 tests in commit 2. State the exact test functions assigned to each commit.

- Correct all “eight empty-subject” statements to nine, while noting that the ninth test name incorrectly says “valid subject.”

- Make Plan 03’s ordering explicit: commit or stage the invariant/census work, snapshot the index/tree, run the four codemods, and compare the post-run tree to that snapshot. A plain repository-wide `git diff --exit-code` is too sensitive to unrelated planned changes.

## Risk Assessment

**HIGH.** The security-sensitive mappings and structural ast-grep mechanisms mostly hold, including `HumanCaller("system")`. The remaining blockers are execution-level contradictions: the committed rule cannot meet its stated count, the migration total includes deliberately excluded calls, and the first commit cannot remain green with the prescribed test allocation. These should be fixed before execution.

---

## pi Review (cycle 2)

All verification is complete. Producing the cycle-2 review.

---

# Cross-AI Plan Review — Phase 02.1, Cycle 2

## Summary

The replan holds up under re-execution. Every load-bearing claim I re-ran reproduced exactly: the corrected ast-grep rules behave as pinned (351/4 arg-2 split, 132/30 declaration split, 185→8 probe census), the double-wrap idempotency bug is real and the `SUBJ` guard genuinely fixes it while being a pre-flip no-op, the `HumanCaller("system")` mapping for `cascade_delete_test.go` is the behavior-preserving choice, and all reconciled figures (355/309/162/31-in-10/51/46-in-9/23-in-16/15 tracer integration sites) match this tree. I found **one new HIGH**: the `SUBJ` guard regex is too narrow — it only excludes `HumanCaller(`/`SystemCaller(` call expressions, but Plan 01's criterion-2 proof test *must* construct a caller via composite literal or a local variable, and the arg-2 rules demonstrably re-wrap both of those forms. That corrupts `caller_test.go` on Plan 02 Task 3's first codemod run and makes the D-63 re-run-to-empty-diff gate unsatisfiable without a rule fix. Everything else is LOW.

**Disclosure:** during verification I accidentally ran one scan with `-U` and applied the arg-2 rule to the worktree; the diff was purely the codemod's own output (351 hunks) and I restored the tree with `git checkout -- .` — `git status --porcelain` is clean. (Incidentally this accidentally proved the external rule applies exactly 351 clean rewrites.)

## Claim verification table

| # | Claim | Command I ran | Observed | Verdict |
|---|-------|---------------|----------|---------|
| 1 | 25 exported `*Service` methods = 23 commands + `UpdateCharacterPreferences`:856 + `SetMovementHook`:160 | `rg -n '^func \(s \*Service\) [A-Z]' internal/world/service.go` | 25; `SetMovementHook` at :160, `UpdateCharacterPreferences` at :856; 23 with arg-2 `subjectID` | **VERIFIED** |
| 2 | `cascade_delete_test.go` :338/:377/:416 map to `HumanCaller("system")`, not `SystemCaller()` | `sed -n '325,420p'`; `head -8` | `AllowAllEngine()` + `context.Background()` at all three; bare `"system"` literal; `//go:build integration` at :4 | **VERIFIED — mapping is correct** (see analysis below) |
| 3 | Exactly two production `Definition.Set` callers: `hostcap/property.go:84`, `world_write.go:319` | `rg -n '\.Set\(ctx' --glob '*.go' -g '!*_test.go' internal/` | Both present at those lines; all other `.Set(ctx` hits are `metadataStore`/`sessionStore`/`kvStore` | **VERIFIED** |
| 4 | `$RECV` blocklist yields exactly `hostcap/world.go` :227/:288/:330/:385, not :143 | built rule per plan, `ast-grep scan` on the file | Exactly those 4 hunks; :143 absent | **VERIFIED** |
| 5 | Decl rule rewrites `fakeVersionMutator` :44/:53, leaves `fakeVersionQuerier` | decl rule on `entity_mutator_test.go` | Exactly two hunks at :44/:53, both `fakeVersionMutator` | **VERIFIED** (rule expressible — needed `has: {field: name, pattern: $NAME}` inside `all:`; duplicate `has:` keys are rejected) |
| 6 | EXPECT guard: 5 mockery sites gone, in-scope `svc.*` hunks kept; 375→370 | rule minus `RECV` vs full rule; naive whole-tree rule | 5 EXPECT hunks without guard, 0 with; `service_test.go` keeps 271 hunks; naive whole-tree = 375 | **VERIFIED** (375 is the whole-tree naive count incl. 5 `package world` matches; plan's 375→370→351 arithmetic is consistent once the package split is accounted) |
| 7 | Package-clause scope: internal 4 (all `grpc_server.go`), external 351, zero overlap, 355 total | both rule variants over repo | 4 (all in `grpc_server.go`) + 351 = 355 | **VERIFIED** |
| 8 | `task invariants:render` / `task lint:invariants` exist at `Taskfile.yaml:227/:222` | `sed -n '222,230p' Taskfile.yaml` | Both present, wrapping `go run ./cmd/inv-render` / `-check` | **VERIFIED** |
| 9 | `Taskfile.yaml:289` interpolates CLI_ARGS | `sed -n '287,291p'` | `{{.CLI_ARGS | default "./..."}}` present | **VERIFIED** (per instruction, not re-litigated) |
| 10 | `withMutatorContext` at :46/:106/:183/:341; :209 is `withQueryContext` (opened :207) | `rg -n 'withMutatorContext\|withQueryContext' world_write.go` | :46/:106/:183/:341 mutator; :207/:366 query | **VERIFIED** |
| NEW HIGH | Double-wrap reproduction + guard is pre-flip no-op | scratch file with migrated sites, rule ±`SUBJ` guard; full-repo run ±guard | Without guard: `HumanCaller(HumanCaller(subjectID))` and `HumanCaller(SystemCaller())` both reproduced. With guard: 0 hunks on migrated lines, unmigrated line still rewritten. Repo count 351 either way | **VERIFIED (bug real, fix works, no-op pre-flip)** |
| Figures | 309 = 355−46; 162 = 132+30; 31 prod in 10 files; 51 build-tagged; 46 tracer in 9 files; 23 decls in 16 files; 15 tracer integration sites (9/3/2/1); probe 185/8; 21 grouped; 7 `_, _ string`; 8 unnamed; 13 assertions; INV-ACCESS 29; 8-of-9 empty-subject; `ViewerSubject` 3-for-4; 615/503 rg census | per-claim commands (tracer rule, decl rules, probe, `rg` chains, per-file integration counts) | Every number reproduced exactly | **ALL VERIFIED** |

## Concerns

- **HIGH — the `SUBJ` idempotency guard does not cover composite literals or local variables, and Plan 01's criterion-2 proof test cannot avoid those forms.** Plan 01 Task 1 mandates: the attribute-carrying caller is built via a *same-package composite literal* ("no production code, exported or unexported, may exist for attribute injection"), and the proof drives `Service.GetLocation` with it. Demonstrated on ast-grep 0.45.1 with the plan's pinned guard:
  - `svc.GetLocation(ctx, c, locID)` (local var) → rewritten to `svc.GetLocation(ctx, HumanCaller(c), locID)`
  - `svc.GetLocation(ctx, Caller{subject: …}, locID)` (composite literal) → rewritten to `HumanCaller(Caller{…})`
  `caller_test.go` is `package world`, so `world-caller-arg2-internal.yml` matches it. Consequences: (a) Plan 02 Task 3's *first* codemod run corrupts `caller_test.go` — a type error caught by `task test`, but the plan's instruction is "fix the rule, not the file", and the rule as pinned cannot express the fix; (b) the D-63 re-run-to-empty-diff gate (Plan 02 Task 3 *and* Plan 03 Task 3) can never go green while any call site passes a caller in one of these forms. The fix is expressible — add `not: {kind: composite_literal}` to the `SUBJ` constraint (verified below to leave `Caller{…}` alone) plus either an `ignores:` entry for `internal/world/caller_test.go` or a plan requirement that proof tests use only constructor-call forms at call sites — but **the plan must pin this now**, because the acceptance criteria in Plan 01 Task 2 ("the `SUBJ` idempotency guard is present and proven") prove only the constructor-call case and would pass while the hole remains.

  ```
  # scratch verification: adding `not: {kind: composite_literal}` to SUBJ
  # leaves `Caller{...}` unwrapped; locals still require a naming/ignore strategy
  ```

- **MEDIUM — STATE.md's supersession record carries stale intermediate figures.** `.planning/STATE.md:530` records "31 arg-2 production call sites not 47, and ~332 true test sites not 347 … ~106 hand-rolled test-double method declarations". The plans' own reconciled figures (355 total true arg-2 sites → 309 for Plan 02; 162 declarations) supersede those in turn — Plan 02's key_links say so explicitly — but the STATE.md record a future session reads first still says 332/106. The plans forbid hand-editing STATE.md (prohibition in all three), so this is a documentation-drift note, not a plan defect; worth a one-line STATE.md annotation at ship time.

- **LOW — decl-rule YAML is executor-authored, and the obvious first attempts fail silently.** The plan describes the declaration rule prose-only ("match nodes of kind `parameter_declaration` that have a `name` field captured to a metavariable"). Metavariables bind only via `pattern`, `has: {field: name, pattern: $NAME}` works only inside an `all:` (duplicate `has:` keys are a YAML-level parse error), and the naive `pattern: $NAME string` is an invalid Go pattern (parses as ERROR node, matches nothing — no diagnostic). I confirmed a working formulation (used for the 132/30 verification above), so the rule is achievable, but an executor's first two or three attempts will produce zero-match rules that *pass* the "loads without error" acceptance check while rewriting nothing. Suggest Plan 01 Task 2 add the `all: [has: {field: name, pattern: $NAME}, …]` shape explicitly.

- **LOW — Plan 02 Task 3's assertion-immutability grep pattern misses `require.` inside multiline calls and `suite`-style assertions, but this is the same mechanism cycle 1 accepted;** the `$BASE`-capture fix (vs. the mutable index) is correct and I verified `git diff` staged-vs-unstaged semantics support the concern. No action.

## Suggestions (specific plan changes)

1. **Plan 01 Task 2 / Plan 02 Task 3:** widen the `SUBJ` constraint to `all: [{not: {regex: '(HumanCaller|SystemCaller)\('}}, {not: {kind: composite_literal}}]` and add `internal/world/caller_test.go` to both arg-2 rules' `ignores:` (the only file that must pass non-constructor callers), with a header comment explaining why. Add an acceptance criterion: a dry run against a scratch file containing `svc.GetLocation(ctx, c, id)` and `svc.GetLocation(ctx, Caller{…}, id)` produces zero hunks for those lines. (My `/tmp` transcript demonstrates both the failure and that `not: {kind: composite_literal}` fixes the literal half.)
2. **Plan 01 Task 2 `<action>`:** pin the working declaration-rule skeleton (`rule: {kind: parameter_declaration, all: [has: {field: name, pattern: $NAME}, has: {field: type, regex: '^string$'}, not: {regex: ','}, inside: …]}`) so the executor doesn't ship a zero-match rule that passes the "loads cleanly" criterion.
3. **Plan 03 Task 3 `<action>`:** when recording residual follow-ups, add "annotate the STATE.md supersession record with the final reconciled figures (355/309/162)" to the summary's follow-up list.

## Risk Assessment

**MEDIUM.** The replan verified out almost perfectly — every number, line reference, rule behavior, and the security-critical `HumanCaller("system")` mapping reproduced under independent re-execution, and the cycle-1 HIGH class ("the rule cannot express this") is now demonstrably expressible. The one new HIGH is narrow and mechanical: a too-narrow idempotency guard that will fire on the criterion-2 proof test the phase itself creates. It is caught by the phase's own gates (compile error + non-empty re-run diff) rather than shipping silently, so it costs an iteration loop, not a correctness or security escape — but it should be fixed in the plan text before execution, because the current acceptance criteria would certify the buggy guard as correct.

---

## Consensus Summary (cycle 2)

### Re-run verification — what the replan got right

Every numeric and rule-level claim below was independently re-derived in this session with
`ast-grep 0.45.1` and `rg`, and separately by both lanes. **All reproduced exactly.**

| Claim | Command | Observed |
|---|---|---|
| 25 exported `*Service` methods = 23 commands + `UpdateCharacterPreferences`:856 + `SetMovementHook`:160 | `rg -n '^func \(s \*Service\) [A-Z]' internal/world/service.go` | 25; `SetMovementHook` `:160` has no `ctx` and one param — excluded by the `context.Context`-first predicate by construction |
| `$RECV` blocklist keeps `hostcap/world.go` :227/:288/:330/:385 and drops :143 | rule ± the `RECV` constraint, scanned on that file | exactly those four; `:143` present only without the constraint |
| Package-clause split: internal 4 (all `grpc_server.go`), external 351, zero overlap, 355 | both variants, `rg -c '│\+'` | 4 (`grpc_server.go` :44/:62/:80/:101) + 351, no file in both |
| Declaration rules: 132 external + 30 internal = 162 | both decl variants | 132 + 30 (9 `mutator.go` + 21 `service.go`) |
| Decl rule rewrites `entity_mutator_test.go` `fakeVersionMutator` :44/:53, leaves every `fakeVersionQuerier` | decl rule on that file | exactly :44 and :53 |
| Five mockery `EXPECT()` sites removed; both files keep their in-scope `svc.*` hunks | set-difference of the two match sets | exactly `service_test.go` :4016/:4054/:4208/:5065 + `movement_hook_test.go:83`; `service_test.go` retains 271 hunks |
| 14 receiver-blocklist exclusions are all false positives | set-difference | exactly `hostcap/world.go:143`, `hostfunc/world.go:119`, `adapter_test.go` :230/:241/:254/:266/:277/:414, `character_repo_test.go` :543/:571/:582/:825/:841/:849 |
| Real sites retained (over-suppression check) | full match set | `hostcap/world.go` :227/:288/:330/:385 and `cascade_delete_test.go` :338/:377/:416 all present |
| Probe census 185 sites across 24 files | probe rule, `rg -c '┌─ '` | 185 / 24 |
| 21 grouped `subjectID, ` sites | grouped probe | 21 |
| 7 grouped `_, _ string` sites | grouped-blank probe | 7, at exactly the enumerated `file:line` pairs |
| 46 tracer sites (`GetLocation`/`GetExitsByLocation`), 15 of them build-tagged | tracer-scoped variants | 44 external + 2 internal = 46; tagged = 9+3+2+1 = 15 |
| 51 build-tagged arg-2 sites | per-file tags over the match set | 48 in `test/integration/**` + 3 in `cascade_delete_test.go` |
| 31 production arg-2 sites in 10 files | production filter over the match set | 27 external + 4 internal, 9 + 1 files |
| Exactly two production `Definition.Set` callers | `rg -n '\.Set\(' …` | `hostcap/property.go:84` (subject at position 4) and `hostfunc/world_write.go:319` |
| `withMutatorContext` :46/:106/:183/:341; `:209` is a `withQueryContext` callback (opened :207) | `rg -n 'withMutatorContext\|withQueryContext' world_write.go` | confirmed |
| `Taskfile.yaml` :222 `lint:invariants`, :227 `invariants:render`, :289 `test:int` interpolates `{{.CLI_ARGS \| default "./..."}}` | `sed` | confirmed |
| `INV-ACCESS` baseline 29; next free `INV-WORLD` is 8 | `rg -c 'INV-ACCESS' docs/architecture/invariants.yaml`; `rg -o 'INV-WORLD-[0-9]+'` | 29; max existing is `INV-WORLD-7` |
| `ViewerSubject` is 3-for-4 on panic-on-empty | `internal/access/prefix.go` :97/:107/:121/:153 | plugin/character/player panic; viewer's anonymous rung *requires* empty |

**The new HIGH the planner self-reported is real and its fix works.** Reproduced on a scratch file:
without `SUBJ: not: regex: '(HumanCaller|SystemCaller)\('` the rule emits
`world.HumanCaller(world.HumanCaller(subjectID))` and `world.HumanCaller(world.SystemCaller())`;
with it, both already-migrated lines are untouched and only the un-migrated line rewrites. And the
guard is genuinely a pre-flip no-op — the external count is **351 with and without it**.

**The `cascade_delete_test.go` → `HumanCaller("system")` mapping is correct** (review focus 3).
All three sites (`:338`/`:377`/`:416`) run under `policytest.AllowAllEngine()` with a plain
`context.Background()` and pass the bare `"system"` literal. `internal/access/policy/engine.go:92`
tests exact subject equality and `:93` requires `access.IsSystemContext`; a subject of `"system"`
*without* the marker is a hard `SYSTEM_SUBJECT_REJECTED` error, not a normal evaluation. So
`HumanCaller("system")` is byte-preserving and behaviour-preserving, while `SystemCaller()` would
have introduced three test-visible total ABAC bypasses that do not exist today. Both lanes
independently reached the same verdict. The T-02.1-11 threat row carries the rationale correctly.

**Source-grounding: zero MISSING symbols.** Every pre-existing type, interface, method, test name,
`path:line`, Taskfile task, invariant id and requirement id cited by the three plans resolves
against this tree. Two AMBIGUOUS entries (both citation-quality, both listed below).

### Agreed Concerns (2+ reviewers, or reviewer + this session)

Six HIGHs remain unresolved. Three are contradictions the replan introduced or left open while
fixing 32 findings; three are enumeration/gating regions the replan's own census lens excluded.

---

### [HIGH] C2-H1 — The `SUBJ` idempotency guard is too narrow: a `Caller` local variable or composite literal at a call site is still double-wrapped

*Raised by `pi`; reproduced independently in this session.*

The pinned guard is textual — `SUBJ: not: regex: '(HumanCaller|SystemCaller)\('`
([02.1-01-PLAN.md:592](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md)). It therefore
excludes only *constructor-call* second arguments. Reproduced with the internal variant against a
`package world` scratch file:

```
_, _ = svc.GetLocation(ctx, c, locID)                      → HumanCaller(c)
_, _ = svc.GetLocation(ctx, Caller{subject: "x"}, locID)   → HumanCaller(Caller{subject: "x"})
_, _ = svc.GetLocation(ctx, HumanCaller("y"), locID)       → untouched ✓
_, _ = svc.GetLocation(ctx, SystemCaller(), locID)         → untouched ✓
```

This is not hypothetical: Plan 01 Task 1 requires the criterion-2 proof to construct the
attribute-carrying caller **via a same-package composite literal** because "no production code,
exported or unexported, may exist for attribute injection"
([02.1-01-PLAN.md:267](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md),
[:296](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md)), and to drive
`Service.GetLocation` with it. `internal/world/caller_test.go` is `package world`
([:275](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md),
[:941](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md)), so
`world-caller-arg2-internal.yml` matches it — and that rule's `ignores:` is pinned as **(none)**
([02.1-01-PLAN.md:713](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md)).

Consequence: the D-63 re-run-to-empty-diff contract that **Plan 02 Task 3 and Plan 03 Task 3 both
gate on** can never go green. Plan 01 Task 2's acceptance criterion
([:788](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md)) proves only the constructor-call
case, so it would certify the buggy guard as correct.

**Fix, partially verified in session:** adding `not: {kind: composite_literal}` to the `SUBJ`
constraint fixes the literal half (confirmed — `Caller{…}` is then left alone) but the
local-variable half still re-wraps. So the plan must ALSO either add
`internal/world/caller_test.go` to both arg-2 rules' `ignores:` (with a header comment saying why),
or pin a requirement that every post-flip call site passes a caller in constructor-call form.
Add an acceptance criterion that dry-runs a scratch file containing both non-constructor forms and
expects zero hunks.

---

### [HIGH] C2-H2 — `world-caller-arg2.yml` cannot satisfy its own 351-match acceptance criterion once its mandated `ignores:` are applied

*Raised by `codex`; re-measured independently in this session.*

[02.1-01-PLAN.md:711](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md) pins the committed
rule's `ignores:` as `internal/grpc/location_follow.go`, `internal/property/**`,
`internal/plugin/hostfunc/world_write.go`, `internal/plugin/hostfunc/cap_property*.go`,
`internal/plugin/hostfunc/cap_world_query*.go`.
[02.1-01-PLAN.md:807](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md) then asserts
`ast-grep scan -r scripts/codemod/world-caller-arg2.yml . | rg -c '│\+'` returns **351**.

Measured both ways on this tree:

| rule | count |
|---|---|
| structural rule, no `ignores:` | **351** |
| the committed rule, with the mandated `ignores:` | **337** |

The 14-hunk difference is exactly the ignored surfaces: `location_follow.go` 2, `entity_mutator.go`
4, `world_write.go` 4, `cap_world_query.go` 2, `cap_property.go` 2. So the acceptance criterion is
unsatisfiable as written — the same "acceptance criterion that cannot hold" class as cycle 1's
AF-2/AF-5. It also means the `355 − 46 = 309` derivation at
[02.1-02-PLAN.md:132-134](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md) silently mixes
two bases (structural candidates vs. rule-emitted hunks).

**Fix:** state three distinct numbers everywhere they appear — 355 structural candidates
(351 + 4); 337 + 4 = 341 emitted by the *committed* rules pre-flip; and the residual for Plan 02
derived by re-running the committed rules against the post-Plan-01 tree rather than by subtraction.
The declaration rules are unaffected: their `cap_*` ignores are redundant (those files contribute
zero declaration matches because of the comma constraint), so **132 holds with and without
`ignores:` — verified**.

---

### [HIGH] C2-H3 — Plan 02 Task 2's "all 31 production arg-2 call sites" includes the four `cap_*` façade calls the same task forbids touching

*Raised by `codex`; confirmed independently.*

[02.1-02-PLAN.md:142](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md) correctly records
that RESEARCH § Q2 continues past its 27-row main table into a second table whose **rows 28-31 are
the `cap_*` façades**. Task 2 then instructs: "Migrate the 31 arg-2 production call sites … working
row by row against RESEARCH § Q2 (**rows 1-31 across its two tables**)"
([:396](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md),
[:459](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md)), and its `<done>` asserts "All 31
production arg-2 call sites (in 10 files) carry a caller value" — in the same paragraph as "the two
out-of-scope Lua facades are untouched"
([:594](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md)).

Those cannot both hold. The 31 comprises `cap_property.go` :74/:140 and `cap_world_query.go`
:84/:119 (Q2 rows 28-31, which RESEARCH explicitly recommends leaving alone), plus
`location_follow.go` :2 sites that **Plan 01 already migrated to `SystemCaller()`**
([02.1-01-PLAN.md:58](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md)). Verified in
session that all four `cap_*` matches target the façade interfaces (`c.world.*`, `c.properties.*`),
never `world.Service` — so leaving them alone is right, and the count is what is wrong.

**Fix:** replace "31 production sites" with "31 structural production candidates — 27 in scope,
of which Plan 01 already migrated 2 (`location_follow.go`), leaving 25 for this task; plus 4
intentionally-retained `cap_*` façade calls". Propagate through the task name, `<action>`, the
`<done>` block, the threat-boundary table row at
[:870](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md), and the reconciliation table at
[:136](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md).

---

### [HIGH] C2-H4 — Plan 01's first commit is declared "additive, zero consumers" but contains three tests that cannot compile until the second commit

*Raised by `codex`; confirmed independently.*

[02.1-01-PLAN.md:124-125](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md) assigns commit 1
= `internal/world/caller.go` + `caller_test.go` + `scripts/codemod/**`, "additive, zero consumers".
But Task 1 specifies `caller_test.go` to contain three tests that drive the *unmigrated* service
([:295-305](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md)):

- `TestWorldServiceCallerAttributesReachActionBag` — "drive a real `Service.GetLocation`"
- `TestWorldServiceSystemCallerSatisfiesTheS1GateWithoutAmbientContext` — "drive
  `Service.GetLocation` with `SystemCaller()`"
- `TestWorldServiceEmptyHumanCallerFailsClosed` — "`Service.GetLocation` with `HumanCaller("")`"

`Service.GetLocation` still takes `subjectID string` at commit 1; the flip is in commit 2. Commit 1
is therefore red, violating the per-commit-green requirement this same plan pins as D-64's outcome.

**Fix:** name the exact test functions per commit — the five constructor-only tests
(`TestHumanCallerCarriesItsSubjectVerbatim`, `…AcceptsAnEmptySubjectWithoutPanicking`,
`TestSystemCallerYieldsTheBareSystemSubject`, `TestHumanCallerCarriesNoAttributes`,
`TestSystemCallerStampsTheSystemMarkerOnlyOnTheDerivedContext`) in commit 1, the three
`Service.*`-driving tests in commit 2 — or move the whole file to commit 2 and shrink commit 1 to
`caller.go` + `scripts/codemod/**`.

---

### [HIGH] C2-H5 — The position-4 `Definition.Set` surface is still unenumerated on the test side; two files are in no plan's `files_modified`

*Raised in this session (review focus 2 — remaining excluded regions).*

Cycle 1's `hostcap/property.go:84` HIGH was "a position-4 `Definition.Set` site the arg-2 census
structurally cannot see". The replan fixed the **production** half. The command it used to prove
completeness — [02.1-02-PLAN.md:137](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md),
`rg -n '\.Set\(ctx' --glob '*.go' -g '!*_test.go' internal/` — **explicitly excludes test files**,
so the test half was never enumerated. Two files break at compile once `Definition.Set`'s position-4
parameter becomes `world.Caller` ([02.1-02-PLAN.md:222](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md),
[:284](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md)):

| Site | Shape | Status |
|---|---|---|
| `internal/property/registry_test.go:29` | `func (d testDefinition) Set(_ context.Context, _ WorldQuerier, _ WorldMutator, _ string, …)` — a `property.Definition` implementer registered at `:35` | **in NO plan's `files_modified`**, and the string `registry_test.go` appears nowhere in the three PLAN.md files |
| `internal/property/definition_error_test.go:22`, `:34` | `def.Set(context.Background(), nil, nil, "subject", "widget", ulid.ULID{}, "value")` — bare string at position 4 | **in NO plan's `files_modified`** |

Both are `package property` with no build tag. Verified that **neither codemod rule can see them**
(`Set` is not one of the 23 command names; the subject is at position 4), and that neither file
appears in the arg-2 or declaration match sets.

*Mitigating:* Plan 02 Task 1's own scoped gate does include `./internal/property/`
([02.1-02-PLAN.md:334](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md)), so the break is
loud rather than silent, and Task 1 carries a catch-all ("fix whatever the compiler surfaces that
the rules could not reach"). Graded HIGH anyway for consistency with cycle 1's identical
`cascade_delete_test.go` finding: a file absent from `files_modified` is an execution-time deviation
requiring a checkpoint, and this is the third recurrence of this blind spot.

**Fix:** add both files to Plan 02's `files_modified` and enumerate the three sites in Task 1's
`<action>`. Re-run the position-4 census **without** `-g '!*_test.go'` and pin the full result.

---

### [HIGH] C2-H6 — Plan 01's new scoped `task test:int` gate does not cover one of Plan 01's own integration-tagged files

*Raised in this session (review focus 4 — green-per-commit).*

The replan closed cycle 1's green-per-commit finding by gating Plan 01's second commit on
`task test:int -- ./test/integration/access/... ./test/integration/resilience/...`
([02.1-01-PLAN.md:473](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md),
[:518](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md),
[:901](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md)). Checking every entry of Plan 01's
`files_modified` for a build tag:

| tagged file in Plan 01's `files_modified` | covered by the scoped gate? |
|---|---|
| `test/integration/access/concurrent_engine_test.go` | yes |
| `test/integration/resilience/m12_lastwritewins_test.go` | yes |
| `test/integration/resilience/m2_dualwrite_test.go` | yes |
| `test/integration/resilience/restart_reconnect_test.go` | yes |
| **`internal/plugin/building_integration_test.go`** | **no** |

`internal/plugin/building_integration_test.go` carries `//go:build integration` at `:4`, is listed at
[02.1-01-PLAN.md:25](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md) and in Task 1's file
list at [:204](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md), and declares
`func (m *mockWorldMutator) GetLocation(_ context.Context, _ string, _ ulid.ULID) …` at `:65` — a
`hostfunc.WorldMutator` implementer that must move with the tracer interface. `task test` skips it
(build tag) and the scoped `test:int` excludes `./internal/plugin/...`, so a mis-migration there
first surfaces at Plan 02 Task 3's unscoped `task test:int` — precisely the deferral the scoped gate
was added to prevent.

Note the gate's accounting reasons over *call* sites ("the tracer's 46 call sites live behind that
tag", [:905](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md)); the exposure is a
*declaration* site. Confirmed: `building_integration_test.go` has 0 tracer call sites and 1 tracer
declaration.

**Fix:** extend the gate to
`task test:int -- ./test/integration/access/... ./test/integration/resilience/... ./internal/plugin/...`
at `:473`, `:518` and `:901`, and restate the accounting over declaration sites as well as call
sites.

---

### Actionable non-HIGH

- **[MEDIUM] C2-M1 — the replan's nine-→-eight "correction" is itself wrong; all nine subtests
  supply an empty subject.** *Raised by `codex`; confirmed.*
  [02.1-01-PLAN.md:53](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md) and
  [02.1-03-PLAN.md:523-524](.planning/phases/02.1-world-caller-model/02.1-03-PLAN.md) now assert
  "eight of those nine supply an empty subject; the ninth … supplies a valid one". The ninth case,
  `"GetCharactersByLocation - valid subject but tests access check boundary"`, invokes
  `svc.GetCharactersByLocation(ctx, "", locID, world.ListOptions{})` at
  `internal/world/service_test.go:6251`, under an inline comment at `:6250` reading "Test with empty
  subject to trigger NewAccessRequest failure". Only the **name** is stale. The verification method
  the plan pins (`rg -n 'name:\s+"'`, counting names containing "with empty subject fails closed")
  measures the name string, not the input — a name-vs-behaviour conflation.
  **Change:** revert to "nine empty-subject subtests", noting that the ninth's name is misleading
  and MUST NOT be edited (success criterion 3 forbids assertion changes). This does not affect the
  do-not-report `HumanCaller("")`-does-not-panic decision, which stands.

- **[MEDIUM] C2-M2 — three of the six cited unnamed-declaration line numbers drifted by +5.**
  *Raised by the source-grounding pass.*
  [02.1-02-PLAN.md:698-700](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md) cites
  `UpdateLocation :84`, `UpdateObject :88`, `FindLocationByName :92` in
  `internal/plugin/lua/corebuilding_brokered_command_test.go`. Actual:
  `:89`, `:93`, `:97` (`:84` lands inside `CreateObject`, `:92` inside `UpdateLocation`). The other
  three (`:56`, `:60`, `:64`) and Plan 01's `:52` are correct. These are the sites the plan says "no
  rule and no probe can see" and must be hand-migrated at, so the coordinates are load-bearing.

- **[MEDIUM] C2-M4 — three more position-4 sites live in files that ARE in `files_modified` but are
  not enumerated.** [02.1-02-PLAN.md:700-703](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md)
  presents `objects_integration_test.go:63`/`:71` as *the* instance of "a position-4 parameter under
  a method name outside the 23-command regex, so no committed rule reaches it". The class has three
  more members: `internal/plugin/hostcap/property_test.go:40` (`fakePropertyDef.Set`),
  `internal/property/entity_mutator_test.go:137` and `:145`
  (`testEntityMutator.SetName`/`SetDescription`), plus that file's six position-4 call arguments at
  `:73`/`:80`/`:90`/`:100`/`:110`/`:119`. Enumerate them alongside the `objects_integration_test.go`
  pair.

- **[LOW] C2-M3 — the `375 → 370` figure is measured on a different basis than every neighbouring
  number.** [02.1-01-PLAN.md:66](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md) reports
  the `EXPECT()` delta as `375 → 370`, then the receiver delta as `365 → 351` "on the external
  variant". Measured here: whole-tree naive = 375, whole-tree EXPECT-only = 370; **external-variant**
  no-`RECV` = 370, EXPECT-only = 365, full = 351. Both pairs are internally correct but on different
  bases in adjacent bullets, so a reader re-deriving 375 against the committed external rule gets
  370. Label each pair with its basis.

- **[LOW] C2-M5 — "eight fully-unnamed declarations" heads a seven-item list.**
  [02.1-02-PLAN.md:696](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md). Eight is the
  repo-wide total; seven remain after Plan 01 takes `corebuilding_brokered_command_test.go:52`. Say
  which.

- **[LOW] C2-L1 — the declaration-rule YAML shape is left to the executor, and the natural first
  attempts fail silently.** *Raised by `pi`; independently hit in this session.* Metavariables bind
  only via `pattern:`; `has: {field: name, pattern: $NAME}` must sit inside an `all:` (duplicate
  `has:` keys are a YAML parse error); and `pattern: $NAME string` parses as an ERROR node and
  matches nothing with **no diagnostic**. A zero-match rule still passes the "loads without error"
  acceptance check. Pin the working skeleton
  (`rule: {kind: parameter_declaration, has: {field: name, pattern: $NAME}, all: [has: {field: type, regex: '^string$'}, not: {regex: ','}, inside: …]}`)
  in Plan 01 Task 2's `<action>`, and make the acceptance criterion a *match count*, not "loads
  cleanly".

- **[LOW] C2-L2 — the internal variant's excluded false positive is missing from the "14 verified
  false positives" enumeration.** [02.1-01-PLAN.md:625-628](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md)
  lists 14 external exclusions. The `characterWriter` constraint also excludes one *internal* match,
  `internal/world/mutator.go:231` (`m.characterWriter.UpdateLocation(txCtx, characterID, …)` — a
  repository write). Confirmed genuine false positive and correctly excluded; it is mentioned only
  in the ignore-table note at [:713](.planning/phases/02.1-world-caller-model/02.1-01-PLAN.md) and
  should join the enumeration so the exclusion set is fully accounted (15 total across both
  variants).

- **[LOW] C2-L3 — Plan 02 Task 2's scoped `task test` drops `./internal/property/`.**
  Task 1's gate ([:334](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md)) includes it;
  Task 2's ([:549](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md),
  [:588](.planning/phases/02.1-world-caller-model/02.1-02-PLAN.md)) does not, even though Task 2
  migrates call sites that reach the D-66 chain. Add `./internal/property/` for symmetry.

- **[LOW] C2-L4 — Plan 03 Task 3's codemod re-run does not state where landmark commit 3 falls
  relative to its "clean working directory" precondition.** *Raised by `codex`.* Tasks 1-2 create
  the census test and edit the invariant registry; unless those are committed first, the
  `git diff --exit-code` check fails on unrelated planned changes. Make the ordering explicit
  (commit, then re-run, then compare).

- **[LOW] C2-L5 — `.planning/STATE.md`'s supersession record carries superseded intermediate
  figures.** *Raised by `pi`.* `.planning/STATE.md:530` records "~332 true test sites" and "~106
  test-double method declarations"; the plans' reconciled figures (355 / 309 / 162) supersede those
  and Plan 02's `key_links` say so. All three plans forbid hand-editing STATE.md, so add
  "annotate the STATE.md supersession record with the final reconciled figures" to Plan 03 Task 3's
  ship-time follow-up list rather than editing it now.

### Divergent Views

- **Empty-subject subtest count.** `codex` says nine (REFUTED the plan's eight); `pi` recorded
  "8-of-9 empty-subject" as VERIFIED, apparently accepting the reviewer brief's premise rather than
  reading `service_test.go:6251`. Independent read of `:6236-6256` settles it for `codex`: nine.
- **`375 → 370`.** `codex` reported it VERIFIED against the whole-tree naive rule; `pi` flagged the
  mixed basis explicitly. Both measurements are right; only the labelling is at issue (C2-M3).
- **Overall risk.** `codex` says HIGH, `pi` says MEDIUM. The difference is scope: `pi` verified the
  mechanics deeply and found one narrow rule defect; `codex` weighted the plan-text contradictions.
  With six HIGHs standing, this session concurs with **HIGH** — but notes that every one of them is
  a plan-text or gating defect, not a design or security defect. The authorization design, the S1
  double-gate reasoning, and the `HumanCaller("system")` mapping all survived a third independent
  pass.

---

## Source-grounding pass (cycle 2)

`plan_review.source_grounding: true`; authority resolved via
`gsd-tools drift-guard authority --raw` → **`intel`**. Severity mapping under that authority
(`drift-guard severity`): VERIFIED → none; MISSING → needs-acknowledgement (`hardBlock: false`);
AMBIGUOUS → MEDIUM (`hardBlock: false`); UNCHECKABLE → INFO (`hardBlock: false`). **No `hardBlock`
was raised, so the convergence cycle continues.**

Every repo symbol cited by the three plans was extracted by kind (types/interfaces, funcs/methods,
pre-existing test names, `path:line` citations, Taskfile tasks, invariant and requirement ids),
excluding symbols declared as phase artifacts, and each was resolved against the tree.

### Result

- **MISSING: 0.** Every pre-existing type, interface, method, test function, file, line range,
  Taskfile task, invariant id and requirement id that the plans cite resolves. The only absent paths
  are phase artifacts (excluded by construction) or the D-65 path the plan itself already flags as
  non-existent.
- **AMBIGUOUS: 2** (both MEDIUM under `intel`, neither a hard block):
  1. `02.1-02-PLAN.md:698-700` — `UpdateLocation :84`, `UpdateObject :88`, `FindLocationByName :92`
     in `corebuilding_brokered_command_test.go` are actually `:89`, `:93`, `:97`. The *set* is
     correct; the coordinates land inside neighbouring method bodies. (= C2-M2.)
  2. `02.1-02-PLAN.md:696` — "eight fully-unnamed declarations" heads a seven-item enumeration.
     (= C2-M5.)
- **VERIFIED: ~80 symbols**, each with a `file:line`. Spot highlights:
  `world.Service.checkAccess` `internal/world/service.go:209`; `buildIntent` `:350`;
  `buildMoveIntent` `:1062`; `world.Mutator` `internal/world/mutator.go:22` and its
  `var _ Mutator = (*Service)(nil)` at `:61`; `grpc.WorldQuerier` `internal/grpc/server.go:129`;
  `command.WorldService` `internal/command/types.go:38` with its assertion at `:110`;
  Deprecated `hostfunc.WorldService` `internal/plugin/hostfunc/adapter.go:23` and the
  `hostfunc.WorldMutator`/`hostcap.WorldMutator` aliases at `adapter.go:32` /
  `capabilities.go:52`; `property.WorldQuerier` `internal/property/registry.go:43`,
  `property.WorldMutator` `:49`, `property.Definition` `:55`, `Definition.Set` `:58`;
  `property.EntityMutator` `internal/property/entity_mutator.go:25`;
  `bootstrap.worldSeeder` `internal/bootstrap/setting.go:29`;
  `types.NewAccessRequest` `internal/access/policy/types/types.go:143`;
  `Engine.Evaluate` `internal/access/policy/engine.go:82` with the S1 exact-equality test at `:92`
  and `IsSystemContext` at `:93`; the `bags.Action` overlay at `:252`/`:258`;
  `access.WithSystemSubject` `internal/access/context.go:12`;
  `wmodel.IntentParams.Actor` `internal/world/wmodel/envelope.go:50`;
  `policytest.AllowAllEngine` `internal/access/policy/policytest/helpers.go:19`;
  `TestWorldEnvelopeCensusMatchesServiceMutatingMethods` `test/meta/world_envelope_census_test.go:187`;
  the registry gates `TestEveryRegistryInvariantHasBinding` `:232`, `TestProvenanceGuard` `:857`,
  `TestBoundInvariantsAreGenuinelyAsserted` `:1128`; `INV-ACCESS` scope
  `docs/architecture/invariants.yaml:571`; `AUTHZ-01` `.planning/REQUIREMENTS.md:256`.

### Verification coverage

**UNCHECKABLE (INFO under `intel`) — never counted as verified:**

1. Every "this will not compile after the flip" claim: the 13 compile-time interface assertions,
   `recordingWorldMutator`'s 7 unnamed methods, `objects_integration_test.go:63`/`:71`,
   `hostcap/property.go:84`, the three `cascade_delete_test.go` sites, and the two
   `internal/property` test files in C2-H5. The *declarations* are VERIFIED to exist with the shapes
   claimed; whether each breaks post-flip is a signature-compatibility claim, and under `intel`
   authority that cannot be asserted without a compile. C2-H5 is therefore reported as an
   **enumeration-completeness and `files_modified`** defect — which *is* statically provable — not as
   a compile prediction.
2. "`hostfunc.WorldMutator` / `hostcap.WorldMutator` migrate for free" — the aliases are VERIFIED;
   that aliasing suffices post-flip is a compile claim.
3. "`world.Mutator` / `command.WorldService` / `hostfunc.WorldService` sit half-migrated after this
   plan and compile fine" (`02.1-01-PLAN.md:135`) — Go permits it in principle; this tree was not
   compiled in a half-migrated state.
4. The post-flip probe residual "exactly 8" and the codemod "re-run → empty diff" contract — both
   require the migrated tree, which does not exist yet. (C2-H1 shows the re-run contract is
   *unsatisfiable as pinned*, which is a static property of the rule and IS reported.)
5. Runtime behaviour of every test the phase creates — attribute reaching `action.<key>`, S1 bypass
   without an ambient marker, outbox-actor byte identity — the tests are not written yet.
6. `abac-reviewer` returning READY, `task pr-prep` and unscoped `task test:int` outcomes — agent
   verdicts and future runs, not static facts.

**Skipped, with reason:**

- stdlib / third-party symbols (`context.Context`, `ulid.ULID`, `require.*`, `assert.*`,
  `slog.*`, `ast.Inspect`) — outside repo-symbol scope.
- The three declared intentional divergences (stale ROADMAP blast-radius block and Goal line; D-65's
  `internal/world/world_envelope_census_test.go` path, which `02.1-03-PLAN.md:113` itself flags as
  non-existent; the `validateAttributes` path — `internal/access/policy/compiler.go:154` spot-checked
  inside the cited `149-171`).
- The seventh grouped `_, _ string` site (`internal/plugin/hostfunc/world_test.go:592`) was counted
  by the probe but not individually opened; the other six were, and all are `FindLocationByName` as
  claimed.

**Deliberately excluded, not skipped — phase artifacts:** `world.Caller`, `world.HumanCaller`,
`world.SystemCaller`, `internal/world/caller.go`, `internal/world/caller_test.go`, `INV-WORLD-8`,
`test/meta/world_caller_census_test.go`, the five `scripts/codemod/*.yml` rules and their
`README.md`, and the ten new `Test*` names the plans say they will write (including
`TestWorldCallerSubjectReachesTheOutboxActorByteForByte`).

---

## Cycle 2 verdict

**6 HIGH unresolved, 10 actionable non-HIGH unresolved.** The replan's substance held up under
independent re-execution: every ast-grep rule behaves as pinned, every reconciled figure reproduces,
the self-reported idempotency bug is real and its guard works for the constructor-call case, and the
security-relevant `HumanCaller("system")` mapping is correct for the third consecutive pass. What
remains is one genuinely new rule defect (C2-H1), two acceptance-criteria contradictions the replan
introduced while reconciling counts (C2-H2, C2-H3), one commit-mapping error (C2-H4), and two
regions the replan's own census lenses excluded (C2-H5, C2-H6). None is a design or authorization
defect. Another convergence cycle is warranted.
