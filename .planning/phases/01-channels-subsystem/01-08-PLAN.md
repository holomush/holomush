---
phase: 01-channels-subsystem
plan: 08
type: tdd
wave: 6
depends_on: ["01-02", "01-05", "01-07"]
files_modified:
  - plugins/core-channels/main.go
  - plugins/core-channels/service.go
  - plugins/core-channels/plugin.yaml
  - plugins/core-channels/session_streams_test.go
requirements: [CHAN-01, CHAN-02]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "core-channels implements SessionStreamsHandler.QuerySessionStreams, returning events.<game>.channel.<id> for the UNION of (the character's non-banned memberships) ∪ (seeded default channels the character is not banned from), deduplicated by channel id"
    - "a guest session (no membership rows) receives EXACTLY the seeded default channels via the ListDefaultChannels ∪ memberships union — guest auto-join without a membership row (full Epic-10 initial scope, D-01; resolves RESEARCH landmine #4's guest-serving half)"
    - "channel join subscribes the live session to the channel stream mid-session via the stream.subscription client with LIVE_ONLY (no history flood)"
    - "channel leave unsubscribes the live session (RemoveStream)"
    - "manifest declares session_streams: true and requires: [capability: stream.subscription]"
  artifacts:
    - "plugins/core-channels/main.go (SessionStreamsHandler impl + StreamSubscriptionAware wiring)"
    - "plugin.yaml: session_streams: true; requires stream.subscription"
  key_links:
    - "QuerySessionStreams (01-02 SDK hook) delivers channel streams at connect/reconnect; join/leave use the stream.subscription capability (01-02) for mid-session delta"
    - "a member connected at establishment receives channel_say live; a non-member does NOT (delivery honors membership)"
    - "guest sessions receive ONLY the seeded default channels (no membership rows, no create/moderate) — served by unioning `store.ListDefaultChannels` (seeded in 01-03) into QuerySessionStreams (D-01)"
  prohibitions:
    - statement: "A non-member MUST NOT receive a channel_say event (live delivery honors membership)"
      status: pending
      verification: "integration test (01-09): member receives channel_say, non-member does not"
---

<objective>
Wire the plugin half of live delivery onto the substrate landed in 01-02: implement `QuerySessionStreams` (session-establishment subscription) and use the `stream.subscription` capability for mid-session `join`/`leave` deltas (LIVE_ONLY). This is the CHAN-01/CHAN-02 live-delivery path.

**Guest auto-join mechanism (RESEARCH landmine #4 resolution — chosen shape):** guest auto-join is served by **unioning the seeded default channels into `QuerySessionStreams`, NOT by writing a membership row at session establishment.** `QuerySessionStreams` returns the deduplicated union of (a) the character's non-banned memberships and (b) `store.ListDefaultChannels` (the `is_default=true` set seeded in 01-03), minus any default the character is banned from. A guest — a session with zero membership rows — therefore receives exactly the seeded default channels. This keeps membership resource-side (no principal RPC, no session-establishment write) and plaintext (D-04). Seeding (01-03) is upstream of this serving via the transitive DAG (01-08 → 01-05 → 01-03).

Purpose: a connected member sees channel messages in real time; a guest is auto-joined to the seeded channels at connect; joining mid-session starts live delivery without reconnect; leaving stops it.
Output: `SessionStreamsHandler` impl (memberships ∪ ListDefaultChannels) + join/leave stream mutation + manifest `session_streams`/`requires`; unit tests green (the full member-vs-non-member delivery assertion runs in the 01-09 integration).

**SCOPING-RISK dependency (Landmine 2):** This plan consumes 01-02's substrate. If 01-02's mid-session serving (`stream.subscription`) landed only partially (per its documented degradation fallback), Task 2 here degrades to "join takes effect on next session-stream refresh" and MUST be flagged in the SUMMARY — do NOT silently drop it. Task 1 (session-establishment via `QuerySessionStreams`) is the delivery floor and depends only on 01-02 Task 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-channels-subsystem/01-CONTEXT.md
@.planning/phases/01-channels-subsystem/01-RESEARCH.md

# The substrate this plan consumes (from 01-02):
@pkg/plugin/session_streams_handler.go
@pkg/plugin/stream_subscription_client.go
# Reference for session-establishment stream contribution (Lua analog behavior):
@internal/plugin/lua/host.go
# Rules:
@.claude/rules/plugin-runtime-symmetry.md
@.claude/rules/event-conventions.md
# Prior artifacts:
@plugins/core-channels/store.go
@plugins/core-channels/service.go
@plugins/core-channels/publish_events.go
</context>

## Artifacts this plan produces

- `plugins/core-channels/main.go` — `channelPlugin` implements `pluginsdk.SessionStreamsHandler.QuerySessionStreams` returning the deduplicated union of the character's non-banned memberships and `store.ListDefaultChannels` (seeded in 01-03) minus banned defaults; receives the `StreamSubscription` client via `StreamSubscriptionAware` at Init.
- `plugins/core-channels/service.go` — `JoinChannel` calls `streamSub.AddStream(ctx, sessionID, dotStyleChannelSubject(...), LIVE_ONLY)`; `LeaveChannel` calls `streamSub.RemoveStream(...)`.
- `plugin.yaml`: `session_streams: true`; `requires: [{capability: stream.subscription, access: write}]`.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: QuerySessionStreams — session-establishment channel subscription (RED→GREEN)</name>
  <files>plugins/core-channels/main.go, plugins/core-channels/plugin.yaml, plugins/core-channels/session_streams_test.go</files>
  <read_first>pkg/plugin/session_streams_handler.go (the SessionStreamsHandler interface from 01-02); plugins/core-channels/store.go (ListForCharacter + ban filter + ListDefaultChannels seeded in 01-03); internal/plugin/lua/host.go:700-780 (on_session_subscribe semantics — the streams a plugin returns); .claude/rules/event-conventions.md (subject format)</read_first>
  <behavior>
    - QuerySessionStreams(req{CharacterID, PlayerID, SessionID}) returns events.<game>.channel.<id> for the UNION of (a) every channel the character is a non-banned member of (`ListForCharacter` minus bans) and (b) every seeded default channel (`store.ListDefaultChannels`) the character is not banned from — deduplicated by channel id (a default the character is also an explicit member of appears once).
    - A guest session (no membership rows) receives EXACTLY the seeded default channels — guest auto-join is the default-channel union, NOT a membership-row write at session establishment (resource-side; no principal RPC). Guests get no create/moderate surface (that is the command/service layer in 01-05/01-07).
    - A character with no explicit memberships still receives the seeded default channels; the returned list is empty (no error) ONLY when there are neither memberships nor seeded default channels.
    - A character banned from a default channel does NOT receive that default's stream (the ban filter applies to defaults too).
    - The subject format matches the emit subject exactly (so delivery lands on the same stream content is emitted to).
  </behavior>
  <action>Implement `func (p *channelPlugin) QuerySessionStreams(ctx, req pluginsdk.SessionStreamsRequest) ([]string, error)`: query the store for (a) the character's non-banned memberships (`ListForCharacter` filtered by ban) and (b) the seeded default channels (`store.ListDefaultChannels`, landed in 01-03), UNION and dedupe by channel id, drop any default the character is banned from, map each to `dotStyleChannelSubject(gameID, chID)`, return the list. Empty list (not error) only when there are neither memberships nor seeded default channels — a guest (no memberships) therefore receives exactly the default set with NO membership-row write. Declare `session_streams: true` in the manifest. Write `session_streams_test.go`: member channels returned; banned membership excluded; a guest with no memberships gets exactly `ListDefaultChannels`; a member+default overlap is deduped to one; a default the character is banned from is excluded; empty only when no memberships and no defaults; subject format matches the emit subject.</action>
  <verify>
    <automated>task test -- ./plugins/core-channels/</automated>
  </verify>
  <done>QuerySessionStreams returns the deduplicated union of the character's non-banned memberships and the seeded default channels (banned defaults excluded); a guest with no memberships gets exactly `ListDefaultChannels`; manifest declares session_streams: true; `task test -- ./plugins/core-channels/` green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Mid-session join/leave stream mutation via stream.subscription (RED→GREEN)</name>
  <files>plugins/core-channels/service.go, plugins/core-channels/plugin.yaml, plugins/core-channels/main.go, plugins/core-channels/service_test.go</files>
  <read_first>pkg/plugin/stream_subscription_client.go (AddStream/RemoveStream + LIVE_ONLY mode from 01-02); plugins/core-channels/service.go (JoinChannel/LeaveChannel from 01-05); .claude/rules/plugin-runtime-symmetry.md</read_first>
  <behavior>
    - JoinChannel, after committing membership, calls streamSub.AddStream(ctx, sessionID, channelSubject, LIVE_ONLY) so the live session starts receiving new channel events without a history flood.
    - LeaveChannel, after removing membership, calls streamSub.RemoveStream(ctx, sessionID, channelSubject).
    - A stream-subscription error is surfaced (logged, and the join/leave result reflects partial success) — membership state and subscription state are kept consistent.
    - When the caller has no active session id (e.g. an out-of-session admin action), the mutation is skipped (no crash).
  </behavior>
  <action>Extend `JoinChannel`/`LeaveChannel` to call the injected `StreamSubscription` client after the store mutation: join → `AddStream(sessionID, dotStyleChannelSubject(...), LIVE_ONLY)`, leave → `RemoveStream(...)`. Wire the client via `StreamSubscriptionAware` in `main.go` Init and thread it to the service. Declare `requires: [{capability: stream.subscription, access: write}]` in the manifest (fail-closed at load if omitted — 01-02 enforces this). Handle a missing session id gracefully. Log inner errors via `slog.*Context`; do not leak. Extend `service_test.go`: join adds LIVE_ONLY subscription, leave removes it, no-session-id skips cleanly, subscription error handled.</action>
  <verify>
    <automated>task test -- ./plugins/core-channels/</automated>
  </verify>
  <done>Mid-session join/leave add/remove the channel stream subscription (LIVE_ONLY) via the stream.subscription capability; manifest requires it; `task test -- ./plugins/core-channels/` green. If 01-02 mid-session serving was only partial, degradation flagged in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session establishment → stream set | The plugin decides which streams a session receives; a bug could subscribe a non-member |
| mid-session join → live delivery | Joining must only ever subscribe the caller's own session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information disclosure | non-member receives live channel_say | high | mitigate | QuerySessionStreams returns the caller's own non-banned memberships unioned with the seeded default channels (public-by-design, D-01), banned excluded from both; join subscribes only the caller's own sessionID |
| T-01-20 | Information disclosure | banned member still receiving live | medium | mitigate | Ban excludes the channel from QuerySessionStreams; kick/ban triggers RemoveStream (01-07 moderation → leave path) |
| T-01-09 | Denial of service | history flood on mid-session join | low | mitigate | AddStream uses LIVE_ONLY (advance cursor to tail, no replay) |
| T-01-SC | Tampering | package installs | n/a | accept | No package installs; SC checkpoint does not fire |
</threat_model>

<verification>
- `task test -- ./plugins/core-channels/` green (session-streams + join/leave subscription unit).
- Member-vs-non-member LIVE delivery + mid-session join asserted in the 01-09 integration.
- `task lint` green (session_streams + stream.subscription manifest declarations).
</verification>

<success_criteria>
A connected member receives channel messages live; joining mid-session starts live delivery via LIVE_ONLY without reconnect; leaving stops it; a guest (no memberships) receives exactly the seeded default channels via the memberships ∪ `ListDefaultChannels` union (D-01). CHAN-01/CHAN-02 live-delivery path complete (or its mid-session increment degraded-and-flagged per the Landmine 2 fallback).
</success_criteria>

<output>
Create `.planning/phases/01-channels-subsystem/01-08-SUMMARY.md` when done. If mid-session subscription degraded due to a partial 01-02, state the degradation explicitly.
</output>
