---
phase: 01-channels-subsystem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/proto/holomush/channel/v1/channel.proto
  - pkg/proto/holomush/channel/v1/
  - web/src/lib/proto/
requirements: [CHAN-01, CHAN-02]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "buf generates holomush.channel.v1.ChannelService Go + web bindings without lint failure"
    - "ChannelService exposes create/join/leave/list/post/who/history + moderation RPC surface mirroring holomush.scene.v1.SceneService"
    - "task lint:proto is green (every element carries a Go-grounded doc comment; no name-echo)"
  artifacts:
    - "api/proto/holomush/channel/v1/channel.proto"
    - "pkg/proto/holomush/channel/v1/channel.pb.go (generated, committed)"
    - "pkg/proto/holomush/channel/v1/channelv1connect/ (generated, committed)"
  key_links:
    - "generated *.pb.go committed in the SAME change as the .proto (stale-diff CI gate)"
    - "ChannelService request/response shapes are the contract service.go (01-05) implements"
  prohibitions: []
---

<objective>
Define the `holomush.channel.v1.ChannelService` proto — the typed service surface `core-channels` provides — and regenerate + commit its Go/web bindings. This is the substrate-parity contract mirroring `holomush.scene.v1.SceneService` (CHAN-05 makes shared substrate patterns visible). service.go (01-05) implements it; a future web BFF consumes it.

Purpose: Establish the typed contract before the service implementation so 01-05 has a concrete generated type to implement against (interface-first).
Output: `api/proto/holomush/channel/v1/channel.proto` + committed generated bindings; `task lint:proto` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-channels-subsystem/01-CONTEXT.md
@.planning/phases/01-channels-subsystem/01-RESEARCH.md

# Reference contract to mirror (read the proto that defines the shipped analog):
@api/proto/holomush/scene/v1/scene.proto
# Proto doc-comment rules (HARD gate — every element needs a Go-grounded comment):
@.claude/rules/proto-doc-comments.md
</context>

## Artifacts this plan produces

- `api/proto/holomush/channel/v1/channel.proto` — new `ChannelService` (create/join/leave/list/post/who/history/invite/mute/ban/kick/transfer) + request/response messages.
- `pkg/proto/holomush/channel/v1/channel.pb.go`, `channel_grpc.pb.go`, `channelv1connect/channel.connect.go` — generated, committed.
- Web TS bindings under `web/src/lib/proto/` (generated by `task web:generate`, committed).

<tasks>

<task type="execute">
  <name>Task 1: Define holomush.channel.v1.ChannelService proto</name>
  <files>api/proto/holomush/channel/v1/channel.proto</files>
  <read_first>api/proto/holomush/scene/v1/scene.proto (the analog contract); .claude/rules/proto-doc-comments.md; plugins/core-scenes/service.go (ground each RPC comment in what the handler will do)</read_first>
  <action>Create `api/proto/holomush/channel/v1/channel.proto`, package `holomush.channel.v1`, `option go_package = ".../pkg/proto/holomush/channel/v1;channelv1"`. Mirror `holomush.scene.v1.SceneService` structurally. Declare a `ChannelService` with RPCs: `CreateChannel`, `JoinChannel`, `LeaveChannel`, `ListChannels`, `PostToChannel`, `WhoInChannel`, `QueryChannelHistory`, `InviteToChannel`, `MuteMember`, `BanMember`, `KickMember`, `TransferOwnership`. Message fields per the entity model in RESEARCH § Postgres schema: channel `id`/`name`/`type`(public|private|admin)/`owner_id`/`archived`/`retention_days`; membership `character_id`/`role`(owner|member)/`muted`/`banned`/`joined_at`. Use `buf.validate` field constraints as scene.proto does (name `min_len`, etc.). Channel identity flows by ID; do NOT add a `channel_name` field to any content/post message (D-08 — identity is subject+live-name-lookup, and the spec forbids payload channel_name for authz). Every message, field, RPC, service element MUST carry a Go-grounded leading doc comment describing what the corresponding service.go handler will do (ground against plugins/core-scenes/service.go semantics; no name-echo — the name-echo gate rejects `// CreateChannelRequest`). Do NOT declare any crypto/sensitive fields (D-04 plaintext). Do NOT put fenced code inside doc comments.</action>
  <verify>
    <automated>test -f api/proto/holomush/channel/v1/channel.proto</automated>
  </verify>
  <done>channel.proto exists with ChannelService + all listed RPCs and messages, each element doc-commented; no payload channel_name field on content messages.</done>
</task>

<task type="execute">
  <name>Task 2: Regenerate and commit Go + web bindings; lint:proto green</name>
  <files>pkg/proto/holomush/channel/v1/, web/src/lib/proto/</files>
  <read_first>Taskfile.yaml (proto + web:generate task definitions)</read_first>
  <action>Run `task proto && task web:generate` to generate Go bindings under `pkg/proto/holomush/channel/v1/` (including the `channelv1connect/` ConnectRPC sub-package) and the web TS bindings. Run `task fmt` (SPDX headers / formatting on any touched Go). Commit ALL generated output in this same change (CI runs a stale-generated-diff check — uncommitted generated code fails). Then confirm `task lint:proto` is green. Do NOT hand-edit any `*.pb.go` (generated).</action>
  <verify>
    <automated>task lint:proto</automated>
  </verify>
  <done>Generated `channel.pb.go` + `channelv1connect/` + web bindings exist and are committed; `task lint:proto` passes; `git status` shows no uncommitted generated diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proto contract → generated code | A malformed/undocumented proto element ships an unsafe or ambiguous contract downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information disclosure | ChannelService message shape | low | mitigate | No content/sensitive fields declared (plaintext, D-04); channel identity by ID not payload name (D-08) so a rename cannot leak stale authz context |
| T-01-SC | Tampering | package installs | n/a | accept | This plan installs NO packages (all in-tree/vendored per RESEARCH § Package Legitimacy Audit); crypto-reviewer/SC checkpoint does not fire |
</threat_model>

<verification>
- `task lint:proto` green (doc-comment + name-echo gate).
- `git status` clean after `task proto && task web:generate && task fmt` (no uncommitted generated diff).
</verification>

<success_criteria>
`holomush.channel.v1.ChannelService` proto + committed generated Go/web bindings exist; `task lint:proto` passes; downstream service.go (01-05) has a concrete generated type to implement.
</success_criteria>

<output>
Create `.planning/phases/01-channels-subsystem/01-01-SUMMARY.md` when done.
</output>
