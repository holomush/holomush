---
phase: 01-channels-subsystem
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - pkg/plugin/session_streams_handler.go
  - pkg/plugin/sdk.go
  - pkg/plugin/stream_subscription_client.go
  - pkg/plugin/capability_declaration.go
  - internal/plugin/manager.go
  - internal/grpc/stream_registry.go
  - internal/grpc/server.go
  - internal/plugin/hostcap/servers.go
  - internal/plugin/hostcap/capabilities.go
  - internal/plugin/hostcap/register.go
  - internal/plugin/pluginauthz/streamsubscribe.go
  - internal/access/policy/seed.go
requirements: [CHAN-01, CHAN-02]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "A binary plugin implementing an optional SessionStreamsHandler has its QuerySessionStreams RPC routed to that handler (no longer Unimplemented)"
    - "Manager.QuerySessionStreams ACCEPTS a dot-style events.<game>.channel.<id> plugin session stream (isValidStreamName no longer drops non-colon subjects) — addresses review HIGH-1 (dot-eradication contract drift, manager.go:1496-1506,1566-1571 vs server.go:707-733/qualify.go)"
    - "The stream.subscription host capability (AddSessionStream/RemoveSessionStream) is SERVED — it calls the host StreamRegistry instead of returning codes.Unimplemented"
    - "STREAM_REPLAY_MODE_LIVE_ONLY is IMPLEMENTED end-to-end: AddStreamWithMode ACCEPTS ReplayModeLiveOnly (no longer REPLAY_MODE_NOT_SUPPORTED) and a mid-session-added stream delivers only new events (no history flood) — addresses review HIGH-2 (stream_registry.go:190-199 rejection + advisory-mode comment :17-29)"
    - "AddSessionStream runs an instance-level concrete-stream authorization guard before mutating a session (qualify → reject wildcards/forbidden namespaces → verify the stream is in a namespace the calling plugin owns), mirroring stream.history's in-handler AuthorizeStreamRead — addresses review HIGH-3 (servers.go:829-854 precedent; no instance guard exists for writes today, seed.go:443-444)"
    - "A binary plugin declaring `requires: [capability: stream.subscription]` receives a StreamSubscription client via an *Aware interface at Init"
  artifacts:
    - "pkg/plugin/session_streams_handler.go (new SessionStreamsHandler interface)"
    - "pkg/plugin/stream_subscription_client.go (new StreamSubscription client + StreamSubscriptionAware)"
    - "internal/plugin/hostcap/servers.go (streamSubscriptionServer real impl WITH concrete-stream authz guard)"
    - "internal/plugin/pluginauthz/streamsubscribe.go (AuthorizeStreamSubscribe: qualify + wildcard/namespace reject + owned-namespace check)"
    - "internal/access/policy/seed.go (seed:plugin-stream-subscribe instance-level write permit; audit/crypto/system forbids override)"
    - "internal/grpc/stream_registry.go (AddStreamWithMode accepts ReplayModeLiveOnly)"
    - "internal/plugin/manager.go (isValidStreamName accepts dot-style events. subjects)"
  key_links:
    - "pluginServerAdapter detects SessionStreamsHandler exactly as it detects CommandHandler (sdk.go GRPCServer)"
    - "streamSubscriptionServer bodies call the HostCapabilities port → host StreamRegistry (AddStreamWithMode/RemoveStream, internal/grpc/stream_registry.go) AFTER the concrete-stream authz guard passes"
    - "LIVE_ONLY no-history-flood is guaranteed by SetFilters preserving the live consumer's start policy on filter rotation (subscriber.go:348-351) — the registry accepting the mode is the fix, proven by a subscribe-loop test"
    - "capability_declaration.go maps StreamSubscriptionAware → token stream.subscription (fail-closed if undeclared)"
    - "the concrete-stream authz guard reuses eventbus.Qualify + the audit/crypto/system stream forbids exactly as AuthorizeStreamRead does, so both runtimes and both stream operations (read/subscribe) enforce identically (plugin-runtime-symmetry)"
  prohibitions:
    - statement: "A plugin that does NOT declare capability stream.subscription MUST NOT be able to mutate another session's subscriptions"
      status: pending
      verification: "validateDeclaredCapabilities fails load when StreamSubscriptionAware implemented without the declared token (unit test)"
    - statement: "A plugin granted stream.subscription MUST NOT subscribe a session to a stream outside a namespace it owns (e.g. events.<game>.system.*, audit/crypto namespaces, or another plugin's domain) — HIGH-3"
      status: pending
      verification: "servers_test: AddSessionStream to a foreign/system/wildcard stream is rejected (PermissionDenied/InvalidArgument); AddSessionStream to the plugin's own domain is permitted"
    - statement: "The LIVE_ONLY change MUST NOT alter FROM_CURSOR replay for existing scene session streams (regression) — HIGH-2 additive"
      status: pending
      verification: "stream_registry / subscribe-loop test: a FROM_CURSOR add still replays from the consumer's start policy; only LIVE_ONLY newly-added streams skip history"
---

<objective>
Close the net-new live-delivery substrate gaps (Landmine 2) so `core-channels` — the FIRST consumer of session-stream subscription — can deliver live messages. Cross-AI review (Codex, source-grounded) confirmed three of these are genuine blockers/security gaps, not just "unserved" stubs; each is now first-class plan work (HIGH-1/2/3):

1. **Session-establishment path:** add the binary-SDK `SessionStreamsHandler` hook so a binary plugin's `QuerySessionStreams` RPC routes to plugin code (today `pluginServerAdapter` embeds `UnimplementedPluginServiceServer` → `codes.Unimplemented`). Lua parity already exists (`on_session_subscribe`, `internal/plugin/lua/host.go`), so this CLOSES a binary/Lua gap rather than opening one. **AND (review HIGH-1)** fix `Manager.QuerySessionStreams` so it does not DROP the dot-style channel streams the plugin returns: `isValidStreamName` (`internal/plugin/manager.go:1496-1506`) currently requires a colon and drops non-colon streams (`:1566-1571`), contradicting the dot-eradication in `internal/grpc/server.go:707-733` + `internal/eventbus/qualify.go`. Make it accept dot-relative / full `events.` subjects that `eventbus.Qualify` accepts.
2. **Mid-session join/leave path:** serve the `stream.subscription` host capability (`StreamSubscriptionService.AddSessionStream`/`RemoveSessionStream` — proto-declared but server-stubbed as `codes.Unimplemented`, tracked `holomush-l6std`) by wiring it to the existing host `StreamRegistry` (`internal/grpc/stream_registry.go`), and add the binary-SDK client + `*Aware` interface so a plugin can call it. **This entails two review findings that are net-new work, not free:**
   - **(HIGH-2) Implement `LIVE_ONLY` end-to-end.** `SessionStreamRegistry.AddStreamWithMode` (`stream_registry.go:190-199`) today REJECTS any mode ≠ `ReplayModeFromCursor` with `REPLAY_MODE_NOT_SUPPORTED`; serving the RPC alone does NOT deliver "no history flood." Make `AddStreamWithMode` ACCEPT `ReplayModeLiveOnly` (additive; keep FROM_CURSOR for scenes, keep rejecting BoundedTail if still unsupported) and PROVE with a subscribe-loop test that a mid-session-added stream delivers only new events. The no-history-flood guarantee is available because `SetFilters` preserves the live consumer's start policy on filter rotation (`internal/eventbus/subscriber.go:348-351`) — the registry accepting the mode is the fix; the test locks the guarantee.
   - **(HIGH-3, SECURITY) Add an instance-level concrete-stream authorization guard.** Both subscription RPCs map to `write` on `stream` (`internal/plugin/hostcap/descriptor.go:154-157`) and the seed default-permits declared plugins at `resource == "stream:*"` at the TYPE level (`internal/access/policy/seed.go:423-427`); the seed comment (`:443-444`) states stream WRITES are "gated solely by the type-level capability" — so without a per-call guard, ANY plugin granted `stream.subscription` could ask the host to subscribe an active session to ARBITRARY streams (including `events.<game>.system.*`, audit/crypto namespaces, or another plugin's domain). Mirror `stream.history`'s in-handler concrete-stream check (`internal/plugin/hostcap/servers.go:829-854` → `pluginauthz.AuthorizeStreamRead`): qualify the requested stream, reject wildcards and forbidden namespaces, and verify the stream is in a namespace the calling plugin OWNS (its declared `emits` domain). Add the SDK client + `*Aware` interface so a plugin can call the served capability.

Purpose: `core-channels` needs both paths — QuerySessionStreams for connect/reconnect subscription, AddStream/RemoveStream(LIVE_ONLY) for `channel join`/`leave` during a live session — and both must be SAFE and non-flooding.
Output: SDK `SessionStreamsHandler` + `StreamSubscription` client/Aware; dot-subject-accepting `QuerySessionStreams`; LIVE_ONLY-honoring registry; served `stream.subscription` capability WITH a concrete-stream authz guard + `seed:plugin-stream-subscribe`.

**abac-reviewer GATE (per orchestrator directive):** Task 2/3 touch `internal/access/policy/seed.go` and `internal/plugin/pluginauthz/` — the `abac-reviewer` domain gate MUST fire before push for this plan (CLAUDE.md § Pre-Push Review Gates; `/holomush-dev:review-abac`). Every host-side change here is SHARED substrate and MUST be ADDITIVE — preserve existing scene session-stream + stream.history behavior (add the FROM_CURSOR regression assertion and a stream.history-unchanged assertion).

**SCOPING RISK (flagged per orchestrator directive):** This is genuine host+SDK substrate plumbing, not plugin domain code. It is BOUNDED and well-scaffolded — the proto (`api/proto/holomush/plugin/host/v1/stream.proto`), the capability token (`internal/plugin/capability_vocab.go`), the auto-generated Lua bridge (`internal/plugin/luabridge/bindings_gen.go`), and the host `StreamRegistry` implementation all already exist; only the server bodies + binary-SDK client/hook + the LIVE_ONLY-accept + the concrete-stream authz guard are missing. If Task 2 (serving `stream.subscription`, i.e. mid-session mutation) proves larger than budgeted, the DELIVERY FLOOR is Task 1 alone: session-establishment delivery via `QuerySessionStreams` (with the HIGH-1 dot-subject fix) still gives every member their channel streams at connect/reconnect. Mid-session live `join` would then degrade to "subscribed on next session-stream refresh" rather than instantly — a UX degradation, NOT a dropped requirement. Do NOT silently drop mid-session delivery; if it cannot land, note the degradation explicitly in the SUMMARY and file a follow-up bead against `holomush-l6std`. The HIGH-3 authz guard is NOT descopable: `stream.subscription` MUST NOT be served without its concrete-stream gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-channels-subsystem/01-RESEARCH.md

# The gaps to close — read these to see the current stub state:
@pkg/plugin/sdk.go
@internal/plugin/hostcap/servers.go
@api/proto/holomush/plugin/host/v1/stream.proto
# Host-side wiring that already exists (do NOT rebuild):
@internal/plugin/host.go
# Parity precedent — how an existing host capability exposes a client + Aware:
@pkg/plugin/capability_declaration.go
# Runtime-symmetry rule (host-RPC parity):
@.claude/rules/plugin-runtime-symmetry.md
</context>

## Artifacts this plan produces

- `pkg/plugin/session_streams_handler.go` — `SessionStreamsHandler` interface (`QuerySessionStreams(ctx, SessionStreamsRequest) ([]string, error)`); `SessionStreamsRequest` SDK struct (CharacterID/PlayerID/SessionID).
- `pkg/plugin.pluginServerAdapter.QuerySessionStreams` — routes to the handler when implemented.
- `internal/plugin/manager.go` — `isValidStreamName` accepts dot-style `events.` subjects (HIGH-1): a channel stream `events.<game>.channel.<id>` is no longer dropped by `QuerySessionStreams`.
- `internal/grpc/stream_registry.go` — `AddStreamWithMode` accepts `ReplayModeLiveOnly` (HIGH-2), threading the mode through the ctrl update; FROM_CURSOR path unchanged.
- `internal/grpc/server.go` — if the mid-session ctrl path must honor LIVE_ONLY beyond SetFilters' cursor preservation, `applyFilterCtrl` consumes the mode (executor verifies against `subscriber.go:348-351`; the FROM_CURSOR regression assertion pins scene behavior).
- `pkg/plugin/stream_subscription_client.go` — `StreamSubscription` client interface + `StreamSubscriptionAware` opt-in interface + client impl over `StreamSubscriptionServiceClient`.
- `internal/plugin/hostcap` — real `streamSubscriptionServer` (AddSessionStream/RemoveSessionStream) WITH the concrete-stream authz guard (HIGH-3) + `HostCapabilities` port method(s) exposing the host `StreamRegistry`.
- `internal/plugin/pluginauthz/streamsubscribe.go` — `AuthorizeStreamSubscribe` (HIGH-3): qualify domain-relative stream, reject `events.`-prefixed/wildcard inputs, reject forbidden namespaces (system/audit/crypto), and enforce owned-namespace (the qualified subject's domain matches one of the calling plugin's declared `emits` domains).
- `internal/access/policy/seed.go` — `seed:plugin-stream-subscribe` (HIGH-3): instance-level `permit(principal is plugin, action in ["write"], resource is stream)` with the existing audit/crypto/system forbids overriding — the write analogue of `seed:plugin-stream-read`. abac-reviewer gate fires.
- `capability_declaration.go` entry: `StreamSubscriptionAware → {"stream.subscription"}`.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Binary SDK SessionStreamsHandler hook + dot-subject session-stream acceptance (HIGH-1) (RED→GREEN)</name>
  <files>pkg/plugin/session_streams_handler.go, pkg/plugin/sdk.go, pkg/plugin/session_streams_handler_test.go, internal/plugin/manager.go, internal/plugin/manager_test.go</files>
  <read_first>pkg/plugin/sdk.go (lines ~22-135: Handler/CommandHandler interfaces, grpcPlugin.GRPCServer CommandHandler detection at ~118, pluginServerAdapter embedding UnimplementedPluginServiceServer at ~133); internal/plugin/goplugin/host.go (host-side QuerySessionStreams call the adapter must satisfy); pkg/proto/holomush/plugin/v1 QuerySessionStreamsRequest/Response fields; internal/plugin/manager.go:1493-1506 (isValidStreamName — the colon requirement) + :1512-1579 (QuerySessionStreams merge/drop loop); internal/grpc/server.go:707-738 (computeInitialFilters/toSubject → eventbus.Qualify accepts dot-relative + full events. subjects, rejects colon); internal/eventbus/qualify.go:23 (Qualify signature — idempotent on events. subjects)</read_first>
  <behavior>
    - RED: a test constructs a pluginServerAdapter whose handler implements SessionStreamsHandler returning ["events.g.channel.c1"], invokes adapter.QuerySessionStreams with a request carrying character/player/session ids, and asserts the response streams == ["events.g.channel.c1"]. Fails today (adapter returns codes.Unimplemented via embed).
    - A plugin whose handler does NOT implement SessionStreamsHandler: adapter.QuerySessionStreams returns an empty (non-error) response — an event-only plugin must not break.
    - The SDK SessionStreamsRequest struct carries CharacterID, PlayerID, SessionID mapped 1:1 from the proto request.
    - HIGH-1 RED: `Manager.QuerySessionStreams` returning a dot-style plugin stream `events.g.channel.c1` KEEPS it (not dropped). Fails today because `isValidStreamName` requires a colon (`strings.Contains(name, ":")`), so a dot subject is dropped at manager.go:1567.
    - HIGH-1 regression: an empty/whitespace/oversized (>256) stream name is STILL dropped (degraded-subscribe policy preserved); a legacy colon-style ref is still accepted (additive, not a rewrite). NOTE: no in-tree plugin declares `session_streams: true` today (core-channels is first), so there is no existing plugin session-stream behavior to regress — the assertion pins the drop-policy for invalid names, not colon removal.
  </behavior>
  <action>Add `SessionStreamsHandler` optional interface + `SessionStreamsRequest` struct in a new `pkg/plugin/session_streams_handler.go` (parallel to `CommandHandler` in sdk.go). In `grpcPlugin.GRPCServer`, detect it exactly as CommandHandler is detected (`if h, ok := p.handler.(SessionStreamsHandler); ok { adapter.streamsHandler = h }`). Implement `func (a *pluginServerAdapter) QuerySessionStreams(ctx, *pluginv1.QuerySessionStreamsRequest) (*pluginv1.QuerySessionStreamsResponse, error)`: if `a.streamsHandler == nil` return an empty response (event-only plugins); else map proto→SDK request, call the handler, map `[]string` back to `QuerySessionStreamsResponse{Streams: ...}`; on handler error set the response `Error` field (mirror the proto contract — a plugin error degrades gracefully host-side per plugin.proto:46). Do NOT leak inner error text past the boundary (`.claude/rules/grpc-errors.md`); log with `slog.*Context`. This is the binary half of a Lua-parity feature (Lua `on_session_subscribe` already exists) — no new host RPC, so no additional Lua work. THEN (HIGH-1) fix `isValidStreamName` (`internal/plugin/manager.go:1496-1506`) so it accepts a dot-style plugin session stream: keep the empty/whitespace/length guards, and replace the bare `strings.Contains(name, ":")` requirement with an acceptance rule aligned to the rest of the dot-migration — a name is valid if it is a full `events.`-prefixed subject OR a domain-relative reference that `eventbus.Qualify` would accept (the same rule `server.go::toSubject` applies), while STILL accepting a legacy colon-style ref (additive). Verify the exact acceptance predicate against `eventbus.Qualify`/`server.go:728-738` before writing it; do not hand-roll a divergent validator. This aligns `Manager.QuerySessionStreams` with `internal/grpc/server.go:707-733` so the dot-style channel streams core-channels returns are delivered, not dropped.</action>
  <verify>
    <automated>task test -- ./pkg/plugin/ ./internal/plugin/</automated>
  </verify>
  <done>A binary plugin implementing SessionStreamsHandler has QuerySessionStreams routed to it; event-only plugins get an empty non-error response; a dot-style `events.<game>.channel.<id>` session stream is accepted by `Manager.QuerySessionStreams` (HIGH-1), invalid names still dropped; `task test -- ./pkg/plugin/ ./internal/plugin/` green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Serve stream.subscription SAFELY — LIVE_ONLY end-to-end (HIGH-2) + concrete-stream authz guard + seed (HIGH-3) (RED→GREEN)</name>
  <files>internal/plugin/hostcap/servers.go, internal/plugin/hostcap/capabilities.go, internal/plugin/hostcap/register.go, internal/plugin/pluginauthz/streamsubscribe.go, internal/access/policy/seed.go, internal/grpc/stream_registry.go, internal/plugin/hostcap/servers_test.go, internal/plugin/pluginauthz/streamsubscribe_test.go, internal/grpc/stream_registry_test.go</files>
  <read_first>internal/plugin/hostcap/servers.go:813-912 (streamHistoryServer.QueryStreamHistory — the concrete-stream authz precedent at :829-854 calling pluginauthz.AuthorizeStreamRead BEFORE reading) and :914-924 (streamSubscriptionServer stub); internal/plugin/pluginauthz/streamread.go (AuthorizeStreamRead: Qualify + STREAM_NOT_RELATIVE + wildcard reject + EvaluateCapabilityAccess on stream:<qualified>); internal/plugin/hostcap/capabilities.go (the HostCapabilities port — how SessionAccess/AccessEngine/Auditor/GameID/manifest are exposed); internal/plugin/host.go (StreamRegistry interface: AddStream/AddStreamWithMode/RemoveStream); internal/grpc/stream_registry.go:178-204 (AddStream/AddStreamWithMode REJECTING non-FromCursor at :190-199, RemoveStream) + :17-30 (advisory replayMode comment); internal/eventbus/subscriber.go:336-373 (SetFilters preserves the live consumer's start policy — the no-history-flood guarantee); internal/session/replay_mode.go (ReplayModeLiveOnly/BoundedTail); internal/access/policy/seed.go:410-450 (seed:plugin-cap-stream type-level + seed:plugin-stream-read instance-level READ permit + the ":443-444 stream writes gated solely by type-level" note + the audit/crypto/system forbids); api/proto/holomush/plugin/host/v1/stream.proto (StreamReplayMode enum mapping); plugins/core-scenes/plugin.yaml (emits: block — the "owned namespace" the guard checks against)</read_first>
  <behavior>
    - HIGH-2 RED (registry): `AddStreamWithMode(sessionID, stream, ReplayModeLiveOnly)` returns NO error (today it returns REPLAY_MODE_NOT_SUPPORTED at stream_registry.go:191-197). A subscribe-loop test asserts a stream ADDED mid-session with LIVE_ONLY delivers only events emitted AFTER the add (no historical replay); a stream added with FROM_CURSOR still replays per the consumer's start policy (regression — scene behavior unchanged). BoundedTail stays rejected if still unsupported.
    - Serve RED: streamSubscriptionServer.AddSessionStream (with a valid owned stream + declared plugin) calls the host StreamRegistry with (sessionID, stream, mode) mapped from the proto StreamReplayMode; RemoveSessionStream forwards (sessionID, stream) and is idempotent. STREAM_REPLAY_MODE_UNSPECIFIED → FROM_CURSOR; STREAM_REPLAY_MODE_LIVE_ONLY → ReplayModeLiveOnly. A registry error returns a generic gRPC status (no inner-error leak).
    - HIGH-3 RED (security): AddSessionStream for a stream OUTSIDE the calling plugin's owned namespace is REJECTED before any registry mutation — specifically: a wildcard/malformed subject → InvalidArgument; a system/audit/crypto namespace (e.g. `events.<game>.system.rekey.*`) → PermissionDenied (the forbids override); another plugin's domain (e.g. core-channels attempting `events.<game>.scene.<id>`) → PermissionDenied (owned-namespace check). AddSessionStream for the calling plugin's OWN declared `emits` domain (core-channels → `events.<game>.channel.<id>`) is PERMITTED.
    - HIGH-3 regression: stream.history read authorization is UNCHANGED (AuthorizeStreamRead behavior identical; the new write permit does not widen read access).
  </behavior>
  <action>Three cohesive sub-changes; the security guard is NOT descopable and gates the serving. (1) HIGH-2: make `SessionStreamRegistry.AddStreamWithMode` (`internal/grpc/stream_registry.go:190-199`) ACCEPT `session.ReplayModeLiveOnly` in addition to `ReplayModeFromCursor` (keep rejecting BoundedTail if still unsupported), threading the mode into the `sessionStreamUpdate`. The no-history-flood guarantee comes from `SetFilters` preserving the live consumer's start policy on filter rotation (`internal/eventbus/subscriber.go:348-351`) — verify whether the mid-session ctrl path (`server.go::applyFilterCtrl`) needs any change to honor LIVE_ONLY beyond this, and if so make it minimal and additive; PROVE the guarantee with a subscribe-loop test (LIVE_ONLY add → only-new events; FROM_CURSOR add → replay preserved). (2) HIGH-3: add `pluginauthz.AuthorizeStreamSubscribe(ctx, in)` in a new `internal/plugin/pluginauthz/streamsubscribe.go`, mirroring `AuthorizeStreamRead` — require a domain-relative reference (reject `events.`-prefixed input STREAM_NOT_RELATIVE), `eventbus.Qualify` it against the host GameID, reject wildcards (`*`/`>`), reject forbidden namespaces via `EvaluateCapabilityAccess` on `stream:<qualified>` with `types.ActionWrite`, AND additionally enforce OWNED-NAMESPACE: the qualified subject's `<domain>` segment MUST be one of the calling plugin's manifest-declared `emits` domains (fetch via the host — do NOT trust the request). Add `seed:plugin-stream-subscribe` in `internal/access/policy/seed.go` — `permit(principal is plugin, action in ["write"], resource is stream);` (type-match concrete, the write analogue of `seed:plugin-stream-read`) so the existing audit/crypto/system forbids (deny-overrides) carve out forbidden namespaces; scope it so it does NOT widen read. (3) Extend the `HostCapabilities` port (`internal/plugin/hostcap/capabilities.go`) with the method(s) to reach the host `StreamRegistry` (AddStreamWithMode/RemoveStream) and the plugin's owned-emits + AccessEngine/Auditor/GameID (following how the port exposes `SessionAccess()`/`AccessEngine()`); replace the `streamSubscriptionServer` stub bodies — `AddSessionStream` runs `AuthorizeStreamSubscribe` FIRST (fail closed: generic gRPC PermissionDenied on deny, Internal on infra error, no inner-error leak — `.claude/rules/grpc-errors.md`), maps the proto `StreamReplayMode` → `session.ReplayMode`, then calls the port add-with-mode; `RemoveSessionStream` runs the same guard then calls remove. The Lua bridge for `stream.subscription` is already auto-generated (`internal/plugin/luabridge/bindings_gen.go`), so serving the server + placing the guard at this common chokepoint satisfies BOTH runtimes identically (plugin-runtime-symmetry) — do not add a Lua-only path. This touches `internal/access/` and `internal/plugin/pluginauthz/` → the `abac-reviewer` gate MUST fire before push.</action>
  <verify>
    <automated>task test -- ./internal/plugin/hostcap/ ./internal/plugin/pluginauthz/ ./internal/grpc/ ./internal/access/</automated>
  </verify>
  <done>AddStreamWithMode accepts LIVE_ONLY and a mid-session LIVE_ONLY add delivers no history (FROM_CURSOR unchanged — regression asserted); AddSessionStream/RemoveSessionStream run the concrete-stream authz guard (own-namespace permitted, foreign/system/wildcard rejected) before mutating the registry; stream.history read authz unchanged; no longer return Unimplemented; abac-reviewer gate flagged; `task test -- ./internal/plugin/hostcap/ ./internal/plugin/pluginauthz/ ./internal/grpc/ ./internal/access/` green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Binary SDK StreamSubscription client + Aware + capability declaration (RED→GREEN)</name>
  <files>pkg/plugin/stream_subscription_client.go, pkg/plugin/capability_declaration.go, pkg/plugin/stream_subscription_client_test.go</files>
  <read_first>pkg/plugin/capability_declaration.go (hostCapabilityRequirements table — how FocusClientAware/HostEvaluatorAware map to tokens; validateDeclaredCapabilities fail-closed); pkg/plugin/evaluate_client.go (the client+Aware precedent to mirror); pkg/proto/holomush/plugin/host/v1 StreamSubscriptionServiceClient</read_first>
  <behavior>
    - RED: a test asserts that a provider implementing StreamSubscriptionAware but NOT declaring the "stream.subscription" token fails validateDeclaredCapabilities (fail-closed). Fails today (no such entry).
    - A StreamSubscription client method AddStream(ctx, sessionID, stream, mode)/RemoveStream(ctx, sessionID, stream) invokes the underlying StreamSubscriptionServiceClient RPC with the mode mapped to the proto enum.
    - LIVE_ONLY is expressible from the SDK client so core-channels can request it on join.
  </behavior>
  <action>Add a `StreamSubscription` client interface (`AddStream(ctx, sessionID, stream string, mode ReplayMode) error`, `RemoveStream(ctx, sessionID, stream string) error`) + a `StreamSubscriptionAware` opt-in interface (mirror `HostEvaluatorAware`/`FocusClientAware`) in `pkg/plugin/stream_subscription_client.go`, backed by `hostv1.StreamSubscriptionServiceClient` over the brokered host connection. Map the SDK replay-mode to the proto `StreamReplayMode` enum. Register `{"StreamSubscriptionAware", func(p any) bool { _, ok := p.(StreamSubscriptionAware); return ok }, []string{"stream.subscription"}}` in `hostCapabilityRequirements` so a plugin that consumes it but omits the manifest declaration fails closed at load (INV-PLUGIN-54 pattern). Wire the client injection at Init parallel to the other `*Aware` clients.</action>
  <verify>
    <automated>task test -- ./pkg/plugin/</automated>
  </verify>
  <done>A plugin can obtain a StreamSubscription client via StreamSubscriptionAware; undeclared use fails closed; `task test -- ./pkg/plugin/` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin → host session state | A plugin mutating a session's stream subscriptions could subscribe a session to streams it should not receive |
| plugin capability declaration → enforcement | Consuming a capability without declaring it must fail closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Elevation of privilege | stream.subscription capability (undeclared use) | high | mitigate | Fail-closed capability declaration (validateDeclaredCapabilities); a plugin cannot call AddStream/RemoveStream without declaring `stream.subscription` (INV-PLUGIN-54 pattern) |
| T-01-22 | Elevation of privilege | cross-namespace / system-stream subscription via a served stream.subscription (HIGH-3) | high | mitigate | Instance-level concrete-stream authz guard in AddSessionStream/RemoveSessionStream (`pluginauthz.AuthorizeStreamSubscribe`): qualify → reject wildcard/forbidden (system/audit/crypto) namespaces → enforce the stream is in the calling plugin's OWNED `emits` domain; `seed:plugin-stream-subscribe` write permit + existing deny-override forbids; mirrors stream.history's concrete gate. abac-reviewer gate fires |
| T-01-08 | Information disclosure | AddSessionStream targeting a foreign session | medium | mitigate | Guard fires first (T-01-22); server then forwards to the host StreamRegistry which owns session validity (SESSION_NOT_FOUND on unknown session); membership authorization is enforced by 01-04/01-05/01-05b before the plugin ever calls AddStream |
| T-01-09 | Denial of service | history flood on mid-session subscribe | low | mitigate | LIVE_ONLY implemented end-to-end (HIGH-2): AddStreamWithMode accepts ReplayModeLiveOnly and SetFilters preserves the live consumer's start policy on filter rotation (subscriber.go:348-351) so a mid-session add delivers no history — proven by a subscribe-loop test; FROM_CURSOR replay for scenes unchanged (regression asserted) |
| T-01-SC | Tampering | package installs | n/a | accept | No package installs (all in-tree); SC checkpoint does not fire |
</threat_model>

<verification>
- `task test -- ./pkg/plugin/ ./internal/plugin/ ./internal/plugin/hostcap/ ./internal/plugin/pluginauthz/ ./internal/grpc/ ./internal/access/` green.
- `task lint` green (runtime-symmetry: no Lua-only capability path introduced).
- abac-reviewer (`/holomush-dev:review-abac`) READY before push — this plan touches `internal/access/` (seed) + the pluginauthz stream-subscribe gate.
</verification>

<success_criteria>
Binary plugins can (a) contribute session streams at establishment via SessionStreamsHandler with dot-style channel subjects ACCEPTED by the manager (HIGH-1) and (b) mutate subscriptions mid-session via the served stream.subscription capability with REAL LIVE_ONLY (no history flood, HIGH-2) and an instance-level concrete-stream authz guard that fences a plugin to its owned namespaces (HIGH-3); undeclared capability use fails closed; existing scene FROM_CURSOR + stream.history behavior preserved. `holomush-l6std` is resolved (or its residual mid-session-delivery degradation is documented per the scoping-risk note; the HIGH-3 guard is not descopable).
</success_criteria>

<output>
Create `.planning/phases/01-channels-subsystem/01-02-SUMMARY.md` when done. If mid-session delivery (Task 2/3) could not fully land, document the degradation and file a follow-up bead against `holomush-l6std`.
</output>
