---
phase: 01-channels-subsystem
plan: 05
type: tdd
wave: 3
depends_on: ["01-01", "01-03", "01-04"]
files_modified:
  - plugins/core-channels/service.go
  - plugins/core-channels/plugin.yaml
  - plugins/core-channels/main.go
  - plugins/core-channels/service_test.go
requirements: [CHAN-01, CHAN-02, CHAN-04]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "ChannelService implements CreateChannel/JoinChannel/LeaveChannel/ListChannels and self-enforces ABAC per verb (INV-SCENE-65 analog) independent of the command wrapper"
    - "CreateChannel is admin-gated (D-06 seed policy) and rate-limited to 5 creations per player per hour (D-06); the 5th allowed, the 6th denied"
    - "A character can join, leave, and list persistent named channels with no reference to the spatial world model (CHAN-01)"
    - "Channel type (public/private/admin) is set at create; private/admin join respects the invite flow (CHAN-04)"
  artifacts:
    - "plugins/core-channels/service.go (ChannelService implementing holomush.channel.v1.ChannelService)"
    - "plugin.yaml: ChannelService added to provides; requires: [capability: eval] for admin-gated commands"
    - "main.go: ChannelService registered via ServiceProvider; resolver + service share the store"
  key_links:
    - "service self-enforces ABAC per RPC via the host eval capability (HostEvaluator) BEFORE mutating state, independent of the telnet command layer"
    - "create rate-limit keyed per PLAYER (not character) per hour (D-06); admin bypass"
    - "list = membership query (ListForCharacter), never a world/location query"
    - "channelService embeds channelv1.UnimplementedChannelServiceServer and implements ONLY create/join/leave/list here; the remaining RPCs (post/who/history/invite/mute/ban/kick/transfer) are added to the SAME type in 01-05b before the 01-07 command layer delegates to them (review HIGH-4) — registering the server in main.go is safe because Unimplemented covers the not-yet-filled methods and nothing calls them until 01-05b lands"
  prohibitions:
    - statement: "CreateChannel MUST NOT succeed for a non-admin principal unless an operator has granted create:channel by policy (default-deny, D-06)"
      status: pending
      verification: "service_test: non-admin create denied; admin create permitted; grant-seam documented"
    - statement: "Operations on a channel the caller cannot see MUST return an identical not-found error (never distinguish absent vs hidden)"
      status: pending
      verification: "service_test: join/leave on a private channel the caller is not in returns the same not-found as a truly absent channel"
---

<objective>
Implement the `ChannelService` structural operations — create, join, leave, list — with per-RPC ABAC self-enforcement (INV-SCENE-65 analog), admin-gated + rate-limited creation (D-06), and channel-type handling for the CHAN-04 public/private/admin distinction. Wire the service into `main.go`.

Purpose: CHAN-01 (join/leave/list persistent named channels, location-independent) and the create/type surface for CHAN-02/CHAN-04. The service is the in-process layer the telnet commands (01-07) delegate to, and the gRPC surface a future web BFF consumes.
Output: `service.go` + manifest `provides`/`requires` + main wiring; service tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-channels-subsystem/01-CONTEXT.md
@.planning/phases/01-channels-subsystem/01-RESEARCH.md

# Reference service to MIRROR (self-enforcing ABAC per verb):
@plugins/core-scenes/service.go
@plugins/core-scenes/plugin.yaml
# Rules:
@.claude/rules/gateway-boundary.md
@.claude/rules/grpc-errors.md
@.claude/rules/plugin-manifest.md
# Prior artifacts:
@plugins/core-channels/store.go
@plugins/core-channels/resolver.go
</context>

## Artifacts this plan produces

- `plugins/core-channels/service.go` — `channelService` implementing `channelv1.ChannelServiceServer` (embeds `UnimplementedChannelServiceServer`): `CreateChannel` (name validation + admin gate + rate limit), `JoinChannel`, `LeaveChannel`, `ListChannels`. The remaining ChannelService RPCs are filled in by 01-05b (HIGH-4). Self-enforces ABAC per RPC via the host `eval` capability before state mutation.
- `plugin.yaml`: add `holomush.channel.v1.ChannelService` to `provides`; add `requires: [{capability: eval, access: read}]` (HostEvaluator for admin-gated create); declare the `create_rate_limit` config consumption.
- `main.go`: construct `channelService` sharing the store, register it via `ServiceProvider.RegisterServices`; wire the HostEvaluator client (`HostEvaluatorAware`).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: ChannelService create/join/leave/list with per-RPC ABAC + rate limit (RED→GREEN)</name>
  <files>plugins/core-channels/service.go, plugins/core-channels/service_test.go</files>
  <read_first>plugins/core-scenes/service.go (SceneServiceImpl: per-verb self-enforcement via the eval capability BEFORE store mutation, INV-SCENE-65; uniform NotFound; error mapping); plugins/core-channels/store.go (Create/Join/Leave/ListForCharacter); .claude/rules/grpc-errors.md (never leak inner errors)</read_first>
  <behavior>
    - CreateChannel: rejects an invalid name (regex) with InvalidArgument; denies a non-admin caller (default-deny via eval) unless granted; permits an admin caller; the 5th create within an hour for a player succeeds, the 6th is denied (rate limit, D-06); admin bypasses the rate limit; the channel type (public/private/admin) is persisted; the creator becomes owner.
    - JoinChannel: a public channel admits any character; a private/admin channel admits only an invited character (invite flow) else returns not-found (error uniformity — indistinguishable from truly absent); idempotent for an existing member.
    - LeaveChannel: removes membership; leaving a channel the caller is not in is a no-op/uniform not-found.
    - ListChannels: returns exactly the caller's memberships (ListForCharacter); never references location.
    - Every RPC self-enforces ABAC via the host eval capability BEFORE mutating the store (independent of any command wrapper), and never trusts client-supplied identity — the caller is the host-vouched dispatch subject.
  </behavior>
  <action>Implement `channelService` mirroring `SceneServiceImpl`. Each RPC: (1) validate inputs (name regex on create), (2) self-enforce ABAC via the injected HostEvaluator (`engine.Evaluate`/`CanPerformAction` analog — for create use the `seed:channel-admin-create`/`create:channel` gate; for join/leave the per-channel membership/invite gate), (3) then mutate the store. Implement the create rate limiter as an in-memory per-player sliding/fixed 1-hour window seeded from `create_rate_limit` config (default 5), with admin bypass; use a monotonic/injected clock so `service_test` can exercise the boundary and window rollover deterministically. Return uniform `codes.NotFound` for a channel the caller cannot see (absent OR hidden) — never distinguish. Map store `oops` codes to gRPC status with generic messages, log inner detail via `slog.*Context` (`.claude/rules/grpc-errors.md`). Write `service_test.go` (table-driven + fakes): admin vs non-admin create, rate-limit 5-allowed/6-denied + rollover + admin bypass, public join, private non-invitee join → not-found (uniform), idempotent join, list = memberships.</action>
  <verify>
    <automated>task test -- ./plugins/core-channels/</automated>
  </verify>
  <done>Service create/join/leave/list self-enforce ABAC, admin-gated + rate-limited create, uniform not-found for hidden channels; `task test -- ./plugins/core-channels/` green.</done>
</task>

<task type="execute">
  <name>Task 2: Register ChannelService in main.go + manifest provides/requires</name>
  <files>plugins/core-channels/plugin.yaml, plugins/core-channels/main.go</files>
  <read_first>plugins/core-scenes/main.go (ServiceProvider.RegisterServices; HostEvaluator wiring via HostEvaluatorAware at Init); plugins/core-scenes/plugin.yaml:9-29 (requires/provides shape)</read_first>
  <action>Add `holomush.channel.v1.ChannelService` to manifest `provides`. Add `requires: [{capability: eval, access: read}]` (HostEvaluator for admin-gated create; do NOT declare `emit` — it is self-gated by the emit fence, added in 01-06). In `main.go`, construct `channelService` sharing the store, implement `ServiceProvider.RegisterServices` to register the ChannelService server, and implement `HostEvaluatorAware` so the service receives the eval client at Init (mirror scenes). Keep the resolver + service sharing one store handle. Run `task fmt`.</action>
  <verify>
    <automated>task test -- ./plugins/core-channels/</automated>
  </verify>
  <done>ChannelService registered via ServiceProvider and declared in provides; eval capability declared + wired; package compiles; `task test -- ./plugins/core-channels/` + `task lint` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command/RPC caller → service | The service must not trust client-supplied identity; it uses the host-vouched dispatch subject |
| create surface → resource creation | Unbounded/unauthorized creation is abuse |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Elevation of privilege | non-member join/post via service | high | mitigate | Per-RPC self-enforced ABAC before state mutation (INV-SCENE-65 analog); private join requires invite |
| T-01-12 | Information disclosure | hidden-channel oracle via join/leave | medium | mitigate | Uniform codes.NotFound for absent vs hidden on all per-channel RPCs |
| T-01-15 | Denial of service | channel-creation spam | medium | mitigate | Admin-gated create (D-06) + 5/hour per-player rate limit with admin bypass |
| T-01-16 | Spoofing | client-supplied caller identity | high | mitigate | Service uses host-vouched dispatch subject; never trusts request-supplied ids |
| T-01-SC | Tampering | package installs | n/a | accept | No package installs; SC checkpoint does not fire |
</threat_model>

<verification>
- `task test -- ./plugins/core-channels/` green (service unit).
- `task test:int` green (service against store testcontainer).
- `task lint` green (manifest schema; capability declarations).
</verification>

<success_criteria>
A character can create (admin-gated, rate-limited), join, leave, and list persistent named channels independent of location; the service self-enforces ABAC per RPC; private/admin channels require invitation; hidden channels return uniform not-found. CHAN-01 satisfied at the service layer; CHAN-04 type distinction in place.
</success_criteria>

<output>
Create `.planning/phases/01-channels-subsystem/01-05-SUMMARY.md` when done.
</output>
