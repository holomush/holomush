---
phase: 03-world-character-commands
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - test/integration/resilience/retire_concurrency_test.go
  - test/integration/world/character_retire_atomicity_test.go
  - test/integration/world/character_lifecycle_test.go
autonomous: true
requirements: [IDENT-10]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "When two replicas drive a retire with the SAME caller-held expected_version, exactly one commits and the stale writer receives WORLD_CONCURRENT_EDIT — and NOT CHARACTER_ALREADY_RETIRED, proving 03-01's version-precheck-before-status-guard order end-to-end (adjacency edge, IDENT-10, INV-WORLD-7; ROADMAP success criterion 3)"
    - "Retire's status write and its ONE character_retired envelope commit or roll back together — no state without envelope, no envelope without state (IDENT-10, D-32/D-33)"
    - "A retired character's name is still refused by the character-creation path — record and name preserved (IDENT-04, ROADMAP success criterion 1)"
    - "The INV-WORLD-6 rename-half registry defect is FILED as a GitHub issue with the research citations, not fixed here (CONTEXT deferred item)"
  artifacts:
    - path: "test/integration/resilience/retire_concurrency_test.go"
      provides: "Two-replica stale-version rejection Describe for RetireCharacter in the existing resilience harness"
      contains: "WORLD_CONCURRENT_EDIT"
    - path: "test/integration/world/character_retire_atomicity_test.go"
      provides: "Row + envelope commit-or-rollback-together proof for the retire path"
      contains: "character_retired"
  key_links:
    - from: "test/integration/resilience/retire_concurrency_test.go"
      to: "internal/world/service.go"
      via: "both replicas drive Service.RetireCharacter; read-backs go straight to the shared pgxpool (never through sessions or subscriber frames)"
      pattern: "RetireCharacter"
---

<objective>
Prove IDENT-10 for the new mutations: point v0.12's existing two-replica resilience harness at `RetireCharacter` (a new `Describe` modelled on the M12 last-write-wins spec), and prove the state-change-plus-one-envelope atomicity of the retire path. Also close two loose ends that belong with verification: the name-reservation assertion (retired names stay refused at creation — success criterion 1's "name still reserved") and filing the INV-WORLD-6 rename-half registry defect as a GitHub issue (a real defect independent of D-44 — file, do NOT fix).

Purpose: ROADMAP success criterion 3 requires the resilience harness "pointed at the new commands" to pass; IDENT-10 requires the in-transaction outbox guarantee to be demonstrated, not assumed.
Output: A passing resilience `Describe`, a passing atomicity test, an extended lifecycle test, and a filed GitHub issue number recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-world-character-commands/03-CONTEXT.md
@.planning/phases/03-world-character-commands/03-RESEARCH.md
@.planning/phases/03-world-character-commands/03-PATTERNS.md
</context>

<review_disposition>
**Codex HIGH ("sequential interleave cannot reach the CAS conflict") + HIGH ("neutralizing the production CAS predicate is an unsafe RED"), corroborated by the source-grounding pass. RESOLVED by 03-01's R1 disposition, consumed here.** 03-01 now gives `RetireCharacter`/`UnretireCharacter` a caller-supplied `expectedVersion int` and pins the version precheck BEFORE the lifecycle-state guard. That makes the m12 struct-held-version mechanism (`m12_lastwritewins_test.go:142-155`: two copies of the same read version) directly expressible through the public command: a fully SEQUENTIAL drive — read once, A commits with N, B calls with N — deterministically yields `WORLD_CONCURRENT_EDIT` from the precheck, with no repo-level interleave hook and no production-code neutralization. Task 1 below is written against that contract and additionally asserts the error is NOT the already-retired code, turning the guard order itself into a harness-level proof.
</review_disposition>

<tasks>

<task type="auto">
  <name>Task 1: Two-replica stale-version rejection Describe for RetireCharacter</name>
  <precondition>Docker is available (`docker info` succeeds) — the resilience suite needs the real NATS container via natstest plus shared Postgres.</precondition>
  <files>test/integration/resilience/retire_concurrency_test.go</files>
  <action>
    Add a new file in `package resilience_test` (build tag `//go:build integration`) with a `Describe` retargeting `m12_lastwritewins_test.go`'s spec 1 mechanism from UpdateLocation to RetireCharacter, using 03-01's caller-supplied expected_version seam (review disposition above): read the character ONCE via the shared pgxpool and capture its version N (the analogue of m12's two struct copies holding the same read version, m12_lastwritewins_test.go:142); drive replica A's `RetireCharacter(…, characterID, N)` to completion (commits: status retired, version N+1); then drive replica B's `RetireCharacter(…, characterID, N)` SEQUENTIALLY — B MUST be rejected with WORLD_CONCURRENT_EDIT from 03-01's version precheck (assert via errutil.AssertErrorCode — the deepest chain code, which WORLD_CONCURRENT_EDIT is; PORTAL-10 rule 5), and the spec MUST additionally assert the failure is NOT CHARACTER_ALREADY_RETIRED (guard-order proof: a stale caller sees the conflict, never the racing writer's outcome). Copy the harness mechanics EXACTLY from m12_lastwritewins_test.go:41-56 — replica boot, the `m12SpecTimeout = 3 * time.Minute` in-file budget precedent, and the read-back rule: read-backs go straight to the shared pgxpool, never through sessions or subscriber frames (the suite's RESEARCH Pitfall 6 note). Assert the committed row: status retired, version bumped exactly once, and the loser's write left no second version bump. Two hard suite rules (resilience_suite_test.go): the suite gates on quarantinetest.Enabled() alone — do NOT add any quarantine-marker pattern anywhere in the package (it would trip the bijection meta-test at test/meta/quarantine_registry_test.go:31); and the suite doc at :20-27 forbids marker idioms explicitly. Calls are caller-shaped per landed 02.1 (both replicas retire as an authorized subject — mirror how the M12 specs authorize their UpdateLocation calls under the landed caller surface, adding whatever grant fixture the suite already uses for world writes).
  </action>
  <verify>
    <automated>HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience ./test/integration/resilience/</automated>
  </verify>
  <done>The new Describe passes under HOLOMUSH_RUN_QUARANTINED=1; the suite still self-skips without the env var; no quarantine markers introduced.</done>
  <acceptance_criteria>
    - `rg -q 'RetireCharacter' test/integration/resilience/retire_concurrency_test.go` succeeds
    - The loser assertion pair is present: errutil.AssertErrorCode against WORLD_CONCURRENT_EDIT AND a negative assertion that the error code is not CHARACTER_ALREADY_RETIRED (guard-order proof)
    - `! rg -q 'quarantinetest' test/integration/resilience/retire_concurrency_test.go` succeeds (gating stays in the suite entry point; no marker idioms in the new file)
    - `HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience ./test/integration/resilience/` exits 0 (long-running: budget 10+ minutes; run in background and monitor — decide pass/fail by EXIT CODE, never by grepping output)
    - Without the env var, `task test:int -- -run TestWorldModelResilience ./test/integration/resilience/` still exits 0 via skip
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Envelope atomicity + name reservation + file the INV-WORLD-6 defect</name>
  <files>test/integration/world/character_retire_atomicity_test.go, test/integration/world/character_lifecycle_test.go</files>
  <action>
    Three deliverables. (1) Atomicity: read the existing INV-WORLD-1 proof pattern FIRST (internal/world/outbox's integration test proving a real row + its envelope commit-or-roll-back together: rollback case, commit case, forced-duplicate-event_id case) and mirror it for the retire path in test/integration/world/character_retire_atomicity_test.go (`//go:build integration`; match the directory's dominant test style — check siblings for `var _ = Describe` vs plain testing.T with `rg -l 'var _ = Describe' test/integration/world/` and conform): a committed RetireCharacter leaves the retired row AND exactly one outbox row with kind character_retired for that aggregate; a rolled-back/failed retire (stale CAS) leaves NEITHER a status change nor an envelope. Assert against the actual DB rows (PORTAL-10 rule 3: at this layer, the actual outbox row, never a populated Go struct). (2) Name reservation: extend test/integration/world/character_lifecycle_test.go with a spec proving a retired character's name is refused by the creation path (drive the same creation entry the existing suite uses; expect the name-taken rejection). HARD CONSTRAINT: do not alter the existing `// Verifies: INV-WORLD-6` block or its claims (test/integration/world/character_lifecycle_test.go:213-219) — extend alongside, never edit bindings. (3) File the INV-WORLD-6 rename-half defect: `gh issue create -R holomush/holomush --label bug` titled "INV-WORLD-6 registry summary overclaims: rename half is false in production" with a body carrying RESEARCH §6.3's grounded citations — the registry summary says a name frees ONLY through two tombstone-emitting hard-delete paths, but CharacterRepository.Rename (internal/world/postgres/character_repo.go:225-235) frees the old normalized name with no tombstone, the shipped operator CLI calls it, the binding test never exercises rename, and INV-WORLD-4 itself enumerates that CLI as a sanctioned writer (cross-entry contradiction). State the disposition: retire half stands; fix is rename territory (backlog 999.20). Record the issue number in the SUMMARY.
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/world/ && gh issue list -R holomush/holomush --limit 10 --json number,title --jq '.[] | select(.title | contains("INV-WORLD-6")) | .number'</automated>
  </verify>
  <done>Atomicity both directions proven against real rows; name-reservation spec green; INV-WORLD-6 issue filed and its number captured; existing INV-WORLD-6 binding block untouched.</done>
  <acceptance_criteria>
    - Atomicity test asserts the outbox row count for the aggregate is exactly 1 after commit and exactly 0 after the failed path
    - `git diff` on character_lifecycle_test.go shows additions only around the new spec; the `// Verifies: INV-WORLD-6` block is byte-identical
    - `gh issue list -R holomush/holomush --limit 10 --json number,title` shows the new INV-WORLD-6 issue
    - `task test:int -- ./test/integration/world/` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | this plan writes tests and files an issue; no production surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Repudiation | IDENT-10 guarantee | medium | mitigate | this plan IS the mitigation: the two-replica rejection and the atomicity proof turn the guarantee from asserted to demonstrated |
| T-03-09 | Information Disclosure | test read-backs via shared pgxpool | low | accept | test-only substrate inside the integration harness; no production path |
</threat_model>

<verification>
- `HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience ./test/integration/resilience/` — run once by hand at phase gate (not on the PR gate; nightly lane owns it)
- `task test:int -- ./test/integration/world/`
- `task lint` before every commit
</verification>

<success_criteria>
- ROADMAP success criterion 3 demonstrated: stale expected_version rejected with the typed signal on the new command, in the real two-replica harness
- IDENT-10's in-transaction outbox guarantee proven in both directions for retire
- Name reservation of retired characters proven at the creation path
- INV-WORLD-6 defect tracked in GitHub Issues with grounded citations
</success_criteria>

<output>
Create `.planning/phases/03-world-character-commands/03-03-SUMMARY.md` when done. Record the filed issue number and the resilience suite runtime.
</output>

## Artifacts this phase produces (plan 03-03)

New symbols created by this plan (source-grounding: exclude from drift verification):
- Test files: `test/integration/resilience/retire_concurrency_test.go`, `test/integration/world/character_retire_atomicity_test.go`
- Extended: `test/integration/world/character_lifecycle_test.go` (new spec alongside, bindings untouched)
- A GitHub issue for the INV-WORLD-6 rename-half registry defect
