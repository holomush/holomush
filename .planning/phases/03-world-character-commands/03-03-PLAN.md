---
phase: 03-world-character-commands
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - test/integration/resilience/retire_cas_test.go
autonomous: true
requirements: [IDENT-10]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "v0.12's existing two-replica resilience harness, pointed at RetireCharacter, passes: two replicas read the same characters.version, one commits, and the stale writer is REJECTED with WORLD_CONCURRENT_EDIT rather than silently overwriting (ROADMAP criterion 3)."
    - "The rejected replica's retire leaves the characters row at the winner's version and writes ZERO outbox envelopes — the CAS failure is atomic, not a partial write."
    - "Under concurrent retire mutations the outbox feed_position sequence is gap-free and strictly increasing per game; ordering comes from the locked world_feed_counter and never from envelope ULID lexical order."
    - "Two concurrent mutations presenting the SAME expected_version collide deterministically — exactly one wins, and which one is observable from the returned error, not inferred."
    - "The new spec adds no quarantine marker of any kind to test/integration/resilience/, which gates solely on quarantinetest.Enabled()."
  artifacts:
    - test/integration/resilience/retire_cas_test.go
  key_links:
    - "world.Service.RetireCharacter (from 03-01) -> the resilience harness's N in-process CoreServer replicas over ONE real NATS container + ONE shared Postgres"
    - "the shared pgxpool read-back path -> the assertion surface (never sessions, never subscriber frames)"
  prohibitions:
    - "A CAS-rejection test MUST NOT be written so that it passes when the guard is absent. The stale-writer rejection must be demonstrated RED against a build without the version predicate before it is trusted."
---

<objective>
Prove IDENT-10's concurrency guarantee for the new lifecycle commands on the substrate v0.12 already
built for it: a new `Describe` in `test/integration/resilience/` that runs two in-process `CoreServer`
replicas against one real NATS JetStream container and one shared Postgres, and shows a stale
`expected_version` on `RetireCharacter` being rejected rather than silently overwriting.

Purpose: ROADMAP success criterion 3 names this harness explicitly. The suite exists; what does not
exist is a spec pointed at the new commands. This plan is the smallest honest way to close that
criterion, and it carries three of the phase's IDENT-10 probe edges (adjacency, ordering, empty).

Output: one new Ginkgo spec file exercising the retire CAS path across replicas.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-world-character-commands/03-CONTEXT.md
@.planning/phases/03-world-character-commands/03-RESEARCH.md
@.planning/phases/03-world-character-commands/03-01-SUMMARY.md
</context>

## Artifacts this phase produces (plan 03-03 share)

| Symbol | Home |
|---|---|
| `test/integration/resilience/retire_cas_test.go` (new file) | — |
| a new Ginkgo `Describe` for the retire CAS path, inside the existing `TestWorldModelResilience` suite | `test/integration/resilience/retire_cas_test.go` |

This plan creates no production symbols.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-replica stale-version rejection on RetireCharacter</name>
  <files>test/integration/resilience/retire_cas_test.go</files>
  <read_first>
    - `test/integration/resilience/resilience_suite_test.go:1-56` — the build tag, the `quarantinetest.Enabled()` gate at `:48-50`, the `RunSpecs` entry point, and the `:20-27` doc block that FORBIDS any quarantine-marker pattern anywhere in this package.
    - `test/integration/resilience/m12_lastwritewins_test.go:41-56` — spec 1, the exact model to retarget: "deterministic interleave (location, service level) — both replicas read the same version, one commits, the stale writer is REJECTED with WORLD_CONCURRENT_EDIT". Copy its replica-construction and interleave shape.
    - `test/integration/resilience/m12_lastwritewins_test.go` — the `const m12SpecTimeout = 3 * time.Minute` in-file budget precedent, and the read-back discipline note ("straight to the shared pgxpool, never through sessions or subscriber frames").
    - `internal/world/service.go` — the `RetireCharacter` signature and error mapping landed by 03-01.
    - `.claude/rules/testing.md` — ACE naming, the tier table, and the rule that this suite is INTEGRATION (Go/gRPC in-process), never "E2E".
    - `internal/testsupport/natstest` — the real-NATS-container harness this suite uses; note the suite needs N replicas each with an INDEPENDENT connection to one broker, which is why embedded NATS cannot express it.
  </read_first>
  <behavior>
    - Given two replicas that both read character C at version N, replica A calls `RetireCharacter(..., N)` and commits; replica B then calls `RetireCharacter(..., N)` and is rejected.
    - B's error resolves to `WORLD_CONCURRENT_EDIT`.
    - After the interleave, C's `status` is `retired` and `version` is exactly `N+1` — B did not bump it a second time.
    - Exactly one outbox envelope of kind `character_retired` exists for C.
  </behavior>
  <action>
Add one new file, `retire_cas_test.go`, in `package resilience_test` with the `//go:build integration`
tag, containing a new `Describe` inside the existing `TestWorldModelResilience` suite. Do NOT create a
second suite entry point — `RunSpecs` already exists in `resilience_suite_test.go`.

Model the interleave on `m12_lastwritewins_test.go`'s spec 1 verbatim in shape, retargeted from
`UpdateLocation` to `RetireCharacter`: stand up the replicas, seed one character, have both replicas
read it to capture the same version, drive replica A's retire to completion, then drive replica B's
retire with the now-stale version.

Assert B's rejection with `errutil.AssertErrorCode` against `WORLD_CONCURRENT_EDIT`. That is the
DEEPEST code in the chain, which is what `AssertErrorCode` resolves under the pinned `samber/oops`
(issue #4902) — so it is the correct helper here. Do not use it to assert a top-level code anywhere in
this file.

Do every read-back straight against the shared `pgxpool`. Never assert through a session, a subscriber
frame, or a service-level getter — a service read could mask a persistence bug the spec exists to catch.

Reuse the in-file spec-timeout constant idiom (`3 * time.Minute` is the established budget) rather than
relying on the Ginkgo default.

Add NO quarantine marker: not `quarantinetest.Skip`, not a `@quarantine` tag, not a `test/quarantine.yaml`
row. This package gates solely on `quarantinetest.Enabled()`, and a marker would trip the bijection
meta-test at `test/meta/quarantine_registry_test.go:31`.

Demonstrate the spec RED before trusting it (PORTAL-10 rule 4): temporarily neutralize the version
predicate in the repo's `SetStatus` guard, observe the spec fail, then revert. Record both the RED
output and the revert in the SUMMARY. Do not leave the neutralization committed.
  </action>
  <verify>
    <automated>HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience ./test/integration/resilience/</automated>
  </verify>
  <acceptance_criteria>
    - `HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience ./test/integration/resilience/` exits 0.
    - `task test:int` (without the env var) exits 0 and the new spec SKIPS — it is not on the required PR gate.
    - `rg -n 'quarantinetest\.Skip|@quarantine' test/integration/resilience/` returns no hits.
    - `rg -c 'RunSpecs' test/integration/resilience/` returns 1 — no second suite entry point was added.
    - `rg -n 'WORLD_CONCURRENT_EDIT' test/integration/resilience/retire_cas_test.go` returns a hit inside an `errutil.AssertErrorCode` call.
    - The SUMMARY records the RED output observed with the version predicate neutralized, and confirms the neutralization was reverted.
  </acceptance_criteria>
  <done>Two replicas race a retire on the same version; the loser is rejected with the typed conflict signal, proven against real rows on the real broker.</done>
  <reversibility rating="reversible">A test-only addition.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Envelope atomicity and feed ordering under concurrent retires</name>
  <files>test/integration/resilience/retire_cas_test.go</files>
  <read_first>
    - `test/integration/resilience/retire_cas_test.go` — the Describe written in Task 1.
    - `internal/world/mutator.go:197-217` — `mutate` runs the write closure and `WriteIntent` inside ONE `Transactor.InTransaction`, which is why a rejected CAS must leave zero envelopes.
    - 03-RESEARCH.md §2.3 for the `expectedVersion == 0` unversioned-write convention and its INV-WORLD-7 / Phase-4 boundary.
    - `internal/world/outbox/` — the `outbox` table's `(game_id, epoch, feed_position)` UNIQUE and gap-free property allocated from the locked `world_feed_counter`.
    - `.claude/rules/event-conventions.md` — ordering is owned by the per-stream sequence and the feed counter, never by ULID lexical order.
  </read_first>
  <behavior>
    - After the Task 1 interleave, the outbox holds exactly ONE `character_retired` envelope for the character — the rejected replica contributed none.
    - Across several concurrent retires of DIFFERENT characters, the resulting `feed_position` values for the game are strictly increasing with no gaps.
    - Sorting the resulting envelopes by `feed_position` yields a different-or-equal order than sorting them by envelope ULID, and the spec asserts on `feed_position`, never on ULID order.
    - A retire issued with `expectedVersion == 0` performs an unversioned write and succeeds regardless of the row's current version, pinning the documented repo convention.
  </behavior>
  <action>
Extend the Task 1 `Describe` with three further `It` blocks, keeping every read-back on the shared
`pgxpool`.

First: assert envelope atomicity for the rejected writer. Query the outbox for the character and assert
exactly one row of kind `character_retired`. A CAS rejection that still wrote an envelope would be a
partial write, and the same-transaction seam is precisely what makes that impossible — this asserts it
rather than assuming it.

Second: drive several concurrent retires against DIFFERENT characters in the same game, then read the
resulting envelopes ordered by `feed_position` and assert the sequence is strictly increasing with no
gaps. Assert explicitly on `feed_position`; do NOT sort by or assert on envelope ULID — ULIDs are
identity and dedup keys, and treating them as an ordering key is the exact confusion the event
conventions forbid.

Third: pin the `expectedVersion == 0` convention with its own `It`. Issue a retire with a zero expected
version against a character at a known non-zero version and assert it succeeds — the `AND version = $3`
clause is not appended for an unversioned write. Add a comment in the spec recording that rejecting
zero at the web mutation boundary is INV-WORLD-7 / Phase-4 territory, so a later RPC-layer rejection is
an additive change rather than a silent flip of this behavior.

Keep all three inside the existing `Describe` and the existing suite; add no new `RunSpecs` and no
quarantine marker.
  </action>
  <verify>
    <automated>HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience ./test/integration/resilience/</automated>
  </verify>
  <acceptance_criteria>
    - `HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience ./test/integration/resilience/` exits 0.
    - `rg -n 'feed_position' test/integration/resilience/retire_cas_test.go` returns hits; `rg -in 'sort.*ulid|order by id' test/integration/resilience/retire_cas_test.go` returns none.
    - An `It` name (ACE-style sentence) explicitly covers the zero-expected-version case, and the file carries the INV-WORLD-7 / Phase-4 boundary comment.
    - The atomicity assertion counts outbox rows with an exact-equality assertion (exactly 1), not a non-emptiness check.
    - `task lint` exits 0.
  </acceptance_criteria>
  <done>The rejected writer is proven to have written nothing, and the feed's ordering guarantee is asserted on `feed_position` rather than on ULID order.</done>
  <reversibility rating="reversible">A test-only addition.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replica A -> shared Postgres -> replica B | The only boundary this plan exercises: two independent processes racing one row. No new production surface is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-20 | Tampering | lost update across replicas | high | mitigate | This plan IS the mitigation's proof: the version-predicated CAS is demonstrated to reject the stale writer, and demonstrated RED against a build with the predicate neutralized. |
| T-03-21 | Repudiation | a partial write leaving state without its envelope | high | mitigate | Exact-equality assertion that the rejected replica contributed zero outbox rows, proving the same-transaction seam holds under contention. |
| T-03-22 | Tampering | feed ordering inferred from ULID instead of the counter | medium | mitigate | Assertions are on `feed_position` only; a grep-level acceptance criterion forbids ULID-order assertions in the file. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed; Ginkgo, Gomega, testify and the NATS/Postgres test harnesses are all already present. |
</threat_model>

<verification>
- `HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience ./test/integration/resilience/` green.
- `task test:int` green with the new spec skipping (it is deliberately off the required PR gate and runs on the nightly Quarantine Health lane).
- `task lint` green.
- The RED demonstration and its revert are recorded in the SUMMARY.
</verification>

<success_criteria>
- ROADMAP success criterion 3 is closed: the existing two-replica harness, pointed at the new commands, passes.
- IDENT-10's adjacency (same-version collision), ordering (feed order under concurrency) and empty (zero expected version) probe edges each have a passing assertion.
</success_criteria>

<output>
Create `.planning/phases/03-world-character-commands/03-03-SUMMARY.md` when done.
</output>
