---
phase: 03-world-character-commands
plan: 04
type: tdd
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - internal/core/session_ended_payload.go
  - internal/world/setup/retirement_subsystem.go
  - internal/world/setup/retirement_reactor.go
  - internal/world/setup/retirement_reactor_test.go
  - test/integration/world/retirement_reactor_test.go
autonomous: true
requirements: [IDENT-04]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Retirement is observably effective, not merely recorded: consuming a character_retired envelope ends the character's live sessions, notifies the location it left, and moves it to the configured starting location (ROADMAP criterion 2)."
    - "The leave notification names the location the character LEFT, because EmitLeave fires at the OLD location BEFORE the move."
    - "The move runs through world.Service.MoveCharacter under the prefixed subject system:retirement, authorized by seed:system-retirement-character — not by a direct location write and not by an ABAC bypass (D-38, D-45)."
    - "The destination is the already-resolved starting_location_id exposed by bootstrap's StartLocationID() — no new manifest key and no new schema (D-37). Retired characters accumulate at that arrival point in characters.location_id but are NOT present there, because presence is session-derived."
    - "A redelivered character_retired message produces exactly ONE leave notification and exactly ONE character_moved envelope in total: session teardown returns nothing on the second pass, and the move is skipped because the character is already at the starting location."
    - "A character un-retired between emit and delivery is ack-and-skipped: no sessions are ended, no leave is emitted, and no move occurs."
    - "Retirement is eventually consistent by construction — the envelope reaches JetStream via the outbox relay, so the reactor is downstream of the relay and never inside the command's transaction (D-33, D-36)."
    - "Ending a session for retirement reports a distinct cause value, so an operator reading the session-ended audit can tell retirement apart from quit, logout, guest_end, kicked, reaped and evicted."
    - statement: "The reactor's v1 scope is sessions only: no scene, channel, page or story fanout is wired, and the seam for later consumers is the subsystem itself rather than an extension point inside the handler."
      verification: backstop
  artifacts:
    - internal/world/setup/retirement_reactor.go
    - internal/core/session_ended_payload.go
    - test/integration/world/retirement_reactor_test.go
  key_links:
    - "outbox relay -> JetStream events.<game>.character.<id> with Type == character_retired -> the reactor's durable consumer (the reactor is downstream of SubsystemOutboxRelay, never of the command)"
    - "presence.Emitter.EmitLeave / EmitSessionEnded reached through a consumer-defined 2-method interface, mirroring internal/auth/auth_service.go:26-29 rather than importing internal/presence"
    - "bootstrap StartLocationID() -> the move destination; it PANICS before Bootstrap.Prepare, which is why the reactor DependsOn(SubsystemBootstrap)"
  prohibitions:
    - "Retirement MUST NOT delete, tombstone, or cascade character data. The reactor ends sessions and moves the character; the irreversible DeleteCharacter path stays untouched."
    - "The reactor MUST NOT be given an ABAC bypass. access.WithSystemSubject MUST NOT be stamped on its context; the prefixed subject plus the narrow seed is the whole authorization story (D-45)."
    - "A retired character MUST NOT be left holding a live session. Session teardown is the single fail-closed effect: if it fails, the message is NAKed for redelivery rather than acked as done."
---

<objective>
Give the retirement reactor its effects. 03-02 landed the subsystem, its durable consumer and the
status guard; this plan puts the three fanout effects behind that guard — end the character's sessions,
notify the location it left, and move it to the configured starting location — and proves the whole
thing idempotent under at-least-once redelivery.

Purpose: ROADMAP success criterion 2 is the difference between retirement being *recorded* and
retirement being *true*. It is also the phase's only genuine exercise of the
`MoveCharacter -> MovementHook -> UpdateLocationOnMove` pipeline, which issue #4788 records as covered
today only by SIMULATED moves.

Output: a working retirement fanout, a new session-ended cause, and a full-stack integration proof
including the redelivery case.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-world-character-commands/03-CONTEXT.md
@.planning/phases/03-world-character-commands/03-RESEARCH.md
@.planning/phases/03-world-character-commands/03-01-SUMMARY.md
@.planning/phases/03-world-character-commands/03-02-SUMMARY.md
</context>

## Artifacts this phase produces (plan 03-04 share)

| Symbol | Home |
|---|---|
| `core.SessionEndedCauseRetired` = `"retired"` | `internal/core/session_ended_payload.go` |
| `worldsetup.retirementReactor` (the handler type behind the 03-02 status guard) | `internal/world/setup/retirement_reactor.go` (new file) |
| consumer-defined interfaces `PresenceEmitter`, `SessionEnder`, `CharacterMover` | `internal/world/setup/retirement_reactor.go` |
| subject literal `"system:retirement"` | `internal/world/setup/retirement_reactor.go` |
| `test/integration/world/retirement_reactor_test.go` (new file) | — |
| `internal/world/setup/retirement_reactor_test.go` (new file) | — |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A distinct session-ended cause for retirement</name>
  <files>internal/core/session_ended_payload.go</files>
  <read_first>
    - `internal/core/session_ended_payload.go:18-31` — the six existing `SessionEndedCause` constants (`quit`, `logout`, `guest_end`, `kicked`, `reaped`, `evicted`) AND the struct field comment at `:20` that lists the causes INLINE. Both sites change together.
    - `internal/presence/session_ended.go` — `EmitSessionEnded`'s signature and its fresh-context discipline, so the new cause flows through unchanged.
    - `.claude/rules/terminology.md` — `character` vs `player`, `location` never `room`, and `presence` is session-derived.
  </read_first>
  <behavior>
    - `core.SessionEndedCauseRetired` exists with the wire value `"retired"` and is distinct from all six existing causes.
    - The `SessionEndedPayload` field comment enumerating valid causes includes `retired`.
    - A session ended for retirement is distinguishable in the emitted payload from one ended by `evicted` or `reaped`.
  </behavior>
  <action>
Add `SessionEndedCauseRetired` with the wire value `"retired"` alongside the six existing constants,
following the block's existing declaration style.

Update the `SessionEndedPayload` struct's field comment that lists the causes inline — it enumerates
them in prose and will otherwise drift from the constant block on the very first read. This is a
two-site edit, not one.

Add a unit test asserting the new constant's wire value and that all seven cause values are distinct
(a set-equality or duplicate-detection assertion, not seven individual equality checks) — a duplicated
cause string would silently conflate two operator-facing reasons.
  </action>
  <verify>
    <automated>task test -- ./internal/core/ ./internal/presence/</automated>
  </verify>
  <acceptance_criteria>
    - `task test -- ./internal/core/ ./internal/presence/` exits 0.
    - `rg -c 'SessionEndedCause[A-Z]' internal/core/session_ended_payload.go` returns 7.
    - `rg -n 'retired' internal/core/session_ended_payload.go` shows hits in BOTH the constant block and the struct field comment.
    - A test asserts the seven cause values are pairwise distinct.
  </acceptance_criteria>
  <done>Retirement-caused session ends are distinguishable from every other cause on the wire.</done>
  <reversibility rating="costly">A published `session_ended` cause value stamped onto durable audit rows, so renaming it later means touching historical data. Rated `costly` rather than `one-way`, and therefore not gated behind a checkpoint, for two reasons: CONTEXT lists "a new `SessionEndedCause` constant for retirement" explicitly under Claude's Discretion, so the choice is already delegated; and the cause list is an open, additive enum with six existing members, so a seventh forecloses nothing — unlike a taxonomy kind, which the census bijection consumes as a closed set.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The three fanout effects, ordered and idempotent</name>
  <files>internal/world/setup/retirement_reactor.go, internal/world/setup/retirement_subsystem.go, internal/world/setup/retirement_reactor_test.go</files>
  <read_first>
    - `internal/world/setup/retirement_subsystem.go` — the subsystem and status guard landed by 03-02. The effects go BEHIND that guard; do not weaken or relocate it.
    - `internal/auth/auth_service.go:26-29` — the consumer-defined 2-method presence interface (`EmitLeave`, `EmitSessionEnded`). Copy THIS shape rather than importing `internal/presence`; it is the in-repo pattern for breaking the cycle.
    - `internal/auth/auth_service.go:248-256` — the closest existing leave + session-ended fanout, including its `core.CharacterRef{ID, Name, LocationID}` construction and log-warn-on-error handling.
    - `internal/session/session.go:303-306, 426-428` — `DeleteByCharacter(ctx, characterID ulid.ULID) (*Info, error)`.
    - `internal/store/session_store.go:809-827` — the implementation. Confirm for yourself that it returns `(nil, nil)` when no matching session exists; that fact is the entire basis of the redelivery gate, and 03-RESEARCH.md flagged it as assumption A6 needing exactly this read.
    - `internal/presence/emitter.go:125-175` — `EmitArrive` / `EmitLeave`, which publish to `location.<id>`.
    - `internal/world/service.go:985-1048` — `MoveCharacter`: it checks `"write"` on `access.CharacterResource(id)` at `:989-992`, routes through `mutate()`, and fires the movement hook POST-commit treating hook failure as operational degradation.
    - `internal/bootstrap/setting.go:133-141` — `"system:bootstrap"` as a raw string literal, the precedent for the prefixed system subject.
    - `internal/bootstrap/setup/subsystem.go:299-303` — `StartLocationID()` and its pre-Prepare panic.
    - `internal/access/policy/seed.go` — `seed:system-retirement-character` as landed by 03-01.
    - `.claude/rules/logging.md` — the reactor is a ctx-carrying loop; every log call is a `*Context` variant.
    - The eight existing `EmitLeave` call sites, surveyed before deciding what to reuse: `internal/auth/auth_service.go:248`, `internal/grpc/lifecycle_handler.go:199` and `:252`, `cmd/holomush/sub_grpc.go:844` and `:870`, `internal/grpc/command_handler.go:268` and `:330`, `internal/grpc/auth_handlers.go:716`.
  </read_first>
  <behavior>
    - Given a retired character with a live session at location L, the handler ends the session, emits a leave naming L, emits a session-ended with cause `retired`, and then moves the character to the starting location.
    - The leave names L (the OLD location), not the starting location — the emit happens BEFORE the move.
    - Given a retired character with NO live session, no leave and no session-ended are emitted, and the move still runs.
    - Given a retired character already at the starting location, the move is skipped entirely — no second `character_moved` envelope.
    - Given a session-teardown failure, the message is NAKed for redelivery rather than acked.
    - The move is authorized as `"system:retirement"` and succeeds under the seeded permit; with the seed absent it is denied.
  </behavior>
  <action>
Create `internal/world/setup/retirement_reactor.go` holding the handler the 03-02 subsystem dispatches
to. Declare three consumer-defined interfaces locally — a 2-method presence emitter mirroring
`internal/auth/auth_service.go:26-29`, a session ender exposing only `DeleteByCharacter`, and a
character mover exposing only `MoveCharacter` — and inject them. Do NOT import `internal/presence`
directly; the narrow local interface is the established cycle-breaking pattern here.

Order the effects deliberately: end the session FIRST (its returned `*Info` carries the `LocationID`
the leave needs), emit the leave at that OLD location, emit the session-ended with
`core.SessionEndedCauseRetired`, and move LAST. Emitting the leave before the move is what makes the
notification name the place the character left.

Gate each effect on observed state so at-least-once redelivery cannot double-fire:
`DeleteByCharacter` returning `(nil, nil)` means there was no session, so skip both emits; read the
character's current location and skip the move when it already equals the starting location. The
03-02 status guard remains the outermost gate — a character no longer in status `retired` is
ack-and-skipped before any of this runs.

Treat session teardown as the one FAIL-CLOSED effect: if it errors, NAK for redelivery rather than
acking, because a retired character holding a live session is the failure ROADMAP criterion 2 exists to
prevent. Treat leave/session-ended emit failures as operational degradation — log with
`errutil.LogErrorContext` and continue — mirroring how `MoveCharacter` itself treats a post-commit
movement-hook failure at `service.go:1035-1045`.

Perform the move through `world.Service.MoveCharacter` with the raw subject literal
`"system:retirement"`, exactly as `internal/bootstrap/setting.go:134` uses `"system:bootstrap"`. Do NOT
call `access.WithSystemSubject` and do NOT use the bare `"system"` subject: the bare form takes the
`EffectSystemBypass` path and skips policy evaluation entirely, which D-45 explicitly rejected, and
without `access.IsSystemContext(ctx)` it is a hard `SYSTEM_SUBJECT_REJECTED` error anyway. The prefixed
subject takes the normal policy path against `seed:system-retirement-character`.

Resolve the starting location from bootstrap's `StartLocationID()`. The reactor already declares
`DependsOn(SubsystemBootstrap)` from 03-02; resolve lazily inside the handler rather than at
construction so the constructor stays allocation-free and resource-free for the topo-order tests.

Keep the v1 scope fence: sessions only. No scene, channel, page or story fanout. Those consumers plug
in as their own subsystems later; the seam is the event, not an extension point inside this handler.

Write unit tests with mocked interfaces covering every branch in the behavior list, including the
NAK-on-teardown-failure path.
  </action>
  <verify>
    <automated>task test -- ./internal/world/setup/ ./internal/core/</automated>
  </verify>
  <acceptance_criteria>
    - `task test -- ./internal/world/setup/ ./internal/core/` exits 0.
    - `rg -n 'WithSystemSubject|IsSystemContext' internal/world/setup/` returns no hits.
    - `rg -n '"system:retirement"' internal/world/setup/retirement_reactor.go` returns exactly one hit, and `rg -n '"system"' internal/world/setup/retirement_reactor.go` returns no bare-literal hit.
    - `rg -n 'holomush/internal/presence' internal/world/setup/retirement_reactor.go` returns no hits — the presence emitter is reached through a locally declared interface.
    - A unit test asserts the leave is emitted with the OLD location id, distinct from the starting location id.
    - A unit test asserts that when `DeleteByCharacter` returns `(nil, nil)`, zero emits occur and the move still runs.
    - A unit test asserts that a `DeleteByCharacter` error results in a NAK, not an ack.
    - `rg -in 'scene|channel|page|story' internal/world/setup/retirement_reactor.go` returns no hits — the v1 fence holds.
  </acceptance_criteria>
  <done>A retired character's sessions end, its old location is told it left, and it lands at the starting location — each effect gated on observed state.</done>
  <reversibility rating="costly">The fanout's ordering and the `system:retirement` subject become an operational contract other consumers and policies will assume.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Full-stack proof, including redelivery</name>
  <files>test/integration/world/retirement_reactor_test.go</files>
  <read_first>
    - `internal/testsupport/integrationtest` — `Start(t)` and the in-process stack (Postgres + embedded NATS JetStream + the production `CoreServer`). Per 03-RESEARCH.md's tier table, embedded NATS is CORRECT here; `natstest` is required only for external-mode-specific behavior, which this is not.
    - `site/src/content/docs/contributing/how-to/integration-tests.md` — the harness's when-to-use and helper inventory.
    - `.claude/rules/testing.md` — Ginkgo/Gomega for full-stack integration, `//go:build integration`, and the tier vocabulary (this is INTEGRATION, not E2E).
    - `internal/world/setup/retirement_reactor.go` — the handler from Task 2.
    - `test/integration/world/character_lifecycle_test.go` — the existing package's conventions and its `// Verifies: INV-WORLD-6` block, which this plan must not disturb.
    - `internal/world/outbox/` and the relay subsystem — the reactor is downstream of `SubsystemOutboxRelay`, so the spec must wait for the envelope to be relayed rather than asserting synchronously after the command returns.
  </read_first>
  <behavior>
    - Retiring a character that has a live session at location L eventually produces: zero live sessions for that character, a leave event on `location.L`, a session-ended with cause `retired`, and the character's `location_id` equal to the starting location.
    - Redelivering the same `character_retired` message produces no additional leave event and no additional `character_moved` envelope.
    - A character un-retired before the message is processed is left with its session intact and its location unchanged.
  </behavior>
  <action>
Add a Ginkgo spec file under `test/integration/world/` with the `//go:build integration` tag, driving
the real stack through `integrationtest.Start(t)`.

Drive the flow through the real `RetireCharacter` command and let the outbox relay carry the envelope —
do not publish a synthetic `character_retired` event to shortcut the relay. The point of the spec is
that the production path works end to end, and the relay hop is exactly where the eventual consistency
D-36 accepts becomes observable. Use `Eventually` for every post-retire assertion; a synchronous
assertion after the command returns will be flaky by construction.

Assert against real state: query the `sessions` table for the character (expect none), the `characters`
row's `location_id` (expect the starting location), and the emitted events. Never assert through a
populated Go struct returned by the code under test (PORTAL-10 rule 3).

Cover the redelivery case explicitly. Redeliver the same message — either by driving the consumer's
handler a second time with the same message or by letting an un-acked message redeliver — and assert
the totals are UNCHANGED: still exactly one leave, still exactly one `character_moved` envelope. Assert
with exact counts, not with non-emptiness.

Cover the un-retire-between-emit-and-delivery case: retire, un-retire before the reactor processes, then
let the message land, and assert the session survives and the location is unchanged — the 03-02 status
guard doing its job at the full-stack tier.

If `task test:int`'s package selection needs updating for this file, check first: `test:int` runs
`{{.CLI_ARGS | default "./..."}}` with no package enumeration, so a new package under
`test/integration/` is picked up automatically and no Taskfile edit is required. Confirm this rather
than assuming it, and record the result in the SUMMARY.
  </action>
  <verify>
    <automated>task test:int -- -run TestRetirementReactor ./test/integration/world/</automated>
  </verify>
  <acceptance_criteria>
    - `task test:int -- -run TestRetirementReactor ./test/integration/world/` exits 0.
    - `task test:int` exits 0 overall.
    - The redelivery spec asserts EXACT counts (exactly one leave, exactly one `character_moved` envelope) rather than non-emptiness.
    - `rg -n 'natstest' test/integration/world/retirement_reactor_test.go` returns no hits — embedded NATS is the correct tier here and the spec does not over-provision.
    - `rg -n 'Eventually' test/integration/world/retirement_reactor_test.go` returns hits on the post-retire assertions.
    - A spec covers the un-retired-before-delivery case and asserts the session survives.
    - `git diff --stat test/integration/world/character_lifecycle_test.go` shows no change from this plan — the existing INV-WORLD-6 block is untouched.
  </acceptance_criteria>
  <done>The full production path — command, outbox, relay, JetStream, reactor, sessions, presence, move — is proven end to end, and proven idempotent under redelivery.</done>
  <reversibility rating="reversible">A test-only addition.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JetStream -> reactor handler | At-least-once delivery into a loop that ends sessions and moves characters — a privileged effect set driven by a replayable message. |
| reactor -> `world.Service.MoveCharacter` | A system subject crossing the ABAC chokepoint into a character write. |
| reactor -> `session.Store` | Session teardown for a character the reactor did not authenticate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-30 | Elevation of Privilege | reactor's `MoveCharacter` authorization | high | mitigate | Prefixed `"system:retirement"` subject evaluated against the narrow `seed:system-retirement-character` permit. `access.WithSystemSubject` is forbidden by a grep-level acceptance criterion, so the bypass cannot be reintroduced silently. |
| T-03-31 | Tampering | redelivery evicting a character un-retired since emit | high | mitigate | The 03-02 status guard ack-and-skips unless the character is still `retired`; asserted at both the unit and full-stack tiers. |
| T-03-32 | Tampering | redelivery double-emitting leave or double-moving | medium | mitigate | Every effect gated on observed state — `DeleteByCharacter` returning `(nil, nil)` skips the emits, an already-at-start location skips the move. Exact-count assertions in the integration spec. |
| T-03-33 | Denial of Service | a retired character retaining a live session because teardown silently failed | high | mitigate | Session teardown is the single fail-closed effect: NAK for redelivery on error rather than ack. Unit-tested explicitly. |
| T-03-34 | Information Disclosure | leave notification naming the wrong location | low | mitigate | Emit ordering pinned (leave before move) with a unit test asserting the emitted location id differs from the starting location id. |
| T-03-35 | Repudiation | retirement-caused session ends indistinguishable from evictions | medium | mitigate | Distinct `SessionEndedCauseRetired` wire value plus a pairwise-distinctness test over all seven causes. |
| T-03-36 | Denial of Service | poison message looping forever on NAK | medium | accept | Bounded by the consumer's `MaxDeliver` configured in 03-02; exhausted deliveries follow the existing JetStream dead-letter path rather than looping indefinitely. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed. |
</threat_model>

<verification>
- `task test -- ./internal/core/ ./internal/world/setup/` green.
- `task test:int -- -run TestRetirementReactor ./test/integration/world/` green, and `task test:int` green overall.
- `task lint` and `task fmt` green with `task fmt` mutations committed.
- No `WithSystemSubject`, no bare `"system"` subject, and no scene/channel/page/story fanout anywhere in `internal/world/setup/`.
</verification>

<success_criteria>
- ROADMAP success criterion 2 is closed: a host reactor consuming `character_retired` ends the character's live sessions, notifies the location it left, and moves it to the configured starting location.
- The fanout is idempotent under at-least-once redelivery, proven with exact counts.
- The `MoveCharacter -> MovementHook -> UpdateLocationOnMove` pipeline gets its first non-simulated production exercise (#4788).
</success_criteria>

<output>
Create `.planning/phases/03-world-character-commands/03-04-SUMMARY.md` when done.
</output>
