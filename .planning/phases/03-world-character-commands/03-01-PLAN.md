---
phase: 03-world-character-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/world/outbox/taxonomy.go
  - internal/world/service.go
  - internal/world/mutator.go
  - internal/world/repository.go
  - internal/world/postgres/character_repo.go
  - internal/world/worldtest/mock_CharacterRepository.go
  - internal/world/service_retire_test.go
  - internal/world/postgres/character_repo_status_test.go
  - internal/world/postgres/character_repo_status_integration_test.go
  - test/meta/world_sql_fence_test.go
autonomous: true
requirements: [IDENT-04, IDENT-10]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "RetireCharacter transitions characters.status active→retired with a version bump and exactly ONE character_retired envelope written in the same transaction (D-31/D-32/D-33, IDENT-04, IDENT-10)"
    - "UnretireCharacter transitions retired→active with exactly ONE character_unretired envelope; on a non-retired character it returns typed error CHARACTER_NOT_RETIRED, never silent success (D-31, IDENT-04)"
    - "A stale CAS on retire/unretire surfaces WORLD_CONCURRENT_EDIT via classifyCASZeroRow; an absent row surfaces CHARACTER_NOT_FOUND — the two zero-row outcomes are distinguished (IDENT-10)"
    - "Retiring the player's default character clears players.default_character_id in the SAME transaction; a failed CAS leaves the pointer untouched; unretire does NOT restore it (D-34)"
    - "Retire/Unretire of a nonexistent character id returns CHARACTER_NOT_FOUND (empty-input edge, IDENT-04)"
    - "No caller-supplied string reaches SQL from the new commands: status is a typed world.Status const bound as a query parameter, and name/normalized_name/name_skeleton bytes are unchanged by both commands (encoding edge, IDENT-04)"
    - "Both commands REQUIRE a caller-supplied expected_version >= 1 and reject 0/negative with typed CHARACTER_VERSION_REQUIRED before any read — the repo layer's expectedVersion-0 unversioned-write affordance stays repo-only and is unreachable through the commands (empty-value edge, IDENT-10, INV-WORLD-7)"
    - "A stale caller-supplied expected_version surfaces WORLD_CONCURRENT_EDIT BEFORE any lifecycle-state guard: a stale retire against a row another writer already retired returns the typed conflict, never CHARACTER_ALREADY_RETIRED (guard-order pin, IDENT-10, INV-WORLD-7; review disposition R1)"
    - "expected_version comparison is numeric SQL equality on the INTEGER version column (AND version = $3), never a string comparison (encoding edge, IDENT-10)"
  artifacts:
    - path: "internal/world/outbox/taxonomy.go"
      provides: "KindCharacterRetired + KindCharacterUnretired consts, registry entries, characterLifecyclePayload schema, AppSchemaVersion bumped to 2"
      contains: "KindCharacterRetired"
    - path: "internal/world/service.go"
      provides: "Service.RetireCharacter + Service.UnretireCharacter (caller-shaped), BuildCharacterLifecyclePayload, local kind consts"
      contains: "func (s *Service) RetireCharacter"
    - path: "internal/world/mutator.go"
      provides: "Two writeCommands census rows + setCharacterStatus executor routing through mutate()"
      contains: "RetireCharacter"
    - path: "internal/world/postgres/character_repo.go"
      provides: "SetStatus CAS write with same-tx players.default_character_id clear on retire"
      contains: "func (r *CharacterRepo) SetStatus"
  key_links:
    - from: "internal/world/service.go"
      to: "internal/world/mutator.go"
      via: "RetireCharacter/UnretireCharacter bodies reference the s.mutator selector — the census AST cross-check keys on exactly this"
      pattern: "s\\.mutator\\.setCharacterStatus"
    - from: "internal/world/mutator.go"
      to: "internal/world/postgres/character_repo.go"
      via: "setCharacterStatus closure calls characterWriter.SetStatus inside mutate() — one tx = write + envelope"
      pattern: "characterWriter\\.SetStatus"
    - from: "internal/world/mutator.go"
      to: "internal/world/outbox/taxonomy.go"
      via: "census Kind strings must equal declared taxonomy kinds (bijection assertion A)"
      pattern: "character_retired"
  prohibitions:
    - statement: "Retire MUST NOT destroy data: no row delete, no entity_properties cascade, no tombstone — DeleteCharacter's irreversible path is untouched by the retire flow (PORTAL-04, success criterion 1)"
      status: kept
      requirement_id: IDENT-04
      category: safety
    - statement: "Retire MUST NOT release the character's name reservation — name, normalized_name, name_skeleton columns are untouched (INV-WORLD-6 retire half)"
      status: kept
      requirement_id: IDENT-04
      category: values
    - statement: "This phase MUST NOT add any transition into the idle status (D-43, bound INV-WORLD-5) and MUST NOT reintroduce RenameCharacter or a character_renamed taxonomy kind (D-44)"
      status: kept
      requirement_id: IDENT-04
      category: values
---

<objective>
Land the two domain commands: `world.Service.RetireCharacter` and `world.Service.UnretireCharacter` (D-31), routed through `s.mutator` per the census AST cross-check (D-33 corrected precedent: `UpdateCharacterDescription`/`UpdateCharacterPreferences`, NOT `Rename`), with two new taxonomy kinds `character_retired`/`character_unretired` (D-32), a new `CharacterRepository.SetStatus` CAS write, and the same-transaction `players.default_character_id` clear (D-34). Both commands carry a CALLER-SUPPLIED `expectedVersion int` (the m12/`UpdateLocation` struct-held-version precedent, per INV-WORLD-7's guarded-mutation wording and review disposition R1 — NOT the internally-read `UpdateCharacterPreferences` version threading; that precedent still governs the service/mutator ROUTING shape only). Ownership is NOT checked in the commands — they call `checkAccess` with the distinct actions `retire`/`unretire` (D-39; D-40's action split stands); the grants land in plan 03-04, where retire/unretire are ADMIN-ONLY for v0.13 (user decision 2026-08-07 superseding D-40's player-retires half).

Purpose: IDENT-04's domain half (soft retire, reversible, record+name preserved) and IDENT-10 (version-guarded, in-transaction outbox emission) for the two new mutations.
Output: Both commands compile against Phase 02.1's landed typed-caller surface, the census meta-tests are green with two new rows, and unit + integration tests prove the CAS split, the same-tx D-34 clear, and the exactly-one-envelope property.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-world-character-commands/03-CONTEXT.md
@.planning/phases/03-world-character-commands/03-RESEARCH.md
@.planning/phases/03-world-character-commands/03-PATTERNS.md
</context>

<caller_shape_note>
**Phase 02.1 interlock (binding).** Every `world.Service` signature in this plan is written caller-shaped: `(ctx context.Context, caller <02.1 Caller type>, …)` instead of `(ctx, subjectID string, …)`. The constructor names `world.HumanCaller(subjectID)` / `world.JobCaller(name, prov)` / `world.SystemCaller()` are `[ASSUMED — 02.1]` (pinned only by 02.2-CONTEXT D-56/D-57; no code exists at plan time). At execution time 02.1 HAS landed (it blocks 02.2 which blocks this phase): reconcile the exact `Caller` type name, constructor arities, and the landed `checkAccess` signature against `internal/world/service.go` as it exists then. The mutator/repo/payload/error-mapping layers below `checkAccess` are unaffected by 02.1 (RESEARCH §0.2).
</caller_shape_note>

<review_disposition>
**R1 — Codex HIGH ×2 ("expectedVersion is discarded" / "semantic-state checks mask stale-version conflicts"), corroborated by the source-grounding pass. ADOPTED: caller-supplied expected version + pinned guard order.** Both commands take `expectedVersion int` from the caller and the guard chain is fixed: (0) reject `expectedVersion <= 0` with typed `CHARACTER_VERSION_REQUIRED`; (1) `checkAccess`; (2) `Get`; (3) version precheck — `char.Version != expectedVersion` → `WORLD_CONCURRENT_EDIT` immediately; (4) lifecycle-state guards (Task 2); (5) CAS passing the CALLER's `expectedVersion` (never the freshly-read `char.Version` — that would make the guard vacuous for caller staleness). Rationale: INV-WORLD-7 (`docs/architecture/invariants.yaml:5108-5118`) requires existing-row character mutations to carry `expected_version`, reject absent/zero, and reject stale with the typed signal; the precheck-before-status-guard order is what makes "a stale caller sees the conflict, never the racing writer's outcome" true, and it is what lets 03-03 drive a deterministic SEQUENTIAL two-replica stale write (m12's struct-held-version mechanism, `m12_lastwritewins_test.go:142-155`). The rejected alternative (internal read, repo-interleave test hook) was declined because it makes staleness unreachable from the public command by construction.

**R2 — SG-5 (stale `character_repo.go:204-205` forward-reference to a D-44-deleted command). ADOPTED: folded into Task 1** — the executor corrects the doc comment while already reading that exact range.
</review_disposition>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — RetireCharacter end-to-end through mutate() with the census green</name>
  <reversibility rating="one-way">D-32: a taxonomy kind is a published wire contract with durable audit rows carrying it. The human already chose this exact shape in discuss-phase (CONTEXT records the rating), so no gating checkpoint is inserted — the rating is kept for the record.</reversibility>
  <precondition>Phase 02.1 has landed: `rg -n "HumanCaller|JobCaller|type Caller" internal/world/` returns the typed-caller surface. If it returns nothing, STOP — this phase executes only after 02.1/02.2 (phase-order violation; RESEARCH §0.2 Pitfall 10).</precondition>
  <files>internal/world/outbox/taxonomy.go, internal/world/service.go, internal/world/mutator.go, internal/world/repository.go, internal/world/postgres/character_repo.go, internal/world/worldtest/mock_CharacterRepository.go, internal/world/service_retire_test.go, internal/world/postgres/character_repo_status_test.go</files>
  <behavior>
    - RetireCharacter happy path: expectedVersion >= 1 validated → checkAccess("retire") → Get → version precheck (char.Version == expectedVersion) → payload → intent(kind character_retired) → mutator → SetStatus CAS with the CALLER's expectedVersion → nil error; mock expectations prove the executor received StatusRetired and the caller-supplied version (R1)
    - RetireCharacter with expectedVersion 0 or negative → typed CHARACTER_VERSION_REQUIRED; neither Get nor the mutator is invoked (INV-WORLD-7: the unversioned affordance is unreachable through the command)
    - RetireCharacter with a stale expectedVersion (Get returns a higher version) → WORLD_CONCURRENT_EDIT from the version precheck, BEFORE any lifecycle-state guard; the mutator is NOT invoked (R1 guard-order pin)
    - RetireCharacter on nonexistent id → CHARACTER_NOT_FOUND (errutil.AssertErrorCode)
    - RetireCharacter when the executor returns ErrConcurrentEdit (row changed between Get and UPDATE) → WORLD_CONCURRENT_EDIT (world.CodeConcurrentEdit)
    - SetStatus (repo, mocked pool or integration): stale expectedVersion with existing row → WORLD_CONCURRENT_EDIT; absent row → CHARACTER_NOT_FOUND; expectedVersion 0 → unversioned write succeeds regardless of current version (repo-only affordance)
    - Census meta-tests observed RED mid-task (descriptor before kind), then GREEN
  </behavior>
  <action>
    RED first (commit `test(03-01): …`): (1) add the census descriptor row `{Command: "RetireCharacter", Kind: kindCharacterRetired}` to `writeCommands` in internal/world/mutator.go plus the bare local const in service.go's kind block, run `task test -- -run 'TestWorldEnvelopeCensus' ./test/meta/`, and OBSERVE both failures — assertion A fails IsDeclared (kind not in outbox registry) and assertion B fails "census descriptor %q is not a world.Service method routing through the executor" (PORTAL-10 rule 4: gates demonstrated RED). (2) Write internal/world/service_retire_test.go and internal/world/postgres/character_repo_status_test.go covering the behaviors above, with minimal method stubs (declarations returning a typed not-implemented error) so RED fails on assertions, not merely compile (repo learning: compile-only red is degenerate).

    GREEN (commit `feat(03-01): …`), per D-33's corrected precedent — copy `UpdateCharacterDescription` (service.go:799-836) for the service shape and `UpdateCharacterPreferences`/`updateCharacterPreferences` (service.go:856-891, mutator.go:245-255) for the version threading:
    - internal/world/outbox/taxonomy.go: exported const `KindCharacterRetired = "character_retired"` in the character block; registry entry `{Kind: KindCharacterRetired, Aggregate: wmodel.AggregateCharacter, SchemaVersion: 1, Payload: characterLifecyclePayload}`; new `characterLifecyclePayload []PayloadField` declaring `character_id` (ulid) + `status` (string) — new-values-only, erasure-safe per the registry rule at taxonomy.go:122-124 (the existing characterUpdatePayload is NOT reusable); bump `AppSchemaVersion` 1→2 (documented obligation, taxonomy.go:13-19; this single bump covers both kinds this plan adds — Task 2 does not bump again).
    - internal/world/repository.go: add `SetStatus(ctx context.Context, characterID ulid.ULID, status Status, expectedVersion int) (*wmodel.MutationDelta, error)` to `CharacterRepository` with a doc comment stating: version-predicated CAS; expectedVersion 0 = unversioned write; returns the delta and writes NO envelope (mutate() writes it); on retire also clears players.default_character_id (D-34 lands in Task 3).
    - internal/world/postgres/character_repo.go: implement SetStatus per the PATTERNS core pattern — `UPDATE characters SET status = $2, version = version + 1 WHERE id = $1` with conditional `AND version = $3` when expectedVersion > 0, `RETURNING version`, inside withTx; `pgx.ErrNoRows` → `classifyCASZeroRow(txCtx, tx, SELECT version FROM characters WHERE id = $1 FOR UPDATE, id, CHARACTER_NOT_FOUND-wrap)`; other errors → oops.Code CHARACTER_STATUS_UPDATE_FAILED; delta via `primaryDeltaVersioned(wmodel.AggregateCharacter, id, false, newVersion-1, newVersion)`. Bind status as `string(status)` — a typed const, never caller text. While in this file (R2/SG-5): correct the stale doc-comment sentence at :204-205 that names a Phase-3 Rename command as a live direct caller of `Rename` — D-44 removed that command from the milestone; reword the sentence so the shipped operator CLI (`holomush character name set`) is the named caller motivating the in-method envelope write, leaving the rest of the comment (including the MUST-NOT-route-through-mutate rule) untouched.
    - Regenerate internal/world/worldtest/mock_CharacterRepository.go with `mockery`; run `task test:int` afterwards to catch any other CharacterRepository implementations (interface widening breaks test fakes silently under `task test` alone).
    - internal/world/mutator.go: add executor `setCharacterStatus(ctx, intent wmodel.EnvelopeIntent, characterID ulid.ULID, status Status, expectedVersion int) (*wmodel.MutationDelta, error)` that returns `m.mutate(ctx, intent, closure calling m.characterWriter.SetStatus(txCtx, characterID, status, expectedVersion))` — one shared executor for both commands (the census keys on Service methods, not executor methods).
    - internal/world/service.go: `BuildCharacterLifecyclePayload(characterID ulid.ULID, status Status) ([]byte, error)` mirroring BuildCharacterUpdatePayload (service.go:818); `RetireCharacter(ctx, caller <02.1 type>, characterID ulid.ULID, expectedVersion int) error` implementing the R1 guard chain IN THIS ORDER — (0) `expectedVersion <= 0` → oops.Code CHARACTER_VERSION_REQUIRED (before any read; INV-WORLD-7 keeps the repo's unversioned affordance unreachable from the command); (1) nil-guard characterRepo; `checkAccess` with action "retire" on `access.CharacterResource(characterID.String())` and `prefixCharacter` (D-40: distinct action; D-39: NO ownership predicate in Go — policy is the control); (2) `Get` (its Status feeds Task 2's guards); (3) version precheck: `char.Version != expectedVersion` → oops.Code(CodeConcurrentEdit).With("character_id", …).With("expected_version", …) — MUST run before any lifecycle-state guard so a stale caller sees the conflict, never the racing writer's outcome; (4) nil-guard mutator; build payload with StatusRetired; `intent := s.buildIntent(kindCharacterRetired, wmodel.AggregateCharacter, characterID, <subject per landed 02.1 shape>, payload)`; (5) call `s.mutator.setCharacterStatus(…, StatusRetired, expectedVersion)` — pass the CALLER's expectedVersion, never the freshly-read char.Version; map executor errors — ErrConcurrentEdit → oops.Code(CodeConcurrentEdit).With("character_id", …), ErrNotFound → CHARACTER_NOT_FOUND, else CHARACTER_RETIRE_FAILED.
    Run `task fmt` (mutates files — commit the edits) and `task lint` before each commit.
  </action>
  <verify>
    <automated>task test -- ./internal/world/... ./test/meta/ && task test:int -- ./internal/world/...</automated>
  </verify>
  <done>Census bijection + AST cross-check green with the RetireCharacter row; retire happy path, CHARACTER_VERSION_REQUIRED, stale-precheck, CHARACTER_NOT_FOUND, WORLD_CONCURRENT_EDIT, and repo-unversioned-0 behaviors all pass; the R1 guard order is unit-proven; git log shows test(03-01) before feat(03-01).</done>
  <acceptance_criteria>
    - `rg -c '"RetireCharacter"' internal/world/mutator.go` returns exactly 1
    - `rg -q 'KindCharacterRetired' internal/world/outbox/taxonomy.go` and `rg -q 'AppSchemaVersion = 2' internal/world/outbox/taxonomy.go` both succeed
    - `task test -- -run 'TestWorldEnvelopeCensus' ./test/meta/` exits 0
    - `rg -q 'func \(s \*Service\) RetireCharacter' internal/world/service.go` succeeds, the signature carries `expectedVersion int`, and the method body contains the `s.mutator` selector
    - `errutil.AssertErrorCode` assertions for CHARACTER_VERSION_REQUIRED, WORLD_CONCURRENT_EDIT, and CHARACTER_NOT_FOUND pass in the new unit tests; the stale-precheck test pairs its conflict assertion with a mock expectation that the mutator was NOT invoked
    - `! rg -q "RenameCharacter calls this method" internal/world/postgres/character_repo.go` succeeds (R2/SG-5: the stale forward-reference is corrected)
    - `git log --oneline --grep='^test(03-01)'` and `--grep='^feat(03-01)'` each return a commit, test first
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UnretireCharacter + exhaustive status-transition guards on both commands</name>
  <reversibility rating="costly">D-31: each command is a census row and a published taxonomy kind; collapsing the pair later rewrites the census bijection and orphans an emitted kind (rating carried from CONTEXT).</reversibility>
  <files>internal/world/outbox/taxonomy.go, internal/world/service.go, internal/world/mutator.go, internal/world/service_retire_test.go</files>
  <behavior>
    - UnretireCharacter on a retired character (fresh expectedVersion) → status active, one character_unretired envelope, version bump
    - UnretireCharacter with expectedVersion 0 or negative → typed CHARACTER_VERSION_REQUIRED, no read, no write
    - UnretireCharacter with a stale expectedVersion → WORLD_CONCURRENT_EDIT from the version precheck, BEFORE the not-retired guard — a stale unretire racing a completed unretire gets the conflict, never CHARACTER_NOT_RETIRED (R1 guard order)
    - UnretireCharacter on an active or idle character (fresh version) → typed error CHARACTER_NOT_RETIRED (fail-loudly discretion from CONTEXT), no envelope, no write
    - RetireCharacter on an already-retired character (fresh version) → typed error CHARACTER_ALREADY_RETIRED, no envelope, no write
    - RetireCharacter on an idle character → proceeds (idle→retired is a legal exit; only the transition INTO idle is absent per D-43/INV-WORLD-5)
    - Both commands: an unrecognized stored status hits a denying default arm (INV-WORLD-5 exhaustive-read rule)
    - UnretireCharacter on a nonexistent id → CHARACTER_NOT_FOUND
  </behavior>
  <action>
    RED: extend service_retire_test.go with the behaviors above; add the `{Command: "UnretireCharacter", Kind: kindCharacterUnretired}` census row first and observe the census RED (undeclared kind), commit `test(03-01)`. GREEN: add `KindCharacterUnretired = "character_unretired"` const + registry entry in taxonomy.go (reusing `characterLifecyclePayload`; AppSchemaVersion stays 2 — one taxonomy revision for the whole plan); local const `kindCharacterUnretired` in service.go; `UnretireCharacter(ctx, caller, characterID, expectedVersion int)` mirroring RetireCharacter's full R1 guard chain (version-required check, checkAccess with action "unretire" per D-40, Get, version precheck, status guard, CAS with the caller's expectedVersion), payload status StatusActive, kind kindCharacterUnretired, error codes CHARACTER_VERSION_REQUIRED / CHARACTER_UNRETIRE_FAILED / CHARACTER_NOT_FOUND / CodeConcurrentEdit. Add the transition guards to BOTH commands AFTER the version precheck (R1: version conflict outranks lifecycle state), as an exhaustive `switch char.Status` over the closed vocabulary (StatusActive, StatusRetired, StatusIdle) with a denying default arm returning the command's _FAILED code for an unknown value: retire rejects StatusRetired with CHARACTER_ALREADY_RETIRED; unretire rejects StatusActive and StatusIdle with CHARACTER_NOT_RETIRED. Guards run BEFORE buildIntent so a rejected transition writes nothing and emits nothing. Note in the unretire doc comment that it does NOT restore players.default_character_id (D-34 asymmetry — the old pointer value is gone by design).
  </action>
  <verify>
    <automated>task test -- ./internal/world/... ./test/meta/</automated>
  </verify>
  <done>Both census rows registered; both kinds declared; all transition-guard behaviors pass; commit pair test(03-01)/feat(03-01) present for this task.</done>
  <acceptance_criteria>
    - `rg -c '"UnretireCharacter"' internal/world/mutator.go` returns exactly 1
    - `rg -q 'KindCharacterUnretired' internal/world/outbox/taxonomy.go` succeeds; `rg -c 'AppSchemaVersion = ' internal/world/outbox/taxonomy.go` returns 1 (still the single const, value 2)
    - Unit tests assert CHARACTER_NOT_RETIRED and CHARACTER_ALREADY_RETIRED via errutil.AssertErrorCode, each paired with a mock expectation that the mutator was NOT invoked
    - A stale-unretire unit test asserts WORLD_CONCURRENT_EDIT (not CHARACTER_NOT_RETIRED) when expectedVersion lags the row — the R1 guard order holds on both commands
    - `task test -- -run 'TestWorldEnvelopeCensus' ./test/meta/` exits 0 with both rows present
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-34 same-transaction players.default_character_id clear + fence-widening documentation</name>
  <files>internal/world/postgres/character_repo.go, internal/world/postgres/character_repo_status_test.go, internal/world/postgres/character_repo_status_integration_test.go, test/meta/world_sql_fence_test.go</files>
  <behavior>
    - Retiring a character that is some player's default: both the status write and the pointer clear commit together
    - Retiring with a stale version (CAS failure): the transaction aborts and players.default_character_id is UNCHANGED (same-tx atomicity proven via the CAS failure path, no fault injection needed)
    - Retiring a character that is nobody's default: zero players rows affected, no error
    - Setting status to active (unretire path): the players table is NOT touched
  </behavior>
  <action>
    RED: write internal/world/postgres/character_repo_status_integration_test.go (build tag `//go:build integration`, mirroring the existing testcontainer harness pattern in internal/world/postgres — find the dir's existing *_integration_test.go setup and copy its pool/migration bootstrap) covering the behaviors above; observe failure; commit test(03-01). GREEN: inside SetStatus's existing withTx body, immediately after the successful version scan and only when `status == world.StatusRetired`, execute `UPDATE players SET default_character_id = NULL WHERE default_character_id = $1` via `tx.Exec`; wrap failures as oops.Code CHARACTER_RETIRE_DEFAULT_CLEAR_FAILED. This write is idempotent (no read-modify-write) and safe under redelivery. Document the layering widening: the reaping-guard precedent (internal/world/postgres/reaping_guard.go, described in world_sql_fence_test.go:49-56) is an auth-table READ on the world conn; D-34 extends it to a WRITE. Add one sentence to the site's doc comment and one sentence to the fence test's doc block (test/meta/world_sql_fence_test.go) naming this as a second intentional, durable auth-table exception per D-34 — so it reads as deliberate, not accidental, to the next reviewer. Note: `players` is NOT in fencedWorldTables (world_sql_fence_test.go:62-65), so no fence-test allowlist change is needed — only prose.
  </action>
  <verify>
    <automated>task test -- ./internal/world/postgres/ && task test:int -- ./internal/world/postgres/</automated>
  </verify>
  <done>All four D-34 behaviors pass under the integration harness; fence meta-test still green; doc prose landed at both sites; commit pair present.</done>
  <acceptance_criteria>
    - `rg -q 'default_character_id = NULL' internal/world/postgres/character_repo.go` succeeds and the statement executes inside the same withTx body as the status UPDATE
    - Integration test proves: successful retire → pointer NULL; stale-version retire → pointer unchanged AND status unchanged
    - `task test -- -run 'TestWorldSQLFence' ./test/meta/` exits 0
    - `rg -q 'D-34' test/meta/world_sql_fence_test.go internal/world/postgres/character_repo.go` succeeds (the widening is documented at both sites)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command entry → world.Service | caller identity crosses into the ABAC chokepoint via checkAccess (D-39: policy is the ONLY ownership control) |
| service → PostgreSQL | status values and ids cross into SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | Service.RetireCharacter/UnretireCharacter | high | mitigate | D-39/D-40: commands enforce checkAccess with distinct retire/unretire actions and carry NO ownership predicate in Go; the authorization surface (ADMIN-ONLY for v0.13 — user decision 2026-08-07) + paired denial/positive tests land in 03-04 (this plan's commands are unreachable without a grant — ABAC is default-deny) |
| T-03-02 | Tampering | SetStatus SQL | low | mitigate | status is a typed world.Status const bound as parameter $2; ids are ULIDs stringified; no caller text is interpolated |
| T-03-03 | Tampering | D-34 players write on the world conn | medium | mitigate | idempotent single-statement clear, same tx as the CAS write; widening documented at the site and in the fence-test doc block |
| T-03-04 | Repudiation | envelope emission | low | mitigate | exactly-one-envelope per command via mutate(); census bijection meta-test enforces the closed set |
</threat_model>

<verification>
- `task test -- ./internal/world/... ./test/meta/` — unit + census + fence green
- `task test:int -- ./internal/world/...` — repo integration + interface-widening compile check
- `task lint` before every commit; `task fmt` output committed
- Prohibition canon-referral note: SQL-injection-class concerns are canon (parameterized queries, OWASP) — covered by T-03-02, deliberately NOT minted as a bespoke prohibition
</verification>

<success_criteria>
- Both commands exist as `*Service` methods routing through `s.mutator` (census AST cross-check green in both directions)
- Two kinds + two census rows + AppSchemaVersion 2, landed in the same plan that introduces the commands (ROADMAP success criterion 4)
- Both commands carry caller-supplied expected_version with the R1 guard order (version-required → precheck-conflict → status guard) unit-proven; stale CAS → WORLD_CONCURRENT_EDIT proven at unit level (the two-replica proof is plan 03-03)
- D-34 same-tx clear proven under the integration harness
- No name column is touched by either command; DeleteCharacter's path has zero diffs
</success_criteria>

<output>
Create `.planning/phases/03-world-character-commands/03-01-SUMMARY.md` when done. Record: the landed 02.1 caller shape actually used (constructor names/arities), any reconciliation deltas from the [ASSUMED] shapes, and the RED observations for the census.
</output>

## Artifacts this phase produces (plan 03-01)

New symbols created by this plan (source-grounding: exclude from drift verification):
- `internal/world/outbox`: `KindCharacterRetired`, `KindCharacterUnretired`, `characterLifecyclePayload`
- `internal/world`: `kindCharacterRetired`, `kindCharacterUnretired` (local consts), `BuildCharacterLifecyclePayload`, `Service.RetireCharacter`, `Service.UnretireCharacter`, `worldMutator.setCharacterStatus`, `CharacterRepository.SetStatus` (interface method)
- `internal/world/postgres`: `(*CharacterRepo).SetStatus`
- Error codes: `CHARACTER_RETIRE_FAILED`, `CHARACTER_UNRETIRE_FAILED`, `CHARACTER_ALREADY_RETIRED`, `CHARACTER_NOT_RETIRED`, `CHARACTER_VERSION_REQUIRED`, `CHARACTER_STATUS_UPDATE_FAILED`, `CHARACTER_RETIRE_DEFAULT_CLEAR_FAILED`
- ABAC action strings: `retire`, `unretire` (open vocabulary — no engine change)
- Test files: `internal/world/service_retire_test.go`, `internal/world/postgres/character_repo_status_test.go`, `internal/world/postgres/character_repo_status_integration_test.go`
- Regenerated: `internal/world/worldtest/mock_CharacterRepository.go`
