---
phase: 03-world-character-commands
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/eventbus/consumer/consumer.go
  - internal/eventbus/consumer/consumer_test.go
  - internal/eventbus/audit/projection.go
  - internal/eventbus/audit/plugin_consumer.go
  - internal/eventbus/audit/projection_unit_test.go
  - internal/retirement/subsystem.go
  - internal/retirement/subsystem_test.go
  - internal/charactivity/subsystem.go
  - internal/charactivity/subsystem_test.go
  - internal/lifecycle/subsystem.go
  - internal/lifecycle/subsystemid_string.go
  - cmd/holomush/core.go
  - cmd/holomush/core_subsystems_test.go
  - cmd/holomush/core_topo_order_test.go
autonomous: true
requirements: [IDENT-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "consumer.CreateWithRetry is importable from the neutral internal/eventbus/consumer package; both audit callers route through it with their original error codes AUDIT_CONSUMER_CREATE_FAILED and AUDIT_PLUGIN_CONSUMER_CREATE_FAILED intact (D-46)"
    - "Exactly 20 production subsystems register; every subsystem count, distinctness, real-constructor-graph, and pinned topological-order test is green at 20 (D-36/D-42 consequence: SubsystemID 18→20)"
    - "Both new subsystem constructors allocate nothing and touch no live resources — the real-constructor graph tests construct them directly"
    - "The two new SubsystemIDs are appended at the const-block END and the stringer is regenerated via task generate, never hand-edited"
    - "charactivity exposes a NewSubsystemWithStorage seam so tests can force jetstream.MemoryStorage (D-42 corrected hazard: FileStorage is the zero value — an unset bucket is file-backed everywhere)"
  artifacts:
    - path: "internal/eventbus/consumer/consumer.go"
      provides: "Exported CreateWithRetry + CreateBackoffs relocated verbatim from package audit (D-46)"
      contains: "func CreateWithRetry"
    - path: "internal/retirement/subsystem.go"
      provides: "Retirement reactor lifecycle.Subsystem skeleton (ID SubsystemRetirementReactor)"
      contains: "SubsystemRetirementReactor"
    - path: "internal/charactivity/subsystem.go"
      provides: "Character-activity lifecycle.Subsystem skeleton with the WithStorage test seam (ID SubsystemCharacterActivity)"
      contains: "NewSubsystemWithStorage"
    - path: "internal/lifecycle/subsystem.go"
      provides: "SubsystemRetirementReactor + SubsystemCharacterActivity appended at const-block END"
      contains: "SubsystemCharacterActivity"
  key_links:
    - from: "internal/eventbus/audit/projection.go"
      to: "internal/eventbus/consumer/consumer.go"
      via: "audit projector creates its durable consumer through the relocated shared helper"
      pattern: "consumer\\.CreateWithRetry"
    - from: "cmd/holomush/core.go"
      to: "internal/retirement/subsystem.go"
      via: "productionSubsystemSet composite literal constructs the reactor subsystem"
      pattern: "retirement\\.NewSubsystem"
    - from: "cmd/holomush/core.go"
      to: "internal/charactivity/subsystem.go"
      via: "productionSubsystemSet composite literal constructs the activity subsystem"
      pattern: "charactivity\\.NewSubsystem"
---

<objective>
Land the shared substrate both Phase-3 subsystems stand on, once: the D-46 relocation of the consumer-create retry helper to a neutral package, the two new subsystem skeleton packages (`internal/retirement` for the D-36 reactor, `internal/charactivity` for the D-42 activity flusher), and the FULL 13-site `SubsystemID` 18→20 composition cascade — worked mechanically from RESEARCH §3.2's verified site table, NOT the refuted "5-site" framing.

Purpose: plans 03-04 and 03-05 fill in behavior without re-touching the lifecycle registry, the fixed-size stub arrays, or the pinned topological order; and the reactor's consumer creation (plus 02.2's provenance stamping per D-55) has one shared, neutral home.
Output: 20 registered subsystems (two inert-but-real skeletons), a green meta-suite at 20, and `consumer.CreateWithRetry` consumed by both audit callers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-world-character-commands/03-CONTEXT.md
@.planning/phases/03-world-character-commands/03-RESEARCH.md
@.planning/phases/03-world-character-commands/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-46 — relocate the consumer-create retry helper to internal/eventbus/consumer</name>
  <reversibility rating="costly">D-46: the relocated wrapper is 02.2's designated provenance stamp site (D-55) and the backoff schedule is an operational constant that must not diverge; undoing the move re-touches both audit callers and whatever 02.2 built on it.</reversibility>
  <precondition>Interlock §0.4-1 check (read-only): run `rg -l "CreateWithRetry|createConsumerWithRetry" internal/eventbus/ --glob '!internal/eventbus/audit/*'`. If a neutral shared package already exists (02.2 pulled the relocation forward for its fixture job), this task shrinks to CONSUME it: verify both audit callers are migrated with codes intact, add anything missing, and skip creation. Do not build a second relocation.</precondition>
  <files>internal/eventbus/consumer/consumer.go, internal/eventbus/consumer/consumer_test.go, internal/eventbus/audit/projection.go, internal/eventbus/audit/plugin_consumer.go, internal/eventbus/audit/projection_unit_test.go</files>
  <action>
    Create package `internal/eventbus/consumer` (SPDX header) holding the retry helper moved VERBATIM from internal/eventbus/audit/projection.go: the backoff table at :93-96 becomes exported `var CreateBackoffs = []time.Duration{100ms, 250ms}` (stays a var so tests can shorten it — preserve the withShortBackoffs seam described at projection.go:88-92, migrating or re-pointing that helper), and the function at :167-188 becomes exported `CreateWithRetry(ctx context.Context, create func(context.Context) (jetstream.Consumer, error)) (jetstream.Consumer, error)` with an identical body and a doc comment naming its consumers (audit projector, plugin consumer manager, retirement reactor, activity listener) and noting it is 02.2's provenance stamp site if 02.2 landed stamping here (D-55 — if 02.2's stamp seam exists elsewhere, do NOT move it; leave a pointer comment only). Migrate BOTH audit callers to the new symbol — projection.go:109 keeps `wrapConsumerCreateError` producing AUDIT_CONSUMER_CREATE_FAILED, plugin_consumer.go:194 keeps `wrapPluginConsumerCreateError` producing AUDIT_PLUGIN_CONSUMER_CREATE_FAILED (D-46: the move MUST NOT change either code; wrappers stay in package audit). Delete the now-unused unexported originals. Move or add a focused unit test in consumer_test.go covering: success on first attempt, success after one failure, exhaustion returns last error, ctx cancellation aborts between attempts.
  </action>
  <verify>
    <automated>task test -- ./internal/eventbus/... && task test:int -- ./internal/eventbus/...</automated>
  </verify>
  <done>The neutral package exists (or was consumed from 02.2), both audit callers compile against it, error codes unchanged, audit unit + integration suites green.</done>
  <acceptance_criteria>
    - `rg -c 'func CreateWithRetry' internal/eventbus/consumer/consumer.go` returns 1 (or the 02.2-landed equivalent is documented in the SUMMARY)
    - `! rg -q 'func createConsumerWithRetry' internal/eventbus/audit/` — the unexported original is gone
    - `rg -q 'AUDIT_CONSUMER_CREATE_FAILED' internal/eventbus/audit/projection.go` and `rg -q 'AUDIT_PLUGIN_CONSUMER_CREATE_FAILED' internal/eventbus/audit/plugin_consumer.go` both succeed
    - `task test -- ./internal/eventbus/...` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Skeleton subsystems — internal/retirement and internal/charactivity</name>
  <files>internal/retirement/subsystem.go, internal/retirement/subsystem_test.go, internal/charactivity/subsystem.go, internal/charactivity/subsystem_test.go</files>
  <action>
    Create two lifecycle.Subsystem packages copying `internal/world/setup/relay_subsystem.go`'s skeleton exactly (the most recent registered subsystem; PATTERNS "Retirement reactor subsystem" section quotes it): constructor allocates NOTHING and touches no live resources (core_topo_order_test.go:113-121 requires it), `Prepare` idempotent behind a nil-field guard, `Activate` idempotent behind a done-channel guard, `Stop` resets both guards so retry works.

    `internal/retirement` (package retirement): `Config{Logger *slog.Logger}` with a nil-logger default to slog.Default(); `NewSubsystem(cfg Config) *Subsystem`; `ID()` returns `lifecycle.SubsystemRetirementReactor`; `DependsOn()` returns `{SubsystemDatabase, SubsystemEventBus, SubsystemWorld, SubsystemSessions, SubsystemBootstrap}` (Bootstrap because StartLocationID() panics before its Prepare — RESEARCH Pitfall 8; Sessions/World for the fanout effects plan 03-04 wires). Prepare/Activate bodies are inert `return nil` for now with a doc comment naming plan 03-04 as the filler — the skeleton is architecture, not a prototype: the lifecycle contract, ID, and dep edges are final.

    `internal/charactivity` (package charactivity): `Config{Logger *slog.Logger}`; the storage-seam PAIR mirroring internal/eventbus/subsystem.go:62-71 verbatim in shape — `NewSubsystem(cfg Config)` forwarding to `NewSubsystemWithStorage(cfg, jetstream.FileStorage)`, and `NewSubsystemWithStorage(cfg Config, storage jetstream.StorageType) *Subsystem` storing the storage field (D-42 corrected hazard: FileStorage is the ZERO VALUE, so tests MUST be able to force MemoryStorage); `ID()` returns `lifecycle.SubsystemCharacterActivity`; `DependsOn()` returns `{SubsystemDatabase, SubsystemEventBus}`. Inert Prepare/Activate as above, filled by plan 03-05.

    Unit tests per package: ID value, DependsOn set, Prepare/Activate idempotency (double-call safe), constructor performs no allocation of live resources (construct-and-discard). ACE names, table-driven where natural.
  </action>
  <verify>
    <automated>task test -- ./internal/retirement/ ./internal/charactivity/</automated>
  </verify>
  <done>Both packages compile with green unit tests; both conform to the Prepare/Activate/Stop contract shape; charactivity carries the WithStorage seam.</done>
  <acceptance_criteria>
    - `rg -q 'func NewSubsystemWithStorage' internal/charactivity/subsystem.go` succeeds
    - `rg -q 'SubsystemBootstrap' internal/retirement/subsystem.go` succeeds (the StartLocationID dep is declared now, not discovered in 03-04)
    - Both constructors contain no goroutine launch, no network dial, no file open (reviewed against core_topo_order_test.go:113-121's requirement)
    - `task test -- ./internal/retirement/ ./internal/charactivity/` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: The 13-site SubsystemID 18→20 cascade, landed once for both IDs</name>
  <reversibility rating="costly">D-36/D-42: two registered lifecycle subsystems; removing one later re-runs this whole cascade and (post-03-04) strands a durable JetStream consumer.</reversibility>
  <files>internal/lifecycle/subsystem.go, internal/lifecycle/subsystemid_string.go, cmd/holomush/core.go, cmd/holomush/core_subsystems_test.go, cmd/holomush/core_topo_order_test.go</files>
  <action>
    Work RESEARCH §3.2's verified 13-site table MECHANICALLY — the CONTEXT's "5-site" count is refuted; a cascade task touching fewer than 5 files is the warning sign (Pitfall 2). Sequence: (1) append `SubsystemRetirementReactor` then `SubsystemCharacterActivity` at the const-block END of internal/lifecycle/subsystem.go (the block's own comment at :47-48 mandates END insertion — mid-block insertion silently renumbers later ids via the linecomment stringer); run `task generate` to regenerate subsystemid_string.go — never hand-edit the packed index array. (2) Wire cmd/holomush/core.go: two named fields on `productionSubsystemSet` (:1188-1210), two entries in the `productionSubsystems()` return slice (:1218-1260), and construction in the real composite literal (:856) as `retirement.NewSubsystem(retirement.Config{Logger: …})` / `charactivity.NewSubsystem(charactivity.Config{Logger: …})` mirroring how neighbouring subsystems obtain their logger. (3) Run `task test -- ./cmd/holomush/` and OBSERVE RED (PORTAL-10 rule 4): the three fixed-size `[18]stubSubsystem` occurrences are a COMPILE error, the two `len == 18` assertions and the two `require.Len(…, 18, …)` real-graph assertions fail. (4) Widen all remaining sites: core_subsystems_test.go — allStubs signature + literal (:63-64), setFromStubs signature + index→field mapping (:88-108), both length assertions (:126-127, :292-293), the distinctness ID list (:134-152), realProductionSubsystemGraphForPropertyTest (:390-427); core_topo_order_test.go — realProductionSubsystemGraph (:126-165) constructing both real subsystems, require.Len to 20, and the pinned `want []lifecycle.SubsystemID` start sequence (:194-213): run the test, read the failure diff, and insert the two IDs at the topologically-correct positions — topoSort tie-breaks by SubsystemID value (:180-182) so appended IDs sort LAST within their ready tier; COMPUTE from the diff, never guess (research open question 5). Line numbers above are verified at branch point 23f7f75d0 — re-locate by symbol if drifted. Run `task fmt` (aligned const blocks reflow — commit the edits), `task lint`, and `task test:int` (the cascade touches shared types; integration files do not compile under task test alone).
  </action>
  <verify>
    <automated>task test -- ./cmd/holomush/ ./internal/lifecycle/ && task test:int</automated>
  </verify>
  <done>All 13 sites widened; 20 subsystems register; pinned topological order updated from the observed diff; stringer regenerated; full unit + integration suites green.</done>
  <acceptance_criteria>
    - `rg -c 'SubsystemRetirementReactor|SubsystemCharacterActivity' internal/lifecycle/subsystem.go` returns 2 and both appear AFTER SubsystemCharacterNameBlockList in the const block
    - `rg -q 'SubsystemCharacterActivity' internal/lifecycle/subsystemid_string.go` succeeds (regenerated, committed)
    - `! rg -q '\[18\]stubSubsystem' cmd/holomush/core_subsystems_test.go` — no stale fixed-size arrays remain
    - `task test -- -run 'TestProductionSubsystems' ./cmd/holomush/` exits 0 (count + topo-order pins green at 20)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit consumer creation → shared helper | operational retry behavior for the durable audit pipeline moves packages |
| host composition → new subsystems | two new inert subsystems enter the production boot graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | D-46 relocation | medium | mitigate | verbatim move; both audit error codes pinned by acceptance greps + existing audit tests; backoff schedule single-sourced so it cannot diverge |
| T-03-06 | Denial of Service | subsystem boot graph | low | mitigate | dep edges declared now; pinned topo-order + acyclicity tests prove the 20-node graph boots deterministically |
| T-03-07 | Repudiation | inert skeleton subsystems in production | low | accept | Prepare/Activate are no-ops until 03-04/03-05; they carry no domain traffic and no external surface (Prepare/Activate contract carve-out) |
</threat_model>

<verification>
- `task test -- ./internal/eventbus/... ./internal/retirement/ ./internal/charactivity/ ./cmd/holomush/ ./internal/lifecycle/`
- `task test:int` — full compile + audit integration (the cascade and the relocation both touch shared types)
- `task lint` before every commit; `task fmt` edits committed
</verification>

<success_criteria>
- `consumer.CreateWithRetry` shared by both audit callers with unchanged error codes (D-46 made true)
- 20 subsystems; count/distinctness/real-graph/topo-order tests green; stringer regenerated
- Both skeletons final in ID, deps, and lifecycle shape — 03-04/03-05 fill behavior only
</success_criteria>

<output>
Create `.planning/phases/03-world-character-commands/03-02-SUMMARY.md` when done. Record: which branch of the §0.4-1 interlock fired (created vs consumed 02.2's relocation), the observed RED diff for the pinned topo order, and the final insertion positions of both IDs.
</output>

## Artifacts this phase produces (plan 03-02)

New symbols created by this plan (source-grounding: exclude from drift verification):
- `internal/eventbus/consumer`: package, `CreateWithRetry`, `CreateBackoffs`
- `internal/retirement`: package, `Subsystem`, `NewSubsystem`, `Config`
- `internal/charactivity`: package, `Subsystem`, `NewSubsystem`, `NewSubsystemWithStorage`, `Config`
- `internal/lifecycle`: `SubsystemRetirementReactor`, `SubsystemCharacterActivity`
- `cmd/holomush`: two new `productionSubsystemSet` fields + wiring
- Test files: `internal/eventbus/consumer/consumer_test.go`, `internal/retirement/subsystem_test.go`, `internal/charactivity/subsystem_test.go`
