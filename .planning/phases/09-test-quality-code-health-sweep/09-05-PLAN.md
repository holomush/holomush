---
phase: 09-test-quality-code-health-sweep
plan: 05
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - internal/store/migrations/000053_sessions_location_index.up.sql
  - internal/store/migrations/000053_sessions_location_index.down.sql
autonomous: true
requirements: [QUAL-05]

must_haves:
  truths:
    - "The sessions table carries an index on its location column, so the presence / who-is-here query stops scanning the table."
    - "The migration is idempotent and its paired down migration returns the schema to its prior state."
    - "The migration runs clean against a fresh database as part of the existing integration lane."
  artifacts:
    - "internal/store/migrations/000053_sessions_location_index.up.sql"
    - "internal/store/migrations/000053_sessions_location_index.down.sql"
  key_links:
    - "migration embed -> integration suite's fresh-database migration run -> presence query plan"
---

<objective>
Add the missing index on the sessions table's location column as migration 000053.

Purpose: presence — the who-is-here query that drives every location view — filters sessions
by location, and that column has no index across all 52 existing migrations. Three indexes
exist on the sessions table and none of them covers this access path; two similarly named
indexes on other tables make the gap easy to misread as already closed. Under load this is a
sequential scan on the hottest read path in the game loop.

Output: a paired, idempotent index migration following the shape every other index migration
in this repository already uses.

Decisions implemented: D-17 (one of the four Medium-cluster items that land), D-19 (plain
idempotent index creation with a paired idempotent drop — no concurrent build, for which this
repository has no precedent and no non-transactional runner support).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-quality-code-health-sweep/09-PATTERNS.md
@.claude/rules/database-migrations.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the paired 000053 index migration</name>
  <files>internal/store/migrations/000053_sessions_location_index.up.sql, internal/store/migrations/000053_sessions_location_index.down.sql</files>
  <read_first>
    - internal/store/migrations/000008_session_player_fk.up.sql (whole file — the exact index-migration shape to copy, including the SPDX comment header and the prose rationale comment)
    - internal/store/migrations/000008_session_player_fk.down.sql (whole file — the reverse-order drop shape)
    - internal/store/migrations/000001_baseline.up.sql around lines 215-230 — the existing sessions-table indexes and their naming convention
    - .claude/rules/database-migrations.md — naming, idempotency, pairing, and the no-triggers/no-functions rule
    - internal/store/migrations/ directory listing — confirm 000052 is the highest number on disk before writing 000053
  </read_first>
  <action>
    Confirm 000053 is still the next free number by listing the migrations directory before
    writing; if another migration has landed since planning, take the next free number and say
    so in the SUMMARY.

    Write the up migration: the two-line SPDX comment header in SQL comment form, a prose
    comment naming issue #4796 and stating that the presence query filters sessions by location
    and previously had no index, then a single idempotent index creation on the sessions table's
    location column. Follow the existing sessions-table index naming convention rather than
    inventing one.

    Write the down migration: the same SPDX header and a single idempotent index drop.

    Do not use a concurrent index build. There is no precedent for it across the 52 existing
    migrations, it cannot run inside a transaction, and adopting it would require verifying or
    adding non-transactional runner support — that is a new capability, not a new migration.
    Do not add triggers, functions, or stored procedures; all logic lives in Go.

    Note the terminology rule: the spatial concept is `location`, never `room`, in the index
    name and in every comment.
  </action>
  <verify>
    <automated>task test:int</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist with the 000053 prefix and matching snake_case suffix.
    - Each file begins with the two-line SPDX Apache-2.0 comment header in SQL comment form.
    - `rg -c 'IF NOT EXISTS' internal/store/migrations/000053_*.up.sql` returns 1 and `rg -c 'IF EXISTS' internal/store/migrations/000053_*.down.sql` returns 1.
    - `rg -ci 'concurrently' internal/store/migrations/000053_*.sql` returns no matches.
    - `rg -ci 'room' internal/store/migrations/000053_*.sql` returns no matches.
    - The up migration carries a comment naming issue #4796 and stating the query pattern the index serves.
    - `task test:int` exits 0 — the integration lane runs every migration against a fresh database, which is what proves the migration is syntactically valid and applies cleanly.
    - The migration has been rolled up and back down once against a scratch database, and the SUMMARY records that the down left the schema in its prior state.
    - `task lint` exits 0.
  </acceptance_criteria>
  <done>The presence access path has an index, created idempotently and reversed cleanly by its pair, and the migration applies on a fresh database in the integration lane.</done>
  <reversibility rating="costly">Once merged the migration number is consumed; undoing requires a further paired migration rather than a code revert.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player request -> presence query -> database | An unbounded scan on a hot read path is reachable from ordinary player traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05-01 | Denial of Service | presence / who-is-here query against the sessions table | medium | mitigate | An unindexed filter column turns the hottest read path into a sequential scan, so ordinary traffic degrades the database as the session table grows. The index closes the access path. |
| T-09-05-02 | Tampering | schema migration reversibility | low | mitigate | A migration that cannot be reversed strands an operator mid-upgrade. The paired idempotent down migration is written and exercised by an up/down round trip before the plan is complete. |
| T-09-05-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed; no dependency manifest touched. |
</threat_model>

<verification>
- `task test:int` exits 0 (migrations run against a fresh database).
- An up/down/up round trip against a scratch database leaves the schema consistent.
- `task lint` exits 0.
</verification>

<success_criteria>
The sessions table's location column is indexed by a paired, idempotent, precedent-following
migration that applies and reverses cleanly.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- `internal/store/migrations/000053_sessions_location_index.up.sql` (NEW)
- `internal/store/migrations/000053_sessions_location_index.down.sql` (NEW)
- One new database index on the sessions table, named per the existing `idx_sessions_*` convention (exact name assigned at execution time)
</artifacts_produced>

<output>
Create `.planning/phases/09-test-quality-code-health-sweep/09-05-SUMMARY.md` when done.
Record the final migration number and the index name.
</output>
