---
phase: 09-test-quality-code-health-sweep
plan: 12
type: execute
wave: 4
depends_on: ["09-07", "09-20"]
files_modified:
  - test/session-matrix.yaml
  - test/integration/session/lifecycle_harness_test.go
  - test/integration/session/lifecycle_attach_test.go
  - test/meta/session_matrix_registry_test.go
autonomous: true
requirements: [QUAL-04]

assumptions:
  - statement: "QUAL-04's edge-coverage probe raised the generic `empty` category (result for empty, single-element, or null input). Mapped onto a session-lifecycle matrix, the meaningful reading is the zero-connection and single-connection cases, and those are already explicit matrix rows: detach-all-connections and reattach-within-TTL. The probe adds no criterion beyond the matrix's own rows and is surfaced here rather than converted into a fabricated one."
    verification: flagged
  - statement: "QUAL-04's edge-coverage probe raised the generic `encoding` category (whose definition of length/equality applies — bytes, code points, graphemes, normalized form). No text-comparison, collation, or normalization surface exists in session lifecycle; the equalities at issue are identifier comparison and timestamp ordering. The probe is dismissed as inapplicable rather than answered."
    verification: flagged

must_haves:
  truths:
    - "Every populated cell of the session-lifecycle matrix has a committed disposition: a named spec, a pointer to a spec that already covers it elsewhere, or an explicit not-applicable marking."
    - "The matrix rows that are marked not-applicable are exactly the ten the source matrix marks that way — no cell is invented and none is dropped."
    - "The administrator-boot row is dispositioned not-implementable-from-harness-defaults, carrying the `resetpassword --kick` citation and both of its gaps — never as spec-covered, and never on the false ground that no entry point exists."
    - "The registry's own shape — row count, identifier uniqueness, exclusive dispositions — is checked by a test from the moment it is committed, not only after the last spec plan lands."
    - "The session integration suite can drive the full in-process stack, not only raw stores, so lifecycle transitions can be exercised through the production server."
    - "That shared stack stays alive for the whole suite rather than being torn down when the first spec that touched it ends, and every spec that opens a session releases it."
    - "Fresh character selection and both reattach paths are covered by passing specs across the applicable transport columns."
  artifacts:
    - "test/session-matrix.yaml — the committed matrix registry with one row per position"
    - "test/meta/session_matrix_registry_test.go — the registry's shape test, landing with the registry itself"
    - "test/integration/session/lifecycle_harness_test.go — the shared harness seam for lifecycle specs"
    - "test/integration/session/lifecycle_attach_test.go — specs for the fresh-selection and reattach transitions"
  key_links:
    - "matrix row id -> in-code marker comment on the spec -> the bijection meta-test that lands later"
    - "shared harness accessor -> integration harness start -> production server, event bus, and session store"
    - "harness plan's administrator-boot disposition (`resetpassword --kick` + its two gaps) -> that row's disposition and notes in this registry"
  prohibitions:
    - "Never mark a matrix cell as covered by a spec or by a pointer without having confirmed that the named spec or file actually exists and actually exercises the transition; a committed table that overstates coverage is the exact defect the source review named as its root theme."
    - "Never disposition a row as spec-covered when the harness plan recorded that its transition cannot be driven from harness defaults. Inventing a satisfiable approximation is precisely the substitution this registry exists to make visible."
    - "Never describe the administrator-boot capability as absent from the tree, in this registry or its SUMMARY. `resetpassword --kick` is a real instance of it. The registry is committed and machine-read, so a false absence claim here is more durable than the same claim in prose — record the path AND its gaps."
---

<objective>
Commit the session-lifecycle matrix as a machine-readable registry, add the harness seam the
lifecycle specs need, and cover the first transition family — fresh character selection and
both reattach paths.

Purpose: the phase's success criterion asks for a matrix covering connect, reconnect,
multi-character, and idle-timeout paths. The source matrix is twelve transitions across four
transport columns with ten positions marked not applicable, leaving thirty-eight populated
cells. Committing it as a registry rather than as prose is what lets a later meta-test prove
the claims are true. Separately, the existing session integration suite is wired against raw
stores, not the in-process harness, so the transition specs need a seam that does not exist yet.

Output: the registry with every position dispositioned, a shared harness accessor, and the
first family of specs.

Decisions implemented: D-12 (the full matrix, with each cell getting a passing spec or an
explicit covered-elsewhere pointer — noting the corrected count of 38 populated cells, not 48),
D-13 (committed matrix plus a bijection meta-test modelled on the quarantine registry; the
meta-test itself lands in a later plan so it is green on arrival), D-16 (the existing multi-tab
authentication suite is cited for the cells it already covers rather than duplicated).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-quality-code-health-sweep/09-RESEARCH.md
@.planning/phases/09-test-quality-code-health-sweep/09-PATTERNS.md
@.planning/phases/09-test-quality-code-health-sweep/09-07-SUMMARY.md
@.planning/phases/09-test-quality-code-health-sweep/09-20-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the session-lifecycle matrix as a registry, with the shape test that guards it</name>
  <files>test/session-matrix.yaml, test/meta/session_matrix_registry_test.go</files>
  <read_first>
    - .planning/phases/09-test-quality-code-health-sweep/09-RESEARCH.md, the section reproducing the source matrix verbatim — twelve transition rows, four columns, with the not-applicable positions marked; this is the authority for which cells exist
    - test/quarantine.yaml lines 1-25 — the registry format to copy: a self-documenting header naming its own meta-test by path and its bijection key, an inline schema line, and a flat list of small maps
    - test/integration/auth/multi_tab_test.go — read the container and spec descriptions so the cells you mark as covered elsewhere point at specs that genuinely exercise the transition, not merely at a plausible-sounding file
    - test/meta/quarantine_registry_test.go lines 20-60 — how the registry side is parsed, so the field you choose as the bijection key is extractable by the same regex shape
    - .planning/phases/09-test-quality-code-health-sweep/09-20-SUMMARY.md — the three harness seams that now exist and, critically, the evidenced finding on whether an administrator-boot entry point exists at all; that finding decides how the administrator-boot row is dispositioned here
  </read_first>
  <action>
    Write a registry with exactly forty-eight rows, one per position in the twelve-by-four
    matrix. Give each row a stable opaque identifier composed from the transition slug and the
    column slug — that identifier is the bijection key, played by the same role the tracker
    token plays in the quarantine registry.

    Give each row exactly one disposition. A cell covered by a spec this phase writes carries
    the spec's file path and its container-and-spec description. A cell already covered
    elsewhere carries the covering file path and the description of the covering spec; the
    multi-session column has several such cells against the existing two-tab authentication
    suite, and citing them is required rather than optional — duplicating that setup is waste.
    A cell the source matrix marks not applicable carries an explicit not-applicable marking
    with a one-line reason. Exactly ten rows must carry the not-applicable marking, and they
    must be the same ten the source matrix marks; if your count differs, you have misread the
    source and must re-read it before continuing.

    Add a fourth disposition — not-implementable-from-harness-defaults — for a row whose
    transition cannot be driven through the harness as this phase configures it. The
    administrator-boot row takes this disposition. Read the harness plan's SUMMARY for its
    disposition of that row and carry BOTH halves of it into the row's notes:

    (a) the entry point that DOES exist — `resetpassword --kick`, a capability-gated admin
        command at `internal/command/handlers/resetpassword.go:197-218` that deletes the session
        rows of every character belonging to the target player; and
    (b) the two reasons it still does not satisfy this cell — it bypasses the
        `RecordBootedSession` / `session_ended` semantics the matrix row asserts, and
        `RegisterAdmin` panics on nil dependencies whose five heavy deps the harness does not
        wire.

    Do NOT phrase this row's notes as an assertion that the capability is absent from the tree, in
    any wording. Such a claim is false, cross-AI review falsified it against source, and a
    committed registry asserting it would be an overstated-assurance artifact of exactly the kind
    this registry exists to prevent — with the added cost that it is durable and machine-read. The
    row's notes describe what the path DOES and where it falls short; they never describe it as
    missing. If the harness SUMMARY you are reading claims absence without the
    `resetpassword --kick` citation, stop: that SUMMARY predates this correction, and you must
    reconcile against `internal/command/handlers/resetpassword.go` directly before writing the row.

    Any issue number cited on this row must be the one tracking the `session_ended` semantic gap,
    not one asserting a missing capability.

    Keep the dispositions distinct in the schema: an inapplicable cell is a question that does not
    arise; a not-implementable-from-harness-defaults cell is a question the system answers, but
    not with the semantics this row asserts and not by any route the harness can currently reach.
    Collapsing either into the other hides a real gap behind a legitimate one.

    For every cell you mark as covered elsewhere, open the cited file and confirm the cited spec
    exists and exercises that transition for that transport. A pointer that merely sounds right
    is the failure mode this registry exists to prevent.

    Write a header comment in the same self-documenting style as the quarantine registry: name
    the meta-test that will enforce the bijection by its eventual path, name the in-code marker
    form specs must carry, state the schema inline, and state that a row claiming spec coverage
    without a matching in-code marker fails the build.

    Do not write any spec in this task. Rows referencing specs that do not exist yet are correct
    at this point — the marker bijection is checked only after every spec plan has run.

    Then land the registry's shape test in the same commit, in the repository's meta-test
    directory, at the path the header names. It parses the registry and asserts the properties a
    reader would otherwise have to trust: the total row count, that every identifier is unique,
    that every row carries exactly one disposition, and that the not-applicable count is exactly
    ten. Do NOT assert the marker bijection here — no spec carries a marker yet, so that assertion
    belongs to the later meta-test plan, which extends this same file.

    Landing the shape test now rather than later is the point. Three spec plans consume this
    registry as their division of labour before the bijection test exists; without a shape test
    they would consume a table whose basic integrity is asserted only by a human having counted
    carefully once. Follow the quarantine registry test's error handling: a parse or read failure
    must fail the test, never be swallowed into an empty set that trivially satisfies a count.

    Prove the shape test is load-bearing: delete one row, observe the test fail on the count;
    duplicate one identifier, observe it fail on uniqueness; give one row two dispositions, observe
    it fail on exclusivity. Restore after each. A shape test that has never been seen failing is a
    shape test nobody has checked.
  </action>
  <verify>
    <automated>set -o pipefail; test -f test/session-matrix.yaml &amp;&amp; test -f test/meta/session_matrix_registry_test.go &amp;&amp; task test:verbose -- -run TestSessionMatrix ./test/meta/ 2&gt;&amp;1 | rg -c '^--- PASS: TestSessionMatrix' &gt;/dev/null &amp;&amp; task lint</automated>
    <note>
      Every conjunct is red when the thing it checks is false, including the case where the
      artifact was never created at all:
      (a) registry file never written — the first `test -f` exits 1;
      (b) SHAPE TEST NEVER WRITTEN — the second `test -f` exits 1. This is the conjunct that must
      not be dropped. `go test -run &lt;pattern&gt;` EXITS 0 WHEN NO TEST MATCHES, printing
      `[no tests to run]`; a gate whose only test invocation is `-run`-scoped therefore passes
      vacuously when the test does not exist, and file-existence of the YAML alone cannot
      distinguish a correct 38-row registry from an empty file;
      (c) shape test exists but is named something else, so `-run` selects nothing — gotestsum
      exits 0 but emits no `--- PASS:` line and `rg -c` exits 1, which is why the verbose format
      and the anchored pin are load-bearing rather than decorative;
      (d) shape test runs and fails on row count, duplicate identifier, doubled disposition, or a
      propagated parse error — gotestsum exits non-zero, `set -o pipefail` surfaces it, and no
      `--- PASS:` line is printed either;
      (e) lint red — the final conjunct exits non-zero.
      Use `rg -c ... &gt;/dev/null`, NOT `rg -q`: `-q` stops reading at the first match and SIGPIPEs
      gotestsum, so under `pipefail` the pipeline returns 141 and the gate goes red on the TRUE
      case. Measured, not assumed.
    </note>
  </verify>
  <acceptance_criteria>
    - `test/session-matrix.yaml` exists and parses as YAML.
    - The file contains exactly 48 rows, each with a unique identifier.
    - Exactly 10 rows carry the not-applicable marking, and each names the transition and column the source matrix marks that way.
    - Every row carries exactly one disposition: a spec reference, a covered-elsewhere reference, a not-applicable marking, or a not-implementable marking. No row carries two, and none carries none.
    - The administrator-boot row is dispositioned not-implementable-from-harness-defaults and its notes cite `resetpassword --kick` by file and line, name the `session_ended` semantic gap, and name the `RegisterAdmin` wiring gap. `rg -c 'resetpassword' test/session-matrix.yaml` returns at least 1.
    - No row's notes assert that an administrator-boot entry point does not exist: `rg -in 'no (administrator[- ])?boot|no entry point|entry point.*(does not|doesn.t) exist' test/session-matrix.yaml` returns no matches. The positive control above runs first, so an unreadable or missing registry fails on that rather than passing this negative vacuously.
    - Any issue number cited on that row tracks the `session_ended` semantic gap, not a missing capability; the SUMMARY names the harness-plan disposition it was derived from.
    - Every covered-elsewhere reference names a file that exists on disk and a spec description that is present in that file — verified by opening each, and the verification recorded in the SUMMARY.
    - The header comment names the meta-test path, the in-code marker form, and the inline schema.
    - `test/meta/session_matrix_registry_test.go` exists on disk, and the scoped run reports a top-level `--- PASS:` line for it. Both are asserted explicitly because a `-run`-scoped invocation alone exits 0 when the test does not exist.
    - All three load-bearing demonstrations were performed and their failures recorded in the SUMMARY: a deleted row fails the count, a duplicated identifier fails uniqueness, a doubled disposition fails exclusivity. Each was restored.
    - The shape test propagates read and parse errors rather than treating them as an empty registry.
    - `task lint` exits 0, including any YAML lint.
  </acceptance_criteria>
  <done>Every position in the matrix has a committed, checkable disposition; the covered-elsewhere pointers have been opened and confirmed; and the table's integrity is enforced by a test that has been observed failing rather than by a careful human count.</done>
  <reversibility rating="reversible">A new registry file; nothing depends on it until the meta-test lands.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Add the shared in-process harness seam to the session integration suite</name>
  <files>test/integration/session/lifecycle_harness_test.go</files>
  <read_first>
    - test/integration/session/session_persistence_suite_test.go (whole file, 108 lines) — the existing suite bootstrap, its exported suite handle, its shared environment struct, and CRITICALLY its `BeforeSuite` block and its `AfterSuite` block. Both already exist. The harness start and stop attach to those two nodes; that placement is the whole point of this task and is not a detail to improvise.
    - test/integration/privacy/privacy_test.go lines 1-80 — the in-process harness start this seam mirrors, including how the suite handle is passed in and which options are and are not used
    - internal/testsupport/integrationtest/harness.go around lines 240-330 — the start options and, importantly, the default access engine, which is permissive by default; the real-policy option is opt-in and these specs must NOT pass it
    - internal/testsupport/integrationtest/harness.go, `Server.Stop` in full — read what it actually tears down. It stops the plugin subsystem and nothing else; Postgres and NATS are cleaned by `t.Cleanup` handlers registered during Start. It does NOT log out sessions or tear down their transports, which is why per-session cleanup is a separate obligation in this task.
    - Ginkgo's `DeferCleanup` doc comment in `core_dsl.go` — the paragraph enumerating which node type a cleanup is attached to. It is explicit that a `DeferCleanup` registered from `It`/`BeforeEach` behaves like an `AfterEach`, and that suite-scoped cleanup requires registration from `BeforeSuite`. This is the defect this task's shape exists to avoid; read it rather than trusting the shape looks right.
    - test/integration/session/session_lease_test.go lines 1-30 — the file header, build tag placement, and dot-import lint form to match
  </read_first>
  <action>
    Add a file to the existing session integration package that exposes a process-wide accessor
    for the in-process harness, so every lifecycle spec across this and the following spec plans
    shares one started stack rather than each standing up its own Postgres, event bus, and server.

    START THE HARNESS IN THE SUITE'S EXISTING `BeforeSuite` AND STOP IT IN ITS EXISTING
    `AfterSuite`. Do NOT start it lazily behind a once-guard on first use, and do NOT register its
    cleanup with `DeferCleanup` from the accessor's call site. Cross-AI review caught the lazy
    shape as a live defect and Ginkgo's own documentation confirms it: a `DeferCleanup` registered
    from within an `It` or a `BeforeEach` is attached to THAT SPEC and fires when that spec ends.
    A once-guarded accessor whose first call happens inside a spec therefore tears the harness down
    at the end of that spec and hands every subsequent spec a stopped server — a failure that
    presents as unrelated downstream specs failing on a closed connection, which is expensive to
    diagnose and easy to misattribute. Suite-scoped cleanup requires registration from
    `BeforeSuite`; the suite already has both nodes, so the correct shape costs nothing.

    If you find a reason the harness genuinely cannot start in `BeforeSuite`, the fallback is a
    per-spec `BeforeEach` harness with matching per-spec cleanup — a fresh stack per spec, slower
    but correct. Do not invent a third shape, and do not mix the two.

    Separately, make per-session teardown an explicit obligation of every spec that opens a
    session, registering it with `DeferCleanup` from within the spec — which is the correct scope
    for it, since a session belongs to one spec. `Server.Stop` stops only the plugin subsystem; it
    does not log sessions out or tear down their transports, so a suite that opens sessions across
    many specs and relies on `Stop` alone leaks subscribe goroutines and transports for the
    suite's whole duration. State this obligation in the accessor's doc comment so later spec
    plans reading only that comment inherit it.

    Do not add a second spec runner. The package already has one, and the new specs join that
    suite; adding another would produce two suites in one package.

    Start the harness with the default access engine, which is permissive — that is what the
    lifecycle transitions need, since they exercise session state rather than authorization, and
    it is also what the history-floor arm in a later plan needs. Do not pass the real-policy
    option; a previously open question about which shape that arm needs resolves to the default,
    not the opt-in.

    Do NOT start with bare defaults, however. Review established that the default command registry
    is empty, so a later plan driving the quit command through the send-command helper would
    dispatch nothing and could pass while proving nothing. Pass the compiled-in command
    registration option the harness plan added. Do not reach for the in-tree-plugins option to get
    the same effect: it does register the compiled-in handlers as a side effect, but it also
    requires built binary plugin artifacts, which these specs have no use for. State this choice
    explicitly in the accessor's doc comment together with its reason — the review found the
    original vague "start with defaults" contract to be the root of the problem, so an unexplained
    option list would reproduce it.

    Match the existing sibling file's header form exactly: build tag, licence header, package
    name, and the lint-suppression comments on the dot imports. Note that the two existing files
    in this directory order the build tag and licence header differently from each other; follow
    the sibling you are extending rather than picking arbitrarily.

    Carry a doc comment on the accessor stating why the seam exists — that the suite's original
    specs are wired against raw stores and cannot reach transport attach and detach, history
    queries, or session expiry — so the next reader understands the two styles coexisting in one
    package.
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/session/...</automated>
  </verify>
  <acceptance_criteria>
    - `task test:int -- ./test/integration/session/...` exits 0 and the previously existing seven specs still pass.
    - `rg -c 'RunSpecs' test/integration/session/` returns exactly 1 — no second spec runner was added.
    - `rg -c 'WithRealABAC' test/integration/session/` returns no matches — the permissive default is used.
    - The accessor passes the compiled-in command registration option, and its doc comment names both that option and the permissive engine choice with reasons. The in-tree-plugins option is NOT passed.
    - A throwaway assertion dispatching the quit command through the accessor's harness returns a recognised-command outcome rather than an unknown-command error; the observation is recorded in the SUMMARY and the throwaway removed. This is what proves the option was actually wired rather than merely named.
    - The harness is started in the suite's `BeforeSuite` and stopped in its `AfterSuite`. `rg -c 'DeferCleanup' test/integration/session/lifecycle_harness_test.go` returns no matches for the harness lifecycle itself — the accessor registers no cleanup of its own.
    - The harness survives across specs, demonstrated rather than assumed: at least two specs in different files use the accessor within one run and both pass. A run in which a later spec fails on a closed connection or a stopped server is the exact defect this shape exists to prevent and must be reported, not worked around by reverting to a per-spec start without saying so.
    - The accessor starts the harness at most once across the whole suite, demonstrated by a log or counter observed during a run and recorded in the SUMMARY.
    - The accessor's doc comment states the per-session teardown obligation — that `Server.Stop` stops only the plugin subsystem, so each spec opening a session registers its own logout/transport cleanup with `DeferCleanup` from inside that spec.
    - The file carries the build tag, the licence header, and the dot-import lint suppressions matching its sibling.
    - `task lint` exits 0.
  </acceptance_criteria>
  <done>Lifecycle specs across three plans share one in-process stack, attached to the suite that already exists, with the store-backed specs untouched.</done>
  <reversibility rating="reversible">An additive test file.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Cover the fresh-selection and both reattach transitions</name>
  <files>test/integration/session/lifecycle_attach_test.go</files>
  <read_first>
    - test/session-matrix.yaml — the rows this task must satisfy: fresh character selection to active, reattach within the time-to-live via the character-selection path, and reattach within the time-to-live via the subscribe compare-and-swap path, across their applicable columns
    - internal/testsupport/integrationtest/session.go lines 160-200 and 290-440 — the wait-for-event, move, detach-transport, and reattach-transport helpers these specs drive
    - .planning/phases/09-test-quality-code-health-sweep/09-07-SUMMARY.md — the exact signature of the timestamped emit helper, if these specs need ordered events
    - test/integration/session/session_lease_test.go lines 21-56 — the spec shape to follow, including the leading comment that cites spec identifiers and the practice of asserting projected fields rather than only counts
    - test/integration/auth/multi_tab_test.go — so the multi-session cells on these rows are cited in the registry rather than re-implemented here
  </read_first>
  <action>
    Write specs for the three attach transitions across the transport columns the registry marks
    as spec-covered for those rows. Each spec carries an in-code marker comment naming the
    matrix row identifier it satisfies, in the form the registry header declares; that marker is
    what the bijection meta-test matches against.

    For each transition assert the observable session state, not merely that a call returned
    without error: after fresh selection the session is active and carries a location arrival
    timestamp; after a reattach within the time-to-live the same session identifier is still in
    play and the arrival timestamp is unchanged; after a reattach through the subscribe path the
    connection is bound to the pre-existing session rather than a new one. Assert the specific
    fields, following the sibling file's practice of asserting projected fields so a column-order
    or field-mapping regression is caught.

    Where a spec identifier from the privacy specification applies to a transition, cite it by
    identifier in the spec's leading comment — the source matrix asks for identifiers to be
    asserted by identifier, and the sibling file already establishes that comment form.

    Use no sleep anywhere. Where ordering matters, use the timestamped emit helper and the
    harness's wait-for-event helper. A sleep added here would be the seventeenth site of a
    pattern the repository already tracks as debt.

    Do not implement any cell the registry marks as covered elsewhere, and do not implement any
    cell belonging to a later plan's rows — the registry is the division of labour.
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/session/...</automated>
  </verify>
  <acceptance_criteria>
    - `task test:int -- ./test/integration/session/...` exits 0.
    - The number of specs in the session integration directory has increased by at least eight over the pre-phase count of seven.
    - Every spec in the new file carries exactly one matrix-row marker comment, and every marker matches a registry row whose disposition is spec coverage.
    - No marker in this file duplicates another marker anywhere in the repository.
    - Each spec asserts at least one concrete session field — identifier, status, or arrival timestamp — not merely absence of error.
    - `rg -c 'time\.Sleep' test/integration/session/` does not increase relative to its pre-change value.
    - No spec name or container description uses a vague placeholder string.
    - `task lint` exits 0 and `task fmt` mutations are committed.
  </acceptance_criteria>
  <done>The connect and reconnect halves of the matrix are covered by specs that assert concrete session state, each tagged with the registry row it satisfies.</done>
  <reversibility rating="reversible">Additive specs.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client transport -> session store | Attach, detach, and reattach transitions decide which connection is bound to which authenticated session |
| committed matrix -> assurance claim | The registry is read as a statement about what is verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-12-01 | Spoofing | reattach paths (character selection and subscribe compare-and-swap) | high | mitigate | An unverified reattach path is where a connection could bind to another player's session (ASVS V3). Specs assert the concrete session identifier and arrival timestamp across both reattach mechanisms rather than only that the call succeeded. |
| T-09-12-02 | Repudiation | committed matrix registry | high | mitigate | A table claiming coverage that does not exist reproduces the root defect the source review named. Every covered-elsewhere pointer is opened and confirmed before being written, every spec-covered row must have a matching in-code marker, and a bijection meta-test lands before the phase closes. |
| T-09-12-04 | Denial of Service | shared harness torn down mid-suite by a mis-scoped cleanup | high | mitigate | A `DeferCleanup` registered from inside a spec fires at that spec's end, so a lazily-started shared harness is stopped after its first user and every later spec receives a dead server. The failure surfaces as unrelated specs failing on closed connections and is routinely misattributed to flakiness. Mitigated by starting in `BeforeSuite` and stopping in `AfterSuite`, by a negative check that the accessor registers no cleanup of its own, and by an acceptance criterion requiring two specs in different files to pass in one run. |
| T-09-12-05 | Denial of Service | leaked subscribe goroutines and transports across a long suite | medium | mitigate | `Server.Stop` tears down only the plugin subsystem, so sessions opened across many specs hold their subscribe goroutines and transports for the suite's whole duration, degrading later specs and masking real ordering bugs. Each spec that opens a session registers its own logout/transport cleanup, and the obligation is stated in the accessor's doc comment where later spec plans will read it. |
| T-09-12-03 | Elevation of Privilege | harness access engine selection | medium | accept | These specs run against the harness's permissive default engine because they exercise session state, not authorization; authorization is covered by the access integration suite. The permissive choice is explicit and asserted by a negative check on the opt-in option, so it cannot be adopted silently by a spec that needs real policy. |
| T-09-12-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed; no dependency manifest touched. |
</threat_model>

<verification>
- `task test:int -- ./test/integration/session/...` exits 0 with at least fifteen specs.
- The registry has 48 rows, exactly 10 not-applicable, and every covered-elsewhere pointer confirmed by opening the cited file.
- One spec runner in the package; the permissive default engine in use.
- `task lint` exits 0.
</verification>

<success_criteria>
The matrix exists as a checkable registry, the suite can drive the real stack, and the connect
and reconnect transitions are covered by specs that assert concrete state.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- `test/session-matrix.yaml` (NEW) — 48-row registry; the bijection key field name is assigned at execution time and declared in the file's own header
- `test/meta/session_matrix_registry_test.go` (NEW) — the registry's shape test (row count, identifier uniqueness, exclusive dispositions), extended into a full marker bijection by plan 09-16
- `test/integration/session/lifecycle_harness_test.go` (NEW) — shared in-process harness accessor for the session suite, wired with the compiled-in command registration option, started in the suite's existing `BeforeSuite` and stopped in its `AfterSuite`
- `test/integration/session/lifecycle_attach_test.go` (NEW) — specs for the fresh-selection and both reattach transitions
- A new in-code marker comment convention for matrix rows, declared in the registry header and consumed by the meta-test in plan 09-16
</artifacts_produced>

<output>
Create `.planning/phases/09-test-quality-code-health-sweep/09-12-SUMMARY.md` when done.
Record the exact marker-comment form and the registry's bijection key field name — plans 09-13,
09-14, 09-15, and 09-16 all depend on both. Also record the accessor's name and the per-session
teardown obligation, since the three spec plans that follow open sessions through it and inherit
that obligation.
</output>
