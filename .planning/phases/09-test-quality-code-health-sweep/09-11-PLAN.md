---
phase: 09-test-quality-code-health-sweep
plan: 11
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - test/integration/eventbus_e2e/audit_drift_detector_test.go
  - test/integration/eventbus_e2e/js_storage_corruption_test.go
  - test/integration/eventbus_e2e/multi_protocol_fanout_test.go
  - test/integration/eventbus_e2e/backfill_rebuild_test.go
autonomous: true
requirements: [QUAL-03]

must_haves:
  truths:
    - "Each of the four unimplemented integration specs retains its description and its skip, and nothing else."
    - "Each skip cites a GitHub issue number that is open, replacing a retired tracker id that resolves to nothing."
    - "Roughly two hundred lines of compiled-but-unreachable setup no longer have to be maintained through refactors."
    - "The integration lane still compiles and still runs the same number of non-skipped specs."
  artifacts:
    - "Four trimmed test files under test/integration/eventbus_e2e/"
  key_links:
    - "skip citation -> open GitHub issue -> the work item that would implement the spec"
  prohibitions:
    - "Never delete a skipped spec outright; the description is the record of what is not covered, and removing it silently shrinks the known-gap list."
---

<objective>
Trim the four unimplemented event-bus integration specs down to their description, their skip,
and a live issue reference — deleting the unreachable setup below.

Purpose: each file carries roughly fifty lines of setup positioned after an unconditional skip.
That code never executes, but it does compile, so every refactor of the harness or the event
types has to keep it building. It is maintenance cost with no verification value. Worse, each
skip cites a retired tracker id that resolves to nothing, so a reader following the reference
finds no work item at all. Issues for all four were filed as a prerequisite.

Output: four small, honest files whose skips point somewhere real.

Decisions implemented: D-11 (trim each body to the skip plus a live issue reference, deleting the
maintained dead code; the call is Ginkgo's bare skip inside a spec closure, not a test-framework
method on a function's first line, which a previous reading got wrong).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-quality-code-health-sweep/09-02-SUMMARY.md
@.planning/phases/09-test-quality-code-health-sweep/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim the four files to description, skip, and issue reference</name>
  <files>test/integration/eventbus_e2e/audit_drift_detector_test.go, test/integration/eventbus_e2e/js_storage_corruption_test.go, test/integration/eventbus_e2e/multi_protocol_fanout_test.go, test/integration/eventbus_e2e/backfill_rebuild_test.go</files>
  <read_first>
    - test/integration/eventbus_e2e/backfill_rebuild_test.go (whole file, 60 lines) — the smallest of the four and the clearest illustration of the target end state; lines 20-30 show the description, the spec, the skip, and where the deletion begins
    - test/integration/eventbus_e2e/audit_drift_detector_test.go (whole file, 70 lines)
    - test/integration/eventbus_e2e/js_storage_corruption_test.go (whole file, 75 lines)
    - test/integration/eventbus_e2e/multi_protocol_fanout_test.go (whole file, 77 lines)
    - .planning/phases/09-test-quality-code-health-sweep/09-02-SUMMARY.md — the file-path to issue-number mapping this task consumes
    - .planning/phases/09-test-quality-code-health-sweep/09-PATTERNS.md, the section on trimming these four files — it carries the target end-state shape
  </read_first>
  <action>
    Find the skip calls with a plain text search for the skip function across the directory. It
    is Ginkgo's bare skip called as the first statement inside a spec closure, not the
    test-framework method on a test function's first line; searching for the latter finds
    nothing and has misled a previous reader.

    For each file, keep exactly four things: the build tag and licence header, the package
    declaration and whatever imports remain genuinely used afterward, the leading comment block
    that explains what the spec would cover and why it is unimplemented, and the container
    description plus spec description plus the skip call. Delete every statement after the skip
    call. Then remove the imports that deletion orphaned — the Go compiler will name them.

    Rewrite the skip message and the follow-up comment so the reference is the GitHub issue
    number recorded by the prerequisite plan for that file, written as a hash-prefixed issue
    number rather than a tracker id. Before writing each number, confirm with a live query that
    the issue is open; do not take the number from the summary without checking it.

    Do not delete any of the four specs. Their descriptions are the record of what the event-bus
    surface does not cover, and deleting them would quietly shrink the known-gap list — which is
    the opposite of what this requirement is for. Do not implement any of the four; that is
    substantial event-bus work explicitly deferred out of this phase.
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/eventbus_e2e/... &amp;&amp; test "$(rg -c 'holomush-[a-z0-9]{4}\b' test/integration/eventbus_e2e/ | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `task test:int -- ./test/integration/eventbus_e2e/...` exits 0 and reports the same number of skipped specs as before the change.
    - Each of the four files is under 35 lines.
    - `rg -c 'holomush-[a-z0-9]{4}\b' test/integration/eventbus_e2e/` returns no matches — every retired tracker id is gone.
    - Each of the four files contains exactly one issue reference in a hash-prefixed numeric form, and `gh issue view <n> -R holomush/holomush --json state --jq .state` returns `OPEN` for each of the four numbers.
    - The four issue numbers are distinct and match the mapping recorded by the prerequisite plan.
    - Each file still contains its container description and its spec description, unchanged in wording.
    - `task lint` exits 0 — no orphaned import survives.
    - `task fmt` has been run and any mutations committed.
  </acceptance_criteria>
  <done>Four files carry only what they actually assert — a named gap and a live pointer to the work that would close it — and roughly two hundred lines of unreachable maintenance burden are gone.</done>
  <reversibility rating="reversible">A file-level revert restores the deleted setup; nothing depends on it.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> known-gap record | The set of skipped specs is the repository's record of what the event-bus surface does not verify |
| repository -> external issue tracker | The skip citations point outside the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-11-01 | Repudiation | skip citations pointing at retired tracker ids | medium | mitigate | A citation that resolves to nothing makes an acknowledged coverage gap untrackable. Each is replaced with an issue number confirmed open by a live query at write time. |
| T-09-11-02 | Tampering | the known-gap record | medium | mitigate | Deleting a skipped spec outright would shrink the visible gap list without closing any gap. The acceptance criteria require all four descriptions to survive verbatim and the skipped-spec count to be unchanged. |
| T-09-11-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed; no dependency manifest touched. |
</threat_model>

<verification>
- `task test:int -- ./test/integration/eventbus_e2e/...` exits 0 with an unchanged skipped-spec count.
- No retired tracker id remains in the directory.
- All four cited issues return `OPEN`.
- `task lint` exits 0.
</verification>

<success_criteria>
Each of the four unimplemented specs is reduced to its description, its skip, and a live issue
reference, with no verification lost and no gap hidden.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- No new files. Modified: the four `_test.go` files under `test/integration/eventbus_e2e/` named in `files_modified`.
- Four hash-prefixed GitHub issue references embedded in skip messages, replacing four retired tracker ids (numbers assigned by plan 09-02).
</artifacts_produced>

<output>
Create `.planning/phases/09-test-quality-code-health-sweep/09-11-SUMMARY.md` when done.
Record the before and after line count for each file and the four issue numbers cited.
</output>
