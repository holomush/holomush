---
phase: 09-test-quality-code-health-sweep
plan: 15
type: execute
wave: 5
depends_on: ["09-12", "09-20"]
files_modified:
  - test/integration/session/lifecycle_privacy_floor_test.go
  - test/integration/privacy/privacy_test.go
autonomous: true
requirements: [QUAL-04]

must_haves:
  truths:
    - "Moving while attached advances the location arrival timestamp, so the history floor follows the character."
    - "A transport-level drop and reconnect with no logout preserves the session and its history floor."
    - "A reattach within the time-to-live returns the events emitted while the player was detached, and an expiry followed by a fresh login does not."
    - "The floor-preservation arm of the privacy specification is asserted directly rather than assumed from adjacent coverage."
  artifacts:
    - "test/integration/session/lifecycle_privacy_floor_test.go — the move, transport-blip, and two named privacy-floor specs"
    - "An added floor-preservation arm in test/integration/privacy/privacy_test.go"
  key_links:
    - "location arrival timestamp -> bounded history query -> which events a player may read"
    - "timestamped emit helper -> events placed at exact instants relative to attach, detach, and reattach"
  prohibitions:
    - "Never assert a history-floor property by counting returned events; assert on the specific event identifiers, so an off-by-one window or an unrelated event cannot make the test pass."
    - "Never reach the expired state through the pre-existing expire helper. Review established it writes a row the reaper never selects, so the second named privacy spec — whose entire premise is that the reaper deleted the session — would be asserting against a state production never produces. Use the detached-past-expiry seam and drive the real reaper."
---

<objective>
Cover the two remaining matrix rows — moving while attached and a transport-level blip — and
land the three named privacy-floor assertions that this matrix is the designated home for.

Purpose: the history floor is the authorization boundary for historical events, and every
transition in this matrix either preserves it, advances it, or resets it. Two of the three
named tests are specified verbatim in the source work item and were explicitly placed here.
The third, the floor-preservation arm of the privacy specification, is a live tracked gap in
the privacy suite. All three need events at exact instants, which is why they depend on the
timestamped emit helper this phase added.

Output: the last two matrix rows covered, plus three floor assertions written to their
specified shapes.

Decisions implemented: D-12 (the move and transport-blip rows of the matrix), D-14 (both adjacent
privacy items folded in — the two named reattach and expiry floor tests, and the
floor-preservation arm — because splitting them would duplicate setup), D-15 (every instant is
placed with the timestamped emit, never with a sleep).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-quality-code-health-sweep/09-12-SUMMARY.md
@.planning/phases/09-test-quality-code-health-sweep/09-07-SUMMARY.md
@.planning/phases/09-test-quality-code-health-sweep/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cover moving while attached and the transport-level blip</name>
  <files>test/integration/session/lifecycle_privacy_floor_test.go</files>
  <read_first>
    - test/session-matrix.yaml — the rows for moving while attached and for a transport drop and reconnect with no logout, and which columns each marks as spec-covered; the move row has no web-guest cell and the blip row's multi-session cell may be cited elsewhere
    - .planning/phases/09-test-quality-code-health-sweep/09-12-SUMMARY.md — the marker-comment form and the shared harness accessor
    - internal/testsupport/integrationtest/session.go, the `MoveTo` helper INCLUDING its full doc comment — read the scope paragraph carefully. It updates the `sessions` row's `location_id` and `location_arrived_at` directly in Postgres, deliberately does NOT invoke `world.Service.MoveCharacter`, and deliberately does NOT update `characters.location_id`. It simulates the post-movement-hook state; it does not produce it.
    - internal/world/service.go, `MoveCharacter` and the movement hook it invokes — the production path `MoveTo` bypasses. Read enough of it to see what a spec driving the real pipeline would additionally exercise (the guarded world mutation and the hook), so the labelling decision below is made against the real difference rather than a guess.
    - internal/testsupport/integrationtest/session.go lines 355-440 and 700-740 — the detach, reattach, and bounded history-query helpers
    - .planning/phases/09-test-quality-code-health-sweep/09-07-SUMMARY.md — the timestamped emit helper's signature
  </read_first>
  <action>
    Write specs for the two remaining transitions across the columns the registry marks as
    spec-covered, each carrying exactly one matrix-row marker.

    For moving while attached: emit an event into the destination location before the move, move
    the character, then read history bounded from the new arrival timestamp and assert the
    pre-move event's identifier is absent while a post-move event's identifier is present.
    Asserting only that the arrival timestamp changed would prove bookkeeping; asserting the
    history consequence proves the property that matters.

    LABEL THIS ROW HONESTLY — review found it claiming more than it proves. The `MoveTo` helper
    updates the `sessions` row directly and, by its own doc comment, deliberately skips
    `world.Service.MoveCharacter` and never touches `characters.location_id`. A spec built on it
    therefore proves that **the privacy floor is applied correctly to a session whose location
    changed** — which is a real and worthwhile property — but it does NOT prove that the
    production movement pipeline advances the floor, because that pipeline never runs. Those are
    different claims and the registry must not conflate them.

    Resolve it one of these two ways, and say which you chose and why in the SUMMARY:

    (a) Preferred if it is tractable within this task: drive the move through
        `world.Service.MoveCharacter` and its movement hook, so the spec exercises the production
        path and the row genuinely covers "moving while attached". If you take this route, verify
        the session row's arrival timestamp is advanced BY the pipeline rather than by the test,
        since that is the whole difference.
    (b) Otherwise: keep `MoveTo` and RELABEL the cell for what it actually verifies —
        privacy-floor behaviour after a simulated move — in both the spec's description and the
        registry row's notes. Then mark the production movement-lifecycle claim explicitly as
        covered elsewhere if a world or movement suite already exercises it, or as uncovered with
        a filed issue if none does. Do not leave that claim implicitly satisfied by this row.

    What is NOT acceptable is leaving the row described as production movement coverage while the
    spec simulates the move. That is the overstated-assurance pattern this phase exists to
    eliminate, and it is more durable here than elsewhere because the claim is committed to a
    machine-read registry.

    For the transport blip: drop the transport and reattach without any logout, then assert the
    session identifier is unchanged, the status returned to active, and the arrival timestamp is
    unchanged — and that an event emitted during the gap is still readable afterward. A blip that
    reset the floor would swallow exactly those events.

    Assert on specific event identifiers returned by the timestamped emit helper, never on the
    count of returned events. A count assertion passes when the window is off by one in a
    direction that happens to keep the total the same, and passes when an unrelated event fills
    the gap.

    Use no sleep. Every instant in these specs is chosen explicitly through the timestamped emit
    helper.
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/session/...</automated>
  </verify>
  <acceptance_criteria>
    - `task test:int -- ./test/integration/session/...` exits 0.
    - The move specs assert the pre-move event identifier is absent and a post-move event identifier is present in a history read bounded from the new arrival timestamp.
    - The move row is labelled for what it actually exercises. The SUMMARY states which route was taken — (a) driving `world.Service.MoveCharacter` and its hook, or (b) keeping the direct helper and relabelling the cell as privacy-floor-after-simulated-move — and, under (b), where the production movement-lifecycle claim is covered instead, or the issue number filed because it is not.
    - Under (b), neither the spec description nor the registry row's notes describe this cell as covering the production movement pipeline. `rg -c 'MoveCharacter' test/integration/session/lifecycle_privacy_floor_test.go` returning 0 while the row claims production movement coverage is a failure of this criterion.
    - The blip specs assert unchanged session identifier, restored active status, unchanged arrival timestamp, and that an event emitted during the gap is still readable.
    - No assertion in this file compares only a count of returned events; every history assertion names an event identifier.
    - Every added spec carries exactly one matrix-row marker matching a spec-covered registry row, and no marker duplicates one used elsewhere.
    - `rg -c 'time\.Sleep' test/integration/session/` has not increased.
    - `task lint` exits 0.
  </acceptance_criteria>
  <done>The move and blip transitions are proven by their history consequences, not by their bookkeeping, closing the last two matrix rows — and the move row's description matches exactly what its spec drives, whether that is the production movement pipeline or a simulated move with the privacy floor as the property under test.</done>
  <reversibility rating="reversible">Additive specs.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Write the two named privacy-floor specs to their specified shapes</name>
  <files>test/integration/session/lifecycle_privacy_floor_test.go</files>
  <read_first>
    - .planning/phases/09-test-quality-code-health-sweep/09-RESEARCH.md, the section reproducing the two named privacy tests verbatim — each specifies an exact sequence of instants and an exact expected visibility outcome; these are the specifications, not suggestions
    - .planning/archive/beads/2026-07-09-beads-live.jsonl — locate the originating record if any detail in the reproduction is ambiguous
    - internal/testsupport/integrationtest/session.go lines 700-740 — the bounded history-query helper
    - .planning/phases/09-test-quality-code-health-sweep/09-20-SUMMARY.md — the detached-past-expiry seam's signature and the recorded reason the pre-existing expire helper cannot stand in for it
    - .planning/phases/09-test-quality-code-health-sweep/09-13-SUMMARY.md — the real-reaper drive established there; reuse its shape rather than re-deriving it
    - The specs written in Task 1, so setup is shared rather than duplicated
  </read_first>
  <action>
    Write the two named specs exactly as specified.

    The first: a character connects at an initial instant in a location, establishing its arrival
    timestamp; a third party emits at a later instant; the character's transport drops, moving the
    session to detached; the third party emits again at a still later instant; the character
    reattaches through the subscribe compare-and-swap path within the time-to-live; the
    character's history query for that location returns the events across the whole window,
    including both third-party events. Assert both identifiers are present.

    The second: a character connects at an initial instant; drops; no reattach occurs for longer
    than the time-to-live; the reaper deletes the session; the character logs in again, producing
    a fresh character selection with a new arrival timestamp; events from before that new
    timestamp are not visible. Assert the earlier event's identifier is absent.

    Keep the two specified test names. They are pinned by a meta-test in the privacy
    specification that finds them by name, so renaming them breaks that meta-test. Note that
    their form uses an underscore separator; under the tightened naming predicate that lands
    later in this phase they are not violations, because their trailing segments are multi-word
    descriptive clauses. Record both names in the SUMMARY so the naming sweep leaves them alone.

    For the second spec, reach the expired state the way production does: detach, apply the
    detached-past-expiry seam, and run a real reaper — the spec's own wording is that the reaper
    deletes the session, so a spec that instead forced a terminal status directly would assert
    against a state production never produces. Do not use the pre-existing expire helper. Use the
    timestamped emit helper for every event placement in both specs, and no sleep in either.
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/session/...</automated>
  </verify>
  <acceptance_criteria>
    - `task test:int -- ./test/integration/session/...` exits 0 with both named specs present and passing.
    - Both specified test names appear verbatim; `rg -c 'ReattachWithinTTLPreservesFloor' test/integration/session/` and `rg -c 'TTLExpiryEndsSessionFreshFloor' test/integration/session/` each return at least 1.
    - The first spec asserts both third-party event identifiers are present in the post-reattach history read.
    - The second spec asserts the pre-expiry event identifier is absent from the post-relogin history read.
    - Neither spec uses a sleep or alters a production time-to-live constant.
    - The second spec reaches the expired state by detach plus the detached-past-expiry seam plus a real reaper run; `rg -c 'ExpireSession' test/integration/session/` returns no matches.
    - Both names are recorded in the SUMMARY as exempt from the naming sweep.
    - `task lint` exits 0.
  </acceptance_criteria>
  <done>The two specified floor tests exist, pass, and are named exactly as the meta-test that finds them expects.</done>
  <reversibility rating="reversible">Additive specs.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Land the floor-preservation arm in the privacy suite</name>
  <files>test/integration/privacy/privacy_test.go</files>
  <read_first>
    - test/integration/privacy/privacy_test.go (whole file) — the existing specs, the harness start, and the gate-bypass arm this completes; note which spec identifiers are already asserted by identifier
    - .planning/phases/09-test-quality-code-health-sweep/09-RESEARCH.md, the section resolving which access engine this arm needs — the answer is the harness default, not the real-policy opt-in
    - .planning/phases/09-test-quality-code-health-sweep/09-07-SUMMARY.md — the timestamped emit helper's signature and the smoke spec that plan added, so this arm builds on it rather than duplicating it
    - test/integration/privacy/privacy_test.go, the `// Verifies: INV-PRIVACY-6` annotation on the existing gate-bypass spec — the form to match if this arm earns the same annotation
    - docs/architecture/invariants.yaml, the INV-PRIVACY-6 entry — its summary and its `asserted_by` list, so the annotation decision is made against the invariant's actual text rather than its identifier
    - .claude/rules/invariants.md — the binding rules, in particular the prohibition on annotating a test that does not genuinely prove the invariant
  </read_first>
  <action>
    Add the floor-preservation arm of the privacy specification's identifier to the privacy
    suite, completing a gap tracked as a live issue. The suite already contains the gate-bypass
    arm of the same identifier; this arm asserts the complementary property, so place it beside
    that one and cite the identifier in the same by-identifier form the suite already uses.

    Start from the harness's default access engine. A previously open question asked whether this
    arm needed the real-policy option; it does not — the harness default is permissive, which is
    what gives this arm its gate bypass, and the real-policy option is the opt-in used by denial
    specs. Do not pass the opt-in here.

    Place every event with the timestamped emit helper and assert on identifiers, not counts, for
    the same reasons as the other floor specs.

    Confirm the tracking issue is open before starting, and record its number in the SUMMARY so
    it can be closed with the phase's pull request.

    Carry a `// Verifies: INV-PRIVACY-6` annotation on this spec — but only if it genuinely
    asserts that invariant. The sibling spec already in this file carries exactly that annotation
    for the gate-bypass arm, and this spec completes the same invariant's floor-preservation arm,
    so the annotation is very likely correct and its absence would leave the two arms
    inconsistently marked. Verify rather than assume: read the invariant's summary in
    `docs/architecture/invariants.yaml` and confirm this spec's assertions actually prove the
    clause it names. If they do, add the annotation and add this file to that entry's
    `asserted_by` list if it is not already there. If they do not, leave the annotation off and
    say so in the SUMMARY — the repo's rules are explicit that a `// Verifies:` on a test that does
    not prove the invariant is a false green, and the registry has a recorded history of exactly
    that bug.
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/privacy/...</automated>
  </verify>
  <acceptance_criteria>
    - `task test:int -- ./test/integration/privacy/...` exits 0 with the new arm present.
    - The new spec cites the privacy specification identifier by identifier, matching the suite's existing citation form.
    - Either the spec carries `// Verifies: INV-PRIVACY-6` and the invariant's `asserted_by` list names this file, or the SUMMARY states explicitly why the annotation was withheld after reading the invariant's summary. Silence on the question does not satisfy this criterion; an annotation added without checking the invariant text is worse than its absence.
    - `rg -c 'WithRealABAC' test/integration/privacy/` has not increased — the harness default is used.
    - The spec asserts on specific event identifiers, not on a count.
    - The tracking issue number is recorded in the SUMMARY and was confirmed open before the work started.
    - `task lint` exits 0 and `task fmt` mutations are committed.
  </acceptance_criteria>
  <done>The floor-preservation arm is asserted directly in the privacy suite, closing a tracked gap, with the access-engine question resolved to the harness default.</done>
  <reversibility rating="reversible">One additive spec in an existing suite.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| location arrival timestamp -> historical event visibility | The floor is the authorization boundary deciding which past events a player may read |
| transport drop and reconnect -> session continuity | A blip must not be mistaken for a departure, nor a departure for a blip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-15-01 | Information Disclosure | history floor across move, blip, reattach, and expiry | high | mitigate | Each transition either preserves, advances, or resets the floor, and getting any of them wrong leaks history a player was not present for or hides history they were. Every spec asserts the specific event identifiers a wrong floor would expose or swallow, across all four transitions. |
| T-09-15-02 | Repudiation | count-based history assertions | medium | mitigate | A count assertion passes under an off-by-one window and under substitution by an unrelated event, so it overstates what it proves. Every history assertion in this plan names an event identifier, checked by an explicit acceptance criterion. |
| T-09-15-03 | Elevation of Privilege | access engine selection for the floor-preservation arm | medium | accept | The arm deliberately runs against the permissive default so it isolates the floor property from policy evaluation; denial behaviour is covered by the suite's existing real-policy specs. A negative check prevents the opt-in being adopted here silently. |
| T-09-15-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed; no dependency manifest touched. |
</threat_model>

<verification>
- `task test:int -- ./test/integration/session/... ./test/integration/privacy/...` exits 0.
- Both specified privacy test names are present verbatim.
- No history assertion in this plan is count-based; none uses a sleep.
- `task lint` exits 0.
</verification>

<success_criteria>
Every populated matrix row now has a passing spec or a confirmed pointer, and the three named
floor assertions exist and pass with the exact shapes their specifications describe.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- `test/integration/session/lifecycle_privacy_floor_test.go` (NEW) — move and transport-blip specs plus the two named privacy-floor tests
- Test names `TestPrivacy_ReattachWithinTTLPreservesFloor` and `TestPrivacy_TTLExpiryEndsSessionFreshFloor` — pinned by an external meta-test and exempt from the naming sweep in plan 09-18
- One new spec in `test/integration/privacy/privacy_test.go` asserting the floor-preservation arm
- Matrix-row marker comments for the move and blip rows, consumed by the bijection meta-test in plan 09-16
</artifacts_produced>

<output>
Create `.planning/phases/09-test-quality-code-health-sweep/09-15-SUMMARY.md` when done.
Record every matrix-row marker consumed, both pinned test names as sweep-exempt, and the
tracking issue number for the floor-preservation arm.
</output>
