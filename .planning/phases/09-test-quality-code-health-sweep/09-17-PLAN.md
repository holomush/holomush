---
phase: 09-test-quality-code-health-sweep
plan: 17
type: execute
wave: 9
depends_on: ["09-21"]
files_modified:
  - .codecov.yml
autonomous: true
requirements: [QUAL-02]

assumptions:
  - statement: "Planner-run live queries against the GitHub status API found that on the three most recent merged Go-changing pull requests, the codecov/patch context appears on the MERGE commit for two of the three and is absent entirely for the third, while codecov/project appears on none of them. On the pull-request HEAD commits, the only status context present is CodeRabbit — neither coverage context appears. So the ref matters, and the project context was not observed posting anywhere. This is evidence for the executor's Task 1 to reproduce and extend, not a conclusion to assume: the queries were run against merged pull requests whose head branches are deleted, and the phase's own open pull request may behave differently."
    verification: verified-against-live-api

must_haves:
  truths:
    - "It is established from live evidence whether both coverage status contexts actually post on a pull request in this repository, and on which ref they post."
    - "If either context does not post, the reason is diagnosed and the configuration corrected before anyone is asked to make it a required check."
    - "The exact context strings that appear in the status rollup are recorded verbatim, because a required check is matched by string."
    - "Every rollup this plan records was observed against a commit that is both the local head and the pull request's head, so no verdict describes configuration the branch has already moved past."
    - "The go-or-no-go verdict is backed by a command that queries the status API and fails when a context is missing — not by a command that lists pull-request numbers."
  artifacts:
    - "A recorded evidence table of coverage status contexts observed on live pull requests, distinguishing the head commit from the merge commit"
    - "Any .codecov.yml correction needed to make both contexts post"
  key_links:
    - "codecov status configuration -> status context string on a specific commit -> the required-check name in the branch ruleset"
    - "the phase's own open pull request (opened by plan 09-21) -> its head commit -> the rollup this plan observes"
  prohibitions:
    - "Never treat the presence of a status block in configuration as evidence that the status posts; a required check that never arrives blocks every pull request with nothing visibly failing, and only live observation distinguishes the two."
    - "Never observe a status rollup without first asserting that the pull request's head commit equals the local head. A local-only or unpushed configuration edit leaves the API answering for the previous commit — plausibly, and for configuration the branch no longer carries — and that verdict flows into a branch-protection mutation two plans later."
    - "Never let a gate for this plan pass without querying the status API. Review found both of this plan's original gates passing without observing a single status context — one listed pull-request numbers, the other confirmed two configuration literals. That is the same false-green class this phase exists to eliminate, and it is the most consequential instance because the action it gates can block every pull request in the repository."
---

<objective>
Establish, from live evidence, that both coverage status contexts actually appear on pull
requests in this repository — and fix the configuration if either does not — before the phase
asks an operator to make them required.

Purpose: making a status required that never posts is a silent deadlock. Every pull request
sits blocked waiting for a check that will never arrive, with nothing failing to point at. This
repository has already suffered that exact failure once, from a different cause, and the
incident is written into its own rules. Independent observation found the patch context posting
and passing on recent merged pull requests, and the project context not appearing in the status
rollup at all. The stated explanation is that the project status only emits when there is a base
to compare against — plausible, but it is an assumption, and it is the assumption the whole
gate rests on.

Output: an evidence table, a corrected configuration if one is needed, and an explicit go or
no-go for the operator action in the final plan.

Decisions implemented: D-04 (make both coverage statuses required within the phase). The
ordering call is mine and is recorded here: this verification runs after the two thinnest-margin
code plans and after the command-package backfill, and the operator action itself moves to the
final plan because the phase ships as a single pull request (D-22), so the only moment live
status evidence exists for this branch is after its own continuous integration has run. That
inverts D-04's literal "require early" while honouring its intent — the gate lands inside the
phase and the tolerance is tightened last — and it eliminates the deadlock risk that requiring an
unobserved status would create.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-quality-code-health-sweep/09-RESEARCH.md
@.planning/phases/09-test-quality-code-health-sweep/09-21-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather live evidence of which coverage status contexts post on pull requests</name>
  <files>(no repo files — evidence recorded in the SUMMARY)</files>
  <read_first>
    - .codecov.yml lines 4-44 — the notification block and both status blocks; note the two-upload wait and the wait-for-continuous-integration setting, which together mean a status is absent rather than failing until both sessions land
    - .planning/phases/09-test-quality-code-health-sweep/09-RESEARCH.md, the section on the gate's current state — it records the ruleset's current required-check list and what was observed on two recent merged pull requests
    - .claude/rules/landing-the-plane.md, the paragraph on required checks that never arrive — the prior incident whose failure mode this task exists to avoid
    - .planning/phases/09-test-quality-code-health-sweep/09-21-SUMMARY.md — the phase's own pull-request number and head commit hash; that commit is the one whose rollup matters most, because it is the branch the ruleset change will be tested against
    - This plan's `assumptions` block — the planner's own live query results, which you are reproducing and extending rather than trusting
  </read_first>
  <action>
    Collect the status rollup for at least three recently merged pull requests that changed Go
    source. Select them by filtering on changed file paths, not by taking the three most recent —
    the planner found that none of the ten most recent merged pull requests changed Go source at
    all, so an unfiltered sample would examine documentation and dependency bumps and conclude
    nothing about coverage. Use the commit status API, not the checks list: coverage statuses are
    commit statuses rather than check runs, and the two are reported through different endpoints.

    Query BOTH refs for each pull request — the head commit and the merge commit — and record them
    separately. The planner observed them differing: the patch context appeared on merge commits
    for two of three sampled pull requests and on no head commit, while the project context
    appeared on neither ref for any of them. Which ref carries the status decides whether a
    required check can ever be satisfied, because a required check is evaluated against the pull
    request's head commit. If that observation reproduces, it is the central finding of this plan,
    not a footnote.

    Then query the phase's own pull request, using the number and head commit the previous plan
    recorded. Judge by the final rollup after continuous integration completes, not by an early
    poll: the configuration deliberately withholds both statuses until two upload sessions have
    merged, so an early query shows an absence meaning "not yet" rather than "broken". Say in the
    SUMMARY whether the run had completed when you queried.

    Record the exact context strings verbatim. A required check is matched by string, so a rollup
    carrying a differently spelled context than the one an operator would type is itself a finding.

    Produce a verdict and state which of four holds: both contexts post on the head commit and
    both can safely be required; only one posts and the other needs a configuration change; a
    context posts under an unexpected name and the operator must be told the exact string; or a
    context posts only on the merge commit, which means it can never satisfy a required check and
    requiring it would deadlock every pull request. Proceed to Task 2 for any verdict other than
    the first.
  </action>
  <verify>
    <automated>set -o pipefail; PRS=$(gh pr list -R holomush/holomush --state merged --limit 60 --json number,files --jq '[.[] | select(any(.files[]?.path; endswith(".go"))) | .number] | .[0:3] | .[]') || exit 1; test "$(printf '%s\n' "$PRS" | rg -c .)" -eq 3 || exit 1; printf '%s\n' "$PRS" | while read -r n; do H=$(gh pr view "$n" -R holomush/holomush --json headRefOid --jq .headRefOid) || exit 1; M=$(gh pr view "$n" -R holomush/holomush --json mergeCommit --jq .mergeCommit.oid) || exit 1; test -n "$H" -a -n "$M" || exit 1; gh api "repos/holomush/holomush/commits/$H/status" --jq '.state' >/dev/null || exit 1; gh api "repos/holomush/holomush/commits/$M/status" --jq '.state' >/dev/null || exit 1; done</automated>
    <note>This gate proves the EVIDENCE was obtainable, which is what Task 1 delivers; it deliberately does not require both contexts to be present, because whether they are present is the finding rather than the gate — a gate demanding them would be unsatisfiable given the planner's live observation that the project context posts nowhere, and would block the very diagnosis Task 2 exists to perform. It is fail-closed on every step that could silently under-report: `pipefail` is set so a failed `gh` cannot be masked by a successful downstream stage; every `gh` invocation is `|| exit 1` so an API error is fatal rather than an empty string; the selection is asserted to yield exactly three Go-changing pull requests, so a filter that matched nothing fails instead of iterating zero times; and both refs are asserted non-empty before being queried. The superseded form listed three pull-request numbers without checking they changed Go and without querying a single status.</note>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains an evidence table with one row per examined pull request AND per ref, listing every coverage-related context string verbatim and its conclusion, with head-commit and merge-commit rows distinguished.
    - At least three merged pull requests that changed Go source were examined, selected by file-path filter rather than by recency alone; the SUMMARY records how many pull requests had to be scanned to find three.
    - The phase's own pull request was queried by the number and head commit recorded by plan 09-21, and the SUMMARY says whether its continuous-integration run had completed.
    - The SUMMARY states one of the four verdicts explicitly, with the observed rollups that support it.
    - If the project context is observed on no ref, the SUMMARY says so plainly and the verdict is no-go pending Task 2 — it is not softened into "did not appear yet".
    - No conclusion in the SUMMARY rests on the presence of a status block in the configuration file; every claim about what posts cites an observed rollup.
  </acceptance_criteria>
  <done>What actually posts is known from observation, recorded verbatim, and a verdict is on record for the operator action in the final plan.</done>
  <reversibility rating="reversible">Evidence gathering only.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Correct the configuration if a context does not post, and re-verify</name>
  <files>.codecov.yml</files>
  <read_first>
    - .codecov.yml lines 23-44 — both status blocks in full, including the comments that describe the current intent
    - The evidence table produced by Task 1
    - .planning/phases/09-test-quality-code-health-sweep/09-01-SUMMARY.md — the project-status comment as this phase rewrote it, so any further edit is consistent with it
  </read_first>
  <action>
    Skip this task entirely if Task 1's verdict was that both contexts post. Say so in the
    SUMMARY and change nothing — an unnecessary configuration edit here risks the very deadlock
    this plan exists to prevent.

    If a context does not post, diagnose why from the observed evidence before changing anything.
    The plausible causes are distinguishable: a status configured as informational reports but
    does not gate and may be filtered from the rollup; a status with no base commit to compare
    against does not emit at all on a first-of-its-kind branch; and a status whose upload count
    never reaches the configured threshold is withheld indefinitely. Each has a different fix and
    the wrong fix makes things worse.

    Make the narrowest change that causes the context to post, and leave the threshold value
    alone — tightening it is the final plan's job. Do not raise the upload-count threshold; the
    file's own comment explains that a higher value blocks notification entirely.

    Re-verify by observing a fresh rollup on the phase pull request's NEW head commit after the
    change has been pushed and continuous integration has completed. A configuration change
    unverified against a live rollup has not established anything, and this plan's entire purpose
    is to avoid asking an operator to act on an unverified assumption.

    PUBLISH BEFORE YOU OBSERVE — this is a required checkpoint, not a suggestion. Plan 09-21
    performed the only unconditional push in the phase, so any edit made HERE exists only locally
    until you push it. The status API answers for whatever commit the pull request currently
    exposes, which after a local-only edit is the PREVIOUS head — carrying the OLD configuration.
    Observing that rollup and recording a verdict from it means the verdict describes configuration
    that is no longer what the branch contains, and plan 09-19 mutates branch protection on the
    strength of that verdict. Nothing in the query itself reveals the mismatch: it returns a
    perfectly plausible answer for the wrong commit.

    So, after any edit in this task, before any observation:

    1. Commit the change and push it.
    2. Assert that the pull request's head is your head, and refuse to proceed if it is not:

           test "$(git rev-parse HEAD)" = "$(gh pr view <n> -R holomush/holomush --json headRefOid --jq .headRefOid)"

    3. Wait for continuous integration to complete on that exact commit
       (`gh pr checks --watch`), and remember that the two-upload notification threshold means
       both statuses are withheld until both sessions merge — an early query shows absence meaning
       "not yet", not "broken".
    4. Query the status API for THAT SHA and record it alongside the SHA.

    Record in the SUMMARY the head commit every observation was taken against. A rollup recorded
    without its SHA cannot be audited later and is not evidence.

    If this task made no edit — the skip case above — no republish is needed, but still record the
    SHA the observation was taken against, and still assert it equals the local head so a stale
    observation cannot be inherited from an earlier plan.

    If, after diagnosis, a context genuinely cannot be made to post on the head commit, that is
    the answer and the verdict is no-go for that context. Say so and stop. Do NOT relax this
    plan's own gate to let the phase proceed: the final plan's operator checkpoint reads this
    verdict, and a no-go there means the ruleset gains only the context that was observed, with
    the other recorded as a follow-up issue. A partial gate that works is worth more than a
    complete gate that deadlocks the repository, and this plan's rules record an incident where
    exactly that deadlock happened.
  </action>
  <verify>
    <automated>set -o pipefail; rg -v '^\s*#' .codecov.yml | rg -c 'after_n_builds: 2' &gt;/dev/null || exit 1; rg -v '^\s*#' .codecov.yml | rg -c 'threshold: 1%' &gt;/dev/null || exit 1; BR=$(git branch --show-current) || exit 1; PR=$(gh pr list -R holomush/holomush --head "$BR" --json number --jq '.[0].number') || exit 1; test -n "$PR" || exit 1; SHA=$(gh pr view "$PR" -R holomush/holomush --json headRefOid --jq .headRefOid) || exit 1; test -n "$SHA" || exit 1; test -z "$(git status --porcelain -- .codecov.yml)" || exit 1; test "$(git rev-parse HEAD)" = "$SHA" || exit 1; gh api "repos/holomush/holomush/commits/$SHA/status" --jq '.statuses[].context' &gt; /tmp/p9-17-head-contexts.txt || exit 1; printf 'sha=%s patch=%s project=%s\n' "$SHA" "$(rg -cx 'codecov/patch' /tmp/p9-17-head-contexts.txt || echo 0)" "$(rg -cx 'codecov/project' /tmp/p9-17-head-contexts.txt || echo 0)" &gt; /tmp/p9-17-verdict.txt || exit 1; cat /tmp/p9-17-verdict.txt; test -s /tmp/p9-17-verdict.txt</automated>
    <note>
      This gate asserts that the EVIDENCE WAS OBTAINED AND THE PER-CONTEXT VERDICT RECORDED, which
      is always achievable — it does NOT assert that both contexts post. The superseded form did,
      and was documented in its own note and acceptance criteria as EXPECTED TO FAIL on first run,
      with a recorded no-go as the intended completion. That contract cannot be honoured: the
      runner treats a non-zero `&lt;automated&gt;` as task failure and will retry or halt, so a plan
      whose designed outcome is a red gate never reaches the diagnosis it exists to perform. The
      go/no-go now lives in the acceptance criteria, where both outcomes are explicit; this
      follows plan 09-01's dual-completion shape.

      TWO STALENESS GUARDS were added after review found this gate could observe a rollup for
      configuration the branch no longer carries. `test -z "$(git status --porcelain -- .codecov.yml)"`
      fails when this task's own edit is uncommitted, and
      `test "$(git rev-parse HEAD)" = "$SHA"` fails when it is committed but unpushed — the case
      where the pull request still exposes the previous head and the API answers, plausibly and
      wrongly, for the OLD configuration. Neither condition is detectable from the query's own
      output: it returns a well-formed rollup either way. Since plan 09-19 mutates branch
      protection on the verdict this gate records, an observation against a stale SHA propagates
      into a repository-settings change, which is why these are hard assertions rather than
      reminders in the prose.

      Still fail-closed on everything that could silently under-report. Falseness: the upload-count
      threshold or the tightening literal was changed — the two positive assertions run first and
      exit 1, so a missing or unreadable file fails here rather than satisfying a later negation.
      Both filter comment lines before counting: the file's own comment prose quotes both literals
      to explain them, so an unfiltered presence check is satisfied by the comments alone and would
      still pass with the real setting deleted. Continuing: the branch has no pull request — `$PR` is empty and `test -n` exits 1 rather than
      querying an empty ref; any `gh` call errors, rate-limits, or fails auth — every one is
      `|| exit 1`, so a failure is fatal instead of yielding an empty context list that would read
      as "no contexts posted"; the head commit cannot be resolved — `test -n "$SHA"` exits 1; the
      status query fails — `|| exit 1`; the verdict file is somehow empty — `test -s` exits 1.
      `pipefail` is set so a failed `gh api` cannot be masked by a successful downstream stage.
      The two counts are whole-line matches (`rg -cx`), so a substring such as `codecov/patch/foo`
      cannot be counted as `codecov/patch`, and a commit carrying zero statuses records `0` for
      both rather than failing — that is a finding, and this task's job is to record it.
    </note>
  </verify>
  <acceptance_criteria>
    - `rg -v '^\s*#' .codecov.yml | rg -c 'after_n_builds: 2'` still returns 1 — the upload-count threshold was not raised.
    - `rg -v '^\s*#' .codecov.yml | rg -c 'threshold: 1%'` still returns 1 — the tightening is left to the final plan.
    - Both counts filter comment lines first. Measured against the file as it stands, the unfiltered forms return 3 and 2 respectively, because the surrounding comment prose quotes both literals to explain them; an unfiltered `== 1` assertion is therefore already false before this task changes anything, and would fail for a reason having nothing to do with the setting.
    - If a change was made, the SUMMARY names the diagnosed cause and why the alternatives were ruled out from the observed evidence, citing rollups rather than reasoning from the configuration file.
    - A fresh rollup on the phase pull request's head commit, observed after continuous integration completed, is recorded in the SUMMARY verbatim — including the case where a context is still absent.
    - Every rollup recorded in the SUMMARY names the SHA it was observed against. A rollup without its SHA is not evidence and does not satisfy any criterion here.
    - If this task edited `.codecov.yml`, that edit was committed AND pushed BEFORE the observation, and `git rev-parse HEAD` equalled the pull request's `headRefOid` at observation time — asserted by the gate, and the equality recorded in the SUMMARY. An observation taken against a head that predates the edit describes configuration the branch no longer carries, and plan 09-19 mutates branch protection from this verdict.
    - The SUMMARY states the SHA it is handing to plan 09-19, so that plan can re-assert head equality before the operator acts rather than trusting the verdict is still current.
    - The SUMMARY closes with an explicit per-context go or no-go and the exact strings an operator should enter. A no-go for one context is an acceptable outcome and must be stated as such, with a follow-up issue number, rather than being smoothed into a go.
    - The verdict line the gate wrote to `/tmp/p9-17-verdict.txt` is transcribed into the SUMMARY verbatim, with its head commit, and exactly one of these two outcomes is recorded for EACH context:
      - GO — the count for that context is 1 on the head commit, so an operator may safely require it, and the SUMMARY gives the exact string.
      - NO-GO — the count is 0 after diagnosis and the narrowest available fix, so the SUMMARY names the diagnosed cause, states plainly that the context posts on no ref, and carries a follow-up issue number. This is an acceptable completion, not a failure to be retried, and it must not be smoothed into a go or softened into "did not appear yet".
    - Neither outcome was reached by editing the gate. The gate asserts only that the evidence was gathered and the verdict recorded; it is silent on which verdict, so there is nothing in it to loosen.
  </acceptance_criteria>
  <done>Either both contexts were observed posting on the phase pull request's head commit, or the reason one cannot is diagnosed, recorded, and carried forward as an explicit per-context no-go with a follow-up issue number — with the operator given only strings that have been seen to arrive. Both are acceptable completions; a go recorded for a context that was not observed arriving is not.</done>
  <reversibility rating="reversible">A configuration edit, reverted by a file-level revert; no external state changed.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external coverage service -> branch protection ruleset | A required check name is matched against a status the repository does not itself produce |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-17-01 | Denial of Service | branch ruleset requiring a status that never posts | high | mitigate | A required check that never arrives blocks every pull request with nothing visibly failing, and undoing it needs an operator round trip. Live rollups are observed on at least three merged pull requests and on this branch before any operator action is requested, and the exact context strings are recorded. |
| T-09-17-02 | Repudiation | configuration presence read as evidence of behaviour | medium | mitigate | A status block in the configuration file proves only intent. Every claim about what posts must cite an observed rollup, and a configuration change must be re-verified against a fresh rollup before the go verdict is issued. |
| T-09-17-03 | Denial of Service | raising the upload-count threshold | medium | mitigate | The file's own comment records that a higher upload-count threshold blocks notification entirely. A positive check pins the current value and runs before any other assertion, so an unreadable file fails the gate rather than satisfying it. |
| T-09-17-04 | Repudiation | this plan's own gates passing without observing a status | high | mitigate | Review found both original gates false-green: one listed pull-request numbers, the other confirmed two configuration literals, and neither queried the status API — while the action they gate can block every pull request in the repository. Both are replaced with commands that query the API, set `pipefail`, treat every API error as fatal, assert the pull request resolves, and match context strings whole-line. The Task 2 gate is expected to fail on first run, and an acceptance criterion forbids weakening it to make the plan pass. |
| T-09-17-06 | Repudiation | verdict recorded against a stale pull-request head | high | mitigate | Plan 09-21 performs the phase's only unconditional push, so a `.codecov.yml` edit made here is local until pushed. The status API then answers for the previous head, carrying the OLD configuration, and returns a well-formed rollup that reveals nothing about the mismatch. Plan 09-19 mutates branch protection from this verdict, so a stale observation becomes a repository-settings change made on evidence that no longer describes the branch. Mitigated by a publish-then-observe checkpoint in the action, by two hard gate assertions (working tree clean for `.codecov.yml`, and local head equal to `headRefOid`), and by requiring every recorded rollup to name its SHA. |
| T-09-17-05 | Denial of Service | requiring a context that posts only on the merge commit | high | mitigate | A required check is evaluated against the pull request's head commit. The planner's live queries found the patch context on merge commits and on no head commit, and the project context on neither. Requiring a merge-commit-only context would block every pull request permanently. Task 1 queries both refs separately and the verdict list carries this outcome explicitly. |
| T-09-17-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed; no dependency manifest touched. |
</threat_model>

<verification>
- The SUMMARY carries an evidence table from at least three merged Go-changing pull requests, with head-commit and merge-commit rows distinguished, plus a per-context verdict.
- The phase's own pull request head commit was queried and its rollup recorded.
- The upload-count threshold and the coverage threshold are both unchanged.
- Every claim about what posts cites an observed rollup; no claim rests on the configuration file.
- Neither gate was weakened to make the plan pass.
</verification>

<success_criteria>
Nobody is asked to make a status required until it has been observed posting, and the exact
strings to enter are recorded.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- An evidence table in the SUMMARY of coverage status contexts observed on live pull requests, with verbatim context strings
- A go or no-go verdict for the operator ruleset action in plan 09-19
- Possibly a narrow `.codecov.yml` status-block correction, only if a context was observed not to post
</artifacts_produced>

<output>
Create `.planning/phases/09-test-quality-code-health-sweep/09-17-SUMMARY.md` when done.
The verdict and the verbatim context strings are the primary artifacts — plan 09-19's operator
checkpoint reads both.
</output>
