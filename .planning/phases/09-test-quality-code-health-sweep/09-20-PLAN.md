---
phase: 09-test-quality-code-health-sweep
plan: 20
type: execute
wave: 3
depends_on: ["09-07"]
files_modified:
  - internal/testsupport/integrationtest/session.go
  - internal/testsupport/integrationtest/harness.go
  - test/integration/privacy/privacy_test.go
autonomous: true
requirements: [QUAL-04]

assumptions:
  - statement: "The doc comment on OpenTelnetSession claims telnet differentiation requires Subscribe goroutine wiring belonging to a different work item. That wiring now exists: Session.attach already starts a real Subscribe goroutine, blocks on the replay-complete control signal, tracks a per-attach connection identifier, and DetachTransport already cancels it and calls the production Disconnect RPC. The TODO predates attach and its conclusion is stale."
    verification: verified-against-source
  - statement: "Telnet is a legal client_type in production: internal/store/session_store.go validates the connection row against exactly {terminal, comms_hub, telnet} and rejects anything else. The only blocker to a telnet column is the hardcoded literal in attach, not a missing production capability."
    verification: verified-against-source
  - statement: "WithInTreePlugins does register the compiled-in handlers as a side effect: the plugin subsystem's Prepare step 12 calls handlers.RegisterAll on its own registry, and the harness adopts that registry. So the choice is not between plugins and no commands; it is between a heavy option that drags in binary plugin artifacts and a narrow option that registers only the compiled-in handlers."
    verification: verified-against-source
  - statement: "There is no administrator-boot command and no administrator-boot RPC anywhere in the tree. The compiled-in registry holds exactly quit and shutdown; the admin registration adds account-management commands, not a boot. The only occurrence of the word is a comment in the core proto. The matrix's administrator-boot row therefore has no production entry point to drive."
    verification: verified-against-source

must_haves:
  truths:
    - "A test can open a session whose connection row records the telnet client type, and that value is read back out of Postgres rather than asserted from the test's own intent."
    - "A test can put a session into the exact state the production reaper selects — detached, with an expiry already past — and observe the real reaper delete it."
    - "A test can drive the compiled-in termination commands through the production dispatcher without dragging in binary plugin artifacts."
    - "Whether an administrator-boot entry point exists at all is settled by evidence and recorded, so the matrix row is dispositioned honestly rather than guessed."
    - "Each seam is proven by its own passing spec before any matrix row is written against it."
  artifacts:
    - "A client-type parameter threaded through the harness attach path, with the telnet session opener implemented rather than fatal"
    - "A detached-past-expiry harness seam whose row state matches the reaper's own selection predicate"
    - "A harness option registering the compiled-in command handlers"
    - "Three smoke specs, one per seam, in the privacy integration suite"
    - "A recorded finding on the administrator-boot entry point"
  key_links:
    - "session client type -> subscribe request -> production subscribe handler -> the connection row's client_type column"
    - "detach-and-backdate seam -> the session store's expired-session query -> the real reaper's sweep -> row deletion"
    - "compiled-in handler registration -> command registry -> dispatcher -> the harness's send-command helper"
  prohibitions:
    - "Never relabel a terminal session as telnet. The value of the telnet column is that the transport differs in production; a renamed constant carrying the same wire value is exactly the assurance theatre this phase exists to eliminate."
    - "Never leave the existing expire helper as the driver for a reaper assertion. It writes a row state the reaper provably never selects, so an assertion built on it passes while proving nothing."
    - "Never invent an administrator-boot entry point to make a matrix row satisfiable. If none exists, that is the finding."
---

<objective>
Build the three QUAL-04 harness seams the session-matrix spec plans assume but the harness does
not have, and prove each one independently before any matrix row is written against it.

Purpose: cross-AI review established that three of the matrix's transition families are written
against harness capabilities that do not exist. The telnet session opener is fatal on call, so
the telnet column — which D-12 calls the highest-value one — has no seam at all. The expiry
helper writes a row state the production reaper provably never selects, so every reaper assertion
built on it would pass while proving nothing. And the default command registry is empty, so the
quit command is not dispatchable. Each would have produced a green spec that verifies nothing,
which is the exact defect this phase exists to eliminate.

Output: three seams, each landing with its own smoke spec, before the matrix registry is written —
so the registry's dispositions can be based on what the harness can actually do rather than on
what a plan assumed it could.

Decisions implemented: D-12 (the telnet column stays in scope; this plan makes it real rather than
re-scoping it away), D-15 (no sleeps anywhere — the reaper seam is driven by explicit row state
and the reaper's own injectable clock).

Addresses review concerns B1 (telnet opener fatal), B2 (the expire helper cannot drive the
reaper), and B3 (the quit command is not dispatchable from harness defaults).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-quality-code-health-sweep/09-07-SUMMARY.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Parameterize attach by client type, implement the telnet session opener, and prove it against the connection row</name>
  <files>internal/testsupport/integrationtest/session.go, test/integration/privacy/privacy_test.go</files>
  <read_first>
    - internal/testsupport/integrationtest/session.go, the Session struct in full — note where the unexported fields begin, after the raw bearer token field; that is where a client-type field belongs
    - internal/testsupport/integrationtest/session.go, `attach` in full — the per-attach connection identifier, the subscribe-request construction with its hardcoded client-type literal, the background subscribe goroutine, and the wait on the replay-complete control signal
    - internal/testsupport/integrationtest/session.go, `DetachTransport` and `ReattachTransport` in full — confirm for yourself that the per-connection transport lifecycle the stale TODO says is missing is in fact already present, because that is the premise of this task
    - internal/testsupport/integrationtest/session.go, `OpenWebSession` in full — the character-selection call, the persisted-row read whose comment explains why the row rather than the player struct is the source of truth, the Session literal, and the attach call
    - internal/testsupport/integrationtest/session.go, the telnet opener's current fatal body and its doc comment — the comment's first half is an accurate specification for this task even though its conclusion is stale
    - internal/store/session_store.go, the client-type allowlist and the error it raises — this is what makes telnet a legal production value rather than a test-only fiction
    - test/integration/privacy/privacy_test.go, the suite's harness start and session setup — the seam the smoke spec attaches to
  </read_first>
  <action>
    Add an unexported client-type field to the Session struct, among the other unexported fields,
    defaulting to the canonical terminal value so no existing behaviour changes. Have attach read
    that field in place of its hardcoded literal, preserving the literal's exact spelling as the
    default so every existing call site stays byte-identical on the wire.

    Extract the shared body of the web session opener — character selection, persisted-row read,
    Session literal, attach — into an unexported helper taking the client type, and have the web
    opener call it with the terminal value. Implement the telnet opener as the same helper called
    with the telnet value. Do not duplicate the body: two copies of the persisted-row sourcing
    logic would drift, and that logic's own comment explains why it is subtle.

    Replace the fatal body entirely. Rewrite its doc comment: keep the accurate first half — that
    web and telnet share the character-selection path and the meaningful production difference is
    the client type recorded on the connection row by the subscribe handler — and replace the
    conclusion. State that the subscribe-goroutine wiring the old comment was waiting on now
    exists in attach, cite attach by name, and state that the differentiation is therefore
    observable by reading the connection row. Remove the retired tracker token rather than
    carrying it forward.

    Update attach's own doc comment, which currently asserts the terminal client type as a fact.
    It becomes caller-selected with a terminal default, and the allowlist it must satisfy is
    enforced by the session store rather than by this helper.

    Then write the smoke spec in the privacy suite. Open a telnet session and a web session, and
    for each read the client type straight out of the connection row in Postgres through the
    harness's pool accessor, asserting one reads telnet and the other reads terminal. Reading the
    column back is the whole point: an assertion phrased against the helper's own argument would
    pass for a session that is telnet in name only, which is the failure this task exists to
    prevent. Assert on the row belonging to the session under test, keyed by session identifier,
    not on a table-wide count.
  </action>
  <verify>
    <automated>set -o pipefail; rg -q 'func \(p \*AuthedPlayer\) OpenTelnetSession' internal/testsupport/integrationtest/session.go &amp;&amp; rg -q 'func \(s \*Session\) attach' internal/testsupport/integrationtest/session.go &amp;&amp; ! rg -q 'TODO iwzt-16' internal/testsupport/integrationtest/session.go &amp;&amp; task test:int -- ./test/integration/privacy/...</automated>
    <note>The two positive controls run first and are load-bearing: a negating `rg` cannot distinguish "no match" from "file unreadable", since ripgrep exits 1 on the former and 2 on the latter and negation accepts both. Proving both symbols are present establishes the file exists and is readable before the absence of the TODO token is trusted. The correctness of the client type itself is deliberately NOT grepped — it is proven by the smoke spec reading the column back out of Postgres, which a grep cannot do.</note>
  </verify>
  <acceptance_criteria>
    - The telnet session opener no longer calls the test-fatal path; `task test:int -- ./test/integration/privacy/...` exits 0 with the new spec passing.
    - The smoke spec reads `client_type` from the `session_connections` row for the specific session identifier under test and asserts it equals the telnet value; a second assertion covers a web session reading the terminal value.
    - No assertion in the spec is phrased against the argument passed to the opener; every claim about the client type comes from a database read.
    - The web session opener and the telnet session opener share one implementation body; `rg -c 'persisted.LocationArrivedAt' internal/testsupport/integrationtest/session.go` returns 1, proving the persisted-row sourcing was not duplicated.
    - The retired tracker token is absent from the file.
    - The rewritten doc comments state that the client type is caller-selected and that the allowlist is enforced by the session store.
    - `task test:int` exits 0 across the whole integration lane — the harness is a shared type and a signature change ripples.
    - `task test` exits 0 and `task lint` exits 0; `task fmt` mutations are committed.
  </acceptance_criteria>
  <done>A telnet session is telnet where production can observe it — in the connection row — and the assertion that proves it reads the column back rather than restating the test's own input.</done>
  <reversibility rating="reversible">An additive field plus a body extraction on a test-support type; revert is a file-level git revert with no external state.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Add a detached-past-expiry seam and prove it drives the real reaper</name>
  <files>internal/testsupport/integrationtest/harness.go, test/integration/privacy/privacy_test.go</files>
  <read_first>
    - internal/testsupport/integrationtest/harness.go, the existing expire helper in full — it sets the status to expired and stamps the expiry; understanding precisely why that row is invisible to the reaper is the premise of this task
    - internal/store/session_store.go, the expired-session query — its predicate requires the detached status AND an expiry already past; a row whose status is expired never matches it
    - internal/session/reaper.go, the sweep that consumes that query — the callback, the status transition, and the delete, in that order
    - internal/session/reaper.go, the reaper config struct — note the injectable clock and the interval, both of which make a sweep observable in milliseconds without sleeping
    - test/integration/presence/lease_reaper_test.go lines 55-135 — the in-repo precedent for driving a real reaper from an integration spec: construct it against the harness's own session store, run it in a goroutine under a bounded context, assert with an eventually-block, and cancel deterministically
    - internal/testsupport/integrationtest/harness.go, the pool and session-store accessors and their doc comments, which say in as many words that they exist so tests can drive reaper-level scenarios end to end
  </read_first>
  <action>
    Add a harness seam that puts a session into the state the reaper actually selects: the
    detached status with an expiry already in the past. Write it beside the existing expire helper
    and take the expiry instant as a parameter so the caller controls it explicitly. Assert one
    row was affected, matching the surrounding helpers' form.

    Do not delete or repurpose the existing expire helper. It has current callers and its
    behaviour — forcing a terminal expired status directly — is legitimate for tests that want the
    end state without a sweep. Instead, amend its doc comment to state plainly that the row it
    writes is NOT selected by the reaper, name the predicate that excludes it, and point to the
    new seam for tests that mean to exercise reaping. A helper whose name suggests it drives
    expiry while it actually bypasses it is how this defect reached three plans.

    Then write the smoke spec in the privacy suite. Drive a session to active, detach its
    transport, apply the new seam with an expiry in the past, construct a real reaper against the
    harness's session store following the presence-suite precedent, run it under a bounded
    context, and assert with an eventually-block that a lookup for that specific session
    identifier returns not-found. Cancel the reaper deterministically and wait for its goroutine,
    as the precedent does, so the spec leaves nothing running.

    Assert absence by a keyed lookup on the session identifier, never by a row count: the suite is
    shared and a concurrently created session would mask a count-based result.

    Use no sleep. The reaper's interval and injectable clock are what make this deterministic;
    that is why they exist.
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/privacy/...</automated>
    <note>The gate is the spec itself, which fails if the seeded row is not reaped. A grep for the new helper's name would prove only that a symbol was added, not that the row it writes is one the reaper selects — and that distinction is the entire content of this task.</note>
  </verify>
  <acceptance_criteria>
    - `task test:int -- ./test/integration/privacy/...` exits 0 with the new spec passing.
    - The spec constructs a real reaper against the harness's session store and runs it; it does not call the reaper's internal sweep directly and does not assert deletion performed by the test itself.
    - The spec asserts not-found on the specific session identifier; `rg -c 'COUNT\(' test/integration/privacy/` does not increase.
    - Seeding the row through the OLD expire helper instead of the new seam makes the spec FAIL — demonstrated once during execution, with the observed failure recorded in the SUMMARY, then reverted. This is the proof that the new seam is load-bearing rather than decorative.
    - The old expire helper's doc comment now states that its row is not selected by the reaper, names the excluding predicate, and points to the new seam.
    - `rg -c 'time\.Sleep' test/integration/privacy/` has not increased.
    - `task test:int` exits 0 across the whole integration lane; `task lint` exits 0.
  </acceptance_criteria>
  <done>A spec can drive the production reaper to delete a session, and swapping in the old helper visibly breaks it — so no later plan can regress to the assertion that proves nothing.</done>
  <reversibility rating="reversible">An additive helper and a doc-comment amendment; no existing call site changes.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Settle how termination is driven, register the compiled-in handlers, and record what has no entry point</name>
  <files>internal/testsupport/integrationtest/harness.go, test/integration/privacy/privacy_test.go</files>
  <read_first>
    - internal/testsupport/integrationtest/harness.go around the dispatcher construction — the comment stating that without the in-tree-plugins option the registry is empty, the registry construction, the reassignment under that option, and the dispatcher build
    - internal/plugin/setup/subsystem.go, the step that creates the command registry and registers the built-in and admin handlers — this is why the in-tree-plugins option already yields a dispatchable quit as a side effect, and why the choice is between a heavy option and a narrow one rather than between commands and none
    - internal/command/handlers/register.go, the register-all function in full — confirm for yourself that the compiled-in set is exactly quit and shutdown
    - internal/command/handlers/register.go, the admin registration function's dependency guards — it panics on missing dependencies, which is why it is not a free addition
    - internal/testsupport/integrationtest/session.go, the send-command helper and the logout helper — the two production entry points available for termination
    - internal/testsupport/integrationtest/harness.go lines 200-255 — the option-type idiom this package uses
  </read_first>
  <action>
    Add a narrow harness option that registers the compiled-in command handlers onto the default
    registry, so a suite can dispatch quit without pulling in binary plugin artifacts. Make it
    interact sanely with the in-tree-plugins option: that option replaces the registry wholesale
    with one that already has the compiled-in handlers, so the narrow option must not
    double-register onto it. Either make the narrow option a no-op when the plugin registry is
    adopted, or register before the adoption and let the adoption win — pick one, implement it,
    and state which in the option's doc comment. A duplicate registration must not be able to
    panic a suite that sets both.

    Record the decision in the option's doc comment: the narrow option exists because the
    lifecycle suite needs exactly two compiled-in commands and the plugin option would require
    built binary plugin artifacts for no benefit to those specs.

    Then establish, by evidence, which termination transitions have a production entry point at
    all. Quit is a compiled-in command reachable through the send-command helper. Logout is not a
    command — it is an RPC, and the harness already has a helper that drives it. For
    administrator boot, search the command registrations, the admin registrations, and the proto
    service definitions. Do not stop at a keyword match in a comment. Record in the SUMMARY, for
    each of the three, the exact entry point a spec would drive, or the finding that none exists.

    If administrator boot has no entry point — which is what the current tree indicates — do NOT
    invent one and do NOT write a spec that approximates it by deleting a row. Record the finding
    with the evidence, file an issue, and state plainly in the SUMMARY that the matrix's
    administrator-boot row must be dispositioned as not-implementable rather than spec-covered.
    The registry plan reads this SUMMARY and will disposition the row from it. Discovering that
    the matrix describes a capability the system does not have is a legitimate and valuable
    outcome; papering over it with an approximating spec is the failure mode.

    Finally, write the smoke spec: open a session, dispatch quit through the send-command helper,
    and assert the command was actually dispatched — that the response is a recognised-command
    outcome, not an unknown-command error. Asserting only that the call returned without error
    would pass against an empty registry, which is precisely the state this task exists to change.
  </action>
  <verify>
    <automated>task test:int -- ./test/integration/privacy/...</automated>
    <note>The gate is the spec, which fails against an empty registry because it asserts a recognised-command outcome rather than a bare no-error. A grep for the option's name would prove a symbol exists, not that a command dispatches.</note>
  </verify>
  <acceptance_criteria>
    - `task test:int -- ./test/integration/privacy/...` exits 0 with the new spec passing.
    - The quit spec asserts a recognised-command outcome; running it without the new option makes it FAIL with an unknown-command outcome — demonstrated once during execution, recorded in the SUMMARY, then reverted.
    - A suite setting both the narrow option and the in-tree-plugins option starts without panicking; the option's doc comment states which registration wins.
    - The SUMMARY names, for each of quit, logout, and administrator boot, the exact production entry point a spec would drive, or records the evidenced finding that none exists — citing the files searched, not a single keyword match.
    - If administrator boot has no entry point, an issue number is recorded and the SUMMARY states explicitly that the matrix row is to be dispositioned not-implementable. No approximating spec was written.
    - `task test:int` exits 0 across the whole integration lane; `task lint` exits 0; `task fmt` mutations are committed.
  </acceptance_criteria>
  <done>Termination is driven through real production entry points where they exist, and where one does not exist that is written down with evidence rather than simulated.</done>
  <reversibility rating="reversible">An additive harness option; the default path is unchanged for every existing suite.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> production session store | The harness writes session and connection rows that production code then reads and acts on |
| harness seam -> assurance claim | A seam that does not reach the production path it names produces green specs that verify nothing |
| client transport identity -> connection row | The client type recorded on a connection is what presence and grid-visibility queries filter on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-20-01 | Repudiation | harness seams that bypass the production path they claim to exercise | high | mitigate | This is the defect the review found in three plans. Every seam here is proven by a spec that fails when the seam is swapped for the bypassing alternative — the reaper spec must break under the old expire helper, the quit spec must break without the registration option, and the telnet assertion must read the column rather than the argument. Each negative demonstration is required and recorded. |
| T-09-20-02 | Spoofing | client type recorded on the connection row | high | mitigate | Presence and grid-visibility queries filter on the connection's client type, so a session mislabelled at attach changes who is visible to whom. The telnet seam threads the value through the production subscribe request and the smoke spec reads it back from the row the production handler wrote. |
| T-09-20-03 | Denial of Service | reaper correctness for detached sessions | medium | mitigate | A reaper that never selects detached-and-expired rows grows the session table without bound. The seam writes exactly the state the reaper's own predicate selects, and the spec drives the real reaper rather than asserting a deletion the test performed. |
| T-09-20-04 | Tampering | test-support helper reaching production code | medium | mitigate | These helpers write session rows directly and must never be importable from production. The package is integration-build-tagged, and plan 09-07 adds it to the dependency-guard deny list so the guarantee is enforced by lint rather than assumed. |
| T-09-20-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed; no dependency manifest touched. |
</threat_model>

<verification>
- `task test:int` exits 0 across the whole integration lane (the harness is a shared type; a signature change ripples beyond the privacy suite).
- `task test` exits 0 and `task lint` exits 0.
- All three negative demonstrations were performed and recorded: the reaper spec fails under the old expire helper, the quit spec fails without the registration option, and the telnet assertion is sourced from a database read rather than the helper argument.
- The administrator-boot entry-point finding is recorded with the files searched.
</verification>

<success_criteria>
The three matrix transition families that had no working seam now have one each, every seam is
proven by a spec that would fail without it, and any transition the system genuinely cannot
perform is recorded as such rather than approximated.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- A client-type field on the harness Session type and an implemented telnet session opener, replacing a test-fatal stub
- A detached-past-expiry harness seam matching the reaper's selection predicate, plus a corrected doc comment on the pre-existing expire helper
- A harness option registering the compiled-in command handlers
- Three smoke specs in `test/integration/privacy/privacy_test.go`, one per seam, each written to fail if its seam is absent
- A recorded, evidenced finding on whether an administrator-boot entry point exists, and possibly one new GitHub issue (number assigned at execution time)
</artifacts_produced>

<output>
Create `.planning/phases/09-test-quality-code-health-sweep/09-20-SUMMARY.md` when done.
Record verbatim: the telnet opener's signature, the new expiry seam's signature, the registration
option's name and its interaction rule with the in-tree-plugins option, and the administrator-boot
entry-point finding. Plans 09-12, 09-13, 09-14, and 09-15 all read this SUMMARY — the registry
plan dispositions the administrator-boot row directly from the finding recorded here.
</output>
