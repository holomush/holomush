---
phase: 09-test-quality-code-health-sweep
plan: 19
type: execute
wave: 10
depends_on: ["09-17"]
files_modified:
  - .codecov.yml
autonomous: false
requirements: [QUAL-02]

must_haves:
  truths:
    - "The measurement chain, the two package floors, and the project target are all verified against the authoritative coverage service rather than against a local tool's output."
    - "The project status becomes a true no-drop ratchet, with the one-percentage-point regression allowance removed."
    - "Every coverage context that plan 09-17 issued a go for is a required check on the protected branch, entered under the exact context string that was observed posting; any context carrying a no-go is absent from the ruleset and recorded with a follow-up issue number."
    - "The operator action is performed by a human with repository administration rights and verified afterward by a query, not assumed."
  artifacts:
    - ".codecov.yml with the project status regression allowance removed"
    - "A recorded verification of the branch's project coverage, the two package floors, and the coverage-service flag for the end-to-end session"
    - "The protected-branch ruleset carrying every go-verdict coverage context as a required check, with any no-go context recorded as a follow-up issue instead"
  key_links:
    - "observed status context strings -> the required-check entries in the protected-branch ruleset"
    - "repaired end-to-end session -> merged project coverage -> the number the no-drop ratchet holds"
  prohibitions:
    - "Never claim a coverage floor was met on the strength of a local tool's output; the gate reads the coverage service's merged number and that is the only figure a floor claim may cite."
---

<objective>
Close the phase's coverage gate: verify every floor against the authoritative number, tighten
the project status to a true no-drop ratchet, and have an operator make both coverage statuses
required on the protected branch.

Purpose: the previous milestone left this gate half-wired — the statuses post but nothing
requires them, which is coverage governance in name only. This plan finishes it. It runs last
because the ratchet must be tightened after the phase's own work has landed, and because the
operator can only be asked to require a status once it has been observed posting, which the
preceding plans established.

Output: verified floors, a no-drop project status, and required checks on the branch.

Decisions implemented: D-02 (the 80% project target, verified against the measured number rather
than a local tool's output), D-04 (both statuses become required, and the zero-tolerance ratchet
is the final plan of the phase), D-05 (a floor is only claimed when a query proves it).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-quality-code-health-sweep/09-01-SUMMARY.md
@.planning/phases/09-test-quality-code-health-sweep/09-08-SUMMARY.md
@.planning/phases/09-test-quality-code-health-sweep/09-10-SUMMARY.md
@.planning/phases/09-test-quality-code-health-sweep/09-17-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify every coverage claim this phase makes against the authoritative number</name>
  <files>(no repo files — verification recorded in the SUMMARY)</files>
  <read_first>
    - .planning/phases/09-test-quality-code-health-sweep/09-01-SUMMARY.md — the repaired end-to-end chain's confirmed root cause and the coverage figure it recorded
    - .planning/phases/09-test-quality-code-health-sweep/09-08-SUMMARY.md and 09-10-SUMMARY.md — the two package floors and any recorded residual
    - .planning/phases/09-test-quality-code-health-sweep/09-17-SUMMARY.md — the verdict on which status contexts post, and the verbatim strings
    - .codecov.yml lines 23-44 — the status blocks as they now stand
  </read_first>
  <action>
    Verify four things against the coverage service's own interface for this branch, after the
    branch's continuous integration has completed. Do not substitute a local coverage tool's
    output for any of them: a local unit-only figure differs from the merged number by more than
    twenty points in this repository, and that discrepancy is the error this whole phase began
    by correcting.

    First, that the end-to-end flag now reports a non-zero coverage figure, and that the commit's
    report shows three upload sessions rather than two. A green end-to-end job is not evidence of
    either — the entire phase started from a green job producing an empty artifact.

    Second, that whole-repository project coverage on this branch is at least eighty percent.

    Third, that the command package reports at least eighty percent, measured with the two wiring
    files no longer excluded. If the earlier plan recorded a residual instead, restate it here
    with its tracking issue and do not overwrite it with a rounder number.

    Fourth, that the certificate package reports at least eighty percent.

    All four figures come from the coverage service's version-2 read interface for this
    repository, rooted at `api.codecov.io/api/v2/github/holomush/repos/holomush`. Three endpoints
    under that root carry everything needed, and all three were confirmed reachable and correctly
    shaped while this plan was written: the flags listing returns one entry per flag with its
    coverage, from which the end-to-end flag's figure is read; the report endpoint returns
    whole-repository totals carrying both the coverage percentage and the upload-session count;
    and the same report endpoint with a path parameter returns the totals scoped to a directory,
    which is how each package floor is read. Pass the branch as a query parameter and let the
    request tool encode it — this milestone's branch name contains a slash.

    Record each figure with the query that produced it. If any floor is not met, say so plainly
    and stop before Task 2. Tightening the ratchet onto an unmet floor would lock in a failing
    state, and fabricating a met floor is the coverage theatre this phase exists to eliminate.
  </action>
  <verify>
    <automated>B=https://api.codecov.io/api/v2/github/holomush/repos/holomush; R="$(git rev-parse --abbrev-ref HEAD)"; curl -fsS -G --data-urlencode "branch=$R" "$B/flags/" | jq -er '[.results[]|select(.flag_name=="e2e").coverage][0]' | awk 'BEGIN{c=1}{c=($1+0&gt;0)?0:1}END{exit c}' &amp;&amp; curl -fsS -G --data-urlencode "branch=$R" "$B/report/" | jq -er '.totals.sessions' | awk 'BEGIN{c=1}{c=($1+0&gt;=3)?0:1}END{exit c}' &amp;&amp; curl -fsS -G --data-urlencode "branch=$R" "$B/report/" | jq -er '.totals.coverage' | awk 'BEGIN{c=1}{c=($1+0&gt;=80)?0:1}END{exit c}' &amp;&amp; curl -fsS -G --data-urlencode "branch=$R" --data-urlencode "path=cmd/holomush" "$B/report/" | jq -er '.totals.coverage' | awk 'BEGIN{c=1}{c=($1+0&gt;=80)?0:1}END{exit c}' &amp;&amp; curl -fsS -G --data-urlencode "branch=$R" --data-urlencode "path=internal/tls" "$B/report/" | jq -er '.totals.coverage' | awk 'BEGIN{c=1}{c=($1+0&gt;=80)?0:1}END{exit c}'</automated>
    <note>Every stage defaults to FAILURE, deliberately. Each `awk` gate initialises its exit code to 1 and only clears it on a line that satisfies the comparison, so an empty stream — which is what a failed `curl -fsS` or a `jq -e` that found nothing produces — exits non-zero rather than passing vacuously. Run against `main` while this plan was written, the chain exits 1 at the first gate: the end-to-end flag reports 0.0. It also fails on every other floor as things stand today (2 sessions not 3, project 78.28%, `cmd/holomush` 64.25%, `internal/tls` 76.23%) — which is precisely the state this phase exists to change, and precisely why a verify that could not fail was unacceptable here.</note>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records four figures, each with the exact query that produced it, and none of them is a local coverage tool's output.
    - The end-to-end flag's reported figure is greater than zero, and the commit report shows three upload sessions.
    - Whole-repository project coverage on this branch is at least 80.0%, or the shortfall is stated with its cause.
    - The command package figure is at least 80.0%, or the previously recorded residual is restated verbatim with its tracking issue number.
    - The certificate package figure is at least 80.0%.
    - If any floor is unmet, the SUMMARY says so and Task 2 does not run.
    - The task's automated gate exits 0. It encodes all five assertions above, and it exits non-zero if any one of them fails or if any query fails to return a figure — judge it by its exit code, never by reading its output.
  </acceptance_criteria>
  <done>Every coverage claim the phase makes is backed by a query against the number the gate itself reads, and any shortfall is stated rather than smoothed.</done>
  <reversibility rating="reversible">Verification only.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Make the project status a true no-drop ratchet</name>
  <files>.codecov.yml</files>
  <read_first>
    - .codecov.yml lines 23-44 — the project status block, its comment as this phase rewrote it, and the patch status block below it
    - .planning/phases/09-test-quality-code-health-sweep/09-01-SUMMARY.md — the baseline figure this phase recorded in that comment, which the tightened wording should stay consistent with
  </read_first>
  <action>
    Set the project status's regression allowance to zero, removing the one-percentage-point
    tolerance. The existing comment already says the tolerance should be tightened once coverage
    stabilises; rewrite it to state that it now is, and that a change lowering whole-repository
    coverage at all fails the status.

    Delete that "tighten once coverage stabilises" sentence outright rather than leaving it
    beside the tightened value. An instruction that has already been carried out reads as one
    still outstanding, and a stale comment left standing next to a changed value is the exact
    failure this phase opened by correcting — a comment nobody updated is how the falsified
    target survived in the first place.

    Do not change the patch status. Its target and tolerance are separately tuned and are not
    part of this decision.

    Do not change the upload-count threshold. The file's own comment records that raising it
    blocks notification entirely, and the tolerance change makes the two-session merge more
    load-bearing, not less.

    Record in the comment that this is a rising-floor ratchet whose floor is now whatever the base
    commit measured, so a future reader understands that the number in the comment is historical
    context rather than the enforced value.
  </action>
  <verify>
    <automated>rg -q '^\s*threshold: 0%' .codecov.yml &amp;&amp; ! rg -q '^\s*threshold: 1%' .codecov.yml &amp;&amp; rg -q '^\s*target: 80%' .codecov.yml &amp;&amp; rg -q '^\s*after_n_builds: 2' .codecov.yml</automated>
    <note>Every pattern is anchored to the start of a key line, because this file discusses its own
    keys in prose. Unanchored, the gate passes vacuously against the UNEDITED file: the comment
    block carries the literal `threshold: 0%` inside the sentence recommending the tightening, and
    `after_n_builds: 2` appears in two comments besides the real key. Anchored, `^\s*threshold: 0%`
    matches nothing today (a comment line is `#`-prefixed after its indent), so the first
    assertion alone fails a skipped task. The second assertion is the discriminator the first
    cannot be: `threshold: 1%` is the value actually being removed, so a run that added the new key
    but left the old one — or a partially-applied edit — fails here rather than passing. The
    remaining two are collateral-damage guards: the patch target must survive untouched, and the
    two-upload merge key must too, since the file's own comment records that the tightened
    tolerance makes that merge more load-bearing, not less. Failing cases, in order: no edit at all
    fails at the first pattern; a missing or unreadable `.codecov.yml` also fails there (rg exits
    non-zero), before the negated pattern could invert an error into a pass; both thresholds
    present fails at the second; a clobbered patch block or merge key fails at the third or fourth.</note>
  </verify>
  <acceptance_criteria>
    - `rg -c '^\s*threshold: 0%' .codecov.yml` returns 1, inside the project status block. Count the anchored form only: the file discusses its own keys in prose, so an unanchored count is satisfied by a comment and would also mis-fire if the rewritten comment restates the phrase.
    - `rg '^\s*threshold: 1%' .codecov.yml` returns no match. The one-percentage-point tolerance is gone, not merely joined by a second key. This is the criterion that distinguishes a tightened ratchet from a task that was never performed.
    - `rg -c '^\s*target: 80%' .codecov.yml` still returns 1 — the patch status is unchanged.
    - `rg -c '^\s*after_n_builds: 2' .codecov.yml` still returns 1 — the upload-count threshold is unchanged. Anchored again: the key appears in two of the file's comments as well, so an unanchored count is 3 today and proves nothing either way.
    - The rewritten comment states that the tolerance is now zero and what that means for a change that lowers coverage, and no longer instructs a future reader to tighten toward zero.
    - `task lint` exits 0.
  </acceptance_criteria>
  <done>Whole-repository coverage can no longer decline; the ratchet is a floor rather than a tolerance.</done>
  <reversibility rating="reversible">A configuration edit, reverted by a file-level revert.</reversibility>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator makes both coverage statuses required on the protected branch</name>
  <files>(no repo files — a GitHub repository-settings change)</files>
  <read_first>
    - .planning/phases/09-test-quality-code-health-sweep/09-17-SUMMARY.md — the go-or-no-go verdict and the verbatim status context strings; the strings entered here MUST be those exact strings, because a required check is matched by string
    - .claude/rules/landing-the-plane.md, the paragraph on required checks that never arrive — the prior incident whose failure mode this checkpoint is guarding against
  </read_first>
  <action>
    HALT and hand control to a human. An operator with repository administration rights must add both coverage status contexts to
    the protected-branch ruleset's required-status-checks rule. This is deliberately not
    automated: it is a repository-settings mutation on the rule that protects the default branch,
    it is outside the authority this phase grants its executors, and undoing it needs the same
    operator round trip.

    Add ONLY the contexts the preceding plan issued a go for. Its verdict is per-context, and a
    no-go for one is an expected outcome — the planner's own live queries found the project
    context posting on no ref at all. Adding a context that carries a no-go, or adding both
    because the plan named both, is the deadlock this checkpoint exists to prevent. If only one
    context carries a go, add that one and record the other's follow-up issue number.

    Do NOT perform this step if this plan's first task found an unmet floor, or if the preceding
    plan issued no go for either context. A status that does not post, made required, blocks every
    pull request in the repository with nothing visibly failing.

    SEQUENCING — this matters and is easy to get wrong. Perform this edit only AFTER the final
    pre-push rebase required by the repository's landing rules, and after the resulting head
    commit's continuous-integration run has completed. GitHub evaluates required checks against
    the pull request's CURRENT head commit, so a rebase or force-push after this edit discards
    the statuses that were just verified and the pull request cannot merge until both re-post on
    the new commit — and if end-to-end coverage flakes once in that window, the pull request is
    stuck behind a check it can no longer produce. Confirm before starting that no further rebase
    is planned:

        git fetch origin && git log --oneline origin/main..HEAD | head -1
        git status --porcelain

    If the branch is behind the mainline, rebase FIRST, re-run `task pr-prep`, push, wait for
    continuous integration to complete, re-confirm both statuses, and only then make this edit.
  </action>
  <instructions>
    1. Read the current ruleset and confirm which checks are required today:
       `gh api repos/holomush/holomush/rulesets/11923801 --jq '[.rules[]|select(.type=="required_status_checks")|.parameters.required_status_checks[].context]'`
       Expect the existing list, with neither coverage context present. Save that list.
    2. Add both coverage contexts to that rule, using the exact strings recorded in the preceding
       plan's summary. This can be done through the repository's rulesets settings page, or by
       reading the ruleset, adding the two entries to the required-status-checks rule's parameter
       array, and writing it back with an authenticated update request. Prefer the settings page
       unless you are comfortable round-tripping the whole ruleset document.
    3. Re-run the query from step 1 and confirm every context you added now appears alongside
       every check that was in the saved list.
    4. Confirm this phase's own pull request still reports every context you added and that each
       passes. If any is stuck pending after continuous integration has completed, remove it again immediately —
       that is the deadlock this checkpoint exists to catch, and it is cheaper to reverse within
       minutes than after other contributors' pull requests have queued behind it.
  </instructions>
  <verification>
    `gh api repos/holomush/holomush/rulesets/11923801 --jq '[.rules[]|select(.type=="required_status_checks")|.parameters.required_status_checks[].context]'`
    returns a list containing every go-verdict coverage context string plus every check present in
    the saved pre-change list, and
    `gh pr view --repo holomush/holomush --json statusCheckRollup` shows each of those contexts
    with a passing conclusion rather than pending. A context the preceding plan verdicted no-go is
    expected to be absent from both.
  </verification>
  <acceptance_criteria>
    - The ruleset query returns a list containing exactly the coverage context strings the preceding plan issued a go for, spelled exactly as it recorded them. A context carrying a no-go was NOT added.
    - The pre-existing required checks are all still present — none was displaced by the edit.
    - This phase's pull request shows each added coverage context with a passing conclusion, not pending, after continuous integration has completed.
    - The edit was made after the final pre-push rebase, and the SUMMARY records that no further rebase was pending when it was made. If a rebase became necessary afterwards, the SUMMARY records that the statuses were re-confirmed on the new head commit.
    - The SUMMARY records the before and after context lists, which contexts were deliberately not added and why, and who performed the change.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the ruleset query returns both contexts and the phase's pull request shows both passing, or describe what you observed instead.</resume-signal>
  <reversibility rating="costly">A repository-settings mutation on the rule protecting the default branch; reversing it needs the same operator round trip, and while it is wrong every pull request in the repository is blocked.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external coverage service -> merge gate | A third-party status becomes a merge-blocking requirement on the default branch |
| operator -> protected-branch ruleset | A settings mutation outside the repository's own review process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-19-01 | Denial of Service | required check that never posts | high | mitigate | The whole repository blocks with nothing visibly failing. The checkpoint is gated on the preceding plan's observed-posting verdict, requires the exact observed strings, and instructs immediate removal if either context is stuck pending. |
| T-09-19-02 | Denial of Service | displacing existing required checks during the edit | high | mitigate | Writing the ruleset back wholesale can drop entries. The operator reads the list before and after and an acceptance criterion requires every pre-existing check to survive. |
| T-09-19-03 | Repudiation | a coverage floor claimed from a local tool's output | high | mitigate | The local unit-only figure differs from the gate's merged number by more than twenty points here, and mistaking one for the other is the error this phase opened by correcting. Every floor claim must cite a query against the coverage service, and an unmet floor stops the plan rather than being smoothed. |
| T-09-19-04 | Tampering | tightening the ratchet onto an unverified state | medium | mitigate | A zero-tolerance ratchet locked onto a failing state is worse than none. Task 2 runs only after Task 1 verifies every floor. |
| T-09-19-05 | Denial of Service | ruleset edit invalidated by a later rebase | high | mitigate | Required checks are evaluated against the pull request's current head commit, and the repository's landing rules mandate a pre-push rebase. An edit made before that rebase leaves the pull request waiting for statuses to re-post on a new commit, and a single end-to-end coverage flake in that window strands it. The checkpoint is sequenced explicitly after the final rebase, instructs the operator to confirm none is pending before editing, and requires re-confirmation on the new head commit if one becomes necessary afterwards. The existing immediate-removal mitigation stays. |
| T-09-19-06 | Denial of Service | adding a context that carries a no-go verdict | high | mitigate | The preceding plan's verdict is per-context and the planner's live queries found the project context posting on no ref. Adding both because the plan names both would require a status that cannot arrive. The checkpoint adds only contexts carrying a go, and an acceptance criterion asserts a no-go context was not added. |
| T-09-19-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed; no dependency manifest touched. |
</threat_model>

<verification>
- Four coverage figures verified against the coverage service, each with its query recorded.
- The project status carries a zero regression allowance; patch status and upload-count threshold unchanged.
- The ruleset query returns every coverage context plan 09-17 issued a go for, plus every pre-existing required check, and does NOT contain a context that carried a no-go.
- This phase's pull request shows every added context passing rather than pending. A context 09-17 recorded as a no-go is absent by design, carries a follow-up issue number, and its absence is not a verification failure — requiring a status that posts on no ref would deadlock every pull request in the repository, which is threat T-09-19-06.
</verification>

<success_criteria>
Coverage governance is complete rather than half-wired: the numbers are verified against the
authoritative source, the project floor cannot decline, and every status observed to post actually
gates merges — with the deadlock failure mode checked for and reversible within minutes. A context
plan 09-17 verdicted no-go is deliberately left out and carried as a follow-up issue; that is a
successful outcome of this plan's design, not a shortfall against it. A partial gate that works is
worth more than a complete gate that blocks every pull request in the repository.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- `.codecov.yml` project status regression allowance set to zero
- Protected-branch ruleset `11923801` gains two required status-check contexts (exact strings recorded by plan 09-17)
- A verification record in the SUMMARY covering the end-to-end flag figure, the upload-session count, whole-repository project coverage, and both package floors
</artifacts_produced>

<output>
Create `.planning/phases/09-test-quality-code-health-sweep/09-19-SUMMARY.md` when done.
Record all four verified figures with their queries, the before-and-after ruleset context lists,
and who performed the operator action.
</output>
