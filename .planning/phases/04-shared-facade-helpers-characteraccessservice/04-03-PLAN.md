---
phase: 04-shared-facade-helpers-characteraccessservice
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-portal-spec/01-SPEC.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [IDENT-02]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "01-SPEC section 9.3's mutation table carries no `CharacterAccessService.RenameCharacter` row, and no prose anywhere in 01-SPEC refers to that row."
    - "01-SPEC section 9.4.2 still explains what guards a create — the unique index on the stored normalized name — without referring to a row that no longer exists."
    - "The three ROADMAP sketch findings for Phase 4 each carry a recorded verdict, and none of them causes code to be written in this phase."
    - "The ROADMAP's Phase-4 plan-count line agrees with the plan list it sits above: nine plans, not eight (cycle-6 L4) — corrected as a value-fill of the existing `**Plans**:` shape, never by inventing structure."
  artifacts:
    - .planning/phases/01-portal-spec/01-SPEC.md
  key_links:
    - "01-SPEC section 9.3 mutation table -> 01-SPEC section 9.4.2 create-exclusion prose (the dangling reference)"
    - "ROADMAP Phase 4 sketch-findings paragraph -> 04-CONTEXT.md D-81 verdicts"
  prohibitions:
    - "Never add a version-bearing or check-mark-bearing `###` heading to a tool-owned planning artifact — the active-milestone extractor ends its section at the next such heading and silently drops phases."
---

<objective>
Apply this phase's amendment pass to 01-SPEC per D-74: strike
`CharacterAccessService.RenameCharacter` from the section 9.3 mutation table and
repair the two prose sentences that explain creation's concurrency guard by
reference to it, and record the three ROADMAP sketch-finding verdicts.

Purpose: `RenameCharacter` left the milestone on 2026-08-06 (Phase 3 D-44,
backlog 999.20). D-72 says the proto declares only the RPCs Phase 4 implements,
so the SPEC and the proto must agree before plan 04-08's descriptor census
compares them. Leaving the row in place makes the census expect a member that
will never exist.

Output: an amended 01-SPEC with no dangling reference, and the three sketch
verdicts recorded where the ROADMAP asked for them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strike RenameCharacter from 01-SPEC section 9.3 and repair its two dangling references</name>

  <files>.planning/phases/01-portal-spec/01-SPEC.md</files>

  <read_first>
- .planning/phases/01-portal-spec/01-SPEC.md lines 2006 through 2140 — section 9.3's opening paragraph, the mutation table, and section 9.4.2 in full, so both dangling sentences are read in their surrounding argument rather than patched blind
- .planning/phases/03-world-character-commands/03-CONTEXT.md — D-44, the decision that removed rename from v0.13 and the rationale to transcribe
- .planning/REQUIREMENTS.md — the IDENT-03 entry, which already records the deferral to backlog Phase 999.20 linked to 999.6
- .claude/rules/planning-artifacts.md — the rule governing edits to tool-owned generated files
  </read_first>

  <action>
D-74 strikes `RenameCharacter` from section 9.3 and requires section 9.4.2's
prose to be repaired in the same edit, because that prose explains creation's
concurrency guard by reference to the row being struck.

Make three narrowly-scoped `Edit` replacements in
`.planning/phases/01-portal-spec/01-SPEC.md`. Use the `Edit` tool with exact
string matches. Do **not** rewrite the file, do **not** restructure any section,
and do **not** add a heading of any level — a new version-bearing or
check-mark-bearing `###` heading truncates the active-milestone scope.

**Edit 1 — the section 9.3 table row (line 2021).** Delete the whole
`| CharacterAccessService.RenameCharacter | owner | owner | ABAC write on character:<id> | ... |`
row. Nothing replaces it; the table simply has one fewer row.

**Edit 2 — the section 9.3 opening paragraph (line 2013).** The sentence
currently reads that what guards a create is section 6.1.3's unique index on the
stored normalized name, "the same index the `RenameCharacter` row below collides
against." Replace the trailing clause so the sentence names the index without
referring to a table row that no longer exists, and states that rename left
v0.13 on 2026-08-06 to backlog Phase 999.20 per Phase 3 D-44. Keep the sentence's
argument intact: the index is still what decides two concurrent creates.

**Edit 3 — the section 9.4.2 prose (line 2116).** The sentence currently reads
"That is the same index the `RenameCharacter` row collides against. Creation's
concurrency safety is therefore already specified, in the section that specifies
it for rename; it is not missing…". Replace it so the paragraph keeps its
conclusion — creation's concurrency safety is already specified by section 6.1.3,
it is not missing, and Phase 4 must not invent a version-shaped substitute for it
— while sourcing that specification to section 6.1.3 directly rather than to the
struck row.

**Measure the heading counts BEFORE editing** and record them in the SUMMARY.
At plan time 01-SPEC carries 116 heading lines total (`rg -c '^#{1,6} '`), of
which 16 are `^## ` and 81 are `^### `. Every one of those three numbers MUST be
identical after the edit — that is the direct measurement that proves no heading
was added, and unlike a `git diff` scan it stays evidence-bearing after the
executor commits. If a pre-edit measurement differs from the pinned number, use
the measured pre-edit value as the expected value and note the drift in the
SUMMARY; the invariant is "unchanged by this edit", not the literal.

After the edits, confirm no occurrence of `RenameCharacter` remains anywhere in
01-SPEC. Note while doing so that section 9.3's table carries no
`AdminRenameCharacter` row at HEAD, so D-81's withdrawal of the admin rename
census requires no SPEC edit — record that observation in the SUMMARY rather than
inventing a row to delete.
  </action>

  <verify>
    <automated>test "$(rg -c 'RenameCharacter' .planning/phases/01-portal-spec/01-SPEC.md || echo 0)" = "0" &amp;&amp; test "$(rg -c '^#{1,6} ' .planning/phases/01-portal-spec/01-SPEC.md)" = "116" &amp;&amp; test "$(rg -c '^### ' .planning/phases/01-portal-spec/01-SPEC.md)" = "81"</automated>
  </verify>

  <acceptance_criteria>
- `rg -c 'RenameCharacter' .planning/phases/01-portal-spec/01-SPEC.md` reports no matches (exit status 1).
- `rg -n '6\.1\.3' .planning/phases/01-portal-spec/01-SPEC.md` still matches inside both section 9.3's opening paragraph and section 9.4.2 — the guard's source survives the edit.
- `git diff --stat -- .planning/phases/01-portal-spec/01-SPEC.md` shows a net change of at most 8 lines.
- `rg -c '^#{1,6} ' .planning/phases/01-portal-spec/01-SPEC.md` returns 116 — the total heading count is unchanged, so no heading of any level was added. This is a direct measurement of the file and stays evidence-bearing after the commit, which a `git diff` scan for added heading lines does not.
- `rg -c '^## ' ...` returns 16 and `rg -c '^### ' ...` returns 81 for the same file — the two levels the milestone extractor keys off are individually unchanged. Record the pre-edit and post-edit numbers for all three counts in the SUMMARY.
- The SUMMARY records that no `AdminRenameCharacter` row existed at HEAD.
  </acceptance_criteria>

  <done>
01-SPEC carries no reference to `RenameCharacter`; sections 9.3 and 9.4.2 each
still explain what guards a create; no heading was added or removed.
  </done>

  <reversibility rating="costly">
01-SPEC is normative for three remaining phases and is read by the plan checker
and the verifier. Restoring the row later means re-deriving the prose that
depended on it; the deferral itself is already locked by Phase 3 D-44 and by
REQUIREMENTS' IDENT-03 entry, so this edit follows a settled decision rather than
making one.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Record the three Phase-4 sketch-finding verdicts</name>

  <files>.planning/ROADMAP.md</files>

  <read_first>
- .planning/ROADMAP.md — the Phase 4 section, specifically the paragraph beginning `**Sketch findings** (must be answered in this phase)`
- .planning/phases/04-shared-facade-helpers-characteraccessservice/04-CONTEXT.md — D-81 in full, carrying all three verdicts and their rationales
- .claude/rules/planning-artifacts.md — the prohibition on inventing structure in a tool-owned artifact
  </read_first>

  <action>
D-81 answers all three sketch findings here and builds none of them: all three
are admin surface and D-72 defers the admin RPCs to Phase 6.

Append the three verdicts to the **existing** `**Sketch findings**` paragraph in
the ROADMAP's Phase 4 section with a single narrowly-scoped `Edit`. Fill in
values in the shape that is already there — do not add a heading, a table, a
list, or any new section, and do not touch any other phase's entry.

The three verdicts to record:
  - **Admin rename census — WITHDRAWN.** Phase 3 D-44 removed rename from v0.13, so sketch 004's rename affordance is not live in v0.13 and sketch 009 finding 5 ("names are reserved, not permanent") is false for v0.13. Answered 2026-08-10 by D-81; no code.
  - **A3, `AdminSearchCharacters` extending to player usernames — ACCEPTED as the design, implemented in Phase 6.** Answered here; no Phase-4 code.
  - **A2's RPC half, the admin list RPC accepting a sort key for the joined `players.username` — ACCEPTED as the design, implemented in Phase 6.** Section 11.3's row was already amended in Phase 2 by D-26.

**Correct the Phase-4 plan count in the same file (cycle-6 L4).** The Phase-4
block's count line reads `**Plans**: 8 plans` (at plan time, ROADMAP.md:484)
while the plan list beneath it enumerates nine (04-01 through 04-09). ROADMAP.md
is a GSD-tool-owned artifact, so this fix is a VALUE-FILL of an existing shape,
never a structural edit: if the GSD roadmap tooling exposes a verb that updates
the plan count, use it; otherwise change the value on the existing
`**Plans**:` line in place — `8 plans` becomes `9 plans` — with the same
narrowly-scoped `Edit` discipline as the verdicts. Change the number only. Do
not add a heading, a list, a table, or any new shape, and touch no other
phase's count line (the `8 plans` string is unique to the Phase-4 block at plan
time; confirm before editing).

**Measure the heading counts BEFORE editing** and record them in the SUMMARY. At
plan time the ROADMAP carries 36 heading lines total (`rg -c '^#{1,6} '`), of
which 28 are `^### ` and 27 are `^### Phase `. All three MUST be identical after
the edit — that is the direct measurement proving no heading was added, and
unlike a `git diff` scan it stays evidence-bearing after the executor commits.
If a pre-edit measurement differs from the pinned number, use the measured
pre-edit value and note the drift in the SUMMARY.

Record the same three verdicts in this plan's SUMMARY so the phase's answer
survives independently of the ROADMAP text.
  </action>

  <verify>
    <automated>rg -q 'WITHDRAWN' .planning/ROADMAP.md &amp;&amp; rg -q 'AdminSearchCharacters' .planning/ROADMAP.md &amp;&amp; test "$(rg -c 'ACCEPTED' .planning/ROADMAP.md)" -ge 2 &amp;&amp; test "$(rg -c '^#{1,6} ' .planning/ROADMAP.md)" = "36" &amp;&amp; test "$(rg -c '^### Phase ' .planning/ROADMAP.md)" = "27" &amp;&amp; rg -q '\*\*Plans\*\*: 9 plans' .planning/ROADMAP.md &amp;&amp; ! rg -q '\*\*Plans\*\*: 8 plans' .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
- `rg -n 'WITHDRAWN' .planning/ROADMAP.md` matches inside the Phase 4 section.
- `rg -n 'Phase 6' .planning/ROADMAP.md` matches within the amended sketch-findings paragraph for both A2 and A3.
- `rg -c '^#{1,6} ' .planning/ROADMAP.md` returns 36 — the total heading count is unchanged, so no heading of any level was added. This is a direct measurement of the file and stays evidence-bearing after the commit, which a `git diff` scan for added heading lines does not.
- `rg -c '^### Phase ' .planning/ROADMAP.md` returns 27 and `rg -c '^### ' .planning/ROADMAP.md` returns 28 — no version-bearing or check-mark-bearing `###` heading joined the set the milestone extractor scans. Record the pre-edit and post-edit numbers for all three counts in the SUMMARY.
- The Phase-4 count line reads `**Plans**: 9 plans` and no `**Plans**: 8 plans` line remains anywhere in the ROADMAP (cycle-6 L4) — the value was filled in the existing shape; no heading, list, or section was added for it, and the executed-phase count lines (`6/6`, `13/13`) are untouched.
- `git diff --stat -- .planning/ROADMAP.md` touches only lines inside the Phase 4 section.
- No file under `internal/`, `api/`, `cmd/`, `pkg/` or `web/` is modified by this plan.
  </acceptance_criteria>

  <done>
All three sketch findings carry a recorded verdict in the ROADMAP and in the
SUMMARY, the Phase-4 plan count reads 9 to match its enumerated plan list, and
no code was written for any of them.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/agent → tool-owned planning artifact | A structural edit here is invisible to the tool that parses it and silently changes what later commands see |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Tampering | `.planning/ROADMAP.md` milestone scope | medium | mitigate | Narrowly-scoped `Edit` only; heading counts asserted identical before and after; no version-bearing or check-mark-bearing `###` heading added |
| T-04-14 | Repudiation | 01-SPEC normative text | medium | mitigate | The deferral is sourced to Phase 3 D-44 and REQUIREMENTS' IDENT-03 entry inside the amended prose, so a later reader can trace why the row left |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no external packages; no install task exists |
</threat_model>

<verification>
- `rg -c 'RenameCharacter' .planning/phases/01-portal-spec/01-SPEC.md` finds nothing
- Heading counts unchanged in both artifacts
- `git diff --stat` shows only the two planning artifacts
- No source tree file modified
</verification>

<success_criteria>
01-SPEC's mutation surface matches what Phase 4 will actually declare in the
proto, with no dangling reference left behind; the three sketch findings are
answered and built nowhere.
</success_criteria>

## Artifacts this phase produces

This plan creates no new symbols. It amends two existing planning artifacts:
`.planning/phases/01-portal-spec/01-SPEC.md` (section 9.3 table row removed;
sections 9.3 and 9.4.2 prose repaired) and `.planning/ROADMAP.md` (Phase 4
sketch-findings paragraph gains three verdicts; the Phase-4 `**Plans**:` count
line is value-filled from 8 to 9 — cycle-6 L4).

<output>
Create `.planning/phases/04-shared-facade-helpers-characteraccessservice/04-03-SUMMARY.md` when done
</output>
