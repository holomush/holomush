---
phase: 04-shared-facade-helpers-characteraccessservice
plan: 08
type: execute
wave: 6
depends_on: ["04-02", "04-05", "04-06", "04-07"]
files_modified:
  - test/meta/meta_helpers_test.go
  - test/meta/characteraccess_routing_census_test.go
  - test/meta/character_rpc_census_test.go
autonomous: true
requirements: [IDENT-02, PROFILE-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A census compared by set equality proves every owner-audience facade RPC routes through the single shared guest gate, and every owner-audience facade RPC that names a character id routes through the single shared ownership resolution."
    - "The routing census's web half carries its own non-vacuous predicate: each owner-audience proxy forwards the session token header and calls its paired facade method by name."
    - "A new owner-audience RPC added later that skips the gate fails the census rather than shipping, and a census entry whose RPC no longer exists fails as a stale entry."
    - "The three expected sets are enumerated by name in the census file — four guest-gate members, three ownership members, four web-proxy members — so a conflict between what a handler does and what the census expects is visible in review rather than only at execution."
    - "The ownership predicate accepts exactly two call shapes: the shared ownedCharacter resolution itself, and one checked-in wrapper name whose own body is separately asserted to reach that resolution. Both of Phase 4's write RPCs (04-06, D-73) are therefore INSIDE criterion 1's set-equality proof rather than dropped from it, and a future indirection under any other name fails the census rather than widening it."
    - "A second census derives, from the generated service descriptors, the set of RPCs whose response transitively contains a character-shaped message, keyed on the fully-qualified proto method name compared as an exact string, and asserts set equality against the checked-in inventory."
    - "Both censuses report failures as a symmetric-difference diff naming which member is extra and which is missing — not as a loop over the expected entries."
    - "Both censuses carry an anti-vacuity guard that fails if the discovered universe is empty, so a layout change cannot silently reduce either census to a trivially passing comparison."
    - "The routing census walks every non-test Go file in internal/grpc rather than one file, so relocating a handler into a sibling file cannot carry it out of the census universe."
    - "Neither census carries an exemption list for a Phase-4 RPC: every declared RPC has a live handler, so the erosion vector is closed by construction."
  artifacts:
    - test/meta/characteraccess_routing_census_test.go
    - test/meta/character_rpc_census_test.go
  key_links:
    - "test/meta/characteraccess_routing_census_test.go -> internal/grpc facade method bodies -> playerGate selectors"
    - "test/meta/characteraccess_routing_census_test.go -> internal/web proxy method bodies -> session-token header identifier + paired facade method name"
    - "test/meta/characteraccess_routing_census_test.go -> ownershipWrapperMethods (one member) -> (*CharacterAccessServer).ownedCharacterForMutation (04-06) -> the embedded playerGate.ownedCharacter (04-02) — the wrapper is named, and its inner call is asserted, not assumed"
    - "test/meta/character_rpc_census_test.go -> protoregistry.GlobalFiles service descriptors -> the checked-in read-surface inventory"
  prohibitions:
    - "Never add an exemption entry to make a census green: an unexpected member is a finding, and the correct response is to add the row or fix the handler."
    - "Never replace a set-equality assertion with a loop over the expected entries — that shape cannot detect a member it was never told about."
    - "Never relax the ownership predicate into following an arbitrary call one hop. The accepted indirection is a closed, checked-in set of one verified name; a general relaxation would let ANY private helper satisfy criterion 1, including one that reaches a repository directly, which is the precise property criterion 1 exists to buy."
---

<objective>
Ship the two censuses this phase is on the hook for: the routing census that
proves the guest gate and the ownership check exist in exactly one place and that
every owner-audience RPC reaches them, and the descriptor-derived census that
binds the character-returning RPC set to the checked-in read-surface inventory.

Purpose: criterion 1 and the second census 01-SPEC section 3.4 assigns to Phase 4.
01-SPEC section 2.6 names the descriptor census the sole mandated enforcement
gate for the audience-and-message-shape section, and PORTAL-10 rule 1 binds set
equality on every phase in this milestone. A per-endpoint suite is structurally
incapable of detecting a missing member of its own set; only a census is.

Output: two meta-tests, the shared helpers they need, and the checked-in
inventory the second one compares against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-CONTEXT.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-RESEARCH.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-PATTERNS.md
@.planning/phases/01-portal-spec/01-SPEC.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared census helpers — facade receiver predicate, identifier predicate, symmetric-difference diff</name>

  <files>test/meta/meta_helpers_test.go</files>

  <read_first>
- test/meta/meta_helpers_test.go — the existing shared-helper home and `findRepoRoot`, which walks up to the module file
- test/meta/world_envelope_census_test.go — `serviceReceiverName`, which hard-codes a single receiver type name; `bodyReferencesSelector`; and the two directional per-element assertions that stand in for a diff today
- test/meta/world_caller_census_test.go — its header explaining why it parses every non-test Go file in a directory rather than one file, and the exemption-list idiom with its one-commented-member-per-justifying-decision rule
- test/meta/grpc_api_coverage_test.go — the anti-vacuity guard asserting the discovered service set is not empty
- .planning/phases/01-portal-spec/01-SPEC.md section 2.6 — the mandated symmetric-difference diff on failure
  </read_first>

  <behavior>
    - Test: the facade receiver predicate accepts a method on a pointer receiver whose type name is in the supplied set and returns the receiver variable name; it rejects a method on a type outside the set, a receiver-less function, and a receiver with no variable name.
    - Test: the identifier predicate finds a bare identifier used as a call argument inside a function body and does not match a same-named field selector on some other expression.
    - Test: the symmetric-difference helper given two equal sets reports no difference; given sets that differ in each direction it reports the extra members and the missing members separately, and its rendered message names both groups.
  </behavior>

  <action>
Add three helpers to `test/meta/meta_helpers_test.go`, each with a doc comment
naming the census that consumes it.

**A facade-aware receiver predicate.** Generalize the envelope census's receiver
filter, which hard-codes one type name, into a function taking a set of accepted
receiver type names and returning the receiver variable name plus an ok flag. It
unwraps a pointer receiver, requires the receiver to carry a variable name, and
returns not-ok otherwise. Leave the original single-purpose function in
`world_envelope_census_test.go` untouched — this is an addition, not a
refactor of a shipped census.

**An identifier predicate.** The existing body predicate matches a named selector
on a named receiver, which is exactly right for the facade half of the routing
census and useless for the web half: a proxy references the session-token header
constant as a bare identifier, not as a selector on its own receiver. Add a
predicate that reports whether a function body references a given bare
identifier, matching an identifier node and deliberately not matching a
same-named field selector on an unrelated expression.

**A symmetric-difference diff.** Add a helper that takes a derived set and an
expected set of strings and returns the two difference sets — members present in
the derived set but not the expected one, and members present in the expected set
but not the derived one — plus a rendered message naming both groups with stable
ordering. 01-SPEC section 2.6 mandates a set-equality assertion producing a
symmetric-difference diff on failure; the shipped precedent's semantics are set
equality but its failure rendering is per-element, so this is a strict
improvement on the precedent rather than a new mechanism.

Write the helper tests in the same file or a sibling test file so each helper
fails if its own contract breaks.
  </action>

  <verify>
    <automated>task test -- ./test/meta/</automated>
  </verify>

  <acceptance_criteria>
- `task test -- ./test/meta/` is green, including the three helper behaviors above.
- **The shipped censuses are untouched — this task is additive, not a retrofit.** Source-state, deliberately not a diff: neither file is in this plan's `files_modified`, so a diff- or status-shaped check is empty before the change and empty again after any commit and proves nothing either way (the vacuity `04-02:131`/`:134` diagnose). Check instead that `test/meta/world_envelope_census_test.go` still declares its own single-purpose `serviceReceiverName` (at HEAD, `:145`) and `bodyReferencesSelector` (`:162`) — `rg -o 'func (serviceReceiverName|bodyReferencesSelector)\(' test/meta/world_envelope_census_test.go | wc -l` returns 2 — and that neither shipped census calls any helper this task adds: `rg -l '<each new helper name>' test/meta/` matches only `meta_helpers_test.go` and the two census files plans 04-08 Tasks 2 and 3 create.
- The diff helper's rendered message contains a distinct label for the extra group and for the missing group; the helper test asserts both labels appear.
- The diff helper sorts each group before rendering, and its test asserts the rendered output is identical for two calls with the same inputs supplied in different insertion orders.
- The identifier predicate's test includes a negative case where a struct field of the same name is accessed on an unrelated expression and asserts no match.
  </acceptance_criteria>

  <done>
The three helpers the two censuses need exist, are tested, and the shipped
censuses are unchanged.
  </done>

  <reversibility rating="reversible">
Test-only helpers in an existing shared-helper file. No production code, no wire
contract, no persisted state.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The routing census — owner-audience RPCs reach the single shared gate</name>

  <files>test/meta/characteraccess_routing_census_test.go (new)</files>

  <read_first>
- internal/grpc/player_gate.go — the two gate method names the facade half greps for
- internal/grpc/characteraccess_owner.go and internal/grpc/characteraccess_write.go — the four owner-audience handlers
- .planning/phases/04-shared-facade-helpers-characteraccessservice/04-06-PLAN.md — Task 1's `ownedCharacterForMutation` paragraphs in full: why the MUTATION surface needs a mapping layer over the shared gate (the 04-02 matrix rewrites `NotFound` into `CHARACTER_NOT_OWNED`/`PermissionDenied` there and nowhere else), and its explicit statement that this census must know that one wrapper name
- test/meta/world_envelope_census_test.go `:162` — `bodyReferencesSelector`, the SHIPPED selector predicate. This census REUSES it for both handler membership and the wrapper-integrity assertion: same `meta` package, so call it directly. Do not copy it, do not re-implement it, and do not edit its file. Read `:187-207` too, for how that census renders a two-directional set comparison — the shape Task 1's diff helper improves on.
- internal/grpc/characteraccess_service.go and internal/grpc/characteraccess_directory.go — the two public-audience handlers, which must NOT be census members
- internal/grpc/sceneaccess_service.go — the second facade embedding the same gate
- internal/web/character_handlers.go — the proxies, and how each reads the session-token header and names its paired facade method
- test/meta/world_caller_census_test.go — the directory-walk collector shape
- test/meta/meta_helpers_test.go — the three helpers from Task 1
- .planning/phases/04-shared-facade-helpers-characteraccessservice/04-CONTEXT.md — D-73 in full, including why the public-audience RPCs are deliberately outside this census's universe
  </read_first>

  <behavior>
    - Test: the derived set of owner-audience facade methods that reference the guest gate on their own receiver equals the checked-in expected set exactly, in both directions.
    - Test: the derived set of owner-audience facade methods that name a character id and reference the ownership resolution — directly, or through the one checked-in wrapper name — equals its checked-in expected set exactly, in both directions.
    - Test (wrapper integrity, runs BEFORE that comparison): the single member of `ownershipWrapperMethods` is declared exactly once across the walked facade files, on an accepted receiver type, and its OWN body references `ownedCharacter` as a selector on its own receiver. A member with no declaration, two declarations, or a body that no longer reaches the shared resolution fails by name.
    - Test: both mutation handlers are members of the ownership set even though neither names the shared resolution directly — they reach it through the verified wrapper, and the phase's two write RPCs are inside criterion 1's proof rather than outside it.
    - Test: the derived set of owner-audience web proxies that both reference the session-token header identifier and name their paired facade method equals its checked-in expected set exactly, in both directions.
    - Test (anti-vacuity): the walk over the facade directory discovers a non-empty set of exported methods on the accepted receiver types, and the walk over the web directory discovers a non-empty set of proxy methods; each is asserted non-empty before any comparison runs.
    - Test: the two public-audience facade RPCs are absent from all three expected sets, and the census asserts that absence explicitly with a comment naming why.
  </behavior>

  <action>
Create `test/meta/characteraccess_routing_census_test.go`.

**Universe.** The census is over the **owner**-audience RPCs, both halves of each
proxy pair (D-73). Criterion 1's literal phrasing — every character-returning RPC
— cannot hold: the directory and the public profile serve anonymous viewers by
design, and the guest gate rejects guests outright, so routing them through it
would break the public profile. The owner audience is exactly the set where the
contract is that the caller must be a non-guest player who owns this character.
Public reads are gated by the reachability floor and the per-attribute floors and
are asserted by their own tests. State that reasoning in the file header.

**Facade half.** Walk **every** non-test Go file in `internal/grpc` — not one
file — using the directory-walk shape the caller census uses, for the reason its
own header gives: relocating a handler into a sibling file must not carry it out
of the universe. For each exported method whose receiver type is in the accepted
facade-type set, use the identifier and selector predicates to decide membership:
a method routes through the gate when its body references the guest-gate method
name as a selector on its own receiver, and through ownership when it likewise
references the ownership method name. Because the gate is reached by method
promotion from the embedded struct, both call shapes are a selector on the
method's own receiver — which is precisely why D-78 chose the embedded-struct
shape.

**The ownership predicate must know about the mutation wrapper — a closed set of
exactly one name (addresses review finding HIGH-1).** 04-06 routes both mutation
handlers through an unexported mapping helper,
`(*CharacterAccessServer).ownedCharacterForMutation`, which calls the embedded
`s.ownedCharacter` and rewrites its `NotFound` into
`CHARACTER_NOT_OWNED`/`PermissionDenied` per the 04-02 matrix — the MUTATION
surface's row 3, which the owner READ surfaces deliberately do not take. Those two
handler bodies therefore reference `s.ownedCharacterForMutation` and never
`s.ownedCharacter`, and the selector predicate above inspects a method's OWN body
without following the call — the same structural limit
`world_envelope_census_test.go:162` has and `04-09:104` records. A predicate that
knows only the base name has exactly two outcomes and both are bad: permanently RED
against correct code, or green only by dropping the phase's two write RPCs — its
highest-risk surface — out of criterion 1's proof.

Teach it the wrapper, and exactly the one wrapper, in two parts:

  1. **A checked-in one-member set named `ownershipWrapperMethods`,** containing
     exactly the string `"ownedCharacterForMutation"`. Declare it as a literal in
     this file with a comment naming 04-06 as the plan that introduces the method
     and 04-02 as the error matrix it applies. Name it exactly that — see the
     naming constraint in this task's acceptance criteria.
  2. **A wrapper-integrity assertion that runs BEFORE the ownership comparison.**
     For each member, find its `*ast.FuncDecl` among the walked facade files,
     require it is declared exactly once and on an accepted receiver type, and
     require its own body references `ownedCharacter` as a selector on its own
     receiver, using the shipped `bodyReferencesSelector`. A member that is
     missing, doubled, or no longer reaching the shared resolution fails here, by
     name, before any set comparison runs.

Membership then reads: an exported owner-audience handler is ownership-routed when
its body references, as a selector on its own receiver, EITHER `ownedCharacter` OR
a member of `ownershipWrapperMethods`.

**This needs no fourth shared helper and no edit to Task 1.** The assertion is the
facade-aware receiver predicate Task 1 adds, plus a `fn.Name.Name` match against
the one-member set, plus the SHIPPED `bodyReferencesSelector` — the same
`meta` package, so a direct call. Task 1's contract (three helpers, shipped
censuses untouched) is unchanged by this task, and
`test/meta/world_envelope_census_test.go` stays out of every `files_modified` in
this phase. This also does not touch Task 3's descriptor census, which keys on
fully-qualified proto method names from the global registry and never reads a Go
handler body: the two censuses share Task 1's diff helper and nothing else.

**Why this leaves criterion 1's property intact — state this reasoning in the file
header, not only here.** The property criterion 1 buys is "an RPC added later that
bypasses the gate fails this test rather than shipping". A general
follow-any-call-one-hop relaxation would forfeit it outright: every private helper
would then satisfy the predicate, and a handler could reach a repository directly
through one. This does not, on three independent counts.

  - **(i) Closed, not open.** The accepted second shape is a literal set of one
    name checked into this file. A future handler routing through some new
    `ownedCharacterForX` is a non-member and the census goes RED; adding that name
    is a visible, reviewable edit to this file, not a silent widening.
  - **(ii) Verified, not trusted.** Part 2 proves the one accepted wrapper itself
    reaches `s.ownedCharacter`. The obvious attack — gut the wrapper and stay green
    because both handlers still reference it — fails at the integrity assertion.
  - **(iii) Additive on the derived side only.** The set adds an accepted call
    shape to the DERIVED set; it never removes a member from the EXPECTED set. It
    therefore cannot make an ungated handler pass — an ungated handler references
    neither name and lands in the missing group exactly as before.

**Enumerate the three expected sets by name — no plan named them until now, which
is how the conflict above stayed invisible.** Check each in as a literal with its
audience reasoning in a neighbouring comment:

  - **guest gate (`resolveAndGate`) — four members:** `ListMyCharacters` and
    `GetMyCharacter` (04-05), `UpdateCharacterProfile` and
    `UpdateCharacterDescription` (04-06).
  - **ownership (`ownedCharacter`, or a verified wrapper) — three members:**
    `GetMyCharacter`, `UpdateCharacterProfile`, `UpdateCharacterDescription`.
    `ListMyCharacters` is deliberately NOT a member: it names no character id and
    resolves the caller's own list, so there is nothing to check ownership of. This
    second set is scoped to the handlers that take a character id, exactly as the
    behavior above says. Put that sentence in the comment beside the set so its
    absence reads as a decision rather than an oversight.
  - **web proxies — four members:** `WebListMyCharacters`, `WebGetMyCharacter`,
    `WebUpdateCharacterProfile`, `WebUpdateCharacterDescription` (04-04).

The two public-audience facade RPCs — `GetCharacterProfile` and
`ListCharacterDirectory` — belong to none of the three, for the D-73 reason the
file header already gives.

**Web half.** The proxies call neither gate method: they forward a token and
compute nothing, so the facade predicate would report every one of them as a
non-member and the census's proxy half would assert nothing at all. Give it its
own predicate: an owner-audience proxy is a member when its body references the
session-token header identifier **and** references its paired facade method name
as a selector. Both conjuncts matter — the first proves the token is read from
the header rather than the body, the second proves the proxy reaches the gated
facade RPC rather than some other surface.

**Comparison.** Each of the three derived sets is compared against a checked-in
expected set by set equality, failing with the symmetric-difference diff from
Task 1. Do not write a loop over the expected entries: that shape iterates the
expected set, so an unexpected member is never visited and a stale expected
member fails for the wrong reason or gets deleted to make the suite green.

**No exemption list.** D-72 is designed so this census needs none — every
declared RPC has a live handler. Do not add one preemptively, and do not add one
later to make a red census green. `ownershipWrapperMethods` is not one and must
not become the seed of one: it names an accepted CALL SHAPE on the derived side,
it names no RPC, and every owner-audience RPC stays in its expected set with or
without it.

**Demonstrate the RED three times, once per predicate.** One demonstration on the
guest half proves only the guest half; the ownership predicate and the
wrapper-integrity assertion each need their own.

This census parses `internal/grpc` and `internal/web` with `go/ast` and imports
neither package, so a temporarily inconsistent handler body still PARSES and the
census reports the membership failure it is meant to report rather than a build
error. Confirm that property before starting — `rg -n 'holomush/internal/grpc"'
test/meta/` returns no match at HEAD — and do not add such an import in this file.

  - **(a) Guest-gate predicate.** Temporarily delete the guest-gate call from one
    existing owner-audience handler; capture the failure naming that handler in the
    guest set's missing group; revert.
  - **(b) Ownership predicate.** Temporarily delete the
    `s.ownedCharacterForMutation` call from `UpdateCharacterProfile`; capture the
    failure naming it in the ownership set's missing group; revert. Without this
    one, the ownership predicate is never exercised at all.
  - **(c) Wrapper integrity.** Temporarily delete the `s.ownedCharacter` call from
    the body of `ownedCharacterForMutation` itself; capture the failure naming the
    wrapper; revert. This is the one attack the wrapper set would otherwise open,
    and note what it looks like: both mutations still reference the wrapper, so
    they stay members and (b)'s comparison stays GREEN — only the integrity
    assertion goes red. That asymmetry is the whole reason part 2 exists.

Record all three verbatim outputs in the SUMMARY as this census's RED evidence.
  </action>

  <verify>
    <automated>task test -- -run 'RoutingCensus' ./test/meta/</automated>
  </verify>

  <acceptance_criteria>
- `task test -- -run 'RoutingCensus' ./test/meta/` is green with all seven behaviors present.
- The SUMMARY carries three verbatim census failures, one per predicate: the guest-gate handler in the guest set's missing group, `UpdateCharacterProfile` in the ownership set's missing group, and `ownedCharacterForMutation` named by the wrapper-integrity assertion.
- **All three temporary removals were reverted.** Source-state, deliberately NOT a `git diff`/`git status` check: `internal/grpc` is outside this plan's `files_modified`, so a diff-shaped criterion is empty before the change and empty again after any commit and proves nothing in either world (the vacuity `04-02:131`/`:134` diagnose). Check instead that all three removed call expressions are present at the committed tree, counting matches with comment lines filtered so a rationale comment cannot move a number: `rg -v '^\s*//' internal/grpc/characteraccess_owner.go internal/grpc/characteraccess_write.go | rg -o 's\.resolveAndGate\(' | wc -l` returns 4 (one per owner-audience handler); `rg -v '^\s*//' internal/grpc/characteraccess_write.go | rg -o 's\.ownedCharacterForMutation\(' | wc -l` returns 2 (one per mutation handler); and `rg -v '^\s*//' internal/grpc/characteraccess_write.go | rg -o 's\.ownedCharacter\(' | wc -l` returns 1 (the call inside the wrapper — the trailing `(` is load-bearing, it is what keeps this from also matching the wrapper's own name).
- `rg -n -A 4 'ownershipWrapperMethods' test/meta/characteraccess_routing_census_test.go` shows a set literal with exactly one member, `ownedCharacterForMutation`, and the wrapper-integrity assertion is positioned before the ownership comparison in the test body — read the order, do not infer it.
- The wrapper-integrity assertion calls the SHIPPED selector predicate rather than a copy: `rg -o 'func bodyReferencesSelector\(' test/meta/ | wc -l` returns 1 across the whole directory.
- The three expected sets are checked in as literals with member counts 4 / 3 / 4, and `ListMyCharacters` appears in the guest-gate and web sets and is absent from the ownership set, with the comment beside that set naming why.
- `rg -n 'UpdateCharacterProfile|UpdateCharacterDescription' test/meta/characteraccess_routing_census_test.go` matches inside BOTH the guest-gate and the ownership expected sets — Phase 4's two write RPCs are inside criterion 1's set-equality proof, not outside it.
- `rg -n 'for .* := range expected' test/meta/characteraccess_routing_census_test.go` returns no match — the comparison is set equality, not a loop over the expected entries.
- `rg -n 'exempt|Exempt|skip|allowlist' test/meta/characteraccess_routing_census_test.go` returns no match. **Naming constraint, load-bearing:** `ownershipWrapperMethods` is the one construct a future reader could mistake for such a thing, and it is deliberately named none of those four words — it adds a second accepted call shape to the DERIVED side and never removes a member from the EXPECTED side, so it cannot make an ungated handler pass. Do not write any of those four words anywhere in this file, in code or in a comment: a synonym sitting beside a verified wrapper set is the next reader's invitation to add a real one.
- The file walks a directory rather than naming a single Go file; `rg -n 'os.ReadDir' test/meta/characteraccess_routing_census_test.go` matches at least twice, once per directory.
- Each of the three anti-vacuity assertions runs before its comparison and carries a message naming what changed if it fires.
- The two public-audience RPC names appear in the file only inside the comment explaining their deliberate exclusion.
  </acceptance_criteria>

  <done>
Criterion 1 is bound: the gate exists in one place, set equality over three
named expected sets proves every owner-audience RPC on both sides of the proxy
pair reaches it — the two mutations through one verified wrapper — and each of
the three predicates has its own demonstrated RED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The descriptor census — character-returning RPCs equal the checked-in inventory</name>

  <files>test/meta/character_rpc_census_test.go (new)</files>

  <read_first>
- .planning/phases/01-portal-spec/01-SPEC.md sections 2.6, 3.3, 3.4, 3.5 — the comparison key, the comparison semantics, the full inventory tables including the four public export surfaces, and the rule that the Phase-4 expected set is the union of section 3.3 and section 9's character-returning rows minus the rows section 2.4 deletes
- .planning/phases/01-portal-spec/01-SPEC.md section 9.2 and section 9.3 — the Phase-4 rows and their audience verdicts
- test/meta/grpc_api_coverage_test.go — how a meta-test enumerates the proto module and its anti-vacuity guard
- internal/plugin/luabridge/gen/luastub_test.go — the in-tree use of the global proto registry for descriptor lookup
- api/proto/holomush/core/v1/core.proto and api/proto/holomush/web/v1/web.proto — the character-shaped message names and the name-reachable scalar cases section 3.3 enumerates
- test/meta/world_caller_census_test.go — the exemption-list idiom, for the shape of an explicitly-enumerated list with a membership-is-exact assertion
  </read_first>

  <behavior>
    - Test: the derived set of fully-qualified proto method names whose response transitively contains a character-shaped message, unioned with the explicitly-enumerated name-reachable members, equals the checked-in inventory exactly, in both directions.
    - Test (anti-vacuity): the registry walk discovers a non-empty set of services and a non-empty set of methods, and the character-shaped message name set is non-empty; each is asserted before the comparison.
    - Test: the removed web directory RPC is absent from the derived set — proving plan 04-07's deletion is complete at the descriptor level, not only in source.
    - Test: the membership of the explicitly-enumerated name-reachable list is exact, so removing a member is a visible edit rather than a silent loosening.
    - Test: the comparison key is an exact string; a member whose name is a strict substring of another member is present in a fixture and both are counted separately.
  </behavior>

  <action>
Create `test/meta/character_rpc_census_test.go`.

**Derivation.** Walk the generated service descriptors through the global proto
registry: iterate registered files, then services, then methods, taking each
method's fully-qualified name in package-service-method form. Blank-import every
generated package whose services must be in scope so their registration runs, and
guard against an incomplete registration with an anti-vacuity assertion on the
discovered service count, following the guard the API-coverage meta-test already
carries.

**Membership predicate.** A method is a member when its output message
transitively contains a message from a checked-in set of character-shaped message
full names — walk the message's fields, recursing into message-typed fields with
a visited set so a cyclic schema terminates. Check in the character-shaped name
set explicitly, one entry per message, and assert it is non-empty.

**The name-reachable members.** Section 3.3 records that several members are
reachable only by explicit enumeration: responses carrying a bare display-name
scalar, and the public export surfaces whose payload is rendered bytes with no
proto field naming a character. A descriptor-graph predicate cannot see those, so
carry them as an explicitly enumerated list with one commented member per
justifying reason, and assert that list's membership is exact — the idiom the
caller census already uses, chosen so removing a member is a visible edit rather
than a silent loosening. Union that list into the derived set before comparing.
This enumeration is **not** an exemption list: it adds members, it never excuses
one.

**Comparison.** The key is the fully-qualified proto method name compared as an
exact string — never a substring or prefix match, because one member's name is a
substring of another's and a substring key would silently collapse them; and
never a Go handler identifier, which is not derivable from a descriptor and is
not stable against refactor. The comparison is set equality, order-independent,
and fails with the symmetric-difference diff from Task 1. The checked-in expected
set is the section 3 inventory: section 3.3's rows plus the Phase-4 rows section
9 adds, minus the row section 2.4 deletes. Transcribe it from those tables; do
not re-derive it. Add a file-header note that a later phase adding a
character-returning RPC adds its inventory row in the same change (D-72).

**Demonstrate the RED both ways.** Temporarily add a character-returning method
to the derived side without an inventory row and capture the extra-group failure;
then temporarily add an inventory row with no RPC and capture the missing-group
failure. Revert both and record the two captured outputs in the SUMMARY, so the
census is shown to fail in each direction rather than only one.
  </action>

  <verify>
    <automated>task test -- -run 'CharacterReturningRPCCensus' ./test/meta/</automated>
    <human-check>At the phase gate, run `/holomush-dev:review-abac` and confirm a READY verdict before pushing — this phase adds two ABAC seed policies in plan 04-01, and the reviewer is mandatory for any change touching the access package.</human-check>
  </verify>

  <acceptance_criteria>
- `task test -- -run 'CharacterReturningRPCCensus' ./test/meta/` is green with all five behaviors present.
- The SUMMARY carries both captured failures — one naming an extra member, one naming a missing member.
- **Both temporary changes were reverted.** Source-state, deliberately not a `git diff`/`git status` check, for the same reason Task 2's revert criterion gives: `api/` and `internal/` are outside this plan's `files_modified`, so a diff-shaped criterion is empty both before the change and after any commit. Check instead that the throwaway artifacts are gone: name the temporary method and the temporary inventory row verbatim in the SUMMARY, then assert `rg -n '<temporary method name>' api/ internal/ pkg/ test/meta/` and `rg -n '<temporary inventory row>' test/meta/` each return no match, and that the census is green — which is the same evidence one layer up, since a surviving half of either edit makes it RED in one direction.
- `rg -n 'strings.Contains|strings.HasPrefix|strings.HasSuffix' test/meta/character_rpc_census_test.go` returns no match on the comparison path; the SUMMARY notes any match found elsewhere in the file and why it is not the key comparison.
- The removed web directory RPC's fully-qualified name appears nowhere in the checked-in inventory, and the absence test asserts it is not in the derived set.
- The name-reachable enumeration carries one comment per member naming the section 3.3 reason, and a membership-is-exact assertion.
- The anti-vacuity assertions fire before the comparison and carry messages naming what layout change would cause them.
  </acceptance_criteria>

  <done>
The descriptor census derives the character-returning RPC set from the generated
descriptors and binds it to the checked-in inventory by set equality, with a
demonstrated RED in each direction.
  </done>

  <reversibility rating="reversible">
A meta-test plus a checked-in data table. Removing it removes a gate; it changes
no production behavior and no persisted state.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a future PR → the owner-audience surface | A new RPC that skips the shared gate is the erosion vector both censuses exist to close |
| a future PR → the character-returning RPC set | A new response carrying character data without an inventory row is how the audience matrix quietly stops covering the tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Spoofing | a future owner-audience RPC bypassing the guest gate | high | mitigate | Routing census with set equality over three named expected sets covering both halves of every proxy pair, failing with a symmetric-difference diff; three demonstrated REDs recorded, one per predicate |
| T-04-32 | Elevation of Privilege | ownership indirection hiding an ungated mutation | high | mitigate | The ownership predicate accepts one checked-in wrapper name and no other, and asserts that wrapper's own body reaches `s.ownedCharacter` before comparing sets — so gutting the wrapper goes RED (04-06, D-73) and any newly-named indirection drops its handler into the missing group rather than passing silently |
| T-04-26 | Information Disclosure | a future character-returning RPC with no audience verdict | high | mitigate | Descriptor-derived census keyed on the exact fully-qualified method name, compared by set equality against the checked-in inventory in both directions |
| T-04-27 | Repudiation | a vacuous census | medium | mitigate | Anti-vacuity guards on every discovered universe; no exemption list; no loop-over-expected comparison; the name-reachable enumeration carries a membership-is-exact assertion |
| T-04-25 | Tampering | an incomplete RPC removal surviving at the descriptor level | medium | mitigate | The census asserts the removed method name is absent from the derived descriptor set, not only from source |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no external packages; it uses only the standard library and packages already in the module |
</threat_model>

<verification>
- `task test -- ./test/meta/` green
- `task pr-prep` green, then `task pr-prep:full` before push — this phase's diff touches the world package, the access package, the gRPC facade, the web proxies and the integration harness, and `task test` does not compile integration files
- `/holomush-dev:review-abac` READY at the phase gate (two ABAC seeds added in plan 04-01)
- `/gsd-code-review` over the phase's changed files
</verification>

<success_criteria>
Two censuses ship, each compared by set equality with a symmetric-difference
diff, each with an anti-vacuity guard and a demonstrated RED, and neither
carrying an exemption list. A later RPC that skips the shared gate, or that
returns character data without an inventory row, fails the suite rather than
shipping.
</success_criteria>

## Artifacts this phase produces

Symbols created by **this plan** (excluded from drift verification):

- Three helpers added to `test/meta/meta_helpers_test.go`: a facade-aware receiver-name predicate taking a set of accepted receiver type names, a bare-identifier body predicate, and a symmetric-difference diff renderer
- Go test file `test/meta/characteraccess_routing_census_test.go`, its three checked-in expected sets (guest gate 4 members, ownership 3, web proxies 4), and the checked-in one-member `ownershipWrapperMethods` set with its wrapper-integrity assertion
- Go test file `test/meta/character_rpc_census_test.go`, its checked-in character-shaped message name set, its explicitly-enumerated name-reachable member list, and the checked-in read-surface inventory

<output>
Create `.planning/phases/04-shared-facade-helpers-characteraccessservice/04-08-SUMMARY.md` when done
</output>
