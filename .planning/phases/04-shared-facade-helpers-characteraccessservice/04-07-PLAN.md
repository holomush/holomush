---
phase: 04-shared-facade-helpers-characteraccessservice
plan: 07
type: execute
wave: 5
depends_on: ["04-01", "04-04", "04-05", "04-06"]
files_modified:
  - api/proto/holomush/characteraccess/v1/characteraccess.proto
  - api/proto/holomush/web/v1/web.proto
  - pkg/proto/holomush/characteraccess/v1
  - pkg/proto/holomush/web/v1
  - web/src/lib/connect/holomush/characteraccess/v1
  - web/src/lib/connect/holomush/web/v1
  - site/src/content/docs/reference/grpc-api.md
  - internal/access/policy/seed.go
  - internal/grpc/characteraccess_directory.go
  - internal/grpc/characteraccess_directory_test.go
  - internal/grpc/characteraccess_projection.go
  - internal/grpc/characteraccess_service.go
  - cmd/holomush/sub_grpc.go
  - internal/testsupport/integrationtest/harness.go
  - internal/web/character_handlers.go
  - internal/web/auth_handlers.go
  - internal/web/auth_handlers_test.go
  - internal/web/handler.go
  - internal/web/handler_test.go
  - web/src/lib/scenes/directoryClient.ts
  - test/integration/access/character_directory_test.go
autonomous: true
requirements: [PROFILE-03, PROFILE-04]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The public character directory returns identity only: the four presence-telemetry fields the shipped roster message carries — active-session flag, session status, last location, last-played time — have no field to occupy in the directory response message."
    - "The §9.2 GATE is ONE ABAC decision on the directory resource: the handler evaluates a viewer-tier floor against access.CharacterDirectoryResource() (internal/access/prefix.go:273) with the shipped list_character_directory action before enumerating anything, and a viewer below that floor is denied the whole call. Bulk enumeration therefore has a single controlling decision, and a game can raise the directory floor independently of profile reachability by editing that one policy's clearing set."
    - "The facade can actually MAKE that decision: CharacterAccessServer holds a narrow characterAccessPolicyEvaluator (one method, Evaluate(ctx, types.AccessRequest) (types.Decision, error)) satisfied by the production *policy.Engine and by the harness's accessEngine without new production code. The gate is never approximated by profilevis.Reachable — that substitution is the §9.2 collapse review finding HIGH-4 removed."
    - "A gate decision that could not be evaluated is neither a permit nor a deny: an engine error AND a DENY decision carrying an infra-failure marker with a nil error both return Internal and leave ListAll uninvoked (01-SPEC 8.10)."
    - "The per-character reachability pass is the §8.7 MEMBERSHIP rule layered on top of that gate, never a substitute for it: a character whose profile the viewer cannot reach is absent from that viewer's listing, and its absence is indistinguishable from the character not existing."
    - "The handler's doc comment states only facts that hold at HEAD: character_directory (prefix.go:273) and seed:directory-list-characters (seed.go:575-579, bound to INV-ACCESS-9) DO exist and are principal is character-scoped; what v0.13 lacked was a viewer-tier floor on that resource, which this plan seeds."
    - "The directory listing is deterministically ordered, so two identical calls against unchanged data marshal to identical bytes."
    - "An empty directory returns an empty list and a success status, never a not-found."
    - "WebListAllCharacters no longer exists anywhere in the tree: not in the proto, not as a Go handler, not in the generated TypeScript client, and not as a thin forwarder."
    - "The web client's directory call reaches WebListCharacterDirectory, and no call site was left pointing at a removed RPC."
    - "CoreService.ListAllCharacters is untouched — only the web-side RPC is removed."
  artifacts:
    - internal/grpc/characteraccess_directory.go
    - test/integration/access/character_directory_test.go
  key_links:
    - "CharacterAccessServer.ListCharacterDirectory -> evaluateGate -> characterAccessPolicyEvaluator.Evaluate (satisfied by *policy.Engine at cmd/holomush/sub_grpc.go:352 and by the harness accessEngine at harness.go:876) on access.CharacterDirectoryResource() — the §9.2 gate, one decision"
    - "CharacterAccessServer.ListCharacterDirectory -> characterAccessDirectoryReader.ListAll -> profilevis.Reachable per character (the §8.7 membership rule) -> projectPublicSummary"
    - "seed:viewer-directory-list-characters (new, viewer twin of the shipped seed:directory-list-characters per D-76's twinning pattern) -> the directory tier floor a game edits"
    - "characterAccessDirectoryReader (new narrow interface, ListAll only) <- auth.CharacterRepository implementations: bootstrapsetup.CharRepoAdapter (cmd/holomush/sub_grpc.go:446) and the harness's authCharRepoAdapter (harness.go:1903)"
    - "web/src/lib/scenes/directoryClient.ts -> WebListCharacterDirectory -> CharacterAccessService.ListCharacterDirectory"
    - "internal/web/auth_handlers.go loses WebListAllCharacters; internal/grpcclient and CoreService.ListAllCharacters are unchanged"
  prohibitions:
    - "Never keep a removed RPC alive as a deprecated alias or a thin forwarder — the replacement is the surface, and a forwarder preserves a contract nobody depends on."
    - "Never publish presence telemetry to the public audience."
---

<objective>
Replace `WebListAllCharacters` with the public-audience
`ListCharacterDirectory` and delete the old RPC outright, so the directory serves
identity only and carries no presence telemetry.

Purpose: 01-SPEC section 2.4 splits the one directory RPC into a public and an
admin surface; the admin half is Phase 6, but the public half and the deletion
are Phase 4's — section 3.3's inventory row says so in as many words, and plan
04-08's descriptor census expects the removed row to be gone.

Output: the directory RPC with its reachability filter and identity-only summary
message, the deletion of the old RPC and every consumer of it, and a full-stack
spec proving an unreachable character is simply absent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-CONTEXT.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-RESEARCH.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-PATTERNS.md
@.planning/phases/01-portal-spec/01-SPEC.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the directory surface and remove the RPC it replaces</name>

  <files>api/proto/holomush/characteraccess/v1/characteraccess.proto, api/proto/holomush/web/v1/web.proto, generated Go and TS output, site/src/content/docs/reference/grpc-api.md</files>

  <read_first>
- api/proto/holomush/web/v1/web.proto — the `WebListAllCharacters` RPC declaration and its request/response messages, plus `CharacterSummary` and the four presence-telemetry fields it carries
- api/proto/holomush/core/v1/core.proto — `CharacterDirectoryEntry` and `ListAllCharacters`, both of which stay, and the `ListAllCharacters` doc comment stating that connection state is a separately-permissioned attribute
- api/proto/holomush/characteraccess/v1/characteraccess.proto — as plans 04-01 and 04-04 left it
- .planning/phases/01-portal-spec/01-SPEC.md sections 2.4, 2.5, 3.3, 3.5, 9.2
- .claude/rules/proto-doc-comments.md
  </read_first>

  <action>
Add to `api/proto/holomush/characteraccess/v1/characteraccess.proto`:
  - `message PublicCharacterSummary` — the list-shaped `public` projection: the character id and the display name, and nothing else. It is `PublicCharacter` restricted to what a list row needs; a list is not a fourth audience. It carries none of the four presence-telemetry fields the shipped roster message has — no active-session flag, no session status, no last location, no last-played time — and it carries no visibility hint of any kind.
  - `ListCharacterDirectoryRequest` carrying the player session token, and `ListCharacterDirectoryResponse` carrying `repeated PublicCharacterSummary`.
  - `rpc ListCharacterDirectory` on `CharacterAccessService`.

Add the matching `WebListCharacterDirectory` RPC and its request/response
messages to `api/proto/holomush/web/v1/web.proto`. The web request carries no
session token field; the token travels in the header.

**Remove `WebListAllCharacters`** from `web.proto` together with its request and
response messages. Do not deprecate it, do not alias it, and do not keep it as a
thin forwarder — 01-SPEC section 2.5 records that v0.13 ships no compatibility
shim and that a keep-the-old-RPC-forwarding task is out of scope and must not be
planned. `CoreService.ListAllCharacters` in `core.proto` is **untouched**; only
the web-side RPC is removed.

Give every new element a Go-grounded leading doc comment naming its implementing
handler, and do not restate the element's own name.

Run `task proto`, `task web:generate` and `task docs:proto`, and commit all
generated output in this same change. The build will be red until Task 2 lands
the handler and Task 3 updates the consumers; land all three tasks before
claiming the plan complete.
  </action>

  <verify>
    <automated>task lint:proto</automated>
  </verify>

  <acceptance_criteria>
- `rg -n 'WebListAllCharacters' api/proto/` returns no match.
- `rg -n 'rpc ListAllCharacters' api/proto/holomush/core/v1/core.proto` still matches — the core RPC survives.
- `rg -n 'message PublicCharacterSummary' -A 12 api/proto/holomush/characteraccess/v1/characteraccess.proto` shows exactly two fields.
- `rg -n 'has_active_session|session_status|last_location|last_played_at' api/proto/holomush/characteraccess/v1/characteraccess.proto` returns no match.
- `task lint:proto` is green.
- `git status --porcelain pkg/proto web/src/lib/connect site/src/content/docs/reference` is empty after a fresh `task proto && task web:generate && task docs:proto`.
  </acceptance_criteria>

  <done>
The directory surface is declared and documented, the replaced RPC is gone from
the proto, and every generated artifact is regenerated and committed.
  </done>

  <reversibility rating="costly">
Removing an RPC and its generated TypeScript client means restoring the handler,
the client shim and the web call site together if it is ever reversed. It is not
`one-way`: 01-SPEC section 2.5 records that the system has no consumer outside
this repository, so no published contract breaks and no migration is needed. It
is also not a decision this plan makes — section 2.4 removes the RPC, section
3.3's inventory row assigns the deletion to Phase 4, and section 2.5 forbids a
compatibility shim.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The directory handler — reachability-filtered, identity only, deterministic</name>

  <files>internal/access/policy/seed.go, internal/grpc/characteraccess_directory.go (new), internal/grpc/characteraccess_projection.go, internal/grpc/characteraccess_service.go, cmd/holomush/sub_grpc.go, internal/testsupport/integrationtest/harness.go, internal/grpc/characteraccess_directory_test.go (new), test/integration/access/character_directory_test.go (new)</files>


  <read_first>
- internal/grpc/characteraccess_service.go — `CharacterAccessServer`, the narrow interfaces, `resolveViewerTier`, `mapProfileError`
- internal/grpc/characteraccess_projection.go — `projectPublic` and `projectOwner`, whose omission discipline the summary projection copies
- internal/access/profilevis/profilevis.go — `Reachable` and its doc comment explaining why a did-any-field-clear-its-floor heuristic is wrong; `PolicyEvaluator` (`:100-102`), the shipped one-method consumer-side slice `characterAccessPolicyEvaluator` copies verbatim; and `evaluate` (`:234-273`), the three-outcome collapse `evaluateGate` is modeled on — read its comment about the second, subtle failure shape (a DENY carrying an `infra:` policy id with a NIL error)
- internal/access/policy/engine.go `:82` — `func (e *Engine) Evaluate(ctx context.Context, req types.AccessRequest) (types.Decision, error)`, the production satisfier's exact signature
- internal/access/policy/types/types.go `:527-538` — `AccessPolicyEngine`, whose first method is that same `Evaluate`; the harness's `accessEngine` field is this type. Also `NewAccessRequest` (`:144`), `Decision.IsAllowed` (`:229`), `Decision.IsInfraFailure` (`:285`), `Decision.PolicyID` (`:244`), `Decision.Reason` (`:239`)
- internal/access/policy/policytest/helpers.go `:120-131` — `NewInfraFailureEngine`, whose `Evaluate` returns a DENY with an `infra:` policy id and a nil error. Its `Evaluate` has exactly the seam's signature, so it satisfies the seam directly and doubles as the fixture (it also carries `CanPerformAction` at `:137`, which the seam ignores)
- internal/access/prefix.go `:273` — `CharacterDirectoryResource()`, which returns the singleton `character_directory:all`. It EXISTS at HEAD. Read it before writing any doc comment claiming otherwise.
- internal/access/policy/seed.go `:567-580` — `seed:directory-list-characters`, `permit(principal is character, action in ["list_character_directory"], resource is character_directory)`, bound to INV-ACCESS-9. It EXISTS at HEAD and is `principal is character`-scoped, which is exactly why a `viewer:` principal does not match it.
- internal/access/policy/seed.go — `seed:profile-reachable` (`:678-682`) and its clearing set: the shape the new viewer-tier directory floor copies, including the "raising the floor is an edit to this clearing set and nothing else" doc comment.
- internal/access/policy/seed_profile_visibility_test.go `:421-447` — `viewerTwins` (exactly five entries at HEAD) and `TestExactlyTheFiveReadSideRowKeyedPoliciesHaveAViewerTwin`, which scopes on the `seed:viewer-property-` PREFIX, and `:450-470` `TestSeedPropertyOwnerWriteHasNoViewerTwin`, which iterates every `seed:viewer-` policy and requires a scoped action clause. Confirm the new policy's name and action clause satisfy both before writing it. **This file is READ, never edited, by this task** — it is deliberately absent from `files_modified` and from this task's `<files>`. If either test goes RED, stop and report rather than relaxing it.
- internal/access/policy/seed_profile_visibility_test.go `:675-700` — `TestNoPhase2SeedIntroducesACharacterResourceTypePermit`, and its explicit note that `character_directory` is a DIFFERENT resource type from `character` and is not swept in. The new seed does not disturb it.
- internal/auth/character_service.go `:38-42` — `CharacterRepository.ListAll(ctx) ([]*world.Character, error)`: "ALL characters (id + name only) for the directory picker — fetch-all, NO pagination, ordered by name ascending. Names only; no connection state." This is the enumeration seam.
- cmd/holomush/sub_grpc.go `:446` — `authCharRepo := bootstrapsetup.NewCharRepoAdapter(pool, charRepo)`, whose `ListAll` is at `internal/bootstrap/setup/adapters.go:151`. It is already in scope where 04-01 constructs the character facade.
- internal/testsupport/integrationtest/harness.go `:1903` — `authCharRepoAdapter.ListAll`, the harness-side satisfier, and `NewSceneAccessServer` at `:1173` for the constructor idiom 04-01 mirrored.
- internal/web/auth_handlers.go — `WebListAllCharacters`, whose current behavior the replacement must cover for the picker
- test/integration/access/profile_public_read_test.go — the Ginkgo conventions and cleanup order
- .planning/phases/01-portal-spec/01-SPEC.md sections 2.4, 3.5, 8.4.2, 9.2 — section 9.2's row reads "tier floor (§8.4) **on the directory resource**"; that is the gate this task implements literally.
- .planning/phases/04-shared-facade-helpers-characteraccessservice/04-CONTEXT.md — D-76, the twinning pattern (a shipped `character`-flavored permit gains a `viewer`-flavored twin) this seed follows, and its recorded consequence that the `abac-reviewer` gate fires before push.
  </read_first>

  <behavior>
    - Test (HIGH-4, the gate): a viewer whose rung is outside `seed:viewer-directory-list-characters`' clearing set is denied the whole call, and the directory reader double records that `ListAll` was NEVER invoked — proving the gate precedes enumeration rather than filtering after it.
    - Test (HIGH-4, paired positive control): a viewer at a clearing rung reaches the enumeration on the same fixture, so the denial above is the floor rather than an empty corpus.
    - Test (HIGH-4, the gate is independent of reachability): with the directory floor raised so the viewer does not clear it, the call is denied even though `profilevis.Reachable` would have permitted every character — the two decisions are separable, which is the property the substituted design could not express.
    - Test: an anonymous viewer receives every character whose profile is reachable at the anonymous rung, each as a `PublicCharacterSummary` carrying id and name.
    - Test: a character whose profile is not reachable for the calling viewer is absent from the list, and the response is otherwise identical in shape to one where that character never existed.
    - Test (paired positive control): the same character is present for a viewer at a rung that clears its reachability floor, so the absence is the floor rather than an empty fixture.
    - Test: an empty result set returns an empty list and a success status, never a not-found.
    - Test: two identical calls against unchanged data marshal to identical bytes, and the listing order is deterministic.
    - Test: an evaluation failure during the reachability pass returns an internal status rather than a silently shortened list.
    - Test: an evaluation failure in the directory-gate decision itself returns an internal status and never falls through to enumeration — a failure to evaluate the gate is not a permit and is not a deny. Drive it with a `characterAccessPolicyEvaluator` double returning a non-nil error, and assert `ListAll` was never invoked.
    - Test (HIGH-B, the branch a hand-written gate gets wrong): a **DENY decision that reports an infrastructure failure with a NIL error** also yields an internal status and leaves `ListAll` uninvoked — it is NOT collapsed into an ordinary denial. Use `policytest.NewInfraFailureEngine(t, reason, "infra:...")` (`internal/access/policy/policytest/helpers.go:120-131`) **directly** as the `characterAccessPolicyEvaluator` double: its `Evaluate` has exactly the seam's signature and returns precisely that shipped DENY-with-`infra:`-id-and-nil-error shape, so it satisfies the seam directly and no fixture is invented (it also declares `CanPerformAction`, which the seam ignores).
  </behavior>

  <action>
**The §9.2 gate, seeded (addresses review finding HIGH-4).** An earlier revision
of this plan instructed the executor to write, in a **production doc comment**,
that *"no directory-shaped resource is seeded"* and to substitute per-character
reachability for the gate. That premise is false at HEAD and the instruction is
withdrawn. `access.CharacterDirectoryResource()` exists
(`internal/access/prefix.go:273`), the resource type `character_directory` exists,
and `seed:directory-list-characters` exists (`internal/access/policy/seed.go:575-579`,
bound to INV-ACCESS-9). What v0.13 lacks is a **viewer-tier floor** on that
resource: the shipped policy is `principal is character`-scoped, and the v0.13
viewer seeds cover only `resource is property` and `resource is profile`. Seed
that floor and gate on it.

Add one seed policy to `internal/access/policy/seed.go`, immediately after
`seed:directory-list-characters` so the family stays together:

  - `seed:viewer-directory-list-characters` —
    `permit(principal is viewer, action in ["list_character_directory"], resource is character_directory) when { principal.viewer.tier in ["anonymous", "guest", "player"] };`
    at `SeedVersion: 1`. `SeedVersion` is **per-policy, not a global counter**:
    `internal/access/policy/bootstrap.go:91` compares a seed's declared version
    against that same policy's own stored row (`*existing.SeedVersion <
    seed.SeedVersion`) purely as an upgrade trigger, so a brand-new policy has no
    prior row to upgrade from and ships at 1 — exactly as the shipped
    `seed:directory-list-characters` does (`seed.go:578`). Do not reach for a
    "next unused" number. It is the `viewer`-flavored twin of the
    shipped `character`-flavored permit, exactly the pattern D-76 locks and Phase
    2's D-01 established. Its doc comment must say, in the form
    `seed:profile-reachable`'s does, that **the clearing set is the whole
    configuration surface** — raising the directory floor is an edit to that list
    and nothing else — and that it is a decision independent of
    `seed:profile-reachable`, so a game may publish the directory at a rung it
    does not publish profiles at, or the reverse.

The action token needs no `action_schema.go` registration:
`POLICY_UNREGISTERED_ACTION_ATTRIBUTE` gates `action.<key>` attribute references
inside a `when {}` clause, not action tokens, and `list_character_directory` is
already a shipped token. `character_directory` needs no AttributeProvider either
— like `resource is profile`, the clause reads no resource attributes, so it is a
target match on the parsed resource type.

Confirm against the two seed meta-tests before writing: the `viewerTwins`
set-equality scopes on the `seed:viewer-property-` prefix, which this name does
not carry, and `TestNoPhase2SeedIntroducesACharacterResourceTypePermit` pins the
`character` resource TYPE, of which `character_directory` is explicitly not a
member. Neither should need editing; if either goes RED, stop and report rather
than relaxing it.

**The evaluator seam, named (addresses review finding HIGH-B — a defect the
HIGH-4 fix introduced by mandating a gate the facade had no way to call).** At
HEAD the facade holds nothing that can evaluate a raw ABAC decision:
`profilevis.Evaluator`'s exported set is exactly `Reachable`
(`internal/access/profilevis/profilevis.go:130`), `AttributeVisible` (`:157`) and
`VisibleAttributes` (`:199`); `evaluate` (`:234`) and `check` (`:278`) are
unexported. **Do not improvise by gating on `Reachable`** — that is precisely the
per-character-reachability substitution HIGH-4 was raised to remove, and it would
silently delete the §9.2 decision again.

Declare a **fourth narrow consumer-side interface** in
`internal/grpc/characteraccess_service.go` — beside the two 04-01 declared and the
`characterAccessDirectoryReader` declared later in this same task:

```
characterAccessPolicyEvaluator interface {
        Evaluate(ctx context.Context, req types.AccessRequest) (types.Decision, error)
}
```

- **Signature source:** verbatim `(*policy.Engine).Evaluate`
  (`internal/access/policy/engine.go:82`), and identical to the shipped narrow
  slice `profilevis.PolicyEvaluator` (`profilevis.go:99-101`) — the same
  one-method shape, declared consumer-side, for the same reason its doc comment
  gives. `types` is `internal/access/policy/types`.
- **Production satisfier, no new production code:** `*policy.Engine`. The
  concrete value is already in scope at the wiring site as `policyEngine`
  (`cmd/holomush/sub_grpc.go:352`) — the same engine 04-01 passes into the
  `profilevis.Evaluator` a few lines away.
- **Harness satisfier:** the harness's `accessEngine` field, typed
  `types.AccessPolicyEngine`, whose interface declares this exact method
  (`internal/access/policy/types/types.go:527-528`); it is set at
  `harness.go:876` and already exposed by `AccessEngine()` at `:1197`.
- **Injection site:** one more parameter on `NewCharacterAccessServer`, passed
  at the same two call sites this task already edits for
  `characterAccessDirectoryReader` — `cmd/holomush/sub_grpc.go` and
  `internal/testsupport/integrationtest/harness.go`. No third call site exists;
  `rg -o 'NewCharacterAccessServer\(' cmd/ internal/ | wc -l` is the check.
- **Rejected: adding a method to `profilevis.Evaluator`.** It would need no new
  wiring, but `profilevis` is scoped by its own package doc to §8.5.1's
  per-attribute conjunction plus §8.4.2 reachability — profile *visibility*. A
  bulk directory-enumeration gate is a different question on a different resource
  type, and widening that package's exported surface to host it makes the package
  name a lie and invites the next reader to route more §9.x decisions through it.
  Keep the phase's narrow-consumer-interface discipline instead.

**Write the three-outcome collapse ONCE, in a helper, and copy `profilevis`'s
reasoning into its doc comment.** Add
`evaluateGate(ctx context.Context, subject, action, resource string) (bool, error)`
to `internal/grpc/characteraccess_directory.go`, modeled line-for-line on
`profilevis.evaluate` (`profilevis.go:234-273`):

  1. build the request with `types.NewAccessRequest(subject, action, resource, nil)`;
     a construction error is an evaluation failure, not a deny;
  2. a non-nil error from `Evaluate` is an evaluation failure;
  3. `decision.IsAllowed()` is the permit;
  4. **`decision.IsInfraFailure()` with a NIL error is also an evaluation
     failure** — this is the subtle shape `profilevis.evaluate`'s doc comment
     calls out, produced by the engine's degraded-mode and session-resolution
     paths. Treating it as an ordinary denial is 01-SPEC section 8.10's forbidden
     masking, and it is the one branch a hand-written gate reliably gets wrong;
  5. anything else is an ordinary policy denial.

Log every failure branch through `errutil.LogErrorContext` and return an error
carrying `profilevis.CodeEvaluationFailed`, so `mapProfileError` maps it to
`codes.Internal` with a generic message exactly as the profile read's failures
already are. The doc comment must state why this is a deliberate, bounded
duplication of `profilevis.evaluate` rather than a reuse: the shipped helper is
unexported and package-scoped to profile visibility, and exporting it is the
alternative rejected above.

**The handler.** Create `internal/grpc/characteraccess_directory.go` with
`ListCharacterDirectory` on `CharacterAccessServer`. Resolve the viewer tier
through the same `resolveViewerIdentity` / `resolveViewerTier` pair the profile
read uses (04-01), then:

  1. **The gate — ONE decision, before any enumeration.** Call `evaluateGate`
     with the viewer subject, the action `list_character_directory`, and
     `access.CharacterDirectoryResource()`. A deny ends the call; an evaluation
     failure ends the call with `Internal`; the caller enumerates nothing and
     learns nothing about the corpus size in either case. This is 01-SPEC section
     9.2's row read literally, and it is what gives bulk enumeration a single
     controlling decision that a game can move independently of profile
     reachability.
  2. **The membership rule — per character, after the gate.** Enumerate through
     the narrow directory reader and include a character only when
     `profilevis.Reachable` permits the viewer for that character's profile.
     This is section 8.7, not section 9.2: listing a character whose profile
     returns the not-found-equivalent would disclose, in bulk, exactly the
     existence that floor was set to withhold. It is layered ON the gate, never
     a substitute for it.

State that two-layer structure in the handler's doc comment, and state it in
terms that are true at HEAD: the directory resource and its `character`-flavored
permit both exist, this phase adds the viewer twin, and the reachability pass is
the membership rule rather than the authorization decision. Do not write that no
directory policy exists.

An evaluation failure in **either** layer aborts the whole call with an internal
status. It must not shorten the list silently — a directory that renders as
legitimately small when nothing was evaluated is the same failure 01-SPEC section
8.10 forbids one layer down.

Add `projectPublicSummary` to `internal/grpc/characteraccess_projection.go` as
the sole constructor of `PublicCharacterSummary`. Per 01-SPEC section 2.4 the
summary is produced by the public projection family — a list is not a second
audience — so place it beside `projectPublic` and have it reuse the same
omission discipline.

Sort the response deterministically (by character id) before returning, so
enumeration order never varies between two identical calls.

Add the corresponding full-stack spec at
`test/integration/access/character_directory_test.go` covering the reachability
filter and its paired positive control against the real seeded corpus.

**The enumeration seam, named (addresses review finding MEDIUM-1).** An earlier
revision said only "extend the facade's narrow world reader with the
character-enumeration method this handler needs". There is no such method to
extend to: `world.Service` exposes `GetCharacter` (`internal/world/service.go:825`)
and `GetCharactersByLocation` (`:1199`) and nothing that enumerates all
characters. Declare a **third narrow consumer-side interface** in
`internal/grpc/characteraccess_service.go`, beside the two 04-01 declared, with
exactly one method:

```
characterAccessDirectoryReader interface {
        ListAll(ctx context.Context) ([]*world.Character, error)
}
```

That is the verbatim signature of `auth.CharacterRepository.ListAll`
(`internal/auth/character_service.go:38-42`), which is already the directory seam
this RPC replaces — it returns id and name only, fetch-all, ordered by name
ascending, and carries no connection state. It is satisfied without new
production code by `bootstrapsetup.CharRepoAdapter`
(`internal/bootstrap/setup/adapters.go:151`), which `cmd/holomush/sub_grpc.go:446`
already builds as `authCharRepo`, and by the harness's `authCharRepoAdapter`
(`internal/testsupport/integrationtest/harness.go:1903`). Add the parameter to
`NewCharacterAccessServer` and pass `authCharRepo` at the production wiring site
and `s.charRepo` in the harness constructor — both are in scope where 04-01
placed those calls.

**Rejected: adding `world.Service.ListCharacters`.** A new ABAC-gated domain read
would have to choose between one bulk decision and N per-row decisions, which is
precisely the choice 01-SPEC section 9.2 already makes at the facade (one
directory decision plus the reachability membership pass). Adding a second,
differently-shaped authorization point inside `world` for the same question is
the duplicate gate section 2.6 warns against, and it is a domain-layer change in
a BFF-facade phase.

Give **both** new interfaces the same load-bearing negative doc comment the other
two carry: `PropertyReader.ListByParent` and `PropertyRepository.ListByParent` are
deliberately absent from this type set, so a direct repository call from this
facade does not compile (PROFILE-10, D-79). Adding a third and a fourth interface
does not weaken the fence — the fence is the absence of the property-repository
methods from every interface the facade holds; `characterAccessDirectoryReader`
names only `ListAll` and `characterAccessPolicyEvaluator` names only `Evaluate`,
and neither can reach a property row.
  </action>

  <verify>
    <automated>task test -- -run 'Directory' ./internal/grpc/ &amp;&amp; task test:int -- ./test/integration/access/...</automated>
  </verify>

  <acceptance_criteria>
- `task test -- -run 'Directory' ./internal/grpc/` passes with all eleven behaviors present.
- `rg -n 'PublicCharacterSummary\{' internal/grpc/` matches only inside `projectPublicSummary`.
- `rg -n 'CharacterDirectoryResource' internal/grpc/characteraccess_directory.go` returns exactly one match, and it appears BEFORE the `ListAll` call in the handler body — the §9.2 gate is one decision and it precedes enumeration.
- **The evaluator seam is declared, injected and satisfied (review finding HIGH-B):** `rg -n 'characterAccessPolicyEvaluator' internal/grpc/characteraccess_service.go` returns exactly one interface declaration naming exactly one method, `Evaluate`; `rg -o 'characterAccessPolicyEvaluator' cmd/holomush/sub_grpc.go internal/testsupport/integrationtest/harness.go internal/grpc/ | wc -l` is non-zero at both wiring files; and `task build` is green, which is what proves `*policy.Engine` and the harness `accessEngine` satisfy it with no new production code.
- `rg -o 'IsInfraFailure\(\)' internal/grpc/characteraccess_directory.go | wc -l` returns 1 — the infra-failure branch of `evaluateGate` is present, not assumed. The matching test is the `policytest.NewInfraFailureEngine` behavior above; the grep alone is a smoke alarm over it.
- **Read-check, not a grep gate:** open `ListCharacterDirectory` and confirm `Reachable` is called only inside the per-character membership loop and never as the gate step. Do not express this as a negative or counted grep — this task's own doc comments and the `evaluateGate` rationale both name `Reachable` in order to explain what it is *not* doing here, so any text search over the file is red exactly when the plan was followed.
- **The seed meta-tests are satisfied by the policy's name and scope, not by editing them** (source-state, deliberately not a `git status` check — that file is untracked-clean before the change and clean again after any commit, so a status-shaped criterion is empty in both worlds and proves nothing; this is the same vacuity `04-02:131`/`:134` diagnose): `rg -o 'seed:viewer-directory-list-characters' internal/access/policy/seed.go | wc -l` returns 1; the new name does **not** carry the `seed:viewer-property-` prefix the `viewerTwins` set-equality scopes on; `rg -o '"seed:viewer-property-[a-z-]*": +"seed:property-' internal/access/policy/seed_profile_visibility_test.go | wc -l` still returns 5 (measured at HEAD; a bare prefix count returns 17 because the file names the prefix in both sweeping tests too), so `viewerTwins` still holds exactly its five HEAD entries; and `task test -- ./internal/access/policy/` is green, which is the evidence-bearing check.
- The handler's doc comment names `character_directory` and `seed:directory-list-characters` as EXISTING and explains that the gap this phase fills is the viewer-tier floor. Verify by reading it. Do not verify with a negative grep for the withdrawn claim: this plan quotes that claim in order to withdraw it, so any `.planning/`-spanning search is self-invalidating (review finding HIGH-4).
- `rg -n 'characterAccessDirectoryReader' internal/grpc/characteraccess_service.go` returns exactly one interface declaration naming exactly one method, `ListAll`; `task build` is green, proving `bootstrapsetup.CharRepoAdapter` and the harness adapter both satisfy it without new production code.
- **Diagnostic, not a gate:** `rg -n 'ListByParent' internal/grpc/` is expected to return no match. Treat a hit as a prompt to look, not as a failure — a comment or an unrelated future identifier containing that substring turns it red while the type fence is fully intact (review finding LOW-1). The evidence-bearing check for the fence is the deliberate-compile-failure demonstration recorded by `04-01` Task 2; this line is a smoke alarm over it.
- The determinism test marshals the same response twice and asserts byte equality.
- The evaluation-failure test asserts an internal status and asserts the returned list is nil rather than partially populated.
- The absence test compares the response for a viewer who cannot reach a character against the response for a corpus in which that character does not exist, and asserts the two are equal.
- `task test:int -- ./test/integration/access/...` is green.
  </acceptance_criteria>

  <done>
The directory serves identity only, filtered by reachability, deterministically
ordered, and fails closed on an evaluation error.
  </done>
</task>

<task type="auto">
  <name>Task 3: Retire every consumer of the removed RPC</name>

  <files>internal/web/character_handlers.go, internal/web/auth_handlers.go, internal/web/auth_handlers_test.go, internal/web/handler.go, internal/web/handler_test.go, web/src/lib/scenes/directoryClient.ts</files>

  <read_first>
- internal/web/auth_handlers.go — `WebListAllCharacters` in full, including which core-client method it calls and what it forwards
- internal/web/handler.go — the narrow core-client interface `Handler` depends on, and the compile-time assertion that `*Handler` satisfies the generated web service handler interface
- internal/web/character_handlers.go — `WebGetCharacterProfile` and the four proxies plan 04-04 added, the shape the directory proxy copies
- internal/grpcclient/client.go — the `ListAllCharacters` delegation to the core service, which stays
- cmd/holomush/deps.go — the core-client interface declaration that also names `ListAllCharacters`
- web/src/lib/scenes/directoryClient.ts — the sole TypeScript call site, and what it passes today
- internal/web/auth_handlers_test.go and internal/web/handler_test.go — the tests that exercise the removed handler
  </read_first>

  <action>
Delete `WebListAllCharacters` from `internal/web/auth_handlers.go` along with its
tests in `internal/web/auth_handlers_test.go` and `internal/web/handler_test.go`.
Do not leave a forwarder, an alias, or a deprecated stub.

Add `WebListCharacterDirectory` to `internal/web/character_handlers.go`, copying
the proxy shape already in that file: nil-client guard, token read from the
header, bounded context, field-by-field forward to
`CharacterAccessService.ListCharacterDirectory`, `errutil.LogErrorContext` plus a
bare error return with a line-scoped nolint. The gateway computes nothing — in
particular it does not filter, sort, or re-shape the list.

Update `web/src/lib/scenes/directoryClient.ts` to call the new RPC. The old call
passed a character id; the new surface is viewer-scoped and takes no character
argument, so drop that parameter and follow the change through to every caller of
this module until `bun run check` (or the repository's TypeScript check invoked by
`task lint`) is clean.

Remove `ListAllCharacters` from any narrow consumer-side interface in
`internal/web` that no longer has a caller for it, and leave
`internal/grpcclient.Client.ListAllCharacters`, `cmd/holomush/deps.go`'s
declaration where it is still used, and `CoreService.ListAllCharacters` itself
untouched. Let the compiler decide which declarations are now unused rather than
guessing: run `task build` after each removal.

Confirm the removal is total: the identifier must not survive anywhere outside
generated files that were regenerated in Task 1.
  </action>

  <verify>
    <automated>task build &amp;&amp; task test -- ./internal/web/ ./internal/grpcclient/ ./cmd/holomush/ &amp;&amp; task lint</automated>
  </verify>

  <acceptance_criteria>
- `rg -n 'WebListAllCharacters|webListAllCharacters' api/ internal/ cmd/ pkg/ web/src/ --glob '!*.pb.go' --glob '!*_pb.ts' --glob '!*.connect.go'` returns no match. The search is scoped to live source and excludes regenerated output; it deliberately does NOT sweep the whole repo, because the identifier legitimately survives in three non-consumer places: `.planning/**` (including this plan, 01-SPEC and the ROADMAP — a repo-wide search is self-invalidating), `docs/superpowers/plans/2026-06-26-scenes-web-membership-actions.md` (historical plan text, not a live consumer), and `site/src/content/docs/reference/grpc-api.md` (regenerated by Task 1, asserted separately below).
- `rg -n 'WebListAllCharacters' site/src/content/docs/reference/grpc-api.md` returns no match — the generated API reference was regenerated in Task 1 and no longer advertises the removed RPC.
- `rg -n 'webListCharacterDirectory' web/src/` matches in `directoryClient.ts` and nowhere else stale.
- `rg -n 'characterId' web/src/lib/scenes/directoryClient.ts` returns no match — the viewer-scoped call takes no character argument.
- `rg -n 'func \(c \*Client\) ListAllCharacters' internal/grpcclient/client.go` still matches, and `rg -n 'rpc ListAllCharacters' api/proto/holomush/core/v1/core.proto` still matches.
- `task build`, `task test -- ./internal/web/ ./internal/grpcclient/ ./cmd/holomush/` and `task lint` are all green.
- `git diff --stat -- api/proto/holomush/core/v1/core.proto` is empty.
  </acceptance_criteria>

  <done>
The removed RPC has no surviving handler, client method, test, or TypeScript call
site, and the replacement is reachable from the web client.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous web reader → directory RPC | Bulk enumeration by an unauthenticated caller; the response shape is the whole protection |
| directory handler → reachability evaluation | Per-character policy decision; a failure here must not silently shrink the result |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-23 | Information Disclosure | directory response message | high | mitigate | `PublicCharacterSummary` carries id and name only; the four presence-telemetry fields have no field to occupy, so the exclusion is a descriptor property rather than a handler discipline |
| T-04-01 | Information Disclosure | directory membership | high | mitigate | An unreachable character is absent, and the absence-versus-nonexistence equality is asserted by comparing whole responses |
| T-04-26 | Elevation of Privilege | bulk enumeration with no controlling decision | high | mitigate | One ABAC decision on `character_directory:all` gates the whole call before any row is read, backed by the new `seed:viewer-directory-list-characters` floor (01-SPEC §9.2), and made through the narrow `characterAccessPolicyEvaluator` seam rather than approximated by `profilevis.Reachable`; a deny is asserted to leave `ListAll` uninvoked, and a gate-evaluation failure returns `Internal` rather than falling through |
| T-04-29 | Tampering | `evaluateGate`'s infra-failure branch | high | mitigate | A DENY decision carrying an `infra:` policy id with a nil error is treated as an evaluation failure, not an ordinary denial — the masking 01-SPEC §8.10 forbids; asserted with `policytest.NewInfraFailureEngine` as the seam's double |
| T-04-04 | Tampering | reachability pass failure | medium | mitigate | An evaluation failure aborts the call with an internal status; the handler never returns a silently shortened list |
| T-04-24 | Information Disclosure | nondeterministic enumeration order | low | mitigate | Deterministic sort before return, asserted by marshaling the same response twice and comparing bytes |
| T-04-25 | Tampering | a surviving forwarder for the removed RPC | medium | mitigate | Total removal asserted by a repository-wide search excluding regenerated files; no alias, no deprecation stub |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no external packages; the TypeScript change edits an existing module and adds no dependency |
</threat_model>

<verification>
- `task lint:proto`, `task lint`, `task build` green
- `task test -- ./internal/access/policy/ ./internal/grpc/ ./internal/web/ ./internal/grpcclient/ ./cmd/holomush/` green
- This plan adds an ABAC seed policy, so the `abac-reviewer` gate fires before push (`/holomush-dev:review-abac`) — the same consequence D-76 records for 04-01
- `task test:int -- ./test/integration/access/...` green
- Repository-wide search for the removed identifier returns nothing outside regenerated output
- Generated artifacts committed
</verification>

<success_criteria>
The public directory is gated by one ABAC decision on the directory resource per
01-SPEC section 9.2, backed by a newly seeded viewer-tier floor; it then serves
identity only and lists exactly the profiles the viewer can reach;
`WebListAllCharacters` is gone from the proto, the Go tree, the tests and the web
client, with no forwarder left behind; `CoreService.ListAllCharacters` is
unchanged.
</success_criteria>

## Artifacts this phase produces

Symbols created by **this plan** (excluded from drift verification):

- Proto messages `PublicCharacterSummary`, `ListCharacterDirectoryRequest`, `ListCharacterDirectoryResponse`; RPC `CharacterAccessService.ListCharacterDirectory`
- Proto messages `WebListCharacterDirectoryRequest`, `WebListCharacterDirectoryResponse`; RPC `WebService.WebListCharacterDirectory`
- Go file `internal/grpc/characteraccess_directory.go`: method `CharacterAccessServer.ListCharacterDirectory` and func/method `evaluateGate`
- Narrow interface `characterAccessDirectoryReader` in `internal/grpc/characteraccess_service.go`, and its constructor parameter on `NewCharacterAccessServer`
- Narrow interface `characterAccessPolicyEvaluator` in `internal/grpc/characteraccess_service.go`, its `CharacterAccessServer` field, and its constructor parameter on `NewCharacterAccessServer` (satisfied at HEAD by `*policy.Engine` and by the harness's `types.AccessPolicyEngine` — no new production type)
- ABAC seed policy `seed:viewer-directory-list-characters` in `internal/access/policy/seed.go`
- Func `projectPublicSummary` in `internal/grpc/characteraccess_projection.go`
- Go method `Handler.WebListCharacterDirectory` in `internal/web/character_handlers.go`
- Go test files `internal/grpc/characteraccess_directory_test.go`, `test/integration/access/character_directory_test.go`

Symbols **removed** by this plan: proto RPC `WebService.WebListAllCharacters` and
its request/response messages; Go method `Handler.WebListAllCharacters`; the
generated TypeScript client method for it.

<output>
Create `.planning/phases/04-shared-facade-helpers-characteraccessservice/04-07-SUMMARY.md` when done
</output>
