---
phase: 04-shared-facade-helpers-characteraccessservice
plan: 06
type: execute
wave: 4
depends_on: ["04-02", "04-04", "04-05", "04-09"]
files_modified:
  - internal/grpc/characteraccess_service.go
  - internal/grpc/characteraccess_write.go
  - internal/grpc/characteraccess_write_test.go
  - test/integration/access/character_write_test.go
autonomous: true
requirements: [IDENT-02, IDENT-02a]

estimate:
  tokens: 62000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An owner edits their character's prose profile fields over the web through a typed RPC — never through the command path — and an over-cap value is rejected server-side before any store work."
    - "Prose profile-field caps are measured in BYTES using the same rule world.ValidateDescription applies: 100 bytes for the short single-line fields, 4000 bytes for the long multi-paragraph fields, with invalid UTF-8 and control characters other than newline and tab rejected."
    - "An UpdateCharacterProfile request whose update_mask is empty returns success and changes nothing, and the short-circuit happens after ownership is verified so a no-op cannot drive downstream work or act as an existence oracle."
    - "On BOTH owner-audience MUTATIONS, a caller who does not own the named character receives CHARACTER_NOT_OWNED at codes.PermissionDenied — row 3 of the gate error matrix recorded in 04-02, transcribed from 01-SPEC section 9.6 (`:2219`), which scopes that code to an owner-audience mutation. The uniform-NotFound rule (`:2223`) is scoped to CHARACTER_PROFILE_NOT_FOUND on the PUBLIC READ surface and does not reach here. The unscoped CHARACTER_NOT_FOUND row (`:2218`) does not reach here either — it is stamped only on a by-id character fetch or update, never by the ListByPlayer membership resolution, so a nonexistent id collapses into row 3 by construction (04-02 records the reconciliation). Because playerGate.ownedCharacter returns NotFound for non-ownership (sceneaccess_service.go:124), the facade carries its own mapping layer; it does not change the shared gate."
    - "That PermissionDenied is UNIFORM across all three causes — an unparseable character id, an id naming no row, and a well-formed id the caller does not own all produce the identical status AND the identical generic message — so raising the status above NotFound loses no opacity and the mutation surface is no more of an existence oracle than the read surface."
    - "An update_mask path outside the closed allowlist is rejected with an invalid-argument status; paths are matched as exact strings with no prefix, wildcard, glob or dotted-subtree expansion."
    - "Mask evaluation is order-independent and duplicate entries are idempotent: reordering a mask's paths produces an identical outcome."
    - "A guarded mutation whose expected_version is absent or zero is rejected at the RPC boundary before the request reaches the domain layer; it is never treated as an instruction to write without the guard and never silently defaulted to the row's current version."
    - "For the PROFILE write, of two concurrent mutations carrying the same expected_version exactly one succeeds: the caller's version is threaded into the domain command's version-predicated UPDATE (04-09), so this is a genuine CAS. The loser surfaces the typed concurrent-edit signal as an aborted status and is not auto-retried."
    - "For the DESCRIPTION write the guard is a TOCTOU NARROWING, not a guarantee, and the plan says so plainly: world.Service.UpdateCharacterDescription (service.go:844) takes NO expectedVersion parameter — it re-reads the row and uses its own freshly-read char.Version as the CAS guard. The facade therefore compares the caller's expected_version against the version it read and rejects a mismatch with the concurrent-edit code BEFORE calling the domain command. A stale client version is detected; a writer that lands inside the window between the facade's read and the domain's re-read is NOT detected and still last-write-wins."
    - "The in-world description write reaches the shipped world.Service.UpdateCharacterDescription rather than a parallel write path, so it inherits that path's validation and its same-transaction outbox emission."
    - "An empty description is accepted, and the resulting profile omits the description field rather than emitting an empty one."
    - "Every profile mutation commits its state change and exactly one world-change envelope in the same transaction."
  artifacts:
    - internal/grpc/characteraccess_write.go
    - internal/grpc/characteraccess_write_test.go
    - test/integration/access/character_write_test.go
  key_links:
    - "CharacterAccessServer.UpdateCharacterDescription -> world.Service.UpdateCharacterDescription -> ValidateDescription -> mutate() same-transaction outbox seam"
    - "CharacterAccessServer.UpdateCharacterProfile -> updateCharacterProfileMaskablePaths allowlist -> the 04-09 world.Service profile-attribute command (expectedVersion threaded) -> outbox kind"
    - "world.ErrConcurrentEdit / world.CodeConcurrentEdit -> codes.Aborted at the facade boundary"
  prohibitions:
    - "Never route a machine-initiated structural write from a web form or button through the human text-command parser."
    - "Never let a web mutation boundary accept an unguarded write: an absent or zero version is a rejection, not a permission."
---

<objective>
Give an owner the two edit surfaces: the prose `profile.*` fields and the
in-world `characters.description` column, both over typed RPCs, both with
server-enforced caps, and both guarded by `expected_version`.

Purpose: criterion 4. The description write MUST reach the shipped
`world.Service.UpdateCharacterDescription` rather than a parallel path, and the
profile write calls the domain command 04-09 adds.

Output: the two facade mutation handlers with the closed mask allowlist and the
byte-measured caps, and integration specs proving the row changed and exactly one
envelope committed.

**Scope note — the domain half moved to 04-09.** An earlier revision of this plan
carried a third task adding the `world.Service` profile-attribute write. That work
was under-sized by a whole layer: `world.Service.propertyRepo` is typed
`PropertyReader` (`service.go:104`) and pinned that way by
`TestServiceHoldsOnlyReaderViews` (`fence_test.go:34`), the write-capable
`PropertyRepository` lives only on `worldMutator`, and the world-envelope census
is bidirectional across three registration surfaces. It is now 04-09, at wave 2,
and this plan depends on it.

**Concurrency scope — read the second `must_haves` truth before writing Task 2.**
The profile path gets a genuine CAS. The description path gets a TOCTOU narrowing
only, because the shipped domain command takes no expected-version parameter. That
asymmetry is deliberate, is stated in the truths, and must not be papered over in
the tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-CONTEXT.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-RESEARCH.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-PATTERNS.md
@.planning/phases/01-portal-spec/01-SPEC.md
@.planning/phases/04-shared-facade-helpers-characteraccessservice/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UpdateCharacterProfile — closed mask allowlist, byte-measured caps, version guard</name>

  <files>internal/grpc/characteraccess_write.go (new), internal/grpc/characteraccess_write_test.go (new)</files>

  <read_first>
- internal/grpc/sceneaccess_service.go — `updateSceneMaskablePaths` and `UpdateScene` in full: the closed-allowlist loop, the invalid-argument rejection, and the empty-mask short-circuit placed after the ownership check with its own rationale comment
- internal/grpc/player_gate.go — `resolveAndGate` and `ownedCharacter`, noting that `ownedCharacter` returns `codes.NotFound` for non-ownership and `codes.Internal` for a repository failure, and that BOTH of those outcomes must survive this facade's mapping layer intact
- .planning/phases/04-shared-facade-helpers-characteraccessservice/04-02-PLAN.md — the section "The Phase-4 gate error matrix (surface × outcome → code)". Rows 3 and 4 govern this plan. Read it before writing any error mapping in this file.
- internal/grpc/sceneaccess_service.go `:695-706` — the shipped consumer that maps `ownedCharacter`'s NotFound to a facade-specific denial while propagating its `Internal` verbatim. This is the precedent `ownedCharacterForMutation` copies exactly.
- internal/grpc/characteraccess_owner.go and internal/grpc/characteraccess_projection.go — the owner-audience handlers and `projectOwner`, whose output this handler returns
- internal/world/validation.go — `MaxNameLength`, `MaxDescriptionLength`, `ValidateDescription`, `hasControlCharsExceptWhitespace`, and `ValidationError`
- internal/world/errors.go — `ErrConcurrentEdit`, `CodeConcurrentEdit`
- internal/world/service.go — the profile-attribute write command 04-09 added, in full: its `expectedVersion` parameter, its pre-read version guard, and its closed twelve-name set. This handler threads the caller's `expected_version` STRAIGHT INTO that parameter, which is what makes the profile path a genuine CAS rather than a narrowing.
- .planning/phases/04-shared-facade-helpers-characteraccessservice/04-09-SUMMARY.md — the exact command name and signature to call
- api/proto/holomush/characteraccess/v1/characteraccess.proto — `UpdateCharacterProfileRequest` and its field mask
- .planning/phases/01-portal-spec/01-SPEC.md sections 9.4, 9.4.1, 9.4.2, 9.4.3, 9.5, 9.6, 9.6.1
- .claude/rules/gateway-boundary.md — the structural-writes-use-typed-RPCs section
  </read_first>

  <behavior>
    - Test: an owner updating two prose fields through a two-path mask succeeds and the response carries the updated `OwnCharacter`.
    - Test: a mask path outside the allowlist is rejected with an invalid-argument status and no store work happens.
    - Test: a prefix of an allowlisted path does not reach it — an entry naming only the container is rejected rather than expanding into its members.
    - Test: an empty mask returns success and changes nothing, and the short-circuit happens after ownership; a caller who does not own the character receives the same `CHARACTER_NOT_OWNED` / `codes.PermissionDenied` denial for an empty mask too, so the no-op discloses nothing about the row.
    - Test (MEDIUM-2): a non-owned character id, an unparseable character id, and an id naming no row each return `codes.PermissionDenied`, and the three `status.Convert(err).Message()` values are asserted EQUAL to one another in a single assertion — the mapping is uniform, so raising the status above NotFound costs no opacity. Assert separately that the message does not contain the string `CHARACTER_NOT_OWNED`.
    - Test: reordering a mask's paths and duplicating an entry both produce an identical outcome to the canonical order with no duplicate.
    - Test (boundary): a short single-line field at exactly 100 bytes is accepted and at 101 bytes is rejected; a long field at exactly 4000 bytes is accepted and at 4001 bytes is rejected.
    - Test (encoding): a value whose rune count is under the cap but whose byte length is over it is rejected — caps are byte-measured, matching the domain rule.
    - Test: a value containing invalid UTF-8, and one containing a control character other than newline or tab, are each rejected.
    - Test: a request with `expected_version` absent and one with it explicitly zero are both rejected with the version-required code and an invalid-argument status, before any domain call — assert with a domain double that fails if called.
    - Test: a stale `expected_version` surfaces as an aborted status; the handler does not retry. For this path the guard is genuine: assert the domain double received the caller's `expected_version` verbatim, so a future refactor that drops it on the floor fails here.
    - Test: a guest session is denied by the shared gate's guest denial and an unauthenticated request by its unauthenticated denial; a character the caller does not own is denied with `CHARACTER_NOT_OWNED` / `codes.PermissionDenied` per the matrix row above.
  </behavior>

  <action>
Create `internal/grpc/characteraccess_write.go` with `UpdateCharacterProfile` on
`CharacterAccessServer`.

Declare `updateCharacterProfileMaskablePaths` as a package-level map keyed by the
exact path string, carrying exactly the twelve section 7.2 profile field paths
and nothing else — copying `updateSceneMaskablePaths`' shape and its rationale
comment about not letting a direct gRPC caller drive an unadvertised mutation
through the mask. Paths are compared by exact-string map lookup: no prefix
matching, no wildcard, no glob, no dotted-subtree expansion.

Gate ordering is normative and follows the precedent exactly: resolve the
session, resolve ownership, reject the version if it is absent or zero, validate
the mask against the allowlist, short-circuit an empty mask as a no-op success,
then validate values, then call the domain write. Authorization is asserted
before the mask is applied, and the empty-mask short-circuit sits after
authorization so a no-op cannot be used by an unauthorized caller to drive
downstream work or as an existence oracle.

Call the profile-attribute command 04-09 added, threading the caller's
`expected_version` straight into that command's `expectedVersion` parameter. Do
NOT re-read the character and substitute its current version — that is exactly the
defect this phase is correcting on the description path, and here it is avoidable
because the domain command accepts the caller's version natively. The facade's own
absent-or-zero rejection is a fast fail at the boundary, not the guard; the guard
is the domain command's version-predicated UPDATE.

Treat the mask as a **set**: normalize it before use so ordering is irrelevant
and a duplicated entry applies once.

Enforce the caps in the handler, where the error code and the wire status live
(D-82). Reuse the shipped world constants rather than minting new numbers: the
short single-line fields — pronouns, concept, species, age, faction, currently,
timezone — cap at `world.MaxNameLength`; the long multi-paragraph fields —
appearance, personality, biography, rumors, rp_preferences — cap at
`world.MaxDescriptionLength`. Apply the same valid-UTF-8 and
control-characters-except-newline-and-tab rules `world.ValidateDescription`
applies, and measure length in **bytes** exactly as that function does, so the
facade and the domain cannot disagree about where the boundary is. Say "bytes" in
the doc comment.

Map errors per **row 3 and row 4 of the gate error matrix recorded in
`04-02-PLAN.md`** (transcribed there from 01-SPEC section 9.6). Do not restate
the matrix; do not derive a different mapping from the shared gate's return
value. Specifically: an absent or zero version is the version-required code at
invalid-argument; an unlisted mask path is the mask-path-unsupported code at
invalid-argument; a non-owned character is `CHARACTER_NOT_OWNED` at
**`codes.PermissionDenied`**; a stale version is the shipped concurrent-edit code at
aborted — Phase 4 introduces this wire mapping, no facade carries it today, and
the loser is surfaced rather than retried, because a retry loop at the facade
would reintroduce last-write-wins one layer up. The wire message stays generic
and never interpolates an inner error; the structured reason goes to
`errutil.LogErrorContext`.

**The facade owns a mapping layer over the shared gate — do not change the gate.**
`playerGate.ownedCharacter` returns `codes.NotFound` for non-ownership
(`internal/grpc/sceneaccess_service.go:124`, moved verbatim by 04-02) and that is
correct for the owner READ surfaces, which consume it unchanged. This is a
MUTATION surface, so declare one small unexported helper in
`internal/grpc/characteraccess_write.go` — call it `ownedCharacterForMutation` —
which calls the embedded `ownedCharacter` and rewrites its outcome per the
matrix: a `codes.NotFound` becomes `CHARACTER_NOT_OWNED` at
`codes.PermissionDenied` with the handler's own generic literal, and a
`codes.Internal` is **propagated verbatim**, exactly as the shipped consumer at
`sceneaccess_service.go:700-706` does, because an infrastructure failure is row 4
of the matrix and not an authorization outcome (this is the HIGH-1 interaction:
the gate keeps its `Internal` branch and this facade must not swallow it either).
Both mutation handlers call that helper; neither re-implements the ownership
resolution, so criterion 1's one-definition property and 04-08's routing census
are untouched — the helper routes THROUGH the gate, it does not replace it.

Assert wire outcomes in the tests with `status.Code` and
`status.Convert(...).Message()`. Do not assert the wire with a chain-walking oops
helper — it resolves the deepest code in the chain and is not evidence about what
the wire carried; it remains correct for asserting which internal code the
handler produced.
  </action>

  <verify>
    <automated>task test -- -run 'UpdateCharacterProfile|Mask|Cap' ./internal/grpc/</automated>
  </verify>

  <acceptance_criteria>
- `task test -- -run 'UpdateCharacterProfile|Mask|Cap' ./internal/grpc/` passes with all twelve behaviors present.
- The non-ownership test asserts `status.Code(err) == codes.PermissionDenied`, and the three-cause message-equality assertion is a single `require.Equal` chain over the unparseable / no-such-row / non-owned messages.
- A test drives `ownedCharacter`'s repository-failure path through `ownedCharacterForMutation` and asserts the result is still `codes.Internal`, not `codes.PermissionDenied` — the row-4 propagation is pinned, not assumed.
- `rg -n 'func \(s \*CharacterAccessServer\) ownedCharacterForMutation' internal/grpc/characteraccess_write.go` returns exactly one match, and its body calls `s.ownedCharacter(` — the helper routes through the shared gate rather than re-resolving ownership.
- `rg -c '"profile\.' internal/grpc/characteraccess_write.go` shows exactly twelve allowlist entries; the SUMMARY lists them.
- The empty-mask handler path returns before any domain-write call, proven by a domain double that fails the test if its write method is invoked.
- The version-rejection tests use the same domain double and assert it was never called.
- The byte-versus-rune test uses a multi-byte value whose rune count is below the cap and whose byte length is above it.
- `rg -n 'utf8.RuneCountInString|len\(\[\]rune' internal/grpc/characteraccess_write.go` returns no match — lengths are byte-measured.
- `rg -n 'oops.AsOops|AssertErrorCode' internal/grpc/characteraccess_write_test.go` matches only in tests asserting an internal code, never in a test whose subject is the wire status or message.
- `rg -n 'HandleCommand|sendCommand' internal/grpc/characteraccess_write.go internal/web/character_handlers.go` returns no match.
  </acceptance_criteria>

  <done>
The profile edit surface accepts only advertised paths and in-range values,
rejects an unguarded write at its own entry point, and surfaces a lost update
instead of resolving it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UpdateCharacterDescription reaches the shipped world command</name>

  <files>internal/grpc/characteraccess_write.go, internal/grpc/characteraccess_write_test.go, test/integration/access/character_write_test.go (new)</files>

  <read_first>
- internal/world/service.go `:844-881` — `UpdateCharacterDescription` in full: its `Caller` parameter, its write `checkAccess`, its `ValidateDescription` call, and its `mutate` seam. Read the signature CAREFULLY: it takes `(ctx, subjectID Caller, characterID ulid.ULID, description string) error` and carries **no expected-version parameter**. At `:852` it re-reads the character and at `:871` it hands `char` — carrying the freshly-read `char.Version` — to `s.mutator.updateCharacter`. That freshly-read version, not the caller's, is the CAS guard. Everything about this task's version handling follows from that fact.
- internal/world/service.go `RetireCharacter` at `:929-933` — the contrasting command that DOES take `expectedVersion` and rejects `<= 0` before any read. This is the shape option (b) below would extend `UpdateCharacterDescription` into; this plan does not take that option.
- internal/world/caller.go — `HumanCaller` and its verbatim-forwarding contract, which matters because the subject string becomes the outbox envelope actor
- internal/access/prefix.go — `CharacterSubject`
- internal/grpc/characteraccess_service.go — the narrow world interfaces; the write half needs `UpdateCharacterDescription` added to the mutate-side interface
- test/integration/access/profile_public_read_test.go — the Ginkgo conventions and cleanup order
- internal/testsupport/integrationtest/harness.go — the harness constructor for the character facade
- .planning/phases/01-portal-spec/01-SPEC.md sections 9.3 and 9.4.3
  </read_first>

  <behavior>
    - Test: an owner setting a new description succeeds and the response carries the updated `OwnCharacter`.
    - Test: the handler reaches `world.Service.UpdateCharacterDescription` and not a repository or a parallel write — assert with a double that records the method invoked.
    - Test: a description exceeding 4000 bytes is rejected with an invalid-argument status; one at exactly 4000 bytes is accepted.
    - Test: an empty description is accepted, and a subsequent public profile read omits the description field rather than emitting an empty one.
    - Test: invalid UTF-8 and a control character other than newline or tab are each rejected.
    - Test: an absent or zero `expected_version` is rejected before the domain call — assert with a domain double that fails the test if its write method is invoked.
    - Test: a stale `expected_version` — one that does not match the version the facade read — surfaces as an aborted status with the concurrent-edit code, and the domain command is never called.
    - Test (names the limit): a writer that lands BETWEEN the facade's read and the domain command's own re-read is NOT detected. Write this as an explicit documenting test that asserts the last-write-wins outcome, so the narrowing is pinned as known behavior rather than mistaken for a guarantee. Reference the follow-up issue in the test's doc comment.
    - Integration test: the description write changes the `characters` row and commits exactly one world-change envelope in the same transaction.
  </behavior>

  <action>
Add `UpdateCharacterDescription` to `CharacterAccessServer` in the same file as
Task 2's handler, and add the method to the facade's mutate-side narrow interface
so the dependency stays declared at the consumer.

**The version-guard decision, stated explicitly — this plan takes option (a).**

`world.Service.UpdateCharacterDescription` takes no expected-version parameter
(`service.go:844`); it re-reads the row and guards on its own freshly-read
`char.Version` (`:852`, `:871`). Two options existed:

  - **(a) Facade-side compare — TAKEN.** The handler reads the character, rejects
    `req.ExpectedVersion != char.Version` with the concurrent-edit code at
    `codes.Aborted` before calling the domain command.
  - **(b) Extend the domain signature** with `expectedVersion int`, following
    `RetireCharacter`'s guard chain. NOT taken: the signature is load-bearing
    outside `world`, reaching `internal/command/types.go:70` (the command-layer
    interface), the plugin capability surface at
    `internal/plugin/hostfunc/cap_property.go:34` and its adapter at `:140`, plus
    roughly eight call sites in `internal/world/*_test.go`. That last surface is
    plugin ABI, which drags in the runtime-symmetry rule and is out of Phase 4's
    scope.

**Say plainly what (a) buys and what it does not.** It is a **TOCTOU NARROWING,
not a guarantee.** It detects a client that submits an out-of-date version — the
common real case, a stale edit form. It does NOT close the window: between the
facade's read and the domain command's own re-read, another writer can land, and
the domain will then compute its guard from the newer row and commit anyway. That
is last-write-wins inside the window, and the documenting test above pins it.

Do NOT describe this as optimistic concurrency control in the doc comment, and do
NOT let the integration spec assert an exactly-one-of-two-concurrent-writers
property for the description path — that property is FALSE here. It is asserted
only for the profile path, whose guard is real (04-09).

File a follow-up issue for option (b) with `gh issue create -R holomush/holomush
--label enhancement` naming the four call-site surfaces above, and cite its number
in both the handler doc comment and the documenting test.

**The rest of the handler.** It resolves the session, resolves ownership, rejects
an absent or zero version, applies the (a) compare, then calls the shipped
`world.Service.UpdateCharacterDescription` with
`world.HumanCaller(access.CharacterSubject(...))` for the owning character. It
does not open a parallel write path, does not touch the character repository
directly, and does not re-implement validation — `ValidateDescription` already
runs on that path, so IDENT-02a inherits the 4000-byte cap, the UTF-8 check and
the control-character rejection for free (D-82). Convert the domain's validation
error type into an invalid-argument status at the boundary with a generic
message.

Note in the doc comment that the cap is measured in bytes and that an empty
description is legal — the domain validator returns early for it — so clearing
the description is a supported edit whose result is an omitted field on the
public profile rather than an empty one.

Add `test/integration/access/character_write_test.go` as a Ginkgo spec with the
integration build tag, driving both mutations through the harness-built facade
and asserting the row changed and exactly one envelope committed.

Assert the exactly-one-of-two-concurrent-writers outcome for the **profile**
mutation ONLY, where the guard is real. Point the existing two-replica concurrency
shape at that RPC rather than writing fresh concurrency machinery: the property is
the one v0.12 already proves; only the entry point changes. Do NOT write the same
assertion for the description mutation — it would be asserting a property the code
does not have, and it would either flake or pass for the wrong reason.
  </action>

  <verify>
    <automated>task test -- -run 'UpdateCharacterDescription' ./internal/grpc/ &amp;&amp; task test:int -- ./test/integration/access/...</automated>
  </verify>

  <acceptance_criteria>
- `task test -- -run 'UpdateCharacterDescription' ./internal/grpc/` passes with all seven unit behaviors present, including the documenting test that pins the narrowing.
- `task test:int -- ./test/integration/access/...` passes with the integration behavior present.
- `rg -n 'characterRepo|CharacterRepository|propertyRepo' internal/grpc/characteraccess_write.go` returns no match — the handler reaches the domain service, not a repository.
- The routing test asserts the recorded method name equals the domain command's name, so a future parallel path fails rather than passes.
- The exactly-4000-bytes and 4001-bytes cases are both asserted, as are the empty-description acceptance and the subsequent omission on the public profile.
- The two-writer concurrency spec targets the PROFILE RPC and asserts exactly one success and exactly one aborted, not merely that one of them errored. No equivalent spec exists for the description RPC.
- The follow-up issue number for option (b) appears in both the handler doc comment and the documenting test; record it in the SUMMARY.
  </acceptance_criteria>

  <done>
The in-world description edit reaches the shipped world command with its
validation and its outbox emission intact; the profile edit's concurrency contract
is proven at the new entry point, and the description edit's weaker guarantee is
written down rather than overclaimed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web form/button → typed mutation RPC | A machine-initiated structural write; the request body is untrusted and the mask is attacker-controlled |
| facade → `world.Service` write commands | The only write path; a repository call from the facade would bypass ABAC and the outbox |
| two concurrent writers → one character row | The optimistic-concurrency predicate is the only thing preventing a silent lost update |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Elevation of Privilege | `update_mask` handling | high | mitigate | Closed exact-string allowlist of exactly twelve paths; unlisted path rejected with the mask-path-unsupported code; empty-mask short-circuit placed after ownership |
| T-04-19 | Tampering | absent or zero `expected_version` | high | mitigate | Rejected at the RPC boundary before any domain call, asserted with a domain double that fails on invocation; never defaulted to the row's current version |
| T-04-20 | Tampering | concurrent PROFILE mutation | high | mitigate | The caller's `expected_version` is threaded into the 04-09 domain command's version-predicated UPDATE — a genuine CAS. Loser surfaced as aborted; no facade-level retry, which would reintroduce last-write-wins |
| T-04-29 | Tampering | concurrent DESCRIPTION mutation | high | accept | PARTIAL: the facade-side compare narrows the window but cannot close it, because the shipped domain command re-reads and guards on its own version. A writer landing inside the read-to-re-read window still last-write-wins. Accepted for Phase 4 and pinned by a documenting test; option (b) — extending the domain signature — is filed as a follow-up issue |
| T-04-06 | Elevation of Privilege | structural write routed through the command parser | medium | mitigate | Typed RPC only; no reference to the command dispatcher in the facade or the proxy (ADR governing the gateway boundary) |
| T-04-30 | Information Disclosure | mutation-surface ownership denial | medium | mitigate | `CHARACTER_NOT_OWNED` at `PermissionDenied` per 01-SPEC `:2219`, made UNIFORM across unparseable / nonexistent / non-owned with one generic message, so the higher status costs no opacity; asserted as a three-way message equality |
| T-04-31 | Repudiation | infrastructure failure masked as an ownership denial | medium | mitigate | `ownedCharacterForMutation` propagates the gate's `codes.Internal` verbatim rather than rewriting it, copying `sceneaccess_service.go:700-706`; pinned by its own test |
| T-04-21 | Tampering | oversized or malformed prose input | medium | mitigate | Byte-measured caps reusing the shipped world constants, plus the shipped UTF-8 and control-character rules; boundary cases asserted at the cap and one byte past it |
| T-04-22 | Repudiation | a state change without its envelope | medium | mitigate | Both writes route through the same-transaction outbox seam; the integration spec asserts exactly one envelope on commit and none after a forced rollback |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no external packages; no install task exists |
</threat_model>

<verification>
- `task test -- ./internal/grpc/` green
- `task test:int -- ./test/integration/access/...` green
- `task lint` and `task build` green
</verification>

<success_criteria>
An owner can edit prose profile fields and the in-world description over the web;
over-cap and malformed input is rejected server-side in bytes; the description
write reaches the existing world command; an unguarded write is rejected rather
than silently applied; the profile path's concurrency guard is a genuine CAS and
the description path's narrowing is documented as such rather than overclaimed.
</success_criteria>

## Artifacts this phase produces

Symbols created by **this plan** (excluded from drift verification):

- Go file `internal/grpc/characteraccess_write.go`: package-level map `updateCharacterProfileMaskablePaths`; methods `CharacterAccessServer.UpdateCharacterProfile`, `CharacterAccessServer.UpdateCharacterDescription`, and the unexported mutation-surface mapping helper `CharacterAccessServer.ownedCharacterForMutation`
- `UpdateCharacterDescription` and the 04-09 profile-attribute command added to the facade's mutate-side narrow interface
- Go test files `internal/grpc/characteraccess_write_test.go`, `test/integration/access/character_write_test.go`

The taxonomy kind and the `world.Service` profile-attribute command are produced
by **04-09**, not this plan.

<output>
Create `.planning/phases/04-shared-facade-helpers-characteraccessservice/04-06-SUMMARY.md` when done
</output>
