---
phase: 03-world-character-commands
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - internal/retirement/subsystem.go
  - internal/retirement/reactor.go
  - internal/retirement/reactor_test.go
  - internal/core/session_ended_payload.go
  - internal/access/policy/seed.go
  - cmd/holomush/core.go
  - test/integration/access/evaluation_test.go
autonomous: true
requirements: [IDENT-04]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The reactor handler, given character_retired for a character with a live session, ends the session, emits leave at the OLD location (named from the deleted session's location), emits session_ended with cause retired, then moves the character to the configured starting location — in that order, unit-proven with fakes (D-36/D-37/D-38; the observable end-to-end proof is plan 03-06)"
    - "Redelivering the same character_retired message produces no second leave and no second move — every effect is gated on observed state (D-36 at-least-once consequence; unit-proven here, redelivery spec in 03-06)"
    - "A character un-retired between emit and delivery is NOT evicted: the reactor acks and skips when the current status is no longer retired"
    - "Retire and unretire are ADMIN-ONLY in v0.13: an admin principal is permitted both actions via the existing seed:admin-full-access bare-action permit, and a non-admin character principal is DENIED retire even on their OWN character — default-deny proven with a paired admin positive control (IDENT-04 as reworded; user decision 2026-08-07 superseding D-40's player-retires half; PORTAL-10 rule 2)"
    - "The reactor's world writes are wired through 02.2's job caller carrying the D-55-stamped provenance — no ABAC bypass, no system-namespace principal (D-47; the instance-scope DENY spec is plan 03-06)"
  artifacts:
    - path: "internal/retirement/reactor.go"
      provides: "Durable-consumer handler: status guard, session teardown, leave/session_ended fanout, job-authorized move"
      contains: "character_retirement_reactor"
    - path: "internal/core/session_ended_payload.go"
      provides: "SessionEndedCauseRetired const alongside the six existing causes"
      contains: "SessionEndedCauseRetired"
    - path: "test/integration/access/evaluation_test.go"
      provides: "Admin-only retire/unretire evaluation specs: admin positive control + non-admin deny on own character"
      contains: "retire"
  key_links:
    - from: "internal/retirement/reactor.go"
      to: "internal/eventbus/consumer"
      via: "durable consumer created through the shared D-46 helper (also 02.2's provenance stamp boundary per D-55)"
      pattern: "consumer\\.CreateWithRetry"
    - from: "internal/retirement/reactor.go"
      to: "internal/world/service.go"
      via: "MoveCharacter called with the 02.2 job caller carrying the provenance triple"
      pattern: "MoveCharacter"
    - from: "cmd/holomush/core.go"
      to: "internal/retirement/subsystem.go"
      via: "composite literal supplies the reactor's real deps (session store, presence emitter, world service, StartLocationID)"
      pattern: "retirement\\.Config"
  prohibitions:
    - statement: "The reactor MUST NOT authorize via an ABAC bypass or a system-namespace principal — no ambient system-context marker on any reactor path and no new system-namespace permit; D-45 is struck and MUST NOT be re-derived (02.2 success criterion 1)"
      status: kept
      requirement_id: IDENT-04
      category: safety
    - statement: "The reactor MUST NOT borrow the originating player's actor identity off the envelope for its own writes — a player authorized to retire was never authorized to end sessions or move characters (D-47 candidate 2 rejection)"
      status: kept
      requirement_id: IDENT-04
      category: safety
---

<objective>
Make retirement effective (D-36): fill the `internal/retirement` skeleton from plan 03-02 with the durable JetStream consumer and the sessions-only v1 fanout — end sessions, notify the old location, `session_ended` with a new `retired` cause, then move the character to the configured starting location through `world.Service.MoveCharacter` (D-37/D-38) — idempotent under at-least-once redelivery. Authorize the move under Phase 02.2's job-identity model (D-47): `JobCaller` + the per-execution provenance triple, instance-scoped by policy. Land this phase's authorization surface: retire/unretire are ADMIN-ONLY for v0.13 (user decision 2026-08-07 — see review_disposition) via the existing `seed:admin-full-access`, plus the `job:retirement` grant if 02.2's fixture-only scope left it to this phase (interlock §0.4-2).

Purpose: IDENT-04's "leaves active play" is a lie without the reactor; ROADMAP success criterion 2 requires it observable — this plan lands the behavior with unit-level proof, and plan 03-06 owns the full-stack observable proof (harness StartOptions + retirement suite).
Output: A live subsystem consuming `events.*.character.>` wired in core.go, unit-proven fanout/idempotency/status-guard behavior, admin-only evaluation specs with paired controls, and the abac-reviewer-gated seed surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-world-character-commands/03-CONTEXT.md
@.planning/phases/03-world-character-commands/03-RESEARCH.md
@.planning/phases/03-world-character-commands/03-PATTERNS.md
@.planning/phases/02.2-background-job-authorization-model/02.2-CONTEXT.md
</context>

<upstream_reconciliation_note>
**Binding interlocks (RESEARCH §0.3/§0.4 — consume, never invent).** At execution time Phases 02.1 and 02.2 have landed. Reconcile ALL `[ASSUMED]` shapes against the landed tree before writing code: (a) the job-caller constructor name/arity (assumed `world.JobCaller("retirement", prov)`); (b) WHERE the provenance triple (`trigger_event_id`/`trigger_event_type`/`trigger_subject`) is stamped — D-55 designates the D-46 consumer wrapper, stamped BEFORE handler logic so the handler cannot alter it; consume 02.2's landed stamp seam, and only if 02.2 stamped its fixture elsewhere and left the wrapper stamp-less, add the stamp at the wrapper boundary mirroring 02.2's fixture shape byte-for-byte; (c) the `trigger_subject` normalization — 02.2's fixture settles whether it is the bare aggregate ULID (comparable to `resource.id` with `==`) or the full qualified subject; copy the fixture's landed normalization exactly (A11); (d) the job-registration API (capability class `{name: "retirement", writes: […]}`, D-50) and the job deny-code shape (D-58). Record every reconciliation delta in the SUMMARY.
</upstream_reconciliation_note>

<review_disposition>
**U1 — USER DECISION (2026-08-07, interactive, supersedes RESEARCH §8.4's ship-both-seeds recommendation and D-40's player-retires half): ADMIN-ONLY RETIRE for v0.13.** Player self-retire does NOT ship. Consequences applied to this plan: the formerly planned player self-retire seed is REMOVED; both `retire` and `unretire` are admin-only actions covered by the existing `seed:admin-full-access` bare-action permit (no new seed needed for the human surface); the ABAC actions stay DISTINCT (D-40's action split survives — only the principal changes; D-40's admin-unretire half stands as-is); the deny proof is retargeted at the admin-action surface — a non-admin character principal is DENIED retire even on their own character, proving default-deny with a paired admin positive control. IDENT-04 was reworded in REQUIREMENTS.md by maintainer authorization to match ("admin-driven in v0.13; player self-retire deferred beyond v0.13") and remains fully covered by admin retire/unretire + the reactor + the reversibility proofs.

**SG-1/SG-2 (false-green `-run` filter; `integrationtest.Start(t)` does not run the relay or the new subsystems — `internal/testsupport/integrationtest/plugins.go:265-266` says so verbatim, and the harness hand-assembles its stack with no `productionSubsystems`). ADOPTED: the full-stack integration proof moves OUT of this plan into new plan 03-06**, which owns harness `StartOption`s (`WithOutboxRelay`, `WithRetirementReactor`) with the harness files in its `files_modified`, and a NEW suite package `test/integration/retirement/` whose entry function `TestRetirementReactor` makes the `-run` filter real (one `RunSpecs` per package — no second-RunSpecs hazard, and no second competing stack inside `test/integration/world/`'s hand-built `testEnv`). This plan keeps the reactor behavior + wiring + unit proof + the access-evaluation specs, which need no relay.

**Codex 03-04 HIGH "move failure disposition is unspecified" — ADOPTED:** Task 1's behavior now pins it: MoveCharacter failure (authz, validation, persistence) → nack/redeliver, safe under the per-effect skip gates; a non-retryable DENY → log + ack with the deny rationale. An executor never acks a retryably-failed move — that would strand the character at the old location.

**Codex 03-04 HIGH "crash idempotency incomplete" — ACCEPTED AS BEST-EFFORT:** a crash after DeleteByCharacter but before EmitLeave/EmitSessionEnded loses those notifications on redelivery (`(nil, nil)` → skip) — accepted, matching the eight existing synchronous fanout sites' semantics; no durable progress state is introduced, and 03-06 spec 3's exact-count (==1) assertion is a happy-path assertion, not a durability contract.
</review_disposition>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reactor consumer + handler with per-effect idempotency (unit level)</name>
  <precondition>Phase 02.2 is complete: its job registry, provenance vocabulary, and fixture job are readable in-tree (locate them via `rg -n "job" internal/access/policy/ --type go -l` and the 02.2 phase SUMMARYs). If absent, STOP — phase-order violation.</precondition>
  <files>internal/retirement/subsystem.go, internal/retirement/reactor.go, internal/retirement/reactor_test.go, internal/core/session_ended_payload.go</files>
  <behavior>
    - character_retired for a character with a live session: DeleteByCharacter returns Info → EmitLeave at Info's location with reason "retired" → EmitSessionEnded with cause "retired" → MoveCharacter to StartLocationID; leave strictly BEFORE the move (discretion: the notification names the place they left)
    - Redelivery: DeleteByCharacter returns (nil, nil) → leave and session_ended are SKIPPED; character already at the starting location → move SKIPPED
    - Status guard first: current status is not retired (un-retired between emit and delivery) → ack-and-skip everything; status read fails → nack/redeliver path, no partial fanout
    - MoveCharacter failure (authz, validation, persistence): nack/redeliver — the per-effect skip gates make the retry safe; a non-retryable DENY → log via errutil.LogErrorContext + ack with the deny rationale (never ack a retryably-failed move — that strands the character at the old location)
    - Events with Type other than character_retired → fast ack-and-skip
    - Offline retiree (no session): no leave, no session_ended, move still runs
    - Unknown/unparsable status value → denying default (INV-WORLD-5 exhaustive-read rule via world.ParseStatus)
  </behavior>
  <action>
    RED: write reactor_test.go against consumer-defined fakes covering every behavior above; commit `test(03-04)`. GREEN: implement in package retirement.

    Consumer wiring (Prepare): via the injected JetStream handle, `js.CreateOrUpdateConsumer(eventbus.StreamName, jetstream.ConsumerConfig{Durable/Name: "character_retirement_reactor", FilterSubject: "events.*.character.>", AckPolicy: AckExplicitPolicy, AckWait/MaxAckPending/MaxDeliver from Config with defaults mirroring the audit projector})` wrapped in `consumer.CreateWithRetry` (the D-46 helper — also the D-55 provenance stamp boundary, see the reconciliation note). Activate starts the Consume loop; Stop drains per the relay/audit precedent already shaped in the 03-02 skeleton.

    Handler (reactor.go): switch on `Event.Type`; for character_retired, derive the character ULID from the handler's OWN processing of the delivered event (never trust caller-supplied provenance — D-55's tamper posture), then run the guarded fanout per RESEARCH §5.3's idempotency table: (1) status guard — read the character through the world read surface as the job caller and `world.ParseStatus` the result with a denying default; skip unless retired; (2) `Store.DeleteByCharacter(ctx, characterID)` — `(nil, nil)` means nothing to do; (3) when Info is non-nil, `EmitLeave(ctx, core.CharacterRef{…from Info + the read character…}, "retired")` then `EmitSessionEnded(ctx, ref, sessionID, SessionEndedCauseRetired, "retired")` — log-warn on emit errors (fanout degradation is operational, per the eight existing fanout sites' handling); (4) move: skip when already at the starting location, else `MoveCharacter(ctx, <job caller>, characterID, startLocationID)`. All logging via slog `*Context` variants and `errutil.LogErrorContext`. Crash-window disposition (accepted): a crash after step (2)'s DeleteByCharacter but before step (3)'s emits means redelivery sees `(nil, nil)` and skips the notifications permanently — that loss is ACCEPTED as best-effort, matching the eight existing synchronous fanout sites; do NOT invent durable progress/outbox state for the notifications, and 03-06 spec 3's exact-count (==1) assertion is a happy-path assertion, not a durability contract.

    Dependency seams (extend the 03-02 Config; consumer-defined interfaces in package retirement, narrow, mirroring internal/auth/auth_service.go:26-29's PresenceEmitter shape): `PresenceEmitter{EmitLeave, EmitSessionEnded}`, `SessionEnder{DeleteByCharacter}`, a world surface exposing the character read + MoveCharacter (caller-shaped per landed 02.1), `StartLocationID func() ulid.ULID` (safe to call at handle time — DependsOn Bootstrap was declared in 03-02; it panics before bootstrap Prepare, RESEARCH Pitfall 8), and a JetStream provider.

    internal/core/session_ended_payload.go: add `SessionEndedCauseRetired = "retired"` to the const block AND update the struct field comment at :20 that lists the causes inline (RESEARCH §5.4).
  </action>
  <verify>
    <automated>task test -- ./internal/retirement/ ./internal/core/</automated>
  </verify>
  <done>All handler behaviors pass at unit level with fakes; the cause const + comment landed; commit pair test(03-04)/feat(03-04) present.</done>
  <acceptance_criteria>
    - `rg -q 'SessionEndedCauseRetired' internal/core/session_ended_payload.go` succeeds and the inline cause list comment mentions retired
    - `rg -q 'character_retirement_reactor' internal/retirement/` succeeds (durable name pinned)
    - `rg -q 'consumer\.CreateWithRetry' internal/retirement/` succeeds (no fresh backoff loop — Don't Hand-Roll)
    - Unit tests assert leave-before-move ordering and each skip gate independently
    - `! rg -q 'WithSystemSubject' internal/retirement/` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Authorization surface — job caller wiring, registration, and the admin-only human surface (U1)</name>
  <reversibility rating="costly">A durable JetStream consumer name and two policy grants; removing the subsystem later strands the consumer (D-36 rating carried from CONTEXT).</reversibility>
  <precondition>Read 02.2's landed seed/registration artifacts first: `rg -n "retirement|job" internal/access/policy/seed.go` plus the 02.2 SUMMARYs — determine whether a real job:retirement grant already shipped (interlock §0.4-2 / assumption A9). If it shipped, consume it and skip creating a duplicate.</precondition>
  <files>internal/retirement/reactor.go, internal/access/policy/seed.go, cmd/holomush/core.go, test/integration/access/evaluation_test.go</files>
  <action>
    Three pieces. (1) Job identity: register the reactor with 02.2's job registry under name "retirement" with its declared capability class listing the character write (D-50 shape); declare session teardown honestly — the session store has NO policy chokepoint, so the declaration narrows nothing there and the plan MUST NOT claim policy-authorized teardown (D-53: claim no more than the bindings prove; RESEARCH §0.3 table). Wire the handler's world calls through the job caller constructor with the provenance the D-55 stamp boundary supplies. (2) Human authorization surface, per U1 (admin-only for v0.13): NO new player-facing seed ships — admins are already covered by `seed:admin-full-access`'s bare-action permit (seed.go:105-109), which matches the distinct `retire` and `unretire` actions for free. Add access-evaluation specs to test/integration/access/evaluation_test.go proving the admin-only surface with paired controls (PORTAL-10 rule 2): (a) positive control — an admin principal is PERMITTED retire and unretire on an arbitrary character; (b) DENY — a non-admin character principal is DENIED retire on their OWN character (no self-grant exists; this is the default-deny proof U1 requires); (c) DENY — a non-admin character principal is DENIED unretire (D-40's admin-only-unretire half). If §0.4-2 resolved to Phase 3: add the job:retirement grant in 02.2's landed vocabulary, instance-scoped per D-54 — the when-clause binds the provenance event type to character_retired AND the provenance subject to the resource id (exact spelling per 02.2's landed fixture seed; include the read the status guard performs, still instance-scoped). Every seed uses only registered namespaces (seed coverage validation fails loudly otherwise). (3) core.go: extend the composite-literal wiring with the reactor's real deps (session store, presence emitter, world service, StartLocationID from the bootstrap subsystem, JetStream provider) mirroring how neighbouring subsystems resolve live values at their own Start (07-09 pattern — no eager resolution at construction).

    D-39 consequence to honor in review: the commands carry no ownership predicate — policy IS the control, and in v0.13 that control is admin-only by decision, so the deny specs are what keep it honest. Keep any job permit as narrow as the DSL allows (a blanket action list over every character resource is the ceiling, not the target). Touching seed.go fires the `abac-reviewer` gate (`/holomush-dev:review-abac`) before push — note it in the SUMMARY for the ship step.
  </action>
  <verify>
    <automated>task test -- ./internal/access/... ./internal/retirement/ ./cmd/holomush/ && task test:int -- ./test/integration/access/</automated>
  </verify>
  <done>Job registration + caller wiring compile against 02.2's landed surface; the job:retirement grant present (or consumed from 02.2); admin-only evaluation specs green with paired controls; core.go wiring complete.</done>
  <acceptance_criteria>
    - Evaluation tests: admin principal → PERMIT for both retire and unretire on an arbitrary character (positive control); non-admin character principal → DENY for retire on their own character AND for unretire (default-deny proof per U1, PORTAL-10 rule 2)
    - `! { git diff -U0 origin/main...HEAD -- internal/access/policy/seed.go | rg '^\+' | rg -q 'principal is system'; }` succeeds — no new system-namespace permit among ADDED lines (diff-scoped so existing bootstrap seeds cannot trip it)
    - The only seed.go additions (if any) are the §0.4-2 job:retirement grant in 02.2's landed vocabulary — no new human-principal permit for retire or unretire ships in v0.13 (U1)
    - `! rg -q 'WithSystemSubject' internal/retirement/` succeeds
    - `task test -- ./cmd/holomush/` exits 0 (wiring did not break the 20-subsystem pins)
  </acceptance_criteria>
</task>

</tasks>

<moved_scope_note>
The full-stack integration proof (fanout, redelivery idempotency, feed order, instance-scope DENY) formerly planned here as Task 3 is now plan 03-06 — see review_disposition SG-1/SG-2. It requires harness `StartOption`s this plan does not own.
</moved_scope_note>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JetStream delivery → reactor handler | at-least-once, unauthenticated-within-process event data drives privileged effects |
| reactor → world.Service | a background job crosses the ABAC chokepoint (the ONLY gated effect — session/presence have no chokepoint today) |
| seed.go | policy text IS the ownership control (D-39) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Elevation of Privilege | reactor authorization | high | mitigate | job identity under 02.2's model; no bypass, no system-namespace principal (D-45 struck); negative gates here + the instance-scope DENY spec in plan 03-06 |
| T-03-11 | Elevation of Privilege | job:retirement grant breadth | high | mitigate | D-54 instance scoping: the when-clause binds provenance event type AND provenance subject to the resource — a blanket grant is the ceiling, not the target; abac-reviewer gate fires |
| T-03-12 | Tampering | handler-supplied provenance | medium | mitigate | D-55: triple stamped at the consumer boundary BEFORE handler logic; handler derives the aggregate independently, so a wrong derivation is DENIED, not self-authorized |
| T-03-13 | Tampering / DoS | redelivery double-effects | medium | mitigate | per-effect observed-state gates (RESEARCH §5.3); unit proof here, redelivery spec in 03-06 |
| T-03-14 | Tampering | eviction of an un-retired character | medium | mitigate | status guard with denying default before any effect |
| T-03-15 | Elevation of Privilege | over-broad human retire surface | high | mitigate | U1: NO human-principal retire/unretire permit ships beyond the existing admin bare-action seed; evaluation specs prove non-admin DENY on own character with paired admin positive control |
</threat_model>

<verification>
- `task test -- ./internal/retirement/ ./internal/core/ ./internal/access/... ./cmd/holomush/`
- `task test:int -- ./test/integration/access/`
- The full-stack reactor gate (`task test:int -- -run TestRetirementReactor ./test/integration/retirement/`) is OWNED BY PLAN 03-06 — do not claim it here
- `abac-reviewer` (`/holomush-dev:review-abac`) MUST run before push — this plan touches seed.go
- `task lint` before every commit
</verification>

<success_criteria>
- The reactor behavior (fanout order, redelivery skip gates, status guard, offline-retiree path) is unit-proven; the end-to-end observable proof is plan 03-06
- The move authorizes as a job under 02.2's model with no bypass and no system-namespace principal
- Retire/unretire are admin-only in v0.13 with the deny/positive evaluation pair green (U1); zero new domain code carries an ownership predicate (D-39)
- core.go supplies the reactor's real production deps — no dangling skeleton wiring
</success_criteria>

<output>
Create `.planning/phases/03-world-character-commands/03-04-SUMMARY.md` when done. Record: every [ASSUMED]→landed reconciliation delta (constructor names, stamp site, trigger_subject normalization, deny-code shape), which branch of interlock §0.4-2 fired (grant created vs consumed), and the abac-reviewer verdict.
</output>

## Artifacts this phase produces (plan 03-04)

New symbols created by this plan (source-grounding: exclude from drift verification):
- `internal/retirement`: `reactor.go` handler, consumer-defined interfaces (`PresenceEmitter`, `SessionEnder`, world surface), extended `Config` fields, durable consumer name `character_retirement_reactor`
- `internal/core`: `SessionEndedCauseRetired`
- `internal/access/policy`: `job:retirement` grant (only if interlock §0.4-2 resolves to Phase 3); NO new human-principal retire/unretire seed (U1)
- Job registration: capability class `retirement` in 02.2's registry
- Test files: `internal/retirement/reactor_test.go`, new specs in `test/integration/access/evaluation_test.go`
