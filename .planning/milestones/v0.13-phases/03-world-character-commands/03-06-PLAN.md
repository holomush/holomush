---
phase: 03-world-character-commands
plan: 06
type: execute
wave: 4
depends_on: ["03-04", "03-05"]
files_modified:
  - internal/testsupport/integrationtest/harness.go
  - internal/testsupport/integrationtest/options.go
  - internal/testsupport/integrationtest/plugins.go
  - test/integration/retirement/retirement_suite_test.go
  - test/integration/retirement/retirement_reactor_test.go
autonomous: true
requirements: [IDENT-04, IDENT-10]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Publishing character_retired for a character with a live session OBSERVABLY ends the session, emits leave at the OLD location, emits session_ended with cause retired, and moves the character to the configured starting location — proven end-to-end through the REAL outbox relay → durable consumer → handler chain, never a synthetic shortcut event (D-36/D-37/D-38; ROADMAP success criterion 2)"
    - "Redelivering the same character_retired event produces no second leave and no second move — exactly one leave and one move after the second delivery (D-36 at-least-once consequence)"
    - "For one retirement flow, the character_retired envelope precedes character_moved in the world feed (feed_position order — ordering edge, IDENT-10)"
    - "The reactor's MoveCharacter authorizes under its OWN job identity and is DENIED against an aggregate other than the one whose event it is handling, with a paired positive control (D-47; 02.2 D-53 claim b / D-54 instance scoping)"
    - "The integrationtest harness can boot the real OutboxRelaySubsystem and the real retirement subsystem via owned StartOptions — the SG-2 gap (harness hand-assembles its stack; relay 'not started by this harness') is closed by owned code, not worked around"
  artifacts:
    - path: "internal/testsupport/integrationtest/options.go"
      provides: "WithOutboxRelay + WithRetirementReactor StartOptions booting the real subsystems inside Start"
      contains: "WithRetirementReactor"
    - path: "test/integration/retirement/retirement_suite_test.go"
      provides: "Suite entry func TestRetirementReactor + the package's single RunSpecs — a REAL -run filter target (SG-1)"
      contains: "func TestRetirementReactor"
    - path: "test/integration/retirement/retirement_reactor_test.go"
      provides: "Four Describes: fanout, feed order, redelivery idempotency, instance-scope DENY + positive control"
      contains: "character_retired"
  key_links:
    - from: "test/integration/retirement/retirement_reactor_test.go"
      to: "internal/testsupport/integrationtest/options.go"
      via: "specs boot the stack with Start(t, WithOutboxRelay(), WithRetirementReactor(), …)"
      pattern: "WithOutboxRelay"
    - from: "internal/testsupport/integrationtest/harness.go"
      to: "internal/world/setup/relay_subsystem.go"
      via: "WithOutboxRelay constructs the REAL setup.NewOutboxRelaySubsystem over the harness pool + embedded bus publisher"
      pattern: "NewOutboxRelaySubsystem"
    - from: "internal/testsupport/integrationtest/harness.go"
      to: "internal/retirement/subsystem.go"
      via: "WithRetirementReactor constructs the REAL retirement subsystem with harness-backed production-shaped deps"
      pattern: "retirement\\.NewSubsystem"
---

<objective>
Prove ROADMAP success criterion 2 end-to-end: land the integrationtest harness `StartOption`s that boot the REAL outbox relay and the REAL retirement subsystem (closing the SG-2 gap — `internal/testsupport/integrationtest/plugins.go:265-266` documents that the relay is "a separate subsystem not started by this harness", and no plan owned a fix), then write the full-stack retirement suite in a NEW package `test/integration/retirement/` whose entry function `TestRetirementReactor` makes the `-run` acceptance filter REAL (closing SG-1's false-green: `test/integration/world/` has exactly one test function, `TestWorld`, so a `-run TestRetirementReactor` filter there runs nothing and exits 0).

Purpose: plan 03-04 landed the reactor behavior with unit proof; without this plan the phase's "retirement is observably effective" claim is asserted, not demonstrated, and success criteria 2's full-stack proof is unexecutable (per 03-REVIEWS.md SG-2).
Output: Two owned harness StartOptions, a one-RunSpecs suite package, and four green specs driven through the real relay → durable consumer → handler chain.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-world-character-commands/03-CONTEXT.md
@.planning/phases/03-world-character-commands/03-RESEARCH.md
@.planning/phases/03-world-character-commands/03-PATTERNS.md
@.planning/phases/03-world-character-commands/03-04-SUMMARY.md
@.planning/phases/03-world-character-commands/03-05-SUMMARY.md
</context>

<review_disposition>
**This plan IS the disposition of 03-REVIEWS.md SG-1 + SG-2 (both HIGH) and Codex's harness-wiring MEDIUM.** The checker's fix — "add an owned task (with the harness files in files_modified) landing a StartOption that boots the relay + the new subsystems" — lands here as its own plan because 03-04 was already at its context budget and the harness work plus suite is a single coherent concern. Verified seams this plan builds on: `StartOption func(*startConfig)` (harness.go:213-215) with precedent options in options.go (WithExternalNATS, WithSharedDatabase); `setup.NewOutboxRelaySubsystem(cfg)` with `PoolProvider{Pool()}` (setup/subsystem.go:21-23) and `EventBusProvider{Publisher(...)}` (relay_subsystem.go:34-36) and Prepare/Activate/Stop lifecycle (relay_subsystem.go:86-140); the embedded bus handle `eventbustest.Embedded{Bus, JS, Conn}` (harness.go:493) exposed via `Server.Bus()`; `Server.Pool()`, `Server.SessionStore()`, `Server.GameID()`. The redelivery proof uses EXPLICIT handler re-invocation (pi suggestion 7 — the JetStream NAK/ack path is unit-covered in 03-04; an un-acked-redelivery variant would couple the spec to AckWait timing).

**Depends on 03-05 (not only 03-04):** 03-05 lands `WithCharacterActivity` in the same `internal/testsupport/integrationtest/options.go` this plan extends — the dependency serializes the shared files across waves (file-ownership rule), and this plan mirrors its option shape.
</review_disposition>

<tasks>

<task type="auto">
  <name>Task 1: Harness StartOptions — WithOutboxRelay + WithRetirementReactor</name>
  <precondition>Plans 03-04 and 03-05 are complete: `rg -q 'func WithCharacterActivity' internal/testsupport/integrationtest/` succeeds (03-05's option shape to mirror) and `internal/retirement/reactor.go` exists with the extended Config seams.</precondition>
  <files>internal/testsupport/integrationtest/harness.go, internal/testsupport/integrationtest/options.go, internal/testsupport/integrationtest/plugins.go</files>
  <action>
    Two StartOptions in internal/testsupport/integrationtest (mirror 03-05's WithCharacterActivity shape: a startConfig field + a Start-path branch + t.Cleanup(Stop)).

    `WithOutboxRelay()`: constructs the REAL `setup.NewOutboxRelaySubsystem(setup.OutboxRelaySubsystemConfig{DB: <small adapter returning s.Pool() — setup.PoolProvider is Pool() *pgxpool.Pool>, EventBus: <adapter forwarding to the embedded bus's Publisher — relay_subsystem.go:34-36's EventBusProvider>, GameID: func() string { return <harness game id> }, Logger: slog.Default()})`, then calls Prepare(ctx) and Activate(ctx) after the database and bus are up, and registers Stop in cleanup. Update the stale half of the comment at plugins.go:265-266 ("The relay itself is a separate subsystem not started by this harness") to note the WithOutboxRelay exception — the comment is the SG-2 tripwire and must not survive stale.

    `WithRetirementReactor()`: constructs the REAL `retirement.NewSubsystem` with production-SHAPED deps resolved from the harness stack (reconcile field names against 03-04's landed Config): JetStream handle from the embedded bus (`s.Bus().JS`); SessionEnder = the harness session store (`Server.SessionStore()`); PresenceEmitter = a REAL `presence.NewEmitter(<embedded bus publisher>, <gameID func>)` — deliberately NOT the harness's own presence emitter, which wraps a noopPublisher (harness.go:630), so the reactor's leave/session_ended events are observable on the bus; the world surface = the world.Service the active harness path built (plugins path, plugins.go:267-277) or, when absent, a service constructed with the identical ServiceConfig wiring including OutboxWriter; StartLocationID = a harness-created location resolved during Start and exposed via a new accessor `Server.RetirementStartLocation() ulid.ULID` so specs can assert the move target. Prepare + Activate + cleanup Stop. Keep the two options ORTHOGONAL (neither implies the other); the retirement suite passes both.

    Job authorization inside the harness: the reactor's MoveCharacter crosses the real ABAC chokepoint. Under the default allow-all engine the fanout specs pass trivially at the gate; the DENY spec requires `WithRealABAC()` plus the seeded job:retirement grant from 03-04/02.2 — verify the real-ABAC path seeds it (it seeds from the production seed corpus) and note in the option's doc comment which engine each spec class needs.
  </action>
  <verify>
    <automated>task test:int -- ./internal/testsupport/integrationtest/</automated>
  </verify>
  <done>Both options compile and boot inside Start; the plugins.go relay comment reflects the new exception; the harness's own integration-tagged tests stay green.</done>
  <acceptance_criteria>
    - `rg -q 'func WithOutboxRelay' internal/testsupport/integrationtest/` and `rg -q 'func WithRetirementReactor' internal/testsupport/integrationtest/` both succeed
    - `rg -q 'WithOutboxRelay' internal/testsupport/integrationtest/plugins.go` succeeds (the stale "not started by this harness" claim now names its exception)
    - `rg -q 'RetirementStartLocation' internal/testsupport/integrationtest/harness.go` succeeds
    - `task test:int -- ./internal/testsupport/integrationtest/` exits 0 (decide by EXIT CODE)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full-stack retirement suite — fanout, feed order, redelivery idempotency, instance-scope DENY</name>
  <precondition>Docker available (`docker info` succeeds) — full-stack integrationtest harness requires the Postgres testcontainer + embedded NATS.</precondition>
  <files>test/integration/retirement/retirement_suite_test.go, test/integration/retirement/retirement_reactor_test.go</files>
  <action>
    New package `retirement_test` under test/integration/retirement/ (`//go:build integration` on every file). retirement_suite_test.go: `func TestRetirementReactor(t *testing.T)` with `RegisterFailHandler(Fail); RunSpecs(t, "Retirement Reactor Suite")` — the package's ONLY RunSpecs and the real `-run` target (SG-1). retirement_reactor_test.go: Ginkgo Describes over `integrationtest.Start(t, integrationtest.WithOutboxRelay(), integrationtest.WithRetirementReactor(), …)`.

    Specs: (1) Fanout: create a player + character with a live session at a location (harness ConnectAuthed helpers), open a bus subscription BEFORE acting (so leave/session_ended frames are captured), read the character and retire via `Service.RetireCharacter(ctx, caller, id, char.Version)` (03-01's caller-supplied expected_version), let the REAL relay carry the envelope — do NOT publish a synthetic character_retired event to shortcut the relay — then Eventually assert: session row gone, a leave event at the OLD location, a session_ended event with cause retired, and characters.location_id equal to `Server.RetirementStartLocation()` (read via the pool; presence is session-derived — never read characters.location_id for presence claims, terminology rule). (2) Feed order: query the outbox rows for the flow and assert the character_retired envelope's feed position precedes character_moved's (ordering edge, IDENT-10). (3) Redelivery idempotency: invoke the handler entry DIRECTLY a second time with the recorded event (explicit re-invocation — the chosen proof per pi suggestion 7; do not rely on AckWait-timed JetStream redelivery) and assert exactly one leave and one move resulted overall. (4) Instance-scope DENY (02.2 D-53 claim b): under `WithRealABAC()`, construct the job caller with provenance for aggregate X and attempt the move against character Y — expect the deny with 02.2's landed job deny-code shape (D-58), PAIRED with the positive control (same provenance, aggregate X → permitted). Use Eventually for all async assertions (the reactor is downstream of the outbox relay — genuinely eventually consistent, D-36 accepted consequence); decide pass/fail by EXIT CODE, never by grepping runner output.
  </action>
  <verify>
    <automated>task test:int -- -run TestRetirementReactor ./test/integration/retirement/</automated>
  </verify>
  <done>All four specs green against the real relay → consumer → handler chain; the DENY spec consumes 02.2's landed deny-code shape; the -run filter demonstrably matches the suite entry.</done>
  <acceptance_criteria>
    - `rg -c 'RunSpecs' test/integration/retirement/` returns 1 and `rg -q 'func TestRetirementReactor' test/integration/retirement/retirement_suite_test.go` succeeds (SG-1 closed: the filter target is a real test function)
    - Spec 1 asserts all four observable effects (session gone, leave at old location, session_ended cause retired, character at the starting location)
    - Spec 2 asserts feed-position ordering between the two envelopes from actual outbox rows
    - Spec 3 asserts leave count == 1 and move count == 1 after the second delivery
    - Spec 4 has BOTH the deny and its positive control in the same spec file
    - `task test:int -- -run TestRetirementReactor ./test/integration/retirement/` exits 0 (decide by EXIT CODE)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in production) | this plan writes test-harness seams and specs; production surfaces are 03-04's |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-21 | Repudiation | ROADMAP success criterion 2 | medium | mitigate | this plan IS the mitigation: the full-stack specs turn 03-04's T-03-10/T-03-11/T-03-13 mitigations from unit-asserted to observably demonstrated |
| T-03-22 | Tampering | harness options masking production gaps | low | mitigate | the options construct the REAL subsystems with production-shaped deps (real relay, real presence emitter over the bus, real world.Service with OutboxWriter) — no stub stand-ins; the no-synthetic-event rule keeps the relay in the loop |
</threat_model>

<verification>
- `task test:int -- ./internal/testsupport/integrationtest/` — harness still green with the new options
- `task test:int -- -run TestRetirementReactor ./test/integration/retirement/` — the phase's full-stack retirement gate (owned here, not by 03-04)
- `task lint` before every commit
</verification>

<success_criteria>
- ROADMAP success criterion 2 demonstrated end-to-end through the real subsystem chain (relay → durable consumer → handler → world.Service)
- The reactor is idempotent under redelivery and its job authorization is instance-scoped, both proven with paired controls
- The harness StartOptions are owned, documented, and reusable by future lifecycle-consumer suites
</success_criteria>

<output>
Create `.planning/phases/03-world-character-commands/03-06-SUMMARY.md` when done. Record: the landed option signatures, which world.Service branch WithRetirementReactor used (reused vs constructed), the observed feed positions from spec 2, and the deny-code shape consumed in spec 4.
</output>

## Artifacts this phase produces (plan 03-06)

New symbols created by this plan (source-grounding: exclude from drift verification):
- `internal/testsupport/integrationtest`: `WithOutboxRelay`, `WithRetirementReactor` StartOptions, `Server.RetirementStartLocation()` accessor, startConfig fields
- Test files: `test/integration/retirement/retirement_suite_test.go`, `test/integration/retirement/retirement_reactor_test.go`
