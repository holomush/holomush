---
phase: 02.2-background-job-authorization-model
plan: 05
type: execute
wave: 5
depends_on:
  - 02.2-01
  - 02.2-02
  - 02.2-04
files_modified:
  - docs/architecture/invariants.yaml
  - docs/architecture/invariants.md
  - internal/world/caller_test.go
  - internal/access/policy/attribute/job_provider_test.go
  - internal/access/policy/seed_smoke_test.go
  - site/src/content/docs/contributing/explanation/background-job-authorization.md
  - .planning/ROADMAP.md
  - .planning/phases/02.2-background-job-authorization-model/02.2-DEFERRED-ISSUES.md   # only when `gh` is unavailable — see Task 2's degraded path
autonomous: true   # stays autonomous BECAUSE Tasks 2 and 3 carry an explicit no-`gh` degraded path (draft-to-file + UNFILED SUMMARY line) instead of blocking on network/auth
requirements:
  - AUTHZ-02

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`INV-ACCESS-13` exists in `docs/architecture/invariants.yaml` with `binding: bound`, `asserted_by` listing every test file that genuinely asserts one of its clauses, and `docs/architecture/invariants.md` regenerated by `go run ./cmd/inv-render` rather than hand-edited."
    - "The invariant summary claims exactly what its bindings prove and no more: it states plainly that a timer-driven job's authority is NOT instance-scoped, and that the model makes no claim against malicious in-process code (D-53)."
    - "`INV-ACCESS-13` does NOT claim consumer-boundary stamping. This phase ships a fixture job only (D-52) and its tests construct `world.JobCaller` directly — there is no message, no consumer wrapper and no handler boundary in the phase, so a bound consumer-boundary clause would be a partial-binding false-green. D-55's stamping contract is registered SEPARATELY as `binding: pending` with no `asserted_by`, for Phase 3 to bind when a real consumer exists — or omitted entirely. It is never bound here."
    - "`INV-ACCESS-13`'s declared-capability clause is scoped to the SHIPPED FIXTURE SEED (`the fixture seed requires principal.job.writes to contain…`), not asserted as a universal rule over all future job seeds — no meta-test enumerates `principal is job` seeds and requires the conjunct, so the universal phrasing would over-claim."
    - "A contributor-facing doc states how a NEW background consumer authorizes — the `job:` path — and states explicitly that `world.SystemCaller()` is NOT that path."
    - "The doc enumerates the 2 remaining production `SystemCaller()` call sites and records that `rg 'SystemCaller\\(\\)' --glob '*.go' -g '!*_test.go'` — NOT `rg WithSystemSubject` — is the enumerating grep after Phase 02.1, so a future audit does not read a clean `WithSystemSubject` grep as \"no bypasses exist\"."
    - "The doc states plainly, in its own section, that a timer-driven background job gets identity plus declared capability class and NO per-execution instance scoping, so its authority is necessarily coarse (D-68)."
    - "The D-67 upgrade risk is disclosed where an operator will see it before upgrading, with the pre-flight SQL query and the remedy, not only inside a planning artifact."
    - "ROADMAP Phase 02.2 success criterion 4 reads as D-68's option A + D, and the ROADMAP was changed by a scoped replacement of the existing criterion text — never by a whole-file rewrite."
    - "ROADMAP criterion 3 says the typo'd `action.*` reference is a POLICY-compile failure at boot (`cache.Reload`), not a Go `go build` failure — criterion 3's substance is unchanged and un-amended; only its failure class is named precisely."
    - "The ROADMAP Wave-4 plan-list line for `02.2-04-PLAN.md` cites **D-66** and **D-67** (not the pre-renumber D-61/D-62, which belong to Phase 02.1) and **4** compilation sites (not 3) — the renumber collision does not survive in the artifact a future reader consults first."
    - "Both deferred structural follow-ups and the upstream GSD tooling gap are accounted for in the SUMMARY — as issue numbers when `gh` is available, otherwise as an explicit UNFILED statement naming `02.2-DEFERRED-ISSUES.md` and quoting the ready-to-run commands. Neither silently evaporates, and neither blocks an autonomous run."
  artifacts:
    - docs/architecture/invariants.yaml
    - site/src/content/docs/contributing/explanation/background-job-authorization.md
  key_links:
    - "invariants.yaml INV-ACCESS-13 asserted_by -> `// Verifies: INV-ACCESS-13` annotations -> test/meta/invariant_registry_test.go binding checks"
    - "02.2-ACTION-AUDIT.md upgrade-risk paragraph -> the operator-facing doc section -> the phase SUMMARY"
  prohibitions:
    - "MUST NOT annotate a test with `// Verifies: INV-ACCESS-13` unless that test genuinely asserts a clause of the summary. A `// Verifies:` on a test that merely touches the code is a false-green — the documented INV-RB-3 failure mode — and `TestBoundInvariantsAreGenuinelyAsserted` catches only the Skip-only case, not a partial binding."
    - "MUST NOT hand-edit inside the `<!-- BEGIN GENERATED: ... -->` regions of `docs/architecture/invariants.md`; regenerate with `go run ./cmd/inv-render`."
    - "MUST NOT rewrite `.planning/ROADMAP.md` with a whole-file `Write`, and MUST NOT add any new version-bearing or checkmark-bearing `###` heading to it — `extractCurrentMilestone` ends the active-milestone section at the next such heading, silently truncating the milestone scope."
---

<objective>
Make the phase's claims durable and honest: register and genuinely bind the invariant, write the
contributor-facing doc criterion 4 owes, disclose the D-67 upgrade risk where an operator will see
it, and amend ROADMAP criterion 4 to D-68's reading.

Purpose: this repo has a documented failure mode of registry summaries claiming more than their
bindings prove (INV-WORLD-6). D-53 forbids repeating it here, and criterion 4's whole content is
documentation. This plan is where the phase either tells the truth about what it built or does not.

Output: `INV-ACCESS-13`, one contributor doc, an operator-facing upgrade-risk disclosure, two filed
follow-up issues, and an amended ROADMAP criterion.

(The `<assumption_delta_decision>` block for this phase lives in `02.2-01-PLAN.md`.)
</objective>

<artifacts_this_phase_produces>
| Kind | Symbol | File |
|---|---|---|
| invariant | `INV-ACCESS-13` (`binding: bound`) | `docs/architecture/invariants.yaml` (+ generated `.md`) |
| invariant | the D-55 consumer-boundary-stamping entry — next free `INV-ACCESS` id after 13, `binding: pending`, NO `asserted_by`; Phase 3 binds it | `docs/architecture/invariants.yaml` (+ generated `.md`) |
| doc (conditional) | `02.2-DEFERRED-ISSUES.md` — only when `gh` is unavailable | `.planning/phases/02.2-background-job-authorization-model/` |
| doc page | `contributing/explanation/background-job-authorization` | `site/src/content/docs/contributing/explanation/background-job-authorization.md` |
| annotation | `// Verifies: INV-ACCESS-13` (3 sites) | `caller_test.go`, `job_provider_test.go`, `seed_smoke_test.go` |
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-background-job-authorization-model/02.2-CONTEXT.md
@.planning/phases/02.2-background-job-authorization-model/02.2-PATTERNS.md
@.planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md
@.planning/phases/02.2-background-job-authorization-model/02.2-01-SUMMARY.md
@.planning/phases/02.2-background-job-authorization-model/02.2-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register and genuinely bind `INV-ACCESS-13`</name>
  <files>docs/architecture/invariants.yaml, docs/architecture/invariants.md, internal/world/caller_test.go, internal/access/policy/attribute/job_provider_test.go, internal/access/policy/seed_smoke_test.go</files>
  <precondition>Plans 01, 02 and 04 have landed and their tests are green — the invariant's clauses must already be asserted before the binding is claimed. Verify with `task test -- ./internal/access/... ./internal/world/`.</precondition>
  <read_first>
docs/architecture/invariants.yaml lines 5125-5155 (INV-WORLD-8 — Phase 02.1's entry, the most recent `bound` one, and the house style to copy: leading ALL-CAPS topic token, the predicate stated exactly as the test computes it, exceptions named inline with path:line, and an explicit note on what the summary deliberately does NOT claim)
docs/architecture/invariants.yaml lines 2260-2310 (INV-ACCESS-8..12 — the scope's existing entries and their phrasing; confirm 13 is still the next free id)
.claude/rules/invariants.md (define / respect / bind / regenerate; the fabricated-binding prohibition; the note that a GSD milestone artifact is OUTSIDE the orphan-check walk root so this id MUST be hand-registered)
internal/world/caller_test.go lines 265-325 (the existing `// Verifies: INV-WORLD-8` annotation placement — one line immediately above the asserting test)
test/meta/invariant_registry_test.go (the drift, provenance, binding-presence and genuinely-asserted checks this entry must satisfy)
  </read_first>
  <action>
Add ONE entry to `docs/architecture/invariants.yaml` in the `INV-ACCESS` scope, id `INV-ACCESS-13`
(re-verify it is still the next free id — do not trust this plan's arithmetic),
`origin_spec: ".planning/phases/02.2-background-job-authorization-model/02.2-CONTEXT.md"`,
`binding: bound`.

The summary MUST state the predicate as the tests compute it, and MUST carry the two D-53
disclaimers. Write it to this substance:

> JOB-PRINCIPAL: a background job that crosses the `world.Service` ABAC chokepoint authorizes under
> the disjoint `job:` principal namespace, and its authority is gated by three independent
> conditions that the SHIPPED FIXTURE SEED `seed:job-fixture-instance-scoped` reads and that its
> tests assert — (1) LIVENESS: `attribute.JobProvider.ResolveSubject` returns `(nil, nil)` for a job
> name absent from the injected `jobs.Registry`, so no `principal.job.*` attribute is stamped and
> every attribute-conditioned permit fails to match; (2) DECLARED CAPABILITY CLASS: the fixture seed
> requires `principal.job.writes` to contain the resource kind, so a job that grows an undeclared
> write path is denied until that seed is deliberately widened; (3) INSTANCE SCOPE, EVENT-DRIVEN
> ONLY: the fixture seed requires `action.job.trigger_subject` — carried on `world.JobCaller` as the
> BARE aggregate ULID, not a dotted NATS subject — to equal `resource.id`, which the handler derives
> independently of the provenance. A timer-driven job constructed via `world.ScheduledJobCaller`
> carries clauses (1) and (2) ONLY; its authority is deliberately coarse and this summary does NOT
> claim instance scoping for it (D-68). This summary also does NOT claim any defense against
> malicious in-process code: nothing inside the process can stop Go from calling `Engine.Evaluate`
> with arbitrary arguments, and the honest claim is scope drift plus handler bugs (D-53).

**Two narrowings above are load-bearing corrections — do not restore the wider wording.**

1. **Clause (3) must NOT say "stamped at the consumer boundary from the delivered message."** That
   is D-55's contract and it is TRUE, but **nothing in this phase proves it**: per D-52 this phase
   ships a FIXTURE job only, plan 01's tests construct `world.JobCaller` directly (the existing
   helper does the same at `internal/world/caller_test.go:271`), and there is no JetStream message,
   no consumer wrapper, and no handler boundary anywhere in the phase. Binding a consumer-boundary
   clause to a test that constructs the caller by hand is exactly the partial-binding false-green
   `.claude/rules/invariants.md` names, the INV-WORLD-6 failure mode D-53 forbids, and this plan's
   own threat T-02.2-22. What clause (3) may claim is what the paired permit/deny actually proves:
   the value must equal `resource.id`, and the handler derives the resource independently.

   The consumer-boundary half is Phase 3's to prove, when a real consumer wrapper exists (D-46/D-55).
   Register it as a **SEPARATE entry** in the same change — next free `INV-ACCESS` id after 13 —
   with `binding: pending` and **NO `asserted_by`** (the meta-test rejects a pending entry carrying
   provenance), `origin_spec` pointing at this phase's CONTEXT, and a summary confined to: provenance
   attributes are stamped at the consumer boundary from the delivered message before handler logic
   runs, and the handler cannot alter them. Record the new id in the SUMMARY so Phase 3 plan 03-04
   knows which entry it binds. If registering a second id is judged out of scope by the executor,
   the fallback is to OMIT the consumer-boundary clause entirely — never to bind it here.

2. **Clause (2) must be scoped to the shipped fixture seed, not to all future job seeds.** The
   seed-smoke mismatch test proves that `seed:job-fixture-instance-scoped` reads
   `principal.job.writes`; it does not establish that every future `principal is job` seed must.
   No meta-test enumerates job seeds and requires the capability conjunct, so a summary phrased as
   a universal rule would over-claim. Phrase it as "the fixture seed requires…", as above. (If a
   future phase adds such a meta-test, the summary can be widened then — that is a registry edit,
   not a rewrite.)

`asserted_by` lists the three test files, each of which must carry a
`// Verifies: INV-ACCESS-13` line immediately above the specific asserting test:
- `internal/access/policy/attribute/job_provider_test.go` — above the not-running/nil-registry test
  (clause 1)
- `internal/access/policy/seed_smoke_test.go` — above the capability-class DENY test (clause 2)
- `internal/world/caller_test.go` — above the end-to-end paired permit/deny (clause 3) and above the
  `ScheduledJobCaller` no-attrs test (the carve-out)

Do not annotate anything else. If any clause turns out NOT to have a genuine assertion, do not
fabricate one: narrow the summary to what IS asserted, or file a coverage bug with
`gh issue create -R holomush/holomush --title "coverage gap: INV-ACCESS-13 clause N unbound" --label bug`
and set `binding: pending` with NO `asserted_by` (the meta-test rejects a pending entry that lists
provenance).

Regenerate with `go run ./cmd/inv-render`. Never hand-edit inside the
`<!-- BEGIN GENERATED: ... -->` regions of `docs/architecture/invariants.md`.
  </action>
  <acceptance_criteria>
- `rg -n 'id: INV-ACCESS-13' docs/architecture/invariants.yaml` returns exactly 1 line.
- `rg -o '// Verifies: INV-ACCESS-13' --glob '*_test.go' | wc -l` is >= 3, and every matching file
  appears in the entry's `asserted_by` list.
- **No consumer-boundary claim is bound.** `INV-ACCESS-13`'s summary does not contain the phrase
  `consumer boundary`:
  `rg -o 'consumer boundary' docs/architecture/invariants.yaml | wc -l` must not increase for the
  `INV-ACCESS-13` entry — check the entry's own line range, not the whole file. If the separate
  `binding: pending` D-55 entry was registered, its summary MAY contain that phrase and MUST carry
  no `asserted_by` key; assert that pairing directly
  (`TestProvenanceGuard` in `test/meta/` rejects a pending entry with provenance, so a green
  `./test/meta/` run is the enforcement).
- The declared-capability clause is fixture-scoped, not universal:
  `rg -o 'fixture seed requires' docs/architecture/invariants.yaml | wc -l` is >= 1 within the
  `INV-ACCESS-13` entry.
- `task test -- -run 'TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted|TestRegistryBindingChecks' ./test/meta/` passes.
- `go run ./cmd/inv-render` leaves `git diff --exit-code docs/architecture/invariants.md` clean
  after the change is committed (the generate-and-diff drift check).
- The summary text contains both disclaimers. Assert them **within the `INV-ACCESS-13` entry's own
  line range**, not as a whole-file delta: extract the entry (e.g. `yq` on the id, or read the
  entry's line span) and confirm it contains two `does NOT claim` occurrences —
  `<entry text> | rg -o 'does NOT claim' | wc -l` is 2. A before/after count across the whole
  ~5000-line `invariants.yaml` is brittle to unrelated concurrent edits, and `rg -c` is the wrong
  counter twice over (it counts matching LINES, and at zero it prints nothing while exiting 1).
  </acceptance_criteria>
  <verify>
    <automated>go run ./cmd/inv-render &amp;&amp; git diff --exit-code docs/architecture/invariants.md &amp;&amp; task test -- ./test/meta/</automated>
  </verify>
  <done>`INV-ACCESS-13` is registered, bound to three tests that genuinely assert its three clauses, regenerated into the rendered registry, and phrased so it claims no more than those bindings prove — specifically, it carries NO consumer-boundary-stamping claim (D-55 is a separate `binding: pending` entry, or is omitted) and its declared-capability clause is scoped to the shipped fixture seed rather than to all future job seeds.</done>
</task>

<task type="auto">
  <name>Task 2: The contributor doc, the SystemCaller enumeration, and the D-67 upgrade disclosure</name>
  <files>site/src/content/docs/contributing/explanation/background-job-authorization.md, .planning/phases/02.2-background-job-authorization-model/02.2-DEFERRED-ISSUES.md (conditional — only on the no-`gh` degraded path)</files>
  <read_first>
site/src/content/docs/contributing/explanation/gateway-boundary.md (an existing short explanation page — frontmatter shape, heading depth, and the house voice to match)
site/src/content/docs/contributing/index.mdx (the audience index — check whether a new page must be linked from it)
scripts/check-docs-ia.sh (the Diátaxis IA invariants: INV-1 parity — every content slug must resolve to a built page; INV-2 one-bucket — a non-index doc must live under `<audience>/<mode>/`; INV-6 nav — the sidebar section cap)
site/astro.config.mjs (sidebar wiring — determine whether explanation pages are auto-globbed or individually listed BEFORE choosing where the page goes)
.planning/phases/02.2-background-job-authorization-model/02.2-ACTION-AUDIT.md (the upgrade-risk paragraph and the pre-flight SQL query — lift them, do not re-derive them)
.planning/phases/02.2-background-job-authorization-model/02.2-01-SUMMARY.md (the landed JobCaller/Provenance signatures and the fixture seed's DSL text)
.claude/rules/terminology.md (location / character / player / session vocabulary — the doc MUST use it exactly)
  </read_first>
  <action>
Write one explanation page at
`site/src/content/docs/contributing/explanation/background-job-authorization.md`. Confirm the
placement satisfies `scripts/check-docs-ia.sh` INV-2 (a non-index doc under
`<audience>/<mode>/`) and INV-1 (the slug resolves to a built page) BEFORE writing — if the sidebar
lists explanation pages individually rather than globbing, wire it in the same change.

Five sections, each carrying an obligation this phase owes:

1. **How a new background consumer authorizes.** The `job:` path end to end, with the landed
   signatures quoted from the 01 SUMMARY: register with `jobs.Registry.Register(name, writes)` at
   subsystem Start and `Unregister(name)` at Stop; construct `world.JobCaller(name, world.Provenance{...})`
   at the consumer boundary BEFORE handler logic (D-55) so the handler cannot alter it; let the
   handler derive the resource independently; write a seed. State the two gates plainly: the
   declaration NARROWS and the seed GRANTS — both must pass, and a `writes` declaration authorizes
   nothing on its own unless a seed reads `principal.job.writes`.

2. **`trigger_subject` is the bare aggregate ULID.** Quote the fixture seed's DSL text verbatim and
   state that `bags.Resource["id"]` is the substring after the first `:`, so a dotted NATS subject
   would silently never match and every job seed would default-deny. This section is what Phase 3
   plan 03-04 reads.

3. **`SystemCaller()` is NOT the background-job path.** Enumerate the 2 remaining production
   `SystemCaller()` call sites — `internal/grpc/location_follow.go:200` (`GetLocation`) and `:213`
   (`GetExitsByLocation`) — re-verifying the line numbers on the current tree, and say why each is a
   synchronous read rather than a background job (D-52 records the rejection of migrating them).
   Record explicitly that after Phase 02.1 the enumerating grep is
   `rg 'SystemCaller\(\)' --glob '*.go' -g '!*_test.go'` and NOT `rg WithSystemSubject`, because
   `WithSystemSubject` now survives only inside `world.Caller.evalContext` — a future auditor
   reading a clean `WithSystemSubject` grep as "no bypasses exist" would be wrong.

4. **Timer-driven jobs are coarse, and that is deliberate (D-68).** State plainly: a timer-driven
   job has no triggering event, so it carries identity and declared capability class ONLY, via
   `world.ScheduledJobCaller(name)`, and its authority is NOT instance-scoped. Name the two rejected
   alternatives and why: a `trigger_kind` discriminator with a tick/window (the window narrows
   nothing a flusher's real authority is bounded by — it looks like instance scoping without being
   it) and synthetic tick provenance with an empty or sentinel `trigger_subject` (an empty-string
   sentinel is fail-OPEN under `.claude/rules/abac-providers.md`, matching any other unresolved
   peer).

   **State Phase 3's `last_active_at` flusher to D-68's actual reading — this is a locked decision
   and an earlier draft of this plan got it backwards.** D-68 (`02.2-CONTEXT.md:242`, `:258-260`) is
   option **A + D**, not option A alone: **EVERY** background consumer gets a `job:` identity and a
   declared capability class; **only event-driven** consumers additionally get per-execution
   instance scoping. D-68's own closing sentence about the flusher is *"under D-68 it takes an
   identity but no per-execution attributes."*

   So write it as: the flusher **registers a `job:` identity and its declared capability class in
   `jobs.Registry`** at subsystem Start (and unregisters at Stop) like every other background
   consumer, and takes **NO per-execution attributes** because it is timer-driven. Separately, and
   as a fact about its current write path rather than a reason to skip the identity: its write
   today lands at the INV-WORLD-4 out-of-world writer boundary and crosses **no ABAC chokepoint**,
   so nothing currently *consumes* that identity at an `Evaluate` call. It still participates in
   the registry lifecycle, so the moment it (or anything sharing its subsystem) does cross a
   chokepoint, the identity and the declared class are already there and correct.

   <!-- planner-discipline-allow: needs no job identity -->
   <!-- planner-discipline-allow: takes neither -->
   **Do NOT write "the flusher needs no job identity" or "it takes neither."** Both are option A
   alone and contradict a locked decision. (Those two literals are quoted here deliberately, and
   allowlisted above, because the executor needs to recognise the exact wrong phrasing; the
   acceptance criterion below negative-greps them **in the DOC PAGE**, not in this plan.)
   This wording is not cosmetic: this plan's SUMMARY is the
   contract `03-05-PLAN.md:100`'s `<flusher_provenance_interlock>` reads, and that block instructs
   Phase 3 to *"follow that answer exactly"* — so a wrong answer here propagates a locked-decision
   violation straight into Phase 3.

5. **Operator upgrade note — undeclared `action.*` policies fail boot.** Lift the audit artifact's
   upgrade-risk paragraph and its pre-flight query verbatim:
   `SELECT id, name, dsl_text FROM policies WHERE dsl_text LIKE '%action.%';` — state that every
   returned row's `action.*` keys must appear in the declared set
   (`attribute.ActionNamespaceSchema()`), that a deployment carrying an undeclared one fails to boot
   after this release, that the boot error names the policy and the key, and that the remedy is a DB
   edit to that policy row. State that this was a deliberate trade (D-67): a typo'd `action.*`
   reference must never silently default-deny anywhere.

Also, in the same task, file the two structural follow-ups the research deferred so they do not
evaporate:
`gh issue create -R holomush/holomush --label enhancement` for (a) converting `reservedActionKeys`
from a denylist to a supply-side allowlist and (b) introducing a host-vouched attribute channel
separate from `AccessRequest.Attributes` — each body citing `02.2-RESEARCH.md` §Q1 and stating that
neither is needed for AUTHZ-02. Record the issue numbers in the SUMMARY.

**Degraded path when `gh` is unavailable — this plan stays `autonomous: true` because of it.**
Issue filing needs network and authentication, which an executor may not have; the same applies to
Task 3's upstream GSD gap report. Probe once with `gh auth status`. If it fails (or `gh issue
create` errors):

- Do **NOT** block, do **NOT** retry in a loop, and do **NOT** silently drop the follow-up.
- Write the issue bodies you would have filed into
  `.planning/phases/02.2-background-job-authorization-model/02.2-DEFERRED-ISSUES.md`, one section
  per issue, each carrying the exact `gh issue create` invocation (repo, labels, title, body) so a
  human can run it verbatim later.
- Record in the SUMMARY, in place of the issue numbers, that the issues are **UNFILED**, name that
  file as their holding location, and quote the ready-to-run commands. An "unfiled, drafted here"
  SUMMARY line is an honest outcome; a SUMMARY that omits them is not.

The same fallback covers Task 3's upstream GSD gap: draft it into the same file rather than
skipping it.
  </action>
  <acceptance_criteria>
- `bash scripts/check-docs-ia.sh` passes (or `task lint` passes, which runs it).
- `rg -o 'SystemCaller' site/src/content/docs/contributing/explanation/background-job-authorization.md | wc -l` is >= 2, and the two enumerated `path:line` citations match the current tree (re-verify with `rg -n 'SystemCaller\(\)' internal/grpc/location_follow.go`). (`-o | wc -l`, never `rg -c`.)
- The D-68 flusher statement is A+D, not A alone: the page states that a timer-driven consumer takes
  an identity and a declared capability class, and the phrases "needs no job identity" and "takes
  neither" appear NOWHERE in it —
  `rg -o 'needs no job identity|takes neither' site/src/content/docs/contributing/explanation/background-job-authorization.md | wc -l`
  reports 0.
- `rg -q "dsl_text LIKE" site/src/content/docs/contributing/explanation/background-job-authorization.md` succeeds.
- `rg -q 'not instance-scoped|NOT instance-scoped' site/src/content/docs/contributing/explanation/background-job-authorization.md` succeeds (the D-68 plain statement).
- `rg -ow 'room' site/src/content/docs/contributing/explanation/background-job-authorization.md | wc -l` reports 0 — the terminology rule forbids the word `room`; the spatial concept is always `location`.
- Both structural follow-ups are accounted for in the SUMMARY: EITHER two GitHub issue numbers, OR
  an explicit UNFILED statement naming
  `.planning/phases/02.2-background-job-authorization-model/02.2-DEFERRED-ISSUES.md` and quoting the
  ready-to-run `gh issue create` commands. Silence about them is a FAILURE of this task; a `gh`
  outage is not.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-docs-ia.sh &amp;&amp; task lint:docs-symmetry</automated>
  </verify>
  <done>A contributor can read one page and learn how to authorize a new background consumer, why `SystemCaller()` is not that path and how to enumerate its remaining sites, that a timer job's authority is coarse by design, and — as an operator — that an undeclared `action.*` DB policy fails boot and how to check for one before upgrading.</done>
</task>

<task type="auto">
  <name>Task 3: Amend ROADMAP criterion 4 to D-68's reading, clarify criterion 3, and repair the stale plan-list line</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
.claude/rules/planning-artifacts.md (the absolute rule: never invent structure in a tool-owned generated file; MAY fill in VALUES in shapes the tool already writes; prefer the tool's own verb then `git diff` to verify; never add a version-bearing or checkmark-bearing `###` heading under `## Phases`)
.planning/ROADMAP.md — the `### Phase 02.2: Background Job Authorization Model (INSERTED)` section, specifically its `**Success Criteria** (what must be TRUE):` numbered list, item 4 (read it with `node <gsd-tools> query roadmap.get-phase 02.2 --pick section`)
.planning/phases/02.2-background-job-authorization-model/02.2-CONTEXT.md D-68 (the exact option A + D reading, and the two rejected options)
  </read_first>
  <action>
FIRST, record the tooling finding — do not paper over it. The sanctioned verb named in the phase
brief, `gsd-tools phase edit`, DOES NOT EXIST on this GSD install. Confirm and record the actual
verb inventory:
`node <gsd-tools> phase 2>&1` -> `uat-passed, next-decimal, add, add-batch, insert, remove, complete, list-plans`
and `node <gsd-tools> roadmap 2>&1` -> `analyze, get-phase, update-plan-progress, annotate-dependencies, validate, upgrade`.
None of them can amend a phase's success-criterion prose. Per
`.claude/rules/planning-artifacts.md` ("When the tool genuinely cannot express it — report it"),
file the gap upstream against GSD and record the reference in the SUMMARY.

THEN apply the amendment as a **scoped `Edit`** — replacing ONLY the text of criterion 4 inside the
existing, tool-generated `**Success Criteria**` numbered list. This is a VALUE fill inside a shape
the tool already writes, which the rule explicitly permits; it invents no heading, no section, and
no block shape. Use `Edit`, never `Write`. Do not touch any other phase's section, and do not add
any `###` heading.

Criterion 4's replacement text must read to D-68's substance:

> 4. Every background consumer gets a `job:` identity and a **declared capability class**
>    (`principal.job.name`, `principal.job.writes`, via the liveness-gated registry and provider);
>    **only event-driven** consumers additionally get **per-execution instance scoping** (the
>    provenance triple bound against `resource.id`). A timer-driven job's authority is therefore
>    necessarily coarse, and the documentation says so plainly rather than implying it is
>    instance-scoped. Existing `WithSystemSubject` / `SystemCaller()` call sites are enumerated,
>    with the note that `rg 'SystemCaller\(\)'` — not `rg WithSystemSubject` — is the enumerating
>    grep after Phase 02.1 (migration itself belonged to Phase 02.1).

Leave criteria 1, 2 and 5 untouched. Criterion 3 gets ONE clarifying phrase and nothing else
(below). Criterion 4 is the only criterion whose substance changes.

**Second scoped `Edit` — criterion 3's failure class.** Criterion 3 currently ends
"...so a typo'd `action.*` reference is a compile-time failure rather than a silent default-deny,
and no shipped seed regresses." After D-66 that failure is a **policy-compile** failure raised at
boot / `cache.Reload` (`internal/access/policy/cache.go:149-152`), NOT a Go `go build` failure.
Criterion 3's SUBSTANCE is correct and stays — bringing D-66 into scope is exactly what makes it
achievable, so no amendment is owed and none is being made. Replace only the phrase
`a compile-time failure` with `a policy-compile failure at boot (cache reload)` so a future reader
cannot read it as `go build`. Change nothing else in criterion 3.

**Third scoped `Edit` — the stale plan-list line.**
<!-- planner-discipline-allow: D-61 compiler -->
`.planning/ROADMAP.md:417` (verified on the tree 2026-08-09) reads:
`- [ ] 02.2-04-PLAN.md — D-61 compiler↔SchemaRegistry wiring (3 sites) + D-62 fatal-for-all-sources`
Two facts in it are wrong and this is the one artifact a future reader is most likely to consult:
- **`D-61` / `D-62` are pre-renumber ids.** They are now **D-66** and **D-67**. D-61…D-65 belong to
  **Phase 02.1** under the shared v0.13 sequence — this is precisely the collision the 2026-08-09
  renumber was performed to prevent (`02.2-CONTEXT.md:41-47`).
- **"3 sites" is stale.** Plans 03 and 04 settled the compilation-site inventory at **4**.
Replace the line's text with:
`- [ ] 02.2-04-PLAN.md — D-66 compiler↔SchemaRegistry wiring (4 sites) + D-67 fatal-for-all-sources`
Same constraints as the criterion edits: scoped `Edit` only, never `Write`, no structural change, no
new heading. Re-read the line before editing — plan-progress tooling may have flipped the checkbox,
in which case preserve whatever checkbox state is on the tree and change only the description text.

AFTER all three edits, verify what was actually written — a validator-green result is not evidence
(`roadmap validate` returns `{"warnings":[]}` for virtually any input). Run BOTH:
- `git diff .planning/ROADMAP.md` and confirm the diff touches only criterion 4's lines
- the milestone-phase-filter property check from the rule:
  `node -e 'const f=require("<gsd-core>/bin/lib/roadmap-parser.cjs").getMilestonePhaseFilter(process.cwd()); console.log(require("fs").readdirSync(".planning/phases").filter(f))'`
  and confirm the phase list is unchanged from before the edit (capture it before AND after).
  </action>
  <acceptance_criteria>
- `node <gsd-tools> query roadmap.get-phase 02.2 --pick section` shows criterion 4 in D-68's
  wording, containing the literal strings `declared capability class`, `only event-driven`, and
  `necessarily coarse`.
- `git diff --stat .planning/ROADMAP.md` shows a change confined to the Phase 02.2 section; the
  diff adds NO line beginning with `### `.
- Criterion 3 names the right failure class:
  `node <gsd-tools> query roadmap.get-phase 02.2 --pick section | rg -o 'policy-compile failure at boot' | wc -l`
  is 1, and the same output yields 0 for the pre-edit phrase (verified present exactly once on the
  tree 2026-08-09, so this gate goes RED before the edit).
- The stale plan-list line is repaired. In `.planning/ROADMAP.md`, all four hold:
  `rg -o 'D-66 compiler' .planning/ROADMAP.md | wc -l` is 1;
  `rg -o 'D-67 fatal-for-all-sources' .planning/ROADMAP.md | wc -l` is 1;
  `rg -o '\(4 sites\)' .planning/ROADMAP.md | wc -l` is 1;
  and the pre-renumber pair is gone from that line —
  `rg -o 'D-61 compiler' .planning/ROADMAP.md | wc -l` reports 0.
  (All counted with `-o | wc -l`, never `rg -c`.)
- The milestone phase filter returns the same phase list before and after the edit (both captured
  in the SUMMARY).
- `node <gsd-tools> query roadmap.validate` returns no errors (necessary, not sufficient — the two
  checks above are the real ones).
- The missing `phase edit` verb is reported upstream and referenced in the SUMMARY — as an issue
  reference if `gh` is available, otherwise as a drafted entry in
  `.planning/phases/02.2-background-job-authorization-model/02.2-DEFERRED-ISSUES.md` with its
  ready-to-run command, named explicitly in the SUMMARY (see Task 2's degraded path).
  </acceptance_criteria>
  <verify>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query roadmap.get-phase 02.2 --pick section | rg -q 'necessarily coarse' &amp;&amp; test "$(git diff .planning/ROADMAP.md | rg -o '^\+### ' | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>ROADMAP criterion 4 reads as D-68's option A + D, criterion 3 names a policy-compile failure at boot rather than a `go build` failure, and the Wave-4 plan-list line cites D-66/D-67 and 4 sites instead of the pre-renumber D-61/D-62 and a stale count — all three applied as scoped value replacements inside shapes the tool already writes, with the milestone phase filter proven unchanged and the missing tool verb reported upstream rather than worked around silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invariant registry -> future contributors | A registry summary is read as a guarantee. Over-claiming here is how a future reviewer approves a change that breaks something the registry said was safe. |
| release documentation -> operator | The only channel by which a deployment learns it must run the pre-flight query before upgrading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-22 | Repudiation | `INV-ACCESS-13` summary vs its bindings | high | mitigate | A summary claiming more than its bindings prove is the documented INV-WORLD-6 failure mode and is forbidden by D-53. Mitigation: the summary states each clause exactly as its test computes it, names the `ScheduledJobCaller` carve-out inline, and carries an explicit does-NOT-claim sentence for malicious in-process code. Two specific over-claims are excluded by construction: the D-55 CONSUMER-BOUNDARY clause (this phase ships a fixture job per D-52 and constructs `world.JobCaller` directly — no message, no consumer, no handler boundary exists to assert against, so it is registered separately as `binding: pending` or omitted) and the universal reading of the declared-capability clause (no meta-test enumerates `principal is job` seeds, so it is scoped to the shipped fixture seed). `TestBoundInvariantsAreGenuinelyAsserted` catches Skip-only stubs; a partial binding needs the human review this checklist forces. |
| T-02.2-23 | Denial of Service | undisclosed D-67 upgrade risk | high | mitigate | An operator who upgrades without running the pre-flight query gets a server that will not boot. Mitigation: the risk, the query, and the remedy are published in a contributor/operator-visible doc page, not only in a planning artifact, and are repeated in the phase SUMMARY. |
| T-02.2-24 | Tampering | `.planning/ROADMAP.md` structure | medium | mitigate | A whole-file rewrite or an invented version-bearing `###` heading silently truncates `extractCurrentMilestone`'s scope, producing a milestone that reads 100% complete with work outstanding. Mitigation: scoped `Edit` only, plus the before/after milestone-phase-filter property check — `roadmap validate` alone is explicitly insufficient. |
| T-02.2-25 | Information Disclosure | the operator doc | low | mitigate | The doc quotes a SQL query and error-message shapes only — no credentials, no connection strings, no policy contents. |
</threat_model>

<verification>
- `task test -- ./test/meta/` green (the invariant registry meta-tests).
- `go run ./cmd/inv-render` produces no diff after commit.
- `task lint` green (it runs `scripts/check-docs-ia.sh` and the markdown/docs-symmetry gates) —
  delegate to the `local-check` agent.
- `task fmt` run and mutations committed — `task fmt` reflows markdown tables, and an unreflowed
  table in the new doc page is a common cause of red CI on an otherwise-green PR.
- The ROADMAP milestone-phase-filter list is identical before and after Task 3.
</verification>

<success_criteria>
- `INV-ACCESS-13` is registered, genuinely bound, regenerated, and claims no more than its bindings.
- One doc page answers "how does a new background consumer authorize", enumerates the remaining
  `SystemCaller()` sites with the correct enumerating grep, states the timer-job coarseness plainly,
  and discloses the D-67 upgrade risk with its pre-flight query.
- ROADMAP criterion 4 reads as D-68 option A + D; criterion 3 names a policy-compile failure at
  boot; the Wave-4 plan-list line cites D-66/D-67 and 4 sites — all applied without touching ROADMAP
  structure.
- The two deferred structural follow-ups and the upstream GSD tooling gap are filed as GitHub
  issues, or drafted into `02.2-DEFERRED-ISSUES.md` with their exact commands and declared UNFILED
  in the SUMMARY. Not lost either way.
</success_criteria>

<output>
Create `.planning/phases/02.2-background-job-authorization-model/02.2-05-SUMMARY.md` when done.

This is the LAST plan of the phase, so its SUMMARY carries the phase-level obligations. It MUST
record: (a) the D-67 upgrade risk in release-note-ready wording; (b) that ONLY the fixture grant
shipped — `job:retirement` and `job:activity-flush` did NOT, and Phase 3 plans 03-04/03-05 must add
their own seeds against 02.2's landed vocabulary (this resolves `03-04-PLAN.md:141`'s precondition);
(c) the `trigger_subject` normalization verbatim, for 03-04 to copy byte-for-byte; (d) D-68's answer
for `03-05-PLAN.md:100`'s reserved timer-job decision, in D-68's own terms and **not** as option A
alone — **the flusher registers a `job:` identity and a declared capability class like every
background consumer, and takes NO per-execution attributes because it is timer-driven**; separately,
its current write crosses no ABAC chokepoint (INV-WORLD-4 out-of-world writer boundary), so nothing
consumes that identity at an `Evaluate` call today, which is a fact about its write path and NOT a
reason to skip the identity. Do not write "it takes neither"; (e) the two filed issue numbers;
(f) the upstream GSD gap for the missing `phase edit` verb; (g) the id of the separate
`binding: pending` D-55 consumer-boundary invariant entry (or, if it was omitted rather than
registered, an explicit statement that D-55's stamping contract is UNBOUND and Phase 3 owns
registering it) — `INV-ACCESS-13` does not carry that clause.

**Parent-session gate, not an executor task:** `abac-reviewer` MUST be run by the PARENT session
before push — sub-agents cannot nest agent calls. Run `/holomush-dev:review-abac` over this phase's
`internal/access/**` changes and record the READY verdict in this SUMMARY (ROADMAP criterion 5).
Then run `task pr-prep` INLINE in the parent as the final gate.
</output>
