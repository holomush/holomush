---
status: complete
phase: 01.1-migration-framework-adopt-goose-for-go-migrations
source: [01.1-VERIFICATION.md]
started: 2026-08-03T02:25:30Z
updated: 2026-08-06T16:14:55Z
---

## Current Test

number: none
name: session complete
awaiting: nothing

## Tests

### 1. Accept or re-derive the recorded one-time pg_dump schema-identity proof

expected: A reviewer accepts (or re-derives) the empty pg_dump diff. Criterion 1 explicitly specifies proof by a one-time pg_dump diff rather than by tests passing, and D-14 keeps the harness out of the tree, so no automated check in the repository can re-derive or contradict it.
result: pass
method: RE-DERIVED independently — the user declined to accept the self-reported artifact.
  Side A applied the 88 pre-conversion `.up.sql` files from `b270c8ac8` via
  `psql -v ON_ERROR_STOP=1 -f` in ascending version order with no migration library
  involved; `APPLIED_COUNT=44` confirmed by counter, not assumption. Side B ran the real
  production `store.NewMigrator(url).Up()`, reaching version 53. Both on
  `postgres:18-alpine`, matched to the repo's testcontainer/CI/compose pin so dump
  formatting could not masquerade as a schema difference.
  `pg_dump --schema-only -T public.schema_migrations -T public.goose_db_version
  -T public.goose_db_version_id_seq` on both: 2224 lines each, diff empty (exit 0).
  Bookkeeping on side B: `goose_db_version` 45 rows / 44 at `version_id > 0`;
  `schema_migrations` absent (`to_regclass` null).
  Numbers were derived BEFORE reading `01.1-01-SUMMARY.md`, then compared — they agree
  with the recorded artifact exactly.
gotcha: pg_dump 18 emits `\restrict` / `\unrestrict` session lock tokens that are RANDOM
  PER INVOCATION, so the raw diff showed 2 differing lines before those were stripped
  (2222 identical lines each side after). Anyone re-running this proof on pg_dump 18 will
  see a non-empty diff that is formatting noise, not schema drift.

### 2. Execute the D-16 pre-deploy rehearsal against a restored sandbox snapshot

expected: Adopt runs against real restored data — 44 rows at `version_id > 0` plus goose's version-0 row, `schema_migrations` renamed to `schema_migrations_pre_goose`, boot completes. Procedure is in `site/src/content/docs/operating/how-to/sandbox/sandbox-restore.md`.
result: pass
method: RECONCILED from the recorded execution of 2026-08-03, not re-run in this session.
  The `blocked_by: release-build` precondition was discharged when v0.12.1 shipped
  (`20ccac13f`), and the rehearsal was driven against real restored sandbox data on a fresh
  `pre-deploy:goose-cutover` snapshot `k8895de81ae827d94862d54a5c9b5b19f` (19:17:26Z), taken
  immediately before the run per the runbook's own drift-window warning, restored into a
  throwaway `postgres:18-alpine`, binary built from `20ccac13f`.
  Side A (pre-adopt): `schema_migrations.version=53`, 1 row (golang-migrate's single-row
  shape), `dirty=false`, `goose_db_version` absent, `schema_migrations_pre_goose` absent,
  51 public tables. The adopt logged `recorded_version=53 seeded_versions=44
  archived_table=schema_migrations_pre_goose`.
  All three checks green — (a) ledger `migrations=44 total_rows=45 lowest=0 highest=53`,
  matching the count DERIVED from `schema_migrations.version` via `derivedSeedVersions`
  (`migrate_adopt.go:355`) rather than a memorized constant; (b) archive present,
  `schema_migrations` NULL; (c) ZERO core drift — 353 columns byte-identical against a
  from-zero freshly-migrated database. A table count alone is too weak for (c); the
  differential against a fresh migrate is the real check.
  Every schema difference was explained as runtime state rather than drift: `plugin_core_*`
  schemas (plugin storage, not core migrations); `access_audit_log_2026_*` and
  `events_audit_2026_*` (`partition_creator.go:38` runtime DDL); and
  `events_audit_legacy_migrated` vs `events_audit_unpartitioned` (`000052:38` renames to
  `_unpartitioned`, `retention_partitions.go:487,569` renames to `_legacy_migrated` only
  after the core's boot backfill — its presence CONFIRMS the v0.11.0 backfill completed,
  while the fresh DB keeps `_unpartitioned` because no core booted against it).
provenance_caveat: This result is reconciled from a recorded run, NOT re-executed here. A
  2026-08-06 attempt to corroborate it independently could not reach the decisive check —
  the runbook's snapshot-discovery step fails (see runbook_defects below) — so the evidence
  above rests on the 2026-08-03 execution record. Accepted deliberately by the maintainer.
runbook_defects: Five, four found by executing it and a fifth found on 2026-08-06.
  (1) `kopia snapshot restore <id> ./backup.sql` creates a DIRECTORY, so the documented
  `psql … < backup.sql` cannot work; the real dump is `backup.sql/holomush-holomush.sql`.
  (2) Restore path A requires a local `psql` client never listed as a prerequisite
  (workaround: `docker exec -i <pg> psql`).
  (3) `scripts/sandbox.env.example:21` says `sfo3`; the real endpoint is `nyc3`.
  (4) A shell-semantics test for a ```bash fence MUST run via `bash -c` — the agent Bash
  tool runs zsh, which does not word-split unquoted parameters.
  (5) The documented snapshot-discovery command
  (`exec -T backup kopia snapshot list --all --tags=pre-deploy:`) fails with
  `ERROR open repository: repository is not connected`. Structural, not transient:
  `backup.sh:38-48` connects lazily only when `kopia repository status` fails, and that
  state lives in `~/.config/kopia`, which is NOT among the service's volumes
  (`compose.prod.yaml:220` mounts only `.cache/kopia`) — so a fresh `exec` never inherits
  a connection. None of the five are yet fixed in the runbook.
residual_risk: D-06 deliberately gives the adopt no in-code schema verification beyond the
  dirty check, ON THE GROUNDS THAT D-16's rehearsal supplies it. That compensating control
  is now exercised. The SIBLING obligation is not: D-18 surgical rollback remains unrun.
  Broken Windows #9 bundled D-16 and D-18; it was closed and the D-18 half split into
  window #13 (`unrun-verify`, open) so the unexercised half stays tracked rather than
  being discharged by association.

### 3. Reconcile Broken Windows ledger entries #5 and #6

expected: #5 and #6 marked fixed, or an explicit reason recorded for keeping them open. Both say "red until plan 02"; plan 02 landed and both files now read one direction via `store.MigrationSectionForTest("...sql", "up"/"down")`. `open_count` is currently 9 and `/gsd-ship` blocks while > 0.
result: pass
method: Verified mechanically rather than asserted. Neither
  `events_audit_partition_migration_integration_test.go` nor
  `migrations_sessions_location_index_integration_test.go` contains any `.up.sql` /
  `.down.sql` path read (`! rg -q` exits 0 on both), and both now call
  `store.MigrationSectionForTest(...)` — at `:119` and `:170`/`:176` respectively. The
  full `task test:int` ran green at 10916 tests with zero failures.
  Closed with the sanctioned tool, not by hand-editing the ledger:
  `gsd-tools windows fixed 5` and `gsd-tools windows fixed 6`.
  `open_count` 9 -> 7, `fixed_count` 0 -> 2.
note: `/gsd-ship` still blocks — 7 entries remain open. #1-#3 belong to phase 09 (prior
  milestone). #4, #7, #8 are 01.1 `deviation` records whose stated conditions appear
  discharged. #9 is `unrun-verify` for D-16 and legitimately stays open while UAT test 2
  is blocked on release-build. Disposition of the remainder is a maintainer call.
superseded_2026-08-06: The #9 clause above no longer holds. #9 bundled D-16 AND D-18; with
  test 2 reconciled to pass, #9 was closed via `gsd-tools windows fixed 9` and its unrun
  D-18 half split into window #13 (`unrun-verify`, open) so the unexercised obligation
  stays tracked. `open_count` is 6. The rest of this note stands as recorded.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
