---
phase: 01.1-migration-framework-adopt-goose-for-go-migrations
plan: 05
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - osv-scanner.toml
autonomous: true
requirements: []

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`go mod why -m github.com/docker/docker` reports the main module does not need it, or the surviving path is named explicitly and the allowlist entries are kept rather than deleted."
    - "`task lint:vuln` is green with the five `[[IgnoredVulns]]` entries removed — all three legs (nats floor guard, govulncheck, osv-scanner v2), judged by exit code."
    - "Issue #4817 is closed with grounded evidence, or commented with the residual path if any advisory survives — never closed on the assumption that dropping golang-migrate was sufficient."
  prohibitions:
    - "The five entries MUST NOT be deleted before `task lint:vuln` has been observed green without them."
    - "`task lint:vuln`'s result MUST NOT be judged by grepping its stdout — exit code only (`.claude/rules/search-tools.md`); a lint that echoes its own fixture strings is exactly the trap that caused the May 2026 re-run loop."
    - "The `cmd/nats-floor-guard` leg and the NATS-related allowlist scoping MUST NOT be touched — that is a separate compensating control for a git-range-only OSV record."
    - "Issue #4817 MUST NOT be closed if any of the five advisories still reports; the issue text is re-scoped to what is true instead."
  artifacts:
    - osv-scanner.toml
  key_links:
    - "`internal/store` -> `golang-migrate/migrate/v4/database/pgx/v5` -> `dhui/dktest` -> `docker/docker` — the chain `go mod why -m` reports today, and the stated cause of all five suppressions"
    - "`osv-scanner.toml` `[[IgnoredVulns]]` -> the `osv-scanner` leg of `task lint:vuln` only (the allowlist is deliberately not scoped to govulncheck)"
---

<objective>
Retire the supply-chain debt that dropping golang-migrate makes retirable: the five
`osv-scanner.toml` `[[IgnoredVulns]]` entries whose stated cause is the
golang-migrate → dktest → docker/docker chain, and issue #4817 that tracks them.

Purpose: an allowlist entry that outlives its cause is a live suppression nobody re-derives. All
five carry `ignoreUntil = 2027-01-15` and a reason naming a dependency this phase deletes. Verified
grounding: `go mod graph` reports exactly two requirers of `github.com/docker/docker` — the main
module's own indirect pin and `golang-migrate/migrate/v4@v4.19.1` — and
`github.com/testcontainers/testcontainers-go@v0.43.0` does **not** require it, so the integration
harness does not keep the chain alive. That makes the retirement plausible; it does not make it
true, which is why this plan measures before it deletes.

Output: five entries retired (or explicitly retained with a corrected reason), and #4817 closed with
evidence or re-scoped.
</objective>

<artifacts_this_phase_produces>
**Modified by this plan:** `osv-scanner.toml` (the five `[[IgnoredVulns]]` blocks for
`GO-2026-4883`, `GO-2026-4887`, `GO-2026-5617`, `GO-2026-5668`, `GO-2026-5746`, and their
explanatory comment block)

**Created by this plan:** nothing in the tree. GitHub side: a closing comment on issue #4817, or a
re-scoping comment if any advisory survives.

**Explicitly NOT touched:** the `cmd/nats-floor-guard` leg of `task lint:vuln`, the NATS
`GHSA-q59r-vq66-pxc2` compensating control, and the header comment explaining the per-entry
`ignoreUntil` expiry requirement.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-migration-framework-adopt-goose-for-go-migrations/01.1-RESEARCH.md
@.planning/phases/01.1-migration-framework-adopt-goose-for-go-migrations/01.1-01-SUMMARY.md
@.claude/rules/search-tools.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the dependency graph, then retire the entries only if it is clean</name>
  <files>osv-scanner.toml</files>
  <read_first>
    - `osv-scanner.toml` lines 1-52 (the header comment explaining the expiry requirement, the
      `docker/docker` block's stated cause, and all five entries)
    - `Taskfile.yaml` around the `lint:vuln` task (the three legs: nats floor guard, govulncheck,
      osv-scanner v2 — and which of them the allowlist is scoped to)
    - `.planning/phases/01.1-.../01.1-RESEARCH.md` §Installation "Side benefit — supply-chain debt
      retires" and §Assumptions Log A3
    - `go.mod` (the `github.com/docker/docker` indirect line, if `go mod tidy` in plan 01 left one)
  </read_first>
  <action>
    Measure first. Run `go mod why -m github.com/docker/docker` and `go mod graph` filtered to
    `docker/docker`, and record both outputs verbatim in the SUMMARY. Before this phase, `go mod
    why -m` reported the chain `internal/store` → `golang-migrate/migrate/v4/database/pgx/v5` →
    `dhui/dktest` → `docker/docker/api/types/container`, and `go mod graph` reported exactly two
    requirers: the main module's own indirect pin and `golang-migrate/migrate/v4@v4.19.1`. Note
    that `testcontainers-go@v0.43.0` does NOT require `docker/docker` — it reaches Docker through
    `docker/go-connections` and `moby/docker-image-spec` — so the integration harness is not a
    surviving path. Confirm that rather than assume it.

    Branch on the measurement:

    - **If `go mod why -m` reports the main module does not need the module**, delete all five
      `[[IgnoredVulns]]` blocks and the `── Allowlisted: test-only github.com/docker/docker vulns ──`
      comment paragraph that introduces them. Leave the file's header comment (the one requiring
      `id` + `reason` + `ignoreUntil` on every entry) and every unrelated entry untouched.
    - **If any path survives**, keep the entries but rewrite each `reason` to name the *actual*
      surviving requirer instead of "via golang-migrate dktest harness", which would otherwise
      become a false statement in the tree. Do not close #4817 in this branch.

    Then run `task lint:vuln` and judge the result by **exit code only**. Do not grep its stdout:
    the lint prints advisory ids and reason strings, so a stdout match proves nothing about
    pass/fail, and `.claude/rules/search-tools.md` records a real May-2026 incident where branching
    on a matched output string produced an infinite re-run. If the run is long, dispatch it via the
    `local-check` offload agent rather than an inline Bash call with a default timeout.
  </action>
  <verify>
    <automated>go mod why -m github.com/docker/docker || true   # measurement, not a gate — see below</automated>
    <automated>task lint:vuln</automated>
    <automated>task lint</automated>
  </verify>
  <acceptance_criteria>
    - The `go mod why` entry is suffixed `|| true` **on purpose**. It is a measurement whose output
      selects the branch, not a pass/fail gate: once the module leaves the module graph entirely,
      `go mod why -m` reports it as an unknown dependency and exits **non-zero on the very success
      path this task is aiming for**. Left bare, it inverts — the cleanest possible outcome reads as
      a failed verify. The real gates in this task are `task lint:vuln` and `task lint`, both judged
      by exit code. Same failure family as a bare negative `rg` (`.claude/rules/search-tools.md`).
    - The SUMMARY records the verbatim `go mod why -m github.com/docker/docker` output, both before
      the decision and as the basis for the branch taken.
    - Either `! rg -q -- 'GO-2026-(4883|4887|5617|5668|5746)' osv-scanner.toml` exits 0 (clean
      branch — note `rg -c` never prints `0`: with no matches it prints nothing and exits 1, so the
      negated-quiet form at `01.1-01-PLAN.md:468` is the one whose exit code is the verdict),
      or all five survive with a `reason` that names the real requirer and contains neither the
      string `golang-migrate` nor `dktest`.
    - `task lint:vuln` exits 0, and the SUMMARY states plainly that the verdict came from the exit
      code, not from matching output text.
    - `rg -n 'nats-floor-guard|GHSA-q59r-vq66-pxc2' osv-scanner.toml Taskfile.yaml` shows those
      surfaces unchanged (`git diff` on them is empty).
    - The `ignoreUntil`-expiry header comment survives in `osv-scanner.toml`.
  </acceptance_criteria>
  <done>
    The allowlist reflects the dependency graph as it actually is after the swap, and `task
    lint:vuln` is green on that basis.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile issue #4817 against the measured result</name>
  <files>osv-scanner.toml</files>
  <read_first>
    - The output of `gh issue view 4817 -R holomush/holomush --json number,title,state,body`
    - `.planning/phases/01.1-.../01.1-05-SUMMARY.md` in progress (Task 1's measurement)
    - `docs/reviews/arch-review/2026-07-11/findings/d9c-deps.md` (the finding that produced the
      allowlist — check whether its claim is now stale)
  </read_first>
  <action>
    Read the current issue body before writing anything; its title is "Revisit allowlisted test-only
    docker/docker vulns (GO-2026-4883/4887/5617/5668/5746)".

    If Task 1 took the clean branch, close #4817 with a comment carrying **grounded evidence**: the
    `go mod why -m` output showing the module is no longer needed, the commit or PR that removed
    golang-migrate, and the `task lint:vuln` exit-code result with the entries deleted. An in-issue
    "fixed by" assertion is not proof; cite the measurement. Include the AI authorship byline the
    repo requires on issue comments.

    If Task 1 took the survives branch, do NOT close the issue. Comment with the surviving requirer,
    state that golang-migrate is no longer the cause, and re-scope the issue title/body to what is
    true — the pattern `09-02` established when two `ec22.9` residue items were found to have
    drifted: re-scope to what is true rather than file at the stale framing.

    In both branches, check `docs/reviews/arch-review/2026-07-11/findings/d9c-deps.md` for a claim
    that is now false. If it names the golang-migrate chain as live, leave the finding document
    itself alone — it is a dated review artifact, not live guidance — but record in the SUMMARY that
    it is now historical, so a future reader does not treat it as current.
  </action>
  <verify>
    <!-- A LISTING, NOT A GATE: this exits 0 whether #4817 is OPEN or CLOSED, and both are
         legitimate outcomes for this task, so no exit-code gate is possible. The verdict for
         this task comes from the SUMMARY's recorded evidence, not from this command's exit
         code. Same treatment as the `gh issue list` at 01.1-07-PLAN.md:372 (labeled at :380). -->
    <automated>gh issue view 4817 -R holomush/holomush --json state,comments --jq '.state'</automated>
  </verify>
  <acceptance_criteria>
    - #4817 is either `CLOSED` with a comment containing the `go mod why -m` evidence and the
      `task lint:vuln` result, or `OPEN` with a comment naming the surviving requirer and a
      re-scoped framing.
    - The comment carries the AI authorship byline.
    - No `gh issue close` was run on the strength of an assumption; the SUMMARY names the specific
      command output that justified the state.
    - The SUMMARY notes whether `docs/reviews/arch-review/2026-07-11/findings/d9c-deps.md` now
      carries a historical claim.
  </acceptance_criteria>
  <done>
    The tracker agrees with the measured dependency graph, with the evidence recorded rather than
    asserted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vulnerability allowlist -> the CI security gate | every `[[IgnoredVulns]]` entry is a live suppression; one whose cause is gone silently widens the accepted risk |
| third-party module graph -> the shipped binary | what `go mod tidy` leaves in the build list determines what govulncheck and osv-scanner can see |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-22 | Tampering | deleting suppressions whose cause survives | high | mitigate | Measure with `go mod why -m` and `go mod graph` BEFORE deleting; branch on the measurement; `task lint:vuln` must be observed green (by exit code) without the entries. Deleting five suppressions on an assumption is the failure this task is shaped to prevent. |
| T-01.1-23 | Repudiation | closing #4817 without evidence | medium | mitigate | The closing comment must carry the `go mod why -m` output and the `task lint:vuln` result. `.claude/rules/subagent-briefing.md` names in-issue "fixed by" claims as insufficient proof for a close. |
| T-01.1-24 | Denial of service (of the control) | judging `lint:vuln` by matching its stdout | medium | mitigate | Exit code only. The lint prints advisory ids and reason strings, so a stdout match cannot distinguish pass from fail — the exact shape of the May-2026 re-run-loop incident in `.claude/rules/search-tools.md`. |
| T-01.1-25 | Tampering | collateral damage to the NATS compensating control | medium | mitigate | `git diff` on the `nats-floor-guard` surfaces must be empty; it guards a git-range-only OSV record no manifest scanner can flag and is unrelated to this change. |
</threat_model>

<verification>
- `task lint:vuln` exits 0 (all three legs). **Long-running: govulncheck plus osv-scanner v2 takes several minutes; dispatch through `local-check` rather than an inline default-timeout call.**
- `task lint` is green.
- `git diff osv-scanner.toml` touches only the `docker/docker` block.
- #4817's state matches the measurement, with the evidence in the comment.
</verification>

<success_criteria>
The vulnerability allowlist contains no suppression whose stated cause this phase removed, the
security gate is green on that basis measured by exit code, and the tracking issue's state was
decided by a command's output rather than by an assumption about what dropping golang-migrate
implies.
</success_criteria>

<output>
Create `.planning/phases/01.1-migration-framework-adopt-goose-for-go-migrations/01.1-05-SUMMARY.md` when done.
</output>
