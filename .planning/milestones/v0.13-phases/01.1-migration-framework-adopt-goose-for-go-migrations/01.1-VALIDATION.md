---
phase: 01.1
slug: migration-framework-adopt-goose-for-go-migrations
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-02
validated: 2026-08-16
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `01.1-RESEARCH.md` § Validation Architecture (lines 917-977), whose
> claims were verified by execution against `postgres:18-alpine`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go `testing` + `stretchr/testify` (unit); Ginkgo v2 / Gomega (integration, `internal/store/store_suite_test.go:22`); testcontainers `postgres:18-alpine` |
| **Config file** | `Taskfile.yaml` (`test:` at `:186`, `test:int:` at `:266`); `.golangci.yaml`; `.codecov.yml` |
| **Quick run command** | `task test -- ./internal/store/ ./cmd/holomush/ ./internal/bootstrap/` |
| **Full suite command** | `task test && task test:int` (integration needs Docker) |
| **Gate before push** | `task pr-prep` (fast lane) — MUST run inline in the parent per `CLAUDE.md` |
| **TDD** | `tdd_mode: true` in `.planning/config.json` — RED/GREEN/REFACTOR gates enforced |

---

## Sampling Rate

- **After every task commit:** `task test -- ./internal/store/ ./cmd/holomush/ ./internal/bootstrap/`
- **After every plan wave:** `task test && task test:int` — **`test:int` is mandatory, not optional**, for
  every wave touching `store.Migrator` (`task test` does NOT compile `//go:build integration` files, so a
  refactor of shared types breaks silently otherwise)
- **Before `/gsd-verify-work`:** full suite green, then `task pr-prep` inline
- **Max feedback latency:** ~90s for the quick run

---

## Per-Task Verification Map

No REQ-IDs map to this phase (enabling work). The acceptance surface is the five ROADMAP Success Criteria.

| Criterion | Behavior | Test Type | Automated Command | File Exists | Status |
|-----------|----------|-----------|-------------------|-------------|--------|
| **C1a** | 88 files → 44 single-file goose migrations; every file parses (`-- +goose Up`/`Down` present, `$$` bodies wrapped) | unit / meta | `task test -- -run TestMigrationsFS ./internal/store/` | ✅ `migrate_embed_test.go` | ✅ green |
| **C1b** | Fresh-DB migrate-up yields an **application schema** byte-identical to pre-conversion | one-time manual proof, deliberately NOT a committed test (D-15) | `pg_dump --schema-only -T public.schema_migrations -T public.goose_db_version` ×2, `diff` empty; recorded in the PR | n/a by design | ✅ discharged — see Manual-Only |
| **C1c** | Bookkeeping end-state: `goose_db_version` with 44 rows at `version_id > 0` plus the version-0 row, no `schema_migrations` | integration | `task test:int -- -run TestStore ./internal/store/` | ✅ `migrate_integration_test.go` | ✅ green (CI `07ca74c46`) |
| **C2a** | Adopt against a DB seeded at the pre-conversion version produces the derived row set, **ascending**, and renames the old table | integration (testcontainer) | `task test:int -- -run TestStore ./internal/store/` | ✅ `migrate_adopt_integration_test.go` | ✅ green (CI `07ca74c46`) |
| **C2b** | **Negative control:** a second adopt is a no-op (populated `goose_db_version` ⇒ skip) | integration | same | ✅ `migrate_adopt_integration_test.go` | ✅ green (CI `07ca74c46`) |
| **C2c** | **Negative control:** `dirty = true` ⇒ refuse with a named-version error, no writes (D-05) | integration | same | ✅ `migrate_adopt_integration_test.go` | ✅ green (CI `07ca74c46`) |
| **C2d** | **Ordering regression guard:** adopt, then `DownTo(0)`, must roll back in descending-version order | integration | same | ✅ `migrate_adopt_integration_test.go` | ✅ green (CI `07ca74c46`) |
| **C2e** | Concurrency: two concurrent adopts ⇒ exactly one seeds, loser no-ops (D-03 in-lock re-check) | integration | same | ✅ `migrate_adopt_integration_test.go` | ✅ green (CI `07ca74c46`) |
| **C3a** | `holomush migrate up/down/status/version` behave equivalently | unit (injectable `migrator` iface, `cmd/holomush/migrate.go:23-32`) | `task test -- ./cmd/holomush/` | ✅ `cmd/holomush/migrate_test.go` (rewritten fake, M7) | ✅ green |
| **C3b** | `holomush migrate force` **removed**, with docs and tests updated (maintainer decision 2026-08-02) | unit | `task test -- ./cmd/holomush/` + absence assertion | ✅ `migrate_test.go:23-59` — **set equality**, not `NotContains`; also forbids `dirty`/`DIRTY` in help strings | ✅ green |
| **C3c** | Startup auto-migration still runs before bootstrap, `HOLOMUSH_DB_AUTO_MIGRATE` honored | unit + integration | `task test -- -run 'TestAutoMigrate\|TestParseAutoMigrate' ./cmd/holomush/`; `task test:int -- ./cmd/holomush/` | ✅ `automigrate_test.go:43-428`, `automigrate_integration_test.go`, `internal/bootstrap/migration_test.go:33-38` | ✅ green |
| **C4** | A Go migration between two SQL migrations runs in version order, observing the prior SQL step's effect | integration (fixture chain, `WithGoMigrations` + `WithDisableGlobalRegistry`) | `task test:int -- -run TestStore ./internal/store/` | ✅ `migrate_gointerleave_integration_test.go` | ✅ green (CI `07ca74c46`) |
| **C4b** | D-09: an irreversible Go migration's down returns an error rather than succeeding | integration | same fixture | ✅ `migrate_gointerleave_integration_test.go` | ✅ green (CI `07ca74c46`) |
| **C5a** | D-13 `$$` ↔ `StatementBegin`/`StatementEnd` pairing across `internal/store/migrations/*.sql` | meta | `task test -- -run TestMigrations ./internal/store/` | ✅ `migrations_format_test.go:146,327` — the violation-string split at `:144` is what **makes a demonstrated RED possible**; table case at `:168` asserts `fixture.sql:2:` | ✅ green |
| **C5b** | `.claude/rules/database-migrations.md` + site guide no longer claim `.up.sql`/`.down.sql` pairing | doc + lint | `task lint` | ✅ rule rewritten to single-file `NNNNNN_name.sql` | ✅ green |
| **C5c** | `lint:no-timestamptz` still matches migration files after the rename | bats / meta | `task lint:no-timestamptz` **plus** a static assertion its glob is `*.sql` | ✅ `lint_meta_test.go:100` (store glob) **and** `:122` (plugin migrator stays separate — the L1/`:907` must-not-change constraint) | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] Rewrite `internal/store/migrate_embed_test.go` — filename pattern + annotation presence (C1a, M1)
- [x] New `internal/store/migrations_format_test.go` — D-13 `$$`↔StatementBegin pairing meta-test (C5a), **with a demonstrated red fixture**
- [x] New `internal/store/migrate_adopt_integration_test.go` — Ginkgo + testcontainer; C2a–C2e including the **ascending-order rollback guard** and both negative controls
- [x] New `internal/store/migrate_gointerleave_integration_test.go` — C4 + C4b fixture chain (copy RESEARCH §Pattern 4; `WithGoMigrations` + `WithDisableGlobalRegistry(true)` so nothing permanent lands)
- [x] New `store.MigrationSectionForTest(name, direction)` in `internal/store/migrations_export_test.go` (M6) + migrate three callers (M3, M4, M5)
- [x] Rewrite `internal/store/migrate_test.go` — replace `mockMigrate`/`migrateIface`; delete the scheme-rewrite, `Force`, `Steps`, dual-`Close`, and dirty-`Version` families (M7)
- [x] Replace the dirty-state specs at `internal/store/migrate_integration_test.go:229, 361` (S5)
- [x] New static assertion that `lint:no-timestamptz`'s glob matches post-conversion filenames (C5c) — `lint_meta_test.go:100`
- [x] Fix `Taskfile.yaml:899` glob (L1) — **`:907` must NOT change** (`plugins/*/migrations/` is a separate migrator, `pkg/plugin/storage/storage.go:69`, that goose never sees). The must-not-change half is now pinned by `lint_meta_test.go:122`.
- [x] Re-ground `cmd/holomush/main_test.go:150` and `cmd/holomush/core_test.go:138` (S2)
- [x] No framework install needed — Ginkgo, testify, gotestsum, testcontainers all present

---

## Manual-Only Verifications

| Behavior | Criterion | Why Manual | Test Instructions | Outcome |
|----------|-----------|------------|-------------------|---------|
| Application-schema byte-identity, pre- vs post-conversion | C1b | D-15: after the cutover lands there is no pre-conversion side left to compare against, so a committed gate would be drift detection wearing this criterion's clothes | Dump both sides with `pg_dump --schema-only -T public.schema_migrations -T public.goose_db_version`; `diff` must be empty; paste the result into the PR | ✅ **Discharged** — `01.1-UAT.md` test 1 `result: pass`. 2224 lines each side, `diff` empty (exit 0). Recorded gotcha: pg_dump 18 emits `\restrict`/`\unrestrict` session tokens that are **random per invocation**, so the raw diff showed 2 differing lines until those were stripped (2222 identical each side after). Anyone re-running this proof must strip them. |
| Sandbox rehearsal on a fresh pre-deploy Kopia snapshot | D-16 | Requires real sandbox data; the integration test only proves the mechanism on synthetic data seeded at v53 | `sandbox-restore.md` "Restore path A" — throwaway Postgres, run the new binary's migrate, verify the rows, the rename, and the schema | ✅ **Discharged** — `01.1-UAT.md` test 2 `result: pass`. Sat at `blocked_by: release-build` until **v0.12.1** shipped (`20ccac13f`); the rehearsal was then driven and the phase closed in `efb6e870b` (#4946), taking UAT 2/3 → **3/3 pass, 0 blocked**. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 90s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validated 2026-08-16 by `/gsd-validate-phase 01.1`.

---

## Validation Audit 2026-08-16

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

Reconciled against `e124617b4`. All 11 Wave-0 artifacts landed; all **15**
automated criteria green — the map carries 16 `C` rows, less **C1b**, which is
manual by design per D-15 (`task test` PASS — 11852 ok / 4 skipped — on this
tree for the unit rows, CI `success` at `07ca74c46` for the seven integration
rows). Both
manual-only rows are discharged with recorded evidence, so `nyquist_compliant`
is `true` under the precedent set by phases 02 and 05 (manual rows are
permitted when each carries an adjudicated outcome).

Every named test symbol was checked for **existence**, not just exit status:
`go test -run <NoSuchTest>` exits 0, so a map naming a test that does not exist
reports green vacuously. `TestMigrationsFS`, `TestMigrations`, `TestAutoMigrate`,
`TestParseAutoMigrate`, and `TestStore` all resolve.

Two criteria deserve note for having been built to resist exactly that failure:
C5a splits its violation strings specifically so a **demonstrated RED** is
possible against an unwrapped `$$` fixture (`migrations_format_test.go:144`),
and C3b uses **set equality** rather than `NotContains("force")` because — in its
own words — a bare `NotContains` "passes vacuously if the command tree fails to
build at all" (`cmd/holomush/migrate_test.go:23-24`).
