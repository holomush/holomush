---
phase: 02-abac-schema-vocabulary
plan: 11
type: execute
wave: 6
depends_on: ["02-02", "02-04", "02-05", "02-06", "02-08", "02-09", "02-10", "02-12", "02-13"]
files_modified:
  - .planning/phases/01-portal-spec/01-SPEC.md
  - .planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md
  - .planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md
  - docs/architecture/invariants.yaml
  - docs/architecture/invariants.md
autonomous: true
requirements: [IDENT-06, IDENT-07, IDENT-08, IDENT-09, PROFILE-11, EXT-07]

estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "01-SPEC.md §8.5.1.1 records option 2 as REJECTED with its reason, and records D-01's viewer-flavored twins as the settled shape — so a later reader does not re-open the choice the phase already made."
    - "01-SPEC.md §11.3's permitted sort/filter table gains a last_active_at row and a joined players.username row; the existing characters.player_id row ('never an ordering') stays unchanged, because A2 is a different claim rather than a contradiction of it."
    - "01-SPEC.md's stale internal/store/migrations/*.up.sql citations are corrected to the current single-file goose paths and line numbers, so a later planner transcribing one does not produce an Edit-tool failure."
    - "01-SPEC.md §6.1.2 records that Mechanism A is implemented over the Script property from stdlib unicode.Scripts rather than Script_Extensions, that the approximation can only miss a rejection and never add one, and that a rune with no script assignment is rejected fail-closed."
    - "01-SPEC.md §13's INV-WORLD-6 text enumerates BOTH sanctioned tombstone-emitting deleters, matching the registry summary plan 02-04 corrected, so the spec and the registry do not disagree about which paths release a name."
    - "01-SPEC.md §8.4.1's viewer attribute table lists five keys — tier, player_id, has_player_id, roles, has_roles — and §8.5 records the derived player-keyed property peers and why the DSL forces them, so a later reader does not see owner and owner_player_id side by side and delete one."
    - "01-SPEC.md §8.6 and 02-CONTEXT.md D-03 record that the v0.13 seed emits TWO tier-floor policies, not three, because the player rung has no seeded member and the DSL list grammar requires at least one literal — the deviation is written down where D-03 is read, not left in a plan SUMMARY. This plan OWNS 02-CONTEXT.md and its Task 1 edits it; the count D-03 records is compared against the count actually present in internal/access/policy/seed.go, so the check is derived rather than self-satisfying."
    - "01-SPEC.md §8.5 records the derived property peers' DERIVATION DIRECTION per 02-CONTEXT.md D-27 — permit-side peers computed in the ALL direction, the forbid-side peer in the ANY direction — because the direction is invisible in the twins' DSL text and a reader comparing a twin against its character-flavored original has nowhere else to find whether the viewer path widens access across a player's alternate characters."
    - "docs/architecture/invariants.yaml's INV-WORLD-4 summary says THREE sanctioned out-of-world writers, not two, naming plan 02-12's operator name-resolution command as the third and stating the property that qualifies it — 02-06's CharacterRepository.Rename emits its outbox envelope inside its own transaction. A bound invariant whose text is false beside a shipped third writer is the false-green .claude/rules/invariants.md forbids, and the count is amended deliberately rather than the atomicity clause being weakened."
    - "02-CONTEXT.md D-03's tier-floor count is COMPARED against internal/access/policy/seed.go and a disagreement HALTS this plan. It is never reconciled by editing D-03: a verification step whose failure mode is 'edit the thing being verified' launders authorization drift into the specification and has no failure mode at all."
    - "Every amendment row is APPLIED to its own artifact, and each application is proven by a recorded search for the superseded string rather than asserted — recording an amendment is not applying it."
    - "02-VALIDATION.md's Per-Task Verification Map covers every task in plans 02-01 through 02-10 plus 02-12 and 02-13 — every plan in the phase except this one's own self-referential closeout tasks — its frontmatter status is set to validated, and nyquist_compliant reflects the observed state rather than an aspiration."
    - "The RED-first table carries one row per RED demonstration named in ANY plan's verification_integrity rule 4, with the count DERIVED from those plans rather than fixed in this plan's prose, and each row records an observed non-zero exit rather than a projected one."
    - "02-VALIDATION.md records that ROADMAP success criterion 4's in-world-description half is NOT discharged in Phase 2 — D-29 defers the resource is character permit that carried characters.description to Phase 4 — and the SUMMARY escalates the wording question to the maintainer with three options and none selected. Criterion 1 is explicitly NOT in the same position: D-30 records that its wording needs no amendment, and it MUST NOT be touched."
    - "abac-reviewer has reviewed the phase diff and returned READY before the phase merges."
  artifacts:
    - .planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md
  key_links:
    - "01-SPEC.md §14's amendments table gains one row per amendment, each quoting the superseded text verbatim alongside its artifact and location, because the quoting is what forces the search that catches a string still live somewhere nobody inventoried."
    - "Each amendment's search target is a substring lying on a SINGLE source line — a multi-line quote searched line-wise returns zero matches whether or not the text survives, which would be a proof that cannot fail."
---

<objective>
Close the phase: apply the eight amendments this phase owes, fill the
validation map, and route the diff to `abac-reviewer`.

**Three amendments were added in the reviews revision (02-REVIEWS.md).** Amendments
A–D are the phase's original four. E, F and G come from findings the review raised
elsewhere in the set whose spec-side text this plan owns: §13's INV-WORLD-6
enumeration (corrected in the registry by plan `02-04` and needing the matching
spec edit), §8.4.1's viewer attribute table and §8.5's derived property peers (the
viewer-identity settlement in plans `02-13` and `02-07`), and §8.6 / D-03's
tier-floor count (the two-versus-three deviation plan `02-07` records). An amendment
that lives only in a plan SUMMARY is invisible to the next planner, which is the
failure §14's preamble exists to name.

Purpose: §14's own preamble records the failure this plan exists to avoid —
*recording an amendment is not applying it.* A downstream planner reads the
artifact directly, so a §8.5.1.1 that still presents option 2 as live will be
planned against regardless of what a table elsewhere says about it.
`.claude/rules/references/design-review-learnings.md` names exactly this — an
amendment row whose superseded string is still live in a sibling artifact — as a
recurring review failure.
Output: eight applied amendments with recorded search proofs — six landing in
`01-SPEC.md`, Amendment G additionally in `02-CONTEXT.md` D-03 (which this plan owns
as of the cycle-2 fix pass), and Amendment H in `docs/architecture/invariants.yaml` —
a filled `02-VALIDATION.md`, and an `abac-reviewer` verdict.

**Amendment H is new in the cycle-3 pass (02-REVIEWS.md, B-12).** Plan `02-12` ships
a THIRD out-of-world writer — the operator name-resolution CLI — while
`INV-WORLD-4`'s registry summary still says "exactly **TWO**". `02-12` cannot carry
the edit itself: plan `02-09` also modifies `docs/architecture/invariants.yaml` and is
in the same wave, so an edit there would be a same-wave file collision. This plan is
wave 6, already depends on both, and is the phase's amendment pass — so the registry
text lands here, with `02-12`'s SUMMARY recording that it is outstanding and naming
this plan as its owner.

**This plan writes only to `.planning/` artifacts and the invariant registry.** It
ships no code, no schema and no policy; the registry edit is Amendment H's text plus
the `cmd/inv-render` regeneration it forces. Its requirement list carries all six phase requirement IDs because it is
the plan that records their discharge and closes the traceability loop, not because
it implements any of them — every one is implemented in plans `02-01` through
`02-10`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md
@.planning/phases/02-abac-schema-vocabulary/02-RESEARCH.md
@.planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md
@.planning/phases/01-portal-spec/01-SPEC.md
@.claude/rules/planning-artifacts.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Apply the amendments — six in 01-SPEC.md, one also in 02-CONTEXT.md, one in the invariant registry — and prove each landed</name>
  <files>.planning/phases/01-portal-spec/01-SPEC.md, .planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md, docs/architecture/invariants.yaml, docs/architecture/invariants.md</files>
  <read_first>
    - .planning/phases/01-portal-spec/01-SPEC.md §14 in full — especially the preamble ("recording an amendment is not applying it"), the note on quotations and single-line search targets, and the ten existing rows as the format to extend
    - docs/architecture/invariants.yaml lines 5045-5062 — `INV-WORLD-4`'s WRITER-BOUNDARY summary, specifically the clause "there are exactly **TWO** sanctioned out-of-world writers, each emitting its envelope atomically", and its `asserted_by:` list. Plan `02-12` ships a third (Amendment H below)
    - .claude/rules/invariants.md — the define/respect/bind workflow, and the rule that `docs/architecture/invariants.md` is GENERATED and must never be hand-edited inside its `<!-- BEGIN GENERATED -->` regions
    - .planning/phases/01-portal-spec/01-SPEC.md §8.5.1.1 (the section being amended), §11.3 (the permitted sort/filter table), §6.1.2 (Mechanism A), and every `internal/store/migrations/*.up.sql` citation in the file
    - .planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md D-05, D-26
    - .planning/phases/02-abac-schema-vocabulary/02-RESEARCH.md § Concerns items 1 and 5
    - .planning/phases/02-abac-schema-vocabulary/02-02-SUMMARY.md and 02-07-SUMMARY.md (what actually shipped, so the amendments describe the code rather than the plan)
    - .claude/rules/planning-artifacts.md (do not invent structure in a tool-owned file; §14's table is the sanctioned shape and is what to extend)
  </read_first>
  <action>
Apply **eight** amendments, each **in place in §14's table AND in the affected
artifact**, and record a single-line search proof for each in the SUMMARY. Six land
in `01-SPEC.md`; Amendment G lands in `01-SPEC.md` §8.6 **and** in
`02-CONTEXT.md` D-03, which this plan owns; Amendment H lands in
`docs/architecture/invariants.yaml` and is new in the cycle-3 pass.

**Amendment A — §8.5.1.1, term B's shape (D-05).** Rewrite the section to record
that **option 2 is REJECTED**: term B evaluating against a co-located character
subject and DENYING where one does not exist violates §8.8 / INV-PRIVACY-10 at the
`anonymous` rung. An anonymous viewer has no character; `profile.pronouns` is an
`entity_properties` row seeded at the `anonymous` floor; so term B would deny it and
a reachable profile would carry `name` without `pronouns`. The failure is **closed**,
so no test in the suite would catch it — it surfaces as "the public profile looks
bare", which is precisely the symptom §8.5.1.1 warns provokes the forbidden repair.
Record D-01's viewer-flavored twins as the settled shape, name the five shipped
twin policies, and record that `seed:property-owner-write` deliberately has none.
Keep the section's core prohibition — removing term B is a normative violation, not
a tuning decision — verbatim.

**Amendment B — §11.3, two new sort/filter rows (D-26).** Add a `characters.last_active_at`
row (Sort: Yes, Filter: Yes) with its safety rationale: it is intrinsic row metadata
carrying no profile content, and the `0` epoch sentinel sorts never-active rows last
under most-recent-first without a `NULLS LAST` clause. Add a joined
`players.username` row for the linkage the admin list already sorts by and §11.3
never enumerated. **Leave the existing `characters.player_id` row unchanged** — its
"never an ordering" verdict stays correct, and the new row is a different claim
rather than a contradiction of it. Say so explicitly in the new row, because a
reader seeing two player-linkage rows will otherwise read one as superseding the
other.

**Amendment C — stale migration citations.** The goose cutover collapsed every
`.up.sql`/`.down.sql` pair into one `.sql` file and shifted every line number, so
**every** `internal/store/migrations/*.up.sql` citation in `01-SPEC.md` is stale in
both filename and line number. Enumerate them with a search, re-derive each against
the current tree, and correct them. Do not transcribe corrected values from
`02-RESEARCH.md` — re-derive, because the tree has moved again since it was written.
Record the count found and the count corrected.

**Amendment D — §6.1.2's Script-vs-Script_Extensions divergence.** Record that
Mechanism A is implemented over the **Script** property from Go's stdlib
`unicode.Scripts`, not over **Script_Extensions**, which UTS #39's Moderately
Restrictive profile is defined over; that no Go module surveyed supplies
Script_Extensions and `golang.org/x/text` has no script package at all; that the
approximation can only ever **miss** a rejection the faithful profile would make and
never add one, and is closed-direction for §6.1.2's three named rejections because
Cyrillic and Greek letters carry real Script values; and that a rune with no script
assignment — the Unicode-15-stdlib versus Unicode-17-confusables skew — is
**rejected** fail-closed. Record it as a divergence with its rationale, in the shape
§14 row 10 uses for the grid-parity divergence, so a reviewer reads it as a choice
rather than an oversight.

**Amendment E — §13's INV-WORLD-6 text (from cross-AI review HIGH #5).** The
registry entry's "ONLY path" claim was already falsified by
`auth.CharacterReapingService`, and plan `02-04` corrects the registry summary in
the same change that binds the invariant. §13 carries the same claim and must not be
left disagreeing with it. Rewrite §13's INV-WORLD-6 text to the corrected form —
retirement never releases the name; a name becomes claimable again only through a
sanctioned tombstone-emitting hard-delete path, of which there are exactly two,
`world.Service.DeleteCharacter` and `auth.CharacterReapingService`. Take the exact
wording from `02-04`'s SUMMARY, which records the before/after summary text, rather
than re-deriving it — two independent rewordings of one guarantee is how the
registry and the spec drifted apart in the first place.

**Amendment F — §8.4.1's viewer attribute table and §8.5's derived peers (from the
review's viewer-identity finding).** §8.4.1 declares exactly three viewer keys;
the phase ships five, adding `roles` and `has_roles`. Add both rows with the
presence rules plan `02-03` implements (`roles` present only on the guest and player
rungs, and only when the lookup is configured and succeeds; `has_roles` on every
path). Then add to §8.5 a short subsection recording the three derived
player-keyed property peers — `owner_player_id`, `visible_to_players`,
`excluded_from_players` — with the reason they exist: the shipped row semantics are
character-keyed, a `viewer:` subject is player-flavored, and the DSL cannot
intersect two attribute lists (`in` needs a scalar left operand,
`containsAny` takes literal needles), so the character→player relation is resolved
in the provider from the row's own fields. State explicitly that the peers are
derived from the ROW and never from the caller. Without this, §8.5 describes a
mapping the code does not have, and the next reader "simplifies" the duplication
away.

**Record the DERIVATION DIRECTION in the same subsection — `02-CONTEXT.md` D-27.**
Each peer is computed in the direction that cannot widen its own policy's effect:
the permit-side peers `owner_player_id` and `visible_to_players` hold a player only
when **every** character of that player appears in the row's character-keyed field,
while `excluded_from_players` holds a player when **any** of theirs does. State that
the plain player union was proposed for both sides and **declined for the permit
side**, because it broadens a grant an author made to a named character into a grant
to the human behind it — an authorization widening across a player's alternate
characters that no artifact had decided. State the consequence too: a player holding
two or more characters does not receive an `owner` or `visible_to` permit through
the viewer path unless the row names all of their characters, so the web path can be
narrower than the grid for identity-keyed rows.

This belongs in the SPEC and not only in a provider comment for the same reason
Amendment F exists at all: the twins' DSL text is identical in shape to their
character-flavored originals, so a reviewer asking "does the viewer path widen
access across a player's characters" has nowhere to look. If plan `02-13`'s Task 0
checkpoint selected the union instead, record THAT here — the amendment describes
what shipped, and `02-13`'s SUMMARY is the source for which option was taken.

**Amendment G — §8.6 and D-03's tier-floor count. This amendment touches TWO
artifacts, and the second one is the point.** D-03 as originally written mandated
three tier-floor policies; the v0.13 seed ships two, because §8.6 seeds no attribute
at the `player` rung and the DSL list grammar requires at least one literal
(`internal/access/policy/dsl/ast.go:232-236`), so a third policy cannot be written
without inventing a member.

Record the deviation in `01-SPEC.md` §8.6 beside the posture table: one policy per
rung **that has at least one seeded member**, and the moment any row is seeded at
`player` the third policy becomes both writable and required. Note that plan `02-07`
ships a conditional guard test that turns RED at exactly that moment.

**Then apply it in `02-CONTEXT.md` D-03 itself, and verify the count against the
code.** The cycle-2 review found this plan claiming a D-03 amendment it neither
owned nor edited — the must-have said the deviation would be "written down where
D-03 is read", while `files_modified` listed only `01-SPEC.md` and
`02-VALIDATION.md`, so the claimed fix had no executable path (C2-20; it was named
as the clearest instance of the failure mode that cycle was chartered to hunt). This
plan now owns `02-CONTEXT.md`.

The cycle-2 fix pass already wrote the D-03 amendment into `02-CONTEXT.md` at
planning time, so this task's job is **not** to write it again. It is to confirm the
record is still TRUE of the code that shipped: read the amended D-03, count the
distinct `seed:profile-tier-floor-` entries actually present in
`internal/access/policy/seed.go`, and assert the two agree. D-03's answer is **two**
(`seed:profile-tier-floor-anonymous` and `seed:profile-tier-floor-guest`), per its
2026-08-04 amendment.

**On disagreement this task FAILS. It does NOT update D-03 (cross-AI review cycle 3,
B-16).** The earlier wording said "if the count differs, update D-03", which makes
the check self-healing and therefore not a check at all: given code that emits an
extra `player`-rung policy or omits the guest one, this task would rewrite the locked
decision until equality held — **laundering an authorization drift into the
specification** rather than detecting it. A verification step whose failure mode is
"edit the thing being verified" has no failure mode.

So: if the counts disagree, **stop and report**, naming both numbers, the entry names
found in `seed.go`, and the entry names D-03 records. Do not edit `02-CONTEXT.md`, do
not edit `01-SPEC.md` §8.6, and do not proceed to the remaining amendments. A
disagreement means one of two things, and both need a human: either `02-07` shipped a
policy set nobody decided, or §8.6 gained a `player`-rung member during the phase —
which is precisely the re-entry condition D-03 records, and which is a **decision to
re-open D-03**, not a count to reconcile. Escalate it as such.

The check is a check because it can fail loudly, not because it can converge.

This is a **recorded deviation from a locked decision**, not a re-decision — say so
in both artifacts, so a reader of D-03 finds the discrepancy explained rather than
discovering it in the code.

**Amendment H — `INV-WORLD-4`'s out-of-world writer count (from cross-AI review
cycle 3, B-12). NEW in this pass.** `docs/architecture/invariants.yaml:5048-5056`
states there are "exactly **TWO** sanctioned out-of-world writers, each emitting its
envelope atomically" — the character-genesis application service and the
character-reaping application service. Plan `02-12` ships a **third**: the operator
name-resolution command `holomush character name set`, which runs in `cmd/holomush`
(already in the import-graph allowlist at
`test/meta/world_import_graph_test.go:79`) and writes `characters` through
`CharacterRepository.Rename`.

Amend the summary to say **THREE**, and name the third writer with the property that
qualifies it: plan `02-06` makes `Rename` emit its outbox envelope **inside its own
transaction**, so the CLI's write carries an envelope even though the command discards
the returned delta. Add `02-12`'s CLI integration spec —
`cmd/holomush/cmd_character_name_integration_test.go`, which asserts the outbox row is
committed alongside the renamed row — to the entry's `asserted_by:` list, so the
amended count is proven rather than merely written.

Then regenerate the rendered view with `go run ./cmd/inv-render` and confirm
`git diff --exit-code -- docs/architecture/invariants.md` is clean afterwards.
**Never hand-edit `invariants.md`** inside its generated regions
(`.claude/rules/invariants.md`).

**Why this is an amendment rather than a violation to fix in code.** The alternative —
routing the CLI through one of the two existing sanctioned writers — does not fit:
neither genesis nor reaping is a rename, and inventing a third application service for
one operator command is more machinery than the guarantee needs. The invariant's
substance is *every out-of-world write emits its envelope atomically*, and the CLI
satisfies that. What was false was the **count**, and leaving a `bound` invariant
whose text is false is the exact failure `.claude/rules/invariants.md` forbids. So the
count is amended deliberately, with the justifying property named — not silently, and
not by weakening the atomicity clause.

**Prove each amendment landed.** For each of the eight, pick a search target that is
a substring lying on a **single** source line — a multi-line quote searched line-wise
returns zero matches whether or not the text survives, which would be a proof that
cannot fail — run the search, and record the target and its result in the SUMMARY.
  </action>
  <verify>
    <automated>[ "$(rg -o 'migrations/[0-9]*_[a-z_]*\.up\.sql' .planning/phases/01-portal-spec/01-SPEC.md | wc -l)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(rg -o 'migrations/[0-9]*_[a-z_]*\.up\.sql' .planning/phases/01-portal-spec/01-SPEC.md | wc -l)" -eq 0 ]` — the gate COUNTS matches and compares numerically. It does not read `rg`'s exit code (which is 1 on the zero matches that mean success), and it does not use `|| true` to paper over that: a failed `rg -c` prints NOTHING, so `test "$(rg -c … || true)" = "0"` compares an empty string against `"0"` and is false. That was the cycle-2 defect (C2-9) on this plan's own closeout gate — a gate that fails permanently on the success path, whose cheapest apparent repair is deletion
    - `rg -n 'option 2' .planning/phases/01-portal-spec/01-SPEC.md` shows it described as rejected, and the section names the five shipped viewer twin policies
    - §11.3's table contains a `characters.last_active_at` row and a `players.username` row, and its `characters.player_id` row is byte-identical to its previous content
    - §6.1.2 contains the words `Script_Extensions` and a statement that the approximation can only miss a rejection
    - §13's INV-WORLD-6 text names both `world.Service.DeleteCharacter` and `CharacterReapingService`, and `[ "$(rg -A 4 'INV-WORLD-6' .planning/phases/01-portal-spec/01-SPEC.md | rg -o 'ONLY' | wc -l)" -eq 0 ]` — matching the registry summary `02-04` corrected. Counted with `-o | wc -l` and a numeric `-eq` because `rg -c` prints nothing and exits 1 on the zero matches that mean success
    - §8.4.1's viewer attribute table has five rows including `roles` and `has_roles`, and §8.5 names all three derived property peers with the DSL reason they exist
    - §8.5 also states the derivation DIRECTION — ALL on the permit side, ANY on the forbid side — names `02-CONTEXT.md` D-27, and records that the plain player union was declined for the permit side. If `02-13`'s Task 0 selected the union instead, §8.5 records THAT and cites `02-13`'s SUMMARY; either way the SPEC describes what shipped
    - §8.6 records the tier-floor-count deviation from D-03 with its cause and its re-entry condition
    - **`02-CONTEXT.md` D-03 states the tier-floor policy count, its cause and its re-entry condition, and that count EQUALS the number of distinct `seed:profile-tier-floor-` entries actually present in `internal/access/policy/seed.go`** — derived on the code side. D-03's recorded answer is **two**. Assert it mechanically: `[ "$(rg -o 'seed:profile-tier-floor-[a-z]+' internal/access/policy/seed.go | sort -u | wc -l)" -eq 2 ]`. Record both numbers in the SUMMARY
    - **On disagreement this task FAILS and reports; it MUST NOT edit `02-CONTEXT.md` D-03 to restore equality** (B-16). The earlier form said to update D-03 if the count differed, which makes an authorization drift self-heal into the specification. The SUMMARY records the halt, both counts, the entry names found in `seed.go`, and which of the two causes applies — an undecided policy set, or a §8.6 `player`-rung member that re-opens D-03. Neither is a count to reconcile
    - `git diff --stat .planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md` shows this task did NOT modify D-03's count on a green run, and the SUMMARY records that D-03's existing record was verified against the code WITH both counted numbers — an unverified "already correct" is the C2-20 failure repeating, and a *corrected* count is the B-16 failure
    - §14's table has EIGHT new rows, each quoting its superseded text verbatim alongside artifact and location; Amendment G's row names BOTH artifacts it touches, and Amendment H's row names `docs/architecture/invariants.yaml`
    - **Amendment H:** `[ "$(rg -A 12 'id: INV-WORLD-4' docs/architecture/invariants.yaml | rg -o 'exactly TWO sanctioned' | wc -l)" -eq 0 ]` and `rg -A 12 'id: INV-WORLD-4' docs/architecture/invariants.yaml | rg -n 'exactly THREE sanctioned'` matches — the count is amended, not left false beside a third shipped writer (B-12)
    - Amendment H's amended text names the operator name-resolution command as the third writer AND states the property that qualifies it (`Rename` emits its envelope inside its own transaction), and the entry's `asserted_by:` gains `cmd/holomush/cmd_character_name_integration_test.go`
    - `go run ./cmd/inv-render && git diff --exit-code -- docs/architecture/invariants.md` exits 0 — the rendered view was regenerated, not hand-edited
    - `task test -- -run 'TestEveryRegistryInvariantHasBinding|TestProvenanceGuard|TestBoundInvariantsAreGenuinelyAsserted' ./test/meta/` exits 0
    - The SUMMARY records, for each of the eight amendments, the single-line search target used and its result
    - `.planning/ROADMAP.md` and `.planning/STATE.md` are NOT edited by this task — no version-bearing or check-marked `###` heading is added anywhere, per `.claude/rules/planning-artifacts.md`
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation edits to a phase artifact; reversible by a revert.</reversibility>
  <done>Eight amendments are applied in place and in §14 — six in `01-SPEC.md`, Amendment G additionally in `02-CONTEXT.md` D-03, and Amendment H in `docs/architecture/invariants.yaml` with the rendered view regenerated — each proven landed by a recorded single-line search rather than asserted, and D-03's recorded tier-floor count checked against the count in `seed.go` with a HALT rather than a correction on disagreement.</done>
</task>

<task type="execute">
  <name>Task 2: Fill the validation map and set its status honestly</name>
  <files>.planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md (the whole file — the Per-Task Verification Map's column set, the RED-first table, the Wave 0 checklist and the sign-off list)
    - .planning/phases/02-abac-schema-vocabulary/02-RESEARCH.md § Validation Architecture → "Phase Requirements → Test Map" (the 31 rows every one of which must land on a task)
    - Every `02-*-SUMMARY.md` produced by plans 02-01 through 02-10 and 02-12 (this plan is the last to run; 02-12 is wave 5 and lands before it)
  </read_first>
  <action>
Fill `02-VALIDATION.md`'s Per-Task Verification Map with one row per task across
**plans `02-01` through `02-10`, plus `02-12` and `02-13`** — every plan in the phase
except this one, whose own tasks are the closeout being audited and cannot audit
themselves. Each row carries: task id, plan, wave, requirement, threat ref (the
`T-02-NN` id from that plan's threat register), secure behavior, test type,
automated command, whether the file existed, and observed status.

The earlier scope said "02-01 through 02-10" while claiming to cover "every task in
this phase", silently excluding `02-12`'s three tasks (review finding, confirmed) —
and this revision adds `02-13`. Derive the plan list from the directory
(`ls .planning/phases/02-abac-schema-vocabulary/02-*-PLAN.md`) rather than from this
sentence, and record the derived list in the SUMMARY, so a plan added later is
included by construction rather than by someone remembering to update prose.

Then walk **all 31 rows** of `02-RESEARCH.md`'s "Phase Requirements → Test Map" and
confirm each lands on a task in the filled table. Record the mapping explicitly.
Any row that does **not** land is a gap: record it as a gap with the reason, and file
a GitHub issue (`gh issue create -R holomush/holomush --label bug`) rather than
quietly dropping it or marking the phase compliant.

**Update the RED-first table with EVERY demonstration the phase carries, and derive
the list rather than fixing a count in prose.** The earlier draft named four
demonstrations in its action and demanded three rows in its acceptance — two numbers
that disagreed with each other and both of which under-counted (review finding,
confirmed). Walk each plan's `<verification_integrity>` **rule 4** paragraph, collect
every demonstration it names, and emit one row per demonstration. Record the derived
count in the SUMMARY.

As of this revision the collection is, at minimum:

- `02-01` — the confusable check against a lookup that always returns false; the
  generated-table drift check against a hand-edited table; the generator's
  input-digest check against modified bytes.
- `02-02` — `MixedScript` against the pre-Task-1 gate; each of the two directional
  import guards against its own synthetic fixture.
- `02-03` — the known-prefix table rows before the constants exist.
- `02-04` — both INV-WORLD specs against the pre-Task-1 predicate and the current
  delete behaviour.
- `02-05` — the block-list rejection; the reload-failure-preserves-last-valid
  assertion; the direct-SQL poller observation against a bare `updated_at`
  indicator; the malformed-value assertion against a `StringSliceN`-based loader;
  the gate-sees-the-reload assertion against a frozen snapshot.
- `02-06` — the character-name admission census against the pre-fix writer boundary,
  where `character_repo.go`'s `Update` still wrote `name` ungated.
- `02-07` — the corpus-compiles smoke assertion; the attribute-reference coverage
  test against a misspelled attribute; the seed-coverage `viewer` no-WARN assertion
  with the provider registration removed.
- `02-08` — the fourth-rung clearing test against an ordinal implementation, on the
  `guest` floor; the additive-permit regression with term B removed; the totality
  gate against a prefix match.
- `02-09` — the id set-equality test in both directions; the byte-identical-refusal
  test against a registry-lookup-first ordering; the boot validator against a
  zero-valued descriptor.
- `02-10` — the widening spec against the corpus excluding the two
  `seed:profile-public-read-*` entries.
- `02-12` — the name-uniqueness gate against the schema staged at `000055` with
  `000056` excluded, plus the deliberate mis-staging observed failing as a SETUP
  error rather than as the demonstration.
- `02-13` — the emitted-vs-declared schema coherence check against an undeclared
  emitted key; the derivation-direction assertion (D-27) against a plain-union
  implementation of the permit-side peers.
- `02-05` additionally — the block-list transport and the `Bootstrap → BlockList`
  dependency edge: record the observed subsystem start order, and note that the
  edge's membership assertion is what turns RED if it is removed.

Each row records the **observed** non-zero exit, not a claim that one would have
occurred. `02-06`'s census and `02-12`'s uniqueness gate are distinct instances —
neither discharges the other, and a table that collapses them into one row is
under-reporting. A row reading "would have failed" is itself a finding.

Set the frontmatter `status: validated` and set `nyquist_compliant` to what was
actually observed. If Docker was unavailable for any integration tier, set it
`false` and say which criteria were therefore unproven — the field distinguishes
NOT-VALIDATED from PARTIAL for the milestone audit, and a `true` that was not
observed defeats that distinction.

**Record the criterion-4 scope reduction D-29 causes, as a MAINTAINER DECISION
ITEM — do not resolve it here (cross-AI review cycle 3, B-2 → D-29).**
`ROADMAP.md:212`'s success criterion 4 reads "An off-location viewer can read a
character's public properties **and in-world description** where
`seed:player-character-colocation` previously denied it". D-29 defers
`seed:profile-public-read-character` — the `resource is character` permit that
carried `characters.description` — to Phase 4. So **criterion 4's
`entity_properties` half is discharged in Phase 2 and its `in-world description`
half is not.** The same applies to PROFILE-11, whose scope covers both.

Write this into `02-VALIDATION.md` as an explicit unmet-criterion row naming D-29 as
the cause, alongside the Phase-4 rows. Then **surface it to the maintainer in this
plan's SUMMARY under a heading a reader cannot miss**, stating the three options
without choosing between them:

1. Reword criterion 4 to the property half, with the description half moving to
   Phase 4's criteria.
2. Leave criterion 4 as-is and accept that Phase 2 partially discharges it, recorded
   in the milestone audit.
3. Re-open D-29 — which the phase would then need to re-plan around, since the
   projection narrowing it depends on is Phase 4 work.

This is a **scope decision about a locked artifact**, not a planning judgement, so it
does not get made here. Note the asymmetry with D-30: that decision explicitly
records that criterion 1's wording needs no amendment, so criterion 1 is settled and
MUST NOT be touched. D-29 makes no such statement about criterion 4, which is why it
is escalated rather than assumed either way.

Do not edit `.planning/ROADMAP.md` or `.planning/STATE.md` in this task — not even to
reword criterion 4. The orchestrator owns ROADMAP updates through `gsd roadmap`, and
inventing or amending structure in a tool-owned parsed file silently changes what the
tool sees (`.claude/rules/planning-artifacts.md`).
  </action>
  <verify>
    <automated>[ "$(rg -o 'pending — allocated when' .planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md | wc -l)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `[ "$(rg -o 'pending — allocated when' .planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md | wc -l)" -eq 0 ]` — the placeholder row is gone. The gate COUNTS matches rather than reading an exit code, and does NOT use `|| true`: a failed `rg -c` prints nothing, so the substitution is empty and the equality is false at exactly zero matches (cycle-2 C2-9, on this plan's other closeout gate)
    - The Per-Task Verification Map has one row per task across every `02-*-PLAN.md` except `02-11` itself, each carrying a `T-02-NN` threat ref and an automated command; the SUMMARY records the plan list as DERIVED from the directory listing, and that list includes `02-12` and `02-13`
    - The SUMMARY records the 31-row mapping from RESEARCH's test map to tasks, with any unmapped row named and its issue number recorded
    - The RED-first table has one row per demonstration collected from every plan's `<verification_integrity>` rule 4, the SUMMARY records the derived count, and every row records an observed outcome rather than a projected one
    - The 02-10 "still publishes `name`" obligation appears in the map as a **Phase-4** row rather than a Phase-2 gap, with its deferral rationale
    - `02-VALIDATION.md` carries an explicit row recording that **ROADMAP success criterion 4's "in-world description" half is NOT discharged in Phase 2**, naming D-29 as the cause and Phase 4 as the owner — and the SUMMARY escalates it to the maintainer as a scope decision with the three options stated and NONE selected. `rg -n 'criterion 4' .planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md` matches
    - `git diff --name-only` does NOT list `.planning/ROADMAP.md` for this task — criterion 4 is not reworded here, in either direction
    - The frontmatter carries `status: validated` and a `nyquist_compliant` value matching what the SUMMARY reports as observed
    - `git diff --stat .planning/ROADMAP.md .planning/STATE.md` shows no change from this task
  </acceptance_criteria>
  <reversibility rating="reversible">A planning artifact edit.</reversibility>
  <done>Every task has a verification row, all 31 test-map rows are accounted for, and the compliance flag reports the observed state.</done>
</task>

<task type="execute">
  <name>Task 3: Route the phase to abac-reviewer and run the phase gate</name>
  <files>(no files created or modified — review pass and gate run)</files>
  <read_first>
    - .planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md D-05 (the explicit routing obligation and why abac-reviewer specifically — it identified the §8.5.1.1 residual and has the context to confirm the rejection does not trade one hole for another)
    - .planning/phases/02-abac-schema-vocabulary/02-07-SUMMARY.md and 02-08-SUMMARY.md and 02-09-SUMMARY.md (the ABAC surface the reviewer is being asked about)
    - .claude/rules/landing-the-plane.md
    - CLAUDE.md § Pre-Push Review Gates
  </read_first>
  <action>
Run `abac-reviewer` (`/holomush-dev:review-abac`) over the phase diff. It is
**mandatory**: the diff touches `internal/access/` throughout, and D-05 routes the
§8.5.1.1 finding to it by name. Brief it explicitly on the three questions this
phase resolved that it is best placed to check:

1. **D-02's rejection of option 2** — does the viewer-flavored-twins shape close the
   §8.5.1.1 residual without trading one hole for another?
2. **The term-A/term-B separation by action token** (`read_profile_attribute` vs
   `read`) — does it actually keep the two families in separate evaluations, given
   `combineDecisions` combines permits disjunctively?
3. **Open Question 1's resolution** — two `seed:profile-public-read-*` entries under
   one conceptual name, with a bare-`resource` single policy explicitly rejected.

Address every finding it returns: fix, or record why not applicable. Do not merge on
a NOT READY.

Note that `crypto-reviewer` is **not** triggered: nothing in this phase touches
`internal/eventbus/crypto/`, `codec/`, `history/dispatcher.go`,
`cold_postgres.go`, `event_emitter.go::Emit`, `audit/projection.go`, a
`crypto.emits` manifest declaration, or a `crypto_keys` / `events_audit` migration.
Record that as a stated absence rather than silently skipping the gate.

Then run the phase gate. **Dispatch `task test:int` through the `local-check` agent,
not inline.** CLAUDE.md's delegation rule covers `task test|lint|build|test:int|test:cover`
and is hook-enforced, so an inline invocation here is a hard failure rather than a
style deviation (review finding, confirmed). `task pr-prep` is the single documented
exception and runs **inline in the parent session** — that carve-out exists
precisely because the final gate must not be reported second-hand.

Order matters: dispatch `local-check` for `task test:int` first and read its verdict
by exit code, never by grepping its output for a success string; then run
`task pr-prep` inline. Commit any files `task fmt` mutated (SPDX headers on this
phase's many new files are the expected case), because uncommitted `fmt` output is a
common cause of red CI on an otherwise-green branch.
  </action>
  <verify>
    <automated>task pr-prep</automated>
    <human-check>Read `abac-reviewer`'s verdict in full. It MUST be a binary READY or NOT READY with every finding citing `path:line`. Do not proceed on "mostly ready with minor concerns" — the agent is constructed to return a binary verdict, so an equivocal one is itself a finding.</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records `abac-reviewer`'s verdict verbatim and, for each finding, whether it was fixed or why it is not applicable
    - The SUMMARY states explicitly that `crypto-reviewer` is not triggered and why
    - `task pr-prep` exits 0, run inline in the parent session
    - `task test:int` was dispatched through `local-check` (not run inline) and returned PASS by exit code, or the SUMMARY records which integration criteria could not run and why; the SUMMARY names the dispatch explicitly so the delegation is auditable
    - `git status` shows a clean working tree after `task fmt`'s edits are committed
    - No commit on this branch carries a `[ci skip]` or equivalent directive
  </acceptance_criteria>
  <reversibility rating="reversible">A review pass and a gate run.</reversibility>
  <done>`abac-reviewer` has returned READY on the phase's authorization surface, the phase gate is green, and formatter output is committed.</done>
</task>

</tasks>

<verification_integrity>
The six PORTAL-10 / §12.1 rules at their fixed numbering, specialized to this plan.

1. **Census with set equality.** This plan ships no census. Phase 2's rule-1 census
   scope is the character-name **admission** census in plan `02-06`
   (`test/meta/character_name_admission_test.go`), and this plan's Task 2 records
   that it landed — including that it is a backstop over a compile-time guarantee
   (`charname.Admitted` has one constructor) rather than the whole guarantee. EXT-04's admin section-registry ↔ descriptor
   census remains Phase 6's; Task 2 records that Phase 2 did not claim it. The
   31-row test-map reconciliation in Task 2 is a coverage check over a hand-written
   list, not a census, and is not offered as one.
2. **Paired positive control on every denial test.** This plan ships **no tests at
   all** — it edits planning artifacts and runs gates. Stated explicitly rather
   than omitted: rule 2 is not satisfied here by the absence of the tests it
   governs. Task 2's obligation is to confirm the pairing landed in the plans that
   do ship denial tests — `02-02`, `02-04`, `02-05`, `02-06`, `02-08`, `02-09`,
   `02-10` and `02-12` — and to record any that did not.
3. **Assertions against marshaled response bytes.** Does not apply — no code and no
   response shape in this plan. Task 2 records that the milestone's wire-level
   absence assertion (INV-ACCESS-11) is correctly deferred to Phase 4 rather than
   claimed here.
4. **Gates demonstrated RED against the pre-fix state.** This plan's own gate is
   Amendment C's citation check, which MUST be observed failing before the
   corrections land — run the `.up.sql` search first, record the non-zero count, then
   correct and record zero. Task 2 additionally verifies that **every** RED
   demonstration named in any plan's rule 4 was observed and recorded, not projected.
   The count is derived from those plans rather than fixed here; a row in the
   RED-first table that says "would have failed" is itself a finding, and a
   demonstration named in a plan but missing from the table is a second one.
5. **Wire-level assertion of every opacity and authorization contract.** No wire
   surface in this plan. Its contribution to rule 5 is Amendment A, which records
   why option 2's failure mode is invisible to the test suite — a closed failure
   with no symptom — so that a later reader understands why the D-04 regression test
   in plan `02-08` is load-bearing rather than redundant.
6. **Invariant-scope discipline.** This plan registers no invariant and writes no
   `// Verifies:` annotation. It records the phase's final registry state: three
   entries bound (INV-WORLD-5 and INV-WORLD-6 in plan `02-04`, INV-PRIVACY-11 in
   plan `02-09`), and five staying `pending` with no `asserted_by` — INV-ACCESS-10,
   INV-ACCESS-11, INV-ACCESS-12, INV-PRIVACY-9 and INV-PRIVACY-10 all bind in
   Phase 4, and INV-WORLD-7 in Phase 4 with its commands landing in Phase 3. All
   sit in existing scopes; no family was minted.
</verification_integrity>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact → downstream planner | A superseded statement left live in a directly-read artifact is planned against regardless of any amendment table. |
| phase diff → merge | The last point at which an authorization regression can be caught before it is in `main`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-67 | Tampering | unapplied amendment | high | mitigate | §8.5.1.1 left presenting option 2 as live would be planned against in Phase 4, reintroducing the §8.8 violation D-02 rejected. Closed by applying each amendment in place and proving it with a recorded single-line search rather than asserting it. |
| T-02-68 | Elevation of privilege | unreviewed authorization surface | high | mitigate | The phase's entire subject is the authorization boundary. Closed by the mandatory `abac-reviewer` gate with a binary verdict and a briefing naming the three questions this phase resolved. |
| T-02-69 | Repudiation | projected rather than observed RED demonstrations | high | mitigate | A gate never seen failing is indistinguishable from a gate that cannot fail, and the phase carries three of them. Closed by Task 2 verifying each recorded a non-zero exit, and by treating "would have failed" as a finding. |
| T-02-70 | Tampering | invented structure in a parsed planning file | medium | mitigate | A version-bearing or check-marked `###` heading in `ROADMAP.md` truncates `extractCurrentMilestone`'s scope silently, producing confident wrong answers with no error. Closed by this plan writing only to `01-SPEC.md`, `02-CONTEXT.md` and `02-VALIDATION.md` — none of which is parsed by `gsd roadmap` — asserted by a no-diff criterion on ROADMAP and STATE. |
| T-02-71 | Repudiation | overstated compliance flag | medium | mitigate | A `nyquist_compliant: true` that was not observed defeats the milestone audit's NOT-VALIDATED-versus-PARTIAL distinction. Closed by setting it from the observed state and naming any unproven criteria. |
| T-02-72 | Tampering | `[ci skip]` on a ship-note commit | medium | mitigate | A CI-skip directive on a branch tip suppresses the docs stand-in too, leaving required checks with no run for that SHA and a merge blocked on checks that will never arrive. Closed by the explicit prohibition and its acceptance criterion. |
</threat_model>

<verification>
- `task pr-prep` green, run inline in the parent session.
- `task test:int` green, or its gaps recorded.
- `abac-reviewer` READY.
- `[ "$(rg -o 'migrations/[0-9]*_[a-z_]*\.up\.sql' .planning/phases/01-portal-spec/01-SPEC.md | wc -l)" -eq 0 ]`.
- `02-VALIDATION.md` has no placeholder row and carries `status: validated`.
- `02-CONTEXT.md` D-03 records a tier-floor count equal to the count in `internal/access/policy/seed.go`, with both numbers in the SUMMARY.
- `git status` clean.
</verification>

<success_criteria>
- Eight amendments applied in place and recorded in §14, each proven by a recorded single-line search — including Amendment H, which corrects INV-WORLD-4's out-of-world writer count from TWO to THREE (B-12).
- Every stale `.up.sql` citation in `01-SPEC.md` corrected.
- §13, §8.4.1, §8.5 and §8.6 agree with the registry and the code on the deleter enumeration, the viewer attribute set, the derived property peers, and the tier-floor count.
- The validation map covers every plan in the phase except this one, derived from the directory rather than from prose.
- All 31 RESEARCH test-map rows mapped to tasks, with any gap filed rather than dropped.
- Every RED-first demonstration named in any plan's rule 4 appears in the table, recorded as observed.
- `abac-reviewer` READY; `crypto-reviewer` stated as not triggered.
- `task test:int` dispatched through `local-check`; `task pr-prep` green inline with formatter output committed.
</success_criteria>

## Artifacts this phase produces (this plan's share)

No new Go symbols, no schema, no policy. Edits only:
`.planning/phases/01-portal-spec/01-SPEC.md` (§6.1.2, §8.4.1, §8.5, §8.5.1.1, §8.6,
§11.3, §13, §14, and every stale migration citation),
`docs/architecture/invariants.yaml` (Amendment H — `INV-WORLD-4`'s out-of-world
writer count TWO → THREE, plus the `asserted_by:` addition) and the regenerated
`docs/architecture/invariants.md`,
`.planning/phases/02-abac-schema-vocabulary/02-CONTEXT.md` (D-03's tier-floor count,
verified against `seed.go` — HALT on disagreement, never corrected), and
`.planning/phases/02-abac-schema-vocabulary/02-VALIDATION.md` (the Per-Task
Verification Map, the RED-first table, and the frontmatter status flags).

<output>
Create `.planning/phases/02-abac-schema-vocabulary/02-11-SUMMARY.md` when done.
</output>
