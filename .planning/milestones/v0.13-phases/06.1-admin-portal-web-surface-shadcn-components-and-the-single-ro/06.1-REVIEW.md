---
phase: 06.1-admin-portal-web-surface-shadcn-components-and-the-single-ro
reviewed: 2026-08-15T20:05:00Z
depth: standard
iteration: 3
files_reviewed: 2
files_reviewed_list:
  - test/meta/web_phone_band_breakpoint_census_test.go
  - web/src/routes/(authed)/admin/characters/+page.svelte
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
status: clean
---

# Phase 06.1: Code Review Report (iteration 3 — final)

**Reviewed:** 2026-08-15
**Depth:** standard (verification pass over two fix commits, `ca0785f36` and `282b27b81`)
**Files Reviewed:** 2
**Status:** clean

## Summary

Two commits, both sound. `ca0785f36` is a pure reordering of two assertions plus a
comment rewrite — mechanically verified as such, not read: every added non-comment line in
the diff has an exactly matching removed line, so no assertion string was weakened, no
filter was introduced, and nothing was subtracted from either side of the equality.
`282b27b81` is a pure move — every added line has a matching removed line, zero net code
change — and it puts the "emptiness by a first-element probe" block back above
`const noRows` at `+page.svelte:235`.

All four claimed mutation results were re-run rather than accepted, and all four reproduced
exactly. Three cases the fixer's table does **not** cover were also run, because a reorder
can regress a case nobody enumerated; all three behave correctly. The respelling-gap
self-correction is true and is not a coverage hole — the sibling occurrence test catches it
with a message that names the file and the exact rule.

Gates re-run inline after restoring every mutation: `task test -- ./test/meta/` exit 0
(204/204) · `task web:test` exit 0 (71 files / 772 tests, svelte-check **0 errors**,
19 pre-existing `Unknown at rule @reference` warnings) · `task lint` exit 0 ·
`git status --porcelain -- web test internal` empty.

## Mutation results (observed, not reported)

Assertion sites in the post-fix file: offenders at `:504`, completeness equality at `:580`.

| # | Mutation | Observed |
|---|---|---|
| 1 | `@media (max-width: 767px)` injected into `AdminNav.svelte:125` | **RED at `:504`** — `Should be empty, but was [web/src/lib/components/admin/AdminNav.svelte:125 …]`, message says *"Derive it: `@media (width < theme(--breakpoint-md))`…"*. `rg -c "Enumerate it"` over the output exits 1 — **the wrong instruction never appears**. |
| 2 | `@media (width < theme(--breakpoint-md))` injected into `AdminNav.svelte`, **no** census entry | **RED at `:580`** — `expected: 18 / actual: 19`, *"MORE reached than claimed … Enumerate it."* |
| 3 | Enumerated `ruleBelowMd` block deleted from `Sidebar.svelte:54-56`, census entry left | **RED at `:580`** — `expected: 18 / actual: 17`, *"FEWER reached than claimed: either a rule was deleted without dropping its census entry…"* |
| 4 | Clean tree | **GREEN**, exit 0; both census tests, and the whole `./test/meta/` lane at 204/204. |

### Cases the fixer's table does not cover — all run here

- **Forbidden literal in an ALLOWLISTED file on a line lacking the exempt symbol.** Appended
  `export const OTHER_QUERY = '(min-width: 1024px)';` to
  `web/src/lib/hooks/mediaQuery.svelte.ts` (allowlisted for `DESKTOP_MEDIA_QUERY`).
  **RED at `:504`**, naming `mediaQuery.svelte.ts:85`, with the *"Derive it"* message. The
  allowlist staleness loop still ran first and stayed green, so symbol-scoping survives the
  reorder — the exemption did not absorb the new line.
- **Hand-written literal in a file with NO census entry.** Appended
  `export const PHONE_Q = '(max-width: 767px)';` to `web/src/lib/admin/client.ts`.
  **RED at `:504`** with *"Derive it"*; `Enumerate it` absent. So the correct instruction is
  not contingent on the file already appearing in the census.
- **Reported by neither assertion?** No new such case. `bandRelevant` is
  `forbidden || carries the token` (`:402-405`); a forbidden line is either allowlisted
  (counted on both sides, exempt by design) or an offender caught at `:504`; a token line is
  caught by the equality at `:580`. The reorder changes which of two failing assertions
  speaks first, never whether one speaks.

### Respelling gap — verdict: not a finding

Respelling `Sidebar.svelte`'s enumerated rule `--breakpoint-md` → `--breakpoint-sm` and
running both tests:

- `TestNoAuthoredViewportRuleCarriesAHandWrittenBreakpointLiteral` → **exit 0 (GREEN)**.
  The fixer's self-correction of the iteration-1 evidence is therefore **true**: the line
  still carries `theme(--breakpoint-`, stays band-relevant, and the tally is unmoved.
- `TestEveryBandedViewportRuleDerivesItsWidthFromTheTailwindToken` → **RED at `:632`**,
  *"Sidebar.svelte carries the token-derived rule `@media (width < theme(--breakpoint-md))`
  0 time(s); this census claims 1."*

The property is covered by the pair, with a clean division of labour the file already
documents at `:565-571`: the equality owns corpus-vs-census disagreement, the sibling owns
per-file occurrence and therefore spelling. Both live in the same package and run in the
same lane, so neither can be run without the other. Not a coverage hole; the iteration-1
report simply cited the wrong mutation for a property that does hold, and the fixer
corrected the record instead of leaving it.

## Iteration 2 Disposition

| Prior | Disposition | Evidence checked (this iteration) |
|---|---|---|
| **WR-01** completeness equality preempts the offender assertion, so a hand-written breakpoint is reported as a missing census entry | **fixed** | `require.Empty(t, offenders, …)` now at `:504`, equality at `:580`, ordering constraint written into the file at `:477-503` and `:513-519` in both directions. Mutation 1 above lands on the offender assertion with *"Derive it"* and no *"Enumerate it"* anywhere in the output. Mutations 2 and 3 confirm the equality was not weakened to achieve it — it still fires in both directions. Diff proven to be a pure reorder: no unmatched added non-comment line. The reviewer's alternative (`bandRelevant-len(offenders)`) was correctly declined; the reorder makes the subtraction unreachable rather than merely unnecessary. |
| **IN-01** `noRows` doc comment orphaned above the `filterBar` handle | **fixed** | `+page.svelte:226-235`: `filterBar` and its one-line JSDoc now sit above the block, which abuts `const noRows` at `:235`. Diff is a pure move (every added line matched by a removed line). `CharacterFilterBar` is imported at `:10` and used only in type position, so hoisting the declaration is inert; `bind:this` at `:337` and the `clearFilters` read are unaffected. 772/772 with svelte-check 0 errors. |
| **IN-02** deferred `$app/stores` migration untracked | **fixed (filed)** | `gh issue view 4983` → **OPEN**, *"web: migrate all 8 $app/stores call sites to $app/state in one change"*, labels `enhancement` / `review-finding` / `priority::low`. A deferred item with a filed issue is not an open finding. |

**Tally:** 3 in scope · 3 closed · 0 new defects introduced by this round's fixes.

## Final State

Three iterations, closing record for the phase.

- **Iteration 1** — 11 findings (0 Critical, 8 Warning, 3 Info), concentrated in the meta-tests
  and comments that this phase treats as load-bearing: a parity regex that dropped reflowed
  entries, a prohibition narrowed by its marker slice, a `browserslist` last-wins hazard, two
  misattributed/miscounted comment claims, a census that claimed "every" without asserting it,
  a body-less mutation that read as success in the UI, plus three smaller items.
- **Iteration 2** — 9 fixed, 1 skipped correctly (`$app/stores`), 1 withdrawn in its
  substantive half (the depth-0 `@reference` claim, falsified at the mechanism with a negative
  control the original probe lacked). Two of the repairs introduced defects of their own:
  a diagnosis regression in the census ordering (Warning) and an orphaned doc comment (Info),
  plus the untracked deferral (Info).
- **Iteration 3 (this pass)** — all three closed by two commits, verified by re-running every
  claimed mutation plus three the fixer did not enumerate. No new defects.

Net: 14 findings raised across the phase, 12 fixed in code, 1 withdrawn as false, 1 deferred
with issue [#4983](https://github.com/holomush/holomush/issues/4983). Zero Critical findings
at any point. Nothing actionable remains in the reviewed surface.

The one item a human should still exercise by hand — carried forward unchanged from
iteration 2, and *not* a review finding — is iteration-1 **WR-08**, the only shipped
behaviour change in the phase: the throw-before-close in `saveEdit` / `applyLifecycle` means
a body-less mutation response now keeps the overlay open and renders the generic failure
copy. The jsdom suite proves the overlay stays open; how that failure *reads* in a real
browser is a UAT question, not a test one.

---

_Reviewed: 2026-08-15_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard (iteration 3 — final verification of 2 fix commits)_
