---
phase: 03-platform-hardening-deployment-scaling
plan: 08
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - compose.cluster.yaml
  - deploy/nats/cluster-config.yaml
  - scripts/smoke/cluster-smoke.sh
  - scripts/smoke/cluster-smoke.bats
autonomous: true
requirements: [CLUSTER-03, CLUSTER-05]
must_haves:
  truths:
    - "compose.cluster.yaml overlays compose.prod.yaml adding a NATS JetStream (file-storage) service and a second core replica, both in external mode against the NATS service (D-14)"
    - "A multi-process smoke brings the stack up, waits for BOTH core replicas to reach readiness, and asserts the cluster converges to 2 members (topology proof, D-05b — proof (b) of decision D-05)"
    - "The second replica runs with --skip-seed-migrations so two replicas do not race the seed bootstrap (RESEARCH Open Question 2)"
    - "The smoke tears the stack down cleanly (no leaked containers/volumes)"
  artifacts:
    - "compose.cluster.yaml — NATS service + 2nd core replica overlay"
    - "deploy/nats/cluster-config.yaml — event_bus external-mode config fragment mounted into the core replicas"
    - "scripts/smoke/cluster-smoke.sh — up -> wait-ready -> assert 2 members -> down"
  key_links:
    - "compose.prod.yaml + compose.cluster.yaml -> 2 core replicas -> single nats service"
    - "core replica external event_bus config -> nats://nats:4222"
  prohibitions:
    - "MUST NOT run seed bootstrap concurrently on both replicas (second replica uses --skip-seed-migrations, RESEARCH Open Question 2)"
    - "MUST NOT decide smoke pass/fail by grepping stdout for a success string — use readiness endpoints + exit codes (repo search-tools rule)"
    - "MUST NOT migrate the sandbox (game.holomush.dev) — deferred (D-15); this is the repo/CI-boundary topology proof only"
---

<objective>
Ship the deployment-shaped capstone: a `compose.cluster.yaml` overlay adding a NATS
JetStream service and a second core replica (both in external mode), plus a
multi-process smoke that brings the stack up, waits for both replicas, and asserts
the cluster converges to two members. This is both the runbook's working example and
the topology proof for CLUSTER-03.

Purpose: The integration tier (Plan 05) proves the protocol; this smoke proves the
deployment topology (D-05b) — two real core processes sharing one Postgres and one
external NATS. It also exercises the concurrent-bootstrap edge (two replicas must not
race the seed migrations, RESEARCH Open Question 2).
Output: `compose.cluster.yaml` + mounted external-mode config + a smoke script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-hardening-deployment-scaling/03-CONTEXT.md
@.planning/phases/03-platform-hardening-deployment-scaling/03-RESEARCH.md
@.planning/phases/03-platform-hardening-deployment-scaling/03-PATTERNS.md
@compose.prod.yaml
@compose.e2e.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: compose.cluster.yaml overlay + external-mode config fragment (D-14)</name>
  <files>compose.cluster.yaml, deploy/nats/cluster-config.yaml</files>
  <read_first>
    - compose.prod.yaml (the base topology: postgres/core/gateway/caddy/cloudflared/otel-collector; the `core` service command + env + healthcheck + networks)
    - compose.e2e.yaml (overlay precedent: how services are extended, env set, KEK provided via --auto-gen-kek, healthcheck shape)
    - internal/eventbus/config.go (event_bus section keys from Plan 01: mode/url/credentials/tls/provision)
    - cmd/holomush/core.go:125-160 (the --config flag + config.Load; --skip-seed-migrations flag at :159)
    - 03-PATTERNS.md § "compose.cluster.yaml (CREATE)" (analog = compose.prod.yaml / compose.e2e.yaml)
  </read_first>
  <action>
    Create `compose.cluster.yaml` as an overlay on compose.prod.yaml (SPDX header). Add a
    `nats` service: `nats:2-alpine` (pin a digest per repo convention), command `-js -sd /data`,
    a file-storage volume under the data dir, on the backend network, with a healthcheck
    (`nats server check` or a TCP probe on 4222). Reconfigure `core` to external mode: mount
    `deploy/nats/cluster-config.yaml` (created here — an event_bus section with `mode: external`,
    `url: nats://nats:4222`, `provision: true`) and pass `--config` to it; add
    `depends_on: nats (service_healthy)`. Add a second core replica service `core2` extending
    the same image/config but with `--skip-seed-migrations` (RESEARCH Open Question 2 — only the
    primary runs the seed bootstrap) and its own readiness healthcheck. Each replica gets a
    distinct cluster member id automatically (core mints one via idgen at boot). Do NOT expose
    NATS to the host in prod shape; keep it on the backend network.
  </action>
  <verify>
    <automated>docker compose -f compose.prod.yaml -f compose.cluster.yaml config >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f compose.prod.yaml -f compose.cluster.yaml config` renders without error (valid merged topology).
    - The rendered config shows a `nats` service (JetStream, file-storage) and two core replicas, both external-mode against `nats://nats:4222`.
    - The second replica carries `--skip-seed-migrations`; the primary does not.
    - `deploy/nats/cluster-config.yaml` sets `event_bus.mode: external` + `url`.
  </acceptance_criteria>
  <done>The two-replica external-NATS topology is expressible as a compose overlay and is the runbook's working example.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-process cluster smoke (up -> wait-ready -> assert 2 members -> down)</name>
  <files>scripts/smoke/cluster-smoke.sh, scripts/smoke/cluster-smoke.bats</files>
  <read_first>
    - compose.e2e.yaml + Taskfile.yaml test:e2e (how compose stacks are driven + torn down in this repo)
    - cmd/holomush/core.go:491-516 (cluster.Registry wiring + the metrics registerer — the member-count signal the smoke can scrape from /metrics), and the /healthz readiness endpoint shape (compose.e2e.yaml healthchecks reference /healthz/readiness on :9100)
    - .claude/rules/search-tools.md (decide pass/fail by exit code, never by grepping a success string) + shell-scripting bats/defensive patterns
    - 03-RESEARCH.md § Open Question 2 (concurrent bootstrap) + § Validation Architecture (CLUSTER-05 smoke row)
  </read_first>
  <action>
    Create `scripts/smoke/cluster-smoke.sh` (SPDX, `set -euo pipefail`, shellcheck-clean): bring
    the stack up (`docker compose -f compose.prod.yaml -f compose.cluster.yaml up -d`), wait for
    BOTH core replicas' readiness endpoints (`/healthz/readiness` on their metrics ports) with a
    bounded retry, then assert the cluster converged to 2 live members by scraping the
    cluster-member signal from each replica's Prometheus `/metrics` (or a `holomush status`
    query) — decide pass/fail by that count and by exit codes, never by grepping logs for a
    success string. On any failure, dump `docker compose ... logs` for diagnosis. Always tear
    down in a trap (`docker compose ... down -v`). Add `scripts/smoke/cluster-smoke.bats`
    exercising the script's argument handling / teardown-trap against a fixture (mirroring
    `scripts/tests/` bats precedents) so the script has a non-Docker unit-level guard. Run
    `task fmt`; commit its output.
  </action>
  <verify>
    <automated>task lint && bash -n scripts/smoke/cluster-smoke.sh && bats scripts/smoke/cluster-smoke.bats</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke/cluster-smoke.sh` is `set -euo pipefail`, shellcheck-clean, and tears the stack down in a trap on every exit path.
    - The smoke asserts both replicas reach readiness AND the cluster reports exactly 2 members (via /metrics or status), keyed on exit code — not a stdout string match.
    - `scripts/smoke/cluster-smoke.bats` passes (argument/teardown behavior) without Docker.
    - The full Docker smoke (`scripts/smoke/cluster-smoke.sh`) is documented in the SUMMARY as the out-of-band deployment proof (Docker required; runbook references it).
  </acceptance_criteria>
  <done>The two-replica external-NATS topology is provable end-to-end via a compose smoke that converges to two cluster members and cleans up after itself.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- New file: `compose.cluster.yaml` (NATS JetStream service + 2nd core replica overlay)
- New file: `deploy/nats/cluster-config.yaml` (event_bus external-mode config fragment)
- New files: `scripts/smoke/cluster-smoke.sh` + `scripts/smoke/cluster-smoke.bats`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compose stack ↔ external NATS service | Two core replicas share one NATS + one Postgres over the backend network |
| concurrent replica bootstrap | Two processes could race seed migrations at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-21 | Tampering / data corruption | concurrent seed bootstrap on two replicas races migrations | high | mitigate | Only the primary runs the seed bootstrap; the second replica uses `--skip-seed-migrations` (RESEARCH Open Question 2) |
| T-03-22 | Info-disclosure | NATS service exposed to the host/public network | medium | mitigate | NATS stays on the backend network with no host port mapping (mirrors compose.prod.yaml intra-stack posture) |
| T-03-23 | Denial of Service | leaked smoke containers/volumes exhaust CI resources | low | mitigate | Teardown trap (`down -v`) on every exit path |
</threat_model>

<verification>
- `docker compose -f compose.prod.yaml -f compose.cluster.yaml config` renders clean.
- `task lint` + `bash -n` + `bats scripts/smoke/cluster-smoke.bats` green.
- Out-of-band: `scripts/smoke/cluster-smoke.sh` converges to 2 members and tears down (Docker required).
</verification>

<success_criteria>
The external-NATS two-replica topology is deployable via a compose overlay and
provable via a self-cleaning multi-process smoke that converges to two cluster
members (CLUSTER-03 topology proof; CLUSTER-05 working example) — with the
concurrent-bootstrap race handled.
</success_criteria>

<output>
Create `.planning/phases/03-platform-hardening-deployment-scaling/03-08-SUMMARY.md` when done.
</output>
