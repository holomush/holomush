---
phase: 03-platform-hardening-deployment-scaling
plan: 09
type: execute
wave: 4
depends_on: ["03-03", "03-04", "03-05", "03-06", "03-07", "03-08"]
files_modified:
  - site/src/content/docs/operating/how-to/external-nats-deployment.md
autonomous: true
requirements: [CLUSTER-05]
must_haves:
  truths:
    - "One end-to-end operator runbook covers: provision NATS with the deploy/nats account templates -> mint creds -> configure holomush (event_bus external) -> cut over from embedded -> run the scoping verification script -> DLQ inspection/replay -> rollback to embedded (D-16)"
    - "The cutover data stance is stated explicitly: Postgres audit is the durable record; the EVENTS JetStream stream starts fresh on the external cluster (D-16)"
    - "The runbook references the shipped tools by their real names: deploy/nats/verify-scoping.sh, holomush audit dlq {list,show,replay}, compose.cluster.yaml (D-11/D-13/D-14)"
    - "Deferred future options are noted, not implemented: sandbox migration (D-15) and the read-only operator account are follow-ups"
  artifacts:
    - "site/src/content/docs/operating/how-to/external-nats-deployment.md — the full-lifecycle runbook"
  key_links:
    - "runbook steps <-> deploy/nats templates + verify-scoping.sh + compose.cluster.yaml + audit dlq CLI"
  prohibitions:
    - "MUST NOT document the sandbox (game.holomush.dev) live migration as an in-scope step — it is deferred (D-15), noted as a future follow-up only"
    - "MUST NOT invent commands/flags — every command MUST match a shipped artifact (config keys, CLI, script, compose overlay) from Plans 01-08"
    - "MUST NOT claim the EVENTS stream migrates on cutover — state the fresh-stream + durable-Postgres-audit stance (D-16)"
---

<objective>
Write the external-NATS deployment runbook: the single operator document that walks
the full lifecycle from provisioning an external NATS cluster through cutover, scoping
verification, DLQ operations, and rollback — with an explicit stance on cutover data.

Purpose: CLUSTER-05. This is the phase's operator-facing deliverable and the reason
the compose overlay, scoping script, and DLQ CLI were shipped. It ties the code
together into a runnable procedure and states the one decision operators must
understand: Postgres audit is the durable record; the EVENTS stream starts fresh on
the external cluster (D-16).
Output: `site/src/content/docs/operating/how-to/external-nats-deployment.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-hardening-deployment-scaling/03-CONTEXT.md
@.planning/phases/03-platform-hardening-deployment-scaling/03-RESEARCH.md
@.planning/phases/03-platform-hardening-deployment-scaling/03-PATTERNS.md
@docs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the full-lifecycle external-NATS runbook (D-16)</name>
  <files>site/src/content/docs/operating/how-to/external-nats-deployment.md</files>
  <read_first>
    - site/src/content/docs/operating/how-to/sandbox/sandbox-operations.md (operating how-to style/precedent) and one existing how-to (e.g. database.md) for the doc shape
    - deploy/nats/README.md + holomush-server.account.conf + verify-scoping.sh (Plan 06 — the provisioning + scoping steps to reference)
    - compose.cluster.yaml + scripts/smoke/cluster-smoke.sh (Plan 08 — the working example + smoke)
    - cmd/holomush/cmd_audit.go (Plan 07 — the exact `holomush audit dlq` subcommands) + internal/eventbus/config.go event_bus keys (Plan 01)
    - docs/CLAUDE.md (kebab-case filename, markdown standards, fenced-code language ids, table alignment)
    - 03-CONTEXT.md D-16 (the exact lifecycle order) + D-15 (sandbox deferred) + Deferred Ideas (read-only operator account)
  </read_first>
  <action>
    Create `site/src/content/docs/operating/how-to/external-nats-deployment.md` (kebab-case,
    Starlight frontmatter title/description, H1). Structure the lifecycle per D-16: (1) Provision
    an external NATS cluster (JetStream, file-storage) and apply the `deploy/nats/`
    holomush-server account scoped to events.>/audit.>/internal.>/_INBOX.> via the nsc
    walkthrough; (2) Mint the `.creds` file; (3) Configure holomush's `event_bus` section
    (`mode: external`, `url`, `credentials`, optional `tls`, `provision`, `dlq.max_age/max_bytes`)
    — reference `compose.cluster.yaml` as the working example; (4) Cut over from embedded, with an
    explicit callout box stating: Postgres `events_audit` is the durable audit record and survives
    cutover unchanged; the EVENTS JetStream stream starts fresh on the external cluster (no stream
    migration); (5) Run `deploy/nats/verify-scoping.sh` with a non-server credential to prove
    lockout, and note the server's own boot self-check refuses an over-scoped account; (6) DLQ
    operations — inspect with `holomush audit dlq list/show`, replay with `holomush audit dlq
    replay` after fixing the outage, and monitor `holomush_audit_dlq_messages_total`; (7) Rollback
    to embedded (set `mode: embedded`, same durable-Postgres stance). Add a "Future options"
    section noting the read-only operator account and the sandbox (game.holomush.dev) migration are
    DEFERRED follow-ups (D-15). Use fenced code with language ids; align tables (run `task fmt`).
  </action>
  <verify>
    <automated>task lint:markdown && rg -q "events_audit|EVENTS stream starts fresh|audit dlq replay|verify-scoping.sh|compose.cluster.yaml" site/src/content/docs/operating/how-to/external-nats-deployment.md</automated>
  </verify>
  <acceptance_criteria>
    - The doc walks all seven lifecycle steps in D-16 order with a fenced, aligned, lint-clean body.
    - The cutover section states the Postgres-durable / EVENTS-fresh stance explicitly.
    - Every command/flag/artifact named exists (event_bus keys, `holomush audit dlq` subcommands, `verify-scoping.sh`, `compose.cluster.yaml`).
    - A "Future options" section marks the read-only operator account and sandbox migration as deferred (D-15).
  </acceptance_criteria>
  <done>The runbook documents the full external-NATS deployment lifecycle grounded in the shipped artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Accuracy pass + docs build (sidebar auto-generation, cross-links)</name>
  <files>site/src/content/docs/operating/how-to/external-nats-deployment.md</files>
  <read_first>
    - site/astro.config.mjs (operating sidebar uses `autogenerate: { directory: 'operating' }` — a new how-to auto-appears; no manual sidebar edit needed)
    - Taskfile.yaml docs:build / lint:markdown targets
    - 03-RESEARCH.md § Validation Architecture (CLUSTER-05 row: `task docs:build`)
  </read_first>
  <action>
    Verify the runbook builds into the Starlight site: confirm the operating sidebar
    autogenerates the new how-to (no astro.config edit required — `autogenerate: { directory:
    'operating' }`), and add cross-links to the adjacent how-tos (sandbox-operations, database)
    where relevant. Re-check every fenced command against the shipped artifacts (config keys,
    CLI subcommands, script name, compose file) — fix any drift. Run `task docs:build` and
    `task lint:markdown`; commit any `task fmt` output.
  </action>
  <verify>
    <automated>task docs:build && task lint:markdown</automated>
  </verify>
  <acceptance_criteria>
    - `task docs:build` succeeds with the new page rendered under operating/how-to.
    - `task lint:markdown` clean on the runbook.
    - No command in the runbook references a non-existent flag/CLI/script (spot-checked against Plans 01-08 artifacts).
  </acceptance_criteria>
  <done>The runbook builds into the operator docs site and is accurate against the shipped code.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- New doc: `site/src/content/docs/operating/how-to/external-nats-deployment.md` (auto-listed in the operating sidebar)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator ← runbook guidance | The runbook is the authoritative deployment procedure; inaccurate guidance is an operational risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-24 | Repudiation / data loss | operator misunderstands cutover data stance and expects EVENTS-stream migration | high | mitigate | Explicit callout: Postgres audit is durable; EVENTS starts fresh (D-16); rollback path documented |
| T-03-25 | Tampering | runbook documents a wrong/nonexistent command, misconfiguring the cluster | medium | mitigate | Accuracy pass grounds every command in a shipped artifact; `task docs:build` gates the page |
</threat_model>

<verification>
- `task docs:build` + `task lint:markdown` green.
- The runbook references only shipped artifacts (config keys, `holomush audit dlq`, verify-scoping.sh, compose.cluster.yaml).
- Deferred items (sandbox migration, read-only operator account) explicitly marked as future.
</verification>

<success_criteria>
Operators have a documented, accurate, buildable runbook for the full external-NATS
deployment lifecycle including the cutover data stance and DLQ operations (CLUSTER-05),
with deferred items clearly marked.
</success_criteria>

<output>
Create `.planning/phases/03-platform-hardening-deployment-scaling/03-09-SUMMARY.md` when done.
</output>
