---
phase: 01-channels-subsystem
plan: 09
type: tdd
wave: 8
depends_on: ["01-08"]
files_modified:
  - test/integration/wholesystem/census_test.go
  - test/integration/channels/channels_e2e_test.go
  - docs/architecture/invariants.yaml
  - docs/architecture/invariants.md
requirements: [CHAN-01, CHAN-02, CHAN-03, CHAN-04, CHAN-05]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "core-channels appears in the whole-system census expectedPlugins and loads (INV-PLUGIN-54 guard: its capability declarations — eval, stream.history, stream.subscription — satisfy fail-closed load). Load also REQUIRES a valid manifest: `emits: [channel]` with `history_scope: custom` (R4-A — `custom` is in the closed enum {grid,scene,custom}; `channel` would fail `ParseManifest` and this census would fail at execution). This census is the end-to-end proof that the 01-03/01-06 `history_scope: custom` value validates."
    - "End-to-end: a member joins, posts, and a second connected member receives the channel_say live; a non-member is denied HISTORY CONTENT and does not receive delivery (history content is membership-gated for all types; public 'openness' is visibility/join-eligibility, not history access — review LOW/MED-7)"
    - "A private/admin channel rejects a non-invited character with a uniform not-found; admin override permits"
    - "A raw-input `=Public hello` reaches core-channels via the manifest-seeded `=` prefix alias and posts to channel `Public` (review MED-6 — the live alias-seeded routing hop, not just the parser unit test)"
    - "core-channels + core-scenes both consume the identical substrate seams (store/audit/resolver/emit), validating INV-S7 (N=2 second-consumer rule, CHAN-05)"
  artifacts:
    - "test/integration/wholesystem/census_test.go (core-channels added to expectedPlugins)"
    - "test/integration/channels/channels_e2e_test.go (Ginkgo end-to-end)"
    - "docs/architecture/invariants.yaml + .md (INV-CHANNEL-N registered; regenerated)"
  key_links:
    - "the whole-system census loading core-channels IS the substrate-consumer proof (CHAN-05 / INV-S7)"
    - "invariants.yaml + generated invariants.md land in the SAME change or TestEveryRegistryInvariantHasBinding fails"
  prohibitions:
    - statement: "A non-member MUST NOT read a channel's HISTORY CONTENT (membership-gated for all types; public openness is visibility/join-eligibility, not history access — LOW/MED-7) AND MUST NOT receive live channel content (end-to-end)"
      status: pending
      verification: "channels_e2e_test: non-member QueryHistory/QueryChannelHistory denied + non-member does not receive channel_say; private non-invitee join → uniform not-found"
---

<objective>
Prove the whole subsystem end-to-end and register its guarantees: add `core-channels` to the whole-system census, write the cross-cutting integration (join → post → live delivery → history, with the non-member negative paths), and register the channel invariants. This closes CHAN-01..05 and validates INV-S7 (N=2 second-consumer rule).

Purpose: phase-gate verification + CHAN-05 (second substrate consumer) + invariant binding.
Output: census entry + e2e integration + registered invariants; `task test:int` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-channels-subsystem/01-RESEARCH.md
@.planning/phases/01-channels-subsystem/01-VALIDATION.md

# Census + integration references

@test/integration/wholesystem/census_test.go
@.claude/rules/testing.md
@.claude/rules/invariants.md

## Integration harness

@internal/testsupport/integrationtest/harness.go
</context>

### Artifacts this plan produces

- `test/integration/wholesystem/census_test.go` — `"core-channels"` added to `expectedPlugins`.
- `test/integration/channels/channels_e2e_test.go` — Ginkgo end-to-end (`//go:build integration`): membership → post → live delivery to a second member; non-member denied read + no delivery; private non-invitee → uniform not-found; admin override; emit→audit round-trip (CHAN-03); AND (review MED-6) a raw-input `=Public hello` reaches core-channels and posts to channel `Public` (the manifest `=` alias is seeded under the whole-system tier — this exercises the live alias-seeded routing hop).
- `docs/architecture/invariants.yaml` (+ regenerated `docs/architecture/invariants.md`): `INV-CHANNEL-1` (membership-gated history CONTENT: a non-member MUST NOT read a channel's history content, for EVERY channel type — public "openness" is visibility/join-eligibility, NOT history access; review LOW/MED-7 reconciliation with 01-04), `INV-CHANNEL-2` (channel error uniformity: a hidden channel returns an identical not-found), registered `binding: bound` where a genuine test asserts them (else `binding: pending`).

<tasks>

<task type="execute">
  <name>Task 1: Add core-channels to the whole-system census</name>
  <files>test/integration/wholesystem/census_test.go</files>
  <read_first>test/integration/wholesystem/census_test.go:25-33 (expectedPlugins list — manifest names; the loader must load core-channels via setup.PluginSubsystem → Manager.LoadAll)</read_first>
  <action>Add `"core-channels"` to `expectedPlugins` (alphabetically near core-building/core-communication). This asserts the plugin loads under the whole-system tier — which requires its capability declarations (`eval`, `stream.history`, `stream.subscription`), `resource_types: [channel]` GetSchema, and `provides` to all satisfy fail-closed load (INV-PLUGIN-54). Ensure the binary artifact builds via `task plugin:build-all` (auto-discovers plugins/**). Do NOT edit `internal/command/validResourceTypes` or `internal/access/prefix.go` — `resource_types: [channel]` auto-registers.</action>
  <verify>
    <automated>task test:int</automated>
  </verify>
  <done>core-channels loads in the whole-system census (help command registered, capabilities satisfy INV-PLUGIN-54); the load implicitly proves the manifest validates — `emits: [channel]` + `history_scope: custom` passes `ParseManifest` (R4-A: a `history_scope: channel` value would fail the closed-enum validator at `internal/plugin/manifest.go:557` and make this census fail); `task test:int` green.
</task>

<task type="tdd" tdd="true">
  <name>Task 2: End-to-end channels integration (Ginkgo) — happy path + negative paths</name>
  <files>test/integration/channels/channels_e2e_test.go</files>
  <read_first>internal/testsupport/integrationtest/harness.go (in-process stack: Postgres + embedded NATS + CoreServer + in-tree plugins via WithInTreePlugins); test/integration/eventbus_e2e/ patterns; .claude/rules/testing.md (Ginkgo/Gomega, //go:build integration)</read_first>
  <behavior>
    - A member creates/joins a channel, posts a say; a SECOND connected member receives the channel_say live (Eventually).
    - A non-member: QueryHistory is denied (PermissionDenied); the non-member does NOT receive the channel_say event.
    - A private channel: a non-invited character's join returns the SAME not-found as a truly absent channel (error uniformity); an invited character joins; an admin overrides.
    - CHAN-03: an emitted channel event lands in plugin_core_channels.channel_log (emit→audit round-trip); a redelivery is idempotent.
    - MED-6 (`=` routing): a raw command-input line `=Public hello` from a member reaches the `core-channels` plugin (via the manifest-seeded `=` prefix alias resolving to `channel Public hello`) and posts `hello` to channel `Public` — the second member receives it live. This exercises the live alias-seeded hop (the manifest `=` alias is loaded under the whole-system tier), NOT just the parser unit test in 01-07.
  </behavior>
  <action>Write `test/integration/channels/channels_e2e_test.go` (`//go:build integration`, Ginkgo/Gomega) using the integration harness with in-tree plugins. Structure Describe/It per the behavior list. Use `Eventually` for async live delivery. Assert the negative paths explicitly (non-member read denied, non-member no delivery, private non-invitee uniform not-found) — these bind the phase prohibitions. Include the emit→audit round-trip + idempotent redelivery (CHAN-03). Include the MED-6 `=Public hello` raw-input routing assertion (the whole-system tier seeds the manifest `=` alias, so drive a raw input line through the command path and assert it reaches core-channels and posts). If this suite needs a new package under test/integration, add it to the `task test:int` package list per Taskfile if not auto-included.</action>
  <verify>
    <automated>task test:int</automated>
  </verify>
  <done>End-to-end join→post→live-delivery + non-member denial + private uniform not-found + admin override + emit→audit round-trip pass; `task test:int` green.</done>
</task>

<task type="execute">
  <name>Task 3: Register channel invariants (INV-CHANNEL-N) + INV-S7 validation</name>
  <files>docs/architecture/invariants.yaml, docs/architecture/invariants.md</files>
  <read_first>.claude/rules/invariants.md (define/respect/bind/regenerate workflow; boundary scope selection; binding ratchet); docs/architecture/invariants.yaml (existing INV-<SCOPE>-N entries; INV-S7 N=2 rule); cmd/inv-render (regeneration)</read_first>
  <action>Register in `docs/architecture/invariants.yaml`: `INV-CHANNEL-1` — "a character that is not a member of a channel MUST NOT read that channel's HISTORY CONTENT (the `channel history` / QueryHistory / QueryChannelHistory path), for every channel type including public" (scope CHANNEL/ boundary; origin_spec this phase). REVIEW LOW/MED-7 RECONCILIATION — word the summary so it does NOT contradict 01-04's public-read permit: history content is membership-gated for ALL types by 01-06 QueryHistory; the 01-04 `read` action (incl. its public clause) governs channel VISIBILITY/discoverability + join-eligibility, NOT history content, so a public channel being "open" does not exempt its history from the membership gate. State this scoping explicitly in the invariant's `summary`/comment so 01-04 and 01-09 are consistent. `INV-CHANNEL-2` — "an operation on a channel the caller cannot see MUST return an identical not-found (no absent-vs-hidden distinction)". Set `binding: bound` with `asserted_by:` pointing at the genuine asserting tests (audit_test.go / channels_e2e_test.go non-member-history-denied for INV-CHANNEL-1; service_test.go/channels_e2e_test.go for INV-CHANNEL-2) and add `// Verifies: INV-CHANNEL-1` / `// Verifies: INV-CHANNEL-2` annotations on those tests — ONLY if the test genuinely asserts the invariant (never fabricate a binding; if unproven leave `binding: pending` and file a coverage bug). For INV-S7 (N=2 second-consumer rule, CHAN-05): if the census/e2e genuinely asserts that core-channels + core-scenes both consume the substrate, bind it; otherwise record CHAN-05 as validated structurally in the SUMMARY and leave INV-S7 binding to the substrate-owner's judgment. **Also bind INV-PRIVACY-7 (opportunity — core-channels is the FIRST `history_scope: custom` adopter).** The privacy spec's I-PRIV-7 integration test is today a skip placeholder (`test/integration/privacy/privacy_test.go` — `Skip("no plugin currently declares history_scope: custom — re-enable when a plugin adopts this field")`) and the invariant is unbound. Since core-channels declares `history_scope: custom` (01-03) and 01-06 implements the divergent membership-gated `QueryHistory` semantics the spec waited for, un-skip that placeholder (or annotate the channels membership-gated-history e2e in this phase) so it GENUINELY exercises channels' custom-scope semantics, add `// Verifies: INV-PRIVACY-7`, and flip the registry entry to `binding: bound` — ONLY if the test genuinely asserts it (never bind a Skip placeholder; leave `pending` + file a coverage bug otherwise, per `.claude/rules/invariants.md`). This closes an invariant the history-scope spec explicitly left open for the first `custom` adopter. Regenerate `docs/architecture/invariants.md` via `go run ./cmd/inv-render` (through `task`) in the SAME change. Do NOT hand-edit generated regions of invariants.md.</action>
  <verify>
    <automated>task test -- -run 'TestEveryRegistryInvariantHasBinding|TestBoundInvariantsAreGenuinelyAsserted|TestProvenanceGuard' ./test/meta/</automated>
  </verify>
  <done>INV-CHANNEL-1/2 registered (bound where genuinely asserted, else pending with a filed coverage bug); invariants.md regenerated in the same change; meta-tests green; CHAN-05/INV-S7 status recorded.</done>
</task>

</tasks>

<threat_model>

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whole-system load → fail-closed | An undeclared capability must prevent load (INV-PLUGIN-54) |
| end-to-end delivery → membership | The composed stack must honor membership on read AND live delivery |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information disclosure | non-member read/live-delivery end-to-end | high | mitigate | channels_e2e_test asserts non-member QueryHistory denied AND non-member receives no channel_say; binds INV-CHANNEL-1 |
| T-01-12 | Information disclosure | hidden-channel oracle end-to-end | medium | mitigate | channels_e2e_test asserts private non-invitee join returns uniform not-found; binds INV-CHANNEL-2 |
| T-01-21 | Elevation of privilege | plugin loads with undeclared capability | high | mitigate | Whole-system census load exercises fail-closed capability enforcement (INV-PLUGIN-54) |
| T-01-SC | Tampering | package installs | n/a | accept | No package installs; SC checkpoint does not fire |

</threat_model>

<verification>
- `task test:int` green (census + channels e2e).
- `task test -- ./test/meta/` invariant meta-tests green.
- `task pr-prep` green before `/gsd-verify-work` (phase gate).
</verification>

<success_criteria>
core-channels loads in the whole-system census; the end-to-end integration proves membership-gated read + live delivery + non-member denial + private uniform not-found + emit→audit round-trip; channel invariants registered (bound where genuinely asserted); CHAN-01..05 satisfied and INV-S7 (N=2) validated.
</success_criteria>

<output>
Create `.planning/phases/01-channels-subsystem/01-09-SUMMARY.md` when done. Record CHAN-05/INV-S7 validation status and any invariant left `pending` with its coverage bead.
</output>
