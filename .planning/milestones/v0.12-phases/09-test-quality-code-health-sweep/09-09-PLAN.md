---
phase: 09-test-quality-code-health-sweep
plan: 09
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - internal/store/alias.go
  - internal/store/alias_test.go
autonomous: true
requirements: [QUAL-03]

must_haves:
  truths:
    - "The interface-satisfaction guarantee that was expressed as a zero-assertion test function is now expressed as a package-scope compile-time declaration, so the compiler proves it."
    - "The constructor whose test asserted nothing now has a test that asserts what the constructor returns."
    - "Every site named by the stale weak-test list has been re-derived against the current tree, with drifted-away sites recorded rather than silently skipped."
  artifacts:
    - "A package-scope interface-satisfaction declaration in internal/store/alias.go"
    - "internal/store/alias_test.go with the zero-assertion functions removed or given real assertions"
    - "A re-derivation record in the SUMMARY covering all sites named by the stale list"
  key_links:
    - "compile-time interface assertion -> build failure on a signature drift, replacing a runtime test that could never fail"
  prohibitions:
    - "Never delete a test to satisfy a quality sweep without replacing the guarantee it carried; the guarantee, not the function, is what must survive."
---

<objective>
Remediate the weak-test half of QUAL-03: replace the surviving zero-assertion compile-canary
tests with the compile-time construct that actually proves the guarantee, and record the
re-derivation of the stale site list against the current tree.

Purpose: an adversarial architecture review already ran a scripted zero-assertion sweep across
this repository and read roughly forty candidates by hand, resolving every one to a genuine
assertion behind a shared helper or a strict-mock expectation. Independently re-deriving the
three-month-old weak-test site list found that eleven of its citations point at a file that no
longer exists and a twelfth at a function that no longer exists. What actually survives is
small and specific. This plan fixes exactly that, and writes down the drift so the stale list
is not re-applied blind by a future phase.

Output: two remediated sites, a compile-time guarantee replacing a runtime one, and a written
re-derivation.

Decisions implemented: D-06 (scope is the union of a re-derived stale site list and a fresh
mechanical sweep; this plan delivers the re-derivation half and the weak-test remediation, while
the naming half is delivered by the last sweep plan).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-quality-code-health-sweep/09-RESEARCH.md
@.claude/rules/testing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the stale weak-test site list against the current tree</name>
  <files>(no repo files — analysis recorded in the SUMMARY)</files>
  <read_first>
    - .planning/archive/beads/2026-07-09-beads-live.jsonl — locate the weak-test record and read its full site list verbatim; the mirror GitHub issue was closed by the tracker migration and does not carry this detail
    - docs/reviews/arch-review/2026-07-11/findings/d9a-testing-ci.md around line 57 — the clean zero-assertion sweep whose result any predicate must be calibrated against
    - internal/store/alias_test.go lines 570-600 — the two surviving cited functions
    - internal/access/resolver_test.go — one cited function is claimed to no longer exist here; confirm
  </read_first>
  <action>
    Take every site the archived weak-test record cites and check each against the current tree,
    one at a time. For each, record one of: still present at the cited location; still present
    but drifted to a different line; the function is gone though the file remains; or the file
    itself is gone. Use the repository's search ladder — the symbol-aware search tool for Go
    symbols, plain text search for literals, never a bare pattern-matching shell command.

    Independently confirm the two claims that most affect scope: that the session in-memory
    store test file cited many times no longer exists, and that the resolver interface-canary
    function no longer exists anywhere in the tree. Do not take either on trust from the
    research document; both are load-bearing for deciding this plan is small.

    Then check the arch review's clean list. Every file it read by hand and cleared must NOT
    appear in this plan's remediation set. If your re-derivation would flag one of them,
    your reading is wrong and the site is genuinely asserting through a shared helper or a
    strict-mock expectation — read the helper before concluding otherwise.

    Record the whole re-derivation as a table in the SUMMARY. This table is the artifact that
    stops a future phase from applying the stale list blind.
  </action>
  <verify>
    <automated>test -f .planning/archive/beads/2026-07-09-beads-live.jsonl &amp;&amp; rg -c 'func TestAliasRepositoryInterface' internal/store/alias_test.go</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a row per cited site with one of the four dispositions and its current path and line where applicable.
    - The claim that the session in-memory store test file is gone is confirmed by a direct filesystem check whose result is recorded.
    - The claim that the resolver interface-canary function is gone is confirmed by a repo-wide symbol search whose result is recorded.
    - No file on the arch review's cleared list appears in this plan's remediation set.
    - The remediation set derived by this task contains at most four functions; if it is larger, stop and record why before writing any code.
  </acceptance_criteria>
  <done>The stale list has been checked site by site against the current tree and the surviving remediation set is written down with evidence.</done>
  <reversibility rating="reversible">Analysis only.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Replace the compile canary with a compile-time assertion and give the constructor test real assertions</name>
  <files>internal/store/alias.go, internal/store/alias_test.go</files>
  <read_first>
    - internal/store/alias_test.go lines 570-605 — the two functions in full, so you can see exactly what each currently proves
    - internal/store/alias.go — the concrete repository type and the interface it is meant to satisfy, and whether a package-scope assertion for it already exists elsewhere in the package
    - internal/store/ — search the package for an existing `var _ ` package-scope interface assertion to match the local convention rather than inventing one
    - .claude/rules/testing.md — the both-paths and assert-behaviour rules
  </read_first>
  <action>
    The interface-satisfaction test proves nothing at runtime: it compiles if and only if the
    type satisfies the interface, and it can never fail once compiled. Express that guarantee
    where the compiler enforces it — a package-scope variable declaration in the production
    file asserting the concrete type satisfies the interface — and delete the test function.
    Follow whatever form the package already uses for such declarations if one exists. Put a
    short comment on the declaration stating that it replaces a runtime canary and naming the
    weak-test finding, so the deletion is legible in a future blame.

    The constructor test currently exercises the constructor without asserting what it returns.
    Give it real assertions: that the returned value is non-nil, and that at least one
    observable property of the constructed repository matches what was passed in. If the
    constructor genuinely has no observable output beyond non-nil-ness, say so in the SUMMARY
    and convert the function into a table row of an existing behavioural test rather than
    leaving a standalone near-empty function.

    Rename any function you keep to an ACE sentence, avoiding a single-token trailing segment
    so it does not become a hit for the naming ratchet landing later in this phase.

    Remediate only the sites the Task 1 re-derivation confirmed. Do not go hunting for more —
    the arch review already swept this space and cleared it, and re-flagging its cleared list is
    the documented failure mode for this requirement.
  </action>
  <verify>
    <automated>task test -- ./internal/store/ &amp;&amp; rg -q '^var _ ' internal/store/alias.go</automated>
  </verify>
  <acceptance_criteria>
    - `task test -- ./internal/store/` exits 0.
    - `rg -c 'func TestAliasRepositoryInterface' internal/store/alias_test.go` returns no matches.
    - `rg -c '^var _ ' internal/store/alias.go` returns at least 1, and the declaration asserts the concrete repository type against its interface.
    - The compile-time declaration is proven load-bearing: temporarily remove a method from the concrete type, confirm `task build` exits non-zero, and restore.
    - The constructor test now contains at least two assertions, or has been folded into an existing behavioural test and the standalone function is gone.
    - No test function whose name survives has a single-token final underscore-delimited segment.
    - `task lint` exits 0 and `task fmt` has been run with its mutations committed.
  </acceptance_criteria>
  <done>The interface guarantee is enforced by the compiler instead of by a test that could never fail, and the constructor test asserts something.</done>
  <reversibility rating="reversible">Two small edits in one package.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> assurance claim | A passing suite is read as evidence that behaviour is verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-09-01 | Repudiation | zero-assertion test functions | medium | mitigate | A test that cannot fail inflates the apparent verification surface. The interface guarantee moves to a compile-time declaration that genuinely fails the build, proven by temporarily breaking it. |
| T-09-09-02 | Tampering | remediation driven by a stale site list | medium | mitigate | Applying a three-month-old list blind would edit files that no longer exist and re-flag sites an adversarial review already cleared. Task 1 re-derives every citation against the current tree and records the drift. |
| T-09-09-SC | Tampering | npm/pip/cargo installs | high | accept | No packages installed; no dependency manifest touched. |
</threat_model>

<verification>
- `task test -- ./internal/store/` exits 0 and `task test` exits 0 repo-wide.
- Removing a method from the concrete type makes `task build` fail, proving the compile-time assertion is real.
- `task lint` exits 0.
</verification>

<success_criteria>
The weak-test half of QUAL-03 is remediated at the sites that actually survive, the guarantee
that was carried by a never-failing test is now carried by the compiler, and the stale list's
drift is written down.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

- One package-scope interface-satisfaction declaration in `internal/store/alias.go` (exact spelling assigned at execution time)
- Removal of `TestAliasRepositoryInterface` from `internal/store/alias_test.go`
- `TestNewPostgresAliasRepository` either given real assertions and an ACE-conformant name, or folded into an existing behavioural test
- A re-derivation table in the SUMMARY covering every site named by the archived weak-test record
</artifacts_produced>

<output>
Create `.planning/phases/09-test-quality-code-health-sweep/09-09-SUMMARY.md` when done.
The re-derivation table is the primary artifact — it is what stops the stale list being applied
blind by a future phase.
</output>
