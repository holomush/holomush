---
phase: 04-world-model-resilience-investigation-decision-f1
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - test/integration/resilience/m2_dualwrite_test.go
  - test/integration/resilience/chaos_helpers_test.go
  - docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md
autonomous: true
requirements: [OPS-05, MODEL-01]
tags: [resilience, m2, dual-write, eventbus, evidence, world-model]
must_haves:
  truths:
    - "The M2 dual-write race window is characterized: with a wired emitter and a paused broker, the DB commit persists, the caller error carries move_succeeded=true, and the move notification never reaches the stream (D-07)"
    - "The production-wiring fact is recorded as evidence: world.Service ships with NO EventEmitter, so the notification leg is dead code in production today"
    - "A documented, reproducible verdict doc exists, quoting the harness M12/M2/chaos results, ready as the ADR's evidence input"
  artifacts:
    - "test/integration/resilience/m2_dualwrite_test.go — control, flap-window, and unwired-production-shape specs with M2-VERDICT report lines"
    - "test/integration/resilience/chaos_helpers_test.go — worldBusAppender + newEmittingWorldService helpers"
    - "docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md — the OPS-05 verdict document (success criterion #2)"
  key_links:
    - "The M2 appender publishes via the RAW Subsystem Publisher (never RenderingPublisher) — stream-presence assertions need no rendering metadata and must not trip EMIT_UNKNOWN_VERB verb-registry lookups"
    - "Stream absence is asserted over an INDEPENDENT broker connection after unpause, bounded-poll — never via connection-state callbacks (RESEARCH Pitfall 5)"
    - "The verdict doc quotes the verbatim M12-VERDICT / M2-VERDICT / CHAOS-VERDICT lines and names the single reproduction command"
---

<objective>
Characterize the M2 dual-write non-atomicity window per D-07 (prove the race window exists on a broker flap; a deterministic lost move is NOT required) and consolidate all harness evidence into the documented, reproducible verdict that success criterion #2 demands and the MODEL-01 ADR (plan 04) consumes.

Grounded facts this plan builds on (verified in-worktree): world.Service.MoveCharacter commits the row then emits post-commit with a ~350ms retry window (events.go emitWithRetry: 3 retries, exponential from 50ms), returning errors tagged move_succeeded=true on emit failure (service.go ~817-849); AND production never wires an EventEmitter into world.Service at all (internal/world/setup/subsystem.go:66-77 omits it; NewEventStoreAdapter has zero production callers) — so the harness must deliberately wire an emitter to make the window observable, and the CURRENT production behavior (notification leg entirely dead) is itself first-class evidence for the ADR.

Purpose: completes OPS-05's evidence set and produces the MODEL-01 grounding document.
Output: M2 specs green under the opt-in lane + `docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md` cross-linked from #4791.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-CONTEXT.md
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-RESEARCH.md
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-PATTERNS.md
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-02-SUMMARY.md
@docs/reviews/arch-review/2026-07-11/verification/f1-eventsourcing-why.md
</context>

## Artifacts this phase produces (plan 03 slice)

| Artifact | Kind | Location |
|----------|------|----------|
| `worldBusAppender` struct (implements `world.EventAppender`) | test helper type | `test/integration/resilience/chaos_helpers_test.go` |
| `newEmittingWorldService(s *integrationtest.Server) *world.Service` | test helper | `test/integration/resilience/chaos_helpers_test.go` |
| `Describe("M2 dual-write window", Ordered, ...)` with 3 specs | Ginkgo specs | `test/integration/resilience/m2_dualwrite_test.go` |
| `M2-VERDICT:` report-line convention | reporting convention | m2 spec file |
| `f1-resilience-verdict.md` | evidence document | `docs/reviews/arch-review/2026-07-11/verification/` |

<tasks>

<task type="auto">
  <name>Task 1: M2 dual-write window specs — control, flap window, and unwired production shape</name>
  <files>test/integration/resilience/m2_dualwrite_test.go, test/integration/resilience/chaos_helpers_test.go</files>
  <read_first>
    - internal/world/service.go — MoveCharacter (~773-858): commit → movementHook → EmitMoveEvent; the hook-failure and emit-failure paths (~817-849) whose oops errors carry move_succeeded=true; note the exact oops code strings before writing assertions
    - internal/world/events.go — EmitMoveEvent (~125) for the stream reference and event type it emits; emitWithRetry (~74-117) for the ~350ms retry window; the EventEmitter interface (~66)
    - internal/world/event_store_adapter.go — full file: EventAppender interface (Append(ctx, core.Event) error), NewEventStoreAdapter, the ActorSystem/WorldServiceActorULID actor it stamps
    - internal/testsupport/integrationtest/harness.go — busEventAppenderAdapter (~1237-1300): the core.Event→eventbus.Event translation (Qualify, NewType, actor mapping) to REIMPLEMENT in the resilience package (it is package-private), and harnessCoreToBusActor/harnessCoreActorKindToBus for the actor-kind mapping
    - internal/eventbus/types.go — eventbus.Actor / ActorKind constants (ActorKindSystem etc.)
    - test/integration/resilience/chaos_helpers_test.go — plan 01/02 helpers (startExternalNATS, startReplica, pauseBroker/unpauseBroker, newWorldService)
    - internal/errutil — AssertErrorCode / AssertErrorContext helper signatures (repo convention for oops assertions; confirm Ginkgo compatibility via GinkgoT() or fall back to oops.AsOops + Expect per external_boot_test.go:96-102)
  </read_first>
  <action>
    Add to chaos_helpers_test.go: (a) `worldBusAppender` — a struct holding an `eventbus.Publisher` and a gameID func, implementing `world.EventAppender.Append(ctx, core.Event) error` by translating exactly as harness.go's busEventAppenderAdapter does (eventbus.Qualify(gameID, event.Stream); eventbus.NewType(string(event.Type)); actor-kind mapping with ULID parse), but publishing through the RAW `s.Bus().Bus.Publisher()` — deliberately NOT eventbus.NewRenderingPublisher, because the RenderingPublisher's verb-registry Lookup would hard-fail EMIT_UNKNOWN_VERB for world event types and rendering metadata is irrelevant to stream-presence assertions; doc-comment this choice. (b) `newEmittingWorldService(s *integrationtest.Server) *world.Service` — same ServiceConfig as newWorldService plus `EventEmitter: world.NewEventStoreAdapter(&worldBusAppender{...})`.

    Create m2_dualwrite_test.go with one `Describe("M2 dual-write window", Ordered, ...)`: BeforeAll starts NATS, boots replica A only (no plugins — MoveCharacter has no in-tree command, F5/#4788; a single replica suffices because M2 is a write-vs-broker race, not a replica race), connects a guest (its CharacterID is the mover; its guest start location is the origin), creates a destination location via replicaA.NewLocation. Three specs:

    1. **It("control: with a healthy broker a move commits and its notification reaches the stream")** — svc := newEmittingWorldService(replicaA); record EVENTS stream LastSeq over an independent conn (env.Conn(t) + jetstream.New, external_boot_test.go:85-93 idiom); svc.MoveCharacter to the destination → expect nil error; assert the DB row moved (`SELECT location_id FROM characters WHERE id = $1` via Pool equals destination) AND the stream received the move event (bounded poll: LastSeq advanced, then read the new message(s) and assert one carries the move event type/subject observed from events.go).

    2. **It("flap window: the DB commit survives while the notification is permanently lost")** — the D-07 verdict. Move the character back or create a second destination; record baseline LastSeq; pauseBroker; call svc.MoveCharacter under a context deadline ≥ 30s (the publish retries block against the frozen broker; the ~350ms retry window plus per-publish timeouts must exhaust); expect a NON-nil error whose oops code matches the emit-failure code read from service.go (assert top-level code per the repo's grpc-errors rule — oops.AsOops(err).Code() — or errutil.AssertErrorCode if it asserts identity) AND whose oops context carries move_succeeded=true (errutil.AssertErrorContext or equivalent context-map inspection); assert the DB row IS at the new destination (the commit persisted); unpauseBroker; then prove permanent loss: bounded-poll the stream for a grace period (≥ 5s, poll not sleep-assert) and assert NO message after the baseline seq carries the move event for this move (the retry loop exhausted while paused and nothing re-emits after recovery). Keep the pause ≤ 60s total.

    3. **It("production shape: with no emitter wired, every move reports emit failure while the DB commits")** — svc := newWorldService(replicaA) (plan 02's emitter-less helper, the exact production construction per setup/subsystem.go:66-77); MoveCharacter with the broker HEALTHY → expect the error path (ErrNoEventEmitter-derived, code read from events.go/service.go) with move_succeeded=true context AND the DB row moved. Doc-comment: this documents that the M2 notification leg is dead code in production wiring today — the ADR records both the designed behavior (spec 2) and the current behavior (spec 3).

    Every spec emits `M2-VERDICT: <spec-short-name>: <result>` via GinkgoWriter + AddReportEntry. SPDX + integration build tag; no quarantine-marker patterns; no time.Sleep. Run `task fmt`; commit its output.
  </action>
  <verify>
    <automated>HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience -timeout 30m ./test/integration/resilience/</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 (full suite: plans 01+02+03 specs all green); output contains ≥ 3 lines matching `M2-VERDICT:` (secondary confirmation)
    - Spec 2 asserts all four legs (source assertions in the spec body): non-nil error, oops context key move_succeeded == true, DB row at destination via direct SQL, and absence of the move event on the stream after unpause
    - `rg -n "NewRenderingPublisher" test/integration/resilience/` exits with no matches (the M2 appender uses the raw publisher)
    - `rg -n "worldBusAppender|newEmittingWorldService" test/integration/resilience/chaos_helpers_test.go` — both defined
    - `rg -n "time\.Sleep" test/integration/resilience/m2_dualwrite_test.go` exits with no matches
    - Env-unset lane safety re-checked: `task test:int -- -run TestWorldModelResilience ./test/integration/resilience/` exits 0 with 0 specs executed
  </acceptance_criteria>
  <done>The M2 window is empirically characterized (D-07): commit persists, caller sees move_succeeded=true, notification permanently lost on flap — plus the production dead-code finding is pinned by a spec.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the resilience verdict document and cross-link it from #4791</name>
  <files>docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md</files>
  <read_first>
    - docs/reviews/arch-review/2026-07-11/verification/f1-eventsourcing-why.md — the sibling doc whose structure/tone this verdict doc mirrors (it is also the ADR's other grounding input)
    - .planning/phases/04-world-model-resilience-investigation-decision-f1/04-02-SUMMARY.md — the recorded M12/CHAOS verdict lines and runtimes
    - test/integration/resilience/ — the suite files (to cite spec names and the reproduction command accurately)
  </read_first>
  <action>
    First re-run the full opt-in suite once as the canonical evidence run — `HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience -timeout 30m ./test/integration/resilience/` — capturing output to the session scratchpad (this is a long run: use run_in_background and poll; judge success by exit code). Extract every `M12-VERDICT:` / `M2-VERDICT:` / `CHAOS-VERDICT:` line verbatim.

    Then write `docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md` (sibling of f1-eventsourcing-why.md — the canonical location per RESEARCH Open Question 3, chosen here; the ADR links to it rather than duplicating it). Required sections:
    1. **Verdict summary** — one paragraph per finding: M12 (reproduced deterministically / k-of-N under natural windows / both-commands-succeed-no-conflict), M2 (window proven: commit persists, move_succeeded=true, notification permanently lost; PLUS the production finding that no emitter is wired at all), restart/reconnect (state recovers from DB, no replay; client reattach resumes delivery).
    2. **Reproduction** — the exact single command (env var + task invocation + Docker prerequisite), suite location, and typical runtime; note the suite is nightly-lane resident (D-05) and stays as the standing regression check for Phase 5's MODEL-03 guard.
    3. **Evidence** — the verbatim verdict lines from the canonical run, each mapped to its spec name and file.
    4. **Mechanism** — the citation chain for M12 (plugins/core-objects describe → SetProperty hostcap → internal/property/entity_mutator.go read-modify-write → internal/world/postgres/location_repo.go:73 unguarded full-row UPDATE → no version column in 000001_baseline.up.sql) and for M2 (service.go MoveCharacter post-commit emit, events.go emitWithRetry ~350ms window, setup/subsystem.go:66-77 unwired emitter). Note the grounded correction: the core-objects `set` command is a write-less stub, so cross-field corruption was demonstrated via describe racing the identical world.Service rename path.
    5. **Implications for MODEL-01** — strictly NEUTRAL framing per D-01 (what the evidence means for BOTH option A and option B; no lean, no recommendation — the weighing belongs to the ADR/checkpoint in plan 04).
    6. **Cross-references** — #4791 (OPS-05), #4784 (MODEL-01), #4798 (M12), f1-eventsourcing-why.md, and the suite path.

    Use `location`/`character` terminology exactly (never room/player for these concepts). Run `task lint` (markdown lint governs docs) and `task fmt` (commit any mutations). Finally post a comment on the resilience issue: `gh issue comment 4791 -R holomush/holomush --body "..."` — 3-6 lines: harness landed (path + opt-in command), verdict one-liners, link to the verdict doc path; end with the AI authorship byline per repo convention.
  </action>
  <verify>
    <automated>task lint</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md` exists and `rg -n "M12-VERDICT|M2-VERDICT|CHAOS-VERDICT" docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md` yields ≥ 3 matches (verbatim evidence lines quoted)
    - `rg -n "HOLOMUSH_RUN_QUARANTINED=1" docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md` — at least 1 match (the reproduction command is documented)
    - `rg -n "4784|4791|4798" docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md` — all three issue numbers referenced
    - The Implications section contains NO recommendation language: `rg -in "we recommend|the evidence favors|should choose" docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md` exits with no matches (D-01 neutrality)
    - `task lint` exits 0
    - `gh issue view 4791 -R holomush/holomush --json comments --jq '.comments[-1].body' | rg -c "f1-resilience-verdict"` exits 0 (the cross-link comment landed)
  </acceptance_criteria>
  <done>Success criterion #2 is met: a documented, reproducible verdict exists at the canonical location, quoting the evidence run, neutral per D-01, and cross-linked from #4791 — ready as plan 04's ADR input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness emitter wiring → production wiring | the deliberately-wired test emitter must not be mistaken for (or promoted into) production wiring |
| evidence doc → ADR | mis-recorded evidence would corrupt the decision input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Denial of Service | required `Integration Test` PR gate | high | mitigate | all new specs inside the gated TestWorldModelResilience entry; env-unset check re-asserted in Task 1 acceptance |
| T-04-06 | Repudiation | verdict doc vs actual run results | medium | mitigate | evidence lines quoted VERBATIM from a single canonical run judged by exit code; reproduction command documented so any reader can re-derive |
| T-04-01 | Elevation of Privilege | worldBusAppender / emitting service in test code | low | mitigate | helpers live under //go:build integration in test/; depguard blocks production import paths |
</threat_model>

<verification>
- Full opt-in suite green (exit code 0) with all three verdict-line families present
- `task lint` and `task test` green
- Verdict doc exists, quotes evidence, stays neutral, cross-linked from #4791
</verification>

<success_criteria>
- D-07 satisfied: M2 window proven (not merely asserted) under the two-replica substrate, plus the unwired-emitter production finding pinned by a spec
- Success criterion #2 satisfied: documented, reproducible verdict at docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md
- The ADR's complete evidence set exists (f1-eventsourcing-why.md + f1-resilience-verdict.md)
</success_criteria>

<output>
Create `.planning/phases/04-world-model-resilience-investigation-decision-f1/04-03-SUMMARY.md` when done. MUST include: the verdict doc path, the verbatim verdict-line set, the exact oops codes observed on the M2 error paths, and the #4791 comment URL.
</output>
