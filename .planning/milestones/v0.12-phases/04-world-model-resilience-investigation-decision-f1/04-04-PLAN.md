---
phase: 04-world-model-resilience-investigation-decision-f1
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - docs/adr/holomush-i4784-world-state-model-decision.md
  - docs/adr/README.md
autonomous: false
requirements: [MODEL-01]
tags: [adr, world-model, event-sourcing, crud, decision-gate]
must_haves:
  truths:
    - "A committed ADR records the world-state model decision, weighing (A) real event sourcing and (B) CRUD-canonical GENUINELY EQUALLY with no starting lean (D-01)"
    - "The ADR records intent — was world-state event sourcing ever meant to be real — closing the drift-by-default gap the F1 archaeology found (D-02)"
    - "The ADR names the CONCRETE mechanism Phase 5 (MODEL-03/MODEL-04) will implement (success criterion #4)"
    - "The decision itself is made by the human decider at a blocking checkpoint, not by the executor"
  artifacts:
    - "docs/adr/holomush-i4784-world-state-model-decision.md — Status: Accepted, Decision token holomush-i4784, grounded in f1-eventsourcing-why.md + f1-resilience-verdict.md"
    - "docs/adr/README.md — index row for the new ADR"
  key_links:
    - "ADR Evidence section links (not duplicates) both grounding docs: the F1 archaeology and the plan-03 resilience verdict"
    - "ADR Consequences names the Phase 5 input contract (MODEL-03 guard + MODEL-04 dual-write mechanism) and where ABAC authorization sits in the chosen mechanism"
    - "Invariant minting is explicitly deferred to Phase 5's spec (the ADR names the mechanism; Phase 5 registers/binds INV-* per .claude/rules/invariants.md)"
---

<objective>
Commit the MODEL-01 ADR: the formal, genuinely open decision between (A) building real event sourcing for world state and (B) CRUD-canonical + optimistic concurrency + transactional outbox — grounded in the F1 archaeology and the plan-03 resilience evidence, recording intent per D-02, and naming the concrete mechanism Phase 5 implements. The human decider chooses at a blocking checkpoint; the executor drafts, presents, and records.

This is the phase's decision gate (#4784). Per D-01 the ADR has NO starting lean — the user explicitly overrode the F1 doc's recommendation toward (B), so option (A) gets a fully costed, fair hearing from the verified inputs in 04-RESEARCH.md "ADR Decision Inputs".

Purpose: Phase 5 (MODEL-02/03/04) and the Phase 7 event-model collapse (ARCH-04) block on this ADR's named mechanism.
Output: an Accepted ADR at docs/adr/holomush-i4784-world-state-model-decision.md + README index row + #4784 cross-link.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-CONTEXT.md
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-RESEARCH.md
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-03-SUMMARY.md
@docs/reviews/arch-review/2026-07-11/verification/f1-eventsourcing-why.md
@docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md
</context>

## Artifacts this phase produces (plan 04 slice)

| Artifact | Kind | Location |
|----------|------|----------|
| `holomush-i4784-world-state-model-decision.md` | ADR (new) | `docs/adr/` |
| README index row for the ADR | doc index entry | `docs/adr/README.md` |
| Decision token `holomush-i4784` | post-beads decision token (grammar `holomush-i<issue#>`) | ADR header + README row |

<tasks>

<task type="auto">
  <name>Task 1: Draft the ADR — context, intent, evidence, and the two options costed equally</name>
  <files>docs/adr/holomush-i4784-world-state-model-decision.md</files>
  <read_first>
    - docs/reviews/arch-review/2026-07-11/verification/f1-eventsourcing-why.md — THE grounding doc: drift-by-default finding, M2/M12 root-cause framing, the original option (A)/(B) spellout
    - docs/reviews/arch-review/2026-07-11/verification/f1-resilience-verdict.md — the plan-03 empirical evidence (M12 verdict, M2 window, unwired-emitter finding)
    - .planning/phases/04-world-model-resilience-investigation-decision-f1/04-RESEARCH.md — the "ADR Decision Inputs" section: the 9 verified Option-A cost points and 4 Option-B mechanics points to carry into Alternatives Considered
    - docs/adr/holomush-zcyvb-merge-local-test-lint-build-offload-agents-into-one-local-ch.md — the most recent ADR template to copy (header shape, section order); OMIT any adr-render source comment (bd is retired; hand-authored is correct)
    - docs/adr/README.md — the index table shape (Title | Date | Status | decision token column)
  </read_first>
  <action>
    Create `docs/adr/holomush-i4784-world-state-model-decision.md` (filename fixed regardless of outcome — the title carries the decision; token grammar `holomush-i<issue#>` per the post-beads convention RESEARCH verified against test/quarantine.yaml). Header: HTML SPDX comment pair, markdownlint MD013 disable comment, `# <Title>` (draft title: "World-State Model: Event Sourcing vs CRUD-Canonical"), then `**Date:**` (today), `**Status:** Proposed`, `**Decision:** holomush-i4784`, `**Deciders:** Sean Brandt`. Sections, using RFC2119 keywords where contracts are stated:

    1. **Context** — the drift-by-default finding: world state was always CRUD; no rebuild path ever existed; no ADR ever recorded the divergence; CRUD-not-ES is the root cause of M2 and M12 (cite f1-eventsourcing-why.md, #4784). Summarize the empirical evidence with a link to f1-resilience-verdict.md (M12 corruption verdict; M2 window; the notification leg being unwired in production) — link, do not duplicate.
    2. **Intent** (D-02) — record whether world-state event sourcing was ever meant to be real: cite the archaeology's conclusion (intent cannot be proven from git; "event sourcing" functioned as shorthand for event-driven + audit log; the missing ADR IS the finding, and this ADR closes it). Do NOT promise further excavation.
    3. **Decision** — placeholder line: `PENDING — resolved by decision checkpoint (Task 2).`
    4. **Rationale** — same placeholder.
    5. **Alternatives Considered** — the load-bearing section, per D-01 weighed EQUALLY with no lean: **Option A (build real event sourcing for world state)** — carry all nine verified cost/benefit points from 04-RESEARCH.md "ADR Decision Inputs" §Option (A): event coverage from ~4 families to ~15-20 types with the write-path inversion; the emitter substrate being unwired (A builds the pipeline, not extends it); no salvageable rebuild substrate (the F-series deletions were client catch-up); the retention conflict with OPS-02 (30-day stream MaxAge vs permanent log — must be resolved if A); projection/rebuild machinery (idempotent replay, per-entity ordering, genesis snapshot, rebuild tooling); the concurrency caveat that A does NOT automatically dissolve M12 (validation reads still race — the guard moves, it does not vanish); ARCH-04 coupling (unified event model becomes state-load-bearing); effort class (multi-phase; a full ES build is a follow-on milestone per REQUIREMENTS.md Out of Scope — Phase 5 would implement a named foundation slice); and what A uniquely buys (replayable/derivable state, auditable reconstruction, time-travel debugging, structural elimination of the M2 class because the event IS the write). **Option B (CRUD-canonical + optimistic concurrency + transactional outbox)** — version columns (`version INTEGER NOT NULL DEFAULT 1`) on locations/characters/objects/exits with guarded `UPDATE … WHERE id = $1 AND version = $2` using RETURNING to distinguish not-found from conflict; entity_mutator/UpdateCharacterDescription carrying the read version with a named conflict-surfacing choice (retry loop vs user-visible concurrent-edit error — the ADR names which); transactional outbox written in the same Transactor.InTransaction seam with a relay publishing under Nats-Msg-Id dedup, cleanup via the OPS-02 retention machinery, and an explicit scope of which events the outbox carries (most world writes emit nothing today); and what B forgoes (no replay/rebuild; the event-sourcing doc principle is downgraded at MODEL-02's ~6 sites to event-driven + append-only audit log).
    6. **Decision framework** (Claude's-discretion item, exercised) — a neutral criteria table scoring BOTH options against: closes M12 / closes M2 / migration+effort class / retention posture vs OPS-02 / replayability value to the project / ARCH-04 impact / operational complexity. Present scores as evidence-backed observations, not a recommendation.
    7. **Consequences** — placeholder pending decision.

    Terminology discipline: `location`/`character`/`player` used exactly per .claude/rules/terminology.md. Do not register any INV-* entries — state explicitly in Consequences (when finalized) that invariant minting/binding belongs to Phase 5's spec per .claude/rules/invariants.md. Run `task lint` and fix any ADR-shape/markdown findings before committing the draft.
  </action>
  <verify>
    <automated>task lint</automated>
  </verify>
  <acceptance_criteria>
    - File exists at docs/adr/holomush-i4784-world-state-model-decision.md with `rg -c "Status:.*Proposed" docs/adr/holomush-i4784-world-state-model-decision.md` ≥ 1
    - `rg -n "holomush-i4784" docs/adr/holomush-i4784-world-state-model-decision.md` — decision token present
    - `rg -n "f1-eventsourcing-why|f1-resilience-verdict" docs/adr/holomush-i4784-world-state-model-decision.md` — BOTH grounding docs linked
    - Option A coverage completeness: `rg -in "retention|ARCH-04|does not automatically dissolve|foundation slice|replay" docs/adr/holomush-i4784-world-state-model-decision.md` yields matches for each of the five terms (the A-costing carried the hard points, not just the easy ones — D-01)
    - Option B coverage completeness: `rg -in "version INTEGER NOT NULL DEFAULT 1|RETURNING|outbox|InTransaction" docs/adr/holomush-i4784-world-state-model-decision.md` yields matches for all four
    - No lean leaked into the draft: `rg -in "we recommend|recommended option|leaning toward" docs/adr/holomush-i4784-world-state-model-decision.md` exits with no matches
    - `task lint` exits 0
  </acceptance_criteria>
  <done>A lint-clean Proposed ADR exists with intent recorded, both options fully and equally costed from verified inputs, and Decision/Rationale/Consequences explicitly pending the checkpoint.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Human decision — world-state model (A) event sourcing vs (B) CRUD-canonical</name>
  <action>Present the decision brief to the human decider and BLOCK until an option is selected. Assemble the brief strictly from the Task 1 draft ADR (do not introduce new claims): the drift-by-default context in two sentences, the verbatim verdict one-liners from f1-resilience-verdict.md, the decision-framework table, and the two options below. Do NOT advocate; per D-01 both options are presented with equal weight. Record the selected option and any decider directives for Task 3.</action>
  <decision>Which world-state model does HoloMUSH commit to, and therefore which concrete mechanism does Phase 5 (MODEL-03/MODEL-04) implement?</decision>
  <context>
    Present a compact brief assembled from the draft ADR (Task 1): the drift-by-default context, the empirical one-liners from f1-resilience-verdict.md (M12 verdict as measured; M2 window as characterized; the unwired-emitter production finding), and the decision-framework table. Per D-01 this decision was deliberately kept open with NO lean — both options below are presented with equal weight. Phase 5's scope, the OPS-02 retention interaction, and the Phase 7 ARCH-04 collapse all take their shape from this choice.
  </context>
  <options>
    <option id="option-a">
      <name>Build real event sourcing for world state (Phase 5 implements a named foundation slice)</name>
      <pros>Replayable/derivable state; auditable reconstruction and time-travel debugging; structurally eliminates the M2 class (the event IS the write — no post-commit notification to lose); realizes the originally stated architectural principle</pros>
      <cons>Multi-phase build (event taxonomy ~15-20 types, write-path inversion, projections, genesis snapshot, rebuild tooling — full build is a follow-on milestone per REQUIREMENTS.md Out of Scope); the emit pipeline must be BUILT, not extended (unwired today); permanent-retention requirement conflicts with OPS-02's stream/audit bounding and must be resolved in the ADR; does NOT automatically dissolve M12 (an optimistic command/aggregate-version protocol is still needed); makes the ARCH-04 unified event model state-load-bearing</cons>
    </option>
    <option id="option-b">
      <name>CRUD-canonical + optimistic concurrency (version guard) + transactional outbox (Phase 5 implements MODEL-03 version guard + MODEL-04 outbox)</name>
      <pros>Directly closes M12 (version-guarded CAS UPDATE with RETURNING) and M2 (outbox written in the same transaction via the existing Transactor.InTransaction seam, relay with Nats-Msg-Id dedup); in-schema precedent exists (access_policies.version); bounded effort inside v0.12; keeps events a notification/audit concern for ARCH-04</pros>
      <cons>Forgoes replay/rebuild permanently for world state; the "event sourcing" principle is formally downgraded across the docs (MODEL-02's ~6 sites); a conflict-surfacing UX choice (retry vs user-visible error) must be made and lived with</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b (optionally with directives for the Rationale/Consequences wording, e.g. the conflict-surfacing choice under B or the foundation-slice scope under A)</resume-signal>
  <done>The human decider has selected option-a or option-b; the selection and any directives are recorded for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize the ADR per the decision, index it, and cross-link #4784</name>
  <files>docs/adr/holomush-i4784-world-state-model-decision.md, docs/adr/README.md</files>
  <read_first>
    - docs/adr/holomush-i4784-world-state-model-decision.md — the Task 1 draft with pending sections
    - docs/adr/README.md — index table format and ordering
    - .planning/phases/04-world-model-resilience-investigation-decision-f1/04-CONTEXT.md — D-01/D-02 wording the final text must still honor
  </read_first>
  <action>
    Fill the pending sections per the checkpoint outcome and any decider directives:
    1. **Decision** — one unambiguous paragraph naming the chosen model AND the concrete Phase 5 mechanism (success criterion #4). If option-b: MODEL-03 = `version INTEGER NOT NULL DEFAULT 1` on locations/characters/objects/exits + guarded full-row `UPDATE … WHERE id = $1 AND version = $2` with `RETURNING` conflict/not-found differentiation + version threading through entity_mutator and UpdateCharacterDescription with the decided conflict-surfacing behavior; MODEL-04 = transactional outbox table written inside Transactor.InTransaction alongside the entity write, relay publishing to JetStream with Nats-Msg-Id = event ULID dedup, cleanup coordinated with OPS-02, and the decided scope of outbox-carried events. If option-a: name the Phase 5 foundation slice explicitly (which entity family projects first, where the write-inversion seam sits, how the OPS-02 retention conflict is resolved, and the optimistic command-version protocol that replaces the M12 guard), and restate that the full ES build is a follow-on milestone.
    2. **Rationale** — grounded in the linked evidence; record that the decision was made genuinely open per D-01 (the F1 doc's original lean was overridden during discuss-phase) and by whom.
    3. **Consequences** — MUST include: the Phase 5 input contract (what MODEL-02 doc corrections will say; what MODEL-03/04 implement); the statement that ABAC authorization placement is UNCHANGED by either option (world.Service checkAccess remains pre-write; no outbox relay or projection consumer bypasses authorization) — this is the plan's threat-model mitigation; the ARCH-04 (Phase 7) coordination note; the note that the resilience suite stands as the regression check for the Phase 5 guard (D-05); and the explicit deferral of INV-* registration/binding to Phase 5's spec per .claude/rules/invariants.md.
    4. Flip `**Status:** Proposed` → `**Status:** Accepted`; set the final title to name the chosen model; keep the filename unchanged.
    5. Add the README.md index row (Title | Date | Status | holomush-i4784) matching the existing table format exactly — edit ONLY the table row insertion.
    6. Run `task lint` and `task fmt` (commit mutations). Post `gh issue comment 4784 -R holomush/holomush --body "..."` — the decision one-liner, the named Phase 5 mechanism, links to the ADR path and the verdict doc; AI authorship byline. ADRs are immutable once Accepted — any future change is a superseding ADR, not an edit.
  </action>
  <verify>
    <automated>task lint</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "Status:.*Accepted" docs/adr/holomush-i4784-world-state-model-decision.md` ≥ 1 and `rg -in "PENDING" docs/adr/holomush-i4784-world-state-model-decision.md` exits with no matches
    - The Decision section names a concrete Phase 5 mechanism: `rg -in "MODEL-03|MODEL-04" docs/adr/holomush-i4784-world-state-model-decision.md` yields matches (success criterion #4)
    - Authorization placement recorded: `rg -in "checkAccess|authorization" docs/adr/holomush-i4784-world-state-model-decision.md` yields ≥ 1 match in Consequences
    - `rg -n "holomush-i4784" docs/adr/README.md` — index row present
    - `task lint` exits 0
    - `gh issue view 4784 -R holomush/holomush --json comments --jq '.comments[-1].body' | rg -c "holomush-i4784-world-state-model-decision"` exits 0
  </acceptance_criteria>
  <done>MODEL-01 complete: the ADR is Accepted, indexed, names Phase 5's concrete mechanism, records intent and the open-decision provenance, and is cross-linked from #4784.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR → Phase 5 implementation | the ADR is the input contract for security-relevant world-write mechanics (guard, outbox/projection) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | ADR mis-recording the security posture of the chosen mechanism (e.g. an outbox relay or projection consumer imagined as bypassing ABAC) | medium | mitigate | Task 3 Consequences MUST state authorization placement: world.Service checkAccess stays pre-write under BOTH options; no relay/consumer bypasses it — acceptance-checked |
| T-04-07 | Elevation of Privilege | decision made without the human decider | medium | mitigate | Task 2 is a blocking checkpoint:decision; the executor cannot self-select an option; plan is autonomous: false |
</threat_model>

<verification>
- ADR Accepted, lint-clean, indexed in README.md
- Decision names the Phase 5 mechanism concretely (MODEL-03/MODEL-04 input contract)
- D-01 (equal weighing, no lean) and D-02 (intent recorded) visibly honored in the final text
- #4784 comment links the ADR and the verdict doc
</verification>

<success_criteria>
- Success criteria #3 and #4 of the phase are met: committed ADR grounded in F1 + resilience evidence; concrete Phase 5 mechanism named
- The human decider made the choice at the checkpoint; the record shows the decision was genuinely open (D-01) and intent was captured (D-02)
</success_criteria>

<output>
Create `.planning/phases/04-world-model-resilience-investigation-decision-f1/04-04-SUMMARY.md` when done. MUST include: the chosen option, the named Phase 5 mechanism (verbatim from the Decision section), and the ADR + issue-comment links. This SUMMARY is Phase 5's planning input.
</output>
