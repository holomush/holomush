---
phase: 04-world-model-resilience-investigation-decision-f1
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - test/integration/resilience/m12_lastwritewins_test.go
  - test/integration/resilience/restart_reconnect_test.go
  - test/integration/resilience/chaos_helpers_test.go
autonomous: true
requirements: [OPS-05]
tags: [resilience, m12, last-write-wins, world-model, integration-test]
must_haves:
  truths:
    - "The M12 lost update is empirically reproduced (or proven impossible) under two-replica concurrency, with a recorded k/N or deterministic verdict (D-06)"
    - "Concurrent commands on the same world entity both report success while one committed field change is silently discarded — no conflict is ever surfaced"
    - "A replica restart preserves canonical world state (DB-derived, no replay), and a detached client reconnects and resumes event delivery"
  artifacts:
    - "test/integration/resilience/m12_lastwritewins_test.go — deterministic mechanism spec + command-fidelity race spec + hybrid probabilistic spec, each emitting M12-VERDICT report lines"
    - "test/integration/resilience/restart_reconnect_test.go — replica restart, client reconnect, and broker-flap-recovery specs"
    - "test/integration/resilience/chaos_helpers_test.go — newWorldService(server) helper mirroring production world.Service construction"
  key_links:
    - "Corruption read-back goes straight to the shared pgxpool (SELECT the row), never through sessions or subscriber frames (RESEARCH Pitfall 6)"
    - "The deterministic spec interleaves the exact Get→mutate→full-row-Update sequence of internal/property/entity_mutator.go against internal/world/postgres/location_repo.go:73"
    - "Verdict lines carry the stable prefix M12-VERDICT: so plan 03's evidence doc can quote run output verbatim"
---

<objective>
Produce the load-bearing OPS-05 evidence: empirically reproduce (or refute) M12 last-write-wins world-state corruption under the two-replica deployment (D-06), and exercise the replica-restart + client-reconnect chaos dimensions of success criterion #1.

Grounded fact from research/planning verification: every world repo Update is a full-row UPDATE with no version guard (location_repo.go:73, character_repo.go:64, object_repo.go:79, exit_repo.go:143), the read-modify-write lives in internal/property/entity_mutator.go, and the world tables have no version column (000001_baseline.up.sql:68-160). The core-objects `set` command is a stub (main.lua handle_set always returns "Unknown property" — verified in this worktree), so the cross-field corruption pair uses `describe here` (command) racing a location rename through world.Service (the identical ABAC→full-row-UPDATE write path a rename command would take).

Purpose: this verdict is success-criterion #2 and one of the two decision inputs the Phase 4 ADR (plan 04) weighs.
Output: three M12 specs + restart/reconnect specs, all green under the opt-in lane, emitting quotable M12-VERDICT report lines.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-CONTEXT.md
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-RESEARCH.md
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-PATTERNS.md
@.planning/phases/04-world-model-resilience-investigation-decision-f1/04-01-SUMMARY.md
@test/integration/resilience/chaos_helpers_test.go
</context>

## Artifacts this phase produces (plan 02 slice)

| Artifact | Kind | Location |
|----------|------|----------|
| `newWorldService(s *integrationtest.Server) *world.Service` | test helper | `test/integration/resilience/chaos_helpers_test.go` |
| `Describe("M12 last-write-wins", Ordered, ...)` with 3 specs | Ginkgo specs | `test/integration/resilience/m12_lastwritewins_test.go` |
| `Describe("Restart and reconnect", Ordered, ...)` with 3 specs | Ginkgo specs | `test/integration/resilience/restart_reconnect_test.go` |
| `M12-VERDICT:` report-line convention (GinkgoWriter + AddReportEntry) | reporting convention | m12 spec file |

<tasks>

<task type="auto">
  <name>Task 1: M12 lost-update specs — deterministic mechanism, command-fidelity race, hybrid cross-field race</name>
  <files>test/integration/resilience/m12_lastwritewins_test.go, test/integration/resilience/chaos_helpers_test.go</files>
  <read_first>
    - test/integration/resilience/chaos_helpers_test.go and boot_smoke_test.go — plan 01's helpers and topology to reuse (startExternalNATS, startReplica)
    - internal/testsupport/integrationtest/plugins.go — lines ~264-278: the exact world.NewService(world.ServiceConfig{...}) construction to mirror in newWorldService (LocationRepo/ExitRepo/ObjectRepo/SceneRepo/CharacterRepo/PropertyRepo via worldpostgres.New*Repository(pool), Engine, Transactor via worldpostgres.NewTransactor)
    - internal/world/service.go — UpdateLocation (~230: signature `(ctx, subjectID string, loc *Location) error`, ABAC checkAccess then repo.Update); grep its production callers for the subjectID string shape
    - internal/property/entity_mutator.go — locationEntityMutator SetDescription/SetName (~157): the Get→mutate one field→UpdateLocation(full object) sequence the deterministic spec replicates
    - internal/world/postgres/location_repo.go — line ~73: the unguarded full-row UPDATE (name, description, type, shadows_id all rewritten from the caller's in-memory copy)
    - plugins/core-objects/main.lua — handle_describe (~100-160): `describe here <text>` → SetProperty on the current location; note handle_set (~347-367) is a stub that never writes
    - internal/testsupport/integrationtest/session.go — Session.SendCommand (~93), MoveTo (~303), exported fields
    - internal/testsupport/integrationtest/harness.go — NewLocation (~854), Pool() (~1202), AccessEngine() (~842)
  </read_first>
  <action>
    Add `newWorldService(s *integrationtest.Server) *world.Service` to chaos_helpers_test.go, mirroring the plugins.go construction verbatim over `s.Pool()` with `Engine: s.AccessEngine()` and no EventEmitter (M2's emitter wiring is plan 03's concern). Then create m12_lastwritewins_test.go with ONE `Describe("M12 last-write-wins", Ordered, ...)` that in BeforeAll: starts NATS (startExternalNATS), boots replica A via startReplica with `integrationtest.WithInTreePlugins()`, boots replica B via startReplica sharing A's ConnStr() also with WithInTreePlugins (A4 fallback: if B's plugin boot fails on shared-DB plugin migrations/policy seeding, boot B WITHOUT plugins and drive B's writes via newWorldService(B) — record the fallback in a report entry and the SUMMARY), creates one location L via replicaA.NewLocation, connects one guest session per replica, and MoveTo(L) on both sessions. Three specs:

    1. **It("deterministic interleave: a stale full-row write silently reverts a committed rename")** — using svcA := newWorldService(replicaA) and svcB := newWorldService(replicaB): read L through A's path and B's path (both now hold in-memory copies); via svcA.UpdateLocation commit a name change (e.g. name-A-committed) using A's copy; then via svcB.UpdateLocation commit a description change using B's STALE copy (whose Name still holds the original). Both calls must return nil error. Read back `SELECT name, description FROM locations WHERE id = $1` via replicaA.Pool(): assert description equals B's new value AND name equals the ORIGINAL (A's committed rename silently destroyed). subjectID: use the production subject shape found at world.Service write-path call sites (grep callers; the allow-all engine accepts any string). This replicates entity_mutator's exact sequence with explicit interleave control — deterministic, 1 round, and it is the D-06 corruption verdict's mechanism proof.

    2. **It("concurrent describe commands both succeed with no conflict signal")** — N=50 rounds; each round fires sessA.SendCommand(ctx, "describe here alpha-<n>") and sessB.SendCommand(ctx, "describe here beta-<n>") from two goroutines released by a start-gun channel + sync.WaitGroup (RESEARCH Pattern 3; no sleeps in any production path). Per round assert: both SendCommand errors are nil, and the read-back description (via Pool) equals exactly one of the two texts. Aggregate assertion: across all N rounds no conflict/error was ever surfaced while N writes were silently superseded. This is D-06's "concurrent commands" reachability evidence.

    3. **It("cross-field race: a concurrent command resurrects a stale field")** — N=100 rounds, early-exit once lost-update count k reaches 5: each round races sessA.SendCommand(ctx, "describe here text-<n>") against a B-side read-modify-write rename (Get L fresh, set Name to name-<n>, svcB.UpdateLocation) via start-gun. After both complete, read back name+description: count a lost update when name != name-<n> (the command's full-row UPDATE resurrected a stale name) OR description != text-<n> (the rename resurrected a stale description). The verdict is k/N; 0/N does NOT refute M12 (spec 1's deterministic proof stands) — it only bounds the natural window's width, and the spec's failure message + report entry must say so.

    Every spec writes a stable-prefix verdict line to GinkgoWriter AND AddReportEntry — format: `M12-VERDICT: <spec-short-name>: <result>` (e.g. reproduced deterministically / both-succeed-no-conflict N=50 / k=<k> of N=<n> natural-window races lost). Read-backs go ONLY through the shared pool (RESEARCH Pitfall 6). Bound every Eventually/context with explicit timeouts; total Describe budget ≤ 10 minutes. SPDX header + integration build tag; nothing in the file may match the quarantine markerLineRE patterns (test/meta/quarantine_registry_test.go:31). Run `task fmt`; commit its output.
  </action>
  <verify>
    <automated>HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience -timeout 25m ./test/integration/resilience/</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 (judge by exit code); the run output contains at least 3 lines matching `M12-VERDICT:` (secondary confirmation, quoted into the SUMMARY)
    - Spec 1 asserts BOTH: nil errors from both UpdateLocation calls AND the read-back name equals the pre-race original (source assertion: the spec contains an Expect on the name being the original value after both writes)
    - `rg -n "SELECT name, description FROM locations" test/integration/resilience/m12_lastwritewins_test.go` — at least 1 match (read-back is direct SQL via the pool)
    - `rg -n "time\.Sleep" test/integration/resilience/m12_lastwritewins_test.go` exits with no matches (no sleep-based sequencing; start-gun + bounded polling only)
    - Env-unset lane safety still holds: `task test:int -- -run TestWorldModelResilience ./test/integration/resilience/` exits 0 with 0 specs executed
  </acceptance_criteria>
  <done>M12 verdict evidence exists in three graduated forms (deterministic mechanism, command-only both-succeed, cross-field natural-window k/N), reproducible via one documented command, with quotable M12-VERDICT report lines.</done>
</task>

<task type="auto">
  <name>Task 2: Replica-restart, client-reconnect, and broker-flap-recovery specs</name>
  <files>test/integration/resilience/restart_reconnect_test.go</files>
  <read_first>
    - test/integration/resilience/chaos_helpers_test.go — startExternalNATS, startReplica, pauseBroker/unpauseBroker, newWorldService
    - internal/testsupport/integrationtest/harness.go — Server.Stop() (~745): what it tears down; whether a second Stop (via t.Cleanup) is tolerated
    - internal/testsupport/integrationtest/session.go — DetachTransport (~366), ReattachTransport (~419), EmitDirectEvent (~770), WaitForEvent (~171)
    - internal/eventbus/eventbustest/embedded.go — AwaitStreamLastSeq (~90): the polling publish-barrier idiom to mirror over an independent conn
    - test/integration/eventbus_external/external_boot_test.go — lines 85-93: independent-conn jetstream inspection
  </read_first>
  <action>
    Create restart_reconnect_test.go with one `Describe("Restart and reconnect", Ordered, ...)`: BeforeAll starts NATS + replica A (fresh DB) and replica B (shared, via A.ConnStr()); NO plugins (direct service writes suffice here — the command path was covered in Task 1). Three specs covering the remaining success-criterion-#1 chaos dimensions:

    1. **It("a restarted replica serves state written before it died")** — write a location change through newWorldService(replicaB); call replicaB.Stop(); boot replica B' via startReplica(t, env.URL, replicaA.ConnStr()); read the pre-restart change back via replicaB'.Pool() and assert it is intact. Also assert B' booted cleanly against the ALREADY-EXISTING EVENTS stream (this is the CreateOrUpdateStream idempotence path — boot success IS the assertion). Doc-comment the observation this proves for the ADR: recovery is DB-read, not event replay — no rebuild path runs at boot. If Server.Stop proves non-idempotent against the later t.Cleanup, restart a DEDICATED extra replica instead of B and note it.

    2. **It("a detached client reattaches and resumes live delivery")** — sess := replicaA.ConnectGuest(ctx); record the EVENTS stream LastSeq over an independent conn (env.Conn(t) + jetstream.New); sess.DetachTransport(ctx); sess.ReattachTransport(ctx); publish via sess.EmitDirectEvent and assert LastSeq advances (bounded poll, AwaitStreamLastSeq idiom) AND, if WaitForEvent semantics allow observing the emitted type post-reattach, assert delivery through the reattached stream too (outcome-based; never assert on connection-state callbacks — RESEARCH Pitfall 5).

    3. **It("publishing resumes after a broker flap")** — record LastSeq; pauseBroker(ctx, env); attempt one EmitDirectEvent while paused (accept EITHER an error return OR a delayed success — record which in a report entry; the JetStream publish path may block up to its internal timeout, so bound the attempt with a context deadline ≥ 10s); unpauseBroker; then EmitDirectEvent again and assert LastSeq advances past the recorded value within a bounded poll. Keep the pause ≤ 10 seconds (RESEARCH Pitfall 7 runtime budget).

    Report entries with stable prefix `CHAOS-VERDICT:` for each spec. SPDX header + integration build tag; no quarantine-marker patterns. Run `task fmt`; commit its output.
  </action>
  <verify>
    <automated>HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience -timeout 25m ./test/integration/resilience/</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 with the new specs green alongside plan 01/02 specs (exit code is the verdict)
    - Run output contains at least 3 lines matching `CHAOS-VERDICT:` (secondary confirmation)
    - `rg -n "DetachTransport|ReattachTransport" test/integration/resilience/restart_reconnect_test.go` — both matched (client-reconnect uses the existing primitives, no new transport plumbing — RESEARCH Don't Hand-Roll)
    - `rg -n "ContainerPause|pauseBroker" test/integration/resilience/restart_reconnect_test.go` — at least 1 match (flap exercised via the plan-01 pause helper)
    - `rg -n "time\.Sleep" test/integration/resilience/restart_reconnect_test.go` exits with no matches
  </acceptance_criteria>
  <done>All four chaos dimensions of success criterion #1 (concurrent commands, broker flap, replica restart, client reconnect) are exercised by green opt-in specs; restart demonstrably recovers from the DB with no replay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resilience suite → gating CI | new specs must stay behind the plan-01 Enabled() gate |
| test world-writes → shared DB | specs mutate only entities they create (own location ULIDs); no fixture cross-contamination |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Denial of Service | required `Integration Test` PR gate | high | mitigate | specs live inside the already-gated TestWorldModelResilience entry; env-unset lane check re-asserted in Task 1 acceptance |
| T-04-05 | Information Disclosure | none new — specs read only rows they created via direct SQL | low | accept | test-only package under //go:build integration; depguard keeps testsupport out of production |
| T-04-03 | Tampering (supply chain) | dependencies | low | accept | zero new packages (RESEARCH audit: not applicable) |
</threat_model>

<verification>
- `HOLOMUSH_RUN_QUARANTINED=1 task test:int -- -run TestWorldModelResilience -timeout 25m ./test/integration/resilience/` exits 0; M12-VERDICT and CHAOS-VERDICT lines present
- `task test:int -- -run TestWorldModelResilience ./test/integration/resilience/` (env unset) exits 0 with 0 specs
- `task lint` green
</verification>

<success_criteria>
- D-06 satisfied: M12 corruption reproduced deterministically (or the specs prove it cannot occur, with the mechanism analysis recorded in the report entries)
- Success criterion #1's four chaos dimensions all exercised and green
- Verdict lines quotable by plan 03's evidence doc
</success_criteria>

<output>
Create `.planning/phases/04-world-model-resilience-investigation-decision-f1/04-02-SUMMARY.md` when done. MUST include: the verbatim M12-VERDICT and CHAOS-VERDICT lines from the green run, the k/N observed in spec 3, whether the A4 dual-plugin fallback was needed, and total suite runtime.
</output>
