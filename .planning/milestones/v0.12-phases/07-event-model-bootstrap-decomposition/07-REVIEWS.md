---
phase: 7
round: 12
reviewers: [codex, opencode]
reviewed_at: 2026-07-17T20:54:37Z
plans_reviewed: [07-01-PLAN.md, 07-02-PLAN.md, 07-03-PLAN.md, 07-04-PLAN.md, 07-05-PLAN.md, 07-06-PLAN.md, 07-07-PLAN.md, 07-08-PLAN.md, 07-09-PLAN.md, 07-10-PLAN.md, 07-11-PLAN.md]
plans_revision: "rev 14 (0e5abac5f)"
notes: "Round 11 preserved at 4b58a046d. FULL round: both reviewers received all 11 plans with the standard source-grounded prompt plus a round-context note (rev 14, round-11 fixes summarized, dispositions settled). codex: default model. opencode: openrouter/moonshotai/kimi-k3 — completed first attempt, no 429s (the correct registered slug was used from the start this round). Both lanes independently re-verified the rev-14 fixes as accurate and both independently converged on 07-09 as the remaining defect surface, with non-overlapping findings. The orchestrator verified every HIGH/MEDIUM against the live tree before writing the consensus."
---

# Cross-AI Plan Review — Phase 7 (Round 12, rev 14, full set)

## Codex Review

# Cross-AI Plan Review — Round 12

## Summary

Revision 14 is unusually thorough and source-grounded. Plans 07-01 through 07-08, 07-10, and 07-11 appear executable and compose coherently. However, the phase is **NOT READY**: 07-09 has an unsatisfiable task boundary. Task 2 makes gRPC depend on TLS and requires a successful full boot, while Task 3 is where TLS is actually added to the production subsystem set. The current orchestrator rejects unregistered dependencies, so Task 2’s boot gate cannot pass without prematurely performing Task 3 work.

## Plan-by-plan assessment

| Plan | Assessment | Grounding |
|---|---|---|
| 07-01 | Ready | The client implementation is genuinely leaf-like: its imports are external libraries and generated protos only ([client.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/grpc/client.go:7)). Telnet currently imports the server monolith solely for the client/error helper ([gateway_handler.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/telnet/gateway_handler.go:27)). |
| 07-02 | Ready | The vocabulary collision is real: telnet renders `core.EventTypeArrive/Leave` directly ([gateway_handler.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/telnet/gateway_handler.go:1246)). A dependency-free vocabulary leaf resolves it without importing eventbus. |
| 07-03 | Ready | The specified gateway leaves correspond to direct gateway uses of `core.ParseCommand` and `core.NewULID` ([gateway_handler.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/telnet/gateway_handler.go:406), [gateway_handler.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/telnet/gateway_handler.go:669)). |
| 07-04 | Ready | The existing gate only inspects direct AST imports ([gateway_imports_test.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/cmd/holomush/gateway_imports_test.go:148)), and the registry is pending with the phantom `internal/auth/service` entry ([invariants.yaml](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/docs/architecture/invariants.yaml:2340)). The planned closure assertion and genuine binding are appropriate. |
| 07-05 | Ready | The auth-cycle repair is necessary: auth stores `*core.Engine` and invokes both disconnect and end-session behavior ([auth_service.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/auth/auth_service.go:26), [auth_service.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/auth/auth_service.go:235)). The consumer-defined interface avoids the documented cycle. |
| 07-06 | Ready | The duplicate broadcast payload is confirmed in command and hostcap ([types.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/command/types.go:618), [system_broadcaster.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/plugin/hostcap/system_broadcaster.go:45)). One builder is the correct convergence point. |
| 07-07 | Ready | Both event representations exist, while `eventbus.Event` contains the package-private `auditRow` seam that prevents moving it elsewhere ([types.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/eventbus/types.go:141), [event.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/core/event.go:233)). |
| 07-08 | Ready | The pagination defect is traced correctly: `ReplayTail` sets only `BeforeID` and destroys `Seq` through `core.Event` conversion ([sub_grpc.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/cmd/holomush/sub_grpc.go:916)); the host cursor hardcodes zero ([servers.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/plugin/hostcap/servers.go:1279)); the history contract says ordering is by sequence and zero means tail ([bus.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/eventbus/bus.go:87)). |
| 07-09 | **Blocked** | Task 2’s boot gate and Task 3’s TLS registration ordering are mutually incompatible. See the HIGH concern below. |
| 07-10 | Ready after 07-09 | The plan correctly addresses both present defects: rollback currently inherits the startup context, and `StopAll` has no deadline or protection from a non-cooperative `Stop` ([orchestrator.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/lifecycle/orchestrator.go:58), [orchestrator.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/lifecycle/orchestrator.go:80)). |
| 07-11 | Ready after 07-09/10 | Revision 14’s Tasks 1–3 atomic unit resolves the former compile-boundary defect. The proposed rollback also improves current behavior by including the failing, potentially partially prepared subsystem; today IDs enter `startOrder` only after successful `Start` ([orchestrator.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/lifecycle/orchestrator.go:58)). |

## Strengths

- Cross-plan dependencies are generally modeled explicitly. In particular, the plans distinguish data-flow ordering from domain-bind ordering and avoid the EventBus/verifier cycle.

- The event-collapse sequence preserves compileability: vocabulary and broadcast dependencies move first, then consumers and bridges collapse.

- 07-08 tests the real cursor circuit rather than merely checking that a nonzero sequence appears. The oldest-event page anchor and Lua/binary symmetry are both pinned.

- 07-10 correctly handles Go defer argument timing by constructing the shutdown timeout inside the deferred closure.

- 07-11’s two-sweep design is backed by rollback, idempotency, partial-prepare cleanup, disabled-mode, and real production-graph tests. Its revised single-commit boundary is consistent with the repository’s “every commit builds” requirement.

## Concerns

- **HIGH — 07-09 Task 2 cannot satisfy its own boot gate before Task 3.**

  Task 2 requires gRPC to declare `SubsystemTLS` as a dependency ([07-09-PLAN.md](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/.planning/phases/07-event-model-bootstrap-decomposition/07-09-PLAN.md:749)) and ends with whole-repository and KEK-wired boot gates ([07-09-PLAN.md](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/.planning/phases/07-event-model-bootstrap-decomposition/07-09-PLAN.md:1120)). But TLS is not added to `productionSubsystems` until Task 3 ([07-09-PLAN.md](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/.planning/phases/07-event-model-bootstrap-decomposition/07-09-PLAN.md:1220)), whose entry gate itself requires Task 2’s boot to have passed.

  The current production registration passes exactly 16 subsystems and no TLS ([core.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/cmd/holomush/core.go:1087)). `topoSort` returns an error when a declared dependency is not registered ([orchestrator.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/lifecycle/orchestrator.go:108)). Adding TLS to the positional helper during Task 2 also cannot satisfy the gate without unplanned Task 3 edits, because its tests and arrays are hard-coded to 16 ([core_subsystems_test.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/cmd/holomush/core_subsystems_test.go:26), [core_subsystems_test.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/cmd/holomush/core_subsystems_test.go:57)).

- **MEDIUM — 07-09 gives contradictory instructions about the TLS package identity.**

  The existing package is named `tlscerts`, not `tls`, in both production and test files ([certs.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/tls/certs.go:4), [certs_test.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/tls/certs_test.go:5)). The plan nevertheless says the package name is `tls`, requires `tls.EnsureCerts`, and says not to rename the package ([07-09-PLAN.md](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/.planning/phases/07-event-model-bootstrap-decomposition/07-09-PLAN.md:635)). A new `package tls` file would fail because one directory cannot contain `tls` and `tlscerts`; retaining `package tlscerts` makes `tls.EnsureCerts` valid only through an explicitly documented import alias.

- **LOW — 07-11 deletes `Start` but leaves stale accessor diagnostics in scope.**

  Many accessors currently document and panic with “called before Start()”, for example the database accessors ([subsystem.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/store/subsystem.go:89)) and plugin accessors ([subsystem.go](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/internal/plugin/setup/subsystem.go:474)). 07-11 preserves these guards and removes only the eager-prestart justification prose ([07-11-PLAN.md](/Volumes/Code/github.com/holomush/.worktrees/v0.12-phase7/.planning/phases/07-event-model-bootstrap-decomposition/07-11-PLAN.md:1046)). After `Start` is removed, those messages describe a nonexistent method. This is not a compile blocker, but it leaves the final API internally contradictory.

## Suggestions

- Make 07-09 Tasks 2 and 3 one atomic execution unit, with a single commit and whole-repository/KEK boot gates at Task 3’s boundary. This mirrors the now-correct 07-11 arrangement and avoids duplicating Task 3 work inside Task 2.

- Alternatively, move the entire named-set conversion, TLS registration, and 16→17 test cascade into Task 2. Do not merely add a positional TLS argument; that recreates the LOW-8 surface Task 3 exists to remove.

- Specify that `internal/tls/subsystem.go` uses `package tlscerts`. Prefer `tlscerts.EnsureCerts`; if `tls.EnsureCerts` is desired, explicitly state that `tls` is only a local import alias.

- Add a 07-11 completion gate that migrates accessor comments and panic messages from “before Start()” to “before Prepare()”, including any exact-string tests.

## Risk Assessment

**Overall risk: HIGH / NOT READY.** The implementation design is otherwise strong, but 07-09’s mandatory task-level boot gate has no satisfiable implementation state. Once that boundary is corrected, residual risk falls to **MEDIUM**, driven mainly by the breadth of the Wave A and Wave B bootstrap refactors rather than an identified architectural defect.
---

## OpenCode Review (openrouter/moonshotai/kimi-k3)

I'll verify the revision-14 plans against the live source tree, focusing on gate satisfiability, compile-order across task boundaries, cross-plan composition, and claims that might contradict the current source.
Round-11 fixes verified accurate so far: both stubs exist with `Start` as claimed (`core_subsystems_test.go:23` with `!integration` tag; `coordinator_error_test.go:36` untagged), and the `bus.GameID()` baseline is exactly 1 (`sub_grpc.go:833`) with the capital-B `EventBus.GameID` method-value usages invisible to the lowercase pattern. Continuing verification:
07-08's claims fully verified. Now checking the ARCH-03 wave claims (07-09/07-10/07-11):
Core bootstrap claims verified. Now checking the 07-06→07-07 cross-plan composition around the `busEventAppender` region and remaining seams:
Now checking 07-11's claims about existing compile-time assertions, the audit Stop, and the eventbus DontListen substrate claim:
Found a potential contradiction: 07-03 asserts "PostgresEventStore was deleted by the F1–F7 JetStream cutover; it does not exist" — but `store.PostgresEventStore` is referenced in `cmd/holomush/cmd_audit.go`. Verifying:
I hit the `rg -r` replace trap the RESEARCH doc warned about — rerunning correctly:
The 07-09 Commit A/B split needs close scrutiny — verifying the remaining harness/hostcap claims:
Verifying that 07-03's end-of-plan zero-closure gate is actually satisfiable — checking the remaining gateway-side packages' transitive deps:
Final verification batch — cluster's split-file layout, and the 07-09 A/B commit boundary mechanism:
One last grounding check for the wrapPublisher mechanism, then I'll write the review:
Verification complete — over 60 claims checked across all 11 plans against the live tree at `0e5abac5f`. Here is the review.

---

# Cross-AI Plan Review — Round 12 (Phase 7, revision 14)

## Summary

Revision 14 is in excellent shape. I re-verified the round-11 fixes directly against source and they are all accurate: the two Start-only stubs that forced 07-11's atomic-unit squash exist exactly as described (`cmd/holomush/core_subsystems_test.go:23` with `//go:build !integration` at `:4`; `internal/eventbus/crypto/invalidation/coordinator_error_test.go:36` untagged and typed against `cluster.Registry`); the 07-06 `bus.GameID()` baseline is exactly 1 (`sub_grpc.go:833`) with the capital-B `EventBus.GameID` method-value forms at `:277/:459/:514/:580/:602` correctly invisible to the case-sensitive pattern; and `grpcSubsystem.Start` indeed has no `bus` local today. Across all 11 plans, every load-bearing source claim I spot-checked (60+) verified accurate — including the previously-litigated dispositions (FINDING-1 cycle, zero-seq tail policy, oldest-anchor, MEDIUM-11 reverse-edge prohibition, D-13.x tables). I found **one MEDIUM execution defect** (an unbootable commit-boundary recommendation in 07-09) and two LOW rationale/phrasing defects. Nothing blocks.

## Strengths

- **The self-correction apparatus is factually accurate, not just thorough.** I traced the three most aggressive retractions to source and all hold: (a) 07-08's `<red_framing_correction>` — `bus.go:88-105` confirms `BeforeID` is a tripwire, `matchesQuery` (`hot_jetstream.go:392-402`) has no `BeforeID` branch, `buildConfig` only takes the cursor path `if q.BeforeSeq > 0` (`:338`), and `cold_postgres.go:132` computes `hasCursor := cursorSeq > 0` — a lone `BeforeID` genuinely paginates nothing, so the quiet-stream repeat is real and the retracted "ID-only fallback" genuinely never existed; (b) `<page_advance_anchor>` — `hostcap/servers.go:900-906` and `stdlib_focus.go:452-462` both already index `[0]` with comments naming the oldest event as the backward anchor, and the two independent `Seq: 0` literals in `stdlib_focus.go` (`:441` per-event, `:459-463` next_cursor) are exactly where the plan says; (c) 07-05's FINDING-1 — `go list -deps ./internal/eventbus` does contain `internal/auth` (and `internal/store`), so the consumer-defined interface in `internal/auth` is mandatory, not optional.
- **Gate-satisfiability engineering is mostly sound.** 07-01/07-05/07-06/07-11's atomic-unit pattern (scoped package gates mid-unit, whole-repo gates at the unit end) is consistent with what actually compiles at each boundary. 07-03's end-of-plan zero-closure gate is satisfiable — I verified every gateway-side package (`telemetry`=3 deps, `gatewaymetrics`/`xdg`/`naming`/`logging`/`observability`=1, `config`=2, `gamenotice`) is domain-free, so once the leaf moves land, nothing else keeps telnet/web's closures non-zero. 07-04's positive control is viable (`go list -deps ./internal/grpc` = 41 internal packages including `internal/store`).
- **Cross-plan composition verified clean where it matters.** Wave-3 (07-03∥07-05) and wave-4 (07-04∥07-06) `files_modified` intersections are empty. The 07-05→07-06→07-07 sequence on `sub_grpc.go` composes: 07-06's two-arg `ConfigureSystemBroadcaster` and `sysbroadcast.NewBroadcaster` take a publisher + gameID closure (not `eventStore`), so 07-07's deletion of `busEventAppender` doesn't strand them; the harness adapter 07-05's FINDING-5 says to copy genuinely exists with the claimed `{publisher, gameID func() string}` shape (`integrationtest/harness.go:1328-1340`).
- **The ARCH-03 wave's hardest claims all check out.** Five eager starts at `core.go:287/:462/:797/:800/:977`; 16-param `productionSubsystems` (`core.go:1462-1472`); `Pool()`/`GameID()` panic-before-Start guards (`internal/store/subsystem.go:90-111`); the MEDIUM-11 data-flow derivation (`core.go:735` `auditPublisher` ← `eventBusSub.Publisher()`; `readstream_wiring.go:115-123` nil-check includes `AuditPublisher`; `verifier_subsystem.go:67-69` returns `[Database]` today); the round-9 plugin cleanup hole (`Stop` no-ops on `s.manager == nil` at `plugin/setup/subsystem.go:451-453`; three pre-manager error paths at `:333/:341/:381`; sweeper launched at construction `goplugin/host.go:359`); the two-stub rev-14 gate mechanics (verified above); the 10 existing `var _ lifecycle.Subsystem` assertions (counted exactly 10; +7 = 17 is coherent).
- **Honest test-pattern documentation.** The KEK-gate phrasing note (07-09/07-10/07-11) is accurate: `admin_rekey_e2e_test.go` declares no top-level `Test*` func; `TestAdminAuthenticateE2E` (`admin_authenticate_e2e_suite_test.go:32`) is the sole Ginkgo entrypoint and runs the whole suite, rekey specs included — so coverage is real while the `|AdminRekey` half of the pattern is a no-op. Keeping the command while documenting this is the right call.

## Concerns

- **MEDIUM — 07-09 Task 2's recommended Commit A/B split is unbootable by the plan's own verified mechanism.** The action recommends: *Commit A = items 1-2 and 5-7* (which includes **item 2's deletion of `dbSub.Start(ctx)`** at `core.go:287`), *Commit B = items 3-4, 8-9* (including **item 9's `cryptoWiring` hoist**). But nine `dbSub.Pool()` reads — `core.go:713, :743, :763, :814, :822, :858, :875, :918, :1015` — remain on `runCore`'s straight-line path until item 9 hoists them, and `Pool()` panics before Start (`internal/store/subsystem.go:90-94`). So after Commit A, boot panics at `core.go:713` (`auditChainRepo := chain.NewPostgresRepo(dbSub.Pool())`) on **every** boot, KEK or not. The plan's own settlement states this exact mechanism ("deleting `dbSub.Start(ctx)` makes **the very next line** (`:296`) panic… One missed disposition is a boot panic") and carefully disposes of `:296`/`:302`/`:385` inside Commit A — then leaves the nine `:713+` reads undisposed past the same deletion. The escape hatch ("if the intermediate state cannot be made to **compile cleanly**… collapse to one commit") doesn't cover the actual failure mode: Commit A compiles fine and panics at **runtime**, surfaced only by the mandated post-commit KEK-wired E2E. An executor following the recommendation literally burns one full KEK-gate cycle discovering the split can't work. Severity is MEDIUM not HIGH because the guidance is advisory ("a convenience, not a contract"), the failure is loud rather than silent, and the task-level hard gates (full green at Task 2's boundary) are unaffected.
- **LOW — 07-03 Task 1's "PostgresEventStore was deleted by the F1–F7 JetStream cutover; it does not exist" is false.** `internal/store/postgres.go:35` — `type PostgresEventStore struct` is alive; its own doc says it "provides system-info and game-ID persistence… Event append/replay is handled by the JetStream event bus (F7+)", and it remains the DB subsystem's default `EventStoreFactory` (`internal/store/subsystem.go:21`) and the audit DLQ CLI's store (`cmd/holomush/cmd_audit.go:197`). What was deleted is the `Replay` method / event-append path (verified: no `Replay` and no `WHERE id > afterID` anywhere in `internal/store/`). The doc-rewrite the plan mandates is still correct — the `core/ulid.go:30-38` and `ulid_test.go:36-44` Replay-ordering rationale is genuinely stale — and the acceptance criteria are satisfiable as written (they grep the new leaf files, which won't mention the type either way). But the flat non-existence claim is the same false-rationale class rounds 5-10 systematically corrected for D-04/D-15/D-16, and an executor who verifies with `rg PostgresEventStore` will find the type alive and may doubt the (correct) correction. (`PostgresSessionStore` likewise survives at `internal/store/session_store.go:21` with no `UpdateCursors` — the plan's hedge on that one resolves correctly.)
- **LOW — 07-05/07-06 carry the same "underlying `eventbus.Publisher`" wrapped-vs-raw ambiguity that 07-07 round-7 explicitly corrected.** `sub_grpc.go:263` (`rawPublisher`) vs `:270` (the `wrapPublisher`-wrapped `publisher`, which is what `busEventAppender.publisher` holds at `:282`). 07-05 Task 3 and 07-06 Task 2 both say "the underlying `eventbus.Publisher`"; 07-07's round-7 correction says the identical phrasing "was ambiguous; it means the WRAPPED one — never `rawPublisher`", with the mechanism: the audit projection fails closed on a missing rendering header (`internal/eventbus/audit/projection.go:382-384`, verified). If an executor reads "underlying" as "the publisher underlying everything" and wires `rawPublisher` into `presence.NewEmitter`/`sysbroadcast.NewBroadcaster`, arrive/leave/session_ended/system broadcasts lose the App-Rendering header and are silently dropped from `events_audit` — the exact regression 07-07's correction exists to prevent on the CoreServer path. The contextual reading (the publisher *the appender holds*) resolves correctly, so this is a propagation gap, not a design error — but the disposition from round 7 was applied to one plan only.

## Suggestions

- **07-09: fix the A/B item assignment (or collapse).** Reassign so no pre-start deletion precedes the hoist: *Commit A = items 1, 5, 6, 7* (cluster/gameID providers + both file moves — all boot-safe with the pre-starts still in place); *Commit B = items 2, 3, 4, 8, 9* (the `cryptoWiring` hoist plus all pre-start deletions and the new edges together). Alternatively delete the split and mandate one commit. Also widen the escape hatch's wording from "cannot be made to compile cleanly" to "cannot be made to build **and boot** green" so the hatch covers the runtime failure mode, not just compilation.
- **07-03: restate the justification, keep the action.** Replace "PostgresEventStore was deleted… it does not exist" with "the event-append/Replay path was deleted by the F1–F7 cutover; `PostgresEventStore` survives only as system-info/game-ID persistence (`internal/store/postgres.go:33-35`), so the Replay-ordering rationale is stale even though the type name still resolves." No criterion changes needed.
- **07-05/07-06: propagate 07-07's wrapPublisher pin.** In 07-05 Task 3 and 07-06 Task 2, change "the underlying `eventbus.Publisher`" to "the `wrapPublisher`-wrapped publisher (`sub_grpc.go:270`) — the value `busEventAppender.publisher` holds today — never `rawPublisher` (`:263`); the audit projection fails closed without the App-Rendering header (`projection.go:382`)", mirroring 07-07 Task 2's round-7 text verbatim.
- **Nit (no action needed):** 07-07 lists `test/integration/pluginparity/session_admin_broadcast_test.go` in `files_modified` while 07-06 already owns its full retype (Stream→Subject rename, qualified literal, eventbus-typed assertions). Sequential waves, so harmless — but 07-07's executor may find nothing to do there; the existing cross-reference comment is adequate.

## Risk Assessment

**LOW–MEDIUM.** The plans are execution-ready. All eleven previously-litigated dispositions I re-verified (cycle repair, zero-seq policy, oldest-anchor, reverse-edge prohibition, D-13.0 barrier scoping, atomic-unit squash, grep-gate spelling, test-migration census) are accurate against the current tree, and the only MEDIUM found is advisory-level guidance with a loud, immediately-detected failure and an explicit escape hatch — not a silent or unrecoverable break. The two LOWs are rationale/phrasing defects whose mandated actions are correct regardless. Recommend: apply the three suggestions (the 07-09 commit-split reassignment is the only one that touches execution mechanics), then proceed to execution.

---

## Consensus Summary

Two grounded lanes; both independently re-verified every rev-14 fix as accurate, and both independently converged on **07-09** as the sole remaining defect surface — with **non-overlapping findings** (codex: the Task-2/Task-3 TLS gate ordering and the package-identity contradiction; kimi: the Commit A/B bootability defect inside Task 2 and two cross-plan propagation LOWs). Verdicts split again — codex NOT READY (HIGH), kimi execution-ready (LOW–MEDIUM, "nothing blocks") — and the orchestrator's source verification again sides with codex on the verdict. **Net: NOT execution-ready by 1 verified blocker + 2 verified MEDIUMs, all in 07-09, all task-gate/execution-guidance mechanics, zero design defects.** Rounds 10, 11, and 12 have now found the same defect class in successively narrower surfaces: this round it is confined to one plan.

### Orchestrator verification of the headline findings

1. **CONFIRMED — BLOCKER (codex HIGH; kimi examined the same boundary but missed this): 07-09 Task 2 cannot satisfy its own boot gate before Task 3 registers TLS.** Task 2 mandates gRPC take a TLS provider "declaring `DependsOn(lifecycle.SubsystemTLS)`" (07-09-PLAN.md:758) and its acceptance criteria require the KEK-wired full-boot E2E green plus whole-repo `task test:int` and a per-task commit. But nothing in Task 1 or Task 2 registers `TLSSubsystem` into the boot set: Task 1 only builds `internal/tls/subsystem.go` + repoints `deps.go:71` (its criteria contain no registration check), and registration is explicitly Task 3's action ("Add TLS to the set with the other subsystems", :1220) because it entails the 16→17 count cascade in `cmd/holomush/core_subsystems_test.go` — a file in Task 3's `<files>`, not Task 2's. `topoSort` hard-errors on an unregistered dependency (`internal/lifecycle/orchestrator.go:110-111` — "missing dependency"), so with the gRPC edge declared and TLS unregistered, `StartAll` fails and the boot E2E is red at Task 2's boundary. Task 3's own entry gate ("do not begin this task until Task 2's boot is proven") completes the circularity.
   **Fix (rev 15):** make 07-09 Tasks 2–3 one atomic execution unit with a single commit and the whole-repo/KEK-boot gates at Task 3's boundary — mirroring the rev-14 07-11 arrangement — or move the entire named-set conversion + TLS registration + 16→17 cascade into Task 2 (do NOT merely add a 17th positional argument; that recreates the LOW-8 surface Task 3 exists to remove).

2. **CONFIRMED — MEDIUM (codex; mechanism sharpened by orchestrator verification): 07-09 Task 1's TLS package-identity instructions are contradictory.** The existing package is `package tlscerts` (`internal/tls/certs.go:4`), but the plan spells every new symbol `tls.EnsureCerts` / `tls.NewTLSSubsystem` / `tls.TLSSubsystem` (:637, :640, :1267-1269) and tells the executor "do not rename the package" while citing an aliasing precedent that does not exist — `deps.go:8` aliases **stdlib** `crypto/tls` as `cryptotls`; `deps.go` does not import `internal/tls` at all today (`TLSCertEnsurer` defaults to the local `ensureTLSCerts`). A new `package tls` file in that directory is a compile error (two packages, one dir); reaching `tls.EnsureCerts` requires an explicit import alias `tls "…/internal/tls"` the plan never states.
   **Fix (rev 15):** state that `internal/tls/subsystem.go` is `package tlscerts`; either respell the references `tlscerts.EnsureCerts` / `tlscerts.NewTLSSubsystem` (preferred — matches the package) or explicitly mandate the `tls` import alias at each importer; correct the false `cryptotls` precedent sentence.

3. **CONFIRMED — MEDIUM (kimi, unique): 07-09 Task 2's recommended Commit A/B split is unbootable as assigned.** Commit A is defined as "items 1-2 and 5-7" — including item 2's deletion of `dbSub.Start(ctx)` (`core.go:287`) — while item 9's `cryptoWiring` hoist of the `:705-1060` block lands only in Commit B. Verified live: nine `dbSub.Pool()` reads sit on `runCore`'s straight-line path inside that block (`core.go:713, :743, :763, :814, :822, :858, :875, :918, :1015`) and `Pool()` panics before Start (`internal/store/subsystem.go:90-94`). After Commit A every boot panics at `:713` — and the plan mandates the KEK boot gate after **each** commit, so the split fails its own gate. The escape hatch covers only "cannot be made to **compile** cleanly"; this failure is a runtime panic on a commit that compiles fine.
   **Fix (rev 15):** reassign the split — Commit A = items 1, 5, 6, 7 (providers + file moves, boot-safe with pre-starts intact); Commit B = items 2, 3, 4, 8, 9 (all pre-start deletions land WITH the hoist) — or collapse to one commit; and widen the escape hatch to "cannot be made to build **and boot** green".

4. **CONFIRMED — LOW (codex): stale "called before Start()" accessor diagnostics after 07-11.** 07-11 renames `Start` → `Prepare`/`Activate` but preserves the accessor panic guards whose strings say "called before Start()" (e.g. `internal/store/subsystem.go:89`, `internal/plugin/setup/subsystem.go:474+`); 07-09 Task 1 even mints a new one for `TLSConfig()`. After 07-11, these messages reference a method that no longer exists on subsystems. Cosmetic; suggest a small 07-11 completion criterion migrating the message strings (and any exact-string tests) to "before Prepare()".

5. **CONFIRMED — LOW (kimi, unique): 07-03's "PostgresEventStore was deleted… it does not exist" is factually false.** `type PostgresEventStore` is alive (`internal/store/postgres.go:35`, system-info/game-ID persistence; used by `cmd/holomush/cmd_audit.go:192`); what the F1–F7 cutover deleted is the event-append/`Replay` path. The plan's mandated doc rewrite remains correct and its criteria are satisfiable as written — only the justification sentence needs restating so an executor who greps the type doesn't doubt the (correct) correction.

6. **CONFIRMED — LOW (kimi, unique): the round-7 wrapped-vs-raw publisher pin was applied to 07-07 only.** 07-05:461 and 07-06:250 both say "the underlying `eventbus.Publisher`" — the same phrasing 07-07's round-7 correction explicitly disambiguated ("it means the WRAPPED one — never `rawPublisher`"). Verified live: `rawPublisher` at `sub_grpc.go:263`, wrapped `publisher` at `:270` (held by `busEventAppender` at `:282`), and the audit projection fails closed on a missing rendering header (`projection.go:383`) — wiring `rawPublisher` into `presence.NewEmitter`/`sysbroadcast.NewBroadcaster` would silently drop arrive/leave/session_ended/system broadcasts from `events_audit`. Propagate 07-07's pin text into 07-05 Task 3 and 07-06 Task 2.

7. **NOTED — nit (kimi, no action required):** 07-07 lists `test/integration/pluginparity/session_admin_broadcast_test.go` in `files_modified` though 07-06 (an earlier wave) already owns its full retype; harmless under sequential waves.

### Agreed Strengths

- **The rev-14 fixes are accurate and complete.** Both lanes independently re-verified them at source: the two Start-only stubs behind 07-11's atomic-unit squash, the 07-06 `bus.GameID()` baseline of exactly 1 with the capital-B method-value forms invisible to the pattern, the 07-08 test-migration fold-in, and the AdminRekey phrasing note. Codex now rates 07-11 "Ready" and both lanes treat the round-11 blockers as closed.
- **The plans' factual base held again.** Kimi traced 60+ claims (eager starts, `productionSubsystems` 16-param shape, panic guards, MEDIUM-11 data-flow derivation, plugin cleanup hole, compile-assertion census, zero-closure gate feasibility for every gateway-side package) with zero drift; codex's per-plan grounding table independently confirms the same anchors.
- **Cross-plan composition is clean.** Wave-3/wave-4 `files_modified` intersections empty; the 07-05→07-06→07-07 `sub_grpc.go` sequence composes (two-arg `ConfigureSystemBroadcaster` survives `busEventAppender`'s deletion); 07-10's rollback/StopAll defects are real and its fixes correct.
- **No settled disposition was overturned** — twelve rounds in, every retraction and settlement re-checked this round verified accurate against the tree.

### Agreed Concerns

- **07-09 is the remaining defect surface** — both lanes, independently, with different specific defects (codex: Task-2/3 TLS gate ordering + package identity; kimi: Commit A/B bootability). No other plan drew a finding above LOW from either lane.

### Divergent Views

- **Overall verdict.** Codex: NOT READY (HIGH) until the 07-09 boundary is corrected. Kimi: execution-ready (LOW–MEDIUM), "nothing blocks". Resolution: the orchestrator confirmed codex's HIGH — kimi examined the same Task 2/3 mechanism (its verification narration names "the 07-09 A/B commit boundary" twice) and found the commit-split defect inside Task 2, but missed the TLS registration ordering across the Task 2/3 boundary. The verified verdict is codex's: **NOT execution-ready until rev 15 lands the 07-09 fixes.** Seventh consecutive round where codex's depth catches the surviving blocker; second consecutive round where the kimi lane also contributes verified unique findings (this round: one MEDIUM, two LOWs).
- **07-09 severity framing.** Codex HIGH (hard task gates mutually incompatible — unsatisfiable on correct code) vs kimi MEDIUM (advisory commit guidance, loud failure, escape hatch). Both are right about their own findings — they are different defects in the same task. The blocker is codex's; kimi's is real but advisory-level.

### Recommended next step

`/gsd-plan-phase 7 --reviews` for a **rev 15 scoped to 07-09 (+ the three LOW propagation/phrasing fixes)**: (a) Tasks 2–3 atomic-unit merge (the rev-14 07-11 pattern) or full registration hoist into Task 2; (b) `package tlscerts` spelling + alias correction; (c) Commit A/B reassignment (A = 1,5,6,7; B = 2,3,4,8,9) + "build and boot" escape-hatch wording; (d) optional: 07-11 panic-message migration criterion, 07-03 justification restate, 07-05/07-06 wrapPublisher pin propagation. Zero design changes required. Then execute.
