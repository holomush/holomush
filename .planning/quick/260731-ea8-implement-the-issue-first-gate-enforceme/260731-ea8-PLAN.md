---
phase: quick-260731-ea8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Taskfile.yaml
  - scripts/pr-gate-paths.sh
  - scripts/tests/pr-gate-paths.bats
  - .github/workflows/issue-gate.yaml
  - .github/workflows/scripts-tests.yaml
  - .github/PULL_REQUEST_TEMPLATE.md
  - .github/PULL_REQUEST_TEMPLATE/feature.md
  - .github/PULL_REQUEST_TEMPLATE/enhancement.md
  - .github/PULL_REQUEST_TEMPLATE/chore.md
  - .github/PULL_REQUEST_TEMPLATE/fix.md
  - CONTRIBUTING.md
autonomous: true
requirements: [GH-4890]

estimate:
  tokens: 150000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A PR whose diff leaves the exempt path sets AND lacks a linked holomush/holomush issue carrying a gate label gets the `gate-violation` label, an explanatory comment, and a RED `Issue Gate` check."
    - "A PR whose diff is confined to the exempt path sets gets a GREEN `Issue Gate` check from the same job — not a skipped job, not a path-filtered workflow."
    - "A diff touching `.github/CODEOWNERS` or root `CODEOWNERS` is NOT exempt even when every other changed file is under `.github/**`."
    - "A draft PR gets a GREEN check without evaluating the gate."
    - "The matcher fails LOUD (exit 1) when a Taskfile var is missing or the changed-file list is truncated — it never degrades to 'everything exempt'."
    - "No PR is ever closed by automation."
    - "All twelve surviving closure assertions each state the automated consequence alongside the human one."
  artifacts:
    - scripts/pr-gate-paths.sh
    - scripts/tests/pr-gate-paths.bats
    - .github/workflows/issue-gate.yaml
    - "Taskfile.yaml vars.REPO_CONFIG_ONLY_PATHS"
    - "GitHub follow-up issue carrying the copy-pasteable ruleset command"
  key_links:
    - "issue-gate.yaml job `name: Issue Gate` -> the string that becomes the ruleset `11923801` required context (byte match, no workflow prefix)"
    - "scripts/pr-gate-paths.sh -> Taskfile.yaml vars (DOCS_ONLY_PATHS, DEPENDENCY_ONLY_PATHS, REPO_CONFIG_ONLY_PATHS) read at runtime, so there is no second copy to drift"
    - "issue-gate.yaml -> scripts/pr-gate-paths.sh exit-code contract 0=exempt / 10=not-exempt / 1=error"
    - "GraphQL closingIssuesReferences -> repository.nameWithOwner filter -> gate-label check"
---

<objective>
Implement the CI enforcement for the issue-first gate documented in `CONTRIBUTING.md`
and the four typed PR templates. Today the gate is prose only: the `gate-violation`
label exists and nothing applies it. Resolves GH #4890.

Purpose: close the exact defect this repo has catalogued ~17 times — a documented
enforcement that cannot fire. #4890 exists because PR #4889 shipped claims about PR
consequences with no workflow behind them.

Output: a `Issue Gate` check that labels + comments + fails on violation and reports
green on exemption from the same job; a git-pathspec matcher with no hand-written glob
compiler; bats coverage; amended prose; a follow-up issue for the post-merge ruleset step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260731-ea8-implement-the-issue-first-gate-enforceme/260731-ea8-CONTEXT.md
@.planning/quick/260731-ea8-implement-the-issue-first-gate-enforceme/260731-ea8-RESEARCH.md
@CLAUDE.md
@Taskfile.yaml
@scripts/lint-docs-paths-sync.sh
@scripts/tests/lint-docs-paths-sync.bats
@.github/workflows/commit-lint.yaml
@.github/workflows/ci-docs-skip.yaml
@CONTRIBUTING.md
</context>

<ground_truth>
Every fact below was verified in this worktree on 2026-07-31. Do not re-derive.

**Taskfile var block shape.** `DOCS_ONLY_PATHS` is at `Taskfile.yaml:30` and
`DEPENDENCY_ONLY_PATHS` at `Taskfile.yaml:98`; both are `  NAME: |` block scalars whose
entries are indented exactly four spaces. `DEPENDENCY_ONLY_PATHS`'s last entry is
`compose.cluster.yaml` at line 113; line 114 is blank and `tasks:` begins at line 115.
Both sibling vars carry a long explanatory comment block immediately ABOVE the key.

**awk extraction works without yq** (verified against both real vars, exit 0):
`awk -v key=NAME '$0 ~ "^  " key ": \\|$" { inblock=1; next } inblock && /^    #/ { next } inblock && /^    [^ ]/ { sub(/^    /,""); sub(/[[:space:]]+$/,""); print; next } inblock { exit }' Taskfile.yaml`
On a missing key it prints zero lines with exit 0 — that is the silent-failure trap the
script must guard against explicitly. `yq` is NOT available to a new workflow;
`scripts-tests.yaml:62-72` builds it only inside the `bats` job.

**git pathspec, measured on scratch repos with git 2.55.0** — every probe exit 0:
- `git ls-files` works on a staged-but-uncommitted index, so the matcher never needs a clone.
- `':(exclude,glob)**/go.mod'` DOES exclude a root-level `go.mod`. `':(exclude,glob)go.tool*.mod'` excludes `go.tool-lint.mod` and `go.tool.mod`. `':(exclude,glob)**/Dockerfile'` excludes root `Dockerfile`.
- `':(exclude,glob).github/**'` **silently swallows `.github/CODEOWNERS`.** Confirmed: a diff of `.github/CODEOWNERS` + `.github/workflows/ci.yaml` produced an EMPTY exclude-query result. The separate positive query `git ls-files -- ':(glob).github/CODEOWNERS' 'CODEOWNERS'` returned `.github/CODEOWNERS`. This is why the carve-out MUST be a second query — folding it inline fails with no error.
- The bare pathspec `'CODEOWNERS'` matches root `CODEOWNERS` and does NOT match `docs/CODEOWNERS`. Verified both directions.
- Paths containing spaces round-trip correctly with `-c core.quotePath=false`.

**Live GitHub fixtures** (all `gh` calls exit 0 this session):

| PR | changed files | path verdict | `closingIssuesReferences` | gate outcome |
|---|---|---|---|---|
| #4885 | 4 (`.planning/**`, `SECURITY.md`) | EXEMPT | 0 nodes | GREEN |
| #4889 | 13 (all `.github/**` or `*.md`) | EXEMPT | #4888, `approved-chore` | GREEN |
| #4894 | 12, includes `Taskfile.yaml` | **NOT exempt** | #4892, `confirmed-bug` | GREEN (gated correctly) |
| #4874 | 174, incl. 113 source files | **NOT exempt** | #576, labels `type::bug, pr-review, pr-review-finding, pr:139, turn:1, aspect:security, priority::high, severity:important` — **no gate label** | **RED** |

#4874 is the live demonstrated-RED fixture: real historical data the gate must flag.
`gh api repos/holomush/holomush/pulls/4874 --jq .changed_files` returns `174`, and the
paginated `/files` list returns exactly 174 filenames — so a count comparison is a valid
truncation guard.

**Repo is public** (`gh api repos/holomush/holomush --jq .visibility` → `"public"`).
No `CODEOWNERS` file exists at either location today; the carve-out is forward-looking
and can only be covered by a synthetic bats fixture.

**Workflow facts.** `.github/workflows/` contains **no** `pull_request_target` workflow
today — this is the first. `commit-lint.yaml` is the closest precedent: PR-scoped, no
paths filter, job `name: Conventional Commit (PR title)` which is byte-identical to its
ruleset context. `actions/checkout` is pinned repo-wide as
`actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7`; `ci-docs-skip.yaml:69-74`
and `commit-lint.yaml:24-26` both set `persist-credentials: false` (zizmor artipacked).
`task lint:actions` (`Taskfile.yaml:744-747`) runs
`actionlint -config-file .github/actionlint.yaml .github/workflows/*`.
`task test:bats` (`Taskfile.yaml:406-416`) runs `bats scripts/tests/` — a new `.bats`
file there is picked up with no registration, and it does NOT re-enter `task`, so the
`Taskfile.test.yaml` fixture indirection used by `pr-prep-lock.bats` is NOT needed here.

**Two DIFFERENT numbers live in Task 3. Keep them labelled; do not collapse them.**

- **SITES TO AMEND = 12** — closure-consequence sentences that must gain the automated
  consequence.
- **MISSING LITERALS PRE-EDIT = 11** — what the Task 3 acceptance gate reports before the
  edits, because the gate asserts two literals across six FILES (12) minus one already
  present (`CONTRIBUTING.md` carries `gate-violation` at `:215` in its label table).

**The twelve closure-consequence sites**, measured this session
(`rg -n -i 'closed without review|gets closed|are closed|they close it'` over the six
files → 12 lines, exit 0):
`.github/PULL_REQUEST_TEMPLATE.md:36`; `CONTRIBUTING.md:15`, `:64`, `:84`, `:86`, `:142`;
`PULL_REQUEST_TEMPLATE/feature.md:20`, `:22`; `PULL_REQUEST_TEMPLATE/enhancement.md:20`,
`:22`; `PULL_REQUEST_TEMPLATE/chore.md:20`; `PULL_REQUEST_TEMPLATE/fix.md:20`.

Two of these are invisible to the narrower phrasings used in CONTEXT.md and RESEARCH.md,
so they are called out explicitly:
- `CONTRIBUTING.md:86` — `incomplete spec; they close it.` It closes the feature-bar
  paragraph that starts at `:83`. Verified absent from the
  `closed without review|gets closed|are closed` sweep, which is why Task 3's survival
  gate must carry the `they close it` alternative or it cannot detect this site's deletion.
- `CONTRIBUTING.md:142` — "PRs that arrive without a properly-labeled linked issue are
  closed."

**Permissions.** `contents: read` + `pull-requests: write` is sufficient for both
labelling and commenting on a PR; `actions/labeler` documents exactly that pair, and
`buf.yml:22-24` is the in-repo precedent for `pull-requests: write`. `checks: write` is
NOT needed because the job's own conclusion is the check.
</ground_truth>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Path-exemption matcher — Taskfile var, git-pathspec script, bats coverage</name>
  <files>Taskfile.yaml, scripts/pr-gate-paths.sh, scripts/tests/pr-gate-paths.bats</files>

  <read_first>
    `Taskfile.yaml:25-115` (both sibling var blocks and their comment style),
    `scripts/lint-docs-paths-sync.sh:1-35` (SPDX header, `set -euo pipefail`, the
    `REPO_ROOT="${REPO_ROOT:-...}"` test-override idiom, and the yq-null hardening
    comment this script's awk guard is the analogue of),
    `scripts/tests/lint-docs-paths-sync.bats:1-30` (the `mktemp -d` fixture-repo shape
    to copy — tests must never touch real repo files).
  </read_first>

  <behavior>
    `scripts/pr-gate-paths.sh` reads changed file paths from stdin, one per line, and
    exits with a three-valued verdict. Every case below becomes a bats test with a real
    assertion; none may be a skip stub.

    - `site/index.md`, `docs/x.md`, `.planning/a/b.md`, `README.md`, `LICENSE`,
      `.claude/rules/x.md` -> exit 0 (docs-only exempt).
    - `go.mod` (ROOT), `a/b/go.mod`, `go.tool-lint.mod`, `go.tool.sum`, `Dockerfile`
      (ROOT), `web/package.json`, `compose.prod.yaml` -> exit 0. Root `go.mod` and
      `go.tool*.mod` are the two shapes that defeat the alternatives rejected in
      RESEARCH.md; this test is the regression pin for choosing git pathspec.
    - `.github/workflows/ci.yaml`, `.github/ISSUE_TEMPLATE/bug_report.yml`,
      `.github/renovate.json` -> exit 0 (repo-config exempt).
    - `.github/CODEOWNERS` alone -> exit 10.
    - `.github/CODEOWNERS` PLUS `.github/workflows/ci.yaml` -> exit 10. This is the
      silent trap: the exclude query alone returns empty for this input (verified), so a
      matcher that omits the second positive query returns 0 here and nothing errors.
    - Root `CODEOWNERS` alone -> exit 10.
    - `docs/CODEOWNERS` alone -> exit 0. The carve-out is path-exact, not a name match.
    - `Taskfile.yaml` alone -> exit 10. `scripts/foo.sh` alone -> exit 10.
    - `scripts/uv.lock` alone -> exit 0 (listed lockfile shape `**/uv.lock`).
    - `scripts/poetry.lock` alone -> exit 10 (colloquially a lockfile, not a listed shape).
    - `docs/a file with spaces.md` -> exit 0 (whitespace in paths does not break parsing).
    - Empty stdin -> exit 0, and stdout contains the words `empty changed-file list`
      so the branch is deliberate and greppable rather than incidental.
    - A `Taskfile.yaml` fixture missing `REPO_CONFIG_ONLY_PATHS` -> exit **1**, not 0
      and not 10. Assert the exit code is exactly 1. This is the fail-loud guard: awk
      returns zero lines with exit 0 on a missing key, so without this the matcher would
      silently treat a broken config as "no exclusions" and flag every PR, or worse.
    - A mixed diff of `site/index.md` + `internal/foo.go` -> exit 10, and stdout lists
      `internal/foo.go` (the operator needs to know WHICH file broke the exemption).
  </behavior>

  <action>
Write the bats file FIRST and observe it RED, then write the script. The RED observation
is a required artifact of this task, not a formality — record the failing output in the
summary. Sequence: (1) create `scripts/tests/pr-gate-paths.bats` with every case from
`<behavior>`; (2) run `bats scripts/tests/pr-gate-paths.bats` and CONFIRM it exits
non-zero with the script absent (expect status 127 per case); (3) only then write
`scripts/pr-gate-paths.sh`; (4) re-run and confirm green.

Add `vars.REPO_CONFIG_ONLY_PATHS` to `Taskfile.yaml` after `DEPENDENCY_ONLY_PATHS`'s last
entry (`compose.cluster.yaml`, line 113) and before `tasks:` at line 115, as a `  NAME: |`
block scalar with four-space-indented entries, containing exactly one entry: `.github/**`.
Precede it with a comment block in the same documented style as its two siblings, stating:
that it is the authoritative version of the "Repo configuration-only" exemption whose
prose mirrors live in `CONTRIBUTING.md` "### Exempt by file path" and
`.github/PULL_REQUEST_TEMPLATE.md` "## Exemptions"; that its sole machine consumer is
`.github/workflows/issue-gate.yaml` via `scripts/pr-gate-paths.sh`, which reads this var
at runtime so there is no second copy and therefore no sync gate (contrast the
`DOCS_ONLY_PATHS` note at lines 25-29); and that the `CODEOWNERS` carve-out is
deliberately NOT expressed here because git pathspec applies all `:(exclude)` terms last
and ANDs them, so an inline exclusion is impossible and the consumer implements it as a
separate positive query.

`scripts/pr-gate-paths.sh` contract — an executable bash script (`chmod +x`) opening with
the two-line SPDX/copyright header used by `scripts/lint-docs-paths-sync.sh`, then
`set -euo pipefail`:

Input: changed file paths on stdin, one per line, read with `while IFS= read -r`, blank
lines skipped. `REPO_ROOT` overridable via env exactly as `lint-docs-paths-sync.sh:12-14`
does, defaulting to the script's parent directory, and used to locate `Taskfile.yaml`.

Exit codes, documented in a header comment: `0` = EXEMPT, `10` = NOT EXEMPT, `1` = ERROR.
Three values, not two — a caller must never be able to read a script malfunction as an
exemption. Say that in the comment.

Var extraction: an `extract_var` shell function using the awk program given verbatim in
`<ground_truth>`. After each call, if the result is empty, print
`ERROR: vars.<NAME> not found or empty in <path>` to stderr and `exit 1`. Do this for all
three of `DOCS_ONLY_PATHS`, `DEPENDENCY_ONLY_PATHS`, `REPO_CONFIG_ONLY_PATHS`.

Empty-input branch: if zero paths were read, print `exempt: empty changed-file list` and
`exit 0`. Make it an explicit early branch with a comment saying the verdict is
deliberate, not a consequence of an empty match set.

Matching: create a scratch directory with `mktemp -d`, `trap`-remove it on EXIT, `git init
-q` inside it, materialize each input path with `mkdir -p "$(dirname "$p")"` then `: > "$p"`,
and `git add -A --force`. Then compute two results, both with `git -c core.quotePath=false
ls-files`:
  - NONEXEMPT: one `:(exclude,glob)<pattern>` pathspec argument per line across all three
    vars, built by appending to a bash array (never by string-splitting, so patterns and
    paths with spaces survive).
  - OWNERS: a separate invocation with exactly two positive pathspecs,
    `':(glob).github/CODEOWNERS'` and `'CODEOWNERS'`. Add a comment stating this cannot be
    folded into the first invocation and that the failure mode of folding it in is silent.

Verdict: exempt only when NONEXEMPT is empty AND OWNERS is empty. On not-exempt, print
`not exempt: N file(s) outside the exempt path sets` followed by the NONEXEMPT lines and,
when OWNERS is non-empty, a distinct `not exempt: CODEOWNERS is never exempt` line naming
the file; then `exit 10`. On exempt, print `exempt: all N changed file(s) matched an exempt
pattern` and `exit 0`.

Use `git init -q` with no global config dependency — do not run `git commit`, since
`git ls-files` reads the index (verified). Do not copy the repo's `.gitignore` into the
scratch dir; a fresh init has no ignore rules, which is what makes `git add -A --force`
faithful to the input list.

Finally run `task fmt` and commit any files it mutates (it applies the SPDX header via
license-eye and reflows markdown; uncommitted `fmt` output is a standard cause of red CI
here). Commit as `feat(ci): add REPO_CONFIG_ONLY_PATHS and the pr-gate path matcher`.
  </action>

  <verify>
    <automated>
bash -euo pipefail -c '
cd "$(git rev-parse --show-toplevel)"
# expect <exit-code> <label>, fed the file list on stdin. Asserts the EXACT code,
# so a script malfunction (1) can never be mistaken for either verdict (0 or 10).
# MUST be `return 1`, not `exit 1`: expect runs as the tail of a pipeline and so
# executes in a subshell. `return 1` fails the pipeline, which set -o pipefail +
# set -e turn into a script abort. Verified both branches.
expect() {
  local want="$1" label="$2" rc=0
  scripts/pr-gate-paths.sh >/dev/null 2>&1 || rc=$?
  [ "$rc" -eq "$want" ] || { echo "FAIL: $label expected exit $want, got $rc"; return 1; }
}
# 1. bats suite green
bats scripts/tests/pr-gate-paths.bats
# 2. three-valued contract against the REAL Taskfile vars
printf "site/index.md\n"                                  | expect 0  "docs-only"
printf "go.mod\ngo.tool-lint.mod\nDockerfile\n"            | expect 0  "dependency-only incl. root shapes"
printf "Taskfile.yaml\n"                                   | expect 10 "Taskfile.yaml is never exempt"
printf ".github/CODEOWNERS\n.github/workflows/ci.yaml\n"   | expect 10 "CODEOWNERS carve-out"
printf ""                                                  | expect 0  "empty diff"
# 3. LIVE data: real merged PRs, real verdicts (all four measured this session)
gh api repos/holomush/holomush/pulls/4885/files --paginate --jq ".[].filename" | expect 0  "#4885 docs-only"
gh api repos/holomush/holomush/pulls/4889/files --paginate --jq ".[].filename" | expect 0  "#4889 repo-config + md"
gh api repos/holomush/holomush/pulls/4894/files --paginate --jq ".[].filename" | expect 10 "#4894 touches Taskfile.yaml"
gh api repos/holomush/holomush/pulls/4874/files --paginate --jq ".[].filename" | expect 10 "#4874 has 113 source files"
echo "OK"'
    </automated>
    <manual>Paste the RED bats output captured at step (2) of the action into the summary. A gate never observed failing is indistinguishable from one that cannot fail.</manual>
  </verify>

  <done>
`vars.REPO_CONFIG_ONLY_PATHS` exists in `Taskfile.yaml` with a sibling-style comment
block. `scripts/pr-gate-paths.sh` is executable, carries the SPDX header, and returns
0/10/1 per the contract. `scripts/tests/pr-gate-paths.bats` covers every `<behavior>`
case with real assertions and was observed RED before the script existed. The verify
command exits 0, including the four live-PR verdicts. `task fmt` output is committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: The Issue Gate workflow, plus the ordered post-merge ruleset follow-up</name>
  <files>.github/workflows/issue-gate.yaml, .github/workflows/scripts-tests.yaml</files>

  <read_first>
    `.github/workflows/commit-lint.yaml` in full — the PR-scoped, no-paths-filter,
    job-name-equals-ruleset-context precedent, including its comment explaining why a
    workflow introduced by a PR cannot reference itself at `@main`.
    `.github/workflows/ci-docs-skip.yaml:1-30` and `:60-75` — the #4878 postmortem comment
    and the `persist-credentials: false` hardening.
    `.github/workflows/buf.yml:22-24` — the `pull-requests: write` precedent.
  </read_first>

  <action>
Create `.github/workflows/issue-gate.yaml` with the SPDX/copyright header used by every
other workflow, `name: Issue Gate`.

Trigger: `on: pull_request_target:` with `branches: [main]` and
`types: [opened, reopened, synchronize, edited, ready_for_review, converted_to_draft]`.
Add a comment block stating, per item: `edited` is required because a `Closes #N` link is
added or removed by a body edit and without it a fixed PR keeps a stale red check;
`synchronize` is required because the exemption verdict is diff-derived and a new push can
add a non-exempt file; `ready_for_review` and `converted_to_draft` implement the
skip-drafts decision in both directions. Also state, in the same block, that there is
deliberately **NO** `paths` or `paths-ignore` filter, because exemption must mean the
check reports GREEN from this same job — a path-filtered workflow leaves the required
context permanently Pending, which is the #4878 failure mode recorded in
`ci-docs-skip.yaml:12-18`, and that consequently no `ci-docs-skip.yaml` stand-in is
needed or wanted here (a same-named stand-in would create two check runs under one
required context).

`permissions: contents: read` plus `pull-requests: write`, with a comment noting
`checks: write` is deliberately absent because the job's own conclusion IS the check.

A single job with id `issue-gate` and `name: Issue Gate`. Comment above `name:` that this
exact string is what will become the required context on ruleset `11923801`, mirroring
`commit-lint.yaml`'s `Conventional Commit (PR title)`, and that renaming the job silently
detaches the gate from branch protection. `runs-on: ubuntu-latest`.

Add a prominent security comment block at the top of the job: `pull_request_target` runs
the BASE-branch workflow with a write token; this job MUST NOT check out the PR head ref,
MUST NOT execute any script, task, or dependency from the PR head, and MUST NOT source a
PR-authored file. It never needs to — the changed-file list comes from the REST API and
the matcher runs from the base checkout. Note this is the repo's first
`pull_request_target` workflow.

Steps:

1. Draft short-circuit. `if: github.event.pull_request.draft == true`, a run step that
   echoes that drafts are not evaluated and exits 0. Give every subsequent step
   `if: github.event.pull_request.draft != true`. Do NOT use a job-level `if` — that would
   skip the job and leave the required context unreported, reintroducing #4878.

2. `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7` with
   `persist-credentials: false` and a comment that this checks out the BASE ref (the
   `pull_request_target` default) which is trusted `main` code, and that PR head is never
   fetched or checked out.

3. Collect changed files. `env: GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` and
   `PR: ${{ github.event.pull_request.number }}`. Run `gh api
   "repos/${{ github.repository }}/pulls/$PR/files" --paginate --jq '.[].filename'` into a
   temp file under `$RUNNER_TEMP`. Then a truncation guard: read `changed_files` from `gh
   api "repos/${{ github.repository }}/pulls/$PR" --jq '.changed_files'` and compare it to
   `wc -l` of the collected list; on mismatch print the two numbers and `exit 1`. Comment
   that the REST files endpoint caps at 3000 files and that an under-reported list would
   silently make a large PR look exempt, so the mismatch fails the job loudly. (Verified:
   for #4874 both values are 174.)

4. Path verdict. Run `scripts/pr-gate-paths.sh` with the collected list on stdin, capturing
   its exit status without `set -e` aborting the step, then branch on exactly three values:
   `0` writes `exempt=true` to `$GITHUB_OUTPUT`; `10` writes `exempt=false`; anything else
   prints the script output and `exit 1`. Comment that collapsing this to a boolean would
   let a script malfunction read as an exemption.

5. Issue-link verdict, only when `exempt=false`. Run the GraphQL query — this exact shape
   is verified working against #4889 and #4874:
   `gh api graphql -f query='{repository(owner:"...",name:"..."){pullRequest(number:N){closingIssuesReferences(first:20){nodes{number state repository{nameWithOwner} labels(first:50){nodes{name}}}}}}}'`
   with owner/name/number interpolated from `github.repository` and the PR number via env,
   never inlined into the query string from untrusted input. Post-process with `--jq` to
   select nodes where `.repository.nameWithOwner` equals `holomush/holomush` and whose
   `.labels.nodes[].name` includes any of `approved-feature`, `approved-enhancement`,
   `approved-chore`, `confirmed-bug`. Comment that the same-repo filter is a real bypass
   guard, not a hypothetical: without it a cross-repo link to a foreign repo's
   approved-looking issue satisfies the gate. Comment that issue `state` is deliberately
   NOT part of the predicate — a legitimate follow-up PR against an early-closed issue must
   not be blocked (#4894 links a CLOSED #4892 and is correctly gated). Comment that any one
   of the four gate labels suffices and the PR's own type is deliberately not classified:
   the linked issue's approval label already encodes the type, and inferring PR type from
   editable body markers would produce false reds with no compensating safety.

6. Verdict actions. On PASS (exempt OR a valid gated link): remove `gate-violation` if
   present, using `gh pr edit "$PR" --remove-label gate-violation` guarded by a check that
   the label is actually on the PR, and exit 0. On FAIL: `gh pr edit "$PR" --add-label
   gate-violation`; then upsert a single comment — list comments via `gh api
   "repos/.../issues/$PR/comments" --paginate`, look for the HTML marker
   `<!-- holomush:issue-gate -->`, PATCH the existing comment body if found else POST a new
   one. The comment body must contain that marker, state that the PR is flagged and blocked
   from merge and that nothing was closed, name the concrete reason (which files fell
   outside the exempt sets, or that no linked `holomush/holomush` issue carries a gate
   label), link to
   `https://github.com/holomush/holomush/blob/main/CONTRIBUTING.md#exempt-by-file-path`,
   and state the remedy — ask a maintainer to apply the gate label to the linked issue —
   explicitly naming the retroactive-triage path from `CONTRIBUTING.md:201-203` so a
   contributor hitting a pre-policy backlog issue knows what to do. Finally `exit 1`.

   Do NOT append `|| true` to the label or comment calls. A swallowed permission error
   would produce a red check with no label and no explanation, which is the same
   unattributable-failure shape as #4878.

Add `.github/workflows/issue-gate.yaml` to `scripts-tests.yaml`'s `on.pull_request.paths`
list (currently lines 8-16), with a comment that this workflow is the sole consumer of
`scripts/pr-gate-paths.sh`'s exit-code contract, so a change to it must re-run the bats
suite that pins that contract.

Then file the follow-up issue. Use `gh issue create -R holomush/holomush` with labels
`type: chore` and `ci` if it exists, titled for the post-merge ruleset step. The body MUST
carry the ordered steps verbatim, because doing them out of order reproduces #4878: (1)
this PR merges to `main` with the context NOT yet required; (2) observe `Issue Gate` emit a
real non-Pending conclusion on the next unrelated PR; (3) only then add the context. State
the structural reason in the body: GitHub sources a `pull_request_target` workflow from the
repository's default branch, so this workflow cannot run on its own PR and the context
would be required-and-permanently-Pending. Include the copy-pasteable command:
`gh api repos/holomush/holomush/rulesets/11923801 > /tmp/rs.json`, then edit
`/tmp/rs.json` to append `{"context":"Issue Gate","integration_id":15368}` to the
`required_status_checks` parameter array, then
`gh api -X PUT repos/holomush/holomush/rulesets/11923801 --input /tmp/rs.json` — noting the
API replaces the whole rules array so the snapshot-and-edit shape is mandatory, and that
the eight existing contexts as of 2026-07-28 are Build, Lint, Test, CodeRabbit,
Integration Test, E2E Test, Conventional Commit (PR title), Vuln.

Run `task fmt` and commit its mutations. Commit as
`feat(ci): enforce the issue-first gate with a required Issue Gate check`.
  </action>

  <verify>
    <automated>
bash -euo pipefail -c '
cd "$(git rev-parse --show-toplevel)"
W=.github/workflows/issue-gate.yaml
task lint:actions
# Structural assertions use yq (mikefarah v4, already required locally by
# task lint:docs-paths-sync) so they are indentation- and flow-vs-block-agnostic
# and survive task fmt reflowing the file. yq -e exits 1 on a missing key, so
# none of these can silently pass. yq is NOT used by the workflow itself.
# job display name is the future ruleset context — exact match
[ "$(yq -e ".jobs[\"issue-gate\"].name" "$W")" = "Issue Gate" ] || { echo "FAIL: job name must be exactly \"Issue Gate\""; exit 1; }
# no path filter may exist on this workflow (that is the #4878 shape)
for k in paths paths-ignore; do
  [ "$(yq ".on.pull_request_target | has(\"$k\")" "$W")" = "false" ] || { echo "FAIL: enforcement workflow must carry no $k filter"; exit 1; }
done
# required trigger types, compared as a normalized set so both over- and under-listing fail
got=$(yq -e ".on.pull_request_target.types[]" "$W" | LC_ALL=C sort -u | paste -sd, -)
want=$(printf "%s\n" converted_to_draft edited opened ready_for_review reopened synchronize | LC_ALL=C sort -u | paste -sd, -)
[ "$got" = "$want" ] || { echo "FAIL: trigger types [$got] != [$want]"; exit 1; }
# the job must NOT be skipped wholesale on drafts — a skipped job never reports the context
[ "$(yq ".jobs[\"issue-gate\"] | has(\"if\")" "$W")" = "false" ] || { echo "FAIL: job-level if would leave the required context unreported (#4878)"; exit 1; }
# never closes a PR
rg -v "^\s*#" "$W" | rg -q "pr close|--state closed" && { echo "FAIL: workflow must never close a PR"; exit 1; }
# never checks out PR head (dot escaped — an unescaped . would also match pull_request_head)
rg -v "^\s*#" "$W" | rg -q "pull_request\.head" && { echo "FAIL: must not reference PR head ref"; exit 1; }
# verdict actions are not swallowed
rg -v "^\s*#" "$W" | rg -q "add-label.*\|\| true" && { echo "FAIL: label call must not be swallowed"; exit 1; }
# scripts-tests re-runs on this workflow
rg -q "issue-gate.yaml" .github/workflows/scripts-tests.yaml || { echo "FAIL: scripts-tests.yaml must trigger on issue-gate.yaml"; exit 1; }
echo "OK"'
    </automated>
    <manual>
Run the gate's decision logic locally against the four live fixtures and record the
verdicts in the summary. The workflow cannot execute on its own PR (structural — GitHub
sources `pull_request_target` from the default branch), so this dry run is the only
pre-merge observation of the decision available. Required outcomes: #4885 GREEN via
exemption; #4889 GREEN via exemption; #4894 NOT exempt but GREEN because #4892 carries
`confirmed-bug`; **#4874 RED** — not exempt, and its linked issue #576 carries no gate
label. If #4874 does not come out RED, the gate cannot fire and the task is not done.
    </manual>
  </verify>

  <done>
`.github/workflows/issue-gate.yaml` exists, passes `task lint:actions`, has job
`name: Issue Gate`, carries no path filter, never closes a PR, never references the PR
head ref, and its label/comment calls are unguarded so a permission failure is loud.
`scripts-tests.yaml` re-runs on changes to it. A follow-up GitHub issue exists carrying
the three ordered post-merge steps and the copy-pasteable ruleset command. The four live
fixtures produced the required verdicts, including a RED on #4874.
  </done>
</task>

<task type="auto">
  <name>Task 3: State the automated consequence alongside the human one at all twelve sites</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md, .github/PULL_REQUEST_TEMPLATE/feature.md, .github/PULL_REQUEST_TEMPLATE/enhancement.md, .github/PULL_REQUEST_TEMPLATE/chore.md, .github/PULL_REQUEST_TEMPLATE/fix.md, CONTRIBUTING.md</files>

  <read_first>
    `.github/PULL_REQUEST_TEMPLATE.md:33-39`, `PULL_REQUEST_TEMPLATE/feature.md:18-24`,
    `PULL_REQUEST_TEMPLATE/enhancement.md:18-24`, `PULL_REQUEST_TEMPLATE/chore.md:18-29`,
    `PULL_REQUEST_TEMPLATE/fix.md:18-24`, `CONTRIBUTING.md:10-20`, `:60-66`, `:82-88`,
    `:138-150`, `:190-204`.
  </read_first>

  <action>
Amend, do NOT delete or invert. Per CONTEXT.md these twelve sentences are not falsified by
this change: they are passive and describe maintainer practice, which stays true — a
maintainer retains the ability to close a non-compliant PR; the workflow simply does not do
it for them. RESEARCH.md §6f's "actively false" framing is overstated and MUST NOT be acted
on as stated. What they are is *incomplete*: they describe a human consequence and say
nothing about the automated one, so a contributor does not learn that a check goes red.

At each of the twelve sites (SITES TO AMEND = 12; see `<ground_truth>` for the labelled
enumeration), add the automated consequence next to the existing human one.
Both literals `gate-violation` and `Issue Gate` must appear in each of the six files. The
substance to convey, in each file's own voice and register: an automated check named
`Issue Gate` evaluates every non-draft PR; on violation it applies the `gate-violation`
label, posts a comment explaining why, and goes red, blocking merge; **it never closes the
PR** — closing remains a maintainer decision. Do not restore any "auto-closed" or
"closed automatically" wording; the truthful automated phrasing is flagged and blocked
from merge.

Site-by-site:
- `.github/PULL_REQUEST_TEMPLATE.md:34-36` — extend the paragraph ending "is closed without
  review."
- `PULL_REQUEST_TEMPLATE/feature.md:20-23` and `enhancement.md:20-23` — each blockquote
  carries TWO closure claims (the "is closed without review" opener and the "gets closed"
  clause). One added sentence covering the blockquote is sufficient provided both literals
  land in the file.
- `PULL_REQUEST_TEMPLATE/chore.md:20-22` and `fix.md:20-22` — extend the `> **Required.**`
  blockquote.
- `CONTRIBUTING.md:14-16` — the intro paragraph.
- `CONTRIBUTING.md:63-65` — the enhancement bar.
- `CONTRIBUTING.md:83-86` — the feature bar. This region holds **two** sites, and both must
  be covered: `:84` ("is closed without review") and `:86` (`incomplete spec; they close
  it.`). `:86` is absent from CONTEXT.md's table and from RESEARCH.md §6f, and is invisible
  to the narrower closure phrasings — do not let it be swept up incidentally by editing the
  paragraph; confirm the automated consequence reads correctly against BOTH sentences.
- `CONTRIBUTING.md:142-144` — "PRs that arrive without a properly-labeled linked issue are
  closed." Also absent from CONTEXT.md's table; same treatment.
- `CONTRIBUTING.md`, in "### Exempt by file path" after the sentence at `:190-191` ("If your
  diff touches anything outside those paths, it is not exempt...") — add that the exemption
  is evaluated mechanically by `Issue Gate` from the authoritative `Taskfile.yaml` path
  vars, and that an exempt PR gets a GREEN `Issue Gate` check from the same job rather than
  a skipped one.
- `CONTRIBUTING.md`, in the "### Labels used by the gate" table beginning at `:205` — verify
  `gate-violation` has a row describing it as applied by the `Issue Gate` check; add or
  correct the row if it is absent or describes it as manual.

Also correct the `pr-template-exempt` paragraph at `CONTRIBUTING.md:193-196` only if it
currently implies the marker waives the gate; per CONTEXT.md it waives nothing and is
informational. If it already says that, leave it alone.

Before editing, run the acceptance gate below and CONFIRM it fails, printing exactly
**MISSING LITERALS PRE-EDIT = 11**. That is six files x two literals, minus one, because
`CONTRIBUTING.md` already contains `gate-violation` at `:215` in its "### Labels used by
the gate" table. Measured pre-edit this session: the gate exits 1 and lists eleven.

Do not confuse this with SITES TO AMEND = 12. They are different metrics over different
units — 11 counts missing (file, literal) pairs; 12 counts sentences. Both are settled;
neither is a typo for the other. If the gate lists twelve the label-table row was lost;
if it exits 0 the gate is not testing what it claims. This is the task's demonstrated-RED:
a gate never observed failing proves nothing. Record the pre-edit output in the summary.

Run `task fmt` afterwards — it reflows markdown via rumdl/dprint and WILL mutate these
files; commit those edits in the same commit or CI goes red on an otherwise-green PR.
Commit as `docs(contributing): state the Issue Gate consequence alongside the human one`.
  </action>

  <verify>
    <automated>
bash -euo pipefail -c '
cd "$(git rev-parse --show-toplevel)"
missing=""
for f in .github/PULL_REQUEST_TEMPLATE.md \
         .github/PULL_REQUEST_TEMPLATE/feature.md \
         .github/PULL_REQUEST_TEMPLATE/enhancement.md \
         .github/PULL_REQUEST_TEMPLATE/chore.md \
         .github/PULL_REQUEST_TEMPLATE/fix.md \
         CONTRIBUTING.md; do
  rg -q "gate-violation" "$f" || missing="$missing $f:gate-violation"
  rg -q "Issue Gate" "$f"     || missing="$missing $f:Issue-Gate"
done
# METRIC: MISSING LITERALS. Pre-edit this gate reports 11; post-edit it must report 0.
[ -z "$missing" ] || { printf "MISSING:%s\n" "$missing" | tr " " "\n"; exit 1; }
# METRIC: SITES. All 12 human-consequence sentences must SURVIVE — amend, never delete.
# The `they close it` alternative is load-bearing: without it CONTRIBUTING.md:86 is
# invisible to this gate and its deletion would go undetected. Measured pre-edit this
# session with the full alternation: exactly 12 (PULL_REQUEST_TEMPLATE.md 1, feature 2,
# enhancement 2, chore 1, fix 1, CONTRIBUTING 5). The `|| true` keeps a zero-match rg
# from aborting opaquely under pipefail so the count check reports the number instead.
n=$({ rg -c -i "closed without review|gets closed|are closed|they close it" \
        .github/PULL_REQUEST_TEMPLATE.md .github/PULL_REQUEST_TEMPLATE/ CONTRIBUTING.md \
        || true; } | awk -F: "{s+=\$2} END {print s+0}")
[ "$n" -ge 12 ] || { echo "FAIL: only $n of 12 human-consequence sentences remain"; exit 1; }
# CONTRIBUTING.md:86 specifically — the site CONTEXT.md and RESEARCH.md both omit.
rg -q -i "they close it" CONTRIBUTING.md || { echo "FAIL: CONTRIBUTING.md:86 was deleted rather than amended"; exit 1; }
# no auto-close wording may be (re)introduced
rg -n -i "auto-closed|closed automatically|automatically closed" \
   .github/PULL_REQUEST_TEMPLATE.md .github/PULL_REQUEST_TEMPLATE/ CONTRIBUTING.md \
   && { echo "FAIL: auto-close wording present"; exit 1; }
echo "OK"'
    </automated>
    <manual>Paste the pre-edit run of the same gate. It must have exited 1 listing exactly ELEVEN missing literals (MISSING LITERALS PRE-EDIT = 11) — not twelve. Twelve is the SITES metric and does not apply to this gate.</manual>
  </verify>

  <done>
All six files contain both `gate-violation` and `Issue Gate` (MISSING LITERALS now 0; it
was 11 pre-edit). All twelve human-consequence sentences survive, amended and not deleted
(SITES >= 12), including `CONTRIBUTING.md:86` which the dedicated `they close it` gate
pins. No auto-close wording exists anywhere in the templates or CONTRIBUTING.md. The gate
was observed failing before the edits. `task fmt` output is committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author -> `pull_request_target` runner | Untrusted PR content crosses into a job holding a write-scoped `GITHUB_TOKEN`. This is the repo's first such workflow. |
| PR author -> gate predicate | The author controls the PR body, title, and diff — all three are inputs to the verdict. |
| GitHub REST `/pulls/N/files` -> matcher | A truncated or partial list changes the verdict in the permissive direction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ea8-01 | Elevation of Privilege | `issue-gate.yaml` under `pull_request_target` | critical | mitigate | Never check out or execute PR head; base checkout only, `persist-credentials: false`. Enforced by the `pull_request.head` negative gate in Task 2's verify. |
| T-ea8-02 | Spoofing | GraphQL `closingIssuesReferences` | high | mitigate | Filter nodes to `repository.nameWithOwner == "holomush/holomush"`. Without it a cross-repo link to a foreign approved-looking issue satisfies the gate — a real bypass. |
| T-ea8-03 | Tampering | `.github/CODEOWNERS` self-exemption | high | mitigate | Separate positive pathspec query; `.github/**` exclusion alone silently swallows it (measured). Pinned by two bats cases including the mixed-diff case. |
| T-ea8-04 | Tampering | Gate-definition files | high | mitigate | `Taskfile.yaml` and `scripts/**` stay non-exempt (except listed lockfile shapes), so the gate's own definition cannot change without a gated issue. Pinned by bats. |
| T-ea8-05 | Denial of Service | Truncated `/files` response | medium | mitigate | Compare the paginated count against the PR's `changed_files`; mismatch fails the job loudly rather than under-reporting into an exemption. |
| T-ea8-06 | Repudiation | Silent matcher malfunction | high | mitigate | Three-valued exit contract (0/10/1); a missing or empty Taskfile var exits 1, never 0. Pinned by a bats case asserting exit code exactly 1. |
| T-ea8-07 | Information Disclosure | Token in job logs | medium | mitigate | `GH_TOKEN` passed via `env:` only; no token interpolated into a URL or command line. |
| T-ea8-08 | Denial of Service | Ruleset context added before merge | critical | mitigate | Ordered post-merge steps in the follow-up issue; the context is NOT added in this PR. Adding it here reproduces #4878 — permanently Pending with nothing failing. |
| T-ea8-09 | Tampering | Job renamed, silently detaching the gate | medium | accept | Byte-exact `name: Issue Gate` gate in Task 2's verify catches it in this repo; a future rename with a matching ruleset update is legitimate. |
</threat_model>

<verification>
1. `task test:bats` — the full bats suite, including the new file.
2. `task lint:actions` — actionlint over all workflows including the new one.
3. `task lint` — the full umbrella; no new sub-task is wired into it, so no intermediate
   commit leaves it broken and `git bisect` stays usable.
4. `task pr-prep` (fast lane) inline in the parent before push, per the repo's
   landing-the-plane rule. Judge by EXIT CODE and the `▸ pr-prep result:` file, never by
   grepping stdout for a success or error string.
5. The four live-PR dry-run verdicts from Task 2, with #4874 RED.
</verification>

<success_criteria>
- The `Issue Gate` job exists, emits a conclusion on every non-draft PR, and reports green
  from that same job on exemption — no path filter, no stand-in workflow, no skipped job.
- A violating PR gets `gate-violation`, an explanatory comment, and a red check. No PR is
  ever closed by automation.
- `.github/CODEOWNERS` and root `CODEOWNERS` are non-exempt, proven by a bats case whose
  input the exclusion query alone returns empty for.
- The matcher contains no hand-written glob compiler; git's own engine does the matching.
- Every acceptance gate in this plan was observed FAILING against a known-violating input
  before it was trusted.
- The ruleset context is NOT added in this PR. The "required check" half is reported as
  **PENDING — POST-MERGE** and tracked by the follow-up issue. Do not tick that box on the
  strength of the workflow existing.
</success_criteria>

<output>
Create `.planning/quick/260731-ea8-implement-the-issue-first-gate-enforceme/260731-ea8-SUMMARY.md` when done.
The summary MUST include the three captured RED observations (bats pre-script, wording gate
pre-edit, live #4874 verdict) and MUST state the ruleset step as PENDING — POST-MERGE with
the follow-up issue number.
</output>
