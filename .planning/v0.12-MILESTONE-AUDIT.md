---
milestone: v0.12
milestone_name: Foundation Hardening
audited: 2026-07-27T00:00:00Z
audited_commit: 4ed921eed
status: gaps_found
scores:
  requirements: 14/19
  phases: 5/6
  integration: 11/13
  flows: 2/2
gaps:
  requirements:
    - id: "QUAL-02"
      status: "unsatisfied"
      phase: "09"
      claimed_by_plans: ["09-01", "09-08", "09-10", "09-17", "09-19", "09-21"]
      completed_by_plans: ["09-01", "09-08"]
      verification_status: "missing"
      evidence: "Phase 09 has no 09-VERIFICATION.md. REQUIREMENTS.md traceability row reads Pending. Deliberate — residual coverage gap tracked as #4861 (cmd/holomush ~9.91 points under its named 80% floor)."
    - id: "QUAL-03"
      status: "unsatisfied"
      phase: "09"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 09 has no 09-VERIFICATION.md; no plan SUMMARY lists QUAL-03 in requirements-completed. REQUIREMENTS.md row reads Pending. Deliberate deferral."
    - id: "QUAL-05"
      status: "unsatisfied"
      phase: "09"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 09 has no 09-VERIFICATION.md; no plan SUMMARY lists QUAL-05. REQUIREMENTS.md row reads Pending. Deliberate deferral."
    - id: "QUAL-04"
      status: "partial"
      phase: "09"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "IMPLEMENTED but UNATTESTED. Artifacts exist: test/session-matrix.yaml, test/meta/session_matrix_registry_test.go, and five suites under test/integration/session/ (lifecycle_attach, lifecycle_ttl, lifecycle_termination, lifecycle_harness, lifecycle_privacy_floor). REQUIREMENTS.md marks it Complete, but no Phase-09 SUMMARY lists QUAL-04 in requirements-completed and there is no 09-VERIFICATION.md. The ledger's Complete has no upstream source."
    - id: "OPS-01"
      status: "orphaned"
      phase: "pre-Phase 4 quick fix"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "orphaned"
      evidence: "IMPLEMENTED but UNATTESTED. connect.WithReadMaxBytes at internal/web/server.go:85; ReadHeaderTimeout 10s at :147; ReadTimeout 30s at :154. Shipped as a /gsd-quick fix outside the phase loop, so it appears in no phase VERIFICATION.md. REQUIREMENTS.md row still reads Pending / unchecked."
  integration:
    - seam: "P5 outbox -> P7 event collapse (EnvelopeToEvent)"
      severity: "non-critical"
      evidence: "internal/world/outbox/wire.go:170-179"
      affected: ["MODEL-04", "ARCH-04"]
      detail: "CLAUDE.md's event-construction rule is WRONG about this file, and the code is right. See 'Rule defect' below."
    - seam: "OPS-01 ledger vs code"
      severity: "non-critical"
      evidence: "internal/web/server.go:85,147,154 vs .planning/REQUIREMENTS.md:25,82"
      affected: ["OPS-01"]
      detail: "Bookkeeping drift only."
  flows: []
  process:
    - id: "PHASE-09-UNVERIFIED"
      severity: "critical"
      detail: "Phase 09 executed 21 plans and merged as PR #4874 with no gsd-verifier pass. Every other milestone phase has a VERIFICATION.md with status passed."
    - id: "PLAN-09-21-SUMMARY-MISSING"
      severity: "critical"
      detail: "Plan 09-21 demonstrably executed (pushed the branch, opened PR #4874) but wrote no SUMMARY. Plan 09-17 declares 09-21-SUMMARY.md as a consumed input (09-17-PLAN.md:72,84) and 09-17-SUMMARY.md:377 flags the absence as blocking. 09-17 therefore sourced its head-commit reference from a file that does not exist."
nyquist:
  compliant_phases: ["08"]
  partial_phases: []
  not_validated_phases: ["04", "05", "06", "07", "09"]
  missing_phases: []
  overall: "1/6 compliant; 5 NOT-VALIDATED (coverage TODO per #2117, not a compliance failure)"
tech_debt:
  - phase: "05"
    items:
      - "World-change feed has ZERO production consumers: internal/world/setup/relay_subsystem.go:109 wires the reference outbox.Consumer with effect = nil. ADR-intended (ADR:100) but the delivery half is exercised only by tests."
  - phase: "06"
    items:
      - ".codecov.yml after_n_builds: 2 against 3 uploads leaves a documented partial-evaluation window (#4876)."
  - phase: "07"
    items:
      - "productionSubsystems slice order is cosmetic; the real order lives in cmd/holomush/core_topo_order_test.go and is easy to misread as the contract."
  - phase: "09"
    items:
      - "task test:e2e:cover deferred teardown never runs — YAML flow-mapping defer: silently ignored by go-task (deferred-items.md)."
      - "LocationProvider.ResolveResource doc comment at internal/access/policy/attribute/location.go:40-42 still prescribes the empty-string sentinels ADR holomush-ti1b forbids and 09-03 removed."
      - "codecov/project status has never posted in 64 observations (#4875); both halves of the D-04 coverage gate deferred (#4876)."
      - "Flaky: TestRunGatewayWithDeps_{HappyPath,WithObservability} time out under full-suite load (#4873)."
      - "task fmt leaks yamlfmt's #magic___^_^___line sentinel into YAML block scalars (#4864)."
      - "No per-connection detach in the test harness — session-matrix cell reattach-cas.multi-session is uncoverable (#4863)."
      - "admin resetpassword --kick terminates sessions without emitting session_ended (#4862)."
---

# Milestone v0.12 "Foundation Hardening" — Audit Report

**Status:** `gaps_found` · **Audited at:** `4ed921eed` · **Scope:** Phases 4–9

The engineering work of this milestone is sound. Every gap below is an
**attestation** gap or a **documentation** gap — the audit found no broken
code seam and no broken end-to-end flow. The reason the status is
`gaps_found` rather than `tech_debt` is structural: Phase 09 shipped without
a verifier pass, so the three requirements it owns have nothing to check the
ledger against.

## 1. Requirements Coverage (3-source cross-reference)

Sources: `REQUIREMENTS.md` traceability · phase `VERIFICATION.md` · plan
`SUMMARY.md` frontmatter.

| REQ-ID | Phase | Ledger | VERIFICATION | SUMMARY | **Final** |
|---|---|---|---|---|---|
| MODEL-01 | 4 | Complete | passed | listed | **satisfied** |
| OPS-05 | 4 | Complete | passed | listed | **satisfied** |
| MODEL-02 | 5 | Complete | passed | listed | **satisfied** |
| MODEL-03 | 5 | Complete | passed | listed | **satisfied** |
| MODEL-04 | 5 | Complete | passed | listed | **satisfied** |
| OPS-02 | 6 | Complete | passed | listed | **satisfied** |
| OPS-03 | 6 | Complete | passed | listed | **satisfied** |
| OPS-04 | 6 | Complete | passed | listed | **satisfied** |
| QUAL-01 | 6 | Complete | passed | listed | **satisfied** |
| ARCH-03 | 7 | Complete | passed | listed | **satisfied** |
| ARCH-04 | 7 | Complete | passed | listed | **satisfied** |
| ARCH-05 | 7 | Complete | passed | listed | **satisfied** |
| ARCH-01 | 8 | Complete | passed | listed | **satisfied** |
| ARCH-02 | 8 | Complete | passed | listed | **satisfied** |
| QUAL-04 | 9 | Complete | **missing** | **missing** | **partial** |
| QUAL-02 | 9 | Pending | **missing** | 09-01, 09-08 | **unsatisfied** |
| QUAL-03 | 9 | Pending | **missing** | missing | **unsatisfied** |
| QUAL-05 | 9 | Pending | **missing** | missing | **unsatisfied** |
| OPS-01 | quick fix | Pending | **orphaned** | n/a | **orphaned** |

**Score: 14/19 satisfied.**

### The two "implemented but unattested" cases

Both are worth stating precisely, because calling them unsatisfied without
qualification would misdescribe the codebase:

- **OPS-01** — the live unauthenticated-OOM DoS cap **is in the tree**:
  `connect.WithReadMaxBytes` (`internal/web/server.go:85`), a 10s
  `ReadHeaderTimeout` (`:147`) and a 30s `ReadTimeout` (`:154`, which closes
  the slow-body window `ReadHeaderTimeout` alone leaves open). It is
  `orphaned` purely because it shipped as a `/gsd-quick` fix outside the
  phase loop, so no phase VERIFICATION covers it — and nobody ever ticked
  its ledger row.
- **QUAL-04** — the session-lifecycle matrix **is in the tree**:
  `test/session-matrix.yaml`, its bijection guard
  `test/meta/session_matrix_registry_test.go`, and five suites under
  `test/integration/session/`. It is `partial` because the ledger's
  "Complete" has no upstream source: no Phase-09 SUMMARY lists it in
  `requirements-completed`, and there is no `09-VERIFICATION.md`.

Only 4 of Phase 09's 21 SUMMARYs carry a `requirements-completed` field at
all, which is why the frontmatter column is thin for that phase.

## 2. Phase Verification

| Phase | VERIFICATION | Status | Score |
|---|---|---|---|
| 04 World-Model Resilience Investigation & Decision | ✓ | passed | 10/10 |
| 05 World-Model Integrity Fixes (M2/M12) | ✓ | passed | 4/4 |
| 06 Operational Hardening & Assurance Gates | ✓ | passed | 10/10 |
| 07 Event-Model & Bootstrap Decomposition | ✓ | passed | 10/10 |
| 08 God-Object Decomposition | ✓ | passed | 3/3 |
| 09 Test-Quality & Code-Health Sweep | **✗ ABSENT** | — | — |

**Score: 5/6.** Phases 4–8 were each verified by re-running the named suites
in the verifier's own process rather than trusting SUMMARY claims — the
04 and 07 reports both record fresh green runs. Phase 09 received no such pass.

## 3. Cross-Phase Integration — **INTEGRATED**

Thirteen seams checked; **no broken seam, no broken flow**. Two rows carry
documentation drift rather than a wiring defect.

| Seam | Status | Evidence | REQ-IDs |
|---|---|---|---|
| P4 ADR → P5 version guard | ✓ | ADR:40; `internal/world/errors.go:26`; `postgres/exit_repo.go:224,253` | MODEL-01/03 |
| P4 ADR → P5 transactional outbox | ✓ | ADR:41; `migrations/000050_world_outbox.up.sql`; `internal/world/mutator.go:197` | MODEL-04 |
| ADR halt-and-alert + genesis + watermark | ✓ | `outbox/relay.go:87-119,195-229`; `postgres/genesis_store.go` | MODEL-04 |
| P5 outbox → P7 event collapse | ✗ drift | `internal/world/outbox/wire.go:170-179` | MODEL-04, ARCH-04 |
| Outbox ID mint site / dedup contract | ✓ | `wmodel/envelope.go`; `outbox/relay.go:54`; `wire.go:151` | MODEL-04, ARCH-04 |
| P7 orchestrator → P5 OutboxRelay subsystem | ✓ | `cmd/holomush/core.go:488,852,1219-1221` | MODEL-04, ARCH-03 |
| P7 orchestrator → P8 CoreServer decomposition | ✓ | `internal/grpc/server.go:396-399,512,560,579,591-616` | ARCH-01/03 |
| P6 RetentionWorker survives P7/P8/P9 | ✓ | `internal/eventbus/audit/subsystem.go:423-425,462-464` | OPS-02 |
| P6 Vuln gate undisturbed by P9 | ✓ | `.github/workflows/ci.yaml:100-134` | OPS-03 |
| P6 coverage policy ↔ P9 codecov edits | ✓ | `.codecov.yml`; `ci.yaml:179-183,221-225,288-292` | QUAL-01/02/03 |
| P7 gateway boundary after P8/P9 | ✓ **ran** | `go list -deps`: no `core\|world\|store\|access\|plugin\|eventbus` in `internal/web` or `internal/telnet` | ARCH-05 |
| Invariant registry coherence | ✓ **ran** | `task test -- -run 'Invariant\|Provenance\|Binding' ./test/meta/` → 20 PASS | MODEL-03/04, ARCH-05 |
| OPS-01 ledger vs code | ✗ drift | `internal/web/server.go:85,147,154` | OPS-01 |

### Rule defect — `CLAUDE.md` instructs a change that breaks dedup

This is the audit's most consequential finding, and it inverts the usual
direction: **the code is correct and the repo rule is wrong.**

`CLAUDE.md` (§"ULID Generation" → Event construction) states that publication
sites MUST construct via `eventbus.NewEvent(...)` and "never a raw
`eventbus.Event{}` literal, never a hand-stamped `ID`" — then names
`internal/world/outbox/wire.go` (`EnvelopeToEvent`) as one of the four
construction sites.

`EnvelopeToEvent` does exactly what the rule forbids, and must:

- `internal/eventbus/types.go:214-224` — `NewEvent` unconditionally assigns
  `ID: core.NewULID()`. There is no parameter by which a caller supplies an ID.
- `internal/world/outbox/wire.go:170` — `EnvelopeToEvent` sets
  `ID: env.EventID`, preserving the ULID minted once at envelope-creation time
  (`wmodel/envelope.go`) and persisted in the outbox row.
- The relay stamps `Nats-Msg-Id` from `Event.ID` (`outbox/relay.go:54`). Retry
  safety depends on that value being **stable across republishes**.

Rewriting `wire.go` to satisfy the rule would mint a fresh ULID on every relay
retry, so JetStream would never dedup, and the outbox's at-least-once
guarantee would become actually-duplicating on the retry path.

**Mitigating fact:** the trap fails loudly, not silently. `relay_test.go:224`,
`:373` and `:385` each assert `env.EventID == ev.ID`, so the "fix" breaks tests
immediately. The hazard is a confused contributor, not a silent production
regression — which is why this is scored non-critical.

**Recommended fix:** amend `CLAUDE.md` to carve out `EnvelopeToEvent` as the
one construction site that MUST preserve a pre-minted ID, with the dedup
rationale stated inline.

## 4. End-to-End Flows — 2/2 whole

- **Character move → guarded write → same-tx outbox intent → relay → publish**
  — whole. `service.go:990` `checkAccess` (pre-write per ADR:101) → `:994`
  read version → `:1021` `buildMoveIntent` → `:1028`
  `mutator.moveCharacter(..., char.Version)` → `mutator.go:197 mutate()`
  (an envelope-less write is a *type* error, not a runtime check) →
  `wire.go:154` → position-ordered publish.
- **telnet/web connect → session → command dispatch → event feed** — whole.
  Both gateways reach core only via gRPC (confirmed by `go list -deps`);
  `CoreServer` delegates to the Phase-8 handlers (`server.go:512,560`).

**Caveat, stated rather than buried:** both flows were **read-traced, not
executed**. The integration checker ran `go list -deps` and the invariant
meta-tests, but did not run `task test:int`, `task test:e2e`, or `task lint`.
Treat the flow verdicts as structural, not behavioral.

## 5. Nyquist Coverage — 1/6 compliant

| Phase | VALIDATION.md | `status` | `nyquist_compliant` | Classification |
|---|---|---|---|---|
| 04 | exists | `planned` | true | NOT-VALIDATED |
| 05 | exists | `draft` | false | NOT-VALIDATED |
| 06 | exists | `draft` | false | NOT-VALIDATED |
| 07 | exists | `draft` | false | NOT-VALIDATED |
| 08 | exists | `validated` | true | **COMPLIANT** |
| 09 | exists | `draft` | false | NOT-VALIDATED |

Per #2117, `draft` means validate-phase never reconciled the file — so
`nyquist_compliant: false` is **not authoritative** here. These are coverage
TODOs, not compliance failures. Phase 04's `status: planned` is not a
recognized lifecycle value at all and is treated as NOT-VALIDATED.

Running `/gsd-validate-phase 04..07` and `09` would yield the real verdicts
and may close these retroactively without new work.

## 6. Process Gaps (the reason status is `gaps_found`)

1. **Phase 09 was never verified.** Twenty-one plans, merged as PR #4874, no
   `gsd-verifier` pass. This is the root cause of four of the five requirement
   gaps above — QUAL-02/03/04/05 have no phase-level attestation to
   cross-check the ledger against.
2. **`09-21-SUMMARY.md` was never written.** Plan 09-21 executed — it performed
   the phase's only unconditional push and opened PR #4874. Plan 09-17 declares
   that SUMMARY as a consumed input (`09-17-PLAN.md:72,84`) for the PR number
   and head commit, and `09-17-SUMMARY.md:377` records the absence as blocking
   under its own Rule 3. So 09-17 proceeded on a reference it could not read.
   Given that 09-17's whole purpose was observing coverage statuses against a
   known head, and that 09-19 then mutated branch protection from 09-17's
   verdict, this is the milestone's thinnest evidentiary link.

## 7. Ledger Hygiene (mechanical, close at completion)

- `ROADMAP.md` still shows Phase 9 unchecked `[ ]` despite the merge.
- `MILESTONES.md` has no v0.12 entry.
- `REQUIREMENTS.md`: OPS-01 unchecked though implemented; QUAL-04 marked
  Complete without a source; ARCH-01/ARCH-02 rows were already flagged stale
  by Phase 8's own verifier.

## Recommendation

The milestone's substance is done and merged. What is missing is proof, not
work. Two paths:

**A. Close the evidence gap first (recommended).** Run
`/gsd-verify-work 09` to produce `09-VERIFICATION.md`, backfill
`09-21-SUMMARY.md` from PR #4874's actual head commit, then re-run this audit.
That converts QUAL-04 from `partial` to `satisfied` and gives QUAL-02/03/05 a
documented, deliberate deferral rather than an unexplained absence.

**B. Accept and complete.** Run `/gsd-complete-milestone v0.12`, recording the
Phase-09 verification gap and the three Pending QUAL requirements as accepted
debt. Defensible — the integration check found no broken seam — but it archives
a milestone whose final phase has no verifier attestation.

Either way, the `CLAUDE.md` `EnvelopeToEvent` rule defect should be fixed
independently: it is a live trap for the next contributor who reads the rule
and follows it.
